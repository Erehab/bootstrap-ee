/*! Bootstrap EE v2.2.0 (https://github.com/Erehab/bootstrap-ee) */
var sn = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function wn(t) {
  return t && t.__esModule && Object.prototype.hasOwnProperty.call(t, "default") ? t.default : t;
}
var kd = { exports: {} };
/*!
 * jQuery JavaScript Library v4.0.0
 * https://jquery.com/
 *
 * Copyright OpenJS Foundation and other contributors
 * Released under the MIT license
 * https://jquery.com/license/
 *
 * Date: 2026-01-18T00:20Z
 */
(function(t) {
  (function(n, e) {
    t.exports = e(n, !0);
  })(typeof window < "u" ? window : sn, function(n, e) {
    if (!n.document)
      throw new Error("jQuery requires a window with a document");
    var r = [], i = Object.getPrototypeOf, s = r.slice, a = r.flat ? function(d) {
      return r.flat.call(d);
    } : function(d) {
      return r.concat.apply([], d);
    }, o = r.push, l = r.indexOf, c = {}, f = c.toString, g = c.hasOwnProperty, b = g.toString, w = b.call(Object), C = {};
    function m(d) {
      return d == null ? d + "" : typeof d == "object" ? c[f.call(d)] || "object" : typeof d;
    }
    function h(d) {
      return d != null && d === d.window;
    }
    function u(d) {
      var _ = !!d && d.length, x = m(d);
      return typeof d == "function" || h(d) ? !1 : x === "array" || _ === 0 || typeof _ == "number" && _ > 0 && _ - 1 in d;
    }
    var p = n.document, v = {
      type: !0,
      src: !0,
      nonce: !0,
      noModule: !0
    };
    function S(d, _, x) {
      x = x || p;
      var D, O = x.createElement("script");
      O.text = d;
      for (D in v)
        _ && _[D] && (O[D] = _[D]);
      x.head.appendChild(O).parentNode && O.parentNode.removeChild(O);
    }
    var T = "4.0.0", A = /HTML$/i, y = function(d, _) {
      return new y.fn.init(d, _);
    };
    y.fn = y.prototype = {
      // The current version of jQuery being used
      jquery: T,
      constructor: y,
      // The default length of a jQuery object is 0
      length: 0,
      toArray: function() {
        return s.call(this);
      },
      // Get the Nth element in the matched element set OR
      // Get the whole matched element set as a clean array
      get: function(d) {
        return d == null ? s.call(this) : d < 0 ? this[d + this.length] : this[d];
      },
      // Take an array of elements and push it onto the stack
      // (returning the new matched element set)
      pushStack: function(d) {
        var _ = y.merge(this.constructor(), d);
        return _.prevObject = this, _;
      },
      // Execute a callback for every element in the matched set.
      each: function(d) {
        return y.each(this, d);
      },
      map: function(d) {
        return this.pushStack(y.map(this, function(_, x) {
          return d.call(_, x, _);
        }));
      },
      slice: function() {
        return this.pushStack(s.apply(this, arguments));
      },
      first: function() {
        return this.eq(0);
      },
      last: function() {
        return this.eq(-1);
      },
      even: function() {
        return this.pushStack(y.grep(this, function(d, _) {
          return (_ + 1) % 2;
        }));
      },
      odd: function() {
        return this.pushStack(y.grep(this, function(d, _) {
          return _ % 2;
        }));
      },
      eq: function(d) {
        var _ = this.length, x = +d + (d < 0 ? _ : 0);
        return this.pushStack(x >= 0 && x < _ ? [this[x]] : []);
      },
      end: function() {
        return this.prevObject || this.constructor();
      }
    }, y.extend = y.fn.extend = function() {
      var d, _, x, D, O, N, B = arguments[0] || {}, K = 1, U = arguments.length, Z = !1;
      for (typeof B == "boolean" && (Z = B, B = arguments[K] || {}, K++), typeof B != "object" && typeof B != "function" && (B = {}), K === U && (B = this, K--); K < U; K++)
        if ((d = arguments[K]) != null)
          for (_ in d)
            D = d[_], !(_ === "__proto__" || B === D) && (Z && D && (y.isPlainObject(D) || (O = Array.isArray(D))) ? (x = B[_], O && !Array.isArray(x) ? N = [] : !O && !y.isPlainObject(x) ? N = {} : N = x, O = !1, B[_] = y.extend(Z, N, D)) : D !== void 0 && (B[_] = D));
      return B;
    }, y.extend({
      // Unique for each copy of jQuery on the page
      expando: "jQuery" + (T + Math.random()).replace(/\D/g, ""),
      // Assume jQuery is ready without the ready module
      isReady: !0,
      error: function(d) {
        throw new Error(d);
      },
      noop: function() {
      },
      isPlainObject: function(d) {
        var _, x;
        return !d || f.call(d) !== "[object Object]" ? !1 : (_ = i(d), _ ? (x = g.call(_, "constructor") && _.constructor, typeof x == "function" && b.call(x) === w) : !0);
      },
      isEmptyObject: function(d) {
        var _;
        for (_ in d)
          return !1;
        return !0;
      },
      // Evaluates a script in a provided context; falls back to the global one
      // if not specified.
      globalEval: function(d, _, x) {
        S(d, { nonce: _ && _.nonce }, x);
      },
      each: function(d, _) {
        var x, D = 0;
        if (u(d))
          for (x = d.length; D < x && _.call(d[D], D, d[D]) !== !1; D++)
            ;
        else
          for (D in d)
            if (_.call(d[D], D, d[D]) === !1)
              break;
        return d;
      },
      // Retrieve the text value of an array of DOM nodes
      text: function(d) {
        var _, x = "", D = 0, O = d.nodeType;
        if (!O)
          for (; _ = d[D++]; )
            x += y.text(_);
        return O === 1 || O === 11 ? d.textContent : O === 9 ? d.documentElement.textContent : O === 3 || O === 4 ? d.nodeValue : x;
      },
      // results is for internal usage only
      makeArray: function(d, _) {
        var x = _ || [];
        return d != null && (u(Object(d)) ? y.merge(
          x,
          typeof d == "string" ? [d] : d
        ) : o.call(x, d)), x;
      },
      inArray: function(d, _, x) {
        return _ == null ? -1 : l.call(_, d, x);
      },
      isXMLDoc: function(d) {
        var _ = d && d.namespaceURI, x = d && (d.ownerDocument || d).documentElement;
        return !A.test(_ || x && x.nodeName || "HTML");
      },
      // Note: an element does not contain itself
      contains: function(d, _) {
        var x = _ && _.parentNode;
        return d === x || !!(x && x.nodeType === 1 && // Support: IE 9 - 11+
        // IE doesn't have `contains` on SVG.
        (d.contains ? d.contains(x) : d.compareDocumentPosition && d.compareDocumentPosition(x) & 16));
      },
      merge: function(d, _) {
        for (var x = +_.length, D = 0, O = d.length; D < x; D++)
          d[O++] = _[D];
        return d.length = O, d;
      },
      grep: function(d, _, x) {
        for (var D, O = [], N = 0, B = d.length, K = !x; N < B; N++)
          D = !_(d[N], N), D !== K && O.push(d[N]);
        return O;
      },
      // arg is for internal usage only
      map: function(d, _, x) {
        var D, O, N = 0, B = [];
        if (u(d))
          for (D = d.length; N < D; N++)
            O = _(d[N], N, x), O != null && B.push(O);
        else
          for (N in d)
            O = _(d[N], N, x), O != null && B.push(O);
        return a(B);
      },
      // A global GUID counter for objects
      guid: 1,
      // jQuery.support is not used in Core but other projects attach their
      // properties to it so it needs to exist.
      support: C
    }), typeof Symbol == "function" && (y.fn[Symbol.iterator] = r[Symbol.iterator]), y.each(
      "Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "),
      function(d, _) {
        c["[object " + _ + "]"] = _.toLowerCase();
      }
    );
    function I(d, _) {
      return d.nodeName && d.nodeName.toLowerCase() === _.toLowerCase();
    }
    var $ = r.pop, V = "[\\x20\\t\\r\\n\\f]", z = p.documentMode, F = z && new RegExp(
      // Support: IE 9 - 11+
      // IE's :disabled selector does not pick up the children of disabled fieldsets
      ":enabled|:disabled|\\[" + V + "*name" + V + "*=" + V + `*(?:''|"")`
    ), P = new RegExp(
      "^" + V + "+|((?:^|[^\\\\])(?:\\\\.)*)" + V + "+$",
      "g"
    ), k = "(?:\\\\[\\da-fA-F]{1,6}" + V + "?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+", R = new RegExp("^" + V + "*([>+~]|" + V + ")" + V + "*"), L = new RegExp(V + "|>"), E = /[+~]/, M = p.documentElement, W = M.matches || M.msMatchesSelector;
    function Y() {
      var d = [];
      function _(x, D) {
        return d.push(x + " ") > y.expr.cacheLength && delete _[d.shift()], _[x + " "] = D;
      }
      return _;
    }
    function J(d) {
      return d && typeof d.getElementsByTagName < "u" && d;
    }
    var X = "\\[" + V + "*(" + k + ")(?:" + V + // Operator (capture 2)
    "*([*^$|!~]?=)" + V + // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
    `*(?:'((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)"|(` + k + "))|)" + V + "*\\]", Q = ":(" + k + `)(?:\\((('((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|` + X + ")*)|.*)\\)|)", ee = {
      ID: new RegExp("^#(" + k + ")"),
      CLASS: new RegExp("^\\.(" + k + ")"),
      TAG: new RegExp("^(" + k + "|[*])"),
      ATTR: new RegExp("^" + X),
      PSEUDO: new RegExp("^" + Q),
      CHILD: new RegExp(
        "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + V + "*(even|odd|(([+-]|)(\\d*)n|)" + V + "*(?:([+-]|)" + V + "*(\\d+)|))" + V + "*\\)|)",
        "i"
      )
    }, ie = new RegExp(Q), oe = new RegExp("\\\\[\\da-fA-F]{1,6}" + V + "?|\\\\([^\\r\\n\\f])", "g"), ce = function(d, _) {
      var x = "0x" + d.slice(1) - 65536;
      return _ || (x < 0 ? String.fromCharCode(x + 65536) : String.fromCharCode(x >> 10 | 55296, x & 1023 | 56320));
    };
    function he(d) {
      return d.replace(oe, ce);
    }
    function we(d) {
      y.error("Syntax error, unrecognized expression: " + d);
    }
    var Oe = new RegExp("^" + V + "*," + V + "*"), Ve = Y();
    function Ie(d, _) {
      var x, D, O, N, B, K, U, Z = Ve[d + " "];
      if (Z)
        return _ ? 0 : Z.slice(0);
      for (B = d, K = [], U = y.expr.preFilter; B; ) {
        (!x || (D = Oe.exec(B))) && (D && (B = B.slice(D[0].length) || B), K.push(O = [])), x = !1, (D = R.exec(B)) && (x = D.shift(), O.push({
          value: x,
          // Cast descendant combinators to space
          type: D[0].replace(P, " ")
        }), B = B.slice(x.length));
        for (N in ee)
          (D = y.expr.match[N].exec(B)) && (!U[N] || (D = U[N](D))) && (x = D.shift(), O.push({
            value: x,
            type: N,
            matches: D
          }), B = B.slice(x.length));
        if (!x)
          break;
      }
      return _ ? B.length : B ? we(d) : (
        // Cache the tokens
        Ve(d, K).slice(0)
      );
    }
    var Ze = {
      ATTR: function(d) {
        return d[1] = he(d[1]), d[3] = he(d[3] || d[4] || d[5] || ""), d[2] === "~=" && (d[3] = " " + d[3] + " "), d.slice(0, 4);
      },
      CHILD: function(d) {
        return d[1] = d[1].toLowerCase(), d[1].slice(0, 3) === "nth" ? (d[3] || we(d[0]), d[4] = +(d[4] ? d[5] + (d[6] || 1) : 2 * (d[3] === "even" || d[3] === "odd")), d[5] = +(d[7] + d[8] || d[3] === "odd")) : d[3] && we(d[0]), d;
      },
      PSEUDO: function(d) {
        var _, x = !d[6] && d[2];
        return ee.CHILD.test(d[0]) ? null : (d[3] ? d[2] = d[4] || d[5] || "" : x && ie.test(x) && // Get excess from tokenize (recursively)
        (_ = Ie(x, !0)) && // advance to the next closing parenthesis
        (_ = x.indexOf(")", x.length - _) - x.length) && (d[0] = d[0].slice(0, _), d[2] = x.slice(0, _)), d.slice(0, 3));
      }
    };
    function et(d) {
      for (var _ = 0, x = d.length, D = ""; _ < x; _++)
        D += d[_].value;
      return D;
    }
    function He(d, _, x, D, O, N, B) {
      var K = 0, U = d.length, Z = x == null;
      if (m(x) === "object") {
        O = !0;
        for (K in x)
          He(d, _, K, x[K], !0, N, B);
      } else if (D !== void 0 && (O = !0, typeof D != "function" && (B = !0), Z && (B ? (_.call(d, D), _ = null) : (Z = _, _ = function(re, le, ne) {
        return Z.call(y(re), ne);
      })), _))
        for (; K < U; K++)
          _(
            d[K],
            x,
            B ? D : D.call(d[K], K, _(d[K], x))
          );
      return O ? d : Z ? _.call(d) : U ? _(d[0], x) : N;
    }
    var Ke = /[^\x20\t\r\n\f]+/g;
    y.fn.extend({
      attr: function(d, _) {
        return He(this, y.attr, d, _, arguments.length > 1);
      },
      removeAttr: function(d) {
        return this.each(function() {
          y.removeAttr(this, d);
        });
      }
    }), y.extend({
      attr: function(d, _, x) {
        var D, O, N = d.nodeType;
        if (!(N === 3 || N === 8 || N === 2)) {
          if (typeof d.getAttribute > "u")
            return y.prop(d, _, x);
          if ((N !== 1 || !y.isXMLDoc(d)) && (O = y.attrHooks[_.toLowerCase()]), x !== void 0) {
            if (x === null || // For compat with previous handling of boolean attributes,
            // remove when `false` passed. For ARIA attributes -
            // many of which recognize a `"false"` value - continue to
            // set the `"false"` value as jQuery <4 did.
            x === !1 && _.toLowerCase().indexOf("aria-") !== 0) {
              y.removeAttr(d, _);
              return;
            }
            return O && "set" in O && (D = O.set(d, x, _)) !== void 0 ? D : (d.setAttribute(_, x), x);
          }
          return O && "get" in O && (D = O.get(d, _)) !== null ? D : (D = d.getAttribute(_), D ?? void 0);
        }
      },
      attrHooks: {},
      removeAttr: function(d, _) {
        var x, D = 0, O = _ && _.match(Ke);
        if (O && d.nodeType === 1)
          for (; x = O[D++]; )
            d.removeAttribute(x);
      }
    }), z && (y.attrHooks.type = {
      set: function(d, _) {
        if (_ === "radio" && I(d, "input")) {
          var x = d.value;
          return d.setAttribute("type", _), x && (d.value = x), _;
        }
      }
    });
    var Ct = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g;
    function yt(d, _) {
      return _ ? d === "\0" ? "�" : d.slice(0, -1) + "\\" + d.charCodeAt(d.length - 1).toString(16) + " " : "\\" + d;
    }
    y.escapeSelector = function(d) {
      return (d + "").replace(Ct, yt);
    };
    var wt = r.sort, rt = r.splice, mt;
    function St(d, _) {
      if (d === _)
        return mt = !0, 0;
      var x = !d.compareDocumentPosition - !_.compareDocumentPosition;
      return x || (x = (d.ownerDocument || d) == (_.ownerDocument || _) ? d.compareDocumentPosition(_) : (
        // Otherwise we know they are disconnected
        1
      ), x & 1 ? d == p || d.ownerDocument == p && y.contains(p, d) ? -1 : _ == p || _.ownerDocument == p && y.contains(p, _) ? 1 : 0 : x & 4 ? -1 : 1);
    }
    y.uniqueSort = function(d) {
      var _, x = [], D = 0, O = 0;
      if (mt = !1, wt.call(d, St), mt) {
        for (; _ = d[O++]; )
          _ === d[O] && (D = x.push(O));
        for (; D--; )
          rt.call(d, x[D], 1);
      }
      return d;
    }, y.fn.uniqueSort = function() {
      return this.pushStack(y.uniqueSort(s.apply(this)));
    };
    var ut, Ot, dt, mr, It, Ht = 0, Ns = 0, Rs = Y(), Ls = Y(), vr = Y(), so = new RegExp(V + "+", "g"), ao = new RegExp("^" + k + "$"), tt = y.extend({
      // For use in libraries implementing .is()
      // We use this for POS matching in `select`
      needsContext: new RegExp("^" + V + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + V + "*((?:-\\d)?\\d*)" + V + "*\\)|)(?=[^-]|$)", "i")
    }, ee), $i = /^(?:input|select|textarea|button)$/i, Vi = /^h\d$/i, oo = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, qr = function() {
      ae();
    }, Wi = me(
      function(d) {
        return d.disabled === !0 && I(d, "fieldset");
      },
      { dir: "parentNode", next: "legend" }
    );
    function ft(d, _, x, D) {
      var O, N, B, K, U, Z, re, le = _ && _.ownerDocument, ne = _ ? _.nodeType : 9;
      if (x = x || [], typeof d != "string" || !d || ne !== 1 && ne !== 9 && ne !== 11)
        return x;
      if (!D && (ae(_), _ = _ || dt, It)) {
        if (ne !== 11 && (U = oo.exec(d)))
          if (O = U[1]) {
            if (ne === 9)
              return (B = _.getElementById(O)) && o.call(x, B), x;
            if (le && (B = le.getElementById(O)) && y.contains(_, B))
              return o.call(x, B), x;
          } else {
            if (U[2])
              return o.apply(x, _.getElementsByTagName(d)), x;
            if ((O = U[3]) && _.getElementsByClassName)
              return o.apply(x, _.getElementsByClassName(O)), x;
          }
        if (!vr[d + " "] && (!F || !F.test(d))) {
          if (re = d, le = _, ne === 1 && (L.test(d) || R.test(d))) {
            for (le = E.test(d) && J(_.parentNode) || _, (le != _ || z) && ((K = _.getAttribute("id")) ? K = y.escapeSelector(K) : _.setAttribute("id", K = y.expando)), Z = Ie(d), N = Z.length; N--; )
              Z[N] = (K ? "#" + K : ":scope") + " " + et(Z[N]);
            re = Z.join(",");
          }
          try {
            return o.apply(
              x,
              le.querySelectorAll(re)
            ), x;
          } catch {
            vr(d, !0);
          } finally {
            K === y.expando && _.removeAttribute("id");
          }
        }
      }
      return Tt(d.replace(P, "$1"), _, x, D);
    }
    function cn(d) {
      return d[y.expando] = !0, d;
    }
    function lo(d) {
      return function(_) {
        return I(_, "input") && _.type === d;
      };
    }
    function j(d) {
      return function(_) {
        return (I(_, "input") || I(_, "button")) && _.type === d;
      };
    }
    function G(d) {
      return function(_) {
        return "form" in _ ? _.parentNode && _.disabled === !1 ? "label" in _ ? "label" in _.parentNode ? _.parentNode.disabled === d : _.disabled === d : _.isDisabled === d || // Where there is no isDisabled, check manually
        _.isDisabled !== !d && Wi(_) === d : _.disabled === d : "label" in _ ? _.disabled === d : !1;
      };
    }
    function te(d) {
      return cn(function(_) {
        return _ = +_, cn(function(x, D) {
          for (var O, N = d([], x.length, _), B = N.length; B--; )
            x[O = N[B]] && (x[O] = !(D[O] = x[O]));
        });
      });
    }
    function ae(d) {
      var _, x = d ? d.ownerDocument || d : p;
      x == dt || x.nodeType !== 9 || (dt = x, mr = dt.documentElement, It = !y.isXMLDoc(dt), z && p != dt && (_ = dt.defaultView) && _.top !== _ && _.addEventListener("unload", qr));
    }
    ft.matches = function(d, _) {
      return ft(d, null, null, _);
    }, ft.matchesSelector = function(d, _) {
      if (ae(d), It && !vr[_ + " "] && (!F || !F.test(_)))
        try {
          return W.call(d, _);
        } catch {
          vr(_, !0);
        }
      return ft(_, dt, null, [d]).length > 0;
    }, y.expr = {
      // Can be adjusted by the user
      cacheLength: 50,
      createPseudo: cn,
      match: tt,
      find: {
        ID: function(d, _) {
          if (typeof _.getElementById < "u" && It) {
            var x = _.getElementById(d);
            return x ? [x] : [];
          }
        },
        TAG: function(d, _) {
          return typeof _.getElementsByTagName < "u" ? _.getElementsByTagName(d) : _.querySelectorAll(d);
        },
        CLASS: function(d, _) {
          if (typeof _.getElementsByClassName < "u" && It)
            return _.getElementsByClassName(d);
        }
      },
      relative: {
        ">": { dir: "parentNode", first: !0 },
        " ": { dir: "parentNode" },
        "+": { dir: "previousSibling", first: !0 },
        "~": { dir: "previousSibling" }
      },
      preFilter: Ze,
      filter: {
        ID: function(d) {
          var _ = he(d);
          return function(x) {
            return x.getAttribute("id") === _;
          };
        },
        TAG: function(d) {
          var _ = he(d).toLowerCase();
          return d === "*" ? function() {
            return !0;
          } : function(x) {
            return I(x, _);
          };
        },
        CLASS: function(d) {
          var _ = Rs[d + " "];
          return _ || (_ = new RegExp("(^|" + V + ")" + d + "(" + V + "|$)")) && Rs(d, function(x) {
            return _.test(
              typeof x.className == "string" && x.className || typeof x.getAttribute < "u" && x.getAttribute("class") || ""
            );
          });
        },
        ATTR: function(d, _, x) {
          return function(D) {
            var O = y.attr(D, d);
            return O == null ? _ === "!=" : _ ? (O += "", _ === "=" ? O === x : _ === "!=" ? O !== x : _ === "^=" ? x && O.indexOf(x) === 0 : _ === "*=" ? x && O.indexOf(x) > -1 : _ === "$=" ? x && O.slice(-x.length) === x : _ === "~=" ? (" " + O.replace(so, " ") + " ").indexOf(x) > -1 : _ === "|=" ? O === x || O.slice(0, x.length + 1) === x + "-" : !1) : !0;
          };
        },
        CHILD: function(d, _, x, D, O) {
          var N = d.slice(0, 3) !== "nth", B = d.slice(-4) !== "last", K = _ === "of-type";
          return D === 1 && O === 0 ? (
            // Shortcut for :nth-*(n)
            function(U) {
              return !!U.parentNode;
            }
          ) : function(U, Z, re) {
            var le, ne, se, ve, Ne, Ee = N !== B ? "nextSibling" : "previousSibling", it = U.parentNode, nt = K && U.nodeName.toLowerCase(), jt = !re && !K, Dt = !1;
            if (it) {
              if (N) {
                for (; Ee; ) {
                  for (se = U; se = se[Ee]; )
                    if (K ? I(se, nt) : se.nodeType === 1)
                      return !1;
                  Ne = Ee = d === "only" && !Ne && "nextSibling";
                }
                return !0;
              }
              if (Ne = [B ? it.firstChild : it.lastChild], B && jt) {
                for (ne = it[y.expando] || (it[y.expando] = {}), le = ne[d] || [], ve = le[0] === Ht && le[1], Dt = ve && le[2], se = ve && it.childNodes[ve]; se = ++ve && se && se[Ee] || // Fallback to seeking `elem` from the start
                (Dt = ve = 0) || Ne.pop(); )
                  if (se.nodeType === 1 && ++Dt && se === U) {
                    ne[d] = [Ht, ve, Dt];
                    break;
                  }
              } else if (jt && (ne = U[y.expando] || (U[y.expando] = {}), le = ne[d] || [], ve = le[0] === Ht && le[1], Dt = ve), Dt === !1)
                for (; (se = ++ve && se && se[Ee] || (Dt = ve = 0) || Ne.pop()) && !((K ? I(se, nt) : se.nodeType === 1) && ++Dt && (jt && (ne = se[y.expando] || (se[y.expando] = {}), ne[d] = [Ht, Dt]), se === U)); )
                  ;
              return Dt -= O, Dt === D || Dt % D === 0 && Dt / D >= 0;
            }
          };
        },
        PSEUDO: function(d, _) {
          var x = y.expr.pseudos[d] || y.expr.setFilters[d.toLowerCase()] || we("unsupported pseudo: " + d);
          return x[y.expando] ? x(_) : x;
        }
      },
      pseudos: {
        // Potentially complex pseudos
        not: cn(function(d) {
          var _ = [], x = [], D = $t(d.replace(P, "$1"));
          return D[y.expando] ? cn(function(O, N, B, K) {
            for (var U, Z = D(O, null, K, []), re = O.length; re--; )
              (U = Z[re]) && (O[re] = !(N[re] = U));
          }) : function(O, N, B) {
            return _[0] = O, D(_, null, B, x), _[0] = null, !x.pop();
          };
        }),
        has: cn(function(d) {
          return function(_) {
            return ft(d, _).length > 0;
          };
        }),
        contains: cn(function(d) {
          return d = he(d), function(_) {
            return (_.textContent || y.text(_)).indexOf(d) > -1;
          };
        }),
        // "Whether an element is represented by a :lang() selector
        // is based solely on the element's language value
        // being equal to the identifier C,
        // or beginning with the identifier C immediately followed by "-".
        // The matching of C against the element's language value is performed case-insensitively.
        // The identifier C does not have to be a valid language name."
        // https://www.w3.org/TR/selectors/#lang-pseudo
        lang: cn(function(d) {
          return ao.test(d || "") || we("unsupported lang: " + d), d = he(d).toLowerCase(), function(_) {
            var x;
            do
              if (x = It ? _.lang : _.getAttribute("xml:lang") || _.getAttribute("lang"))
                return x = x.toLowerCase(), x === d || x.indexOf(d + "-") === 0;
            while ((_ = _.parentNode) && _.nodeType === 1);
            return !1;
          };
        }),
        // Miscellaneous
        target: function(d) {
          var _ = n.location && n.location.hash;
          return _ && _.slice(1) === d.id;
        },
        root: function(d) {
          return d === mr;
        },
        focus: function(d) {
          return d === dt.activeElement && dt.hasFocus() && !!(d.type || d.href || ~d.tabIndex);
        },
        // Boolean properties
        enabled: G(!1),
        disabled: G(!0),
        checked: function(d) {
          return I(d, "input") && !!d.checked || I(d, "option") && !!d.selected;
        },
        selected: function(d) {
          return z && d.parentNode && d.parentNode.selectedIndex, d.selected === !0;
        },
        // Contents
        empty: function(d) {
          for (d = d.firstChild; d; d = d.nextSibling)
            if (d.nodeType < 6)
              return !1;
          return !0;
        },
        parent: function(d) {
          return !y.expr.pseudos.empty(d);
        },
        // Element/input types
        header: function(d) {
          return Vi.test(d.nodeName);
        },
        input: function(d) {
          return $i.test(d.nodeName);
        },
        button: function(d) {
          return I(d, "input") && d.type === "button" || I(d, "button");
        },
        text: function(d) {
          return I(d, "input") && d.type === "text";
        },
        // Position-in-collection
        first: te(function() {
          return [0];
        }),
        last: te(function(d, _) {
          return [_ - 1];
        }),
        eq: te(function(d, _, x) {
          return [x < 0 ? x + _ : x];
        }),
        even: te(function(d, _) {
          for (var x = 0; x < _; x += 2)
            d.push(x);
          return d;
        }),
        odd: te(function(d, _) {
          for (var x = 1; x < _; x += 2)
            d.push(x);
          return d;
        }),
        lt: te(function(d, _, x) {
          var D;
          for (x < 0 ? D = x + _ : x > _ ? D = _ : D = x; --D >= 0; )
            d.push(D);
          return d;
        }),
        gt: te(function(d, _, x) {
          for (var D = x < 0 ? x + _ : x; ++D < _; )
            d.push(D);
          return d;
        })
      }
    }, y.expr.pseudos.nth = y.expr.pseudos.eq;
    for (ut in { radio: !0, checkbox: !0, file: !0, password: !0, image: !0 })
      y.expr.pseudos[ut] = lo(ut);
    for (ut in { submit: !0, reset: !0 })
      y.expr.pseudos[ut] = j(ut);
    function pe() {
    }
    pe.prototype = y.expr.pseudos, y.expr.setFilters = new pe();
    function me(d, _, x) {
      var D = _.dir, O = _.next, N = O || D, B = x && N === "parentNode", K = Ns++;
      return _.first ? (
        // Check against closest ancestor/preceding element
        function(U, Z, re) {
          for (; U = U[D]; )
            if (U.nodeType === 1 || B)
              return d(U, Z, re);
          return !1;
        }
      ) : (
        // Check against all ancestor/preceding elements
        function(U, Z, re) {
          var le, ne, se = [Ht, K];
          if (re) {
            for (; U = U[D]; )
              if ((U.nodeType === 1 || B) && d(U, Z, re))
                return !0;
          } else
            for (; U = U[D]; )
              if (U.nodeType === 1 || B)
                if (ne = U[y.expando] || (U[y.expando] = {}), O && I(U, O))
                  U = U[D] || U;
                else {
                  if ((le = ne[N]) && le[0] === Ht && le[1] === K)
                    return se[2] = le[2];
                  if (ne[N] = se, se[2] = d(U, Z, re))
                    return !0;
                }
          return !1;
        }
      );
    }
    function Re(d) {
      return d.length > 1 ? function(_, x, D) {
        for (var O = d.length; O--; )
          if (!d[O](_, x, D))
            return !1;
        return !0;
      } : d[0];
    }
    function ye(d, _, x) {
      for (var D = 0, O = _.length; D < O; D++)
        ft(d, _[D], x);
      return x;
    }
    function Fe(d, _, x, D, O) {
      for (var N, B = [], K = 0, U = d.length, Z = _ != null; K < U; K++)
        (N = d[K]) && (!x || x(N, D, O)) && (B.push(N), Z && _.push(K));
      return B;
    }
    function be(d, _, x, D, O, N) {
      return D && !D[y.expando] && (D = be(D)), O && !O[y.expando] && (O = be(O, N)), cn(function(B, K, U, Z) {
        var re, le, ne, se, ve = [], Ne = [], Ee = K.length, it = B || ye(
          _ || "*",
          U.nodeType ? [U] : U,
          []
        ), nt = d && (B || !_) ? Fe(it, ve, d, U, Z) : it;
        if (x ? (se = O || (B ? d : Ee || D) ? (
          // ...intermediate processing is necessary
          []
        ) : (
          // ...otherwise use results directly
          K
        ), x(nt, se, U, Z)) : se = nt, D)
          for (re = Fe(se, Ne), D(re, [], U, Z), le = re.length; le--; )
            (ne = re[le]) && (se[Ne[le]] = !(nt[Ne[le]] = ne));
        if (B) {
          if (O || d) {
            if (O) {
              for (re = [], le = se.length; le--; )
                (ne = se[le]) && re.push(nt[le] = ne);
              O(null, se = [], re, Z);
            }
            for (le = se.length; le--; )
              (ne = se[le]) && (re = O ? l.call(B, ne) : ve[le]) > -1 && (B[re] = !(K[re] = ne));
          }
        } else
          se = Fe(
            se === K ? se.splice(Ee, se.length) : se
          ), O ? O(null, K, se, Z) : o.apply(K, se);
      });
    }
    function De(d) {
      for (var _, x, D, O = d.length, N = y.expr.relative[d[0].type], B = N || y.expr.relative[" "], K = N ? 1 : 0, U = me(function(le) {
        return le === _;
      }, B, !0), Z = me(function(le) {
        return l.call(_, le) > -1;
      }, B, !0), re = [function(le, ne, se) {
        var ve = !N && (se || ne != Ot) || ((_ = ne).nodeType ? U(le, ne, se) : Z(le, ne, se));
        return _ = null, ve;
      }]; K < O; K++)
        if (x = y.expr.relative[d[K].type])
          re = [me(Re(re), x)];
        else {
          if (x = y.expr.filter[d[K].type].apply(null, d[K].matches), x[y.expando]) {
            for (D = ++K; D < O && !y.expr.relative[d[D].type]; D++)
              ;
            return be(
              K > 1 && Re(re),
              K > 1 && et(
                // If the preceding token was a descendant combinator, insert an implicit any-element `*`
                d.slice(0, K - 1).concat({ value: d[K - 2].type === " " ? "*" : "" })
              ).replace(P, "$1"),
              x,
              K < D && De(d.slice(K, D)),
              D < O && De(d = d.slice(D)),
              D < O && et(d)
            );
          }
          re.push(x);
        }
      return Re(re);
    }
    function Xe(d, _) {
      var x = _.length > 0, D = d.length > 0, O = function(N, B, K, U, Z) {
        var re, le, ne, se = 0, ve = "0", Ne = N && [], Ee = [], it = Ot, nt = N || D && y.expr.find.TAG("*", Z), jt = Ht += it == null ? 1 : Math.random() || 0.1;
        for (Z && (Ot = B == dt || B || Z); (re = nt[ve]) != null; ve++) {
          if (D && re) {
            for (le = 0, !B && re.ownerDocument != dt && (ae(re), K = !It); ne = d[le++]; )
              if (ne(re, B || dt, K)) {
                o.call(U, re);
                break;
              }
            Z && (Ht = jt);
          }
          x && ((re = !ne && re) && se--, N && Ne.push(re));
        }
        if (se += ve, x && ve !== se) {
          for (le = 0; ne = _[le++]; )
            ne(Ne, Ee, B, K);
          if (N) {
            if (se > 0)
              for (; ve--; )
                Ne[ve] || Ee[ve] || (Ee[ve] = $.call(U));
            Ee = Fe(Ee);
          }
          o.apply(U, Ee), Z && !N && Ee.length > 0 && se + _.length > 1 && y.uniqueSort(U);
        }
        return Z && (Ht = jt, Ot = it), Ne;
      };
      return x ? cn(O) : O;
    }
    function $t(d, _) {
      var x, D = [], O = [], N = Ls[d + " "];
      if (!N) {
        for (_ || (_ = Ie(d)), x = _.length; x--; )
          N = De(_[x]), N[y.expando] ? D.push(N) : O.push(N);
        N = Ls(
          d,
          Xe(O, D)
        ), N.selector = d;
      }
      return N;
    }
    function Tt(d, _, x, D) {
      var O, N, B, K, U, Z = typeof d == "function" && d, re = !D && Ie(d = Z.selector || d);
      if (x = x || [], re.length === 1) {
        if (N = re[0] = re[0].slice(0), N.length > 2 && (B = N[0]).type === "ID" && _.nodeType === 9 && It && y.expr.relative[N[1].type]) {
          if (_ = (y.expr.find.ID(
            he(B.matches[0]),
            _
          ) || [])[0], _)
            Z && (_ = _.parentNode);
          else
            return x;
          d = d.slice(N.shift().value.length);
        }
        for (O = tt.needsContext.test(d) ? 0 : N.length; O-- && (B = N[O], !y.expr.relative[K = B.type]); )
          if ((U = y.expr.find[K]) && (D = U(
            he(B.matches[0]),
            E.test(N[0].type) && J(_.parentNode) || _
          ))) {
            if (N.splice(O, 1), d = D.length && et(N), !d)
              return o.apply(x, D), x;
            break;
          }
      }
      return (Z || $t(d, re))(
        D,
        _,
        !It,
        x,
        !_ || E.test(d) && J(_.parentNode) || _
      ), x;
    }
    ae(), y.find = ft, ft.compile = $t, ft.select = Tt, ft.setDocument = ae, ft.tokenize = Ie;
    function dn(d, _, x) {
      for (var D = [], O = x !== void 0; (d = d[_]) && d.nodeType !== 9; )
        if (d.nodeType === 1) {
          if (O && y(d).is(x))
            break;
          D.push(d);
        }
      return D;
    }
    function Bn(d, _) {
      for (var x = []; d; d = d.nextSibling)
        d.nodeType === 1 && d !== _ && x.push(d);
      return x;
    }
    var Ms = y.expr.match.needsContext, Gr = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
    function Fs(d) {
      return d[0] === "<" && d[d.length - 1] === ">" && d.length >= 3;
    }
    function ji(d, _, x) {
      return typeof _ == "function" ? y.grep(d, function(D, O) {
        return !!_.call(D, O, D) !== x;
      }) : _.nodeType ? y.grep(d, function(D) {
        return D === _ !== x;
      }) : typeof _ != "string" ? y.grep(d, function(D) {
        return l.call(_, D) > -1 !== x;
      }) : y.filter(_, d, x);
    }
    y.filter = function(d, _, x) {
      var D = _[0];
      return x && (d = ":not(" + d + ")"), _.length === 1 && D.nodeType === 1 ? y.find.matchesSelector(D, d) ? [D] : [] : y.find.matches(d, y.grep(_, function(O) {
        return O.nodeType === 1;
      }));
    }, y.fn.extend({
      find: function(d) {
        var _, x, D = this.length, O = this;
        if (typeof d != "string")
          return this.pushStack(y(d).filter(function() {
            for (_ = 0; _ < D; _++)
              if (y.contains(O[_], this))
                return !0;
          }));
        for (x = this.pushStack([]), _ = 0; _ < D; _++)
          y.find(d, O[_], x);
        return D > 1 ? y.uniqueSort(x) : x;
      },
      filter: function(d) {
        return this.pushStack(ji(this, d || [], !1));
      },
      not: function(d) {
        return this.pushStack(ji(this, d || [], !0));
      },
      is: function(d) {
        return !!ji(
          this,
          // If this is a positional/relative selector, check membership in the returned set
          // so $("p:first").is("p:last") won't return true for a doc with two "p".
          typeof d == "string" && Ms.test(d) ? y(d) : d || [],
          !1
        ).length;
      }
    });
    var Ur, uo = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/, co = y.fn.init = function(d, _) {
      var x, D;
      if (!d)
        return this;
      if (d.nodeType)
        return this[0] = d, this.length = 1, this;
      if (typeof d == "function")
        return Ur.ready !== void 0 ? Ur.ready(d) : (
          // Execute immediately if ready is not present
          d(y)
        );
      if (x = d + "", Fs(x))
        x = [null, d, null];
      else if (typeof d == "string")
        x = uo.exec(d);
      else
        return y.makeArray(d, this);
      if (x && (x[1] || !_))
        if (x[1]) {
          if (_ = _ instanceof y ? _[0] : _, y.merge(this, y.parseHTML(
            x[1],
            _ && _.nodeType ? _.ownerDocument || _ : p,
            !0
          )), Gr.test(x[1]) && y.isPlainObject(_))
            for (x in _)
              typeof this[x] == "function" ? this[x](_[x]) : this.attr(x, _[x]);
          return this;
        } else
          return D = p.getElementById(x[2]), D && (this[0] = D, this.length = 1), this;
      else
        return !_ || _.jquery ? (_ || Ur).find(d) : this.constructor(_).find(d);
    };
    co.prototype = y.fn, Ur = y(p);
    var fo = /^(?:parents|prev(?:Until|All))/, ho = {
      children: !0,
      contents: !0,
      next: !0,
      prev: !0
    };
    y.fn.extend({
      has: function(d) {
        var _ = y(d, this), x = _.length;
        return this.filter(function() {
          for (var D = 0; D < x; D++)
            if (y.contains(this, _[D]))
              return !0;
        });
      },
      closest: function(d, _) {
        var x, D = 0, O = this.length, N = [], B = typeof d != "string" && y(d);
        if (!Ms.test(d)) {
          for (; D < O; D++)
            for (x = this[D]; x && x !== _; x = x.parentNode)
              if (x.nodeType < 11 && (B ? B.index(x) > -1 : (
                // Don't pass non-elements to jQuery#find
                x.nodeType === 1 && y.find.matchesSelector(x, d)
              ))) {
                N.push(x);
                break;
              }
        }
        return this.pushStack(N.length > 1 ? y.uniqueSort(N) : N);
      },
      // Determine the position of an element within the set
      index: function(d) {
        return d ? typeof d == "string" ? l.call(y(d), this[0]) : l.call(
          this,
          // If it receives a jQuery object, the first element is used
          d.jquery ? d[0] : d
        ) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1;
      },
      add: function(d, _) {
        return this.pushStack(
          y.uniqueSort(
            y.merge(this.get(), y(d, _))
          )
        );
      },
      addBack: function(d) {
        return this.add(
          d == null ? this.prevObject : this.prevObject.filter(d)
        );
      }
    });
    function Bs(d, _) {
      for (; (d = d[_]) && d.nodeType !== 1; )
        ;
      return d;
    }
    y.each({
      parent: function(d) {
        var _ = d.parentNode;
        return _ && _.nodeType !== 11 ? _ : null;
      },
      parents: function(d) {
        return dn(d, "parentNode");
      },
      parentsUntil: function(d, _, x) {
        return dn(d, "parentNode", x);
      },
      next: function(d) {
        return Bs(d, "nextSibling");
      },
      prev: function(d) {
        return Bs(d, "previousSibling");
      },
      nextAll: function(d) {
        return dn(d, "nextSibling");
      },
      prevAll: function(d) {
        return dn(d, "previousSibling");
      },
      nextUntil: function(d, _, x) {
        return dn(d, "nextSibling", x);
      },
      prevUntil: function(d, _, x) {
        return dn(d, "previousSibling", x);
      },
      siblings: function(d) {
        return Bn((d.parentNode || {}).firstChild, d);
      },
      children: function(d) {
        return Bn(d.firstChild);
      },
      contents: function(d) {
        return d.contentDocument != null && // Support: IE 11+
        // <object> elements with no `data` attribute has an object
        // `contentDocument` with a `null` prototype.
        i(d.contentDocument) ? d.contentDocument : (I(d, "template") && (d = d.content || d), y.merge([], d.childNodes));
      }
    }, function(d, _) {
      y.fn[d] = function(x, D) {
        var O = y.map(this, _, x);
        return d.slice(-5) !== "Until" && (D = x), D && typeof D == "string" && (O = y.filter(D, O)), this.length > 1 && (ho[d] || y.uniqueSort(O), fo.test(d) && O.reverse()), this.pushStack(O);
      };
    });
    function Xh(d) {
      var _ = {};
      return y.each(d.match(Ke) || [], function(x, D) {
        _[D] = !0;
      }), _;
    }
    y.Callbacks = function(d) {
      d = typeof d == "string" ? Xh(d) : y.extend({}, d);
      var _, x, D, O, N = [], B = [], K = -1, U = function() {
        for (O = O || d.once, D = _ = !0; B.length; K = -1)
          for (x = B.shift(); ++K < N.length; )
            N[K].apply(x[0], x[1]) === !1 && d.stopOnFalse && (K = N.length, x = !1);
        d.memory || (x = !1), _ = !1, O && (x ? N = [] : N = "");
      }, Z = {
        // Add a callback or a collection of callbacks to the list
        add: function() {
          return N && (x && !_ && (K = N.length - 1, B.push(x)), function re(le) {
            y.each(le, function(ne, se) {
              typeof se == "function" ? (!d.unique || !Z.has(se)) && N.push(se) : se && se.length && m(se) !== "string" && re(se);
            });
          }(arguments), x && !_ && U()), this;
        },
        // Remove a callback from the list
        remove: function() {
          return y.each(arguments, function(re, le) {
            for (var ne; (ne = y.inArray(le, N, ne)) > -1; )
              N.splice(ne, 1), ne <= K && K--;
          }), this;
        },
        // Check if a given callback is in the list.
        // If no argument is given, return whether or not list has callbacks attached.
        has: function(re) {
          return re ? y.inArray(re, N) > -1 : N.length > 0;
        },
        // Remove all callbacks from the list
        empty: function() {
          return N && (N = []), this;
        },
        // Disable .fire and .add
        // Abort any current/pending executions
        // Clear all callbacks and values
        disable: function() {
          return O = B = [], N = x = "", this;
        },
        disabled: function() {
          return !N;
        },
        // Disable .fire
        // Also disable .add unless we have memory (since it would have no effect)
        // Abort any pending executions
        lock: function() {
          return O = B = [], !x && !_ && (N = x = ""), this;
        },
        locked: function() {
          return !!O;
        },
        // Call all callbacks with the given context and arguments
        fireWith: function(re, le) {
          return O || (le = le || [], le = [re, le.slice ? le.slice() : le], B.push(le), _ || U()), this;
        },
        // Call all the callbacks with the given arguments
        fire: function() {
          return Z.fireWith(this, arguments), this;
        },
        // To know if the callbacks have already been called at least once
        fired: function() {
          return !!D;
        }
      };
      return Z;
    };
    function Kr(d) {
      return d;
    }
    function Hs(d) {
      throw d;
    }
    function Du(d, _, x, D) {
      var O;
      try {
        d && typeof (O = d.promise) == "function" ? O.call(d).done(_).fail(x) : d && typeof (O = d.then) == "function" ? O.call(d, _, x) : _.apply(void 0, [d].slice(D));
      } catch (N) {
        x(N);
      }
    }
    y.extend({
      Deferred: function(d) {
        var _ = [
          // action, add listener, callbacks,
          // ... .then handlers, argument index, [final state]
          [
            "notify",
            "progress",
            y.Callbacks("memory"),
            y.Callbacks("memory"),
            2
          ],
          [
            "resolve",
            "done",
            y.Callbacks("once memory"),
            y.Callbacks("once memory"),
            0,
            "resolved"
          ],
          [
            "reject",
            "fail",
            y.Callbacks("once memory"),
            y.Callbacks("once memory"),
            1,
            "rejected"
          ]
        ], x = "pending", D = {
          state: function() {
            return x;
          },
          always: function() {
            return O.done(arguments).fail(arguments), this;
          },
          catch: function(N) {
            return D.then(null, N);
          },
          // Keep pipe for back-compat
          pipe: function() {
            var N = arguments;
            return y.Deferred(function(B) {
              y.each(_, function(K, U) {
                var Z = typeof N[U[4]] == "function" && N[U[4]];
                O[U[1]](function() {
                  var re = Z && Z.apply(this, arguments);
                  re && typeof re.promise == "function" ? re.promise().progress(B.notify).done(B.resolve).fail(B.reject) : B[U[0] + "With"](
                    this,
                    Z ? [re] : arguments
                  );
                });
              }), N = null;
            }).promise();
          },
          then: function(N, B, K) {
            var U = 0;
            function Z(re, le, ne, se) {
              return function() {
                var ve = this, Ne = arguments, Ee = function() {
                  var nt, jt;
                  if (!(re < U)) {
                    if (nt = ne.apply(ve, Ne), nt === le.promise())
                      throw new TypeError("Thenable self-resolution");
                    jt = nt && // Support: Promises/A+ section 2.3.4
                    // https://promisesaplus.com/#point-64
                    // Only check objects and functions for thenability
                    (typeof nt == "object" || typeof nt == "function") && nt.then, typeof jt == "function" ? se ? jt.call(
                      nt,
                      Z(U, le, Kr, se),
                      Z(U, le, Hs, se)
                    ) : (U++, jt.call(
                      nt,
                      Z(U, le, Kr, se),
                      Z(U, le, Hs, se),
                      Z(
                        U,
                        le,
                        Kr,
                        le.notifyWith
                      )
                    )) : (ne !== Kr && (ve = void 0, Ne = [nt]), (se || le.resolveWith)(ve, Ne));
                  }
                }, it = se ? Ee : function() {
                  try {
                    Ee();
                  } catch (nt) {
                    y.Deferred.exceptionHook && y.Deferred.exceptionHook(
                      nt,
                      it.error
                    ), re + 1 >= U && (ne !== Hs && (ve = void 0, Ne = [nt]), le.rejectWith(ve, Ne));
                  }
                };
                re ? it() : (y.Deferred.getErrorHook && (it.error = y.Deferred.getErrorHook()), n.setTimeout(it));
              };
            }
            return y.Deferred(function(re) {
              _[0][3].add(
                Z(
                  0,
                  re,
                  typeof K == "function" ? K : Kr,
                  re.notifyWith
                )
              ), _[1][3].add(
                Z(
                  0,
                  re,
                  typeof N == "function" ? N : Kr
                )
              ), _[2][3].add(
                Z(
                  0,
                  re,
                  typeof B == "function" ? B : Hs
                )
              );
            }).promise();
          },
          // Get a promise for this deferred
          // If obj is provided, the promise aspect is added to the object
          promise: function(N) {
            return N != null ? y.extend(N, D) : D;
          }
        }, O = {};
        return y.each(_, function(N, B) {
          var K = B[2], U = B[5];
          D[B[1]] = K.add, U && K.add(
            function() {
              x = U;
            },
            // rejected_callbacks.disable
            // fulfilled_callbacks.disable
            _[3 - N][2].disable,
            // rejected_handlers.disable
            // fulfilled_handlers.disable
            _[3 - N][3].disable,
            // progress_callbacks.lock
            _[0][2].lock,
            // progress_handlers.lock
            _[0][3].lock
          ), K.add(B[3].fire), O[B[0]] = function() {
            return O[B[0] + "With"](this === O ? void 0 : this, arguments), this;
          }, O[B[0] + "With"] = K.fireWith;
        }), D.promise(O), d && d.call(O, O), O;
      },
      // Deferred helper
      when: function(d) {
        var _ = arguments.length, x = _, D = Array(x), O = s.call(arguments), N = y.Deferred(), B = function(K) {
          return function(U) {
            D[K] = this, O[K] = arguments.length > 1 ? s.call(arguments) : U, --_ || N.resolveWith(D, O);
          };
        };
        if (_ <= 1 && (Du(
          d,
          N.done(B(x)).resolve,
          N.reject,
          !_
        ), N.state() === "pending" || typeof (O[x] && O[x].then) == "function"))
          return N.then();
        for (; x--; )
          Du(O[x], B(x), N.reject);
        return N.promise();
      }
    });
    var Qh = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
    y.Deferred.exceptionHook = function(d, _) {
      d && Qh.test(d.name) && n.console.warn(
        "jQuery.Deferred exception",
        d,
        _
      );
    }, y.readyException = function(d) {
      n.setTimeout(function() {
        throw d;
      });
    };
    var po = y.Deferred();
    y.fn.ready = function(d) {
      return po.then(d).catch(function(_) {
        y.readyException(_);
      }), this;
    }, y.extend({
      // Is the DOM ready to be used? Set to true once it occurs.
      isReady: !1,
      // A counter to track how many items to wait for before
      // the ready event fires. See trac-6781
      readyWait: 1,
      // Handle when the DOM is ready
      ready: function(d) {
        (d === !0 ? --y.readyWait : y.isReady) || (y.isReady = !0, !(d !== !0 && --y.readyWait > 0) && po.resolveWith(p, [y]));
      }
    }), y.ready.then = po.then;
    function $s() {
      p.removeEventListener("DOMContentLoaded", $s), n.removeEventListener("load", $s), y.ready();
    }
    p.readyState !== "loading" ? n.setTimeout(y.ready) : (p.addEventListener("DOMContentLoaded", $s), n.addEventListener("load", $s));
    var Jh = /-([a-z])/g;
    function Zh(d, _) {
      return _.toUpperCase();
    }
    function br(d) {
      return d.replace(Jh, Zh);
    }
    function zi(d) {
      return d.nodeType === 1 || d.nodeType === 9 || !+d.nodeType;
    }
    function Yi() {
      this.expando = y.expando + Yi.uid++;
    }
    Yi.uid = 1, Yi.prototype = {
      cache: function(d) {
        var _ = d[this.expando];
        return _ || (_ = /* @__PURE__ */ Object.create(null), zi(d) && (d.nodeType ? d[this.expando] = _ : Object.defineProperty(d, this.expando, {
          value: _,
          configurable: !0
        }))), _;
      },
      set: function(d, _, x) {
        var D, O = this.cache(d);
        if (typeof _ == "string")
          O[br(_)] = x;
        else
          for (D in _)
            O[br(D)] = _[D];
        return x;
      },
      get: function(d, _) {
        return _ === void 0 ? this.cache(d) : (
          // Always use camelCase key (gh-2257)
          d[this.expando] && d[this.expando][br(_)]
        );
      },
      access: function(d, _, x) {
        return _ === void 0 || _ && typeof _ == "string" && x === void 0 ? this.get(d, _) : (this.set(d, _, x), x !== void 0 ? x : _);
      },
      remove: function(d, _) {
        var x, D = d[this.expando];
        if (D !== void 0) {
          if (_ !== void 0)
            for (Array.isArray(_) ? _ = _.map(br) : (_ = br(_), _ = _ in D ? [_] : _.match(Ke) || []), x = _.length; x--; )
              delete D[_[x]];
          (_ === void 0 || y.isEmptyObject(D)) && (d.nodeType ? d[this.expando] = void 0 : delete d[this.expando]);
        }
      },
      hasData: function(d) {
        var _ = d[this.expando];
        return _ !== void 0 && !y.isEmptyObject(_);
      }
    };
    var Le = new Yi(), Vt = new Yi(), ep = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, tp = /[A-Z]/g;
    function np(d) {
      return d === "true" ? !0 : d === "false" ? !1 : d === "null" ? null : d === +d + "" ? +d : ep.test(d) ? JSON.parse(d) : d;
    }
    function Eu(d, _, x) {
      var D;
      if (x === void 0 && d.nodeType === 1)
        if (D = "data-" + _.replace(tp, "-$&").toLowerCase(), x = d.getAttribute(D), typeof x == "string") {
          try {
            x = np(x);
          } catch {
          }
          Vt.set(d, _, x);
        } else
          x = void 0;
      return x;
    }
    y.extend({
      hasData: function(d) {
        return Vt.hasData(d) || Le.hasData(d);
      },
      data: function(d, _, x) {
        return Vt.access(d, _, x);
      },
      removeData: function(d, _) {
        Vt.remove(d, _);
      },
      // TODO: Now that all calls to _data and _removeData have been replaced
      // with direct calls to dataPriv methods, these can be deprecated.
      _data: function(d, _, x) {
        return Le.access(d, _, x);
      },
      _removeData: function(d, _) {
        Le.remove(d, _);
      }
    }), y.fn.extend({
      data: function(d, _) {
        var x, D, O, N = this[0], B = N && N.attributes;
        if (d === void 0) {
          if (this.length && (O = Vt.get(N), N.nodeType === 1 && !Le.get(N, "hasDataAttrs"))) {
            for (x = B.length; x--; )
              B[x] && (D = B[x].name, D.indexOf("data-") === 0 && (D = br(D.slice(5)), Eu(N, D, O[D])));
            Le.set(N, "hasDataAttrs", !0);
          }
          return O;
        }
        return typeof d == "object" ? this.each(function() {
          Vt.set(this, d);
        }) : He(this, function(K) {
          var U;
          if (N && K === void 0)
            return U = Vt.get(N, d), U !== void 0 || (U = Eu(N, d), U !== void 0) ? U : void 0;
          this.each(function() {
            Vt.set(this, d, K);
          });
        }, null, _, arguments.length > 1, null, !0);
      },
      removeData: function(d) {
        return this.each(function() {
          Vt.remove(this, d);
        });
      }
    }), y.extend({
      queue: function(d, _, x) {
        var D;
        if (d)
          return _ = (_ || "fx") + "queue", D = Le.get(d, _), x && (!D || Array.isArray(x) ? D = Le.set(d, _, y.makeArray(x)) : D.push(x)), D || [];
      },
      dequeue: function(d, _) {
        _ = _ || "fx";
        var x = y.queue(d, _), D = x.length, O = x.shift(), N = y._queueHooks(d, _), B = function() {
          y.dequeue(d, _);
        };
        O === "inprogress" && (O = x.shift(), D--), O && (_ === "fx" && x.unshift("inprogress"), delete N.stop, O.call(d, B, N)), !D && N && N.empty.fire();
      },
      // Not public - generate a queueHooks object, or return the current one
      _queueHooks: function(d, _) {
        var x = _ + "queueHooks";
        return Le.get(d, x) || Le.set(d, x, {
          empty: y.Callbacks("once memory").add(function() {
            Le.remove(d, [_ + "queue", x]);
          })
        });
      }
    }), y.fn.extend({
      queue: function(d, _) {
        var x = 2;
        return typeof d != "string" && (_ = d, d = "fx", x--), arguments.length < x ? y.queue(this[0], d) : _ === void 0 ? this : this.each(function() {
          var D = y.queue(this, d, _);
          y._queueHooks(this, d), d === "fx" && D[0] !== "inprogress" && y.dequeue(this, d);
        });
      },
      dequeue: function(d) {
        return this.each(function() {
          y.dequeue(this, d);
        });
      },
      clearQueue: function(d) {
        return this.queue(d || "fx", []);
      },
      // Get a promise resolved when queues of a certain type
      // are emptied (fx is the type by default)
      promise: function(d, _) {
        var x, D = 1, O = y.Deferred(), N = this, B = this.length, K = function() {
          --D || O.resolveWith(N, [N]);
        };
        for (typeof d != "string" && (_ = d, d = void 0), d = d || "fx"; B--; )
          x = Le.get(N[B], d + "queueHooks"), x && x.empty && (D++, x.empty.add(K));
        return K(), O.promise(_);
      }
    });
    var Au = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source, qi = new RegExp("^(?:([+-])=|)(" + Au + ")([a-z%]*)$", "i"), Kn = ["Top", "Right", "Bottom", "Left"];
    function Vs(d, _) {
      return d = _ || d, d.style.display === "none" || d.style.display === "" && y.css(d, "display") === "none";
    }
    var rp = /^[a-z]/, ip = /^(?:Border(?:Top|Right|Bottom|Left)?(?:Width|)|(?:Margin|Padding)?(?:Top|Right|Bottom|Left)?|(?:Min|Max)?(?:Width|Height))$/;
    function Ws(d) {
      return rp.test(d) && ip.test(d[0].toUpperCase() + d.slice(1));
    }
    function ku(d, _, x, D) {
      var O, N, B = 20, K = D ? function() {
        return D.cur();
      } : function() {
        return y.css(d, _, "");
      }, U = K(), Z = x && x[3] || (Ws(_) ? "px" : ""), re = d.nodeType && (!Ws(_) || Z !== "px" && +U) && qi.exec(y.css(d, _));
      if (re && re[3] !== Z) {
        for (U = U / 2, Z = Z || re[3], re = +U || 1; B--; )
          y.style(d, _, re + Z), (1 - N) * (1 - (N = K() / U || 0.5)) <= 0 && (B = 0), re = re / N;
        re = re * 2, y.style(d, _, re + Z), x = x || [];
      }
      return x && (re = +re || +U || 0, O = x[1] ? re + (x[1] + 1) * x[2] : +x[2], D && (D.unit = Z, D.start = re, D.end = O)), O;
    }
    var sp = /^-ms-/;
    function go(d) {
      return br(d.replace(sp, "ms-"));
    }
    var Pu = {};
    function ap(d) {
      var _, x = d.ownerDocument, D = d.nodeName, O = Pu[D];
      return O || (_ = x.body.appendChild(x.createElement(D)), O = y.css(_, "display"), _.parentNode.removeChild(_), O === "none" && (O = "block"), Pu[D] = O, O);
    }
    function Xr(d, _) {
      for (var x, D, O = [], N = 0, B = d.length; N < B; N++)
        D = d[N], D.style && (x = D.style.display, _ ? (x === "none" && (O[N] = Le.get(D, "display") || null, O[N] || (D.style.display = "")), D.style.display === "" && Vs(D) && (O[N] = ap(D))) : x !== "none" && (O[N] = "none", Le.set(D, "display", x)));
      for (N = 0; N < B; N++)
        O[N] != null && (d[N].style.display = O[N]);
      return d;
    }
    y.fn.extend({
      show: function() {
        return Xr(this, !0);
      },
      hide: function() {
        return Xr(this);
      },
      toggle: function(d) {
        return typeof d == "boolean" ? d ? this.show() : this.hide() : this.each(function() {
          Vs(this) ? y(this).show() : y(this).hide();
        });
      }
    });
    var Gi = function(d) {
      return y.contains(d.ownerDocument, d) || d.getRootNode(op) === d.ownerDocument;
    }, op = { composed: !0 };
    M.getRootNode || (Gi = function(d) {
      return y.contains(d.ownerDocument, d);
    });
    var Ou = /<([a-z][^\/\0>\x20\t\r\n\f]*)/i, Hn = {
      // Table parts need to be wrapped with `<table>` or they're
      // stripped to their contents when put in a div.
      // XHTML parsers do not magically insert elements in the
      // same way that tag soup parsers do, so we cannot shorten
      // this by omitting <tbody> or other required elements.
      thead: ["table"],
      col: ["colgroup", "table"],
      tr: ["tbody", "table"],
      td: ["tr", "tbody", "table"]
    };
    Hn.tbody = Hn.tfoot = Hn.colgroup = Hn.caption = Hn.thead, Hn.th = Hn.td;
    function Wt(d, _) {
      var x;
      return typeof d.getElementsByTagName < "u" ? x = r.slice.call(d.getElementsByTagName(_ || "*")) : typeof d.querySelectorAll < "u" ? x = d.querySelectorAll(_ || "*") : x = [], _ === void 0 || _ && I(d, _) ? y.merge([d], x) : x;
    }
    var Iu = /^$|^module$|\/(?:java|ecma)script/i;
    function mo(d, _) {
      for (var x = 0, D = d.length; x < D; x++)
        Le.set(
          d[x],
          "globalEval",
          !_ || Le.get(_[x], "globalEval")
        );
    }
    var lp = /<|&#?\w+;/;
    function Nu(d, _, x, D, O) {
      for (var N, B, K, U, Z, re, le = _.createDocumentFragment(), ne = [], se = 0, ve = d.length; se < ve; se++)
        if (N = d[se], N || N === 0)
          if (m(N) === "object" && (N.nodeType || u(N)))
            y.merge(ne, N.nodeType ? [N] : N);
          else if (!lp.test(N))
            ne.push(_.createTextNode(N));
          else {
            for (B = B || le.appendChild(_.createElement("div")), K = (Ou.exec(N) || ["", ""])[1].toLowerCase(), U = Hn[K] || r, re = U.length; --re > -1; )
              B = B.appendChild(_.createElement(U[re]));
            B.innerHTML = y.htmlPrefilter(N), y.merge(ne, B.childNodes), B = le.firstChild, B.textContent = "";
          }
      for (le.textContent = "", se = 0; N = ne[se++]; ) {
        if (D && y.inArray(N, D) > -1) {
          O && O.push(N);
          continue;
        }
        if (Z = Gi(N), B = Wt(le.appendChild(N), "script"), Z && mo(B), x)
          for (re = 0; N = B[re++]; )
            Iu.test(N.type || "") && x.push(N);
      }
      return le;
    }
    function up(d) {
      return d.type = (d.getAttribute("type") !== null) + "/" + d.type, d;
    }
    function cp(d) {
      return (d.type || "").slice(0, 5) === "true/" ? d.type = d.type.slice(5) : d.removeAttribute("type"), d;
    }
    function Qr(d, _, x, D) {
      _ = a(_);
      var O, N, B, K, U, Z, re = 0, le = d.length, ne = le - 1, se = _[0], ve = typeof se == "function";
      if (ve)
        return d.each(function(Ne) {
          var Ee = d.eq(Ne);
          _[0] = se.call(this, Ne, Ee.html()), Qr(Ee, _, x, D);
        });
      if (le && (O = Nu(_, d[0].ownerDocument, !1, d, D), N = O.firstChild, O.childNodes.length === 1 && (O = N), N || D)) {
        for (B = y.map(Wt(O, "script"), up), K = B.length; re < le; re++)
          U = O, re !== ne && (U = y.clone(U, !0, !0), K && y.merge(B, Wt(U, "script"))), x.call(d[re], U, re);
        if (K)
          for (Z = B[B.length - 1].ownerDocument, y.map(B, cp), re = 0; re < K; re++)
            U = B[re], Iu.test(U.type || "") && !Le.get(U, "globalEval") && y.contains(Z, U) && (U.src && (U.type || "").toLowerCase() !== "module" ? y._evalUrl && !U.noModule && y._evalUrl(U.src, {
              nonce: U.nonce,
              crossOrigin: U.crossOrigin
            }, Z) : S(U.textContent, U, Z));
      }
      return d;
    }
    var js = /^(?:checkbox|radio)$/i, Ru = /^([^.]*)(?:\.(.+)|)/;
    function Jr() {
      return !0;
    }
    function Zr() {
      return !1;
    }
    function vo(d, _, x, D, O, N) {
      var B, K;
      if (typeof _ == "object") {
        typeof x != "string" && (D = D || x, x = void 0);
        for (K in _)
          vo(d, K, x, D, _[K], N);
        return d;
      }
      if (D == null && O == null ? (O = x, D = x = void 0) : O == null && (typeof x == "string" ? (O = D, D = void 0) : (O = D, D = x, x = void 0)), O === !1)
        O = Zr;
      else if (!O)
        return d;
      return N === 1 && (B = O, O = function(U) {
        return y().off(U), B.apply(this, arguments);
      }, O.guid = B.guid || (B.guid = y.guid++)), d.each(function() {
        y.event.add(this, _, O, D, x);
      });
    }
    y.event = {
      add: function(d, _, x, D, O) {
        var N, B, K, U, Z, re, le, ne, se, ve, Ne, Ee = Le.get(d);
        if (zi(d))
          for (x.handler && (N = x, x = N.handler, O = N.selector), O && y.find.matchesSelector(M, O), x.guid || (x.guid = y.guid++), (U = Ee.events) || (U = Ee.events = /* @__PURE__ */ Object.create(null)), (B = Ee.handle) || (B = Ee.handle = function(it) {
            return typeof y < "u" && y.event.triggered !== it.type ? y.event.dispatch.apply(d, arguments) : void 0;
          }), _ = (_ || "").match(Ke) || [""], Z = _.length; Z--; )
            K = Ru.exec(_[Z]) || [], se = Ne = K[1], ve = (K[2] || "").split(".").sort(), se && (le = y.event.special[se] || {}, se = (O ? le.delegateType : le.bindType) || se, le = y.event.special[se] || {}, re = y.extend({
              type: se,
              origType: Ne,
              data: D,
              handler: x,
              guid: x.guid,
              selector: O,
              needsContext: O && y.expr.match.needsContext.test(O),
              namespace: ve.join(".")
            }, N), (ne = U[se]) || (ne = U[se] = [], ne.delegateCount = 0, (!le.setup || le.setup.call(d, D, ve, B) === !1) && d.addEventListener && d.addEventListener(se, B)), le.add && (le.add.call(d, re), re.handler.guid || (re.handler.guid = x.guid)), O ? ne.splice(ne.delegateCount++, 0, re) : ne.push(re));
      },
      // Detach an event or set of events from an element
      remove: function(d, _, x, D, O) {
        var N, B, K, U, Z, re, le, ne, se, ve, Ne, Ee = Le.hasData(d) && Le.get(d);
        if (!(!Ee || !(U = Ee.events))) {
          for (_ = (_ || "").match(Ke) || [""], Z = _.length; Z--; ) {
            if (K = Ru.exec(_[Z]) || [], se = Ne = K[1], ve = (K[2] || "").split(".").sort(), !se) {
              for (se in U)
                y.event.remove(d, se + _[Z], x, D, !0);
              continue;
            }
            for (le = y.event.special[se] || {}, se = (D ? le.delegateType : le.bindType) || se, ne = U[se] || [], K = K[2] && new RegExp("(^|\\.)" + ve.join("\\.(?:.*\\.|)") + "(\\.|$)"), B = N = ne.length; N--; )
              re = ne[N], (O || Ne === re.origType) && (!x || x.guid === re.guid) && (!K || K.test(re.namespace)) && (!D || D === re.selector || D === "**" && re.selector) && (ne.splice(N, 1), re.selector && ne.delegateCount--, le.remove && le.remove.call(d, re));
            B && !ne.length && ((!le.teardown || le.teardown.call(d, ve, Ee.handle) === !1) && y.removeEvent(d, se, Ee.handle), delete U[se]);
          }
          y.isEmptyObject(U) && Le.remove(d, "handle events");
        }
      },
      dispatch: function(d) {
        var _, x, D, O, N, B, K = new Array(arguments.length), U = y.event.fix(d), Z = (Le.get(this, "events") || /* @__PURE__ */ Object.create(null))[U.type] || [], re = y.event.special[U.type] || {};
        for (K[0] = U, _ = 1; _ < arguments.length; _++)
          K[_] = arguments[_];
        if (U.delegateTarget = this, !(re.preDispatch && re.preDispatch.call(this, U) === !1)) {
          for (B = y.event.handlers.call(this, U, Z), _ = 0; (O = B[_++]) && !U.isPropagationStopped(); )
            for (U.currentTarget = O.elem, x = 0; (N = O.handlers[x++]) && !U.isImmediatePropagationStopped(); )
              (!U.rnamespace || N.namespace === !1 || U.rnamespace.test(N.namespace)) && (U.handleObj = N, U.data = N.data, D = ((y.event.special[N.origType] || {}).handle || N.handler).apply(O.elem, K), D !== void 0 && (U.result = D) === !1 && (U.preventDefault(), U.stopPropagation()));
          return re.postDispatch && re.postDispatch.call(this, U), U.result;
        }
      },
      handlers: function(d, _) {
        var x, D, O, N, B, K = [], U = _.delegateCount, Z = d.target;
        if (U && // Support: Firefox <=42 - 66+
        // Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
        // https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
        // Support: IE 11+
        // ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
        !(d.type === "click" && d.button >= 1)) {
          for (; Z !== this; Z = Z.parentNode || this)
            if (Z.nodeType === 1 && !(d.type === "click" && Z.disabled === !0)) {
              for (N = [], B = {}, x = 0; x < U; x++)
                D = _[x], O = D.selector + " ", B[O] === void 0 && (B[O] = D.needsContext ? y(O, this).index(Z) > -1 : y.find(O, this, null, [Z]).length), B[O] && N.push(D);
              N.length && K.push({ elem: Z, handlers: N });
            }
        }
        return Z = this, U < _.length && K.push({ elem: Z, handlers: _.slice(U) }), K;
      },
      addProp: function(d, _) {
        Object.defineProperty(y.Event.prototype, d, {
          enumerable: !0,
          configurable: !0,
          get: typeof _ == "function" ? function() {
            if (this.originalEvent)
              return _(this.originalEvent);
          } : function() {
            if (this.originalEvent)
              return this.originalEvent[d];
          },
          set: function(x) {
            Object.defineProperty(this, d, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: x
            });
          }
        });
      },
      fix: function(d) {
        return d[y.expando] ? d : new y.Event(d);
      },
      special: y.extend(/* @__PURE__ */ Object.create(null), {
        load: {
          // Prevent triggered image.load events from bubbling to window.load
          noBubble: !0
        },
        click: {
          // Utilize native event to ensure correct state for checkable inputs
          setup: function(d) {
            var _ = this || d;
            return js.test(_.type) && _.click && I(_, "input") && zs(_, "click", !0), !1;
          },
          trigger: function(d) {
            var _ = this || d;
            return js.test(_.type) && _.click && I(_, "input") && zs(_, "click"), !0;
          },
          // For cross-browser consistency, suppress native .click() on links
          // Also prevent it if we're currently inside a leveraged native-event stack
          _default: function(d) {
            var _ = d.target;
            return js.test(_.type) && _.click && I(_, "input") && Le.get(_, "click") || I(_, "a");
          }
        },
        beforeunload: {
          postDispatch: function(d) {
            d.result !== void 0 && d.preventDefault();
          }
        }
      })
    };
    function zs(d, _, x) {
      if (!x) {
        Le.get(d, _) === void 0 && y.event.add(d, _, Jr);
        return;
      }
      Le.set(d, _, !1), y.event.add(d, _, {
        namespace: !1,
        handler: function(D) {
          var O, N = Le.get(this, _);
          if (D.isTrigger & 1 && this[_]) {
            if (N.length)
              (y.event.special[_] || {}).delegateType && D.stopPropagation();
            else if (N = s.call(arguments), Le.set(this, _, N), this[_](), O = Le.get(this, _), Le.set(this, _, !1), N !== O)
              return D.stopImmediatePropagation(), D.preventDefault(), O && O.value;
          } else
            N.length && (Le.set(this, _, {
              value: y.event.trigger(
                N[0],
                N.slice(1),
                this
              )
            }), D.stopPropagation(), D.isImmediatePropagationStopped = Jr);
        }
      });
    }
    y.removeEvent = function(d, _, x) {
      d.removeEventListener && d.removeEventListener(_, x);
    }, y.Event = function(d, _) {
      if (!(this instanceof y.Event))
        return new y.Event(d, _);
      d && d.type ? (this.originalEvent = d, this.type = d.type, this.isDefaultPrevented = d.defaultPrevented ? Jr : Zr, this.target = d.target, this.currentTarget = d.currentTarget, this.relatedTarget = d.relatedTarget) : this.type = d, _ && y.extend(this, _), this.timeStamp = d && d.timeStamp || Date.now(), this[y.expando] = !0;
    }, y.Event.prototype = {
      constructor: y.Event,
      isDefaultPrevented: Zr,
      isPropagationStopped: Zr,
      isImmediatePropagationStopped: Zr,
      isSimulated: !1,
      preventDefault: function() {
        var d = this.originalEvent;
        this.isDefaultPrevented = Jr, d && !this.isSimulated && d.preventDefault();
      },
      stopPropagation: function() {
        var d = this.originalEvent;
        this.isPropagationStopped = Jr, d && !this.isSimulated && d.stopPropagation();
      },
      stopImmediatePropagation: function() {
        var d = this.originalEvent;
        this.isImmediatePropagationStopped = Jr, d && !this.isSimulated && d.stopImmediatePropagation(), this.stopPropagation();
      }
    }, y.each({
      altKey: !0,
      bubbles: !0,
      cancelable: !0,
      changedTouches: !0,
      ctrlKey: !0,
      detail: !0,
      eventPhase: !0,
      metaKey: !0,
      pageX: !0,
      pageY: !0,
      shiftKey: !0,
      view: !0,
      char: !0,
      code: !0,
      charCode: !0,
      key: !0,
      keyCode: !0,
      button: !0,
      buttons: !0,
      clientX: !0,
      clientY: !0,
      offsetX: !0,
      offsetY: !0,
      pointerId: !0,
      pointerType: !0,
      screenX: !0,
      screenY: !0,
      targetTouches: !0,
      toElement: !0,
      touches: !0,
      which: !0
    }, y.event.addProp), y.each({ focus: "focusin", blur: "focusout" }, function(d, _) {
      function x(D) {
        var O = y.event.fix(D);
        O.type = D.type === "focusin" ? "focus" : "blur", O.isSimulated = !0, O.target === O.currentTarget && Le.get(this, "handle")(O);
      }
      y.event.special[d] = {
        // Utilize native event if possible so blur/focus sequence is correct
        setup: function() {
          if (zs(this, d, !0), z)
            this.addEventListener(_, x);
          else
            return !1;
        },
        trigger: function() {
          return zs(this, d), !0;
        },
        teardown: function() {
          if (z)
            this.removeEventListener(_, x);
          else
            return !1;
        },
        // Suppress native focus or blur if we're currently inside
        // a leveraged native-event stack
        _default: function(D) {
          return Le.get(D.target, d);
        },
        delegateType: _
      };
    }), y.each({
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      pointerenter: "pointerover",
      pointerleave: "pointerout"
    }, function(d, _) {
      y.event.special[d] = {
        delegateType: _,
        bindType: _,
        handle: function(x) {
          var D, O = this, N = x.relatedTarget, B = x.handleObj;
          return (!N || N !== O && !y.contains(O, N)) && (x.type = B.origType, D = B.handler.apply(this, arguments), x.type = _), D;
        }
      };
    }), y.fn.extend({
      on: function(d, _, x, D) {
        return vo(this, d, _, x, D);
      },
      one: function(d, _, x, D) {
        return vo(this, d, _, x, D, 1);
      },
      off: function(d, _, x) {
        var D, O;
        if (d && d.preventDefault && d.handleObj)
          return D = d.handleObj, y(d.delegateTarget).off(
            D.namespace ? D.origType + "." + D.namespace : D.origType,
            D.selector,
            D.handler
          ), this;
        if (typeof d == "object") {
          for (O in d)
            this.off(O, _, d[O]);
          return this;
        }
        return (_ === !1 || typeof _ == "function") && (x = _, _ = void 0), x === !1 && (x = Zr), this.each(function() {
          y.event.remove(this, d, x, _);
        });
      }
    });
    var dp = /<script|<style|<link/i;
    function Lu(d, _) {
      return I(d, "table") && I(_.nodeType !== 11 ? _ : _.firstChild, "tr") && y(d).children("tbody")[0] || d;
    }
    function Mu(d, _) {
      var x, D, O, N = Le.get(d, "events");
      if (_.nodeType === 1) {
        if (N) {
          Le.remove(_, "handle events");
          for (x in N)
            for (D = 0, O = N[x].length; D < O; D++)
              y.event.add(_, x, N[x][D]);
        }
        Vt.hasData(d) && Vt.set(_, y.extend({}, Vt.get(d)));
      }
    }
    function Fu(d, _, x) {
      for (var D, O = _ ? y.filter(_, d) : d, N = 0; (D = O[N]) != null; N++)
        !x && D.nodeType === 1 && y.cleanData(Wt(D)), D.parentNode && (x && Gi(D) && mo(Wt(D, "script")), D.parentNode.removeChild(D));
      return d;
    }
    y.extend({
      htmlPrefilter: function(d) {
        return d;
      },
      clone: function(d, _, x) {
        var D, O, N, B, K = d.cloneNode(!0), U = Gi(d);
        if (z && (d.nodeType === 1 || d.nodeType === 11) && !y.isXMLDoc(d))
          for (B = Wt(K), N = Wt(d), D = 0, O = N.length; D < O; D++)
            I(B[D], "textarea") && (B[D].defaultValue = N[D].defaultValue);
        if (_)
          if (x)
            for (N = N || Wt(d), B = B || Wt(K), D = 0, O = N.length; D < O; D++)
              Mu(N[D], B[D]);
          else
            Mu(d, K);
        return B = Wt(K, "script"), B.length > 0 && mo(B, !U && Wt(d, "script")), K;
      },
      cleanData: function(d) {
        for (var _, x, D, O = y.event.special, N = 0; (x = d[N]) !== void 0; N++)
          if (zi(x)) {
            if (_ = x[Le.expando]) {
              if (_.events)
                for (D in _.events)
                  O[D] ? y.event.remove(x, D) : y.removeEvent(x, D, _.handle);
              x[Le.expando] = void 0;
            }
            x[Vt.expando] && (x[Vt.expando] = void 0);
          }
      }
    }), y.fn.extend({
      detach: function(d) {
        return Fu(this, d, !0);
      },
      remove: function(d) {
        return Fu(this, d);
      },
      text: function(d) {
        return He(this, function(_) {
          return _ === void 0 ? y.text(this) : this.empty().each(function() {
            (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) && (this.textContent = _);
          });
        }, null, d, arguments.length);
      },
      append: function() {
        return Qr(this, arguments, function(d) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var _ = Lu(this, d);
            _.appendChild(d);
          }
        });
      },
      prepend: function() {
        return Qr(this, arguments, function(d) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var _ = Lu(this, d);
            _.insertBefore(d, _.firstChild);
          }
        });
      },
      before: function() {
        return Qr(this, arguments, function(d) {
          this.parentNode && this.parentNode.insertBefore(d, this);
        });
      },
      after: function() {
        return Qr(this, arguments, function(d) {
          this.parentNode && this.parentNode.insertBefore(d, this.nextSibling);
        });
      },
      empty: function() {
        for (var d, _ = 0; (d = this[_]) != null; _++)
          d.nodeType === 1 && (y.cleanData(Wt(d, !1)), d.textContent = "");
        return this;
      },
      clone: function(d, _) {
        return d = d ?? !1, _ = _ ?? d, this.map(function() {
          return y.clone(this, d, _);
        });
      },
      html: function(d) {
        return He(this, function(_) {
          var x = this[0] || {}, D = 0, O = this.length;
          if (_ === void 0 && x.nodeType === 1)
            return x.innerHTML;
          if (typeof _ == "string" && !dp.test(_) && !Hn[(Ou.exec(_) || ["", ""])[1].toLowerCase()]) {
            _ = y.htmlPrefilter(_);
            try {
              for (; D < O; D++)
                x = this[D] || {}, x.nodeType === 1 && (y.cleanData(Wt(x, !1)), x.innerHTML = _);
              x = 0;
            } catch {
            }
          }
          x && this.empty().append(_);
        }, null, d, arguments.length);
      },
      replaceWith: function() {
        var d = [];
        return Qr(this, arguments, function(_) {
          var x = this.parentNode;
          y.inArray(this, d) < 0 && (y.cleanData(Wt(this)), x && x.replaceChild(_, this));
        }, d);
      }
    }), y.each({
      appendTo: "append",
      prependTo: "prepend",
      insertBefore: "before",
      insertAfter: "after",
      replaceAll: "replaceWith"
    }, function(d, _) {
      y.fn[d] = function(x) {
        for (var D, O = [], N = y(x), B = N.length - 1, K = 0; K <= B; K++)
          D = K === B ? this : this.clone(!0), y(N[K])[_](D), o.apply(O, D);
        return this.pushStack(O);
      };
    });
    var fp = new RegExp("^(" + Au + ")(?!px)[a-z%]+$", "i"), bo = /^--/;
    function Ys(d) {
      var _ = d.ownerDocument.defaultView;
      return _ || (_ = n), _.getComputedStyle(d);
    }
    function hp(d, _, x) {
      var D, O, N = {};
      for (O in _)
        N[O] = d.style[O], d.style[O] = _[O];
      D = x.call(d);
      for (O in _)
        d.style[O] = N[O];
      return D;
    }
    function Bu(d, _, x) {
      var D, O = bo.test(_);
      return x = x || Ys(d), x && (D = x.getPropertyValue(_) || x[_], O && D && (D = D.replace(P, "$1") || void 0), D === "" && !Gi(d) && (D = y.style(d, _))), D !== void 0 ? (
        // Support: IE <=9 - 11+
        // IE returns zIndex value as an integer.
        D + ""
      ) : D;
    }
    var Hu = ["Webkit", "Moz", "ms"], $u = p.createElement("div").style;
    function pp(d) {
      for (var _ = d[0].toUpperCase() + d.slice(1), x = Hu.length; x--; )
        if (d = Hu[x] + _, d in $u)
          return d;
    }
    function yo(d) {
      return d in $u ? d : pp(d) || d;
    }
    var Vu, Wu, Xn = p.createElement("table");
    function ju() {
      if (
        // This is a singleton, we need to execute it only once
        !(!Xn || // Finish early in limited (non-browser) environments
        !Xn.style)
      ) {
        var d, _ = p.createElement("col"), x = p.createElement("tr"), D = p.createElement("td");
        if (Xn.style.cssText = "position:absolute;left:-11111px;border-collapse:separate;border-spacing:0", x.style.cssText = "box-sizing:content-box;border:1px solid;height:1px", D.style.cssText = "height:9px;width:9px;padding:0", _.span = 2, M.appendChild(Xn).appendChild(_).parentNode.appendChild(x).appendChild(D).parentNode.appendChild(D.cloneNode(!0)), Xn.offsetWidth === 0) {
          M.removeChild(Xn);
          return;
        }
        d = n.getComputedStyle(x), Wu = z || Math.round(
          parseFloat(
            n.getComputedStyle(_).width
          )
        ) === 18, Vu = Math.round(parseFloat(d.height) + parseFloat(d.borderTopWidth) + parseFloat(d.borderBottomWidth)) === x.offsetHeight, M.removeChild(Xn), Xn = null;
      }
    }
    y.extend(C, {
      reliableTrDimensions: function() {
        return ju(), Vu;
      },
      reliableColDimensions: function() {
        return ju(), Wu;
      }
    });
    var gp = { position: "absolute", visibility: "hidden", display: "block" }, zu = {
      letterSpacing: "0",
      fontWeight: "400"
    };
    function Yu(d, _, x) {
      var D = qi.exec(_);
      return D ? (
        // Guard against undefined "subtract", e.g., when used as in cssHooks
        Math.max(0, D[2] - (x || 0)) + (D[3] || "px")
      ) : _;
    }
    function qu(d, _, x, D, O, N) {
      var B = _ === "width" ? 1 : 0, K = 0, U = 0, Z = 0;
      if (x === (D ? "border" : "content"))
        return 0;
      for (; B < 4; B += 2)
        x === "margin" && (Z += y.css(d, x + Kn[B], !0, O)), D ? (x === "content" && (U -= y.css(d, "padding" + Kn[B], !0, O)), x !== "margin" && (U -= y.css(d, "border" + Kn[B] + "Width", !0, O))) : (U += y.css(d, "padding" + Kn[B], !0, O), x !== "padding" ? U += y.css(d, "border" + Kn[B] + "Width", !0, O) : K += y.css(d, "border" + Kn[B] + "Width", !0, O));
      return !D && N >= 0 && (U += Math.max(0, Math.ceil(
        d["offset" + _[0].toUpperCase() + _.slice(1)] - N - U - K - 0.5
        // If offsetWidth/offsetHeight is unknown, then we can't determine content-box scroll gutter
        // Use an explicit zero to avoid NaN (gh-3964)
      )) || 0), U + Z;
    }
    function Gu(d, _, x) {
      var D = Ys(d), O = z || x, N = O && y.css(d, "boxSizing", !1, D) === "border-box", B = N, K = Bu(d, _, D), U = "offset" + _[0].toUpperCase() + _.slice(1);
      if (fp.test(K)) {
        if (!x)
          return K;
        K = "auto";
      }
      return (
        // Fall back to offsetWidth/offsetHeight when value is "auto"
        // This happens for inline elements with no explicit setting (gh-3571)
        (K === "auto" || // Support: IE 9 - 11+
        // Use offsetWidth/offsetHeight for when box sizing is unreliable.
        // In those cases, the computed value can be trusted to be border-box.
        z && N || !C.reliableColDimensions() && I(d, "col") || !C.reliableTrDimensions() && I(d, "tr")) && // Make sure the element is visible & connected
        d.getClientRects().length && (N = y.css(d, "boxSizing", !1, D) === "border-box", B = U in d, B && (K = d[U])), K = parseFloat(K) || 0, K + qu(
          d,
          _,
          x || (N ? "border" : "content"),
          B,
          D,
          // Provide the current computed size to request scroll gutter calculation (gh-3589)
          K
        ) + "px"
      );
    }
    y.extend({
      // Add in style property hooks for overriding the default
      // behavior of getting and setting a style property
      cssHooks: {},
      // Get and set the style property on a DOM Node
      style: function(d, _, x, D) {
        if (!(!d || d.nodeType === 3 || d.nodeType === 8 || !d.style)) {
          var O, N, B, K = go(_), U = bo.test(_), Z = d.style;
          if (U || (_ = yo(K)), B = y.cssHooks[_] || y.cssHooks[K], x !== void 0) {
            if (N = typeof x, N === "string" && (O = qi.exec(x)) && O[1] && (x = ku(d, _, O), N = "number"), x == null || x !== x)
              return;
            N === "number" && (x += O && O[3] || (Ws(K) ? "px" : "")), z && x === "" && _.indexOf("background") === 0 && (Z[_] = "inherit"), (!B || !("set" in B) || (x = B.set(d, x, D)) !== void 0) && (U ? Z.setProperty(_, x) : Z[_] = x);
          } else
            return B && "get" in B && (O = B.get(d, !1, D)) !== void 0 ? O : Z[_];
        }
      },
      css: function(d, _, x, D) {
        var O, N, B, K = go(_), U = bo.test(_);
        return U || (_ = yo(K)), B = y.cssHooks[_] || y.cssHooks[K], B && "get" in B && (O = B.get(d, !0, x)), O === void 0 && (O = Bu(d, _, D)), O === "normal" && _ in zu && (O = zu[_]), x === "" || x ? (N = parseFloat(O), x === !0 || isFinite(N) ? N || 0 : O) : O;
      }
    }), y.each(["height", "width"], function(d, _) {
      y.cssHooks[_] = {
        get: function(x, D, O) {
          if (D)
            return y.css(x, "display") === "none" ? hp(x, gp, function() {
              return Gu(x, _, O);
            }) : Gu(x, _, O);
        },
        set: function(x, D, O) {
          var N, B = Ys(x), K = O && y.css(x, "boxSizing", !1, B) === "border-box", U = O ? qu(
            x,
            _,
            O,
            K,
            B
          ) : 0;
          return U && (N = qi.exec(D)) && (N[3] || "px") !== "px" && (x.style[_] = D, D = y.css(x, _)), Yu(x, D, U);
        }
      };
    }), y.each({
      margin: "",
      padding: "",
      border: "Width"
    }, function(d, _) {
      y.cssHooks[d + _] = {
        expand: function(x) {
          for (var D = 0, O = {}, N = typeof x == "string" ? x.split(" ") : [x]; D < 4; D++)
            O[d + Kn[D] + _] = N[D] || N[D - 2] || N[0];
          return O;
        }
      }, d !== "margin" && (y.cssHooks[d + _].set = Yu);
    }), y.fn.extend({
      css: function(d, _) {
        return He(this, function(x, D, O) {
          var N, B, K = {}, U = 0;
          if (Array.isArray(D)) {
            for (N = Ys(x), B = D.length; U < B; U++)
              K[D[U]] = y.css(x, D[U], !1, N);
            return K;
          }
          return O !== void 0 ? y.style(x, D, O) : y.css(x, D);
        }, d, _, arguments.length > 1);
      }
    });
    function fn(d, _, x, D, O) {
      return new fn.prototype.init(d, _, x, D, O);
    }
    y.Tween = fn, fn.prototype = {
      constructor: fn,
      init: function(d, _, x, D, O, N) {
        this.elem = d, this.prop = x, this.easing = O || y.easing._default, this.options = _, this.start = this.now = this.cur(), this.end = D, this.unit = N || (Ws(x) ? "px" : "");
      },
      cur: function() {
        var d = fn.propHooks[this.prop];
        return d && d.get ? d.get(this) : fn.propHooks._default.get(this);
      },
      run: function(d) {
        var _, x = fn.propHooks[this.prop];
        return this.options.duration ? this.pos = _ = y.easing[this.easing](
          d,
          this.options.duration * d,
          0,
          1,
          this.options.duration
        ) : this.pos = _ = d, this.now = (this.end - this.start) * _ + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), x && x.set ? x.set(this) : fn.propHooks._default.set(this), this;
      }
    }, fn.prototype.init.prototype = fn.prototype, fn.propHooks = {
      _default: {
        get: function(d) {
          var _;
          return d.elem.nodeType !== 1 || d.elem[d.prop] != null && d.elem.style[d.prop] == null ? d.elem[d.prop] : (_ = y.css(d.elem, d.prop, ""), !_ || _ === "auto" ? 0 : _);
        },
        set: function(d) {
          y.fx.step[d.prop] ? y.fx.step[d.prop](d) : d.elem.nodeType === 1 && (y.cssHooks[d.prop] || d.elem.style[yo(d.prop)] != null) ? y.style(d.elem, d.prop, d.now + d.unit) : d.elem[d.prop] = d.now;
        }
      }
    }, y.easing = {
      linear: function(d) {
        return d;
      },
      swing: function(d) {
        return 0.5 - Math.cos(d * Math.PI) / 2;
      },
      _default: "swing"
    }, y.fx = fn.prototype.init, y.fx.step = {};
    var ei, qs, mp = /^(?:toggle|show|hide)$/, vp = /queueHooks$/;
    function wo() {
      qs && (p.hidden === !1 && n.requestAnimationFrame ? n.requestAnimationFrame(wo) : n.setTimeout(wo, 13), y.fx.tick());
    }
    function Uu() {
      return n.setTimeout(function() {
        ei = void 0;
      }), ei = Date.now();
    }
    function Gs(d, _) {
      var x, D = 0, O = { height: d };
      for (_ = _ ? 1 : 0; D < 4; D += 2 - _)
        x = Kn[D], O["margin" + x] = O["padding" + x] = d;
      return _ && (O.opacity = O.width = d), O;
    }
    function Ku(d, _, x) {
      for (var D, O = (xn.tweeners[_] || []).concat(xn.tweeners["*"]), N = 0, B = O.length; N < B; N++)
        if (D = O[N].call(x, _, d))
          return D;
    }
    function bp(d, _, x) {
      var D, O, N, B, K, U, Z, re, le = "width" in _ || "height" in _, ne = this, se = {}, ve = d.style, Ne = d.nodeType && Vs(d), Ee = Le.get(d, "fxshow");
      x.queue || (B = y._queueHooks(d, "fx"), B.unqueued == null && (B.unqueued = 0, K = B.empty.fire, B.empty.fire = function() {
        B.unqueued || K();
      }), B.unqueued++, ne.always(function() {
        ne.always(function() {
          B.unqueued--, y.queue(d, "fx").length || B.empty.fire();
        });
      }));
      for (D in _)
        if (O = _[D], mp.test(O)) {
          if (delete _[D], N = N || O === "toggle", O === (Ne ? "hide" : "show"))
            if (O === "show" && Ee && Ee[D] !== void 0)
              Ne = !0;
            else
              continue;
          se[D] = Ee && Ee[D] || y.style(d, D);
        }
      if (U = !y.isEmptyObject(_), !(!U && y.isEmptyObject(se))) {
        le && d.nodeType === 1 && (x.overflow = [ve.overflow, ve.overflowX, ve.overflowY], Z = Ee && Ee.display, Z == null && (Z = Le.get(d, "display")), re = y.css(d, "display"), re === "none" && (Z ? re = Z : (Xr([d], !0), Z = d.style.display || Z, re = y.css(d, "display"), Xr([d]))), (re === "inline" || re === "inline-block" && Z != null) && y.css(d, "float") === "none" && (U || (ne.done(function() {
          ve.display = Z;
        }), Z == null && (re = ve.display, Z = re === "none" ? "" : re)), ve.display = "inline-block")), x.overflow && (ve.overflow = "hidden", ne.always(function() {
          ve.overflow = x.overflow[0], ve.overflowX = x.overflow[1], ve.overflowY = x.overflow[2];
        })), U = !1;
        for (D in se)
          U || (Ee ? "hidden" in Ee && (Ne = Ee.hidden) : Ee = Le.set(d, "fxshow", { display: Z }), N && (Ee.hidden = !Ne), Ne && Xr([d], !0), ne.done(function() {
            Ne || Xr([d]), Le.remove(d, "fxshow");
            for (D in se)
              y.style(d, D, se[D]);
          })), U = Ku(Ne ? Ee[D] : 0, D, ne), D in Ee || (Ee[D] = U.start, Ne && (U.end = U.start, U.start = 0));
      }
    }
    function yp(d, _) {
      var x, D, O, N, B;
      for (x in d)
        if (D = go(x), O = _[D], N = d[x], Array.isArray(N) && (O = N[1], N = d[x] = N[0]), x !== D && (d[D] = N, delete d[x]), B = y.cssHooks[D], B && "expand" in B) {
          N = B.expand(N), delete d[D];
          for (x in N)
            x in d || (d[x] = N[x], _[x] = O);
        } else
          _[D] = O;
    }
    function xn(d, _, x) {
      var D, O, N = 0, B = xn.prefilters.length, K = y.Deferred().always(function() {
        delete U.elem;
      }), U = function() {
        if (O)
          return !1;
        for (var le = ei || Uu(), ne = Math.max(0, Z.startTime + Z.duration - le), se = 1 - (ne / Z.duration || 0), ve = 0, Ne = Z.tweens.length; ve < Ne; ve++)
          Z.tweens[ve].run(se);
        return K.notifyWith(d, [Z, se, ne]), se < 1 && Ne ? ne : (Ne || K.notifyWith(d, [Z, 1, 0]), K.resolveWith(d, [Z]), !1);
      }, Z = K.promise({
        elem: d,
        props: y.extend({}, _),
        opts: y.extend(!0, {
          specialEasing: {},
          easing: y.easing._default
        }, x),
        originalProperties: _,
        originalOptions: x,
        startTime: ei || Uu(),
        duration: x.duration,
        tweens: [],
        createTween: function(le, ne) {
          var se = y.Tween(
            d,
            Z.opts,
            le,
            ne,
            Z.opts.specialEasing[le] || Z.opts.easing
          );
          return Z.tweens.push(se), se;
        },
        stop: function(le) {
          var ne = 0, se = le ? Z.tweens.length : 0;
          if (O)
            return this;
          for (O = !0; ne < se; ne++)
            Z.tweens[ne].run(1);
          return le ? (K.notifyWith(d, [Z, 1, 0]), K.resolveWith(d, [Z, le])) : K.rejectWith(d, [Z, le]), this;
        }
      }), re = Z.props;
      for (yp(re, Z.opts.specialEasing); N < B; N++)
        if (D = xn.prefilters[N].call(Z, d, re, Z.opts), D)
          return typeof D.stop == "function" && (y._queueHooks(Z.elem, Z.opts.queue).stop = D.stop.bind(D)), D;
      return y.map(re, Ku, Z), typeof Z.opts.start == "function" && Z.opts.start.call(d, Z), Z.progress(Z.opts.progress).done(Z.opts.done, Z.opts.complete).fail(Z.opts.fail).always(Z.opts.always), y.fx.timer(
        y.extend(U, {
          elem: d,
          anim: Z,
          queue: Z.opts.queue
        })
      ), Z;
    }
    y.Animation = y.extend(xn, {
      tweeners: {
        "*": [function(d, _) {
          var x = this.createTween(d, _);
          return ku(x.elem, d, qi.exec(_), x), x;
        }]
      },
      tweener: function(d, _) {
        typeof d == "function" ? (_ = d, d = ["*"]) : d = d.match(Ke);
        for (var x, D = 0, O = d.length; D < O; D++)
          x = d[D], xn.tweeners[x] = xn.tweeners[x] || [], xn.tweeners[x].unshift(_);
      },
      prefilters: [bp],
      prefilter: function(d, _) {
        _ ? xn.prefilters.unshift(d) : xn.prefilters.push(d);
      }
    }), y.speed = function(d, _, x) {
      var D = d && typeof d == "object" ? y.extend({}, d) : {
        complete: x || _ || typeof d == "function" && d,
        duration: d,
        easing: x && _ || _ && typeof _ != "function" && _
      };
      return y.fx.off ? D.duration = 0 : typeof D.duration != "number" && (D.duration in y.fx.speeds ? D.duration = y.fx.speeds[D.duration] : D.duration = y.fx.speeds._default), (D.queue == null || D.queue === !0) && (D.queue = "fx"), D.old = D.complete, D.complete = function() {
        typeof D.old == "function" && D.old.call(this), D.queue && y.dequeue(this, D.queue);
      }, D;
    }, y.fn.extend({
      fadeTo: function(d, _, x, D) {
        return this.filter(Vs).css("opacity", 0).show().end().animate({ opacity: _ }, d, x, D);
      },
      animate: function(d, _, x, D) {
        var O = y.isEmptyObject(d), N = y.speed(_, x, D), B = function() {
          var K = xn(this, y.extend({}, d), N);
          (O || Le.get(this, "finish")) && K.stop(!0);
        };
        return B.finish = B, O || N.queue === !1 ? this.each(B) : this.queue(N.queue, B);
      },
      stop: function(d, _, x) {
        var D = function(O) {
          var N = O.stop;
          delete O.stop, N(x);
        };
        return typeof d != "string" && (x = _, _ = d, d = void 0), _ && this.queue(d || "fx", []), this.each(function() {
          var O = !0, N = d != null && d + "queueHooks", B = y.timers, K = Le.get(this);
          if (N)
            K[N] && K[N].stop && D(K[N]);
          else
            for (N in K)
              K[N] && K[N].stop && vp.test(N) && D(K[N]);
          for (N = B.length; N--; )
            B[N].elem === this && (d == null || B[N].queue === d) && (B[N].anim.stop(x), O = !1, B.splice(N, 1));
          (O || !x) && y.dequeue(this, d);
        });
      },
      finish: function(d) {
        return d !== !1 && (d = d || "fx"), this.each(function() {
          var _, x = Le.get(this), D = x[d + "queue"], O = x[d + "queueHooks"], N = y.timers, B = D ? D.length : 0;
          for (x.finish = !0, y.queue(this, d, []), O && O.stop && O.stop.call(this, !0), _ = N.length; _--; )
            N[_].elem === this && N[_].queue === d && (N[_].anim.stop(!0), N.splice(_, 1));
          for (_ = 0; _ < B; _++)
            D[_] && D[_].finish && D[_].finish.call(this);
          delete x.finish;
        });
      }
    }), y.each(["toggle", "show", "hide"], function(d, _) {
      var x = y.fn[_];
      y.fn[_] = function(D, O, N) {
        return D == null || typeof D == "boolean" ? x.apply(this, arguments) : this.animate(Gs(_, !0), D, O, N);
      };
    }), y.each({
      slideDown: Gs("show"),
      slideUp: Gs("hide"),
      slideToggle: Gs("toggle"),
      fadeIn: { opacity: "show" },
      fadeOut: { opacity: "hide" },
      fadeToggle: { opacity: "toggle" }
    }, function(d, _) {
      y.fn[d] = function(x, D, O) {
        return this.animate(_, x, D, O);
      };
    }), y.timers = [], y.fx.tick = function() {
      var d, _ = 0, x = y.timers;
      for (ei = Date.now(); _ < x.length; _++)
        d = x[_], !d() && x[_] === d && x.splice(_--, 1);
      x.length || y.fx.stop(), ei = void 0;
    }, y.fx.timer = function(d) {
      y.timers.push(d), y.fx.start();
    }, y.fx.start = function() {
      qs || (qs = !0, wo());
    }, y.fx.stop = function() {
      qs = null;
    }, y.fx.speeds = {
      slow: 600,
      fast: 200,
      // Default speed
      _default: 400
    }, y.fn.delay = function(d, _) {
      return d = y.fx && y.fx.speeds[d] || d, _ = _ || "fx", this.queue(_, function(x, D) {
        var O = n.setTimeout(x, d);
        D.stop = function() {
          n.clearTimeout(O);
        };
      });
    };
    var wp = /^(?:input|select|textarea|button)$/i, _p = /^(?:a|area)$/i;
    y.fn.extend({
      prop: function(d, _) {
        return He(this, y.prop, d, _, arguments.length > 1);
      },
      removeProp: function(d) {
        return this.each(function() {
          delete this[y.propFix[d] || d];
        });
      }
    }), y.extend({
      prop: function(d, _, x) {
        var D, O, N = d.nodeType;
        if (!(N === 3 || N === 8 || N === 2))
          return (N !== 1 || !y.isXMLDoc(d)) && (_ = y.propFix[_] || _, O = y.propHooks[_]), x !== void 0 ? O && "set" in O && (D = O.set(d, x, _)) !== void 0 ? D : d[_] = x : O && "get" in O && (D = O.get(d, _)) !== null ? D : d[_];
      },
      propHooks: {
        tabIndex: {
          get: function(d) {
            var _ = d.getAttribute("tabindex");
            return _ ? parseInt(_, 10) : wp.test(d.nodeName) || // href-less anchor's `tabIndex` property value is `0` and
            // the `tabindex` attribute value: `null`. We want `-1`.
            _p.test(d.nodeName) && d.href ? 0 : -1;
          }
        }
      },
      propFix: {
        for: "htmlFor",
        class: "className"
      }
    }), z && (y.propHooks.selected = {
      get: function(d) {
        var _ = d.parentNode;
        return _ && _.parentNode && _.parentNode.selectedIndex, null;
      },
      set: function(d) {
        var _ = d.parentNode;
        _ && (_.selectedIndex, _.parentNode && _.parentNode.selectedIndex);
      }
    }), y.each([
      "tabIndex",
      "readOnly",
      "maxLength",
      "cellSpacing",
      "cellPadding",
      "rowSpan",
      "colSpan",
      "useMap",
      "frameBorder",
      "contentEditable"
    ], function() {
      y.propFix[this.toLowerCase()] = this;
    });
    function yr(d) {
      var _ = d.match(Ke) || [];
      return _.join(" ");
    }
    function ti(d) {
      return d.getAttribute && d.getAttribute("class") || "";
    }
    function _o(d) {
      return Array.isArray(d) ? d : typeof d == "string" ? d.match(Ke) || [] : [];
    }
    y.fn.extend({
      addClass: function(d) {
        var _, x, D, O, N, B;
        return typeof d == "function" ? this.each(function(K) {
          y(this).addClass(d.call(this, K, ti(this)));
        }) : (_ = _o(d), _.length ? this.each(function() {
          if (D = ti(this), x = this.nodeType === 1 && " " + yr(D) + " ", x) {
            for (N = 0; N < _.length; N++)
              O = _[N], x.indexOf(" " + O + " ") < 0 && (x += O + " ");
            B = yr(x), D !== B && this.setAttribute("class", B);
          }
        }) : this);
      },
      removeClass: function(d) {
        var _, x, D, O, N, B;
        return typeof d == "function" ? this.each(function(K) {
          y(this).removeClass(d.call(this, K, ti(this)));
        }) : arguments.length ? (_ = _o(d), _.length ? this.each(function() {
          if (D = ti(this), x = this.nodeType === 1 && " " + yr(D) + " ", x) {
            for (N = 0; N < _.length; N++)
              for (O = _[N]; x.indexOf(" " + O + " ") > -1; )
                x = x.replace(" " + O + " ", " ");
            B = yr(x), D !== B && this.setAttribute("class", B);
          }
        }) : this) : this.attr("class", "");
      },
      toggleClass: function(d, _) {
        var x, D, O, N;
        return typeof d == "function" ? this.each(function(B) {
          y(this).toggleClass(
            d.call(this, B, ti(this), _),
            _
          );
        }) : typeof _ == "boolean" ? _ ? this.addClass(d) : this.removeClass(d) : (x = _o(d), x.length ? this.each(function() {
          for (N = y(this), O = 0; O < x.length; O++)
            D = x[O], N.hasClass(D) ? N.removeClass(D) : N.addClass(D);
        }) : this);
      },
      hasClass: function(d) {
        var _, x, D = 0;
        for (_ = " " + d + " "; x = this[D++]; )
          if (x.nodeType === 1 && (" " + yr(ti(x)) + " ").indexOf(_) > -1)
            return !0;
        return !1;
      }
    }), y.fn.extend({
      val: function(d) {
        var _, x, D, O = this[0];
        return arguments.length ? (D = typeof d == "function", this.each(function(N) {
          var B;
          this.nodeType === 1 && (D ? B = d.call(this, N, y(this).val()) : B = d, B == null ? B = "" : typeof B == "number" ? B += "" : Array.isArray(B) && (B = y.map(B, function(K) {
            return K == null ? "" : K + "";
          })), _ = y.valHooks[this.type] || y.valHooks[this.nodeName.toLowerCase()], (!_ || !("set" in _) || _.set(this, B, "value") === void 0) && (this.value = B));
        })) : O ? (_ = y.valHooks[O.type] || y.valHooks[O.nodeName.toLowerCase()], _ && "get" in _ && (x = _.get(O, "value")) !== void 0 ? x : (x = O.value, x ?? "")) : void 0;
      }
    }), y.extend({
      valHooks: {
        select: {
          get: function(d) {
            var _, x, D, O = d.options, N = d.selectedIndex, B = d.type === "select-one", K = B ? null : [], U = B ? N + 1 : O.length;
            for (N < 0 ? D = U : D = B ? N : 0; D < U; D++)
              if (x = O[D], x.selected && // Don't return options that are disabled or in a disabled optgroup
              !x.disabled && (!x.parentNode.disabled || !I(x.parentNode, "optgroup"))) {
                if (_ = y(x).val(), B)
                  return _;
                K.push(_);
              }
            return K;
          },
          set: function(d, _) {
            for (var x, D, O = d.options, N = y.makeArray(_), B = O.length; B--; )
              D = O[B], (D.selected = y.inArray(y(D).val(), N) > -1) && (x = !0);
            return x || (d.selectedIndex = -1), N;
          }
        }
      }
    }), z && (y.valHooks.option = {
      get: function(d) {
        var _ = d.getAttribute("value");
        return _ ?? // Support: IE <=10 - 11+
        // option.text throws exceptions (trac-14686, trac-14858)
        // Strip and collapse whitespace
        // https://html.spec.whatwg.org/#strip-and-collapse-whitespace
        yr(y.text(d));
      }
    }), y.each(["radio", "checkbox"], function() {
      y.valHooks[this] = {
        set: function(d, _) {
          if (Array.isArray(_))
            return d.checked = y.inArray(y(d).val(), _) > -1;
        }
      };
    });
    var Xu = /^(?:focusinfocus|focusoutblur)$/, Qu = function(d) {
      d.stopPropagation();
    };
    y.extend(y.event, {
      trigger: function(d, _, x, D) {
        var O, N, B, K, U, Z, re, le, ne = [x || p], se = g.call(d, "type") ? d.type : d, ve = g.call(d, "namespace") ? d.namespace.split(".") : [];
        if (N = le = B = x = x || p, !(x.nodeType === 3 || x.nodeType === 8) && !Xu.test(se + y.event.triggered) && (se.indexOf(".") > -1 && (ve = se.split("."), se = ve.shift(), ve.sort()), U = se.indexOf(":") < 0 && "on" + se, d = d[y.expando] ? d : new y.Event(se, typeof d == "object" && d), d.isTrigger = D ? 2 : 3, d.namespace = ve.join("."), d.rnamespace = d.namespace ? new RegExp("(^|\\.)" + ve.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, d.result = void 0, d.target || (d.target = x), _ = _ == null ? [d] : y.makeArray(_, [d]), re = y.event.special[se] || {}, !(!D && re.trigger && re.trigger.apply(x, _) === !1))) {
          if (!D && !re.noBubble && !h(x)) {
            for (K = re.delegateType || se, Xu.test(K + se) || (N = N.parentNode); N; N = N.parentNode)
              ne.push(N), B = N;
            B === (x.ownerDocument || p) && ne.push(B.defaultView || B.parentWindow || n);
          }
          for (O = 0; (N = ne[O++]) && !d.isPropagationStopped(); )
            le = N, d.type = O > 1 ? K : re.bindType || se, Z = (Le.get(N, "events") || /* @__PURE__ */ Object.create(null))[d.type] && Le.get(N, "handle"), Z && Z.apply(N, _), Z = U && N[U], Z && Z.apply && zi(N) && (d.result = Z.apply(N, _), d.result === !1 && d.preventDefault());
          return d.type = se, !D && !d.isDefaultPrevented() && (!re._default || re._default.apply(ne.pop(), _) === !1) && zi(x) && U && typeof x[se] == "function" && !h(x) && (B = x[U], B && (x[U] = null), y.event.triggered = se, d.isPropagationStopped() && le.addEventListener(se, Qu), x[se](), d.isPropagationStopped() && le.removeEventListener(se, Qu), y.event.triggered = void 0, B && (x[U] = B)), d.result;
        }
      },
      // Piggyback on a donor event to simulate a different one
      // Used only for `focus(in | out)` events
      simulate: function(d, _, x) {
        var D = y.extend(
          new y.Event(),
          x,
          {
            type: d,
            isSimulated: !0
          }
        );
        y.event.trigger(D, null, _);
      }
    }), y.fn.extend({
      trigger: function(d, _) {
        return this.each(function() {
          y.event.trigger(d, _, this);
        });
      },
      triggerHandler: function(d, _) {
        var x = this[0];
        if (x)
          return y.event.trigger(d, _, x, !0);
      }
    });
    var Ui = n.location, Ju = { guid: Date.now() }, Co = /\?/;
    y.parseXML = function(d) {
      var _, x;
      if (!d || typeof d != "string")
        return null;
      try {
        _ = new n.DOMParser().parseFromString(d, "text/xml");
      } catch {
      }
      return x = _ && _.getElementsByTagName("parsererror")[0], (!_ || x) && y.error("Invalid XML: " + (x ? y.map(x.childNodes, function(D) {
        return D.textContent;
      }).join(`
`) : d)), _;
    };
    var Cp = /\[\]$/, Zu = /\r?\n/g, xp = /^(?:submit|button|image|reset|file)$/i, Sp = /^(?:input|select|textarea|keygen)/i;
    function xo(d, _, x, D) {
      var O;
      if (Array.isArray(_))
        y.each(_, function(N, B) {
          x || Cp.test(d) ? D(d, B) : xo(
            d + "[" + (typeof B == "object" && B != null ? N : "") + "]",
            B,
            x,
            D
          );
        });
      else if (!x && m(_) === "object")
        for (O in _)
          xo(d + "[" + O + "]", _[O], x, D);
      else
        D(d, _);
    }
    y.param = function(d, _) {
      var x, D = [], O = function(N, B) {
        var K = typeof B == "function" ? B() : B;
        D[D.length] = encodeURIComponent(N) + "=" + encodeURIComponent(K ?? "");
      };
      if (d == null)
        return "";
      if (Array.isArray(d) || d.jquery && !y.isPlainObject(d))
        y.each(d, function() {
          O(this.name, this.value);
        });
      else
        for (x in d)
          xo(x, d[x], _, O);
      return D.join("&");
    }, y.fn.extend({
      serialize: function() {
        return y.param(this.serializeArray());
      },
      serializeArray: function() {
        return this.map(function() {
          var d = y.prop(this, "elements");
          return d ? y.makeArray(d) : this;
        }).filter(function() {
          var d = this.type;
          return this.name && !y(this).is(":disabled") && Sp.test(this.nodeName) && !xp.test(d) && (this.checked || !js.test(d));
        }).map(function(d, _) {
          var x = y(this).val();
          return x == null ? null : Array.isArray(x) ? y.map(x, function(D) {
            return { name: _.name, value: D.replace(Zu, `\r
`) };
          }) : { name: _.name, value: x.replace(Zu, `\r
`) };
        }).get();
      }
    });
    var Tp = /%20/g, Dp = /#.*$/, Ep = /([?&])_=[^&]*/, Ap = /^(.*?):[ \t]*([^\r\n]*)$/mg, kp = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, Pp = /^(?:GET|HEAD)$/, Op = /^\/\//, ec = {}, So = {}, tc = "*/".concat("*"), To = p.createElement("a");
    To.href = Ui.href;
    function nc(d) {
      return function(_, x) {
        typeof _ != "string" && (x = _, _ = "*");
        var D, O = 0, N = _.toLowerCase().match(Ke) || [];
        if (typeof x == "function")
          for (; D = N[O++]; )
            D[0] === "+" ? (D = D.slice(1) || "*", (d[D] = d[D] || []).unshift(x)) : (d[D] = d[D] || []).push(x);
      };
    }
    function rc(d, _, x, D) {
      var O = {}, N = d === So;
      function B(K) {
        var U;
        return O[K] = !0, y.each(d[K] || [], function(Z, re) {
          var le = re(_, x, D);
          if (typeof le == "string" && !N && !O[le])
            return _.dataTypes.unshift(le), B(le), !1;
          if (N)
            return !(U = le);
        }), U;
      }
      return B(_.dataTypes[0]) || !O["*"] && B("*");
    }
    function Do(d, _) {
      var x, D, O = y.ajaxSettings.flatOptions || {};
      for (x in _)
        _[x] !== void 0 && ((O[x] ? d : D || (D = {}))[x] = _[x]);
      return D && y.extend(!0, d, D), d;
    }
    function Ip(d, _, x) {
      for (var D, O, N, B, K = d.contents, U = d.dataTypes; U[0] === "*"; )
        U.shift(), D === void 0 && (D = d.mimeType || _.getResponseHeader("Content-Type"));
      if (D) {
        for (O in K)
          if (K[O] && K[O].test(D)) {
            U.unshift(O);
            break;
          }
      }
      if (U[0] in x)
        N = U[0];
      else {
        for (O in x) {
          if (!U[0] || d.converters[O + " " + U[0]]) {
            N = O;
            break;
          }
          B || (B = O);
        }
        N = N || B;
      }
      if (N)
        return N !== U[0] && U.unshift(N), x[N];
    }
    function Np(d, _, x, D) {
      var O, N, B, K, U, Z = {}, re = d.dataTypes.slice();
      if (re[1])
        for (B in d.converters)
          Z[B.toLowerCase()] = d.converters[B];
      for (N = re.shift(); N; )
        if (d.responseFields[N] && (x[d.responseFields[N]] = _), !U && D && d.dataFilter && (_ = d.dataFilter(_, d.dataType)), U = N, N = re.shift(), N) {
          if (N === "*")
            N = U;
          else if (U !== "*" && U !== N) {
            if (B = Z[U + " " + N] || Z["* " + N], !B) {
              for (O in Z)
                if (K = O.split(" "), K[1] === N && (B = Z[U + " " + K[0]] || Z["* " + K[0]], B)) {
                  B === !0 ? B = Z[O] : Z[O] !== !0 && (N = K[0], re.unshift(K[1]));
                  break;
                }
            }
            if (B !== !0)
              if (B && d.throws)
                _ = B(_);
              else
                try {
                  _ = B(_);
                } catch (le) {
                  return {
                    state: "parsererror",
                    error: B ? le : "No conversion from " + U + " to " + N
                  };
                }
          }
        }
      return { state: "success", data: _ };
    }
    y.extend({
      // Counter for holding the number of active queries
      active: 0,
      // Last-Modified header cache for next request
      lastModified: {},
      etag: {},
      ajaxSettings: {
        url: Ui.href,
        type: "GET",
        isLocal: kp.test(Ui.protocol),
        global: !0,
        processData: !0,
        async: !0,
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        /*
        timeout: 0,
        data: null,
        dataType: null,
        username: null,
        password: null,
        cache: null,
        throws: false,
        traditional: false,
        headers: {},
        */
        accepts: {
          "*": tc,
          text: "text/plain",
          html: "text/html",
          xml: "application/xml, text/xml",
          json: "application/json, text/javascript"
        },
        contents: {
          xml: /\bxml\b/,
          html: /\bhtml/,
          json: /\bjson\b/
        },
        responseFields: {
          xml: "responseXML",
          text: "responseText",
          json: "responseJSON"
        },
        // Data converters
        // Keys separate source (or catchall "*") and destination types with a single space
        converters: {
          // Convert anything to text
          "* text": String,
          // Text to html (true = no transformation)
          "text html": !0,
          // Evaluate text as a json expression
          "text json": JSON.parse,
          // Parse text as xml
          "text xml": y.parseXML
        },
        // For options that shouldn't be deep extended:
        // you can add your own custom options here if
        // and when you create one that shouldn't be
        // deep extended (see ajaxExtend)
        flatOptions: {
          url: !0,
          context: !0
        }
      },
      // Creates a full fledged settings object into target
      // with both ajaxSettings and settings fields.
      // If target is omitted, writes into ajaxSettings.
      ajaxSetup: function(d, _) {
        return _ ? (
          // Building a settings object
          Do(Do(d, y.ajaxSettings), _)
        ) : (
          // Extending ajaxSettings
          Do(y.ajaxSettings, d)
        );
      },
      ajaxPrefilter: nc(ec),
      ajaxTransport: nc(So),
      // Main method
      ajax: function(d, _) {
        typeof d == "object" && (_ = d, d = void 0), _ = _ || {};
        var x, D, O, N, B, K, U, Z, re, le, ne = y.ajaxSetup({}, _), se = ne.context || ne, ve = ne.context && (se.nodeType || se.jquery) ? y(se) : y.event, Ne = y.Deferred(), Ee = y.Callbacks("once memory"), it = ne.statusCode || {}, nt = {}, jt = {}, Dt = "canceled", Ye = {
          readyState: 0,
          // Builds headers hashtable if needed
          getResponseHeader: function(Ge) {
            var ht;
            if (U) {
              if (!N)
                for (N = {}; ht = Ap.exec(O); )
                  N[ht[1].toLowerCase() + " "] = (N[ht[1].toLowerCase() + " "] || []).concat(ht[2]);
              ht = N[Ge.toLowerCase() + " "];
            }
            return ht == null ? null : ht.join(", ");
          },
          // Raw string
          getAllResponseHeaders: function() {
            return U ? O : null;
          },
          // Caches the header
          setRequestHeader: function(Ge, ht) {
            return U == null && (Ge = jt[Ge.toLowerCase()] = jt[Ge.toLowerCase()] || Ge, nt[Ge] = ht), this;
          },
          // Overrides response content-type header
          overrideMimeType: function(Ge) {
            return U == null && (ne.mimeType = Ge), this;
          },
          // Status-dependent callbacks
          statusCode: function(Ge) {
            var ht;
            if (Ge)
              if (U)
                Ye.always(Ge[Ye.status]);
              else
                for (ht in Ge)
                  it[ht] = [it[ht], Ge[ht]];
            return this;
          },
          // Cancel the request
          abort: function(Ge) {
            var ht = Ge || Dt;
            return x && x.abort(ht), Us(0, ht), this;
          }
        };
        if (Ne.promise(Ye), ne.url = ((d || ne.url || Ui.href) + "").replace(Op, Ui.protocol + "//"), ne.type = _.method || _.type || ne.method || ne.type, ne.dataTypes = (ne.dataType || "*").toLowerCase().match(Ke) || [""], ne.crossDomain == null) {
          K = p.createElement("a");
          try {
            K.href = ne.url, K.href = K.href, ne.crossDomain = To.protocol + "//" + To.host != K.protocol + "//" + K.host;
          } catch {
            ne.crossDomain = !0;
          }
        }
        if (rc(ec, ne, _, Ye), ne.data && ne.processData && typeof ne.data != "string" && (ne.data = y.param(ne.data, ne.traditional)), U)
          return Ye;
        Z = y.event && ne.global, Z && y.active++ === 0 && y.event.trigger("ajaxStart"), ne.type = ne.type.toUpperCase(), ne.hasContent = !Pp.test(ne.type), D = ne.url.replace(Dp, ""), ne.hasContent ? ne.data && ne.processData && (ne.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && (ne.data = ne.data.replace(Tp, "+")) : (le = ne.url.slice(D.length), ne.data && (ne.processData || typeof ne.data == "string") && (D += (Co.test(D) ? "&" : "?") + ne.data, delete ne.data), ne.cache === !1 && (D = D.replace(Ep, "$1"), le = (Co.test(D) ? "&" : "?") + "_=" + Ju.guid++ + le), ne.url = D + le), ne.ifModified && (y.lastModified[D] && Ye.setRequestHeader("If-Modified-Since", y.lastModified[D]), y.etag[D] && Ye.setRequestHeader("If-None-Match", y.etag[D])), (ne.data && ne.hasContent && ne.contentType !== !1 || _.contentType) && Ye.setRequestHeader("Content-Type", ne.contentType), Ye.setRequestHeader(
          "Accept",
          ne.dataTypes[0] && ne.accepts[ne.dataTypes[0]] ? ne.accepts[ne.dataTypes[0]] + (ne.dataTypes[0] !== "*" ? ", " + tc + "; q=0.01" : "") : ne.accepts["*"]
        );
        for (re in ne.headers)
          Ye.setRequestHeader(re, ne.headers[re]);
        if (ne.beforeSend && (ne.beforeSend.call(se, Ye, ne) === !1 || U))
          return Ye.abort();
        if (Dt = "abort", Ee.add(ne.complete), Ye.done(ne.success), Ye.fail(ne.error), x = rc(So, ne, _, Ye), !x)
          Us(-1, "No Transport");
        else {
          if (Ye.readyState = 1, Z && ve.trigger("ajaxSend", [Ye, ne]), U)
            return Ye;
          ne.async && ne.timeout > 0 && (B = n.setTimeout(function() {
            Ye.abort("timeout");
          }, ne.timeout));
          try {
            U = !1, x.send(nt, Us);
          } catch (Ge) {
            if (U)
              throw Ge;
            Us(-1, Ge);
          }
        }
        function Us(Ge, ht, ac, Fp) {
          var Qn, Ao, Ki, ni, ri, Pn = ht;
          U || (U = !0, B && n.clearTimeout(B), x = void 0, O = Fp || "", Ye.readyState = Ge > 0 ? 4 : 0, Qn = Ge >= 200 && Ge < 300 || Ge === 304, ac && (ni = Ip(ne, Ye, ac)), !Qn && y.inArray("script", ne.dataTypes) > -1 && y.inArray("json", ne.dataTypes) < 0 && (ne.converters["text script"] = function() {
          }), ni = Np(ne, ni, Ye, Qn), Qn ? (ne.ifModified && (ri = Ye.getResponseHeader("Last-Modified"), ri && (y.lastModified[D] = ri), ri = Ye.getResponseHeader("etag"), ri && (y.etag[D] = ri)), Ge === 204 || ne.type === "HEAD" ? Pn = "nocontent" : Ge === 304 ? Pn = "notmodified" : (Pn = ni.state, Ao = ni.data, Ki = ni.error, Qn = !Ki)) : (Ki = Pn, (Ge || !Pn) && (Pn = "error", Ge < 0 && (Ge = 0))), Ye.status = Ge, Ye.statusText = (ht || Pn) + "", Qn ? Ne.resolveWith(se, [Ao, Pn, Ye]) : Ne.rejectWith(se, [Ye, Pn, Ki]), Ye.statusCode(it), it = void 0, Z && ve.trigger(
            Qn ? "ajaxSuccess" : "ajaxError",
            [Ye, ne, Qn ? Ao : Ki]
          ), Ee.fireWith(se, [Ye, Pn]), Z && (ve.trigger("ajaxComplete", [Ye, ne]), --y.active || y.event.trigger("ajaxStop")));
        }
        return Ye;
      },
      getJSON: function(d, _, x) {
        return y.get(d, _, x, "json");
      },
      getScript: function(d, _) {
        return y.get(d, void 0, _, "script");
      }
    }), y.each(["get", "post"], function(d, _) {
      y[_] = function(x, D, O, N) {
        return (typeof D == "function" || D === null) && (N = N || O, O = D, D = void 0), y.ajax(y.extend({
          url: x,
          type: _,
          dataType: N,
          data: D,
          success: O
        }, y.isPlainObject(x) && x));
      };
    }), y.ajaxPrefilter(function(d) {
      var _;
      for (_ in d.headers)
        _.toLowerCase() === "content-type" && (d.contentType = d.headers[_] || "");
    }), y._evalUrl = function(d, _, x) {
      return y.ajax({
        url: d,
        // Make this explicit, since user can override this through ajaxSetup (trac-11264)
        type: "GET",
        dataType: "script",
        cache: !0,
        async: !1,
        global: !1,
        scriptAttrs: _.crossOrigin ? { crossOrigin: _.crossOrigin } : void 0,
        // Only evaluate the response if it is successful (gh-4126)
        // dataFilter is not invoked for failure responses, so using it instead
        // of the default converter is kludgy but it works.
        converters: {
          "text script": function() {
          }
        },
        dataFilter: function(D) {
          y.globalEval(D, _, x);
        }
      });
    }, y.fn.extend({
      wrapAll: function(d) {
        var _;
        return this[0] && (typeof d == "function" && (d = d.call(this[0])), _ = y(d, this[0].ownerDocument).eq(0).clone(!0), this[0].parentNode && _.insertBefore(this[0]), _.map(function() {
          for (var x = this; x.firstElementChild; )
            x = x.firstElementChild;
          return x;
        }).append(this)), this;
      },
      wrapInner: function(d) {
        return typeof d == "function" ? this.each(function(_) {
          y(this).wrapInner(d.call(this, _));
        }) : this.each(function() {
          var _ = y(this), x = _.contents();
          x.length ? x.wrapAll(d) : _.append(d);
        });
      },
      wrap: function(d) {
        var _ = typeof d == "function";
        return this.each(function(x) {
          y(this).wrapAll(_ ? d.call(this, x) : d);
        });
      },
      unwrap: function(d) {
        return this.parent(d).not("body").each(function() {
          y(this).replaceWith(this.childNodes);
        }), this;
      }
    }), y.expr.pseudos.hidden = function(d) {
      return !y.expr.pseudos.visible(d);
    }, y.expr.pseudos.visible = function(d) {
      return !!(d.offsetWidth || d.offsetHeight || d.getClientRects().length);
    }, y.ajaxSettings.xhr = function() {
      return new n.XMLHttpRequest();
    };
    var Rp = {
      // File protocol always yields status code 0, assume 200
      0: 200
    };
    y.ajaxTransport(function(d) {
      var _;
      return {
        send: function(x, D) {
          var O, N = d.xhr();
          if (N.open(
            d.type,
            d.url,
            d.async,
            d.username,
            d.password
          ), d.xhrFields)
            for (O in d.xhrFields)
              N[O] = d.xhrFields[O];
          d.mimeType && N.overrideMimeType && N.overrideMimeType(d.mimeType), !d.crossDomain && !x["X-Requested-With"] && (x["X-Requested-With"] = "XMLHttpRequest");
          for (O in x)
            N.setRequestHeader(O, x[O]);
          _ = function(B) {
            return function() {
              _ && (_ = N.onload = N.onerror = N.onabort = N.ontimeout = null, B === "abort" ? N.abort() : B === "error" ? D(
                // File: protocol always yields status 0; see trac-8605, trac-14207
                N.status,
                N.statusText
              ) : D(
                Rp[N.status] || N.status,
                N.statusText,
                // For XHR2 non-text, let the caller handle it (gh-2498)
                (N.responseType || "text") === "text" ? { text: N.responseText } : { binary: N.response },
                N.getAllResponseHeaders()
              ));
            };
          }, N.onload = _(), N.onabort = N.onerror = N.ontimeout = _("error"), _ = _("abort");
          try {
            N.send(d.hasContent && d.data || null);
          } catch (B) {
            if (_)
              throw B;
          }
        },
        abort: function() {
          _ && _();
        }
      };
    });
    function ic(d) {
      return d.scriptAttrs || !d.headers && (d.crossDomain || // When dealing with JSONP (`s.dataTypes` include "json" then)
      // don't use a script tag so that error responses still may have
      // `responseJSON` set. Continue using a script tag for JSONP requests that:
      //   * are cross-domain as AJAX requests won't work without a CORS setup
      //   * have `scriptAttrs` set as that's a script-only functionality
      // Note that this means JSONP requests violate strict CSP script-src settings.
      // A proper solution is to migrate from using JSONP to a CORS setup.
      d.async && y.inArray("json", d.dataTypes) < 0);
    }
    y.ajaxSetup({
      accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
      },
      converters: {
        "text script": function(d) {
          return y.globalEval(d), d;
        }
      }
    }), y.ajaxPrefilter("script", function(d) {
      d.cache === void 0 && (d.cache = !1), ic(d) && (d.type = "GET");
    }), y.ajaxTransport("script", function(d) {
      if (ic(d)) {
        var _, x;
        return {
          send: function(D, O) {
            _ = y("<script>").attr(d.scriptAttrs || {}).prop({ charset: d.scriptCharset, src: d.url }).on("load error", x = function(N) {
              _.remove(), x = null, N && O(N.type === "error" ? 404 : 200, N.type);
            }), p.head.appendChild(_[0]);
          },
          abort: function() {
            x && x();
          }
        };
      }
    });
    var sc = [], Eo = /(=)\?(?=&|$)|\?\?/;
    y.ajaxSetup({
      jsonp: "callback",
      jsonpCallback: function() {
        var d = sc.pop() || y.expando + "_" + Ju.guid++;
        return this[d] = !0, d;
      }
    }), y.ajaxPrefilter("jsonp", function(d, _, x) {
      var D, O, N, B = d.jsonp !== !1 && (Eo.test(d.url) ? "url" : typeof d.data == "string" && (d.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && Eo.test(d.data) && "data");
      return D = d.jsonpCallback = typeof d.jsonpCallback == "function" ? d.jsonpCallback() : d.jsonpCallback, B ? d[B] = d[B].replace(Eo, "$1" + D) : d.jsonp !== !1 && (d.url += (Co.test(d.url) ? "&" : "?") + d.jsonp + "=" + D), d.converters["script json"] = function() {
        return N || y.error(D + " was not called"), N[0];
      }, d.dataTypes[0] = "json", O = n[D], n[D] = function() {
        N = arguments;
      }, x.always(function() {
        O === void 0 ? y(n).removeProp(D) : n[D] = O, d[D] && (d.jsonpCallback = _.jsonpCallback, sc.push(D)), N && typeof O == "function" && O(N[0]), N = O = void 0;
      }), "script";
    }), y.ajaxPrefilter(function(d, _) {
      typeof d.data != "string" && !y.isPlainObject(d.data) && !Array.isArray(d.data) && // Don't disable data processing if explicitly set by the user.
      !("processData" in _) && (d.processData = !1), d.data instanceof n.FormData && (d.contentType = !1);
    }), y.parseHTML = function(d, _, x) {
      if (typeof d != "string" && !Fs(d + ""))
        return [];
      typeof _ == "boolean" && (x = _, _ = !1);
      var D, O;
      return _ || (_ = new n.DOMParser().parseFromString("", "text/html")), D = Gr.exec(d), O = !x && [], D ? [_.createElement(D[1])] : (D = Nu([d], _, O), O && O.length && y(O).remove(), y.merge([], D.childNodes));
    }, y.fn.load = function(d, _, x) {
      var D, O, N, B = this, K = d.indexOf(" ");
      return K > -1 && (D = yr(d.slice(K)), d = d.slice(0, K)), typeof _ == "function" ? (x = _, _ = void 0) : _ && typeof _ == "object" && (O = "POST"), B.length > 0 && y.ajax({
        url: d,
        // If "type" variable is undefined, then "GET" method will be used.
        // Make value of this field explicit since
        // user can override it through ajaxSetup method
        type: O || "GET",
        dataType: "html",
        data: _
      }).done(function(U) {
        N = arguments, B.html(D ? (
          // If a selector was specified, locate the right elements in a dummy div
          // Exclude scripts to avoid IE 'Permission Denied' errors
          y("<div>").append(y.parseHTML(U)).find(D)
        ) : (
          // Otherwise use the full result
          U
        ));
      }).always(x && function(U, Z) {
        B.each(function() {
          x.apply(this, N || [U.responseText, Z, U]);
        });
      }), this;
    }, y.expr.pseudos.animated = function(d) {
      return y.grep(y.timers, function(_) {
        return d === _.elem;
      }).length;
    }, y.offset = {
      setOffset: function(d, _, x) {
        var D, O, N, B, K, U, Z, re = y.css(d, "position"), le = y(d), ne = {};
        re === "static" && (d.style.position = "relative"), K = le.offset(), N = y.css(d, "top"), U = y.css(d, "left"), Z = (re === "absolute" || re === "fixed") && (N + U).indexOf("auto") > -1, Z ? (D = le.position(), B = D.top, O = D.left) : (B = parseFloat(N) || 0, O = parseFloat(U) || 0), typeof _ == "function" && (_ = _.call(d, x, y.extend({}, K))), _.top != null && (ne.top = _.top - K.top + B), _.left != null && (ne.left = _.left - K.left + O), "using" in _ ? _.using.call(d, ne) : le.css(ne);
      }
    }, y.fn.extend({
      // offset() relates an element's border box to the document origin
      offset: function(d) {
        if (arguments.length)
          return d === void 0 ? this : this.each(function(O) {
            y.offset.setOffset(this, d, O);
          });
        var _, x, D = this[0];
        if (D)
          return D.getClientRects().length ? (_ = D.getBoundingClientRect(), x = D.ownerDocument.defaultView, {
            top: _.top + x.pageYOffset,
            left: _.left + x.pageXOffset
          }) : { top: 0, left: 0 };
      },
      // position() relates an element's margin box to its offset parent's padding box
      // This corresponds to the behavior of CSS absolute positioning
      position: function() {
        if (this[0]) {
          var d, _, x, D = this[0], O = { top: 0, left: 0 };
          if (y.css(D, "position") === "fixed")
            _ = D.getBoundingClientRect();
          else {
            for (_ = this.offset(), x = D.ownerDocument, d = D.offsetParent || x.documentElement; d && d !== x.documentElement && y.css(d, "position") === "static"; )
              d = d.offsetParent || x.documentElement;
            d && d !== D && d.nodeType === 1 && y.css(d, "position") !== "static" && (O = y(d).offset(), O.top += y.css(d, "borderTopWidth", !0), O.left += y.css(d, "borderLeftWidth", !0));
          }
          return {
            top: _.top - O.top - y.css(D, "marginTop", !0),
            left: _.left - O.left - y.css(D, "marginLeft", !0)
          };
        }
      },
      // This method will return documentElement in the following cases:
      // 1) For the element inside the iframe without offsetParent, this method will return
      //    documentElement of the parent window
      // 2) For the hidden or detached element
      // 3) For body or html element, i.e. in case of the html node - it will return itself
      //
      // but those exceptions were never presented as a real life use-cases
      // and might be considered as more preferable results.
      //
      // This logic, however, is not guaranteed and can change at any point in the future
      offsetParent: function() {
        return this.map(function() {
          for (var d = this.offsetParent; d && y.css(d, "position") === "static"; )
            d = d.offsetParent;
          return d || M;
        });
      }
    }), y.each({ scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function(d, _) {
      var x = _ === "pageYOffset";
      y.fn[d] = function(D) {
        return He(this, function(O, N, B) {
          var K;
          if (h(O) ? K = O : O.nodeType === 9 && (K = O.defaultView), B === void 0)
            return K ? K[_] : O[N];
          K ? K.scrollTo(
            x ? K.pageXOffset : B,
            x ? B : K.pageYOffset
          ) : O[N] = B;
        }, d, D, arguments.length);
      };
    }), y.each({ Height: "height", Width: "width" }, function(d, _) {
      y.each({
        padding: "inner" + d,
        content: _,
        "": "outer" + d
      }, function(x, D) {
        y.fn[D] = function(O, N) {
          var B = arguments.length && (x || typeof O != "boolean"), K = x || (O === !0 || N === !0 ? "margin" : "border");
          return He(this, function(U, Z, re) {
            var le;
            return h(U) ? D.indexOf("outer") === 0 ? U["inner" + d] : U.document.documentElement["client" + d] : U.nodeType === 9 ? (le = U.documentElement, Math.max(
              U.body["scroll" + d],
              le["scroll" + d],
              U.body["offset" + d],
              le["offset" + d],
              le["client" + d]
            )) : re === void 0 ? (
              // Get width or height on the element, requesting but not forcing parseFloat
              y.css(U, Z, K)
            ) : (
              // Set width or height on the element
              y.style(U, Z, re, K)
            );
          }, _, B ? O : void 0, B);
        };
      });
    }), y.each([
      "ajaxStart",
      "ajaxStop",
      "ajaxComplete",
      "ajaxError",
      "ajaxSuccess",
      "ajaxSend"
    ], function(d, _) {
      y.fn[_] = function(x) {
        return this.on(_, x);
      };
    }), y.fn.extend({
      bind: function(d, _, x) {
        return this.on(d, null, _, x);
      },
      unbind: function(d, _) {
        return this.off(d, null, _);
      },
      delegate: function(d, _, x, D) {
        return this.on(_, d, x, D);
      },
      undelegate: function(d, _, x) {
        return arguments.length === 1 ? this.off(d, "**") : this.off(_, d || "**", x);
      },
      hover: function(d, _) {
        return this.on("mouseenter", d).on("mouseleave", _ || d);
      }
    }), y.each(
      "blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu".split(" "),
      function(d, _) {
        y.fn[_] = function(x, D) {
          return arguments.length > 0 ? this.on(_, null, x, D) : this.trigger(_);
        };
      }
    ), y.proxy = function(d, _) {
      var x, D, O;
      if (typeof _ == "string" && (x = d[_], _ = d, d = x), typeof d == "function")
        return D = s.call(arguments, 2), O = function() {
          return d.apply(_ || this, D.concat(s.call(arguments)));
        }, O.guid = d.guid = d.guid || y.guid++, O;
    }, y.holdReady = function(d) {
      d ? y.readyWait++ : y.ready(!0);
    }, y.expr[":"] = y.expr.filters = y.expr.pseudos;
    var Lp = n.jQuery, Mp = n.$;
    return y.noConflict = function(d) {
      return n.$ === y && (n.$ = Mp), d && n.jQuery === y && (n.jQuery = Lp), y;
    }, typeof e > "u" && (n.jQuery = n.$ = y), y;
  });
})(kd);
var wa = kd.exports;
const lt = /* @__PURE__ */ wn(wa);
/*! DataTables 2.3.7
 * © SpryMedia Ltd - datatables.net/license
 */
var q = lt, H = function(t, n) {
  if (H.factory(t, n))
    return H;
  if (this instanceof H)
    return q(t).DataTable(n);
  n = t;
  var e = this, r = n === void 0, i = this.length;
  return r && (n = {}), this.api = function() {
    return new We(this);
  }, this.each(function() {
    var s = {}, a = i > 1 ? (
      // optimisation for single table case
      mc(s, n, !0)
    ) : n, o = 0, l, c = this.getAttribute("id"), f = H.defaults, g = q(this);
    if (this.nodeName.toLowerCase() != "table") {
      mn(null, 0, "Non-table node initialisation (" + this.nodeName + ")", 2);
      return;
    }
    a.on && a.on.options && vc(g, "options", a.on.options), g.trigger("options.dt", a), lc(f), Rd(f.column), rr(f, f, !0), rr(f.column, f.column, !0), rr(f, q.extend(a, Qd(g.data())), !0);
    var b = H.settings;
    for (o = 0, l = b.length; o < l; o++) {
      var w = b[o];
      if (w.nTable == this || w.nTHead && w.nTHead.parentNode == this || w.nTFoot && w.nTFoot.parentNode == this) {
        var C = a.bRetrieve !== void 0 ? a.bRetrieve : f.bRetrieve, m = a.bDestroy !== void 0 ? a.bDestroy : f.bDestroy;
        if (r || C)
          return w.oInstance;
        if (m) {
          new H.Api(w).destroy();
          break;
        } else {
          mn(w, 0, "Cannot reinitialise DataTable", 3);
          return;
        }
      }
      if (w.sTableId == this.id) {
        b.splice(o, 1);
        break;
      }
    }
    (c === null || c === "") && (c = "DataTables_Table_" + H.ext._unique++, this.id = c), g.children("colgroup").remove();
    var h = q.extend(!0, {}, H.models.oSettings, {
      sDestroyWidth: g[0].style.width,
      sInstance: c,
      sTableId: c,
      colgroup: q("<colgroup>"),
      fastData: function(R, L, E) {
        return _n(h, R, L, E);
      }
    });
    h.nTable = this, h.oInit = a, b.push(h), h.api = new We(h), h.oInstance = e.length === 1 ? e : g.dataTable(), lc(a), a.aLengthMenu && !a.iDisplayLength && (a.iDisplayLength = Array.isArray(a.aLengthMenu[0]) ? a.aLengthMenu[0][0] : q.isPlainObject(a.aLengthMenu[0]) ? a.aLengthMenu[0].value : a.aLengthMenu[0]), a = mc(q.extend(!0, {}, f), a), sr(h.oFeatures, a, [
      "bPaginate",
      "bLengthChange",
      "bFilter",
      "bSort",
      "bSortMulti",
      "bInfo",
      "bProcessing",
      "bAutoWidth",
      "bSortClasses",
      "bServerSide",
      "bDeferRender"
    ]), sr(h, a, [
      "ajax",
      "fnFormatNumber",
      "sServerMethod",
      "aaSorting",
      "aaSortingFixed",
      "aLengthMenu",
      "sPaginationType",
      "iStateDuration",
      "bSortCellsTop",
      "iTabIndex",
      "sDom",
      "fnStateLoadCallback",
      "fnStateSaveCallback",
      "renderer",
      "searchDelay",
      "rowId",
      "caption",
      "layout",
      "orderDescReverse",
      "orderIndicators",
      "orderHandler",
      "titleRow",
      "typeDetect",
      "columnTitleTag",
      ["iCookieDuration", "iStateDuration"],
      // backwards compat
      ["oSearch", "oPreviousSearch"],
      ["aoSearchCols", "aoPreSearchCols"],
      ["iDisplayLength", "_iDisplayLength"]
    ]), sr(h.oScroll, a, [
      ["sScrollX", "sX"],
      ["sScrollXInner", "sXInner"],
      ["sScrollY", "sY"],
      ["bScrollCollapse", "bCollapse"]
    ]), sr(h.oLanguage, a, "fnInfoCallback"), hn(h, "aoDrawCallback", a.fnDrawCallback), hn(h, "aoStateSaveParams", a.fnStateSaveParams), hn(h, "aoStateLoadParams", a.fnStateLoadParams), hn(h, "aoStateLoaded", a.fnStateLoaded), hn(h, "aoRowCallback", a.fnRowCallback), hn(h, "aoRowCreatedCallback", a.fnCreatedRow), hn(h, "aoHeaderCallback", a.fnHeaderCallback), hn(h, "aoFooterCallback", a.fnFooterCallback), hn(h, "aoInitComplete", a.fnInitComplete), hn(h, "aoPreDrawCallback", a.fnPreDrawCallback), h.rowIdFn = Ci(a.rowId), a.on && Object.keys(a.on).forEach(function(R) {
      vc(g, R, a.on[R]);
    }), jp(h);
    var u = h.oClasses;
    q.extend(u, H.ext.classes, a.oClasses), g.addClass(u.table), h.oFeatures.bPaginate || (a.iDisplayStart = 0), h.iInitDisplayStart === void 0 && (h.iInitDisplayStart = a.iDisplayStart, h._iDisplayStart = a.iDisplayStart);
    var p = a.iDeferLoading;
    if (p !== null) {
      h.deferLoading = !0;
      var v = Array.isArray(p);
      h._iRecordsDisplay = v ? p[0] : p, h._iRecordsTotal = v ? p[1] : p;
    }
    var S = [], T = this.getElementsByTagName("thead"), A = Vd(h, T[0]);
    if (a.aoColumns)
      S = a.aoColumns;
    else if (A.length)
      for (o = 0, l = A[0].length; o < l; o++)
        S.push(null);
    for (o = 0, l = S.length; o < l; o++)
      Ld(h);
    qp(h, a.aoColumnDefs, S, A, function(R, L) {
      pl(h, R, L);
    });
    var y = g.children("tbody").find("tr:first-child").eq(0);
    if (y.length) {
      var I = function(R, L) {
        return R.getAttribute("data-" + L) !== null ? L : null;
      };
      q(y[0]).children("th, td").each(function(R, L) {
        var E = h.aoColumns[R];
        if (E || mn(h, 0, "Incorrect column count", 18), E.mData === R) {
          var M = I(L, "sort") || I(L, "order"), W = I(L, "filter") || I(L, "search");
          (M !== null || W !== null) && (E.mData = {
            _: R + ".display",
            sort: M !== null ? R + ".@data-" + M : void 0,
            type: M !== null ? R + ".@data-" + M : void 0,
            filter: W !== null ? R + ".@data-" + W : void 0
          }, E._isArrayHost = !0, pl(h, R));
        }
      });
    }
    hn(h, "aoDrawCallback", Ya);
    var $ = h.oFeatures;
    if (a.bStateSave && ($.bStateSave = !0), a.aaSorting === void 0) {
      var V = h.aaSorting;
      for (o = 0, l = V.length; o < l; o++)
        V[o][1] = h.aoColumns[o].asSorting[0];
    }
    vl(h), hn(h, "aoDrawCallback", function() {
      (h.bSorted || an(h) === "ssp" || $.bDeferRender) && vl(h);
    });
    var z = g.children("caption");
    h.caption && (z.length === 0 && (z = q("<caption/>").prependTo(g)), z.html(h.caption)), z.length && (z[0]._captionSide = z.css("caption-side"), h.captionNode = z[0]), z.length ? h.colgroup.insertAfter(z) : h.colgroup.prependTo(h.nTable), T.length === 0 && (T = q("<thead/>").appendTo(g)), h.nTHead = T[0];
    var F = g.children("tbody");
    F.length === 0 && (F = q("<tbody/>").insertAfter(T)), h.nTBody = F[0];
    var P = g.children("tfoot");
    P.length === 0 && (P = q("<tfoot/>").appendTo(g)), h.nTFoot = P[0], h.aiDisplay = h.aiDisplayMaster.slice(), h.bInitialised = !0;
    var k = h.oLanguage;
    q.extend(!0, k, a.oLanguage), k.sUrl ? q.ajax({
      dataType: "json",
      url: k.sUrl,
      success: function(R) {
        rr(f.oLanguage, R), q.extend(!0, k, R, h.oInit.oLanguage), qe(h, null, "i18n", [h], !0), fa(h);
      },
      error: function() {
        mn(h, 0, "i18n file loading error", 21), fa(h);
      }
    }) : (qe(h, null, "i18n", [h], !0), fa(h));
  }), e = null, this;
};
H.ext = _t = {
  /**
   * DataTables build type (expanded by the download builder)
   *
   *  @type string
   */
  builder: "-source-",
  /**
   * Buttons. For use with the Buttons extension for DataTables. This is
   * defined here so other extensions can define buttons regardless of load
   * order. It is _not_ used by DataTables core.
   *
   *  @type object
   *  @default {}
   */
  buttons: {},
  /**
   * ColumnControl buttons and content
   *
   *  @type object
   */
  ccContent: {},
  /**
   * Element class names
   *
   *  @type object
   *  @default {}
   */
  classes: {},
  /**
   * Error reporting.
   * 
   * How should DataTables report an error. Can take the value 'alert',
   * 'throw', 'none' or a function.
   *
   *  @type string|function
   *  @default alert
   */
  errMode: "alert",
  /** HTML entity escaping */
  escape: {
    /** When reading data-* attributes for initialisation options */
    attributes: !1
  },
  /**
   * Legacy so v1 plug-ins don't throw js errors on load
   */
  feature: [],
  /**
   * Feature plug-ins.
   * 
   * This is an object of callbacks which provide the features for DataTables
   * to be initialised via the `layout` option.
   */
  features: {},
  /**
   * Row searching.
   * 
   * This method of searching is complimentary to the default type based
   * searching, and a lot more comprehensive as it allows you complete control
   * over the searching logic. Each element in this array is a function
   * (parameters described below) that is called for every row in the table,
   * and your logic decides if it should be included in the searching data set
   * or not.
   *
   * Searching functions have the following input parameters:
   *
   * 1. `{object}` DataTables settings object: see
   *    {@link DataTable.models.oSettings}
   * 2. `{array|object}` Data for the row to be processed (same as the
   *    original format that was passed in as the data source, or an array
   *    from a DOM data source
   * 3. `{int}` Row index ({@link DataTable.models.oSettings.aoData}), which
   *    can be useful to retrieve the `TR` element if you need DOM interaction.
   *
   * And the following return is expected:
   *
   * * {boolean} Include the row in the searched result set (true) or not
   *   (false)
   *
   * Note that as with the main search ability in DataTables, technically this
   * is "filtering", since it is subtractive. However, for consistency in
   * naming we call it searching here.
   *
   *  @type array
   *  @default []
   *
   *  @example
   *    // The following example shows custom search being applied to the
   *    // fourth column (i.e. the data[3] index) based on two input values
   *    // from the end-user, matching the data in a certain range.
   *    $.fn.dataTable.ext.search.push(
   *      function( settings, data, dataIndex ) {
   *        var min = document.getElementById('min').value * 1;
   *        var max = document.getElementById('max').value * 1;
   *        var version = data[3] == "-" ? 0 : data[3]*1;
   *
   *        if ( min == "" && max == "" ) {
   *          return true;
   *        }
   *        else if ( min == "" && version < max ) {
   *          return true;
   *        }
   *        else if ( min < version && "" == max ) {
   *          return true;
   *        }
   *        else if ( min < version && version < max ) {
   *          return true;
   *        }
   *        return false;
   *      }
   *    );
   */
  search: [],
  /**
   * Selector extensions
   *
   * The `selector` option can be used to extend the options available for the
   * selector modifier options (`selector-modifier` object data type) that
   * each of the three built in selector types offer (row, column and cell +
   * their plural counterparts). For example the Select extension uses this
   * mechanism to provide an option to select only rows, columns and cells
   * that have been marked as selected by the end user (`{selected: true}`),
   * which can be used in conjunction with the existing built in selector
   * options.
   *
   * Each property is an array to which functions can be pushed. The functions
   * take three attributes:
   *
   * * Settings object for the host table
   * * Options object (`selector-modifier` object type)
   * * Array of selected item indexes
   *
   * The return is an array of the resulting item indexes after the custom
   * selector has been applied.
   *
   *  @type object
   */
  selector: {
    cell: [],
    column: [],
    row: []
  },
  /**
   * Legacy configuration options. Enable and disable legacy options that
   * are available in DataTables.
   *
   *  @type object
   */
  legacy: {
    /**
     * Enable / disable DataTables 1.9 compatible server-side processing
     * requests
     *
     *  @type boolean
     *  @default null
     */
    ajax: null
  },
  /**
   * Pagination plug-in methods.
   * 
   * Each entry in this object is a function and defines which buttons should
   * be shown by the pagination rendering method that is used for the table:
   * {@link DataTable.ext.renderer.pageButton}. The renderer addresses how the
   * buttons are displayed in the document, while the functions here tell it
   * what buttons to display. This is done by returning an array of button
   * descriptions (what each button will do).
   *
   * Pagination types (the four built in options and any additional plug-in
   * options defined here) can be used through the `paginationType`
   * initialisation parameter.
   *
   * The functions defined take two parameters:
   *
   * 1. `{int} page` The current page index
   * 2. `{int} pages` The number of pages in the table
   *
   * Each function is expected to return an array where each element of the
   * array can be one of:
   *
   * * `first` - Jump to first page when activated
   * * `last` - Jump to last page when activated
   * * `previous` - Show previous page when activated
   * * `next` - Show next page when activated
   * * `{int}` - Show page of the index given
   * * `{array}` - A nested array containing the above elements to add a
   *   containing 'DIV' element (might be useful for styling).
   *
   * Note that DataTables v1.9- used this object slightly differently whereby
   * an object with two functions would be defined for each plug-in. That
   * ability is still supported by DataTables 1.10+ to provide backwards
   * compatibility, but this option of use is now decremented and no longer
   * documented in DataTables 1.10+.
   *
   *  @type object
   *  @default {}
   *
   *  @example
   *    // Show previous, next and current page buttons only
   *    $.fn.dataTableExt.oPagination.current = function ( page, pages ) {
   *      return [ 'previous', page, 'next' ];
   *    };
   */
  pager: {},
  renderer: {
    pageButton: {},
    header: {}
  },
  /**
   * Ordering plug-ins - custom data source
   * 
   * The extension options for ordering of data available here is complimentary
   * to the default type based ordering that DataTables typically uses. It
   * allows much greater control over the data that is being used to
   * order a column, but is necessarily therefore more complex.
   * 
   * This type of ordering is useful if you want to do ordering based on data
   * live from the DOM (for example the contents of an 'input' element) rather
   * than just the static string that DataTables knows of.
   * 
   * The way these plug-ins work is that you create an array of the values you
   * wish to be ordering for the column in question and then return that
   * array. The data in the array much be in the index order of the rows in
   * the table (not the currently ordering order!). Which order data gathering
   * function is run here depends on the `dt-init columns.orderDataType`
   * parameter that is used for the column (if any).
   *
   * The functions defined take two parameters:
   *
   * 1. `{object}` DataTables settings object: see
   *    {@link DataTable.models.oSettings}
   * 2. `{int}` Target column index
   *
   * Each function is expected to return an array:
   *
   * * `{array}` Data for the column to be ordering upon
   *
   *  @type array
   *
   *  @example
   *    // Ordering using `input` node values
   *    $.fn.dataTable.ext.order['dom-text'] = function  ( settings, col )
   *    {
   *      return this.api().column( col, {order:'index'} ).nodes().map( function ( td, i ) {
   *        return $('input', td).val();
   *      } );
   *    }
   */
  order: {},
  /**
   * Type based plug-ins.
   *
   * Each column in DataTables has a type assigned to it, either by automatic
   * detection or by direct assignment using the `type` option for the column.
   * The type of a column will effect how it is ordering and search (plug-ins
   * can also make use of the column type if required).
   *
   * @namespace
   */
  type: {
    /**
     * Automatic column class assignment
     */
    className: {},
    /**
     * Type detection functions.
     *
     * The functions defined in this object are used to automatically detect
     * a column's type, making initialisation of DataTables super easy, even
     * when complex data is in the table.
     *
     * The functions defined take two parameters:
     *
        *  1. `{*}` Data from the column cell to be analysed
        *  2. `{settings}` DataTables settings object. This can be used to
        *     perform context specific type detection - for example detection
        *     based on language settings such as using a comma for a decimal
        *     place. Generally speaking the options from the settings will not
        *     be required
     *
     * Each function is expected to return:
     *
     * * `{string|null}` Data type detected, or null if unknown (and thus
     *   pass it on to the other type detection functions.
     *
     *  @type array
     *
     *  @example
     *    // Currency type detection plug-in:
     *    $.fn.dataTable.ext.type.detect.push(
     *      function ( data, settings ) {
     *        // Check the numeric part
     *        if ( ! data.substring(1).match(/[0-9]/) ) {
     *          return null;
     *        }
     *
     *        // Check prefixed by currency
     *        if ( data.charAt(0) == '$' || data.charAt(0) == '&pound;' ) {
     *          return 'currency';
     *        }
     *        return null;
     *      }
     *    );
     */
    detect: [],
    /**
     * Automatic renderer assignment
     */
    render: {},
    /**
     * Type based search formatting.
     *
     * The type based searching functions can be used to pre-format the
     * data to be search on. For example, it can be used to strip HTML
     * tags or to de-format telephone numbers for numeric only searching.
     *
     * Note that is a search is not defined for a column of a given type,
     * no search formatting will be performed.
     * 
     * Pre-processing of searching data plug-ins - When you assign the sType
     * for a column (or have it automatically detected for you by DataTables
     * or a type detection plug-in), you will typically be using this for
     * custom sorting, but it can also be used to provide custom searching
     * by allowing you to pre-processing the data and returning the data in
     * the format that should be searched upon. This is done by adding
     * functions this object with a parameter name which matches the sType
     * for that target column. This is the corollary of <i>afnSortData</i>
     * for searching data.
     *
     * The functions defined take a single parameter:
     *
        *  1. `{*}` Data from the column cell to be prepared for searching
     *
     * Each function is expected to return:
     *
     * * `{string|null}` Formatted string that will be used for the searching.
     *
     *  @type object
     *  @default {}
     *
     *  @example
     *    $.fn.dataTable.ext.type.search['title-numeric'] = function ( d ) {
     *      return d.replace(/\n/g," ").replace( /<.*?>/g, "" );
     *    }
     */
    search: {},
    /**
     * Type based ordering.
     *
     * The column type tells DataTables what ordering to apply to the table
     * when a column is sorted upon. The order for each type that is defined,
     * is defined by the functions available in this object.
     *
     * Each ordering option can be described by three properties added to
     * this object:
     *
     * * `{type}-pre` - Pre-formatting function
     * * `{type}-asc` - Ascending order function
     * * `{type}-desc` - Descending order function
     *
     * All three can be used together, only `{type}-pre` or only
     * `{type}-asc` and `{type}-desc` together. It is generally recommended
     * that only `{type}-pre` is used, as this provides the optimal
     * implementation in terms of speed, although the others are provided
     * for compatibility with existing JavaScript sort functions.
     *
     * `{type}-pre`: Functions defined take a single parameter:
     *
        *  1. `{*}` Data from the column cell to be prepared for ordering
     *
     * And return:
     *
     * * `{*}` Data to be sorted upon
     *
     * `{type}-asc` and `{type}-desc`: Functions are typical JavaScript sort
     * functions, taking two parameters:
     *
        *  1. `{*}` Data to compare to the second parameter
        *  2. `{*}` Data to compare to the first parameter
     *
     * And returning:
     *
     * * `{*}` Ordering match: <0 if first parameter should be sorted lower
     *   than the second parameter, ===0 if the two parameters are equal and
     *   >0 if the first parameter should be sorted height than the second
     *   parameter.
     * 
     *  @type object
     *  @default {}
     *
     *  @example
     *    // Numeric ordering of formatted numbers with a pre-formatter
     *    $.extend( $.fn.dataTable.ext.type.order, {
     *      "string-pre": function(x) {
     *        a = (a === "-" || a === "") ? 0 : a.replace( /[^\d\-\.]/g, "" );
     *        return parseFloat( a );
     *      }
     *    } );
     *
     *  @example
     *    // Case-sensitive string ordering, with no pre-formatting method
     *    $.extend( $.fn.dataTable.ext.order, {
     *      "string-case-asc": function(x,y) {
     *        return ((x < y) ? -1 : ((x > y) ? 1 : 0));
     *      },
     *      "string-case-desc": function(x,y) {
     *        return ((x < y) ? 1 : ((x > y) ? -1 : 0));
     *      }
     *    } );
     */
    order: {}
  },
  /**
   * Unique DataTables instance counter
   *
   * @type int
   * @private
   */
  _unique: 0,
  //
  // Depreciated
  // The following properties are retained for backwards compatibility only.
  // The should not be used in new projects and will be removed in a future
  // version
  //
  /**
   * Version check function.
   *  @type function
   *  @depreciated Since 1.10
   */
  fnVersionCheck: H.fnVersionCheck,
  /**
   * Index for what 'this' index API functions should use
   *  @type int
   *  @deprecated Since v1.10
   */
  iApiIndex: 0,
  /**
   * Software version
   *  @type string
   *  @deprecated Since v1.10
   */
  sVersion: H.version
};
q.extend(_t, {
  afnFiltering: _t.search,
  aTypes: _t.type.detect,
  ofnSearch: _t.type.search,
  oSort: _t.type.order,
  afnSortData: _t.order,
  aoFeatures: _t.feature,
  oStdClasses: _t.classes,
  oPagination: _t.pager
});
q.extend(H.ext.classes, {
  container: "dt-container",
  empty: {
    row: "dt-empty"
  },
  info: {
    container: "dt-info"
  },
  layout: {
    row: "dt-layout-row",
    cell: "dt-layout-cell",
    tableRow: "dt-layout-table",
    tableCell: "",
    start: "dt-layout-start",
    end: "dt-layout-end",
    full: "dt-layout-full"
  },
  length: {
    container: "dt-length",
    select: "dt-input"
  },
  order: {
    canAsc: "dt-orderable-asc",
    canDesc: "dt-orderable-desc",
    isAsc: "dt-ordering-asc",
    isDesc: "dt-ordering-desc",
    none: "dt-orderable-none",
    position: "sorting_"
  },
  processing: {
    container: "dt-processing"
  },
  scrolling: {
    body: "dt-scroll-body",
    container: "dt-scroll",
    footer: {
      self: "dt-scroll-foot",
      inner: "dt-scroll-footInner"
    },
    header: {
      self: "dt-scroll-head",
      inner: "dt-scroll-headInner"
    }
  },
  search: {
    container: "dt-search",
    input: "dt-input"
  },
  table: "dataTable",
  tbody: {
    cell: "",
    row: ""
  },
  thead: {
    cell: "",
    row: ""
  },
  tfoot: {
    cell: "",
    row: ""
  },
  paging: {
    active: "current",
    button: "dt-paging-button",
    container: "dt-paging",
    disabled: "disabled",
    nav: ""
  }
});
var _t, We, _e, Qe, ko = {}, Bp = /[\r\n\u2028]/g, Rl = /<([^>]*>)/g, Hp = Math.pow(2, 28), oc = /^\d{2,4}[./-]\d{1,2}[./-]\d{1,2}([T ]{1}\d{1,2}[:.]\d{2}([.:]\d{2})?)?$/, $p = new RegExp("(\\" + ["/", ".", "*", "+", "?", "|", "(", ")", "[", "]", "{", "}", "\\", "$", "^", "-"].join("|\\") + ")", "g"), Ll = /['\u00A0,$£€¥%\u2009\u202F\u20BD\u20a9\u20BArfkɃΞ]/gi, nn = function(t) {
  return !t || t === !0 || t === "-";
}, Pd = function(t) {
  var n = parseInt(t, 10);
  return !isNaN(n) && isFinite(t) ? n : null;
}, Od = function(t, n) {
  return ko[n] || (ko[n] = new RegExp(ml(n), "g")), typeof t == "string" && n !== "." ? t.replace(/\./g, "").replace(ko[n], ".") : t;
}, hs = function(t, n, e, r) {
  var i = typeof t, s = i === "string";
  return i === "number" || i === "bigint" || r && nn(t) ? !0 : (n && s && (t = Od(t, n)), e && s && (t = t.replace(Ll, "")), !isNaN(parseFloat(t)) && isFinite(t));
}, Vp = function(t) {
  return nn(t) || typeof t == "string";
}, _a = function(t, n, e, r) {
  if (r && nn(t))
    return !0;
  if (typeof t == "string" && t.match(/<(input|select)/i))
    return null;
  var i = Vp(t);
  return i && hs(jn(t), n, e, r) ? !0 : null;
}, kt = function(t, n, e) {
  var r = [], i = 0, s = t.length;
  if (e !== void 0)
    for (; i < s; i++)
      t[i] && t[i][n] && r.push(t[i][n][e]);
  else
    for (; i < s; i++)
      t[i] && r.push(t[i][n]);
  return r;
}, bs = function(t, n, e, r) {
  var i = [], s = 0, a = n.length;
  if (r !== void 0)
    for (; s < a; s++)
      t[n[s]] && t[n[s]][e] && i.push(t[n[s]][e][r]);
  else
    for (; s < a; s++)
      t[n[s]] && i.push(t[n[s]][e]);
  return i;
}, tr = function(t, n) {
  var e = [], r;
  n === void 0 ? (n = 0, r = t) : (r = n, n = t);
  for (var i = n; i < r; i++)
    e.push(i);
  return e;
}, Id = function(t) {
  for (var n = [], e = 0, r = t.length; e < r; e++)
    t[e] && n.push(t[e]);
  return n;
}, jn = function(t, n) {
  if (!t || typeof t != "string")
    return t;
  if (t.length > Hp)
    throw new Error("Exceeded max str len");
  var e;
  t = t.replace(Rl, n || "");
  do
    e = t, t = t.replace(/<script/i, "");
  while (t !== e);
  return e;
}, Or = function(t) {
  return Array.isArray(t) && (t = t.join(",")), typeof t == "string" ? t.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;") : t;
}, Ca = function(t, n) {
  if (typeof t != "string")
    return t;
  var e = t.normalize ? t.normalize("NFD") : t;
  return e.length !== t.length ? (n === !0 ? t + " " : "") + e.replace(/[\u0300-\u036f]/g, "") : e;
}, Wp = function(t) {
  if (t.length < 2)
    return !0;
  for (var n = t.slice().sort(), e = n[0], r = 1, i = n.length; r < i; r++) {
    if (n[r] === e)
      return !1;
    e = n[r];
  }
  return !0;
}, Oi = function(t) {
  if (Array.from && Set)
    return Array.from(new Set(t));
  if (Wp(t))
    return t.slice();
  var n = [], e, r, i = t.length, s, a = 0;
  e:
    for (r = 0; r < i; r++) {
      for (e = t[r], s = 0; s < a; s++)
        if (n[s] === e)
          continue e;
      n.push(e), a++;
    }
  return n;
}, Nd = function(t, n) {
  if (Array.isArray(n))
    for (var e = 0; e < n.length; e++)
      Nd(t, n[e]);
  else
    t.push(n);
  return t;
};
function vi(t, n) {
  n && n.split(" ").forEach(function(e) {
    e && t.classList.add(e);
  });
}
H.util = {
  /**
   * Return a string with diacritic characters decomposed
   * @param {*} mixed Function or string to normalize
   * @param {*} both Return original string and the normalized string
   * @returns String or undefined
   */
  diacritics: function(t, n) {
    var e = typeof t;
    if (e !== "function")
      return Ca(t, n);
    Ca = t;
  },
  /**
   * Debounce a function
   *
   * @param {function} fn Function to be called
   * @param {integer} freq Call frequency in mS
   * @return {function} Wrapped function
   */
  debounce: function(t, n) {
    var e;
    return function() {
      var r = this, i = arguments;
      clearTimeout(e), e = setTimeout(function() {
        t.apply(r, i);
      }, n || 250);
    };
  },
  /**
   * Throttle the calls to a function. Arguments and context are maintained
   * for the throttled function.
   *
   * @param {function} fn Function to be called
   * @param {integer} freq Call frequency in mS
   * @return {function} Wrapped function
   */
  throttle: function(t, n) {
    var e = n !== void 0 ? n : 200, r, i;
    return function() {
      var s = this, a = +/* @__PURE__ */ new Date(), o = arguments;
      r && a < r + e ? (clearTimeout(i), i = setTimeout(function() {
        r = void 0, t.apply(s, o);
      }, e)) : (r = a, t.apply(s, o));
    };
  },
  /**
   * Escape a string such that it can be used in a regular expression
   *
   *  @param {string} val string to escape
   *  @returns {string} escaped string
   */
  escapeRegex: function(t) {
    return t.replace($p, "\\$1");
  },
  /**
   * Create a function that will write to a nested object or array
   * @param {*} source JSON notation string
   * @returns Write function
   */
  set: function(t) {
    if (q.isPlainObject(t))
      return H.util.set(t._);
    if (t === null)
      return function() {
      };
    if (typeof t == "function")
      return function(e, r, i) {
        t(e, "set", r, i);
      };
    if (typeof t == "string" && (t.indexOf(".") !== -1 || t.indexOf("[") !== -1 || t.indexOf("(") !== -1)) {
      var n = function(e, r, i) {
        for (var s = cc(i), a, o = s[s.length - 1], l, c, f, g, b = 0, w = s.length - 1; b < w; b++) {
          if (s[b] === "__proto__" || s[b] === "constructor")
            throw new Error("Cannot set prototype values");
          if (l = s[b].match(Xi), c = s[b].match(ii), l) {
            if (s[b] = s[b].replace(Xi, ""), e[s[b]] = [], a = s.slice(), a.splice(0, b + 1), g = a.join("."), Array.isArray(r))
              for (var C = 0, m = r.length; C < m; C++)
                f = {}, n(f, r[C], g), e[s[b]].push(f);
            else
              e[s[b]] = r;
            return;
          } else
            c && (s[b] = s[b].replace(ii, ""), e = e[s[b]](r));
          (e[s[b]] === null || e[s[b]] === void 0) && (e[s[b]] = {}), e = e[s[b]];
        }
        o.match(ii) ? e = e[o.replace(ii, "")](r) : e[o.replace(Xi, "")] = r;
      };
      return function(e, r) {
        return n(e, r, t);
      };
    } else
      return function(e, r) {
        e[t] = r;
      };
  },
  /**
   * Create a function that will read nested objects from arrays, based on JSON notation
   * @param {*} source JSON notation string
   * @returns Value read
   */
  get: function(t) {
    if (q.isPlainObject(t)) {
      var n = {};
      return q.each(t, function(r, i) {
        i && (n[r] = H.util.get(i));
      }), function(r, i, s, a) {
        var o = n[i] || n._;
        return o !== void 0 ? o(r, i, s, a) : r;
      };
    } else {
      if (t === null)
        return function(r) {
          return r;
        };
      if (typeof t == "function")
        return function(r, i, s, a) {
          return t(r, i, s, a);
        };
      if (typeof t == "string" && (t.indexOf(".") !== -1 || t.indexOf("[") !== -1 || t.indexOf("(") !== -1)) {
        var e = function(r, i, s) {
          var a, o, l, c;
          if (s !== "")
            for (var f = cc(s), g = 0, b = f.length; g < b; g++) {
              if (a = f[g].match(Xi), o = f[g].match(ii), a) {
                if (f[g] = f[g].replace(Xi, ""), f[g] !== "" && (r = r[f[g]]), l = [], f.splice(0, g + 1), c = f.join("."), Array.isArray(r))
                  for (var w = 0, C = r.length; w < C; w++)
                    l.push(e(r[w], i, c));
                var m = a[0].substring(1, a[0].length - 1);
                r = m === "" ? l : l.join(m);
                break;
              } else if (o) {
                f[g] = f[g].replace(ii, ""), r = r[f[g]]();
                continue;
              }
              if (r === null || r[f[g]] === null)
                return null;
              if (r === void 0 || r[f[g]] === void 0)
                return;
              r = r[f[g]];
            }
          return r;
        };
        return function(r, i) {
          return e(r, i, t);
        };
      } else
        return function(r) {
          return r[t];
        };
    }
  },
  stripHtml: function(t, n) {
    var e = typeof t;
    if (e === "function") {
      jn = t;
      return;
    } else if (e === "string")
      return jn(t, n);
    return t;
  },
  escapeHtml: function(t) {
    var n = typeof t;
    if (n === "function") {
      Or = t;
      return;
    } else if (n === "string" || Array.isArray(t))
      return Or(t);
    return t;
  },
  unique: Oi
};
function Ha(t) {
  var n = "a aa ai ao as b fn i m o s ", e, r, i = {};
  q.each(t, function(s) {
    e = s.match(/^([^A-Z]+?)([A-Z])/), e && n.indexOf(e[1] + " ") !== -1 && (r = s.replace(e[0], e[2].toLowerCase()), i[r] = s, e[1] === "o" && Ha(t[s]));
  }), t._hungarianMap = i;
}
function rr(t, n, e) {
  t._hungarianMap || Ha(t);
  var r;
  q.each(n, function(i) {
    r = t._hungarianMap[i], r !== void 0 && (e || n[r] === void 0) && (r.charAt(0) === "o" ? (n[r] || (n[r] = {}), q.extend(!0, n[r], n[i]), rr(t[r], n[r], e)) : n[r] = n[i]);
  });
}
var Yt = function(t, n, e) {
  t[n] !== void 0 && (t[e] = t[n]);
};
function lc(t) {
  Yt(t, "ordering", "bSort"), Yt(t, "orderMulti", "bSortMulti"), Yt(t, "orderClasses", "bSortClasses"), Yt(t, "orderCellsTop", "bSortCellsTop"), Yt(t, "order", "aaSorting"), Yt(t, "orderFixed", "aaSortingFixed"), Yt(t, "paging", "bPaginate"), Yt(t, "pagingType", "sPaginationType"), Yt(t, "pageLength", "iDisplayLength"), Yt(t, "searching", "bFilter"), typeof t.sScrollX == "boolean" && (t.sScrollX = t.sScrollX ? "100%" : ""), typeof t.scrollX == "boolean" && (t.scrollX = t.scrollX ? "100%" : ""), typeof t.bSort == "object" ? (t.orderIndicators = t.bSort.indicators !== void 0 ? t.bSort.indicators : !0, t.orderHandler = t.bSort.handler !== void 0 ? t.bSort.handler : !0, t.bSort = !0) : t.bSort === !1 ? (t.orderIndicators = !1, t.orderHandler = !1) : t.bSort === !0 && (t.orderIndicators = !0, t.orderHandler = !0), typeof t.bSortCellsTop == "boolean" && (t.titleRow = t.bSortCellsTop);
  var n = t.aoSearchCols;
  if (n)
    for (var e = 0, r = n.length; e < r; e++)
      n[e] && rr(H.models.oSearch, n[e]);
  t.serverSide && !t.searchDelay && (t.searchDelay = 400);
}
function Rd(t) {
  Yt(t, "orderable", "bSortable"), Yt(t, "orderData", "aDataSort"), Yt(t, "orderSequence", "asSorting"), Yt(t, "orderDataType", "sortDataType");
  var n = t.aDataSort;
  typeof n == "number" && !Array.isArray(n) && (t.aDataSort = [n]);
}
function jp(t) {
  if (!H.__browser) {
    var n = {};
    H.__browser = n;
    var e = q("<div/>").css({
      position: "fixed",
      top: 0,
      left: -1 * window.pageXOffset,
      // allow for scrolling
      height: 1,
      width: 1,
      overflow: "hidden"
    }).append(
      q("<div/>").css({
        position: "absolute",
        top: 1,
        left: 1,
        width: 100,
        overflow: "scroll"
      }).append(
        q("<div/>").css({
          width: "100%",
          height: 10
        })
      )
    ).appendTo("body"), r = e.children(), i = r.children();
    n.barWidth = r[0].offsetWidth - r[0].clientWidth, n.bScrollbarLeft = Math.round(i.offset().left) !== 1, e.remove();
  }
  q.extend(t.oBrowser, H.__browser), t.oScroll.iBarWidth = H.__browser.barWidth;
}
function Ld(t) {
  var n = H.defaults.column, e = t.aoColumns.length, r = q.extend({}, H.models.oColumn, n, {
    aDataSort: n.aDataSort ? n.aDataSort : [e],
    mData: n.mData ? n.mData : e,
    idx: e,
    searchFixed: {},
    colEl: q("<col>").attr("data-dt-column", e)
  });
  t.aoColumns.push(r);
  var i = t.aoPreSearchCols;
  i[e] = q.extend({}, H.models.oSearch, i[e]);
}
function pl(t, n, e) {
  var r = t.aoColumns[n];
  if (e != null) {
    Rd(e), rr(H.defaults.column, e, !0), e.mDataProp !== void 0 && !e.mData && (e.mData = e.mDataProp), e.sType && (r._sManualType = e.sType), e.className && !e.sClass && (e.sClass = e.className);
    var i = r.sClass;
    q.extend(r, e), sr(r, e, "sWidth", "sWidthOrig"), i !== r.sClass && (r.sClass = i + " " + r.sClass), e.iDataSort !== void 0 && (r.aDataSort = [e.iDataSort]), sr(r, e, "aDataSort");
  }
  var s = r.mData, a = Ci(s);
  if (r.mRender && Array.isArray(r.mRender)) {
    var o = r.mRender.slice(), l = o.shift();
    r.mRender = H.render[l].apply(window, o);
  }
  r._render = r.mRender ? Ci(r.mRender) : null;
  var c = function(f) {
    return typeof f == "string" && f.indexOf("@") !== -1;
  };
  r._bAttrSrc = q.isPlainObject(s) && (c(s.sort) || c(s.type) || c(s.filter)), r._setter = null, r.fnGetData = function(f, g, b) {
    var w = a(f, g, void 0, b);
    return r._render && g ? r._render(w, g, f, b) : w;
  }, r.fnSetData = function(f, g, b) {
    return Dr(s)(f, g, b);
  }, typeof s != "number" && !r._isArrayHost && (t._rowReadObject = !0), t.oFeatures.bSort || (r.bSortable = !1);
}
function $a(t) {
  og(t), zp(t);
  var n = t.oScroll;
  (n.sY !== "" || n.sX !== "") && Yd(t), qe(t, null, "column-sizing", [t]);
}
function zp(t) {
  for (var n = t.aoColumns, e = 0; e < n.length; e++) {
    var r = Md(t, [e], !1, !1);
    n[e].colEl.css("width", r), t.oScroll.sX && n[e].colEl.css("min-width", r);
  }
}
function Ml(t, n) {
  var e = Fl(t, "bVisible");
  return typeof e[n] == "number" ? e[n] : null;
}
function ys(t, n) {
  var e = Fl(t, "bVisible"), r = e.indexOf(n);
  return r !== -1 ? r : null;
}
function Va(t) {
  var n = t.aoHeader, e = t.aoColumns, r = 0;
  if (n.length)
    for (var i = 0, s = n[0].length; i < s; i++)
      e[i].bVisible && q(n[0][i].cell).css("display") !== "none" && r++;
  return r;
}
function Fl(t, n) {
  var e = [];
  return t.aoColumns.map(function(r, i) {
    r[n] && e.push(i);
  }), e;
}
function Po(t, n) {
  return n === !0 ? t._name : n;
}
function Wa(t) {
  var n = t.aoColumns, e = t.aoData, r = H.ext.type.detect, i, s, a, o, l, c, f, g, b;
  for (i = 0, s = n.length; i < s; i++) {
    if (f = n[i], b = [], !f.sType && f._sManualType)
      f.sType = f._sManualType;
    else if (!f.sType) {
      if (!t.typeDetect)
        return;
      for (a = 0, o = r.length; a < o; a++) {
        var w = r[a], C = w.oneOf, m = w.allOf || w, h = w.init, u = !1;
        if (g = null, h && (g = Po(w, h(t, f, i)), g)) {
          f.sType = g;
          break;
        }
        for (l = 0, c = e.length; l < c && !(e[l] && (b[l] === void 0 && (b[l] = _n(t, l, i, "type")), C && !u && (u = Po(w, C(b[l], t))), g = Po(w, m(b[l], t)), !g && a !== r.length - 3 || g === "html" && !nn(b[l]))); l++)
          ;
        if (C && u && g || !C && g) {
          f.sType = g;
          break;
        }
      }
      f.sType || (f.sType = "string");
    }
    var p = _t.type.className[f.sType];
    p && (uc(t.aoHeader, i, p), uc(t.aoFooter, i, p));
    var v = _t.type.render[f.sType];
    v && !f._render && (f._render = H.util.get(v), Yp(t, i));
  }
}
function Yp(t, n) {
  for (var e = t.aoData, r = 0; r < e.length; r++)
    if (e[r].nTr) {
      var i = _n(t, r, n, "display");
      e[r].displayData[n] = i, xa(e[r].anCells[n], i);
    }
}
function uc(t, n, e) {
  t.forEach(function(r) {
    r[n] && r[n].unique && vi(r[n].cell, e);
  });
}
function qp(t, n, e, r, i) {
  var s, a, o, l, c, f, g, b = t.aoColumns;
  if (e)
    for (s = 0, a = e.length; s < a; s++)
      e[s] && e[s].name && (b[s].sName = e[s].name);
  if (n)
    for (s = n.length - 1; s >= 0; s--) {
      g = n[s];
      var w = g.target !== void 0 ? g.target : g.targets !== void 0 ? g.targets : g.aTargets;
      for (Array.isArray(w) || (w = [w]), o = 0, l = w.length; o < l; o++) {
        var C = w[o];
        if (typeof C == "number" && C >= 0) {
          for (; b.length <= C; )
            Ld(t);
          i(C, g);
        } else if (typeof C == "number" && C < 0)
          i(b.length + C, g);
        else if (typeof C == "string")
          for (c = 0, f = b.length; c < f; c++)
            C === "_all" ? i(c, g) : C.indexOf(":name") !== -1 ? b[c].sName === C.replace(":name", "") && i(c, g) : r.forEach(function(m) {
              if (m[c]) {
                var h = q(m[c].cell);
                C.match(/^[a-z][\w-]*$/i) && (C = "." + C), h.is(C) && i(c, g);
              }
            });
      }
    }
  if (e)
    for (s = 0, a = e.length; s < a; s++)
      i(s, e[s]);
}
function Md(t, n, e, r) {
  Array.isArray(n) || (n = Bl(n));
  for (var i = 0, s, a = t.aoColumns, o = 0, l = n.length; o < l; o++) {
    var c = a[n[o]], f = e ? c.sWidthOrig : c.sWidth;
    if (!(!r && c.bVisible === !1)) {
      if (f == null)
        return null;
      if (typeof f == "number")
        s = "px", i += f;
      else {
        var g = f.match(/([\d\.]+)([^\d]*)/);
        g && (i += g[1] * 1, s = g.length === 3 ? g[2] : "px");
      }
    }
  }
  return i + s;
}
function Bl(t) {
  var n = q(t).closest("[data-dt-column]").attr("data-dt-column");
  return n ? n.split(",").map(function(e) {
    return e * 1;
  }) : [];
}
function Nr(t, n, e, r) {
  var i = t.aoData.length, s = q.extend(!0, {}, H.models.oRow, {
    src: e ? "dom" : "data",
    idx: i
  });
  s._aData = n, t.aoData.push(s);
  for (var a = t.aoColumns, o = 0, l = a.length; o < l; o++)
    a[o].sType = null;
  t.aiDisplayMaster.push(i);
  var c = t.rowIdFn(n);
  return c !== void 0 && (t.aIds[c] = s), (e || !t.oFeatures.bDeferRender) && Bd(t, i, e, r), i;
}
function Hl(t, n) {
  var e;
  return n instanceof q || (n = q(n)), n.map(function(r, i) {
    return e = Fd(t, i), Nr(t, e.data, i, e.cells);
  });
}
function _n(t, n, e, r) {
  r === "search" ? r = "filter" : r === "order" && (r = "sort");
  var i = t.aoData[n];
  if (i) {
    var s = t.iDraw, a = t.aoColumns[e], o = i._aData, l = a.sDefaultContent, c = a.fnGetData(o, r, {
      settings: t,
      row: n,
      col: e
    });
    if (r !== "display" && c && typeof c == "object" && c.nodeName && (c = c.innerHTML), c === void 0)
      return t.iDrawError != s && l === null && (mn(t, 0, "Requested unknown parameter " + (typeof a.mData == "function" ? "{function}" : "'" + a.mData + "'") + " for row " + n + ", column " + e, 4), t.iDrawError = s), l;
    if ((c === o || c === null) && l !== null && r !== void 0)
      c = l;
    else if (typeof c == "function")
      return c.call(o);
    if (c === null && r === "display")
      return "";
    if (r === "filter") {
      var f = H.ext.type.search;
      f[a.sType] && (c = f[a.sType](c));
    }
    return c;
  }
}
function Gp(t, n, e, r) {
  var i = t.aoColumns[e], s = t.aoData[n]._aData;
  i.fnSetData(s, r, {
    settings: t,
    row: n,
    col: e
  });
}
function xa(t, n) {
  n && typeof n == "object" && n.nodeName ? q(t).empty().append(n) : t.innerHTML = n;
}
var Xi = /\[.*?\]$/, ii = /\(\)$/;
function cc(t) {
  var n = t.match(/(\\.|[^.])+/g) || [""];
  return n.map(function(e) {
    return e.replace(/\\\./g, ".");
  });
}
var Ci = H.util.get, Dr = H.util.set;
function dc(t) {
  return kt(t.aoData, "_aData");
}
function $l(t) {
  t.aoData.length = 0, t.aiDisplayMaster.length = 0, t.aiDisplay.length = 0, t.aIds = {};
}
function ja(t, n, e, r) {
  var i = t.aoData[n], s, a;
  if (i._aSortData = null, i._aFilterData = null, i.displayData = null, e === "dom" || (!e || e === "auto") && i.src === "dom")
    i._aData = Fd(
      t,
      i,
      r,
      r === void 0 ? void 0 : i._aData
    ).data;
  else {
    var o = i.anCells, l = Vl(t, n);
    if (o)
      if (r !== void 0)
        xa(o[r], l[r]);
      else
        for (s = 0, a = o.length; s < a; s++)
          xa(o[s], l[s]);
  }
  var c = t.aoColumns;
  if (r !== void 0)
    c[r].sType = null, c[r].wideStrings = null;
  else {
    for (s = 0, a = c.length; s < a; s++)
      c[s].sType = null, c[s].wideStrings = null;
    Hd(t, i);
  }
}
function Fd(t, n, e, r) {
  var i = [], s = n.firstChild, a, o, l = 0, c, f = t.aoColumns, g = t._rowReadObject;
  r = r !== void 0 ? r : g ? {} : [];
  var b = function(p, v) {
    if (typeof p == "string") {
      var S = p.indexOf("@");
      if (S !== -1) {
        var T = p.substring(S + 1), A = Dr(p);
        A(r, v.getAttribute(T));
      }
    }
  }, w = function(p) {
    if (e === void 0 || e === l)
      if (o = f[l], c = p.innerHTML.trim(), o && o._bAttrSrc) {
        var v = Dr(o.mData._);
        v(r, c), b(o.mData.sort, p), b(o.mData.type, p), b(o.mData.filter, p);
      } else
        g ? (o._setter || (o._setter = Dr(o.mData)), o._setter(r, c)) : r[l] = c;
    l++;
  };
  if (s)
    for (; s; )
      a = s.nodeName.toUpperCase(), (a == "TD" || a == "TH") && (w(s), i.push(s)), s = s.nextSibling;
  else {
    i = n.anCells;
    for (var C = 0, m = i.length; C < m; C++)
      w(i[C]);
  }
  var h = n.firstChild ? n : n.nTr;
  if (h) {
    var u = h.getAttribute("id");
    u && Dr(t.rowId)(r, u);
  }
  return {
    data: r,
    cells: i
  };
}
function Vl(t, n) {
  var e = t.aoData[n], r = t.aoColumns;
  if (!e.displayData) {
    e.displayData = [];
    for (var i = 0, s = r.length; i < s; i++)
      e.displayData.push(
        _n(t, n, i, "display")
      );
  }
  return e.displayData;
}
function Bd(t, n, e, r) {
  var i = t.aoData[n], s = i._aData, a = [], o, l, c, f, g, b, w = t.oClasses.tbody.row;
  if (i.nTr === null) {
    for (o = e || document.createElement("tr"), i.nTr = o, i.anCells = a, vi(o, w), o._DT_RowIndex = n, Hd(t, i), f = 0, g = t.aoColumns.length; f < g; f++) {
      c = t.aoColumns[f], b = !(e && r[f]), l = b ? document.createElement(c.sCellType) : r[f], l || mn(t, 0, "Incorrect column count", 18), l._DT_CellIndex = {
        row: n,
        column: f
      }, a.push(l);
      var C = Vl(t, n);
      (b || (c.mRender || c.mData !== f) && (!q.isPlainObject(c.mData) || c.mData._ !== f + ".display")) && xa(l, C[f]), vi(l, c.sClass), c.bVisible && b ? o.appendChild(l) : !c.bVisible && !b && l.parentNode.removeChild(l), c.fnCreatedCell && c.fnCreatedCell.call(
        t.oInstance,
        l,
        _n(t, n, f),
        s,
        n,
        f
      );
    }
    qe(t, "aoRowCreatedCallback", "row-created", [o, s, n, a]);
  } else
    vi(i.nTr, w);
}
function Hd(t, n) {
  var e = n.nTr, r = n._aData;
  if (e) {
    var i = t.rowIdFn(r);
    if (i && (e.id = i), r.DT_RowClass) {
      var s = r.DT_RowClass.split(" ");
      n.__rowc = n.__rowc ? Oi(n.__rowc.concat(s)) : s, q(e).removeClass(n.__rowc.join(" ")).addClass(r.DT_RowClass);
    }
    r.DT_RowAttr && q(e).attr(r.DT_RowAttr), r.DT_RowData && q(e).data(r.DT_RowData);
  }
}
function fc(t, n) {
  var e = t.oClasses, r = t.aoColumns, i, s, a, o = n === "header" ? t.nTHead : t.nTFoot, l = n === "header" ? "sTitle" : n;
  if (o) {
    if ((n === "header" || kt(t.aoColumns, l).join("")) && (a = q("tr", o), a.length || (a = q("<tr/>").appendTo(o)), a.length === 1)) {
      var c = 0;
      for (q("td, th", a).each(function() {
        c += this.colSpan;
      }), i = c, s = r.length; i < s; i++)
        q("<th/>").html(r[i][l] || "").appendTo(a);
    }
    var f = Vd(t, o, !0);
    n === "header" ? (t.aoHeader = f, q("tr", o).addClass(e.thead.row)) : (t.aoFooter = f, q("tr", o).addClass(e.tfoot.row)), q(o).children("tr").children("th, td").each(function() {
      Da(t, n)(
        t,
        q(this),
        e
      );
    });
  }
}
function $d(t, n, e) {
  var r, i, s, a = [], o = [], l = t.aoColumns, c = l.length, f, g;
  if (n) {
    for (e || (e = tr(c).filter(function(C) {
      return l[C].bVisible;
    })), r = 0; r < n.length; r++)
      a[r] = n[r].slice().filter(function(C, m) {
        return e.includes(m);
      }), o.push([]);
    for (r = 0; r < a.length; r++)
      for (i = 0; i < a[r].length; i++)
        if (f = 1, g = 1, o[r][i] === void 0) {
          for (s = a[r][i].cell; a[r + f] !== void 0 && a[r][i].cell == a[r + f][i].cell; )
            o[r + f][i] = null, f++;
          for (; a[r][i + g] !== void 0 && a[r][i].cell == a[r][i + g].cell; ) {
            for (var b = 0; b < f; b++)
              o[r + b][i + g] = null;
            g++;
          }
          var w = q(".dt-column-title", s);
          o[r][i] = {
            cell: s,
            colspan: g,
            rowspan: f,
            title: w.length ? w.html() : q(s).html()
          };
        }
    return o;
  }
}
function Sa(t, n) {
  for (var e = $d(t, n), r, i, s = 0; s < n.length; s++) {
    if (r = n[s].row, r)
      for (; i = r.firstChild; )
        r.removeChild(i);
    for (var a = 0; a < e[s].length; a++) {
      var o = e[s][a];
      o && q(o.cell).appendTo(r).attr("rowspan", o.rowspan).attr("colspan", o.colspan);
    }
  }
}
function Ii(t, n) {
  Jp(t);
  var e = qe(t, "aoPreDrawCallback", "preDraw", [t]);
  if (e.indexOf(!1) !== -1) {
    rn(t, !1);
    return;
  }
  var r = [], i = 0, s = an(t) == "ssp", a = t.aiDisplay, o = t._iDisplayStart, l = t.fnDisplayEnd(), c = t.aoColumns, f = q(t.nTBody);
  if (t.bDrawing = !0, t.deferLoading)
    t.deferLoading = !1, t.iDraw++, rn(t, !1);
  else if (!s)
    t.iDraw++;
  else if (!t.bDestroying && !n) {
    t.iDraw === 0 && f.empty().append(hc(t)), Zp(t);
    return;
  }
  if (a.length !== 0)
    for (var g = s ? 0 : o, b = s ? t.aoData.length : l, w = g; w < b; w++) {
      var C = a[w], m = t.aoData[C];
      if (m !== null) {
        m.nTr === null && Bd(t, C);
        for (var h = m.nTr, u = 0; u < c.length; u++) {
          var p = c[u], v = m.anCells[u];
          vi(v, _t.type.className[p.sType]), vi(v, t.oClasses.tbody.cell);
        }
        qe(
          t,
          "aoRowCallback",
          null,
          [h, m._aData, i, w, C]
        ), r.push(h), i++;
      }
    }
  else
    r[0] = hc(t);
  qe(t, "aoHeaderCallback", "header", [
    q(t.nTHead).children("tr")[0],
    dc(t),
    o,
    l,
    a
  ]), qe(t, "aoFooterCallback", "footer", [
    q(t.nTFoot).children("tr")[0],
    dc(t),
    o,
    l,
    a
  ]), f[0].replaceChildren ? f[0].replaceChildren.apply(f[0], r) : (f.children().detach(), f.append(q(r))), q(t.nTableWrapper).toggleClass("dt-empty-footer", q("tr", t.nTFoot).length === 0), qe(t, "aoDrawCallback", "draw", [t], !0), t.bSorted = !1, t.bFiltered = !1, t.bDrawing = !1;
}
function xi(t, n, e) {
  var r = t.oFeatures, i = r.bSort, s = r.bFilter;
  (e === void 0 || e === !0) && (Wa(t), i && zl(t), s ? gs(t, t.oPreviousSearch) : t.aiDisplay = t.aiDisplayMaster.slice()), n !== !0 && (t._iDisplayStart = 0), t._drawHold = n, Ii(t), t.api.one("draw", function() {
    t._drawHold = !1;
  });
}
function hc(t) {
  var n = t.oLanguage, e = n.sZeroRecords, r = an(t);
  return (r === "ssp" || r === "ajax") && !t.json ? e = n.sLoadingRecords : n.sEmptyTable && t.fnRecordsTotal() === 0 && (e = n.sEmptyTable), q("<tr/>").append(q("<td />", {
    colSpan: Va(t),
    class: t.oClasses.empty.row
  }).html(e))[0];
}
function gl(t, n, e) {
  if (Array.isArray(e)) {
    for (var r = 0; r < e.length; r++)
      gl(t, n, e[r]);
    return;
  }
  var i = t[n];
  q.isPlainObject(e) ? e.features ? (e.rowId && (t.id = e.rowId), e.rowClass && (t.className = e.rowClass), i.id = e.id, i.className = e.className, gl(t, n, e.features)) : Object.keys(e).map(function(s) {
    i.contents.push({
      feature: s,
      opts: e[s]
    });
  }) : i.contents.push(e);
}
function Up(t, n, e) {
  for (var r, i = 0; i < t.length; i++)
    if (r = t[i], r.rowNum === n && (e === "full" && r.full || (e === "start" || e === "end") && (r.start || r.end)))
      return r[e] || (r[e] = {
        contents: []
      }), r;
  return r = {
    rowNum: n
  }, r[e] = {
    contents: []
  }, t.push(r), r;
}
function pc(t, n, e) {
  var r = [];
  q.each(n, function(s, a) {
    if (a !== null) {
      var o = s.match(/^([a-z]+)([0-9]*)([A-Za-z]*)$/), l = o[2] ? o[2] * 1 : 0, c = o[3] ? o[3].toLowerCase() : "full";
      if (o[1] === e) {
        var f = Up(r, l, c);
        gl(f, c, a);
      }
    }
  }), r.sort(function(s, a) {
    var o = s.rowNum, l = a.rowNum;
    if (o === l) {
      var c = s.full && !a.full ? -1 : 1;
      return e === "bottom" ? c * -1 : c;
    }
    return l - o;
  }), e === "bottom" && r.reverse();
  for (var i = 0; i < r.length; i++)
    delete r[i].rowNum, Kp(t, r[i]);
  return r;
}
function Kp(t, n) {
  var e = function(i, s) {
    return _t.features[i] || mn(t, 0, "Unknown feature: " + i), _t.features[i].apply(this, [t, s]);
  }, r = function(i) {
    if (n[i])
      for (var s = n[i].contents, a = 0, o = s.length; a < o; a++)
        if (s[a]) {
          if (typeof s[a] == "string")
            s[a] = e(s[a], null);
          else if (q.isPlainObject(s[a]))
            s[a] = e(s[a].feature, s[a].opts);
          else if (typeof s[a].node == "function")
            s[a] = s[a].node(t);
          else if (typeof s[a] == "function") {
            var l = s[a](t);
            s[a] = typeof l.node == "function" ? l.node() : l;
          }
        } else
          continue;
  };
  r("start"), r("end"), r("full");
}
function Xp(t) {
  var n = t.oClasses, e = q(t.nTable), r = q("<div/>").attr({
    id: t.sTableId + "_wrapper",
    class: n.container
  }).insertBefore(e);
  if (t.nTableWrapper = r[0], t.sDom)
    Qp(t, t.sDom, r);
  else {
    var i = pc(t, t.layout, "top"), s = pc(t, t.layout, "bottom"), a = Da(t, "layout");
    i.forEach(function(o) {
      a(t, r, o);
    }), a(t, r, {
      full: {
        table: !0,
        contents: [zd(t)]
      }
    }), s.forEach(function(o) {
      a(t, r, o);
    });
  }
  ag(t);
}
function Qp(t, n, e) {
  for (var r = n.match(/(".*?")|('.*?')|./g), i, s, a, o, l, c = 0; c < r.length; c++) {
    if (i = null, s = r[c], s == "<") {
      if (a = q("<div/>"), o = r[c + 1], o[0] == "'" || o[0] == '"') {
        l = o.replace(/['"]/g, "");
        var f = "", g;
        if (l.indexOf(".") != -1) {
          var b = l.split(".");
          f = b[0], g = b[1];
        } else
          l[0] == "#" ? f = l : g = l;
        a.attr("id", f.substring(1)).addClass(g), c++;
      }
      e.append(a), e = a;
    } else
      s == ">" ? e = e.parent() : s == "t" ? i = zd(t) : H.ext.feature.forEach(function(w) {
        s == w.cFeature && (i = w.fnInit(t));
      });
    i && e.append(i);
  }
}
function Vd(t, n, e) {
  var r = t.aoColumns, i = q(n).children("tr"), s, a, o, l, c, f, g, b, w, C, m = t.titleRow, h = n && n.nodeName.toLowerCase() === "thead", u = [], p, v = function(V, z, F) {
    for (var P = V[z]; P[F]; )
      F++;
    return F;
  };
  for (o = 0, f = i.length; o < f; o++)
    u.push([]);
  for (o = 0, f = i.length; o < f; o++)
    for (s = i[o], b = 0, a = s.firstChild; a; ) {
      if (a.nodeName.toUpperCase() == "TD" || a.nodeName.toUpperCase() == "TH") {
        var S = [], T = q(a);
        if (w = a.getAttribute("colspan") * 1, C = a.getAttribute("rowspan") * 1, w = !w || w === 0 || w === 1 ? 1 : w, C = !C || C === 0 || C === 1 ? 1 : C, g = v(u, o, b), p = w === 1, e) {
          if (p) {
            pl(t, g, Qd(T.data()));
            var A = r[g], y = a.getAttribute("width") || null, I = a.style.width.match(/width:\s*(\d+[pxem%]+)/);
            I && (y = I[1]), A.sWidthOrig = A.sWidth || y, h ? (A.sTitle !== null && !A.autoTitle && (m === !0 && o === 0 || // top row
            m === !1 && o === i.length - 1 || // bottom row
            m === o || // specific row
            m === null) && (a.innerHTML = A.sTitle), !A.sTitle && p && (A.sTitle = jn(a.innerHTML), A.autoTitle = !0)) : A.footer && (a.innerHTML = A.footer), A.ariaTitle || (A.ariaTitle = T.attr("aria-label") || A.sTitle), A.className && T.addClass(A.className);
          }
          q(".dt-column-title", a).length === 0 && q(document.createElement(t.columnTitleTag)).addClass("dt-column-title").append(a.childNodes).appendTo(a), t.orderIndicators && h && T.filter(":not([data-dt-order=disable])").length !== 0 && T.parent(":not([data-dt-order=disable])").length !== 0 && q(".dt-column-order", a).length === 0 && q(document.createElement(t.columnTitleTag)).addClass("dt-column-order").appendTo(a);
          var $ = h ? "header" : "footer";
          q("div.dt-column-" + $, a).length === 0 && q("<div>").addClass("dt-column-" + $).append(a.childNodes).appendTo(a);
        }
        for (c = 0; c < w; c++) {
          for (l = 0; l < C; l++)
            u[o + l][g + c] = {
              cell: a,
              unique: p
            }, u[o + l].row = s;
          S.push(g + c);
        }
        a.setAttribute("data-dt-column", Oi(S).join(","));
      }
      a = a.nextSibling;
    }
  return u;
}
function Jp(t) {
  var n = an(t) == "ssp", e = t.iInitDisplayStart;
  e !== void 0 && e !== -1 && (t._iDisplayStart = n ? e : e >= t.fnRecordsDisplay() ? 0 : e, t.iInitDisplayStart = -1);
}
function Wl(t, n, e) {
  var r, i = t.ajax, s = t.oInstance, a = function(f) {
    var g = t.jqXHR ? t.jqXHR.status : null;
    (f === null || typeof g == "number" && g == 204) && (f = {}, ps(t, f, []));
    var b = f.error || f.sError;
    if (b && mn(t, 0, b), f.d && typeof f.d == "string")
      try {
        f = JSON.parse(f.d);
      } catch {
      }
    t.json = f, qe(t, null, "xhr", [t, f, t.jqXHR], !0), e(f);
  };
  if (q.isPlainObject(i) && i.data) {
    r = i.data;
    var o = typeof r == "function" ? r(n, t) : r;
    n = typeof r == "function" && o ? o : q.extend(!0, n, o), delete i.data;
  }
  var l = {
    url: typeof i == "string" ? i : "",
    data: n,
    success: a,
    dataType: "json",
    cache: !1,
    type: t.sServerMethod,
    error: function(f, g) {
      var b = qe(
        t,
        null,
        "xhr",
        [t, null, t.jqXHR],
        !0
      );
      b.indexOf(!0) === -1 && (g == "parsererror" ? mn(t, 0, "Invalid JSON response", 1) : f.readyState === 4 && mn(t, 0, "Ajax error", 7)), rn(t, !1);
    }
  };
  if (q.isPlainObject(i) && q.extend(l, i), t.oAjaxData = n, qe(t, null, "preXhr", [t, n, l], !0), l.submitAs === "json" && typeof n == "object" && (l.data = JSON.stringify(n), l.contentType || (l.contentType = "application/json; charset=utf-8")), typeof i == "function")
    t.jqXHR = i.call(s, n, a, t);
  else if (i.url === "") {
    var c = {};
    ps(t, c, []), a(c);
  } else
    t.jqXHR = q.ajax(l);
  r && (i.data = r);
}
function Zp(t) {
  t.iDraw++, rn(t, !0), Wl(t, eg(t), function(n) {
    tg(t, n);
  });
}
function eg(t) {
  var n = t.aoColumns, e = t.oFeatures, r = t.oPreviousSearch, i = t.aoPreSearchCols, s = function(a, o) {
    return typeof n[a][o] == "function" ? "function" : n[a][o];
  };
  return {
    draw: t.iDraw,
    columns: n.map(function(a, o) {
      return {
        data: s(o, "mData"),
        name: a.sName,
        searchable: a.bSearchable,
        orderable: a.bSortable,
        search: {
          value: i[o].search,
          regex: i[o].regex,
          fixed: Object.keys(a.searchFixed).map(function(l) {
            return {
              name: l,
              term: typeof a.searchFixed[l] != "function" ? a.searchFixed[l].toString() : "function"
            };
          })
        }
      };
    }),
    order: za(t).map(function(a) {
      return {
        column: a.col,
        dir: a.dir,
        name: s(a.col, "sName")
      };
    }),
    start: t._iDisplayStart,
    length: e.bPaginate ? t._iDisplayLength : -1,
    search: {
      value: r.search,
      regex: r.regex,
      fixed: Object.keys(t.searchFixed).map(function(a) {
        return {
          name: a,
          term: typeof t.searchFixed[a] != "function" ? t.searchFixed[a].toString() : "function"
        };
      })
    }
  };
}
function tg(t, n) {
  var e = ps(t, n), r = Oo(t, "draw", n), i = Oo(t, "recordsTotal", n), s = Oo(t, "recordsFiltered", n);
  if (r !== void 0) {
    if (r * 1 < t.iDraw)
      return;
    t.iDraw = r * 1;
  }
  e || (e = []), $l(t), t._iRecordsTotal = parseInt(i, 10), t._iRecordsDisplay = parseInt(s, 10);
  for (var a = 0, o = e.length; a < o; a++)
    Nr(t, e[a]);
  t.aiDisplay = t.aiDisplayMaster.slice(), Wa(t), Ii(t, !0), Ta(t), rn(t, !1);
}
function ps(t, n, e) {
  var r = "data";
  if (q.isPlainObject(t.ajax) && t.ajax.dataSrc !== void 0) {
    var i = t.ajax.dataSrc;
    typeof i == "string" || typeof i == "function" ? r = i : i.data !== void 0 && (r = i.data);
  }
  if (!e)
    return r === "data" ? n.aaData || n[r] : r !== "" ? Ci(r)(n) : n;
  Dr(r)(n, e);
}
function Oo(t, n, e) {
  var r = q.isPlainObject(t.ajax) ? t.ajax.dataSrc : null;
  if (r && r[n])
    return Ci(r[n])(e);
  var i = "";
  return n === "draw" ? i = "sEcho" : n === "recordsTotal" ? i = "iTotalRecords" : n === "recordsFiltered" && (i = "iTotalDisplayRecords"), e[i] !== void 0 ? e[i] : e[n];
}
function gs(t, n) {
  var e = t.aoPreSearchCols;
  if (an(t) != "ssp") {
    sg(t), t.aiDisplay = t.aiDisplayMaster.slice(), Ks(t.aiDisplay, t, n.search, n), q.each(t.searchFixed, function(s, a) {
      Ks(t.aiDisplay, t, a, {});
    });
    for (var r = 0; r < e.length; r++) {
      var i = e[r];
      Ks(
        t.aiDisplay,
        t,
        i.search,
        i,
        r
      ), q.each(t.aoColumns[r].searchFixed, function(s, a) {
        Ks(t.aiDisplay, t, a, {}, r);
      });
    }
    ng(t);
  }
  t.bFiltered = !0, qe(t, null, "search", [t]);
}
function ng(t) {
  for (var n = H.ext.search, e = t.aiDisplay, r, i, s = 0, a = n.length; s < a; s++) {
    for (var o = [], l = 0, c = e.length; l < c; l++)
      i = e[l], r = t.aoData[i], n[s](t, r._aFilterData, i, r._aData, l) && o.push(i);
    e.length = 0, qa(e, o);
  }
}
function Ks(t, n, e, r, i) {
  if (e !== "") {
    var s = 0, a = [], o = typeof e == "function" ? e : null, l = e instanceof RegExp ? e : o ? null : rg(e, r);
    for (s = 0; s < t.length; s++) {
      var c = n.aoData[t[s]], f = i === void 0 ? c._sFilterRow : c._aFilterData[i];
      (o && o(f, c._aData, t[s], i) || l && l.test(f)) && a.push(t[s]);
    }
    for (t.length = a.length, s = 0; s < a.length; s++)
      t[s] = a[s];
  }
}
function rg(t, n) {
  var e = [], r = q.extend({}, {
    boundary: !1,
    caseInsensitive: !0,
    exact: !1,
    regex: !1,
    smart: !0
  }, n);
  if (typeof t != "string" && (t = t.toString()), t = Ca(t), r.exact)
    return new RegExp(
      "^" + ml(t) + "$",
      r.caseInsensitive ? "i" : ""
    );
  if (t = r.regex ? t : ml(t), r.smart) {
    var i = t.match(/!?["\u201C][^"\u201D]+["\u201D]|[^ ]+/g) || [""], s = i.map(function(l) {
      var c = !1, f;
      return l.charAt(0) === "!" && (c = !0, l = l.substring(1)), l.charAt(0) === '"' ? (f = l.match(/^"(.*)"$/), l = f ? f[1] : l) : l.charAt(0) === "“" && (f = l.match(/^\u201C(.*)\u201D$/), l = f ? f[1] : l), c && (l.length > 1 && e.push("(?!" + l + ")"), l = ""), l.replace(/"/g, "");
    }), a = e.length ? e.join("") : "", o = r.boundary ? "\\b" : "";
    t = "^(?=.*?" + o + s.join(")(?=.*?" + o) + ")(" + a + ".)*$";
  }
  return new RegExp(t, r.caseInsensitive ? "i" : "");
}
var ml = H.util.escapeRegex, da = q("<div>")[0], ig = da.textContent !== void 0;
function sg(t) {
  for (var n = t.aoColumns, e = t.aoData, r, i, s, a, o, l, c = !1, f = 0; f < e.length; f++)
    if (e[f] && (l = e[f], !l._aFilterData)) {
      for (a = [], i = 0, s = n.length; i < s; i++)
        r = n[i], r.bSearchable ? (o = _n(t, f, i, "filter"), o === null && (o = ""), typeof o != "string" && o.toString && (o = o.toString())) : o = "", o.indexOf && o.indexOf("&") !== -1 && (da.innerHTML = o, o = ig ? da.textContent : da.innerText), o.replace && (o = o.replace(/[\r\n\u2028]/g, "")), a.push(o);
      l._aFilterData = a, l._sFilterRow = a.join("  "), c = !0;
    }
  return c;
}
function fa(t) {
  var n, e = t.oInit, r = t.deferLoading, i = an(t);
  if (!t.bInitialised) {
    setTimeout(function() {
      fa(t);
    }, 200);
    return;
  }
  fc(t, "header"), fc(t, "footer"), fg(t, e, function() {
    Sa(t, t.aoHeader), Sa(t, t.aoFooter);
    var s = t.iInitDisplayStart;
    if (e.aaData)
      for (n = 0; n < e.aaData.length; n++)
        Nr(t, e.aaData[n]);
    else
      (r || i == "dom") && Hl(t, q(t.nTBody).children("tr"));
    t.aiDisplay = t.aiDisplayMaster.slice(), Xp(t), ug(t), qd(t), rn(t, !0), qe(t, null, "preInit", [t], !0), xi(t), (i != "ssp" || r) && (i == "ajax" ? Wl(t, {}, function(a) {
      var o = ps(t, a);
      for (n = 0; n < o.length; n++)
        Nr(t, o[n]);
      t.iInitDisplayStart = s, xi(t), rn(t, !1), Ta(t);
    }) : (Ta(t), rn(t, !1)));
  });
}
function Ta(t) {
  if (!t._bInitComplete) {
    var n = [t, t.json];
    t._bInitComplete = !0, $a(t), qe(t, null, "plugin-init", n, !0), qe(t, "aoInitComplete", "init", n, !0);
  }
}
function Wd(t, n) {
  var e = parseInt(n, 10);
  t._iDisplayLength = e, Xd(t), qe(t, null, "length", [t, e]);
}
function jl(t, n, e) {
  var r = t._iDisplayStart, i = t._iDisplayLength, s = t.fnRecordsDisplay();
  if (s === 0 || i === -1)
    r = 0;
  else if (typeof n == "number")
    r = n * i, r > s && (r = 0);
  else if (n == "first")
    r = 0;
  else if (n == "previous")
    r = i >= 0 ? r - i : 0, r < 0 && (r = 0);
  else if (n == "next")
    r + i < s && (r += i);
  else if (n == "last")
    r = Math.floor((s - 1) / i) * i;
  else {
    if (n === "ellipsis")
      return;
    mn(t, 0, "Unknown paging action: " + n, 5);
  }
  var a = t._iDisplayStart !== r;
  return t._iDisplayStart = r, qe(t, null, a ? "page" : "page-nc", [t]), a && e && Ii(t), a;
}
function ag(t) {
  var n = t.nTable, e = t.oScroll.sX !== "" || t.oScroll.sY !== "";
  if (t.oFeatures.bProcessing) {
    var r = q("<div/>", {
      id: t.sTableId + "_processing",
      class: t.oClasses.processing.container,
      role: "status"
    }).html(t.oLanguage.sProcessing).append("<div><div></div><div></div><div></div><div></div></div>");
    e ? r.prependTo(q("div.dt-scroll", t.nTableWrapper)) : r.insertBefore(n), q(n).on("processing.dt.DT", function(i, s, a) {
      r.css("display", a ? "block" : "none");
    });
  }
}
function rn(t, n) {
  t.bDrawing && n === !1 || qe(t, null, "processing", [t, n]);
}
function jd(t, n, e) {
  n ? (rn(t, !0), setTimeout(function() {
    e(), rn(t, !1);
  }, 0)) : e();
}
function zd(t) {
  var n = q(t.nTable), e = t.oScroll;
  if (e.sX === "" && e.sY === "")
    return t.nTable;
  var r = e.sX, i = e.sY, s = t.oClasses.scrolling, a = t.captionNode, o = a ? a._captionSide : null, l = q(n[0].cloneNode(!1)), c = q(n[0].cloneNode(!1)), f = n.children("tfoot"), g = "<div/>", b = function(p) {
    return p ? ir(p) : null;
  };
  f.length || (f = null);
  var w = q(g, { class: s.container }).append(
    q(g, { class: s.header.self }).css({
      overflow: "hidden",
      position: "relative",
      border: 0,
      width: r ? b(r) : "100%"
    }).append(
      q(g, { class: s.header.inner }).css({
        "box-sizing": "content-box",
        width: e.sXInner || "100%"
      }).append(
        l.removeAttr("id").css("margin-left", 0).append(o === "top" ? a : null).append(
          n.children("thead")
        )
      )
    )
  ).append(
    q(g, { class: s.body }).css({
      position: "relative",
      overflow: "auto",
      width: b(r)
    }).append(n)
  );
  f && w.append(
    q(g, { class: s.footer.self }).css({
      overflow: "hidden",
      border: 0,
      width: r ? b(r) : "100%"
    }).append(
      q(g, { class: s.footer.inner }).append(
        c.removeAttr("id").css("margin-left", 0).append(o === "bottom" ? a : null).append(
          n.children("tfoot")
        )
      )
    )
  );
  var C = w.children(), m = C[0], h = C[1], u = f ? C[2] : null;
  return q(h).on("scroll.DT", function() {
    var p = this.scrollLeft;
    m.scrollLeft = p, f && (u.scrollLeft = p);
  }), q("th, td", m).on("focus", function() {
    var p = m.scrollLeft;
    h.scrollLeft = p, f && (h.scrollLeft = p);
  }), q(h).css("max-height", i), e.bCollapse || q(h).css("height", i), t.nScrollHead = m, t.nScrollBody = h, t.nScrollFoot = u, t.aoDrawCallback.push(Yd), w[0];
}
function Yd(t) {
  var n = t.oScroll, e = n.iBarWidth, r = q(t.nScrollHead), i = r.children("div"), s = i.children("table"), a = t.nScrollBody, o = q(a), l = q(t.nScrollFoot), c = l.children("div"), f = c.children("table"), g = q(t.nTHead), b = q(t.nTable), w = t.nTFoot && q("th, td", t.nTFoot).length ? q(t.nTFoot) : null, C = t.oBrowser, m, h, u = a.scrollHeight > a.clientHeight;
  if (t.scrollBarVis !== u && t.scrollBarVis !== void 0) {
    t.scrollBarVis = u, $a(t);
    return;
  } else
    t.scrollBarVis = u;
  if (b.children("thead, tfoot").remove(), m = g.clone().prependTo(b), m.find("th, td").removeAttr("tabindex"), m.find("[id]").removeAttr("id"), w && (h = w.clone().prependTo(b), h.find("[id]").removeAttr("id")), t.aiDisplay.length) {
    var p = null, v = an(t) !== "ssp" ? t._iDisplayStart : 0;
    for (y = v; y < v + t.aiDisplay.length; y++) {
      var S = t.aiDisplay[y], T = t.aoData[S].nTr;
      if (T) {
        p = T;
        break;
      }
    }
    if (p)
      for (var A = q(p).children("th, td").map(function(P) {
        return {
          idx: Ml(t, P),
          width: q(this).outerWidth()
        };
      }), y = 0; y < A.length; y++) {
        var I = t.aoColumns[A[y].idx].colEl[0], $ = I.style.width.replace("px", "");
        $ !== A[y].width && (I.style.width = A[y].width + "px", n.sX && (I.style.minWidth = A[y].width + "px"));
      }
  }
  s.find("colgroup").remove(), s.append(t.colgroup.clone()), w && (f.find("colgroup").remove(), f.append(t.colgroup.clone())), q("th, td", m).each(function() {
    q(this.childNodes).wrapAll('<div class="dt-scroll-sizing">');
  }), w && q("th, td", h).each(function() {
    q(this.childNodes).wrapAll('<div class="dt-scroll-sizing">');
  });
  var V = Math.floor(b.height()) > a.clientHeight || o.css("overflow-y") == "scroll", z = "padding" + (C.bScrollbarLeft ? "Left" : "Right"), F = b.outerWidth();
  s.css("width", ir(F)), i.css("width", ir(F)).css(z, V ? e + "px" : "0px"), w && (f.css("width", ir(F)), c.css("width", ir(F)).css(z, V ? e + "px" : "0px")), b.children("colgroup").prependTo(b), o.trigger("scroll"), (t.bSorted || t.bFiltered) && !t._drawHold && (a.scrollTop = 0);
}
function og(t) {
  if (t.oFeatures.bAutoWidth) {
    var n = t.nTable, e = t.aoColumns, r = t.oScroll, i = r.sY, s = r.sX, a = r.sXInner, o = Fl(t, "bVisible"), l = n.getAttribute("width"), c = n.parentNode, f, g, b, w, C = n.style.width, m = gc(t);
    if (m === t.containerWidth)
      return !1;
    t.containerWidth = m, !C && !l && (n.style.width = "100%", C = "100%"), C && C.indexOf("%") !== -1 && (l = C), qe(
      t,
      null,
      "column-calc",
      { visible: o },
      !1
    );
    var h = q(n.cloneNode()).css("visibility", "hidden").css("margin", 0).removeAttr("id");
    h.append("<tbody/>"), h.append(q(t.nTHead).clone()).append(q(t.nTFoot).clone()), h.find("tfoot th, tfoot td").css("width", ""), h.find("thead th, thead td").each(function() {
      var R = Md(t, this, !0, !1);
      R ? (this.style.width = R, s && (this.style.minWidth = R, q(this).append(q("<div/>").css({
        width: R,
        margin: 0,
        padding: 0,
        border: 0,
        height: 1
      })))) : this.style.width = "";
    });
    var u = [];
    for (f = 0; f < o.length; f++)
      u.push(lg(t, o[f]));
    if (u.length)
      for (f = 0; f < u[0].length; f++) {
        var p = q("<tr/>").appendTo(h.find("tbody"));
        for (g = 0; g < o.length; g++) {
          w = o[g], b = e[w];
          var v = u[g][f] || "", S = _t.type.className[b.sType], T = b.sContentPadding || (s ? "-" : ""), A = v + T, y = v.indexOf("<") === -1 && v.indexOf("&") === -1 ? document.createTextNode(A) : A;
          q("<td/>").addClass(S).addClass(b.sClass).append(y).appendTo(p);
        }
      }
    q("[name]", h).removeAttr("name");
    var I = q("<div/>").css(
      s || i ? {
        position: "absolute",
        top: 0,
        left: 0,
        height: 1,
        right: 0,
        overflow: "hidden"
      } : {}
    ).append(h).appendTo(c);
    s && a ? h.width(a) : s ? (h.css("width", "auto"), h.removeAttr("width"), h.outerWidth() < c.clientWidth && l && h.outerWidth(c.clientWidth)) : i ? h.outerWidth(c.clientWidth) : l && h.outerWidth(l);
    var $ = 0, V = h.find("tbody tr").eq(0).children();
    for (f = 0; f < o.length; f++) {
      var z = V[f].getBoundingClientRect().width;
      $ += z, e[o[f]].sWidth = ir(z);
    }
    if (n.style.width = ir($), I.remove(), l && (n.style.width = ir(l)), (l || s) && !t._reszEvt) {
      var F = H.util.throttle(function() {
        var R = gc(t);
        !t.bDestroying && R !== 0 && $a(t);
      });
      if (window.ResizeObserver) {
        var P = q(t.nTableWrapper).is(":visible"), k = q("<div>").css({
          width: "100%",
          height: 0
        }).addClass("dt-autosize").appendTo(t.nTableWrapper);
        t.resizeObserver = new ResizeObserver(function(R) {
          P ? P = !1 : F();
        }), t.resizeObserver.observe(k[0]);
      } else
        q(window).on("resize.DT-" + t.sInstance, F);
      t._reszEvt = !0;
    }
  }
}
function gc(t) {
  return q(t.nTableWrapper).is(":visible") ? q(t.nTableWrapper).width() : 0;
}
function lg(t, n) {
  var e = t.aoColumns[n];
  if (!e.wideStrings) {
    for (var r = [], i = [], s = 0, a = t.aiDisplayMaster.length; s < a; s++) {
      var o = t.aiDisplayMaster[s], l = Vl(t, o)[n], c = l && typeof l == "object" && l.nodeType ? l.innerHTML : l + "";
      c = c.replace(/id=".*?"/g, "").replace(/name=".*?"/g, ""), c = c.replace(/<script.*?<\/script>/gi, " ");
      var f = jn(c, " ").replace(/&nbsp;/g, " ");
      i.push({
        str: c,
        len: f.length
      }), r.push(f);
    }
    i.sort(function(b, w) {
      return w.len - b.len;
    }).splice(3), e.wideStrings = i.map(function(b) {
      return b.str;
    });
    let g = r.join(" ").split(" ");
    g.sort(function(b, w) {
      return w.length - b.length;
    }), g.length && e.wideStrings.push(g[0]), g.length > 1 && e.wideStrings.push(g[1]), g.length > 2 && e.wideStrings.push(g[3]);
  }
  return e.wideStrings;
}
function ir(t) {
  return t === null ? "0px" : typeof t == "number" ? t < 0 ? "0px" : t + "px" : t.match(/\d$/) ? t + "px" : t;
}
function qd(t) {
  var n = t.aoColumns;
  for (t.colgroup.empty(), bt = 0; bt < n.length; bt++)
    n[bt].bVisible && t.colgroup.append(n[bt].colEl);
}
function ug(t) {
  var n = t.nTHead, e = n.querySelectorAll("tr"), r = t.titleRow, i = ':not([data-dt-order="disable"]):not([data-dt-order="icon-only"])';
  r === !0 ? n = e[0] : r === !1 ? n = e[e.length - 1] : r !== null && (n = e[r]), t.orderHandler && Gd(
    t,
    n,
    n === t.nTHead ? "tr" + i + " th" + i + ", tr" + i + " td" + i : "th" + i + ", td" + i
  );
  var s = [];
  Er(t, s, t.aaSorting), t.aaSorting = s;
}
function Gd(t, n, e, r, i) {
  Kd(n, e, function(s) {
    var a = !1, o = r === void 0 ? Bl(s.target) : typeof r == "function" ? r() : Array.isArray(r) ? r : [r];
    if (o.length) {
      for (var l = 0, c = o.length; l < c; l++) {
        var f = cg(t, o[l], l, s.shiftKey);
        if (f !== !1 && (a = !0), t.aaSorting.length === 1 && t.aaSorting[0][1] === "")
          break;
      }
      a && jd(t, !0, function() {
        zl(t), Ud(t, t.aiDisplay), xi(t, !1, !1), i && i();
      });
    }
  });
}
function Ud(t, n) {
  if (!(n.length < 2)) {
    var e = t.aiDisplayMaster, r = {}, i = {}, s;
    for (s = 0; s < e.length; s++)
      r[e[s]] = s;
    for (s = 0; s < n.length; s++)
      i[n[s]] = r[n[s]];
    n.sort(function(a, o) {
      return i[a] - i[o];
    });
  }
}
function Er(t, n, e) {
  var r = function(s) {
    if (q.isPlainObject(s)) {
      if (s.idx !== void 0)
        n.push([s.idx, s.dir]);
      else if (s.name) {
        var a = kt(t.aoColumns, "sName"), o = a.indexOf(s.name);
        o !== -1 && n.push([o, s.dir]);
      }
    } else
      n.push(s);
  };
  if (q.isPlainObject(e))
    r(e);
  else if (e.length && typeof e[0] == "number")
    r(e);
  else if (e.length)
    for (var i = 0; i < e.length; i++)
      r(e[i]);
}
function za(t) {
  var n, e, r, i = [], s = H.ext.type.order, a = t.aoColumns, o, l, c, f, g = t.aaSortingFixed, b = q.isPlainObject(g), w = [];
  if (!t.oFeatures.bSort)
    return i;
  for (Array.isArray(g) && Er(t, w, g), b && g.pre && Er(t, w, g.pre), Er(t, w, t.aaSorting), b && g.post && Er(t, w, g.post), n = 0; n < w.length; n++)
    if (f = w[n][0], a[f])
      for (o = a[f].aDataSort, e = 0, r = o.length; e < r; e++)
        l = o[e], c = a[l].sType || "string", w[n]._idx === void 0 && (w[n]._idx = a[l].asSorting.indexOf(w[n][1])), w[n][1] && i.push({
          src: f,
          col: l,
          dir: w[n][1],
          index: w[n]._idx,
          type: c,
          formatter: s[c + "-pre"],
          sorter: s[c + "-" + w[n][1]]
        });
  return i;
}
function zl(t, n, e) {
  var r, i, s = [], a = H.ext.type.order, o = t.aoData, l, c = t.aiDisplayMaster, f;
  if (Wa(t), n !== void 0) {
    var g = t.aoColumns[n];
    f = [{
      src: n,
      col: n,
      dir: e,
      index: 0,
      type: g.sType,
      formatter: a[g.sType + "-pre"],
      sorter: a[g.sType + "-" + e]
    }], c = c.slice();
  } else
    f = za(t);
  for (r = 0, i = f.length; r < i; r++)
    l = f[r], dg(t, l.col);
  if (an(t) != "ssp" && f.length !== 0) {
    for (r = 0, i = c.length; r < i; r++)
      s[r] = r;
    f.length && f[0].dir === "desc" && t.orderDescReverse && s.reverse(), c.sort(function(b, w) {
      var C, m, h, u, p, v = f.length, S = o[b]._aSortData, T = o[w]._aSortData;
      for (h = 0; h < v; h++)
        if (p = f[h], C = S[p.col], m = T[p.col], p.sorter) {
          if (u = p.sorter(C, m), u !== 0)
            return u;
        } else if (u = C < m ? -1 : C > m ? 1 : 0, u !== 0)
          return p.dir === "asc" ? u : -u;
      return C = s[b], m = s[w], C < m ? -1 : C > m ? 1 : 0;
    });
  } else
    f.length === 0 && c.sort(function(b, w) {
      return b < w ? -1 : b > w ? 1 : 0;
    });
  return n === void 0 && (t.bSorted = !0, t.sortDetails = f, qe(t, null, "order", [t, f])), c;
}
function cg(t, n, e, r) {
  var i = t.aoColumns[n], s = t.aaSorting, a = i.asSorting, o, l = function(f, g) {
    var b = f._idx;
    return b === void 0 && (b = a.indexOf(f[1])), b + 1 < a.length ? b + 1 : g ? null : 0;
  };
  if (!i.bSortable)
    return !1;
  if (typeof s[0] == "number" && (s = t.aaSorting = [s]), (r || e) && t.oFeatures.bSortMulti) {
    var c = kt(s, "0").indexOf(n);
    c !== -1 ? (o = l(s[c], !0), o === null && s.length === 1 && (o = 0), o === null || a[o] === "" ? s.splice(c, 1) : (s[c][1] = a[o], s[c]._idx = o)) : r ? (s.push([n, a[0], 0]), s[s.length - 1]._idx = 0) : (s.push([n, s[0][1], 0]), s[s.length - 1]._idx = 0);
  } else
    s.length && s[0][0] == n ? (o = l(s[0]), s.length = 1, s[0][1] = a[o], s[0]._idx = o) : (s.length = 0, s.push([n, a[0]]), s[0]._idx = 0);
}
function vl(t) {
  var n = t.aLastSort, e = t.oClasses.order.position, r = za(t), i = t.oFeatures, s, a, o;
  if (i.bSort && i.bSortClasses) {
    for (s = 0, a = n.length; s < a; s++)
      o = n[s].src, q(kt(t.aoData, "anCells", o)).removeClass(e + (s < 2 ? s + 1 : 3));
    for (s = 0, a = r.length; s < a; s++)
      o = r[s].src, q(kt(t.aoData, "anCells", o)).addClass(e + (s < 2 ? s + 1 : 3));
  }
  t.aLastSort = r;
}
function dg(t, n) {
  var e = t.aoColumns[n], r = H.ext.order[e.sSortDataType], i;
  r && (i = r.call(
    t.oInstance,
    t,
    n,
    ys(t, n)
  ));
  for (var s, a, o = H.ext.type.order[e.sType + "-pre"], l = t.aoData, c = 0; c < l.length; c++)
    l[c] && (s = l[c], s._aSortData || (s._aSortData = []), (!s._aSortData[n] || r) && (a = r ? i[c] : (
      // If there was a custom sort function, use data from there
      _n(t, c, n, "sort")
    ), s._aSortData[n] = o ? o(a, t) : a));
}
function Ya(t) {
  if (!t._bLoadingState) {
    var n = [];
    Er(t, n, t.aaSorting);
    var e = t.aoColumns, r = {
      time: +/* @__PURE__ */ new Date(),
      start: t._iDisplayStart,
      length: t._iDisplayLength,
      order: n.map(function(i) {
        return e[i[0]] && e[i[0]].sName ? [e[i[0]].sName, i[1]] : i.slice();
      }),
      search: q.extend({}, t.oPreviousSearch),
      columns: t.aoColumns.map(function(i, s) {
        return {
          name: i.sName,
          visible: i.bVisible,
          search: q.extend({}, t.aoPreSearchCols[s])
        };
      })
    };
    t.oSavedState = r, qe(t, "aoStateSaveParams", "stateSaveParams", [t, r]), t.oFeatures.bStateSave && !t.bDestroying && t.fnStateSaveCallback.call(t.oInstance, t, r);
  }
}
function fg(t, n, e) {
  if (!t.oFeatures.bStateSave) {
    e();
    return;
  }
  var r = function(s) {
    bl(t, s, e);
  }, i = t.fnStateLoadCallback.call(t.oInstance, t, r);
  return i !== void 0 && bl(t, i, e), !0;
}
function bl(t, n, e) {
  var r, i, s = t.aoColumns, a = kt(t.aoColumns, "sName");
  t._bLoadingState = !0;
  var o = t._bInitComplete ? new H.Api(t) : null;
  if (!n || !n.time) {
    t._bLoadingState = !1, e();
    return;
  }
  var l = t.iStateDuration;
  if (l > 0 && n.time < +/* @__PURE__ */ new Date() - l * 1e3) {
    t._bLoadingState = !1, e();
    return;
  }
  var c = qe(t, "aoStateLoadParams", "stateLoadParams", [t, n]);
  if (c.indexOf(!1) !== -1) {
    t._bLoadingState = !1, e();
    return;
  }
  if (t.oLoadedState = q.extend(!0, {}, n), qe(t, null, "stateLoadInit", [t, n], !0), n.length !== void 0 && (o ? o.page.len(n.length) : t._iDisplayLength = n.length), n.start !== void 0 && (o === null ? (t._iDisplayStart = n.start, t.iInitDisplayStart = n.start) : jl(t, n.start / t._iDisplayLength)), n.order !== void 0 && (t.aaSorting = [], q.each(n.order, function(C, m) {
    var h = [m[0], m[1]];
    if (typeof m[0] == "string") {
      var u = a.indexOf(m[0]);
      if (u < 0)
        return;
      h[0] = u;
    } else if (h[0] >= s.length)
      return;
    t.aaSorting.push(h);
  })), n.search !== void 0 && q.extend(t.oPreviousSearch, n.search), n.columns) {
    var f = n.columns, g = kt(n.columns, "name");
    if (g.join("").length && g.join("") !== a.join(""))
      for (f = [], r = 0; r < a.length; r++)
        if (a[r] != "") {
          var b = g.indexOf(a[r]);
          b >= 0 ? f.push(n.columns[b]) : f.push({});
        } else
          f.push({});
    if (f.length === s.length) {
      for (r = 0, i = f.length; r < i; r++) {
        var w = f[r];
        w.visible !== void 0 && (o ? o.column(r).visible(w.visible, !1) : s[r].bVisible = w.visible), w.search !== void 0 && q.extend(t.aoPreSearchCols[r], w.search);
      }
      o && o.one("draw", function() {
        o.columns.adjust();
      });
    }
  }
  t._bLoadingState = !1, qe(t, "aoStateLoaded", "stateLoaded", [t, n]), e();
}
function mn(t, n, e, r) {
  if (e = "DataTables warning: " + (t ? "table id=" + t.sTableId + " - " : "") + e, r && (e += ". For more information about this error, please see https://datatables.net/tn/" + r), n)
    window.console && console.log && console.log(e);
  else {
    var i = H.ext, s = i.sErrMode || i.errMode;
    if (t && qe(t, null, "dt-error", [t, r, e], !0), s == "alert")
      alert(e);
    else {
      if (s == "throw")
        throw new Error(e);
      typeof s == "function" && s(t, r, e);
    }
  }
}
function sr(t, n, e, r) {
  if (Array.isArray(e)) {
    q.each(e, function(i, s) {
      Array.isArray(s) ? sr(t, n, s[0], s[1]) : sr(t, n, s);
    });
    return;
  }
  r === void 0 && (r = e), n[e] !== void 0 && (t[r] = n[e]);
}
function mc(t, n, e) {
  var r;
  for (var i in n)
    Object.prototype.hasOwnProperty.call(n, i) && (r = n[i], q.isPlainObject(r) ? (q.isPlainObject(t[i]) || (t[i] = {}), q.extend(!0, t[i], r)) : e && i !== "data" && i !== "aaData" && Array.isArray(r) ? t[i] = r.slice() : t[i] = r);
  return t;
}
function Kd(t, n, e) {
  q(t).on("click.DT", n, function(r) {
    e(r);
  }).on("keypress.DT", n, function(r) {
    r.which === 13 && (r.preventDefault(), e(r));
  }).on("selectstart.DT", n, function() {
    return !1;
  });
}
function hn(t, n, e) {
  e && t[n].push(e);
}
function qe(t, n, e, r, i) {
  var s = [];
  if (n && (s = t[n].slice().reverse().map(function(l) {
    return l.apply(t.oInstance, r);
  })), e !== null) {
    var a = q.Event(e + ".dt"), o = q(t.nTable);
    a.dt = t.api, o[i ? "trigger" : "triggerHandler"](a, r), i && o.parents("body").length === 0 && q("body").trigger(a, r), s.push(a.result);
  }
  return s;
}
function Xd(t) {
  var n = t._iDisplayStart, e = t.fnDisplayEnd(), r = t._iDisplayLength;
  n >= e && (n = e - r), n -= n % r, (r === -1 || n < 0) && (n = 0), t._iDisplayStart = n;
}
function Da(t, n) {
  var e = t.renderer, r = H.ext.renderer[n];
  return q.isPlainObject(e) && e[n] ? r[e[n]] || r._ : typeof e == "string" && r[e] || r._;
}
function an(t) {
  return t.oFeatures.bServerSide ? "ssp" : t.ajax ? "ajax" : "dom";
}
function Yl(t, n, e) {
  var r = t.fnFormatNumber, i = t._iDisplayStart + 1, s = t._iDisplayLength, a = t.fnRecordsDisplay(), o = t.fnRecordsTotal(), l = s === -1;
  return n.replace(/_START_/g, r.call(t, i)).replace(/_END_/g, r.call(t, t.fnDisplayEnd())).replace(/_MAX_/g, r.call(t, o)).replace(/_TOTAL_/g, r.call(t, a)).replace(/_PAGE_/g, r.call(t, l ? 1 : Math.ceil(i / s))).replace(/_PAGES_/g, r.call(t, l ? 1 : Math.ceil(a / s))).replace(/_ENTRIES_/g, t.api.i18n("entries", "", e)).replace(/_ENTRIES-MAX_/g, t.api.i18n("entries", "", o)).replace(/_ENTRIES-TOTAL_/g, t.api.i18n("entries", "", a));
}
function qa(t, n) {
  if (n)
    if (n.length < 1e4)
      t.push.apply(t, n);
    else
      for (bt = 0; bt < n.length; bt++)
        t.push(n[bt]);
}
function vc(t, n, e) {
  for (Array.isArray(e) || (e = [e]), bt = 0; bt < e.length; bt++)
    t.on(n + ".dt", e[bt]);
}
function Qd(t) {
  return H.ext.escape.attributes && q.each(t, function(n, e) {
    t[n] = Or(e);
  }), t;
}
var ql = [], Nt = Array.prototype, hg = function(t) {
  var n, e, r = H.settings, i = kt(r, "nTable");
  if (t) {
    if (t.nTable && t.oFeatures)
      return [t];
    if (t.nodeName && t.nodeName.toLowerCase() === "table")
      return n = i.indexOf(t), n !== -1 ? [r[n]] : null;
    if (t && typeof t.settings == "function")
      return t.settings().toArray();
    typeof t == "string" ? e = q(t).get() : t instanceof q && (e = t.get());
  } else
    return [];
  if (e)
    return r.filter(function(s, a) {
      return e.includes(i[a]);
    });
};
We = function(t, n) {
  if (!(this instanceof We))
    return new We(t, n);
  var e, r = [], i = function(s) {
    var a = hg(s);
    a && r.push.apply(r, a);
  };
  if (Array.isArray(t))
    for (e = 0; e < t.length; e++)
      i(t[e]);
  else
    i(t);
  this.context = r.length > 1 ? Oi(r) : r, qa(this, n), this.selector = {
    rows: null,
    cols: null,
    opts: null
  }, We.extend(this, this, ql);
};
H.Api = We;
q.extend(We.prototype, {
  any: function() {
    return this.count() !== 0;
  },
  context: [],
  // array of table settings objects
  count: function() {
    return this.flatten().length;
  },
  each: function(t) {
    for (var n = 0, e = this.length; n < e; n++)
      t.call(this, this[n], n, this);
    return this;
  },
  eq: function(t) {
    var n = this.context;
    return n.length > t ? new We(n[t], this[t]) : null;
  },
  filter: function(t) {
    var n = Nt.filter.call(this, t, this);
    return new We(this.context, n);
  },
  flatten: function() {
    var t = [];
    return new We(this.context, t.concat.apply(t, this.toArray()));
  },
  get: function(t) {
    return this[t];
  },
  join: Nt.join,
  includes: function(t) {
    return this.indexOf(t) !== -1;
  },
  indexOf: Nt.indexOf,
  iterator: function(t, n, e, r) {
    var i = [], s, a, o, l, c, f = this.context, g, b, w, C = this.selector;
    for (typeof t == "string" && (r = e, e = n, n = t, t = !1), a = 0, o = f.length; a < o; a++) {
      var m = new We(f[a]);
      if (n === "table")
        s = e.call(m, f[a], a), s !== void 0 && i.push(s);
      else if (n === "columns" || n === "rows")
        s = e.call(m, f[a], this[a], a), s !== void 0 && i.push(s);
      else if (n === "every" || n === "column" || n === "column-rows" || n === "row" || n === "cell")
        for (b = this[a], n === "column-rows" && (g = Ga(f[a], C.opts)), l = 0, c = b.length; l < c; l++)
          w = b[l], n === "cell" ? s = e.call(m, f[a], w.row, w.column, a, l) : s = e.call(m, f[a], w, a, l, g), s !== void 0 && i.push(s);
    }
    if (i.length || r) {
      var h = new We(f, t ? i.concat.apply([], i) : i), u = h.selector;
      return u.rows = C.rows, u.cols = C.cols, u.opts = C.opts, h;
    }
    return this;
  },
  lastIndexOf: Nt.lastIndexOf,
  length: 0,
  map: function(t) {
    var n = Nt.map.call(this, t, this);
    return new We(this.context, n);
  },
  pluck: function(t) {
    var n = H.util.get(t);
    return this.map(function(e) {
      return n(e);
    });
  },
  pop: Nt.pop,
  push: Nt.push,
  reduce: Nt.reduce,
  reduceRight: Nt.reduceRight,
  reverse: Nt.reverse,
  // Object with rows, columns and opts
  selector: null,
  shift: Nt.shift,
  slice: function() {
    return new We(this.context, this);
  },
  sort: Nt.sort,
  splice: Nt.splice,
  toArray: function() {
    return Nt.slice.call(this);
  },
  to$: function() {
    return q(this);
  },
  toJQuery: function() {
    return q(this);
  },
  unique: function() {
    return new We(this.context, Oi(this.toArray()));
  },
  unshift: Nt.unshift
});
function pg(t, n, e) {
  return function() {
    var r = n.apply(t || this, arguments);
    return We.extend(r, r, e.methodExt), r;
  };
}
function gg(t, n) {
  for (var e = 0, r = t.length; e < r; e++)
    if (t[e].name === n)
      return t[e];
  return null;
}
window.__apiStruct = ql;
We.extend = function(t, n, e) {
  if (!(!e.length || !n || !(n instanceof We) && !n.__dt_wrapper)) {
    var r, i, s;
    for (r = 0, i = e.length; r < i; r++)
      s = e[r], s.name !== "__proto__" && (n[s.name] = s.type === "function" ? pg(t, s.val, s) : s.type === "object" ? {} : s.val, n[s.name].__dt_wrapper = !0, We.extend(t, n[s.name], s.propExt));
  }
};
We.register = _e = function(t, n) {
  if (Array.isArray(t)) {
    for (var e = 0, r = t.length; e < r; e++)
      We.register(t[e], n);
    return;
  }
  var i, s, a = t.split("."), o = ql, l, c;
  for (i = 0, s = a.length; i < s; i++) {
    c = a[i].indexOf("()") !== -1, l = c ? a[i].replace("()", "") : a[i];
    var f = gg(o, l);
    f || (f = {
      name: l,
      val: {},
      methodExt: [],
      propExt: [],
      type: "object"
    }, o.push(f)), i === s - 1 ? (f.val = n, f.type = typeof n == "function" ? "function" : q.isPlainObject(n) ? "object" : "other") : o = c ? f.methodExt : f.propExt;
  }
};
We.registerPlural = Qe = function(t, n, e) {
  We.register(t, e), We.register(n, function() {
    var r = e.apply(this, arguments);
    return r === this ? this : r instanceof We ? r.length ? Array.isArray(r[0]) ? new We(r.context, r[0]) : (
      // Array results are 'enhanced'
      r[0]
    ) : void 0 : r;
  });
};
var Jd = function(t, n) {
  if (Array.isArray(t)) {
    var e = [];
    return t.forEach(function(i) {
      var s = Jd(i, n);
      qa(e, s);
    }), e.filter(function(i) {
      return i;
    });
  }
  if (typeof t == "number")
    return [n[t]];
  var r = n.map(function(i) {
    return i.nTable;
  });
  return q(r).filter(t).map(function() {
    var i = r.indexOf(this);
    return n[i];
  }).toArray();
};
_e("tables()", function(t) {
  return t != null ? new We(Jd(t, this.context)) : this;
});
_e("table()", function(t) {
  var n = this.tables(t), e = n.context;
  return e.length ? new We(e[0]) : n;
});
[
  ["nodes", "node", "nTable"],
  ["body", "body", "nTBody"],
  ["header", "header", "nTHead"],
  ["footer", "footer", "nTFoot"]
].forEach(function(t) {
  Qe(
    "tables()." + t[0] + "()",
    "table()." + t[1] + "()",
    function() {
      return this.iterator("table", function(n) {
        return n[t[2]];
      }, 1);
    }
  );
});
[
  ["header", "aoHeader"],
  ["footer", "aoFooter"]
].forEach(function(t) {
  _e("table()." + t[0] + ".structure()", function(n) {
    var e = this.columns(n).indexes().flatten().toArray(), r = this.context[0], i = $d(r, r[t[1]], e), s = e.slice().sort(function(a, o) {
      return a - o;
    });
    return i.map(function(a) {
      return e.map(function(o) {
        return a[s.indexOf(o)];
      });
    });
  });
});
Qe("tables().containers()", "table().container()", function() {
  return this.iterator("table", function(t) {
    return t.nTableWrapper;
  }, 1);
});
_e("tables().every()", function(t) {
  var n = this;
  return this.iterator("table", function(e, r) {
    t.call(n.table(r), r);
  });
});
_e("caption()", function(t, n) {
  var e = this.context;
  if (t === void 0) {
    var r = e[0].captionNode;
    return r && e.length ? r.innerHTML : null;
  }
  return this.iterator("table", function(i) {
    var s = q(i.nTable), a = q(i.captionNode), o = q(i.nTableWrapper);
    if (a.length || (a = q("<caption/>").html(t), i.captionNode = a[0], n || (s.prepend(a), n = a.css("caption-side"))), a.html(t), n && (a.css("caption-side", n), a[0]._captionSide = n), o.find("div.dataTables_scroll").length) {
      var l = n === "top" ? "Head" : "Foot";
      o.find("div.dataTables_scroll" + l + " table").prepend(a);
    } else
      s.prepend(a);
  }, 1);
});
_e("caption.node()", function() {
  var t = this.context;
  return t.length ? t[0].captionNode : null;
});
_e("draw()", function(t) {
  return this.iterator("table", function(n) {
    t === "page" ? Ii(n) : (typeof t == "string" && (t = t !== "full-hold"), xi(n, t === !1));
  });
});
_e("page()", function(t) {
  return t === void 0 ? this.page.info().page : this.iterator("table", function(n) {
    jl(n, t);
  });
});
_e("page.info()", function() {
  if (this.context.length !== 0) {
    var t = this.context[0], n = t._iDisplayStart, e = t.oFeatures.bPaginate ? t._iDisplayLength : -1, r = t.fnRecordsDisplay(), i = e === -1;
    return {
      page: i ? 0 : Math.floor(n / e),
      pages: i ? 1 : Math.ceil(r / e),
      start: n,
      end: t.fnDisplayEnd(),
      length: e,
      recordsTotal: t.fnRecordsTotal(),
      recordsDisplay: r,
      serverSide: an(t) === "ssp"
    };
  }
});
_e("page.len()", function(t) {
  return t === void 0 ? this.context.length !== 0 ? this.context[0]._iDisplayLength : void 0 : this.iterator("table", function(n) {
    Wd(n, t);
  });
});
var Zd = function(t, n, e) {
  if (e) {
    var r = new We(t);
    r.one("draw", function() {
      e(r.ajax.json());
    });
  }
  if (an(t) == "ssp")
    xi(t, n);
  else {
    rn(t, !0);
    var i = t.jqXHR;
    i && i.readyState !== 4 && i.abort(), Wl(t, {}, function(s) {
      $l(t);
      for (var a = ps(t, s), o = 0, l = a.length; o < l; o++)
        Nr(t, a[o]);
      xi(t, n), Ta(t), rn(t, !1);
    });
  }
};
_e("ajax.json()", function() {
  var t = this.context;
  if (t.length > 0)
    return t[0].json;
});
_e("ajax.params()", function() {
  var t = this.context;
  if (t.length > 0)
    return t[0].oAjaxData;
});
_e("ajax.reload()", function(t, n) {
  return this.iterator("table", function(e) {
    Zd(e, n === !1, t);
  });
});
_e("ajax.url()", function(t) {
  var n = this.context;
  return t === void 0 ? n.length === 0 ? void 0 : (n = n[0], q.isPlainObject(n.ajax) ? n.ajax.url : n.ajax) : this.iterator("table", function(e) {
    q.isPlainObject(e.ajax) ? e.ajax.url = t : e.ajax = t;
  });
});
_e("ajax.url().load()", function(t, n) {
  return this.iterator("table", function(e) {
    Zd(e, n === !1, t);
  });
});
var Gl = function(t, n, e, r, i) {
  var s = [], a, o, l, c = typeof n;
  for ((!n || c === "string" || c === "function" || n.length === void 0) && (n = [n]), o = 0, l = n.length; o < l; o++)
    a = e(typeof n[o] == "string" ? n[o].trim() : n[o]), a = a.filter(function(g) {
      return g != null;
    }), a && a.length && (s = s.concat(a));
  var f = _t.selector[t];
  if (f.length)
    for (o = 0, l = f.length; o < l; o++)
      s = f[o](r, i, s);
  return Oi(s);
}, Ul = function(t) {
  return t || (t = {}), t.filter && t.search === void 0 && (t.search = t.filter), q.extend({
    columnOrder: "implied",
    search: "none",
    order: "current",
    page: "all"
  }, t);
}, Kl = function(t) {
  var n = new We(t.context[0]);
  return t.length && n.push(t[0]), n.selector = t.selector, n.length && n[0].length > 1 && n[0].splice(1), n;
}, Ga = function(t, n) {
  var e, r, i, s = [], a = t.aiDisplay, o = t.aiDisplayMaster, l = n.search, c = n.order, f = n.page;
  if (an(t) == "ssp")
    return l === "removed" ? [] : tr(0, o.length);
  if (f == "current")
    for (e = t._iDisplayStart, r = t.fnDisplayEnd(); e < r; e++)
      s.push(a[e]);
  else if (c == "current" || c == "applied") {
    if (l == "none")
      s = o.slice();
    else if (l == "applied")
      s = a.slice();
    else if (l == "removed") {
      var g = {};
      for (e = 0, r = a.length; e < r; e++)
        g[a[e]] = null;
      o.forEach(function(w) {
        Object.prototype.hasOwnProperty.call(g, w) || s.push(w);
      });
    }
  } else if (c == "index" || c == "original")
    for (e = 0, r = t.aoData.length; e < r; e++)
      t.aoData[e] && (l == "none" ? s.push(e) : (i = a.indexOf(e), (i === -1 && l == "removed" || i >= 0 && l == "applied") && s.push(e)));
  else if (typeof c == "number") {
    var b = zl(t, c, "asc");
    if (l === "none")
      s = b;
    else
      for (e = 0; e < b.length; e++)
        i = a.indexOf(b[e]), (i === -1 && l == "removed" || i >= 0 && l == "applied") && s.push(b[e]);
  }
  return s;
}, mg = function(t, n, e) {
  var r, i = function(a) {
    var o = Pd(a), l = t.aoData;
    if (o !== null && !e)
      return [o];
    if (r || (r = Ga(t, e)), o !== null && r.indexOf(o) !== -1)
      return [o];
    if (a == null || a === "")
      return r;
    if (typeof a == "function")
      return r.map(function(C) {
        var m = l[C];
        return a(C, m._aData, m.nTr) ? C : null;
      });
    if (a.nodeName) {
      var c = a._DT_RowIndex, f = a._DT_CellIndex;
      if (c !== void 0)
        return l[c] && l[c].nTr === a ? [c] : [];
      if (f)
        return l[f.row] && l[f.row].nTr === a.parentNode ? [f.row] : [];
      var g = q(a).closest("*[data-dt-row]");
      return g.length ? [g.data("dt-row")] : [];
    }
    if (typeof a == "string" && a.charAt(0) === "#") {
      var b = t.aIds[a.replace(/^#/, "")];
      if (b !== void 0)
        return [b.idx];
    }
    var w = Id(
      bs(t.aoData, r, "nTr")
    );
    return q(w).filter(a).map(function() {
      return this._DT_RowIndex;
    }).toArray();
  }, s = Gl("row", n, i, t, e);
  return (e.order === "current" || e.order === "applied") && Ud(t, s), s;
};
_e("rows()", function(t, n) {
  t === void 0 ? t = "" : q.isPlainObject(t) && (n = t, t = ""), n = Ul(n);
  var e = this.iterator("table", function(r) {
    return mg(r, t, n);
  }, 1);
  return e.selector.rows = t, e.selector.opts = n, e;
});
_e("rows().nodes()", function() {
  return this.iterator("row", function(t, n) {
    return t.aoData[n].nTr || void 0;
  }, 1);
});
_e("rows().data()", function() {
  return this.iterator(!0, "rows", function(t, n) {
    return bs(t.aoData, n, "_aData");
  }, 1);
});
Qe("rows().cache()", "row().cache()", function(t) {
  return this.iterator("row", function(n, e) {
    var r = n.aoData[e];
    return t === "search" ? r._aFilterData : r._aSortData;
  }, 1);
});
Qe("rows().invalidate()", "row().invalidate()", function(t) {
  return this.iterator("row", function(n, e) {
    ja(n, e, t);
  });
});
Qe("rows().indexes()", "row().index()", function() {
  return this.iterator("row", function(t, n) {
    return n;
  }, 1);
});
Qe("rows().ids()", "row().id()", function(t) {
  for (var n = [], e = this.context, r = 0, i = e.length; r < i; r++)
    for (var s = 0, a = this[r].length; s < a; s++) {
      var o = e[r].rowIdFn(e[r].aoData[this[r][s]]._aData);
      n.push((t === !0 ? "#" : "") + o);
    }
  return new We(e, n);
});
Qe("rows().remove()", "row().remove()", function() {
  return this.iterator("row", function(t, n) {
    var e = t.aoData, r = e[n], i = t.aiDisplayMaster.indexOf(n);
    i !== -1 && t.aiDisplayMaster.splice(i, 1), t._iRecordsDisplay > 0 && t._iRecordsDisplay--, Xd(t);
    var s = t.rowIdFn(r._aData);
    s !== void 0 && delete t.aIds[s], e[n] = null;
  }), this;
});
_e("rows.add()", function(t) {
  var n = this.iterator("table", function(r) {
    var i, s, a, o = [];
    for (s = 0, a = t.length; s < a; s++)
      i = t[s], i.nodeName && i.nodeName.toUpperCase() === "TR" ? o.push(Hl(r, i)[0]) : o.push(Nr(r, i));
    return o;
  }, 1), e = this.rows(-1);
  return e.pop(), qa(e, n), e;
});
_e("row()", function(t, n) {
  return Kl(this.rows(t, n));
});
_e("row().data()", function(t) {
  var n = this.context;
  if (t === void 0)
    return n.length && this.length && this[0].length ? n[0].aoData[this[0]]._aData : void 0;
  var e = n[0].aoData[this[0]];
  return e._aData = t, Array.isArray(t) && e.nTr && e.nTr.id && Dr(n[0].rowId)(t, e.nTr.id), ja(n[0], this[0], "data"), this;
});
_e("row().node()", function() {
  var t = this.context;
  if (t.length && this.length && this[0].length) {
    var n = t[0].aoData[this[0]];
    if (n && n.nTr)
      return n.nTr;
  }
  return null;
});
_e("row.add()", function(t) {
  t instanceof q && t.length && (t = t[0]);
  var n = this.iterator("table", function(e) {
    return t.nodeName && t.nodeName.toUpperCase() === "TR" ? Hl(e, t)[0] : Nr(e, t);
  });
  return this.row(n[0]);
});
q(document).on("plugin-init.dt", function(t, n) {
  var e = new We(n);
  e.on("stateSaveParams.DT", function(r, i, s) {
    for (var a = i.rowIdFn, o = i.aiDisplayMaster, l = [], c = 0; c < o.length; c++) {
      var f = o[c], g = i.aoData[f];
      g._detailsShow && l.push("#" + a(g._aData));
    }
    s.childRows = l;
  }), e.on("stateLoaded.DT", function(r, i, s) {
    bc(e, s);
  }), bc(e, e.state.loaded());
});
var bc = function(t, n) {
  n && n.childRows && t.rows(n.childRows.map(function(e) {
    return e.replace(/([^:\\]*(?:\\.[^:\\]*)*):/g, "$1\\:");
  })).every(function() {
    qe(t.settings()[0], null, "requestChild", [this]);
  });
}, vg = function(t, n, e, r) {
  var i = [], s = function(a, o) {
    if (Array.isArray(a) || a instanceof q) {
      for (var l = 0, c = a.length; l < c; l++)
        s(a[l], o);
      return;
    }
    if (a.nodeName && a.nodeName.toLowerCase() === "tr")
      a.setAttribute("data-dt-row", n.idx), i.push(a);
    else {
      var f = q("<tr><td></td></tr>").attr("data-dt-row", n.idx).addClass(o);
      q("td", f).addClass(o).html(a)[0].colSpan = Va(t), i.push(f[0]);
    }
  };
  s(e, r), n._details && n._details.detach(), n._details = q(i), n._detailsShow && n._details.insertAfter(n.nTr);
}, ef = H.util.throttle(
  function(t) {
    Ya(t[0]);
  },
  500
), Xl = function(t, n) {
  var e = t.context;
  if (e.length) {
    var r = e[0].aoData[n !== void 0 ? n : t[0]];
    r && r._details && (r._details.detach(), r._detailsShow = void 0, r._details = void 0, q(r.nTr).removeClass("dt-hasChild"), ef(e));
  }
}, tf = function(t, n) {
  var e = t.context;
  if (e.length && t.length) {
    var r = e[0].aoData[t[0]];
    r._details && (r._detailsShow = n, n ? (r._details.insertAfter(r.nTr), q(r.nTr).addClass("dt-hasChild")) : (r._details.detach(), q(r.nTr).removeClass("dt-hasChild")), qe(e[0], null, "childRow", [n, t.row(t[0])]), bg(e[0]), ef(e));
  }
}, bg = function(t) {
  var n = new We(t), e = ".dt.DT_details", r = "draw" + e, i = "column-sizing" + e, s = "destroy" + e, a = t.aoData;
  n.off(r + " " + i + " " + s), kt(a, "_details").length > 0 && (n.on(r, function(o, l) {
    t === l && n.rows({ page: "current" }).eq(0).each(function(c) {
      var f = a[c];
      f._detailsShow && f._details.insertAfter(f.nTr);
    });
  }), n.on(i, function(o, l) {
    if (t === l)
      for (var c, f = Va(l), g = 0, b = a.length; g < b; g++)
        c = a[g], c && c._details && c._details.each(function() {
          var w = q(this).children("td");
          w.length == 1 && w.attr("colspan", f);
        });
  }), n.on(s, function(o, l) {
    if (t === l)
      for (var c = 0, f = a.length; c < f; c++)
        a[c] && a[c]._details && Xl(n, c);
  }));
}, yg = "", ws = yg + "row().child", Ua = ws + "()";
_e(Ua, function(t, n) {
  var e = this.context;
  return t === void 0 ? e.length && this.length && e[0].aoData[this[0]] ? e[0].aoData[this[0]]._details : void 0 : (t === !0 ? this.child.show() : t === !1 ? Xl(this) : e.length && this.length && vg(e[0], e[0].aoData[this[0]], t, n), this);
});
_e([
  ws + ".show()",
  Ua + ".show()"
  // only when `child()` was called with parameters (without
], function() {
  return tf(this, !0), this;
});
_e([
  ws + ".hide()",
  Ua + ".hide()"
  // only when `child()` was called with parameters (without
], function() {
  return tf(this, !1), this;
});
_e([
  ws + ".remove()",
  Ua + ".remove()"
  // only when `child()` was called with parameters (without
], function() {
  return Xl(this), this;
});
_e(ws + ".isShown()", function() {
  var t = this.context;
  return t.length && this.length && t[0].aoData[this[0]] && t[0].aoData[this[0]]._detailsShow || !1;
});
var wg = /^([^:]+)?:(name|title|visIdx|visible)$/, Ql = function(t, n, e, r, i, s) {
  for (var a = [], o = 0, l = i.length; o < l; o++)
    a.push(_n(t, i[o], n, s));
  return a;
}, nf = function(t, n, e) {
  var r = t.aoHeader, i = t.titleRow, s = null;
  if (e !== void 0)
    s = e;
  else if (i === !0)
    s = 0;
  else if (i === !1)
    s = r.length - 1;
  else if (i !== null)
    s = i;
  else {
    for (var a = 0; a < r.length; a++)
      r[a][n].unique && q(".dt-column-title", r[a][n].cell).text() && (s = a);
    s === null && (s = 0);
  }
  return r[s][n].cell;
}, _g = function(t) {
  for (var n = [], e = 0; e < t.length; e++)
    for (var r = 0; r < t[e].length; r++) {
      var i = t[e][r].cell;
      n.includes(i) || n.push(i);
    }
  return n;
}, Cg = function(t, n, e) {
  var r = t.aoColumns, i, s, a = _g(t.aoHeader), o = function(c) {
    var f = Pd(c);
    if (c === "")
      return tr(r.length);
    if (f !== null)
      return [
        f >= 0 ? f : (
          // Count from left
          r.length + f
        )
        // Count from right (+ because its a negative value)
      ];
    if (typeof c == "function") {
      var g = Ga(t, e);
      return r.map(function(u, p) {
        return c(
          p,
          Ql(t, p, 0, 0, g),
          nf(t, p)
        ) ? p : null;
      });
    }
    var b = typeof c == "string" ? c.match(wg) : "";
    if (b)
      switch (b[2]) {
        case "visIdx":
        case "visible":
          if (b[1] && b[1].match(/^\d+$/)) {
            var w = parseInt(b[1], 10);
            if (w < 0) {
              var C = r.map(function(u, p) {
                return u.bVisible ? p : null;
              });
              return [C[C.length + w]];
            }
            return [Ml(t, w)];
          }
          return r.map(function(u, p) {
            return !u.bVisible || u.responsiveVisible === !1 ? null : b[1] ? q(a[p]).filter(b[1]).length > 0 ? p : null : p;
          });
        case "name":
          return i || (i = kt(r, "sName")), i.map(function(u, p) {
            return u === b[1] ? p : null;
          });
        case "title":
          return s || (s = kt(r, "sTitle")), s.map(function(u, p) {
            return u === b[1] ? p : null;
          });
        default:
          return [];
      }
    if (c.nodeName && c._DT_CellIndex)
      return [c._DT_CellIndex.column];
    var m = q(a).filter(c).map(function() {
      return Bl(this);
    }).toArray().sort(function(u, p) {
      return u - p;
    });
    if (m.length || !c.nodeName)
      return m;
    var h = q(c).closest("*[data-dt-column]");
    return h.length ? [h.data("dt-column")] : [];
  }, l = Gl("column", n, o, t, e);
  return e.columnOrder && e.columnOrder === "index" ? l.sort(function(c, f) {
    return c - f;
  }) : l;
}, xg = function(t, n, e) {
  var r = t.aoColumns, i = r[n], s = t.aoData, a, o, l, c;
  if (e === void 0)
    return i.bVisible;
  if (i.bVisible === e)
    return !1;
  if (e) {
    var f = kt(r, "bVisible").indexOf(!0, n + 1);
    for (o = 0, l = s.length; o < l; o++)
      s[o] && (c = s[o].nTr, a = s[o].anCells, c && c.insertBefore(a[n], a[f] || null));
  } else
    q(kt(t.aoData, "anCells", n)).detach();
  return i.bVisible = e, qd(t), !0;
};
_e("columns()", function(t, n) {
  t === void 0 ? t = "" : q.isPlainObject(t) && (n = t, t = ""), n = Ul(n);
  var e = this.iterator("table", function(r) {
    return Cg(r, t, n);
  }, 1);
  return e.selector.cols = t, e.selector.opts = n, e;
});
Qe("columns().header()", "column().header()", function(t) {
  return this.iterator("column", function(n, e) {
    return nf(n, e, t);
  }, 1);
});
Qe("columns().footer()", "column().footer()", function(t) {
  return this.iterator("column", function(n, e) {
    var r = n.aoFooter;
    return r.length ? n.aoFooter[t !== void 0 ? t : 0][e].cell : null;
  }, 1);
});
Qe("columns().data()", "column().data()", function() {
  return this.iterator("column-rows", Ql, 1);
});
Qe("columns().render()", "column().render()", function(t) {
  return this.iterator("column-rows", function(n, e, r, i, s) {
    return Ql(n, e, r, i, s, t);
  }, 1);
});
Qe("columns().dataSrc()", "column().dataSrc()", function() {
  return this.iterator("column", function(t, n) {
    return t.aoColumns[n].mData;
  }, 1);
});
Qe("columns().cache()", "column().cache()", function(t) {
  return this.iterator("column-rows", function(n, e, r, i, s) {
    return bs(
      n.aoData,
      s,
      t === "search" ? "_aFilterData" : "_aSortData",
      e
    );
  }, 1);
});
Qe("columns().init()", "column().init()", function() {
  return this.iterator("column", function(t, n) {
    return t.aoColumns[n];
  }, 1);
});
Qe("columns().names()", "column().name()", function() {
  return this.iterator("column", function(t, n) {
    return t.aoColumns[n].sName;
  }, 1);
});
Qe("columns().nodes()", "column().nodes()", function() {
  return this.iterator("column-rows", function(t, n, e, r, i) {
    return bs(t.aoData, i, "anCells", n);
  }, 1);
});
Qe("columns().titles()", "column().title()", function(t, n) {
  return this.iterator("column", function(e, r) {
    typeof t == "number" && (n = t, t = void 0);
    var i = q(".dt-column-title", this.column(r).header(n));
    return t !== void 0 ? (i.html(t), this) : i.html();
  }, 1);
});
Qe("columns().types()", "column().type()", function() {
  return this.iterator("column", function(t, n) {
    var e = t.aoColumns[n], r = e.sType;
    return r || (Wa(t), r = e.sType), r;
  }, 1);
});
Qe("columns().visible()", "column().visible()", function(t, n) {
  var e = this, r = [], i = this.iterator("column", function(s, a) {
    if (t === void 0)
      return s.aoColumns[a].bVisible;
    xg(s, a, t) && r.push(a);
  });
  return t !== void 0 && this.iterator("table", function(s) {
    Sa(s, s.aoHeader), Sa(s, s.aoFooter), s.aiDisplay.length || q(s.nTBody).find("td[colspan]").attr("colspan", Va(s)), Ya(s), e.iterator("column", function(a, o) {
      r.includes(o) && qe(a, null, "column-visibility", [a, o, t, n]);
    }), r.length && (n === void 0 || n) && e.columns.adjust();
  }), i;
});
Qe("columns().widths()", "column().width()", function() {
  var t = this.columns(":visible").count(), n = q("<tr>").html("<td>" + Array(t).join("</td><td>") + "</td>");
  q(this.table().body()).append(n);
  var e = n.children().map(function() {
    return q(this).outerWidth();
  });
  return n.remove(), this.iterator("column", function(r, i) {
    var s = ys(r, i);
    return s !== null ? e[s] : 0;
  }, 1);
});
Qe("columns().indexes()", "column().index()", function(t) {
  return this.iterator("column", function(n, e) {
    return t === "visible" ? ys(n, e) : e;
  }, 1);
});
_e("columns.adjust()", function() {
  return this.iterator("table", function(t) {
    t.containerWidth = -1, $a(t);
  }, 1);
});
_e("column.index()", function(t, n) {
  if (this.context.length !== 0) {
    var e = this.context[0];
    if (t === "fromVisible" || t === "toData")
      return Ml(e, n);
    if (t === "fromData" || t === "toVisible")
      return ys(e, n);
  }
});
_e("column()", function(t, n) {
  return Kl(this.columns(t, n));
});
var Sg = function(t, n, e) {
  var r = t.aoData, i = Ga(t, e), s = Id(bs(r, i, "anCells")), a = q(Nd([], s)), o, l = t.aoColumns.length, c, f, g, b, w, C, m = function(h) {
    var u = typeof h == "function";
    if (h == null || u) {
      for (c = [], f = 0, g = i.length; f < g; f++)
        for (o = i[f], b = 0; b < l; b++)
          w = {
            row: o,
            column: b
          }, u ? (C = r[o], h(w, _n(t, o, b), C.anCells ? C.anCells[b] : null) && c.push(w)) : c.push(w);
      return c;
    }
    if (q.isPlainObject(h))
      return h.column !== void 0 && h.row !== void 0 && i.indexOf(h.row) !== -1 ? [h] : [];
    var p = a.filter(h).map(function(v, S) {
      return {
        // use a new object, in case someone changes the values
        row: S._DT_CellIndex.row,
        column: S._DT_CellIndex.column
      };
    }).toArray();
    return p.length || !h.nodeName ? p : (C = q(h).closest("*[data-dt-row]"), C.length ? [{
      row: C.data("dt-row"),
      column: C.data("dt-column")
    }] : []);
  };
  return Gl("cell", n, m, t, e);
};
_e("cells()", function(t, n, e) {
  if (q.isPlainObject(t) && (t.row === void 0 ? (e = t, t = null) : (e = n, n = null)), q.isPlainObject(n) && (e = n, n = null), n == null)
    return this.iterator("table", function(b) {
      return Sg(b, t, Ul(e));
    });
  var r = e ? {
    page: e.page,
    order: e.order,
    search: e.search
  } : {}, i = this.columns(n, r), s = this.rows(t, r), a, o, l, c, f = this.iterator("table", function(b, w) {
    var C = [];
    for (a = 0, o = s[w].length; a < o; a++)
      for (l = 0, c = i[w].length; l < c; l++)
        C.push({
          row: s[w][a],
          column: i[w][l]
        });
    return C;
  }, 1), g = e && e.selected ? this.cells(f, e) : f;
  return q.extend(g.selector, {
    cols: n,
    rows: t,
    opts: e
  }), g;
});
Qe("cells().nodes()", "cell().node()", function() {
  return this.iterator("cell", function(t, n, e) {
    var r = t.aoData[n];
    return r && r.anCells ? r.anCells[e] : void 0;
  }, 1);
});
_e("cells().data()", function() {
  return this.iterator("cell", function(t, n, e) {
    return _n(t, n, e);
  }, 1);
});
Qe("cells().cache()", "cell().cache()", function(t) {
  return t = t === "search" ? "_aFilterData" : "_aSortData", this.iterator("cell", function(n, e, r) {
    return n.aoData[e][t][r];
  }, 1);
});
Qe("cells().render()", "cell().render()", function(t) {
  return this.iterator("cell", function(n, e, r) {
    return _n(n, e, r, t);
  }, 1);
});
Qe("cells().indexes()", "cell().index()", function() {
  return this.iterator("cell", function(t, n, e) {
    return {
      row: n,
      column: e,
      columnVisible: ys(t, e)
    };
  }, 1);
});
Qe("cells().invalidate()", "cell().invalidate()", function(t) {
  return this.iterator("cell", function(n, e, r) {
    ja(n, e, t, r);
  });
});
_e("cell()", function(t, n, e) {
  return Kl(this.cells(t, n, e));
});
_e("cell().data()", function(t) {
  var n = this.context, e = this[0];
  return t === void 0 ? n.length && e.length ? _n(n[0], e[0].row, e[0].column) : void 0 : (Gp(n[0], e[0].row, e[0].column, t), ja(n[0], e[0].row, "data", e[0].column), this);
});
_e("order()", function(t, n) {
  var e = this.context, r = Array.prototype.slice.call(arguments);
  return t === void 0 ? e.length !== 0 ? e[0].aaSorting : void 0 : (typeof t == "number" ? t = [[t, n]] : r.length > 1 && (t = r), this.iterator("table", function(i) {
    var s = [];
    Er(i, s, t), i.aaSorting = s;
  }));
});
_e("order.listener()", function(t, n, e) {
  return this.iterator("table", function(r) {
    Gd(r, t, {}, n, e);
  });
});
_e("order.fixed()", function(t) {
  if (!t) {
    var n = this.context, e = n.length ? n[0].aaSortingFixed : void 0;
    return Array.isArray(e) ? { pre: e } : e;
  }
  return this.iterator("table", function(r) {
    r.aaSortingFixed = q.extend(!0, {}, t);
  });
});
_e([
  "columns().order()",
  "column().order()"
], function(t) {
  var n = this;
  return t ? this.iterator("table", function(e, r) {
    e.aaSorting = n[r].map(function(i) {
      return [i, t];
    });
  }) : this.iterator("column", function(e, r) {
    for (var i = za(e), s = 0, a = i.length; s < a; s++)
      if (i[s].col === r)
        return i[s].dir;
    return null;
  }, 1);
});
Qe("columns().orderable()", "column().orderable()", function(t) {
  return this.iterator("column", function(n, e) {
    var r = n.aoColumns[e];
    return t ? r.asSorting : r.bSortable;
  }, 1);
});
_e("processing()", function(t) {
  return this.iterator("table", function(n) {
    rn(n, t);
  });
});
_e("search()", function(t, n, e, r) {
  var i = this.context;
  return t === void 0 ? i.length !== 0 ? i[0].oPreviousSearch.search : void 0 : this.iterator("table", function(s) {
    s.oFeatures.bFilter && (typeof n == "object" ? gs(s, q.extend(s.oPreviousSearch, n, {
      search: t
    })) : gs(s, q.extend(s.oPreviousSearch, {
      search: t,
      regex: n === null ? !1 : n,
      smart: e === null ? !0 : e,
      caseInsensitive: r === null ? !0 : r
    })));
  });
});
_e("search.fixed()", function(t, n) {
  var e = this.iterator(!0, "table", function(r) {
    var i = r.searchFixed;
    if (t) {
      if (n === void 0)
        return i[t];
      n === null ? delete i[t] : i[t] = n;
    } else
      return Object.keys(i);
    return this;
  });
  return t !== void 0 && n === void 0 ? e[0] : e;
});
Qe(
  "columns().search()",
  "column().search()",
  function(t, n, e, r) {
    return this.iterator("column", function(i, s) {
      var a = i.aoPreSearchCols;
      if (t === void 0)
        return a[s].search;
      i.oFeatures.bFilter && (typeof n == "object" ? q.extend(a[s], n, {
        search: t
      }) : q.extend(a[s], {
        search: t,
        regex: n === null ? !1 : n,
        smart: e === null ? !0 : e,
        caseInsensitive: r === null ? !0 : r
      }), gs(i, i.oPreviousSearch));
    });
  }
);
_e(
  [
    "columns().search.fixed()",
    "column().search.fixed()"
  ],
  function(t, n) {
    var e = this.iterator(!0, "column", function(r, i) {
      var s = r.aoColumns[i].searchFixed;
      if (t) {
        if (n === void 0)
          return s[t] || null;
        n === null ? delete s[t] : s[t] = n;
      } else
        return Object.keys(s);
      return this;
    });
    return t !== void 0 && n === void 0 ? e[0] : e;
  }
);
_e("state()", function(t, n) {
  if (!t)
    return this.context.length ? this.context[0].oSavedState : null;
  var e = q.extend(!0, {}, t);
  return this.iterator("table", function(r) {
    n !== !1 && (e.time = +/* @__PURE__ */ new Date() + 100), bl(r, e, function() {
    });
  });
});
_e("state.clear()", function() {
  return this.iterator("table", function(t) {
    t.fnStateSaveCallback.call(t.oInstance, t, {});
  });
});
_e("state.loaded()", function() {
  return this.context.length ? this.context[0].oLoadedState : null;
});
_e("state.save()", function() {
  return this.iterator("table", function(t) {
    Ya(t);
  });
});
var yc, wc;
H.use = function(t, n) {
  var e = typeof t == "string" ? n : t, r = typeof n == "string" ? n : t;
  if (e === void 0 && typeof r == "string")
    switch (r) {
      case "lib":
      case "jq":
        return q;
      case "win":
        return window;
      case "datetime":
        return H.DateTime;
      case "luxon":
        return or;
      case "moment":
        return Rr;
      case "bootstrap":
        return yc || window.bootstrap;
      case "foundation":
        return wc || window.Foundation;
      default:
        return null;
    }
  r === "lib" || r === "jq" || e && e.fn && e.fn.jquery ? q = e : r === "win" || e && e.document ? (window = e, document = e.document) : r === "datetime" || e && e.type === "DateTime" ? H.DateTime = e : r === "luxon" || e && e.FixedOffsetZone ? or = e : r === "moment" || e && e.isMoment ? Rr = e : r === "bootstrap" || e && e.Modal && e.Modal.NAME === "modal" ? yc = e : (r === "foundation" || e && e.Reveal) && (wc = e);
};
H.factory = function(t, n) {
  var e = !1;
  return t && t.document && (window = t, document = t.document), n && n.fn && n.fn.jquery && (q = n, e = !0), e;
};
H.versionCheck = function(t, n) {
  for (var e = n ? n.split(".") : H.version.split("."), r = t.split("."), i, s, a = 0, o = r.length; a < o; a++)
    if (i = parseInt(e[a], 10) || 0, s = parseInt(r[a], 10) || 0, i !== s)
      return i > s;
  return !0;
};
H.isDataTable = function(t) {
  var n = q(t).get(0), e = !1;
  return t instanceof H.Api ? !0 : (q.each(H.settings, function(r, i) {
    var s = i.nScrollHead ? q("table", i.nScrollHead)[0] : null, a = i.nScrollFoot ? q("table", i.nScrollFoot)[0] : null;
    (i.nTable === n || s === n || a === n) && (e = !0);
  }), e);
};
H.tables = function(t) {
  var n = !1;
  q.isPlainObject(t) && (n = t.api, t = t.visible);
  var e = H.settings.filter(function(r) {
    return !!(!t || t && q(r.nTable).is(":visible"));
  }).map(function(r) {
    return r.nTable;
  });
  return n ? new We(e) : e;
};
H.camelToHungarian = rr;
_e("$()", function(t, n) {
  var e = this.rows(n).nodes(), r = q(e);
  return q([].concat(
    r.filter(t).toArray(),
    r.find(t).toArray()
  ));
});
q.each(["on", "one", "off"], function(t, n) {
  _e(n + "()", function() {
    var e = Array.prototype.slice.call(arguments);
    e[0] = e[0].split(/\s/).map(function(i) {
      return i.match(/\.dt\b/) ? i : i + ".dt";
    }).join(" ");
    var r = q(this.tables().nodes());
    return r[n].apply(r, e), this;
  });
});
_e("clear()", function() {
  return this.iterator("table", function(t) {
    $l(t);
  });
});
_e("error()", function(t) {
  return this.iterator("table", function(n) {
    mn(n, 0, t);
  });
});
_e("settings()", function() {
  return new We(this.context, this.context);
});
_e("init()", function() {
  var t = this.context;
  return t.length ? t[0].oInit : null;
});
_e("data()", function() {
  return this.iterator("table", function(t) {
    return kt(t.aoData, "_aData");
  }).flatten();
});
_e("trigger()", function(t, n, e) {
  return this.iterator("table", function(r) {
    return qe(r, null, t, n, e);
  }).flatten();
});
_e("ready()", function(t) {
  var n = this.context;
  return t ? this.tables().every(function() {
    var e = this;
    this.context[0]._bInitComplete ? t.call(e) : this.on("init.dt.DT", function() {
      t.call(e);
    });
  }) : n.length ? n[0]._bInitComplete || !1 : null;
});
_e("destroy()", function(t) {
  return t = t || !1, this.iterator("table", function(n) {
    var e = n.oClasses, r = n.nTable, i = n.nTBody, s = n.nTHead, a = n.nTFoot, o = q(r), l = q(i), c = q(n.nTableWrapper), f = n.aoData.map(function(h) {
      return h ? h.nTr : null;
    }), g = e.order;
    n.bDestroying = !0, qe(n, "aoDestroyCallback", "destroy", [n], !0), t || new We(n).columns().visible(!0), n.resizeObserver && n.resizeObserver.disconnect(), c.off(".DT").find(":not(tbody *)").off(".DT"), q(window).off(".DT-" + n.sInstance), r != s.parentNode && (o.children("thead").detach(), o.append(s)), a && r != a.parentNode && (o.children("tfoot").detach(), o.append(a)), _c(s, "header"), _c(a, "footer"), n.colgroup.remove(), n.aaSorting = [], n.aaSortingFixed = [], vl(n), q(o).find("th, td").removeClass(
      q.map(H.ext.type.className, function(h) {
        return h;
      }).join(" ")
    ), q("th, td", s).removeClass(
      g.none + " " + g.canAsc + " " + g.canDesc + " " + g.isAsc + " " + g.isDesc
    ).css("width", "").removeAttr("aria-sort"), l.children().detach(), l.append(f);
    var b = n.nTableWrapper.parentNode, w = n.nTableWrapper.nextSibling, C = t ? "remove" : "detach";
    o[C](), c[C](), !t && b && (b.insertBefore(r, w), o.css("width", n.sDestroyWidth).removeClass(e.table));
    var m = H.settings.indexOf(n);
    m !== -1 && H.settings.splice(m, 1);
  });
});
q.each(["column", "row", "cell"], function(t, n) {
  _e(n + "s().every()", function(e) {
    var r = this.selector.opts, i = this, s, a = 0;
    return this.iterator("every", function(o, l, c) {
      s = i[n](l, r), n === "cell" ? e.call(s, s[0][0].row, s[0][0].column, c, a) : e.call(s, l, c, a), a++;
    });
  });
});
_e("i18n()", function(t, n, e) {
  var r = this.context[0], i = Ci(t)(r.oLanguage);
  return i === void 0 && (i = n), q.isPlainObject(i) && (i = e !== void 0 && i[e] !== void 0 ? i[e] : e === !1 ? i : i._), typeof i == "string" ? i.replace("%d", e) : i;
});
function _c(t, n) {
  q(t).find(".dt-column-order").remove(), q(t).find(".dt-column-title").each(function() {
    var e = q(this).html();
    q(this).parent().parent().append(e), q(this).remove();
  }), q(t).find("div.dt-column-" + n).remove(), q("th, td", t).removeAttr("data-dt-column");
}
H.version = "2.3.7";
H.settings = [];
H.models = {};
H.models.oSearch = {
  /**
   * Flag to whether or not the filtering should be case-insensitive
   */
  caseInsensitive: !0,
  /**
   * Applied search term
   */
  search: "",
  /**
   * Flag to indicate if the search term should be interpreted as a
   * regular expression (true) or not (false) and therefore and special
   * regex characters escaped.
   */
  regex: !1,
  /**
   * Flag to indicate if DataTables is to use its smart filtering or not.
   */
  smart: !0,
  /**
   * Flag to indicate if DataTables should only trigger a search when
   * the return key is pressed.
   */
  return: !1
};
H.models.oRow = {
  /**
   * TR element for the row
   */
  nTr: null,
  /**
   * Array of TD elements for each row. This is null until the row has been
   * created.
   */
  anCells: null,
  /**
   * Data object from the original data source for the row. This is either
   * an array if using the traditional form of DataTables, or an object if
   * using mData options. The exact type will depend on the passed in
   * data from the data source, or will be an array if using DOM a data
   * source.
   */
  _aData: [],
  /**
   * Sorting data cache - this array is ostensibly the same length as the
   * number of columns (although each index is generated only as it is
   * needed), and holds the data that is used for sorting each column in the
   * row. We do this cache generation at the start of the sort in order that
   * the formatting of the sort data need be done only once for each cell
   * per sort. This array should not be read from or written to by anything
   * other than the master sorting methods.
   */
  _aSortData: null,
  /**
   * Per cell filtering data cache. As per the sort data cache, used to
   * increase the performance of the filtering in DataTables
   */
  _aFilterData: null,
  /**
   * Filtering data cache. This is the same as the cell filtering cache, but
   * in this case a string rather than an array. This is easily computed with
   * a join on `_aFilterData`, but is provided as a cache so the join isn't
   * needed on every search (memory traded for performance)
   */
  _sFilterRow: null,
  /**
   * Denote if the original data source was from the DOM, or the data source
   * object. This is used for invalidating data, so DataTables can
   * automatically read data from the original source, unless uninstructed
   * otherwise.
   */
  src: null,
  /**
   * Index in the aoData array. This saves an indexOf lookup when we have the
   * object, but want to know the index
   */
  idx: -1,
  /**
   * Cached display value
   */
  displayData: null
};
H.models.oColumn = {
  /**
   * Column index.
   */
  idx: null,
  /**
   * A list of the columns that sorting should occur on when this column
   * is sorted. That this property is an array allows multi-column sorting
   * to be defined for a column (for example first name / last name columns
   * would benefit from this). The values are integers pointing to the
   * columns to be sorted on (typically it will be a single integer pointing
   * at itself, but that doesn't need to be the case).
   */
  aDataSort: null,
  /**
   * Define the sorting directions that are applied to the column, in sequence
   * as the column is repeatedly sorted upon - i.e. the first value is used
   * as the sorting direction when the column if first sorted (clicked on).
   * Sort it again (click again) and it will move on to the next index.
   * Repeat until loop.
   */
  asSorting: null,
  /**
   * Flag to indicate if the column is searchable, and thus should be included
   * in the filtering or not.
   */
  bSearchable: null,
  /**
   * Flag to indicate if the column is sortable or not.
   */
  bSortable: null,
  /**
   * Flag to indicate if the column is currently visible in the table or not
   */
  bVisible: null,
  /**
   * Store for manual type assignment using the `column.type` option. This
   * is held in store so we can manipulate the column's `sType` property.
   */
  _sManualType: null,
  /**
   * Flag to indicate if HTML5 data attributes should be used as the data
   * source for filtering or sorting. True is either are.
   */
  _bAttrSrc: !1,
  /**
   * Developer definable function that is called whenever a cell is created (Ajax source,
   * etc) or processed for input (DOM source). This can be used as a compliment to mRender
   * allowing you to modify the DOM element (add background colour for example) when the
   * element is available.
   */
  fnCreatedCell: null,
  /**
   * Function to get data from a cell in a column. You should <b>never</b>
   * access data directly through _aData internally in DataTables - always use
   * the method attached to this property. It allows mData to function as
   * required. This function is automatically assigned by the column
   * initialisation method
   */
  fnGetData: null,
  /**
   * Function to set data for a cell in the column. You should <b>never</b>
   * set the data directly to _aData internally in DataTables - always use
   * this method. It allows mData to function as required. This function
   * is automatically assigned by the column initialisation method
   */
  fnSetData: null,
  /**
   * Property to read the value for the cells in the column from the data
   * source array / object. If null, then the default content is used, if a
   * function is given then the return from the function is used.
   */
  mData: null,
  /**
   * Partner property to mData which is used (only when defined) to get
   * the data - i.e. it is basically the same as mData, but without the
   * 'set' option, and also the data fed to it is the result from mData.
   * This is the rendering method to match the data method of mData.
   */
  mRender: null,
  /**
   * The class to apply to all TD elements in the table's TBODY for the column
   */
  sClass: null,
  /**
   * When DataTables calculates the column widths to assign to each column,
   * it finds the longest string in each column and then constructs a
   * temporary table and reads the widths from that. The problem with this
   * is that "mmm" is much wider then "iiii", but the latter is a longer
   * string - thus the calculation can go wrong (doing it properly and putting
   * it into an DOM object and measuring that is horribly(!) slow). Thus as
   * a "work around" we provide this option. It will append its value to the
   * text that is found to be the longest string for the column - i.e. padding.
   */
  sContentPadding: null,
  /**
   * Allows a default value to be given for a column's data, and will be used
   * whenever a null data source is encountered (this can be because mData
   * is set to null, or because the data source itself is null).
   */
  sDefaultContent: null,
  /**
   * Name for the column, allowing reference to the column by name as well as
   * by index (needs a lookup to work by name).
   */
  sName: null,
  /**
   * Custom sorting data type - defines which of the available plug-ins in
   * afnSortData the custom sorting will use - if any is defined.
   */
  sSortDataType: "std",
  /**
   * Class to be applied to the header element when sorting on this column
   */
  sSortingClass: null,
  /**
   * Title of the column - what is seen in the TH element (nTh).
   */
  sTitle: null,
  /**
   * Column sorting and filtering type
   */
  sType: null,
  /**
   * Width of the column
   */
  sWidth: null,
  /**
   * Width of the column when it was first "encountered"
   */
  sWidthOrig: null,
  /** Cached longest strings from a column */
  wideStrings: null,
  /**
   * Store for named searches
   */
  searchFixed: null
};
H.defaults = {
  /**
   * An array of data to use for the table, passed in at initialisation which
   * will be used in preference to any data which is already in the DOM. This is
   * particularly useful for constructing tables purely in JavaScript, for
   * example with a custom Ajax call.
   */
  aaData: null,
  /**
   * If ordering is enabled, then DataTables will perform a first pass sort on
   * initialisation. You can define which column(s) the sort is performed
   * upon, and the sorting direction, with this variable. The `sorting` array
   * should contain an array for each column to be sorted initially containing
   * the column's index and a direction string ('asc' or 'desc').
   */
  aaSorting: [[0, "asc"]],
  /**
   * This parameter is basically identical to the `sorting` parameter, but
   * cannot be overridden by user interaction with the table. What this means
   * is that you could have a column (visible or hidden) which the sorting
   * will always be forced on first - any sorting after that (from the user)
   * will then be performed as required. This can be useful for grouping rows
   * together.
   */
  aaSortingFixed: [],
  /**
   * DataTables can be instructed to load data to display in the table from a
   * Ajax source. This option defines how that Ajax call is made and where to.
   *
   * The `ajax` property has three different modes of operation, depending on
   * how it is defined. These are:
   *
   * * `string` - Set the URL from where the data should be loaded from.
   * * `object` - Define properties for `jQuery.ajax`.
   * * `function` - Custom data get function
   *
   * `string`
   * --------
   *
   * As a string, the `ajax` property simply defines the URL from which
   * DataTables will load data.
   *
   * `object`
   * --------
   *
   * As an object, the parameters in the object are passed to
   * [jQuery.ajax](https://api.jquery.com/jQuery.ajax/) allowing fine control
   * of the Ajax request. DataTables has a number of default parameters which
   * you can override using this option. Please refer to the jQuery
   * documentation for a full description of the options available, although
   * the following parameters provide additional options in DataTables or
   * require special consideration:
   *
   * * `data` - As with jQuery, `data` can be provided as an object, but it
   *   can also be used as a function to manipulate the data DataTables sends
   *   to the server. The function takes a single parameter, an object of
   *   parameters with the values that DataTables has readied for sending. An
   *   object may be returned which will be merged into the DataTables
   *   defaults, or you can add the items to the object that was passed in and
   *   not return anything from the function. This supersedes `fnServerParams`
   *   from DataTables 1.9-.
   *
   * * `dataSrc` - By default DataTables will look for the property `data` (or
   *   `aaData` for compatibility with DataTables 1.9-) when obtaining data
   *   from an Ajax source or for server-side processing - this parameter
   *   allows that property to be changed. You can use JavaScript dotted
   *   object notation to get a data source for multiple levels of nesting, or
   *   it my be used as a function. As a function it takes a single parameter,
   *   the JSON returned from the server, which can be manipulated as
   *   required, with the returned value being that used by DataTables as the
   *   data source for the table.
   *
   * * `success` - Should not be overridden it is used internally in
   *   DataTables. To manipulate / transform the data returned by the server
   *   use `ajax.dataSrc`, or use `ajax` as a function (see below).
   *
   * `function`
   * ----------
   *
   * As a function, making the Ajax call is left up to yourself allowing
   * complete control of the Ajax request. Indeed, if desired, a method other
   * than Ajax could be used to obtain the required data, such as Web storage
   * or an AIR database.
   *
   * The function is given four parameters and no return is required. The
   * parameters are:
   *
   * 1. _object_ - Data to send to the server
   * 2. _function_ - Callback function that must be executed when the required
   *    data has been obtained. That data should be passed into the callback
   *    as the only parameter
   * 3. _object_ - DataTables settings object for the table
   */
  ajax: null,
  /**
   * This parameter allows you to readily specify the entries in the length drop
   * down menu that DataTables shows when pagination is enabled. It can be
   * either a 1D array of options which will be used for both the displayed
   * option and the value, or a 2D array which will use the array in the first
   * position as the value, and the array in the second position as the
   * displayed options (useful for language strings such as 'All').
   *
   * Note that the `pageLength` property will be automatically set to the
   * first value given in this array, unless `pageLength` is also provided.
   */
  aLengthMenu: [10, 25, 50, 100],
  /**
   * The `columns` option in the initialisation parameter allows you to define
   * details about the way individual columns behave. For a full list of
   * column options that can be set, please see
   * {@link DataTable.defaults.column}. Note that if you use `columns` to
   * define your columns, you must have an entry in the array for every single
   * column that you have in your table (these can be null if you don't which
   * to specify any options).
   */
  aoColumns: null,
  /**
   * Very similar to `columns`, `columnDefs` allows you to target a specific
   * column, multiple columns, or all columns, using the `targets` property of
   * each object in the array. This allows great flexibility when creating
   * tables, as the `columnDefs` arrays can be of any length, targeting the
   * columns you specifically want. `columnDefs` may use any of the column
   * options available: {@link DataTable.defaults.column}, but it _must_
   * have `targets` defined in each object in the array. Values in the `targets`
   * array may be:
   *   <ul>
   *     <li>a string - class name will be matched on the TH for the column</li>
   *     <li>0 or a positive integer - column index counting from the left</li>
   *     <li>a negative integer - column index counting from the right</li>
   *     <li>the string "_all" - all columns (i.e. assign a default)</li>
   *   </ul>
   */
  aoColumnDefs: null,
  /**
   * Basically the same as `search`, this parameter defines the individual column
   * filtering state at initialisation time. The array must be of the same size
   * as the number of columns, and each element be an object with the parameters
   * `search` and `escapeRegex` (the latter is optional). 'null' is also
   * accepted and the default will be used.
   */
  aoSearchCols: [],
  /**
   * Enable or disable automatic column width calculation. This can be disabled
   * as an optimisation (it takes some time to calculate the widths) if the
   * tables widths are passed in using `columns`.
   */
  bAutoWidth: !0,
  /**
   * Deferred rendering can provide DataTables with a huge speed boost when you
   * are using an Ajax or JS data source for the table. This option, when set to
   * true, will cause DataTables to defer the creation of the table elements for
   * each row until they are needed for a draw - saving a significant amount of
   * time.
   */
  bDeferRender: !0,
  /**
   * Replace a DataTable which matches the given selector and replace it with
   * one which has the properties of the new initialisation object passed. If no
   * table matches the selector, then the new DataTable will be constructed as
   * per normal.
   */
  bDestroy: !1,
  /**
   * Enable or disable filtering of data. Filtering in DataTables is "smart" in
   * that it allows the end user to input multiple words (space separated) and
   * will match a row containing those words, even if not in the order that was
   * specified (this allow matching across multiple columns). Note that if you
   * wish to use filtering in DataTables this must remain 'true' - to remove the
   * default filtering input box and retain filtering abilities, please use
   * {@link DataTable.defaults.dom}.
   */
  bFilter: !0,
  /**
   * Used only for compatibility with DT1
   * @deprecated
   */
  bInfo: !0,
  /**
   * Used only for compatibility with DT1
   * @deprecated
   */
  bLengthChange: !0,
  /**
   * Enable or disable pagination.
   */
  bPaginate: !0,
  /**
   * Enable or disable the display of a 'processing' indicator when the table is
   * being processed (e.g. a sort). This is particularly useful for tables with
   * large amounts of data where it can take a noticeable amount of time to sort
   * the entries.
   */
  bProcessing: !1,
  /**
   * Retrieve the DataTables object for the given selector. Note that if the
   * table has already been initialised, this parameter will cause DataTables
   * to simply return the object that has already been set up - it will not take
   * account of any changes you might have made to the initialisation object
   * passed to DataTables (setting this parameter to true is an acknowledgement
   * that you understand this). `destroy` can be used to reinitialise a table if
   * you need.
   */
  bRetrieve: !1,
  /**
   * When vertical (y) scrolling is enabled, DataTables will force the height of
   * the table's viewport to the given height at all times (useful for layout).
   * However, this can look odd when filtering data down to a small data set,
   * and the footer is left "floating" further down. This parameter (when
   * enabled) will cause DataTables to collapse the table's viewport down when
   * the result set will fit within the given Y height.
   */
  bScrollCollapse: !1,
  /**
   * Configure DataTables to use server-side processing. Note that the
   * `ajax` parameter must also be given in order to give DataTables a
   * source to obtain the required data for each draw.
   */
  bServerSide: !1,
  /**
   * Enable or disable sorting of columns. Sorting of individual columns can be
   * disabled by the `sortable` option for each column.
   */
  bSort: !0,
  /**
   * Enable or display DataTables' ability to sort multiple columns at the
   * same time (activated by shift-click by the user).
   */
  bSortMulti: !0,
  /**
   * Allows control over whether DataTables should use the top (true) unique
   * cell that is found for a single column, or the bottom (false - default).
   * This is useful when using complex headers.
   */
  bSortCellsTop: null,
  /** Specify which row is the title row in the header. Replacement for bSortCellsTop */
  titleRow: null,
  /**
   * Enable or disable the addition of the classes `sorting\_1`, `sorting\_2` and
   * `sorting\_3` to the columns which are currently being sorted on. This is
   * presented as a feature switch as it can increase processing time (while
   * classes are removed and added) so for large data sets you might want to
   * turn this off.
   */
  bSortClasses: !0,
  /**
   * Enable or disable state saving. When enabled HTML5 `localStorage` will be
   * used to save table display information such as pagination information,
   * display length, filtering and sorting. As such when the end user reloads
   * the page the display will match what thy had previously set up.
   */
  bStateSave: !1,
  /**
   * This function is called when a TR element is created (and all TD child
   * elements have been inserted), or registered if using a DOM source, allowing
   * manipulation of the TR element (adding classes etc).
   */
  fnCreatedRow: null,
  /**
   * This function is called on every 'draw' event, and allows you to
   * dynamically modify any aspect you want about the created DOM.
   */
  fnDrawCallback: null,
  /**
   * Identical to fnHeaderCallback() but for the table footer this function
   * allows you to modify the table footer on every 'draw' event.
   */
  fnFooterCallback: null,
  /**
   * When rendering large numbers in the information element for the table
   * (i.e. "Showing 1 to 10 of 57 entries") DataTables will render large numbers
   * to have a comma separator for the 'thousands' units (e.g. 1 million is
   * rendered as "1,000,000") to help readability for the end user. This
   * function will override the default method DataTables uses.
   */
  fnFormatNumber: function(t) {
    return t.toString().replace(
      /\B(?=(\d{3})+(?!\d))/g,
      this.oLanguage.sThousands
    );
  },
  /**
   * This function is called on every 'draw' event, and allows you to
   * dynamically modify the header row. This can be used to calculate and
   * display useful information about the table.
   */
  fnHeaderCallback: null,
  /**
   * The information element can be used to convey information about the current
   * state of the table. Although the internationalisation options presented by
   * DataTables are quite capable of dealing with most customisations, there may
   * be times where you wish to customise the string further. This callback
   * allows you to do exactly that.
   */
  fnInfoCallback: null,
  /**
   * Called when the table has been initialised. Normally DataTables will
   * initialise sequentially and there will be no need for this function,
   * however, this does not hold true when using external language information
   * since that is obtained using an async XHR call.
   */
  fnInitComplete: null,
  /**
   * Called at the very start of each table draw and can be used to cancel the
   * draw by returning false, any other return (including undefined) results in
   * the full draw occurring).
   */
  fnPreDrawCallback: null,
  /**
   * This function allows you to 'post process' each row after it have been
   * generated for each table draw, but before it is rendered on screen. This
   * function might be used for setting the row class name etc.
   */
  fnRowCallback: null,
  /**
   * Load the table state. With this function you can define from where, and how, the
   * state of a table is loaded. By default DataTables will load from `localStorage`
   * but you might wish to use a server-side database or cookies.
   */
  fnStateLoadCallback: function(t) {
    try {
      return JSON.parse(
        (t.iStateDuration === -1 ? sessionStorage : localStorage).getItem(
          "DataTables_" + t.sInstance + "_" + location.pathname
        )
      );
    } catch {
      return {};
    }
  },
  /**
   * Callback which allows modification of the saved state prior to loading that state.
   * This callback is called when the table is loading state from the stored data, but
   * prior to the settings object being modified by the saved state. Note that for
   * plug-in authors, you should use the `stateLoadParams` event to load parameters for
   * a plug-in.
   */
  fnStateLoadParams: null,
  /**
   * Callback that is called when the state has been loaded from the state saving method
   * and the DataTables settings object has been modified as a result of the loaded state.
   */
  fnStateLoaded: null,
  /**
   * Save the table state. This function allows you to define where and how the state
   * information for the table is stored By default DataTables will use `localStorage`
   * but you might wish to use a server-side database or cookies.
   */
  fnStateSaveCallback: function(t, n) {
    try {
      (t.iStateDuration === -1 ? sessionStorage : localStorage).setItem(
        "DataTables_" + t.sInstance + "_" + location.pathname,
        JSON.stringify(n)
      );
    } catch {
    }
  },
  /**
   * Callback which allows modification of the state to be saved. Called when the table
   * has changed state a new state save is required. This method allows modification of
   * the state saving object prior to actually doing the save, including addition or
   * other state properties or modification. Note that for plug-in authors, you should
   * use the `stateSaveParams` event to save parameters for a plug-in.
   */
  fnStateSaveParams: null,
  /**
   * Duration for which the saved state information is considered valid. After this period
   * has elapsed the state will be returned to the default.
   * Value is given in seconds.
   */
  iStateDuration: 7200,
  /**
   * Number of rows to display on a single page when using pagination. If
   * feature enabled (`lengthChange`) then the end user will be able to override
   * this to a custom setting using a pop-up menu.
   */
  iDisplayLength: 10,
  /**
   * Define the starting point for data display when using DataTables with
   * pagination. Note that this parameter is the number of records, rather than
   * the page number, so if you have 10 records per page and want to start on
   * the third page, it should be "20".
   */
  iDisplayStart: 0,
  /**
   * By default DataTables allows keyboard navigation of the table (sorting, paging,
   * and filtering) by adding a `tabindex` attribute to the required elements. This
   * allows you to tab through the controls and press the enter key to activate them.
   * The tabindex is default 0, meaning that the tab follows the flow of the document.
   * You can overrule this using this parameter if you wish. Use a value of -1 to
   * disable built-in keyboard navigation.
   */
  iTabIndex: 0,
  /**
   * Classes that DataTables assigns to the various components and features
   * that it adds to the HTML table. This allows classes to be configured
   * during initialisation in addition to through the static
   * {@link DataTable.ext.oStdClasses} object).
   */
  oClasses: {},
  /**
   * All strings that DataTables uses in the user interface that it creates
   * are defined in this object, allowing you to modified them individually or
   * completely replace them all as required.
   */
  oLanguage: {
    /**
     * Strings that are used for WAI-ARIA labels and controls only (these are not
     * actually visible on the page, but will be read by screenreaders, and thus
     * must be internationalised as well).
     */
    oAria: {
      /**
       * ARIA label that is added to the table headers when the column may be sorted
       */
      orderable: ": Activate to sort",
      /**
       * ARIA label that is added to the table headers when the column is currently being sorted
       */
      orderableReverse: ": Activate to invert sorting",
      /**
       * ARIA label that is added to the table headers when the column is currently being 
       * sorted and next step is to remove sorting
       */
      orderableRemove: ": Activate to remove sorting",
      paginate: {
        first: "First",
        last: "Last",
        next: "Next",
        previous: "Previous",
        number: ""
      }
    },
    /**
     * Pagination string used by DataTables for the built-in pagination
     * control types.
     */
    oPaginate: {
      /**
       * Label and character for first page button («)
       */
      sFirst: "«",
      /**
       * Last page button (»)
       */
      sLast: "»",
      /**
       * Next page button (›)
       */
      sNext: "›",
      /**
       * Previous page button (‹)
       */
      sPrevious: "‹"
    },
    /**
     * Plural object for the data type the table is showing
     */
    entries: {
      _: "entries",
      1: "entry"
    },
    /**
     * Page length options
     */
    lengthLabels: {
      "-1": "All"
    },
    /**
     * This string is shown in preference to `zeroRecords` when the table is
     * empty of data (regardless of filtering). Note that this is an optional
     * parameter - if it is not given, the value of `zeroRecords` will be used
     * instead (either the default or given value).
     */
    sEmptyTable: "No data available in table",
    /**
     * This string gives information to the end user about the information
     * that is current on display on the page. The following tokens can be
     * used in the string and will be dynamically replaced as the table
     * display updates. This tokens can be placed anywhere in the string, or
     * removed as needed by the language requires:
     *
     * * `\_START\_` - Display index of the first record on the current page
     * * `\_END\_` - Display index of the last record on the current page
     * * `\_TOTAL\_` - Number of records in the table after filtering
     * * `\_MAX\_` - Number of records in the table without filtering
     * * `\_PAGE\_` - Current page number
     * * `\_PAGES\_` - Total number of pages of data in the table
     */
    sInfo: "Showing _START_ to _END_ of _TOTAL_ _ENTRIES-TOTAL_",
    /**
     * Display information string for when the table is empty. Typically the
     * format of this string should match `info`.
     */
    sInfoEmpty: "Showing 0 to 0 of 0 _ENTRIES-TOTAL_",
    /**
     * When a user filters the information in a table, this string is appended
     * to the information (`info`) to give an idea of how strong the filtering
     * is. The variable _MAX_ is dynamically updated.
     */
    sInfoFiltered: "(filtered from _MAX_ total _ENTRIES-MAX_)",
    /**
     * If can be useful to append extra information to the info string at times,
     * and this variable does exactly that. This information will be appended to
     * the `info` (`infoEmpty` and `infoFiltered` in whatever combination they are
     * being used) at all times.
     */
    sInfoPostFix: "",
    /**
     * This decimal place operator is a little different from the other
     * language options since DataTables doesn't output floating point
     * numbers, so it won't ever use this for display of a number. Rather,
     * what this parameter does is modify the sort methods of the table so
     * that numbers which are in a format which has a character other than
     * a period (`.`) as a decimal place will be sorted numerically.
     *
     * Note that numbers with different decimal places cannot be shown in
     * the same table and still be sortable, the table must be consistent.
     * However, multiple different tables on the page can use different
     * decimal place characters.
     */
    sDecimal: "",
    /**
     * DataTables has a build in number formatter (`formatNumber`) which is
     * used to format large numbers that are used in the table information.
     * By default a comma is used, but this can be trivially changed to any
     * character you wish with this parameter.
     */
    sThousands: ",",
    /**
     * Detail the action that will be taken when the drop down menu for the
     * pagination length option is changed. The '_MENU_' variable is replaced
     * with a default select list of 10, 25, 50 and 100, and can be replaced
     * with a custom select box if required.
     */
    sLengthMenu: "_MENU_ _ENTRIES_ per page",
    /**
     * When using Ajax sourced data and during the first draw when DataTables is
     * gathering the data, this message is shown in an empty row in the table to
     * indicate to the end user the data is being loaded. Note that this
     * parameter is not used when loading data by server-side processing, just
     * Ajax sourced data with client-side processing.
     */
    sLoadingRecords: "Loading...",
    /**
     * Text which is displayed when the table is processing a user action
     * (usually a sort command or similar).
     */
    sProcessing: "",
    /**
     * Details the actions that will be taken when the user types into the
     * filtering input text box. The variable "_INPUT_", if used in the string,
     * is replaced with the HTML text box for the filtering input allowing
     * control over where it appears in the string. If "_INPUT_" is not given
     * then the input box is appended to the string automatically.
     */
    sSearch: "Search:",
    /**
     * Assign a `placeholder` attribute to the search `input` element
     *  @type string
     *  @default 
     *
     *  @dtopt Language
     *  @name DataTable.defaults.language.searchPlaceholder
     */
    sSearchPlaceholder: "",
    /**
     * All of the language information can be stored in a file on the
     * server-side, which DataTables will look up if this parameter is passed.
     * It must store the URL of the language file, which is in a JSON format,
     * and the object has the same properties as the oLanguage object in the
     * initialiser object (i.e. the above parameters). Please refer to one of
     * the example language files to see how this works in action.
     */
    sUrl: "",
    /**
     * Text shown inside the table records when the is no information to be
     * displayed after filtering. `emptyTable` is shown when there is simply no
     * information in the table at all (regardless of filtering).
     */
    sZeroRecords: "No matching records found"
  },
  /** The initial data order is reversed when `desc` ordering */
  orderDescReverse: !0,
  /**
   * This parameter allows you to have define the global filtering state at
   * initialisation time. As an object the `search` parameter must be
   * defined, but all other parameters are optional. When `regex` is true,
   * the search string will be treated as a regular expression, when false
   * (default) it will be treated as a straight string. When `smart`
   * DataTables will use it's smart filtering methods (to word match at
   * any point in the data), when false this will not be done.
   */
  oSearch: q.extend({}, H.models.oSearch),
  /**
   * Table and control layout. This replaces the legacy `dom` option.
   */
  layout: {
    topStart: "pageLength",
    topEnd: "search",
    bottomStart: "info",
    bottomEnd: "paging"
  },
  /**
   * Legacy DOM layout option
   */
  sDom: null,
  /**
   * Search delay option. This will throttle full table searches that use the
   * DataTables provided search input element (it does not effect calls to
   * `dt-api search()`, providing a delay before the search is made.
   */
  searchDelay: null,
  /**
   * DataTables features six different built-in options for the buttons to
   * display for pagination control:
   *
   * * `numbers` - Page number buttons only
   * * `simple` - 'Previous' and 'Next' buttons only
   * * 'simple_numbers` - 'Previous' and 'Next' buttons, plus page numbers
   * * `full` - 'First', 'Previous', 'Next' and 'Last' buttons
   * * `full_numbers` - 'First', 'Previous', 'Next' and 'Last' buttons, plus page numbers
   * * `first_last_numbers` - 'First' and 'Last' buttons, plus page numbers
   */
  sPaginationType: "",
  /**
   * Enable horizontal scrolling. When a table is too wide to fit into a
   * certain layout, or you have a large number of columns in the table, you
   * can enable x-scrolling to show the table in a viewport, which can be
   * scrolled. This property can be `true` which will allow the table to
   * scroll horizontally when needed, or any CSS unit, or a number (in which
   * case it will be treated as a pixel measurement). Setting as simply `true`
   * is recommended.
   */
  sScrollX: "",
  /**
   * This property can be used to force a DataTable to use more width than it
   * might otherwise do when x-scrolling is enabled. For example if you have a
   * table which requires to be well spaced, this parameter is useful for
   * "over-sizing" the table, and thus forcing scrolling. This property can by
   * any CSS unit, or a number (in which case it will be treated as a pixel
   * measurement).
   */
  sScrollXInner: "",
  /**
   * Enable vertical scrolling. Vertical scrolling will constrain the DataTable
   * to the given height, and enable scrolling for any data which overflows the
   * current viewport. This can be used as an alternative to paging to display
   * a lot of data in a small area (although paging and scrolling can both be
   * enabled at the same time). This property can be any CSS unit, or a number
   * (in which case it will be treated as a pixel measurement).
   */
  sScrollY: "",
  /**
   * __Deprecated__ The functionality provided by this parameter has now been
   * superseded by that provided through `ajax`, which should be used instead.
   *
   * Set the HTTP method that is used to make the Ajax call for server-side
   * processing or Ajax sourced data.
   */
  sServerMethod: "GET",
  /**
   * DataTables makes use of renderers when displaying HTML elements for
   * a table. These renderers can be added or modified by plug-ins to
   * generate suitable mark-up for a site. For example the Bootstrap
   * integration plug-in for DataTables uses a paging button renderer to
   * display pagination buttons in the mark-up required by Bootstrap.
   *
   * For further information about the renderers available see
   * DataTable.ext.renderer
   */
  renderer: null,
  /**
   * Set the data property name that DataTables should use to get a row's id
   * to set as the `id` property in the node.
   */
  rowId: "DT_RowId",
  /**
   * Caption value
   */
  caption: null,
  /**
   * For server-side processing - use the data from the DOM for the first draw
   */
  iDeferLoading: null,
  /** Event listeners */
  on: null,
  /** Title wrapper element type */
  columnTitleTag: "span"
};
Ha(H.defaults);
H.defaults.column = {
  /**
   * Define which column(s) an order will occur on for this column. This
   * allows a column's ordering to take multiple columns into account when
   * doing a sort or use the data from a different column. For example first
   * name / last name columns make sense to do a multi-column sort over the
   * two columns.
   */
  aDataSort: null,
  iDataSort: -1,
  ariaTitle: "",
  /**
   * You can control the default ordering direction, and even alter the
   * behaviour of the sort handler (i.e. only allow ascending ordering etc)
   * using this parameter.
   */
  asSorting: ["asc", "desc", ""],
  /**
   * Enable or disable filtering on the data in this column.
   */
  bSearchable: !0,
  /**
   * Enable or disable ordering on this column.
   */
  bSortable: !0,
  /**
   * Enable or disable the display of this column.
   */
  bVisible: !0,
  /**
   * Developer definable function that is called whenever a cell is created (Ajax source,
   * etc) or processed for input (DOM source). This can be used as a compliment to mRender
   * allowing you to modify the DOM element (add background colour for example) when the
   * element is available.
   */
  fnCreatedCell: null,
  /**
   * This property can be used to read data from any data source property,
   * including deeply nested objects / properties. `data` can be given in a
   * number of different ways which effect its behaviour:
   *
   * * `integer` - treated as an array index for the data source. This is the
   *   default that DataTables uses (incrementally increased for each column).
   * * `string` - read an object property from the data source. There are
   *   three 'special' options that can be used in the string to alter how
   *   DataTables reads the data from the source object:
   *    * `.` - Dotted JavaScript notation. Just as you use a `.` in
   *      JavaScript to read from nested objects, so to can the options
   *      specified in `data`. For example: `browser.version` or
   *      `browser.name`. If your object parameter name contains a period, use
   *      `\\` to escape it - i.e. `first\\.name`.
   *    * `[]` - Array notation. DataTables can automatically combine data
   *      from and array source, joining the data with the characters provided
   *      between the two brackets. For example: `name[, ]` would provide a
   *      comma-space separated list from the source array. If no characters
   *      are provided between the brackets, the original array source is
   *      returned.
   *    * `()` - Function notation. Adding `()` to the end of a parameter will
   *      execute a function of the name given. For example: `browser()` for a
   *      simple function on the data source, `browser.version()` for a
   *      function in a nested property or even `browser().version` to get an
   *      object property if the function called returns an object. Note that
   *      function notation is recommended for use in `render` rather than
   *      `data` as it is much simpler to use as a renderer.
   * * `null` - use the original data source for the row rather than plucking
   *   data directly from it. This action has effects on two other
   *   initialisation options:
   *    * `defaultContent` - When null is given as the `data` option and
   *      `defaultContent` is specified for the column, the value defined by
   *      `defaultContent` will be used for the cell.
   *    * `render` - When null is used for the `data` option and the `render`
   *      option is specified for the column, the whole data source for the
   *      row is used for the renderer.
   * * `function` - the function given will be executed whenever DataTables
   *   needs to set or get the data for a cell in the column. The function
   *   takes three parameters:
   *    * Parameters:
   *      * `{array|object}` The data source for the row
   *      * `{string}` The type call data requested - this will be 'set' when
   *        setting data or 'filter', 'display', 'type', 'sort' or undefined
   *        when gathering data. Note that when `undefined` is given for the
   *        type DataTables expects to get the raw data for the object back<
   *      * `{*}` Data to set when the second parameter is 'set'.
   *    * Return:
   *      * The return value from the function is not required when 'set' is
   *        the type of call, but otherwise the return is what will be used
   *        for the data requested.
   *
   * Note that `data` is a getter and setter option. If you just require
   * formatting of data for output, you will likely want to use `render` which
   * is simply a getter and thus simpler to use.
   *
   * Note that prior to DataTables 1.9.2 `data` was called `mDataProp`. The
   * name change reflects the flexibility of this property and is consistent
   * with the naming of mRender. If 'mDataProp' is given, then it will still
   * be used by DataTables, as it automatically maps the old name to the new
   * if required.
   */
  mData: null,
  /**
   * This property is the rendering partner to `data` and it is suggested that
   * when you want to manipulate data for display (including filtering,
   * sorting etc) without altering the underlying data for the table, use this
   * property. `render` can be considered to be the read only companion to
   * `data` which is read / write (then as such more complex). Like `data`
   * this option can be given in a number of different ways to effect its
   * behaviour:
   *
   * * `integer` - treated as an array index for the data source. This is the
   *   default that DataTables uses (incrementally increased for each column).
   * * `string` - read an object property from the data source. There are
   *   three 'special' options that can be used in the string to alter how
   *   DataTables reads the data from the source object:
   *    * `.` - Dotted JavaScript notation. Just as you use a `.` in
   *      JavaScript to read from nested objects, so to can the options
   *      specified in `data`. For example: `browser.version` or
   *      `browser.name`. If your object parameter name contains a period, use
   *      `\\` to escape it - i.e. `first\\.name`.
   *    * `[]` - Array notation. DataTables can automatically combine data
   *      from and array source, joining the data with the characters provided
   *      between the two brackets. For example: `name[, ]` would provide a
   *      comma-space separated list from the source array. If no characters
   *      are provided between the brackets, the original array source is
   *      returned.
   *    * `()` - Function notation. Adding `()` to the end of a parameter will
   *      execute a function of the name given. For example: `browser()` for a
   *      simple function on the data source, `browser.version()` for a
   *      function in a nested property or even `browser().version` to get an
   *      object property if the function called returns an object.
   * * `object` - use different data for the different data types requested by
   *   DataTables ('filter', 'display', 'type' or 'sort'). The property names
   *   of the object is the data type the property refers to and the value can
   *   defined using an integer, string or function using the same rules as
   *   `render` normally does. Note that an `_` option _must_ be specified.
   *   This is the default value to use if you haven't specified a value for
   *   the data type requested by DataTables.
   * * `function` - the function given will be executed whenever DataTables
   *   needs to set or get the data for a cell in the column. The function
   *   takes three parameters:
   *    * Parameters:
   *      * {array|object} The data source for the row (based on `data`)
   *      * {string} The type call data requested - this will be 'filter',
   *        'display', 'type' or 'sort'.
   *      * {array|object} The full data source for the row (not based on
   *        `data`)
   *    * Return:
   *      * The return value from the function is what will be used for the
   *        data requested.
   */
  mRender: null,
  /**
   * Change the cell type created for the column - either TD cells or TH cells. This
   * can be useful as TH cells have semantic meaning in the table body, allowing them
   * to act as a header for a row (you may wish to add scope='row' to the TH elements).
   */
  sCellType: "td",
  /**
   * Class to give to each cell in this column.
   */
  sClass: "",
  /**
   * When DataTables calculates the column widths to assign to each column,
   * it finds the longest string in each column and then constructs a
   * temporary table and reads the widths from that. The problem with this
   * is that "mmm" is much wider then "iiii", but the latter is a longer
   * string - thus the calculation can go wrong (doing it properly and putting
   * it into an DOM object and measuring that is horribly(!) slow). Thus as
   * a "work around" we provide this option. It will append its value to the
   * text that is found to be the longest string for the column - i.e. padding.
   * Generally you shouldn't need this!
   */
  sContentPadding: "",
  /**
   * Allows a default value to be given for a column's data, and will be used
   * whenever a null data source is encountered (this can be because `data`
   * is set to null, or because the data source itself is null).
   */
  sDefaultContent: null,
  /**
   * This parameter is only used in DataTables' server-side processing. It can
   * be exceptionally useful to know what columns are being displayed on the
   * client side, and to map these to database fields. When defined, the names
   * also allow DataTables to reorder information from the server if it comes
   * back in an unexpected order (i.e. if you switch your columns around on the
   * client-side, your server-side code does not also need updating).
   */
  sName: "",
  /**
   * Defines a data source type for the ordering which can be used to read
   * real-time information from the table (updating the internally cached
   * version) prior to ordering. This allows ordering to occur on user
   * editable elements such as form inputs.
   */
  sSortDataType: "std",
  /**
   * The title of this column.
   */
  sTitle: null,
  /**
   * The type allows you to specify how the data for this column will be
   * ordered. Four types (string, numeric, date and html (which will strip
   * HTML tags before ordering)) are currently available. Note that only date
   * formats understood by JavaScript's Date() object will be accepted as type
   * date. For example: "Mar 26, 2008 5:03 PM". May take the values: 'string',
   * 'numeric', 'date' or 'html' (by default). Further types can be adding
   * through plug-ins.
   */
  sType: null,
  /**
   * Defining the width of the column, this parameter may take any CSS value
   * (3em, 20px etc). DataTables applies 'smart' widths to columns which have not
   * been given a specific width through this interface ensuring that the table
   * remains readable.
   */
  sWidth: null
};
Ha(H.defaults.column);
H.models.oSettings = {
  /**
   * Primary features of DataTables and their enablement state.
   */
  oFeatures: {
    /**
     * Flag to say if DataTables should automatically try to calculate the
     * optimum table and columns widths (true) or not (false).
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bAutoWidth: null,
    /**
     * Delay the creation of TR and TD elements until they are actually
     * needed by a driven page draw. This can give a significant speed
     * increase for Ajax source and JavaScript source data, but makes no
     * difference at all for DOM and server-side processing tables.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bDeferRender: null,
    /**
     * Enable filtering on the table or not. Note that if this is disabled
     * then there is no filtering at all on the table, including fnFilter.
     * To just remove the filtering input use sDom and remove the 'f' option.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bFilter: null,
    /**
     * Used only for compatibility with DT1
     * @deprecated
     */
    bInfo: !0,
    /**
     * Used only for compatibility with DT1
     * @deprecated
     */
    bLengthChange: !0,
    /**
     * Pagination enabled or not. Note that if this is disabled then length
     * changing must also be disabled.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bPaginate: null,
    /**
     * Processing indicator enable flag whenever DataTables is enacting a
     * user request - typically an Ajax request for server-side processing.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bProcessing: null,
    /**
     * Server-side processing enabled flag - when enabled DataTables will
     * get all data from the server for every draw - there is no filtering,
     * sorting or paging done on the client-side.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bServerSide: null,
    /**
     * Sorting enablement flag.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bSort: null,
    /**
     * Multi-column sorting
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bSortMulti: null,
    /**
     * Apply a class to the columns which are being sorted to provide a
     * visual highlight or not. This can slow things down when enabled since
     * there is a lot of DOM interaction.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bSortClasses: null,
    /**
     * State saving enablement flag.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bStateSave: null
  },
  /**
   * Scrolling settings for a table.
   */
  oScroll: {
    /**
     * When the table is shorter in height than sScrollY, collapse the
     * table container down to the height of the table (when true).
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    bCollapse: null,
    /**
     * Width of the scrollbar for the web-browser's platform. Calculated
     * during table initialisation.
     */
    iBarWidth: 0,
    /**
     * Viewport width for horizontal scrolling. Horizontal scrolling is
     * disabled if an empty string.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    sX: null,
    /**
     * Width to expand the table to when using x-scrolling. Typically you
     * should not need to use this.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     *  @deprecated
     */
    sXInner: null,
    /**
     * Viewport height for vertical scrolling. Vertical scrolling is disabled
     * if an empty string.
     * Note that this parameter will be set by the initialisation routine. To
     * set a default use {@link DataTable.defaults}.
     */
    sY: null
  },
  /**
   * Language information for the table.
   */
  oLanguage: {
    /**
     * Information callback function. See
     * {@link DataTable.defaults.fnInfoCallback}
     */
    fnInfoCallback: null
  },
  /**
   * Browser support parameters
   */
  oBrowser: {
    /**
     * Determine if the vertical scrollbar is on the right or left of the
     * scrolling container - needed for rtl language layout, although not
     * all browsers move the scrollbar (Safari).
     */
    bScrollbarLeft: !1,
    /**
     * Browser scrollbar width
     */
    barWidth: 0
  },
  ajax: null,
  /**
   * Array referencing the nodes which are used for the features. The
   * parameters of this object match what is allowed by sDom - i.e.
   *   <ul>
   *     <li>'l' - Length changing</li>
   *     <li>'f' - Filtering input</li>
   *     <li>'t' - The table!</li>
   *     <li>'i' - Information</li>
   *     <li>'p' - Pagination</li>
   *     <li>'r' - pRocessing</li>
   *   </ul>
   */
  aanFeatures: [],
  /**
   * Store data information - see {@link DataTable.models.oRow} for detailed
   * information.
   */
  aoData: [],
  /**
   * Array of indexes which are in the current display (after filtering etc)
   */
  aiDisplay: [],
  /**
   * Array of indexes for display - no filtering
   */
  aiDisplayMaster: [],
  /**
   * Map of row ids to data indexes
   */
  aIds: {},
  /**
   * Store information about each column that is in use
   */
  aoColumns: [],
  /**
   * Store information about the table's header
   */
  aoHeader: [],
  /**
   * Store information about the table's footer
   */
  aoFooter: [],
  /**
   * Store the applied global search information in case we want to force a
   * research or compare the old search to a new one.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  oPreviousSearch: {},
  /**
   * Store for named searches
   */
  searchFixed: {},
  /**
   * Store the applied search for each column - see
   * {@link DataTable.models.oSearch} for the format that is used for the
   * filtering information for each column.
   */
  aoPreSearchCols: [],
  /**
   * Sorting that is applied to the table. Note that the inner arrays are
   * used in the following manner:
   * <ul>
   *   <li>Index 0 - column number</li>
   *   <li>Index 1 - current sorting direction</li>
   * </ul>
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  aaSorting: null,
  /**
   * Sorting that is always applied to the table (i.e. prefixed in front of
   * aaSorting).
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  aaSortingFixed: [],
  /**
   * If restoring a table - we should restore its width
   */
  sDestroyWidth: 0,
  /**
   * Callback functions array for every time a row is inserted (i.e. on a draw).
   */
  aoRowCallback: [],
  /**
   * Callback functions for the header on each draw.
   */
  aoHeaderCallback: [],
  /**
   * Callback function for the footer on each draw.
   */
  aoFooterCallback: [],
  /**
   * Array of callback functions for draw callback functions
   */
  aoDrawCallback: [],
  /**
   * Array of callback functions for row created function
   */
  aoRowCreatedCallback: [],
  /**
   * Callback functions for just before the table is redrawn. A return of
   * false will be used to cancel the draw.
   */
  aoPreDrawCallback: [],
  /**
   * Callback functions for when the table has been initialised.
   */
  aoInitComplete: [],
  /**
   * Callbacks for modifying the settings to be stored for state saving, prior to
   * saving state.
   */
  aoStateSaveParams: [],
  /**
   * Callbacks for modifying the settings that have been stored for state saving
   * prior to using the stored values to restore the state.
   */
  aoStateLoadParams: [],
  /**
   * Callbacks for operating on the settings object once the saved state has been
   * loaded
   */
  aoStateLoaded: [],
  /**
   * Cache the table ID for quick access
   */
  sTableId: "",
  /**
   * The TABLE node for the main table
   */
  nTable: null,
  /**
   * Permanent ref to the thead element
   */
  nTHead: null,
  /**
   * Permanent ref to the tfoot element - if it exists
   */
  nTFoot: null,
  /**
   * Permanent ref to the tbody element
   */
  nTBody: null,
  /**
   * Cache the wrapper node (contains all DataTables controlled elements)
   */
  nTableWrapper: null,
  /**
   * Indicate if all required information has been read in
   */
  bInitialised: !1,
  /**
   * Information about open rows. Each object in the array has the parameters
   * 'nTr' and 'nParent'
   */
  aoOpenRows: [],
  /**
   * Dictate the positioning of DataTables' control elements - see
   * {@link DataTable.model.oInit.sDom}.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  sDom: null,
  /**
   * Search delay (in mS)
   */
  searchDelay: null,
  /**
   * Which type of pagination should be used.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  sPaginationType: "two_button",
  /**
   * Number of paging controls on the page. Only used for backwards compatibility
   */
  pagingControls: 0,
  /**
   * The state duration (for `stateSave`) in seconds.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  iStateDuration: 0,
  /**
   * Array of callback functions for state saving. Each array element is an
   * object with the following parameters:
   *   <ul>
   *     <li>function:fn - function to call. Takes two parameters, oSettings
   *       and the JSON string to save that has been thus far created. Returns
   *       a JSON string to be inserted into a json object
   *       (i.e. '"param": [ 0, 1, 2]')</li>
   *     <li>string:sName - name of callback</li>
   *   </ul>
   */
  aoStateSave: [],
  /**
   * Array of callback functions for state loading. Each array element is an
   * object with the following parameters:
   *   <ul>
   *     <li>function:fn - function to call. Takes two parameters, oSettings
   *       and the object stored. May return false to cancel state loading</li>
   *     <li>string:sName - name of callback</li>
   *   </ul>
   */
  aoStateLoad: [],
  /**
   * State that was saved. Useful for back reference
   */
  oSavedState: null,
  /**
   * State that was loaded. Useful for back reference
   */
  oLoadedState: null,
  /**
   * Note if draw should be blocked while getting data
   */
  bAjaxDataGet: !0,
  /**
   * The last jQuery XHR object that was used for server-side data gathering.
   * This can be used for working with the XHR information in one of the
   * callbacks
   */
  jqXHR: null,
  /**
   * JSON returned from the server in the last Ajax request
   */
  json: void 0,
  /**
   * Data submitted as part of the last Ajax request
   */
  oAjaxData: void 0,
  /**
   * Send the XHR HTTP method - GET or POST (could be PUT or DELETE if
   * required).
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  sServerMethod: null,
  /**
   * Format numbers for display.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  fnFormatNumber: null,
  /**
   * List of options that can be used for the user selectable length menu.
   * Note that this parameter will be set by the initialisation routine. To
   * set a default use {@link DataTable.defaults}.
   */
  aLengthMenu: null,
  /**
   * Counter for the draws that the table does. Also used as a tracker for
   * server-side processing
   */
  iDraw: 0,
  /**
   * Indicate if a redraw is being done - useful for Ajax
   */
  bDrawing: !1,
  /**
   * Draw index (iDraw) of the last error when parsing the returned data
   */
  iDrawError: -1,
  /**
   * Paging display length
   */
  _iDisplayLength: 10,
  /**
   * Paging start point - aiDisplay index
   */
  _iDisplayStart: 0,
  /**
   * Server-side processing - number of records in the result set
   * (i.e. before filtering), Use fnRecordsTotal rather than
   * this property to get the value of the number of records, regardless of
   * the server-side processing setting.
   */
  _iRecordsTotal: 0,
  /**
   * Server-side processing - number of records in the current display set
   * (i.e. after filtering). Use fnRecordsDisplay rather than
   * this property to get the value of the number of records, regardless of
   * the server-side processing setting.
   */
  _iRecordsDisplay: 0,
  /**
   * The classes to use for the table
   */
  oClasses: {},
  /**
   * Flag attached to the settings object so you can check in the draw
   * callback if filtering has been done in the draw. Deprecated in favour of
   * events.
   *  @deprecated
   */
  bFiltered: !1,
  /**
   * Flag attached to the settings object so you can check in the draw
   * callback if sorting has been done in the draw. Deprecated in favour of
   * events.
   *  @deprecated
   */
  bSorted: !1,
  /**
   * Indicate that if multiple rows are in the header and there is more than
   * one unique cell per column. Replaced by titleRow
   */
  bSortCellsTop: null,
  /**
   * Initialisation object that is used for the table
   */
  oInit: null,
  /**
   * Destroy callback functions - for plug-ins to attach themselves to the
   * destroy so they can clean up markup and events.
   */
  aoDestroyCallback: [],
  /**
   * Get the number of records in the current record set, before filtering
   */
  fnRecordsTotal: function() {
    return an(this) == "ssp" ? this._iRecordsTotal * 1 : this.aiDisplayMaster.length;
  },
  /**
   * Get the number of records in the current record set, after filtering
   */
  fnRecordsDisplay: function() {
    return an(this) == "ssp" ? this._iRecordsDisplay * 1 : this.aiDisplay.length;
  },
  /**
   * Get the display end point - aiDisplay index
   */
  fnDisplayEnd: function() {
    var t = this._iDisplayLength, n = this._iDisplayStart, e = n + t, r = this.aiDisplay.length, i = this.oFeatures, s = i.bPaginate;
    return i.bServerSide ? s === !1 || t === -1 ? n + r : Math.min(n + t, this._iRecordsDisplay) : !s || e > r || t === -1 ? r : e;
  },
  /**
   * The DataTables object for this table
   */
  oInstance: null,
  /**
   * Unique identifier for each instance of the DataTables object. If there
   * is an ID on the table node, then it takes that value, otherwise an
   * incrementing internal counter is used.
   */
  sInstance: null,
  /**
   * tabindex attribute value that is added to DataTables control elements, allowing
   * keyboard navigation of the table and its controls.
   */
  iTabIndex: 0,
  /**
   * DIV container for the footer scrolling table if scrolling
   */
  nScrollHead: null,
  /**
   * DIV container for the footer scrolling table if scrolling
   */
  nScrollFoot: null,
  /**
   * Last applied sort
   */
  aLastSort: [],
  /**
   * Stored plug-in instances
   */
  oPlugins: {},
  /**
   * Function used to get a row's id from the row's data
   */
  rowIdFn: null,
  /**
   * Data location where to store a row's id
   */
  rowId: null,
  caption: "",
  captionNode: null,
  colgroup: null,
  /** Delay loading of data */
  deferLoading: null,
  /** Allow auto type detection */
  typeDetect: !0,
  /** ResizeObserver for the container div */
  resizeObserver: null,
  /** Keep a record of the last size of the container, so we can skip duplicates */
  containerWidth: -1,
  /** Reverse the initial order of the data set on desc ordering */
  orderDescReverse: null,
  /** Show / hide ordering indicators in headers */
  orderIndicators: !0,
  /** Default ordering listener */
  orderHandler: !0,
  /** Title row indicator */
  titleRow: null,
  /** Title wrapper element type */
  columnTitleTag: "span"
};
var Tg = H.ext.pager;
q.extend(Tg, {
  simple: function() {
    return ["previous", "next"];
  },
  full: function() {
    return ["first", "previous", "next", "last"];
  },
  numbers: function() {
    return ["numbers"];
  },
  simple_numbers: function() {
    return ["previous", "numbers", "next"];
  },
  full_numbers: function() {
    return ["first", "previous", "numbers", "next", "last"];
  },
  first_last: function() {
    return ["first", "last"];
  },
  first_last_numbers: function() {
    return ["first", "numbers", "last"];
  },
  // For testing and plug-ins to use
  _numbers: of,
  // Number of number buttons - legacy, use `numbers` option for paging feature
  numbers_length: 7
});
q.extend(!0, H.ext.renderer, {
  pagingButton: {
    _: function(t, n, e, r, i) {
      var s = t.oClasses.paging, a = [s.button], o;
      return r && a.push(s.active), i && a.push(s.disabled), n === "ellipsis" ? o = q('<span class="ellipsis"></span>').html(e)[0] : o = q("<button>", {
        class: a.join(" "),
        role: "link",
        type: "button"
      }).html(e), {
        display: o,
        clicker: o
      };
    }
  },
  pagingContainer: {
    _: function(t, n) {
      return n;
    }
  }
});
var Ni = function(t, n) {
  return function(e) {
    return nn(e) || typeof e != "string" || (e = e.replace(Bp, " "), t && (e = jn(e)), n && (e = Ca(e, !1))), e;
  };
};
function Cc(t, n, e, r, i) {
  return Rr ? t[n](i) : or ? t[e](i) : r ? t[r](i) : t;
}
var xc = !1, or, Rr;
function Dg() {
  window.luxon && !or && (or = window.luxon), window.moment && !Rr && (Rr = window.moment);
}
function Ea(t, n, e) {
  var r;
  if (Dg(), Rr) {
    if (r = Rr.utc(t, n, e, !0), !r.isValid())
      return null;
  } else if (or) {
    if (r = n && typeof t == "string" ? or.DateTime.fromFormat(t, n) : or.DateTime.fromISO(t), !r.isValid)
      return null;
    r = r.setLocale(e);
  } else
    n ? (xc || alert("DataTables warning: Formatted date without Moment.js or Luxon - https://datatables.net/tn/17"), xc = !0) : r = new Date(t);
  return r;
}
function Io(t) {
  return function(n, e, r, i) {
    arguments.length === 0 ? (r = "en", e = null, n = null) : arguments.length === 1 ? (r = "en", e = n, n = null) : arguments.length === 2 && (r = e, e = n, n = null);
    var s = "datetime" + (e ? "-" + e : "");
    return H.ext.type.order[s + "-pre"] || H.type(s, {
      detect: function(a) {
        return a === s ? s : !1;
      },
      order: {
        pre: function(a) {
          return a.valueOf();
        }
      },
      className: "dt-right"
    }), function(a, o) {
      if (a == null)
        if (i === "--now") {
          var l = /* @__PURE__ */ new Date();
          a = new Date(Date.UTC(
            l.getFullYear(),
            l.getMonth(),
            l.getDate(),
            l.getHours(),
            l.getMinutes(),
            l.getSeconds()
          ));
        } else
          a = "";
      if (o === "type")
        return s;
      if (a === "")
        return o !== "sort" ? "" : Ea("0000-01-01 00:00:00", null, r);
      if (e !== null && n === e && o !== "sort" && o !== "type" && !(a instanceof Date))
        return a;
      var c = Ea(a, n, r);
      if (c === null)
        return a;
      if (o === "sort")
        return c;
      var f = e === null ? Cc(c, "toDate", "toJSDate", "")[t](
        navigator.language,
        { timeZone: "UTC" }
      ) : Cc(c, "format", "toFormat", "toISOString", e);
      return o === "display" ? Or(f) : f;
    };
  };
}
var rf = ",", sf = ".";
if (window.Intl !== void 0)
  try {
    for (var Qi = new Intl.NumberFormat().formatToParts(100000.1), bt = 0; bt < Qi.length; bt++)
      Qi[bt].type === "group" ? rf = Qi[bt].value : Qi[bt].type === "decimal" && (sf = Qi[bt].value);
  } catch {
  }
H.datetime = function(t, n) {
  var e = "datetime-" + t;
  n || (n = "en"), H.ext.type.order[e] || H.type(e, {
    detect: function(r) {
      var i = Ea(r, t, n);
      return r === "" || i ? e : !1;
    },
    order: {
      pre: function(r) {
        return Ea(r, t, n) || 0;
      }
    },
    className: "dt-right"
  });
};
H.render = {
  date: Io("toLocaleDateString"),
  datetime: Io("toLocaleString"),
  time: Io("toLocaleTimeString"),
  number: function(t, n, e, r, i) {
    return t == null && (t = rf), n == null && (n = sf), {
      display: function(s) {
        if (typeof s != "number" && typeof s != "string" || s === "" || s === null)
          return s;
        var a = s < 0 ? "-" : "", o = parseFloat(s), l = Math.abs(o);
        if (l >= 1e11 || l < 1e-4 && l !== 0) {
          var c = o.toExponential(e).split(/e\+?/);
          return c[0] + " x 10<sup>" + c[1] + "</sup>";
        }
        if (isNaN(o))
          return Or(s);
        o = o.toFixed(e), s = Math.abs(o);
        var f = parseInt(s, 10), g = e ? n + (s - f).toFixed(e).substring(2) : "";
        return f === 0 && parseFloat(g) === 0 && (a = ""), a + (r || "") + f.toString().replace(
          /\B(?=(\d{3})+(?!\d))/g,
          t
        ) + g + (i || "");
      }
    };
  },
  text: function() {
    return {
      display: Or,
      filter: Or
    };
  }
};
var Lt = H.ext.type;
H.type = function(t, n, e) {
  if (!n)
    return {
      className: Lt.className[t],
      detect: Lt.detect.find(function(a) {
        return a._name === t;
      }),
      order: {
        pre: Lt.order[t + "-pre"],
        asc: Lt.order[t + "-asc"],
        desc: Lt.order[t + "-desc"]
      },
      render: Lt.render[t],
      search: Lt.search[t]
    };
  var r = function(a, o) {
    Lt[a][t] = o;
  }, i = function(a) {
    Object.defineProperty(a, "_name", { value: t });
    var o = Lt.detect.findIndex(function(l) {
      return l._name === t;
    });
    o === -1 ? Lt.detect.unshift(a) : Lt.detect.splice(o, 1, a);
  }, s = function(a) {
    Lt.order[t + "-pre"] = a.pre, Lt.order[t + "-asc"] = a.asc, Lt.order[t + "-desc"] = a.desc;
  };
  e === void 0 && (e = n, n = null), n === "className" ? r("className", e) : n === "detect" ? i(e) : n === "order" ? s(e) : n === "render" ? r("render", e) : n === "search" ? r("search", e) : n || (e.className && r("className", e.className), e.detect !== void 0 && i(e.detect), e.order && s(e.order), e.render !== void 0 && r("render", e.render), e.search !== void 0 && r("search", e.search));
};
H.types = function() {
  return Lt.detect.map(function(t) {
    return t._name;
  });
};
var yl = function(t, n) {
  return t = t != null ? t.toString().toLowerCase() : "", n = n != null ? n.toString().toLowerCase() : "", t.localeCompare(n, navigator.languages[0] || navigator.language, {
    numeric: !0,
    ignorePunctuation: !0
  });
}, Sc = function(t, n) {
  return t = jn(t), n = jn(n), yl(t, n);
};
H.type("string", {
  detect: function() {
    return "string";
  },
  order: {
    pre: function(t) {
      return nn(t) && typeof t != "boolean" ? "" : typeof t == "string" ? t.toLowerCase() : t.toString ? t.toString() : "";
    }
  },
  search: Ni(!1, !0)
});
H.type("string-utf8", {
  detect: {
    allOf: function(t) {
      return !0;
    },
    oneOf: function(t) {
      return !nn(t) && navigator.languages && typeof t == "string" && t.match(/[^\x00-\x7F]/);
    }
  },
  order: {
    asc: yl,
    desc: function(t, n) {
      return yl(t, n) * -1;
    }
  },
  search: Ni(!1, !0)
});
H.type("html", {
  detect: {
    allOf: function(t) {
      return nn(t) || typeof t == "string" && t.indexOf("<") !== -1;
    },
    oneOf: function(t) {
      return !nn(t) && typeof t == "string" && t.indexOf("<") !== -1;
    }
  },
  order: {
    pre: function(t) {
      return nn(t) ? "" : t.replace ? jn(t).trim().toLowerCase() : t + "";
    }
  },
  search: Ni(!0, !0)
});
H.type("html-utf8", {
  detect: {
    allOf: function(t) {
      return nn(t) || typeof t == "string" && t.indexOf("<") !== -1;
    },
    oneOf: function(t) {
      return navigator.languages && !nn(t) && typeof t == "string" && t.indexOf("<") !== -1 && typeof t == "string" && t.match(/[^\x00-\x7F]/);
    }
  },
  order: {
    asc: Sc,
    desc: function(t, n) {
      return Sc(t, n) * -1;
    }
  },
  search: Ni(!0, !0)
});
H.type("date", {
  className: "dt-type-date",
  detect: {
    allOf: function(t) {
      if (t && !(t instanceof Date) && !oc.test(t))
        return null;
      var n = Date.parse(t);
      return n !== null && !isNaN(n) || nn(t);
    },
    oneOf: function(t) {
      return t instanceof Date || typeof t == "string" && oc.test(t);
    }
  },
  order: {
    pre: function(t) {
      var n = Date.parse(t);
      return isNaN(n) ? -1 / 0 : n;
    }
  }
});
H.type("html-num-fmt", {
  className: "dt-type-numeric",
  detect: {
    allOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return _a(t, e, !0, !1);
    },
    oneOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return _a(t, e, !0, !1);
    }
  },
  order: {
    pre: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return Ka(t, e, Rl, Ll);
    }
  },
  search: Ni(!0, !0)
});
H.type("html-num", {
  className: "dt-type-numeric",
  detect: {
    allOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return _a(t, e, !1, !0);
    },
    oneOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return _a(t, e, !1, !1);
    }
  },
  order: {
    pre: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return Ka(t, e, Rl);
    }
  },
  search: Ni(!0, !0)
});
H.type("num-fmt", {
  className: "dt-type-numeric",
  detect: {
    allOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return hs(t, e, !0, !0);
    },
    oneOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return hs(t, e, !0, !1);
    }
  },
  order: {
    pre: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return Ka(t, e, Ll);
    }
  }
});
H.type("num", {
  className: "dt-type-numeric",
  detect: {
    allOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return hs(t, e, !1, !0);
    },
    oneOf: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return hs(t, e, !1, !1);
    }
  },
  order: {
    pre: function(t, n) {
      var e = n.oLanguage.sDecimal;
      return Ka(t, e);
    }
  }
});
var Ka = function(t, n, e, r) {
  if (t !== 0 && (!t || t === "-"))
    return -1 / 0;
  var i = typeof t;
  return i === "number" || i === "bigint" ? t : (n && (t = Od(t, n)), t.replace && (e && (t = t.replace(e, "")), r && (t = t.replace(r, ""))), t * 1);
};
q.extend(!0, H.ext.renderer, {
  footer: {
    _: function(t, n, e) {
      n.addClass(e.tfoot.cell);
    }
  },
  header: {
    _: function(t, n, e) {
      n.addClass(e.thead.cell), t.oFeatures.bSort || n.addClass(e.order.none);
      var r = t.titleRow, i = n.closest("thead").find("tr"), s = n.parent().index();
      // Cells and rows which have the attribute to disable the icons
      n.attr("data-dt-order") === "disable" || n.parent().attr("data-dt-order") === "disable" || // titleRow support, for defining a specific row in the header
      r === !0 && s !== 0 || r === !1 && s !== i.length - 1 || typeof r == "number" && s !== r || q(t.nTable).on("order.dt.DT column-visibility.dt.DT", function(a, o, l) {
        if (t === o) {
          var c = o.sortDetails;
          if (c) {
            var f = kt(c, "col");
            if (!(a.type === "column-visibility" && !f.includes(l))) {
              var g, b = e.order, w = o.api.columns(n), C = t.aoColumns[w.flatten()[0]], m = w.orderable().includes(!0), h = "", u = w.indexes(), p = w.orderable(!0).flatten(), v = t.iTabIndex, S = o.orderHandler && m;
              n.removeClass(
                b.isAsc + " " + b.isDesc
              ).toggleClass(b.none, !m).toggleClass(b.canAsc, S && p.includes("asc")).toggleClass(b.canDesc, S && p.includes("desc"));
              var T = !0;
              for (g = 0; g < u.length; g++)
                f.includes(u[g]) || (T = !1);
              if (T) {
                var A = w.order();
                n.addClass(
                  A.includes("asc") ? b.isAsc : "" + A.includes("desc") ? b.isDesc : ""
                );
              }
              var y = -1;
              for (g = 0; g < f.length; g++)
                if (t.aoColumns[f[g]].bVisible) {
                  y = f[g];
                  break;
                }
              if (u[0] == y) {
                var I = c[0], $ = C.asSorting;
                n.attr("aria-sort", I.dir === "asc" ? "ascending" : "descending"), h = $[I.index + 1] ? "Reverse" : "Remove";
              } else
                n.removeAttr("aria-sort");
              if (m) {
                var V = n.find(".dt-column-order");
                V.attr("role", "button").attr(
                  "aria-label",
                  m ? C.ariaTitle + o.api.i18n("oAria.orderable" + h) : C.ariaTitle
                ), v !== -1 && V.attr("tabindex", v);
              }
            }
          }
        }
      });
    }
  },
  layout: {
    _: function(t, n, e) {
      var r = t.oClasses.layout, i = q("<div/>").attr("id", e.id || null).addClass(e.className || r.row).appendTo(n);
      H.ext.renderer.layout._forLayoutRow(e, function(s, a) {
        if (!(s === "id" || s === "className")) {
          var o = "";
          a.table && (i.addClass(r.tableRow), o += r.tableCell + " "), s === "start" ? o += r.start : s === "end" ? o += r.end : o += r.full, q("<div/>").attr({
            id: a.id || null,
            class: a.className ? a.className : r.cell + " " + o
          }).append(a.contents).appendTo(i);
        }
      });
    },
    // Shared for use by the styling frameworks
    _forLayoutRow: function(t, n) {
      var e = function(r) {
        switch (r) {
          case "":
            return 0;
          case "start":
            return 1;
          case "end":
            return 2;
          default:
            return 3;
        }
      };
      Object.keys(t).sort(function(r, i) {
        return e(r) - e(i);
      }).forEach(function(r) {
        n(r, t[r]);
      });
    }
  }
});
H.feature = {};
H.feature.register = function(t, n, e) {
  H.ext.features[t] = n, e && _t.feature.push({
    cFeature: e,
    fnInit: n
  });
};
function Xs(t, n, e) {
  e && (t[n] = e);
}
H.feature.register("div", function(t, n) {
  var e = q("<div>")[0];
  return n && (Xs(e, "className", n.className), Xs(e, "id", n.id), Xs(e, "innerHTML", n.html), Xs(e, "textContent", n.text)), e;
});
H.feature.register("info", function(t, n) {
  if (!t.oFeatures.bInfo)
    return null;
  var e = t.oLanguage, r = t.sTableId, i = q("<div/>", {
    class: t.oClasses.info.container
  });
  return n = q.extend({
    callback: e.fnInfoCallback,
    empty: e.sInfoEmpty,
    postfix: e.sInfoPostFix,
    search: e.sInfoFiltered,
    text: e.sInfo
  }, n), t.aoDrawCallback.push(function(s) {
    Eg(s, n, i);
  }), t._infoEl || (i.attr({
    "aria-live": "polite",
    id: r + "_info",
    role: "status"
  }), q(t.nTable).attr("aria-describedby", r + "_info"), t._infoEl = i), i;
}, "i");
function Eg(t, n, e) {
  var r = t._iDisplayStart + 1, i = t.fnDisplayEnd(), s = t.fnRecordsTotal(), a = t.fnRecordsDisplay(), o = a ? n.text : n.empty;
  a !== s && (o += " " + n.search), o += n.postfix, o = Yl(t, o), n.callback && (o = n.callback.call(
    t.oInstance,
    t,
    r,
    i,
    s,
    a,
    o
  )), e.html(o), qe(t, null, "info", [t, e[0], o]);
}
var No = 0;
H.feature.register("search", function(t, n) {
  if (!t.oFeatures.bFilter)
    return null;
  var e = t.oClasses.search, r = t.sTableId, i = t.oLanguage, s = t.oPreviousSearch, a = '<input type="search" class="' + e.input + '"/>';
  n = q.extend({
    placeholder: i.sSearchPlaceholder,
    processing: !1,
    text: i.sSearch
  }, n), n.text.indexOf("_INPUT_") === -1 && (n.text += "_INPUT_"), n.text = Yl(t, n.text);
  var o = n.text.match(/_INPUT_$/), l = n.text.match(/^_INPUT_/), c = n.text.replace(/_INPUT_/, ""), f = "<label>" + n.text + "</label>";
  l ? f = "_INPUT_<label>" + c + "</label>" : o && (f = "<label>" + c + "</label>_INPUT_");
  var g = q("<div>").addClass(e.container).append(f.replace(/_INPUT_/, a));
  g.find("label").attr("for", "dt-search-" + No), g.find("input").attr("id", "dt-search-" + No), No++;
  var b = function(m) {
    var h = this.value;
    s.return && m.key !== "Enter" || h != s.search && jd(t, n.processing, function() {
      s.search = h, gs(t, s), t._iDisplayStart = 0, Ii(t);
    });
  }, w = t.searchDelay !== null ? t.searchDelay : 0, C = q("input", g).val(s.search).attr("placeholder", n.placeholder).on(
    "keyup.DT search.DT input.DT paste.DT cut.DT",
    w ? H.util.debounce(b, w) : b
  ).on("mouseup.DT", function(m) {
    setTimeout(function() {
      b.call(C[0], m);
    }, 10);
  }).on("keypress.DT", function(m) {
    if (m.keyCode == 13)
      return !1;
  }).attr("aria-controls", r);
  return q(t.nTable).on("search.dt.DT", function(m, h) {
    t === h && C[0] !== document.activeElement && C.val(
      typeof s.search != "function" ? s.search : ""
    );
  }), g;
}, "f");
H.feature.register("paging", function(t, n) {
  if (!t.oFeatures.bPaginate)
    return null;
  n = q.extend({
    buttons: H.ext.pager.numbers_length,
    type: t.sPaginationType,
    boundaryNumbers: !0,
    firstLast: !0,
    previousNext: !0,
    numbers: !0
  }, n);
  var e = q("<div/>").addClass(t.oClasses.paging.container + (n.type ? " paging_" + n.type : "")).append(
    q("<nav>").attr("aria-label", "pagination").addClass(t.oClasses.paging.nav)
  ), r = function() {
    af(t, e.children(), n);
  };
  return t.aoDrawCallback.push(r), q(t.nTable).on("column-sizing.dt.DT", r), e;
}, "p");
function Ag(t) {
  var n = [];
  return t.numbers && n.push("numbers"), t.previousNext && (n.unshift("previous"), n.push("next")), t.firstLast && (n.unshift("first"), n.push("last")), n;
}
function af(t, n, e) {
  if (t._bInitComplete) {
    var r = e.type ? H.ext.pager[e.type] : Ag, i = t.oLanguage.oAria.paginate || {}, s = t._iDisplayStart, a = t._iDisplayLength, o = t.fnRecordsDisplay(), l = a === -1, c = l ? 0 : Math.ceil(s / a), f = l ? 1 : Math.ceil(o / a), g = [], b = [], w = r(e).map(function(A) {
      return A === "numbers" ? of(c, f, e.buttons, e.boundaryNumbers) : A;
    });
    g = g.concat.apply(g, w);
    for (var C = 0; C < g.length; C++) {
      var m = g[C], h = kg(t, m, c, f), u = Da(t, "pagingButton")(
        t,
        m,
        h.display,
        h.active,
        h.disabled
      ), p = typeof m == "string" ? i[m] : i.number ? i.number + (m + 1) : null;
      q(u.clicker).attr({
        "aria-controls": t.sTableId,
        "aria-disabled": h.disabled ? "true" : null,
        "aria-current": h.active ? "page" : null,
        "aria-label": p,
        "data-dt-idx": m,
        tabIndex: h.disabled ? -1 : t.iTabIndex && u.clicker[0].nodeName.toLowerCase() !== "span" ? t.iTabIndex : null
        // `0` doesn't need a tabIndex since it is the default
      }), typeof m != "number" && q(u.clicker).addClass(m), Kd(
        u.clicker,
        { action: m },
        function(A) {
          A.preventDefault(), jl(t, A.data.action, !0);
        }
      ), b.push(u.display);
    }
    var v = Da(t, "pagingContainer")(
      t,
      b
    ), S = n.find(document.activeElement).data("dt-idx");
    if (n.empty().append(v), S !== void 0 && n.find("[data-dt-idx=" + S + "]").trigger("focus"), b.length) {
      var T = q(b[0]).outerHeight();
      e.buttons > 1 && // prevent infinite
      T > 0 && // will be 0 if hidden
      q(n).height() >= T * 2 - 10 && af(t, n, q.extend({}, e, { buttons: e.buttons - 2 }));
    }
  }
}
function kg(t, n, e, r) {
  var i = t.oLanguage.oPaginate, s = {
    display: "",
    active: !1,
    disabled: !1
  };
  switch (n) {
    case "ellipsis":
      s.display = "&#x2026;";
      break;
    case "first":
      s.display = i.sFirst, e === 0 && (s.disabled = !0);
      break;
    case "previous":
      s.display = i.sPrevious, e === 0 && (s.disabled = !0);
      break;
    case "next":
      s.display = i.sNext, (r === 0 || e === r - 1) && (s.disabled = !0);
      break;
    case "last":
      s.display = i.sLast, (r === 0 || e === r - 1) && (s.disabled = !0);
      break;
    default:
      typeof n == "number" && (s.display = t.fnFormatNumber(n + 1), e === n && (s.active = !0));
      break;
  }
  return s;
}
function of(t, n, e, r) {
  var i = [], s = Math.floor(e / 2), a = r ? 2 : 1, o = r ? 1 : 0;
  return n <= e ? i = tr(0, n) : e === 1 ? i = [t] : e === 3 ? t <= 1 ? i = [0, 1, "ellipsis"] : t >= n - 2 ? (i = tr(n - 2, n), i.unshift("ellipsis")) : i = ["ellipsis", t, "ellipsis"] : t <= s ? (i = tr(0, e - a), i.push("ellipsis"), r && i.push(n - 1)) : t >= n - 1 - s ? (i = tr(n - (e - a), n), i.unshift("ellipsis"), r && i.unshift(0)) : (i = tr(t - s + a, t + s - o), i.push("ellipsis"), i.unshift("ellipsis"), r && (i.push(n - 1), i.unshift(0))), i;
}
var Ro = 0;
H.feature.register("pageLength", function(t, n) {
  var e = t.oFeatures;
  if (!e.bPaginate || !e.bLengthChange)
    return null;
  n = q.extend({
    menu: t.aLengthMenu,
    text: t.oLanguage.sLengthMenu
  }, n);
  var r = t.oClasses.length, i = t.sTableId, s = n.menu, a = [], o = [], l;
  if (Array.isArray(s[0]))
    a = s[0], o = s[1];
  else
    for (l = 0; l < s.length; l++)
      q.isPlainObject(s[l]) ? (a.push(s[l].value), o.push(s[l].label)) : (a.push(s[l]), o.push(s[l]));
  var c = n.text.match(/_MENU_$/), f = n.text.match(/^_MENU_/), g = n.text.replace(/_MENU_/, ""), b = "<label>" + n.text + "</label>";
  f ? b = "_MENU_<label>" + g + "</label>" : c && (b = "<label>" + g + "</label>_MENU_");
  var w = "tmp-" + +/* @__PURE__ */ new Date(), C = q("<div/>").addClass(r.container).append(
    b.replace("_MENU_", '<span id="' + w + '"></span>')
  ), m = [];
  Array.prototype.slice.call(C.find("label")[0].childNodes).forEach(function(v) {
    v.nodeType === Node.TEXT_NODE && m.push({
      el: v,
      text: v.textContent
    });
  });
  var h = function(v) {
    m.forEach(function(S) {
      S.el.textContent = Yl(t, S.text, v);
    });
  }, u = q("<select/>", {
    "aria-controls": i,
    class: r.select
  });
  for (l = 0; l < a.length; l++) {
    var p = t.api.i18n("lengthLabels." + a[l], null);
    p === null && (p = typeof o[l] == "number" ? t.fnFormatNumber(o[l]) : o[l]), u[0][l] = new Option(p, a[l]);
  }
  return C.find("label").attr("for", "dt-length-" + Ro), u.attr("id", "dt-length-" + Ro), Ro++, C.find("#" + w).replaceWith(u), q("select", C).val(t._iDisplayLength).on("change.DT", function() {
    Wd(t, q(this).val()), Ii(t);
  }), q(t.nTable).on("length.dt.DT", function(v, S, T) {
    t === S && (q("select", C).val(T), h(T));
  }), h(t._iDisplayLength), C;
}, "l");
q.fn.dataTable = H;
H.$ = q;
q.fn.dataTableSettings = H.settings;
q.fn.dataTableExt = H.ext;
q.fn.DataTable = function(t) {
  return q(this).dataTable(t).api();
};
q.each(H, function(t, n) {
  q.fn.DataTable[t] = n;
});
/*! DataTables Bootstrap 5 integration
 * © SpryMedia Ltd - datatables.net/license
 */
let ms = lt;
ms.extend(!0, H.defaults, {
  renderer: "bootstrap"
});
ms.extend(!0, H.ext.classes, {
  container: "dt-container dt-bootstrap5",
  search: {
    input: "form-control form-control-sm"
  },
  length: {
    select: "form-select form-select-sm"
  },
  processing: {
    container: "dt-processing card"
  },
  layout: {
    row: "row mt-2 justify-content-between",
    cell: "d-md-flex justify-content-between align-items-center",
    tableCell: "col-12",
    start: "dt-layout-start col-md-auto me-auto",
    end: "dt-layout-end col-md-auto ms-auto",
    full: "dt-layout-full col-md"
  }
});
H.ext.renderer.pagingButton.bootstrap = function(t, n, e, r, i) {
  var s = ["dt-paging-button", "page-item"];
  r && s.push("active"), i && s.push("disabled");
  var a = ms("<li>").addClass(s.join(" ")), o = ms("<button>", {
    class: "page-link",
    role: "link",
    type: "button"
  }).html(e).appendTo(a);
  return {
    display: a,
    clicker: o
  };
};
H.ext.renderer.pagingContainer.bootstrap = function(t, n) {
  return ms("<ul/>").addClass("pagination").append(n);
};
/*! Buttons for DataTables 3.2.6
 * © SpryMedia Ltd - datatables.net/license
 */
let ue = lt;
var Pg = 0, Og = 0, en = H.ext.buttons, wl = null;
function Jl(t, n, e) {
  ue.fn.animate ? t.stop().fadeIn(n, e) : (t.css("display", "block"), e && e.call(t));
}
function Zl(t, n, e) {
  ue.fn.animate ? t.stop().fadeOut(n, e) : (t.css("display", "none"), e && e.call(t));
}
var Je = function(t, n) {
  if (!H.versionCheck("2"))
    throw "Warning: Buttons requires DataTables 2 or newer";
  if (!(this instanceof Je))
    return function(e) {
      return new Je(e, t).container();
    };
  typeof n > "u" && (n = {}), n === !0 && (n = {}), Array.isArray(n) && (n = { buttons: n }), this.c = ue.extend(!0, {}, Je.defaults, n), n.buttons && (this.c.buttons = n.buttons), this.s = {
    dt: new H.Api(t),
    buttons: [],
    listenKeys: "",
    namespace: "dtb" + Pg++
  }, this.dom = {
    container: ue("<" + this.c.dom.container.tag + "/>").addClass(
      this.c.dom.container.className
    )
  }, this._constructor();
};
ue.extend(Je.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Public methods
   */
  /**
   * Get the action of a button
   * @param  {int|string} Button index
   * @return {function}
   */
  /**
  * Set the action of a button
  * @param  {node} node Button element
  * @param  {function} action Function to set
  * @return {Buttons} Self for chaining
  */
  action: function(t, n) {
    var e = this._nodeToButton(t);
    return n === void 0 ? e.conf.action : (e.conf.action = n, this);
  },
  /**
   * Add an active class to the button to make to look active or get current
   * active state.
   * @param  {node} node Button element
   * @param  {boolean} [flag] Enable / disable flag
   * @return {Buttons} Self for chaining or boolean for getter
   */
  active: function(t, n) {
    var e = this._nodeToButton(t), r = this.c.dom.button.active, i = ue(e.node);
    return e.inCollection && this.c.dom.collection.button && this.c.dom.collection.button.active !== void 0 && (r = this.c.dom.collection.button.active), n === void 0 ? i.hasClass(r) : (i.toggleClass(r, n === void 0 ? !0 : n), this);
  },
  /**
   * Add a new button
   * @param {object} config Button configuration object, base string name or function
   * @param {int|string} [idx] Button index for where to insert the button
   * @param {boolean} [draw=true] Trigger a draw. Set a false when adding
   *   lots of buttons, until the last button.
   * @return {Buttons} Self for chaining
   */
  add: function(t, n, e) {
    var r = this.s.buttons;
    if (typeof n == "string") {
      for (var i = n.split("-"), s = this.s, a = 0, o = i.length - 1; a < o; a++)
        s = s.buttons[i[a] * 1];
      r = s.buttons, n = i[i.length - 1] * 1;
    }
    let l = this._expandButton(
      r,
      t,
      t !== void 0 ? t.split : void 0,
      (t === void 0 || t.split === void 0 || t.split.length === 0) && s !== void 0,
      !1,
      n
    );
    return (e === void 0 || e === !0) && this._draw(), l;
  },
  /**
   * Clear buttons from a collection and then insert new buttons
   */
  collectionRebuild: function(t, n) {
    var e = this._nodeToButton(t);
    if (n !== void 0) {
      var r;
      for (r = e.buttons.length - 1; r >= 0; r--)
        this.remove(e.buttons[r].node);
      for (e.conf.prefixButtons && n.unshift.apply(n, e.conf.prefixButtons), e.conf.postfixButtons && n.push.apply(n, e.conf.postfixButtons), r = 0; r < n.length; r++) {
        var i = n[r];
        this._expandButton(
          e.buttons,
          i,
          i !== void 0 && i.config !== void 0 && i.config.split !== void 0,
          !0,
          i.parentConf !== void 0 && i.parentConf.split !== void 0,
          null,
          i.parentConf
        );
      }
    }
    this._draw(e.collection, e.buttons);
  },
  /**
   * Get the container node for the buttons
   * @return {jQuery} Buttons node
   */
  container: function() {
    return this.dom.container;
  },
  /**
   * Disable a button
   * @param  {node} node Button node
   * @return {Buttons} Self for chaining
   */
  disable: function(t) {
    var n = this._nodeToButton(t);
    return n.isSplit ? ue(n.node.childNodes[0]).addClass(this.c.dom.button.disabled).prop("disabled", !0) : ue(n.node).addClass(this.c.dom.button.disabled).prop("disabled", !0), n.disabled = !0, this._checkSplitEnable(), this;
  },
  /**
   * Destroy the instance, cleaning up event handlers and removing DOM
   * elements
   * @return {Buttons} Self for chaining
   */
  destroy: function() {
    ue("body").off("keyup." + this.s.namespace);
    var t = this.s.buttons.slice(), n, e;
    for (n = 0, e = t.length; n < e; n++)
      this.remove(t[n].node);
    this.dom.container.remove();
    var r = this.s.dt.settings()[0];
    for (n = 0, e = r.length; n < e; n++)
      if (r.inst === this) {
        r.splice(n, 1);
        break;
      }
    return this;
  },
  /**
   * Enable / disable a button
   * @param  {node} node Button node
   * @param  {boolean} [flag=true] Enable / disable flag
   * @return {Buttons} Self for chaining
   */
  enable: function(t, n) {
    if (n === !1)
      return this.disable(t);
    var e = this._nodeToButton(t);
    return e.isSplit ? ue(e.node.childNodes[0]).removeClass(this.c.dom.button.disabled).prop("disabled", !1) : ue(e.node).removeClass(this.c.dom.button.disabled).prop("disabled", !1), e.disabled = !1, this._checkSplitEnable(), this;
  },
  /**
   * Get a button's index
   *
   * This is internally recursive
   * @param {element} node Button to get the index of
   * @return {string} Button index
   */
  index: function(t, n, e) {
    n || (n = "", e = this.s.buttons);
    for (var r = 0, i = e.length; r < i; r++) {
      var s = e[r].buttons;
      if (e[r].node === t)
        return n + r;
      if (s && s.length) {
        var a = this.index(t, r + "-", s);
        if (a !== null)
          return a;
      }
    }
    return null;
  },
  /**
   * Get the instance name for the button set selector
   * @return {string} Instance name
   */
  name: function() {
    return this.c.name;
  },
  /**
   * Get a button's node of the buttons container if no button is given
   * @param  {node} [node] Button node
   * @return {jQuery} Button element, or container
   */
  node: function(t) {
    if (!t)
      return this.dom.container;
    var n = this._nodeToButton(t);
    return ue(n.node);
  },
  /**
   * Set / get a processing class on the selected button
   * @param {element} node Triggering button node
   * @param  {boolean} flag true to add, false to remove, undefined to get
   * @return {boolean|Buttons} Getter value or this if a setter.
   */
  processing: function(t, n) {
    var e = this.s.dt, r = this._nodeToButton(t);
    return n === void 0 ? ue(r.node).hasClass("processing") : (ue(r.node).toggleClass("processing", n), ue(e.table().node()).triggerHandler("buttons-processing.dt", [
      n,
      e.button(t),
      e,
      ue(t),
      r.conf
    ]), this);
  },
  /**
   * Remove a button.
   * @param  {node} node Button node
   * @return {Buttons} Self for chaining
   */
  remove: function(t) {
    var n = this._nodeToButton(t), e = this._nodeToHost(t), r = this.s.dt;
    if (n.buttons.length)
      for (var i = n.buttons.length - 1; i >= 0; i--)
        this.remove(n.buttons[i].node);
    n.conf.destroying = !0, n.conf.destroy && n.conf.destroy.call(r.button(t), r, ue(t), n.conf), this._removeKey(n.conf), ue(n.node).remove(), n.inserter && ue(n.inserter).remove();
    var s = ue.inArray(n, e);
    return e.splice(s, 1), this;
  },
  /**
   * Get the text for a button
   * @param  {int|string} node Button index
   * @return {string} Button text
   */
  /**
  * Set the text for a button
  * @param  {int|string|function} node Button index
  * @param  {string} label Text
  * @return {Buttons} Self for chaining
  */
  text: function(t, n) {
    var e = this._nodeToButton(t), r = e.textNode, i = this.s.dt, s = ue(e.node), a = function(o) {
      return typeof o == "function" ? o(i, s, e.conf) : o;
    };
    return n === void 0 ? a(e.conf.text) : (e.conf.text = n, r.html(a(n)), this);
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Buttons constructor
   * @private
   */
  _constructor: function() {
    var t = this, n = this.s.dt, e = n.settings()[0], r = this.c.buttons;
    e._buttons || (e._buttons = []), e._buttons.push({
      inst: this,
      name: this.c.name
    });
    for (var i = 0, s = r.length; i < s; i++)
      this.add(r[i]);
    n.on("destroy", function(a, o) {
      o === e && t.destroy();
    }), ue("body").on("keyup." + this.s.namespace, function(a) {
      if (!document.activeElement || document.activeElement === document.body) {
        var o = String.fromCharCode(a.keyCode).toLowerCase();
        t.s.listenKeys.toLowerCase().indexOf(o) !== -1 && t._keypress(o, a);
      }
    });
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Add a new button to the key press listener
   * @param {object} conf Resolved button configuration object
   * @private
   */
  _addKey: function(t) {
    t.key && (this.s.listenKeys += ue.isPlainObject(t.key) ? t.key.key : t.key);
  },
  /**
   * Insert the buttons into the container. Call without parameters!
   * @param  {node} [container] Recursive only - Insert point
   * @param  {array} [buttons] Recursive only - Buttons array
   * @private
   */
  _draw: function(t, n) {
    t || (t = this.dom.container, n = this.s.buttons), t.children().detach();
    for (var e = 0, r = n.length; e < r; e++)
      t.append(n[e].inserter), t.append(" "), n[e].buttons && n[e].buttons.length && this._draw(n[e].collection, n[e].buttons);
  },
  /**
   * Create buttons from an array of buttons
   * @param  {array} attachTo Buttons array to attach to
   * @param  {object} button Button definition
   * @param  {boolean} inCollection true if the button is in a collection
   * @private
   */
  _expandButton: function(t, n, e, r, i, s, a) {
    var o = this.s.dt, l = !1, c = this.c.dom.collection, f = Array.isArray(n) ? n : [n], g;
    n === void 0 && (f = Array.isArray(e) ? e : [e]);
    for (var b = 0, w = f.length; b < w; b++) {
      var C = this._resolveExtends(f[b]);
      if (C) {
        if (l = !!(C.config && C.config.split), Array.isArray(C)) {
          this._expandButton(
            t,
            C,
            m !== void 0 && m.conf !== void 0 ? m.conf.split : void 0,
            r,
            a !== void 0 && a.split !== void 0,
            s,
            a
          );
          continue;
        }
        var m = this._buildButton(
          C,
          r,
          C.split !== void 0 || C.config !== void 0 && C.config.split !== void 0,
          i
        );
        if (m) {
          if (s != null ? (t.splice(s, 0, m), s++) : t.push(m), m.conf.dropIcon && !m.conf.split && ue(m.node).addClass(this.c.dom.button.dropClass).append(this.c.dom.button.dropHtml), m.conf.buttons && (m.collection = ue(
            "<" + c.container.content.tag + "/>"
          ), m.conf._collection = m.collection, this._expandButton(
            m.buttons,
            m.conf.buttons,
            m.conf.split,
            !l,
            l,
            s,
            m.conf
          )), m.conf.split) {
            m.collection = ue("<" + c.container.tag + "/>"), m.conf._collection = m.collection;
            for (var h = 0; h < m.conf.split.length; h++) {
              var u = m.conf.split[h];
              typeof u == "object" && (u.parent = a, u.collectionLayout === void 0 && (u.collectionLayout = m.conf.collectionLayout), u.dropup === void 0 && (u.dropup = m.conf.dropup), u.fade === void 0 && (u.fade = m.conf.fade));
            }
            this._expandButton(
              m.buttons,
              m.conf.buttons,
              m.conf.split,
              !l,
              l,
              s,
              m.conf
            );
          }
          m.conf.parent = a, C.init && C.init.call(o.button(m.node), o, ue(m.node), C), g = m.node;
        }
      }
    }
    return g;
  },
  /**
   * Create an individual button
   * @param  {object} config            Resolved button configuration
   * @param  {boolean} inCollection `true` if a collection button
   * @return {object} Completed button description object
   * @private
   */
  _buildButton: function(t, n, e, r) {
    var i = this, s = this.c.dom, a, o = this.s.dt, l = !1, c = function(F) {
      return typeof F == "function" ? F(o, b, t) : F;
    }, f = ue.extend(!0, {}, s.button);
    if (n && e && s.collection.split ? ue.extend(!0, f, s.collection.split.action) : r || n ? ue.extend(!0, f, s.collection.button) : e && ue.extend(!0, f, s.split.button), t.spacer) {
      var g = ue("<" + f.spacer.tag + "/>").addClass(
        "dt-button-spacer " + t.style + " " + f.spacer.className
      ).html(c(t.text));
      return {
        conf: t,
        node: g,
        nodeChild: null,
        inserter: g,
        buttons: [],
        inCollection: n,
        isSplit: e,
        collection: null,
        textNode: g
      };
    }
    if (t.available && !t.available(o, t) && !t.html)
      return !1;
    var b;
    if (t.html)
      b = ue(t.html);
    else {
      var w = function(F, P, k, R, L) {
        R.action.call(P.button(k), F, P, k, R, L), ue(P.table().node()).triggerHandler("buttons-action.dt", [
          P.button(k),
          P,
          k,
          R
        ]);
      }, C = function(F, P, k, R) {
        R.async ? (i.processing(k[0], !0), setTimeout(function() {
          w(F, P, k, R, function() {
            i.processing(k[0], !1);
          });
        }, R.async)) : w(F, P, k, R, function() {
        });
      }, m = t.tag || f.tag, h = t.clickBlurs === void 0 ? !0 : t.clickBlurs;
      if (b = ue("<" + m + "/>").addClass(f.className).attr("aria-controls", this.s.dt.table().node().id).on("click.dtb", function(F) {
        F.preventDefault(), !b.hasClass(f.disabled) && t.action && C(F, o, b, t), h && b.trigger("blur");
      }).on("keypress.dtb", function(F) {
        F.keyCode === 13 && (F.preventDefault(), !b.hasClass(f.disabled) && t.action && C(F, o, b, t));
      }), m.toLowerCase() === "a" && b.attr("href", "#"), m.toLowerCase() === "button" && b.attr("type", "button"), f.liner.tag) {
        var u = f.liner.tag.toLowerCase(), p = ue("<" + u + "/>").html(c(t.text)).addClass(f.liner.className);
        u === "a" && p.attr("href", "#"), (u === "a" || u === "button") && (p.attr("tabindex", this.s.dt.settings()[0].iTabIndex), l = !0), b.append(p), a = p;
      } else
        b.html(c(t.text)), a = b;
      l || b.attr("tabindex", this.s.dt.settings()[0].iTabIndex), t.enabled === !1 && b.addClass(f.disabled), t.className && b.addClass(t.className), t.titleAttr && b.attr("title", c(t.titleAttr)), t.attr && b.attr(t.attr), t.namespace || (t.namespace = ".dt-button-" + Og++), t.config !== void 0 && t.config.split && (t.split = t.config.split);
    }
    var v = this.c.dom.buttonContainer, S;
    v && v.tag ? S = ue("<" + v.tag + "/>").addClass(v.className).append(b) : S = b, this._addKey(t), this.c.buttonCreated && (S = this.c.buttonCreated(t, S));
    var T;
    if (e) {
      var A = n ? ue.extend(!0, this.c.dom.split, this.c.dom.collection.split) : this.c.dom.split, y = A.wrapper;
      T = ue("<" + y.tag + "/>").addClass(y.className).append(b);
      var I = ue.extend(t, {
        autoClose: !0,
        align: A.dropdown.align,
        attr: {
          "aria-haspopup": "dialog",
          "aria-expanded": !1
        },
        className: A.dropdown.className,
        closeButton: !1,
        splitAlignClass: A.dropdown.splitAlignClass,
        text: A.dropdown.text
      });
      this._addKey(I);
      var $ = function(F, P, k, R) {
        en.split.action.call(
          P.button(T),
          F,
          P,
          k,
          R
        ), ue(P.table().node()).triggerHandler("buttons-action.dt", [
          P.button(k),
          P,
          k,
          R
        ]), k.attr("aria-expanded", !0);
      }, V = ue(
        '<button class="' + A.dropdown.className + ' dt-button"></button>'
      ).html(this.c.dom.button.dropHtml).addClass(this.c.dom.button.dropClass).on("click.dtb", function(F) {
        F.preventDefault(), F.stopPropagation(), V.hasClass(f.disabled) || $(F, o, V, I), h && V.trigger("blur");
      }).on("keypress.dtb", function(F) {
        F.keyCode === 13 && (F.preventDefault(), V.hasClass(f.disabled) || $(F, o, V, I));
      });
      t.split.length === 0 && V.addClass("dtb-hide-drop"), T.append(V).attr(I.attr);
    }
    var z = e ? T.get(0) : b.get(0);
    return {
      conf: t,
      node: z,
      nodeChild: z && z.children && z.children.length ? z.children[0] : null,
      inserter: e ? T : S,
      buttons: [],
      inCollection: n,
      isSplit: e,
      inSplit: r,
      collection: null,
      textNode: a
    };
  },
  /**
   * Spin over buttons checking if splits should be enabled or not.
   * @param {*} buttons Array of buttons to check
   */
  _checkSplitEnable: function(t) {
    t || (t = this.s.buttons);
    for (var n = 0; n < t.length; n++) {
      var e = t[n];
      if (e.isSplit) {
        var r = e.node.childNodes[1];
        this._checkAnyEnabled(e.buttons) ? ue(r).removeClass(this.c.dom.button.disabled).prop("disabled", !1) : ue(r).addClass(this.c.dom.button.disabled).prop("disabled", !1);
      } else
        e.isCollection && this._checkSplitEnable(e.buttons);
    }
  },
  /**
   * Check an array of buttons and see if any are enabled in it
   * @param {*} buttons Button array
   * @returns true if a button is enabled, false otherwise
   */
  _checkAnyEnabled: function(t) {
    for (var n = 0; n < t.length; n++)
      if (!t[n].disabled)
        return !0;
    return !1;
  },
  /**
   * Get the button object from a node (recursive)
   * @param  {node} node Button node
   * @param  {array} [buttons] Button array, uses base if not defined
   * @return {object} Button object
   * @private
   */
  _nodeToButton: function(t, n) {
    n || (n = this.s.buttons);
    for (var e = 0, r = n.length; e < r; e++) {
      if (n[e].node === t || n[e].nodeChild === t)
        return n[e];
      if (n[e].buttons.length) {
        var i = this._nodeToButton(t, n[e].buttons);
        if (i)
          return i;
      }
    }
  },
  /**
   * Get container array for a button from a button node (recursive)
   * @param  {node} node Button node
   * @param  {array} [buttons] Button array, uses base if not defined
   * @return {array} Button's host array
   * @private
   */
  _nodeToHost: function(t, n) {
    n || (n = this.s.buttons);
    for (var e = 0, r = n.length; e < r; e++) {
      if (n[e].node === t)
        return n;
      if (n[e].buttons.length) {
        var i = this._nodeToHost(t, n[e].buttons);
        if (i)
          return i;
      }
    }
  },
  /**
   * Handle a key press - determine if any button's key configured matches
   * what was typed and trigger the action if so.
   * @param  {string} character The character pressed
   * @param  {object} e Key event that triggered this call
   * @private
   */
  _keypress: function(t, n) {
    if (!n._buttonsHandled) {
      var e = function(i, s) {
        if (i.key) {
          if (i.key === t)
            n._buttonsHandled = !0, ue(s).click();
          else if (ue.isPlainObject(i.key)) {
            if (i.key.key !== t || i.key.shiftKey && !n.shiftKey || i.key.altKey && !n.altKey || i.key.ctrlKey && !n.ctrlKey || i.key.metaKey && !n.metaKey)
              return;
            n._buttonsHandled = !0, ue(s).click();
          }
        }
      }, r = function(i) {
        for (var s = 0, a = i.length; s < a; s++)
          e(i[s].conf, i[s].node), i[s].buttons.length && r(i[s].buttons);
      };
      r(this.s.buttons);
    }
  },
  /**
   * Remove a key from the key listener for this instance (to be used when a
   * button is removed)
   * @param  {object} conf Button configuration
   * @private
   */
  _removeKey: function(t) {
    if (t.key) {
      var n = ue.isPlainObject(t.key) ? t.key.key : t.key, e = this.s.listenKeys.split(""), r = ue.inArray(n, e);
      e.splice(r, 1), this.s.listenKeys = e.join("");
    }
  },
  /**
   * Resolve a button configuration
   * @param  {string|function|object} conf Button config to resolve
   * @return {object} Button configuration
   * @private
   */
  _resolveExtends: function(t) {
    var n = this, e = this.s.dt, r, i, s = function(f) {
      for (var g = 0; !ue.isPlainObject(f) && !Array.isArray(f); ) {
        if (f === void 0)
          return;
        if (typeof f == "function") {
          if (f = f.call(n, e, t), !f)
            return !1;
        } else if (typeof f == "string") {
          if (!en[f])
            return { html: f };
          f = en[f];
        }
        if (g++, g > 30)
          throw "Buttons: Too many iterations";
      }
      return Array.isArray(f) ? f : ue.extend({}, f);
    };
    for (t = s(t); t && t.extend; ) {
      if (!en[t.extend])
        throw "Cannot extend unknown button type: " + t.extend;
      var a = s(en[t.extend]);
      if (Array.isArray(a))
        return a;
      if (!a)
        return !1;
      var o = a.className;
      t.config !== void 0 && a.config !== void 0 && (t.config = ue.extend({}, a.config, t.config)), t = ue.extend({}, a, t), o && t.className !== o && (t.className = o + " " + t.className), t.extend = a.extend;
    }
    var l = t.postfixButtons;
    if (l)
      for (t.buttons || (t.buttons = []), r = 0, i = l.length; r < i; r++)
        t.buttons.push(l[r]);
    var c = t.prefixButtons;
    if (c)
      for (t.buttons || (t.buttons = []), r = 0, i = c.length; r < i; r++)
        t.buttons.splice(r, 0, c[r]);
    return t;
  },
  /**
   * Display (and replace if there is an existing one) a popover attached to a button
   * @param {string|node} content Content to show
   * @param {DataTable.Api} hostButton DT API instance of the button
   * @param {object} inOpts Options (see object below for all options)
   */
  _popover: function(t, n, e) {
    var r = n, i = this.c, s = !1, a = ue.extend(
      {
        align: "button-left",
        // button-right, dt-container, split-left, split-right
        autoClose: !1,
        background: !0,
        backgroundClassName: "dt-button-background",
        closeButton: !0,
        containerClassName: i.dom.collection.container.className,
        contentClassName: i.dom.collection.container.content.className,
        collectionLayout: "",
        collectionTitle: "",
        dropup: !1,
        fade: 400,
        popoverTitle: "",
        rightAlignClassName: "dt-button-right",
        tag: i.dom.collection.container.tag
      },
      e
    ), o = a.tag + "." + a.containerClassName.replace(/ /g, "."), l = n.node(), c = a.collectionLayout.includes("fixed") ? ue("body") : n.node(), f = function() {
      s = !0, Zl(ue(o), a.fade, function() {
        ue(this).detach();
      }), ue(
        r.buttons('[aria-haspopup="dialog"][aria-expanded="true"]').nodes()
      ).attr("aria-expanded", "false"), ue("div.dt-button-background").off("click.dtb-collection"), Je.background(
        !1,
        a.backgroundClassName,
        a.fade,
        c
      ), ue(window).off("resize.resize.dtb-collection"), ue("body").off(".dtb-collection"), r.off("buttons-action.b-internal"), r.off("destroy.dtb-popover"), ue("body").trigger("buttons-popover-hide.dt");
    };
    if (t === !1) {
      f();
      return;
    }
    var g = ue(
      r.buttons('[aria-haspopup="dialog"][aria-expanded="true"]').nodes()
    );
    if (g.length && (c.closest(o).length && (c = g.eq(0)), f()), a.sort) {
      var b = ue("button", t).map(function(F, P) {
        return {
          text: ue(P).text(),
          el: P
        };
      }).toArray();
      b.sort(function(F, P) {
        return F.text.localeCompare(P.text);
      }), ue(t).append(b.map(function(F) {
        return F.el;
      }));
    }
    var w = ue(".dt-button", t).length, C = "";
    w === 3 ? C = "dtb-b3" : w === 2 ? C = "dtb-b2" : w === 1 && (C = "dtb-b1");
    var m = ue("<" + a.tag + "/>").addClass(a.containerClassName).addClass(a.collectionLayout).addClass(a.splitAlignClass).addClass(C).css("display", "none").attr({
      "aria-modal": !0,
      role: "dialog"
    });
    t = ue(t).addClass(a.contentClassName).attr("role", "menu").appendTo(m), l.attr("aria-expanded", "true"), c.parents("body")[0] !== document.body && (c = ue(document.body).children("div, section, p").last()), a.popoverTitle ? m.prepend(
      '<div class="dt-button-collection-title">' + a.popoverTitle + "</div>"
    ) : a.collectionTitle && m.prepend(
      '<div class="dt-button-collection-title">' + a.collectionTitle + "</div>"
    ), a.closeButton && m.prepend('<div class="dtb-popover-close">&times;</div>').addClass("dtb-collection-closeable"), Jl(m.insertAfter(c), a.fade);
    var h = ue(n.table().container()), u = m.css("position");
    if ((a.span === "container" || a.align === "dt-container") && (c = c.parent(), m.css("width", h.width())), u === "absolute") {
      var p = ue(c[0].offsetParent), v = c.position(), S = c.offset(), T = p.offset(), A = p.position(), y = window.getComputedStyle(p[0]);
      T.height = p.outerHeight(), T.width = p.width() + parseFloat(y.paddingLeft), T.right = T.left + T.width, T.bottom = T.top + T.height;
      var I = v.top + c.outerHeight(), $ = v.left;
      m.css({
        top: I,
        left: $
      }), y = window.getComputedStyle(m[0]);
      var V = m.offset();
      V.height = m.outerHeight(), V.width = m.outerWidth(), V.right = V.left + V.width, V.bottom = V.top + V.height, V.marginTop = parseFloat(y.marginTop), V.marginBottom = parseFloat(y.marginBottom), a.dropup && (I = v.top - V.height - V.marginTop - V.marginBottom), (a.align === "button-right" || m.hasClass(a.rightAlignClassName)) && ($ = v.left - V.width + c.outerWidth()), (a.align === "dt-container" || a.align === "container") && $ < v.left && ($ = -v.left), A.left + $ + V.width > ue(window).width() && ($ = ue(window).width() - V.width - A.left), S.left + $ < 0 && ($ = -S.left), A.top + I + V.height > ue(window).height() + ue(window).scrollTop() && (I = v.top - V.height - V.marginTop - V.marginBottom), p.offset().top + I < ue(window).scrollTop() && (I = v.top + c.outerHeight()), m.css({
        top: I,
        left: $
      });
    } else {
      var z = function() {
        var F = ue(window).height() / 2, P = m.height() / 2;
        P > F && (P = F), m.css("marginTop", P * -1);
      };
      z(), ue(window).on("resize.dtb-collection", function() {
        z();
      });
    }
    a.background && Je.background(
      !0,
      a.backgroundClassName,
      a.fade,
      a.backgroundHost || c
    ), ue("div.dt-button-background").on(
      "click.dtb-collection",
      function() {
      }
    ), a.autoClose && setTimeout(function() {
      r.on("buttons-action.b-internal", function(F, P, k, R) {
        R[0] !== c[0] && f();
      });
    }, 0), ue(m).trigger("buttons-popover.dt"), r.on("destroy.dtb-popover", f), setTimeout(function() {
      s = !1, ue("body").on("click.dtb-collection", function(F) {
        if (!s) {
          var P = ue.fn.addBack ? "addBack" : "andSelf", k = ue(F.target).parent()[0];
          (!ue(F.target).parents()[P]().filter(t).length && !ue(k).hasClass("dt-buttons") || ue(F.target).hasClass("dt-button-background")) && f();
        }
      }).on("keyup.dtb-collection", function(F) {
        F.keyCode === 27 && f();
      }).on("keydown.dtb-collection", function(F) {
        var P = ue("a, button", t), k = document.activeElement;
        F.keyCode === 9 && (P.index(k) === -1 ? (P.first().focus(), F.preventDefault()) : F.shiftKey ? k === P[0] && (P.last().focus(), F.preventDefault()) : k === P.last()[0] && (P.first().focus(), F.preventDefault()));
      });
    }, 0);
  }
});
Je.background = function(t, n, e, r) {
  e === void 0 && (e = 400), r || (r = document.body), t ? Jl(
    ue("<div/>").addClass(n).css("display", "none").insertAfter(r),
    e
  ) : Zl(ue("div." + n), e, function() {
    ue(this).removeClass(n).remove();
  });
};
Je.instanceSelector = function(t, n) {
  if (t == null)
    return ue.map(n, function(s) {
      return s.inst;
    });
  var e = [], r = ue.map(n, function(s) {
    return s.name;
  }), i = function(s) {
    if (Array.isArray(s)) {
      for (var a = 0, o = s.length; a < o; a++)
        i(s[a]);
      return;
    }
    if (typeof s == "string")
      if (s.indexOf(",") !== -1)
        i(s.split(","));
      else {
        var l = ue.inArray(s.trim(), r);
        l !== -1 && e.push(n[l].inst);
      }
    else if (typeof s == "number")
      e.push(n[s].inst);
    else if (typeof s == "object" && s.nodeName)
      for (var c = 0; c < n.length; c++)
        n[c].inst.dom.container[0] === s && e.push(n[c].inst);
    else
      typeof s == "object" && e.push(s);
  };
  return i(t), e;
};
Je.buttonSelector = function(t, n) {
  for (var e = [], r = function(l, c, f) {
    for (var g, b, w = 0, C = c.length; w < C; w++)
      g = c[w], g && (b = f !== void 0 ? f + w : w + "", l.push({
        node: g.node,
        name: g.conf.name,
        idx: b
      }), g.buttons && r(l, g.buttons, b + "-"));
  }, i = function(l, c) {
    var f, g, b = [];
    r(b, c.s.buttons);
    var w = ue.map(b, function(p) {
      return p.node;
    });
    if (Array.isArray(l) || l instanceof ue) {
      for (f = 0, g = l.length; f < g; f++)
        i(l[f], c);
      return;
    }
    if (l == null || l === "*")
      for (f = 0, g = b.length; f < g; f++)
        e.push({
          inst: c,
          node: b[f].node
        });
    else if (typeof l == "number")
      c.s.buttons[l] && e.push({
        inst: c,
        node: c.s.buttons[l].node
      });
    else if (typeof l == "string")
      if (l.indexOf(",") !== -1) {
        var C = l.split(",");
        for (f = 0, g = C.length; f < g; f++)
          i(C[f].trim(), c);
      } else if (l.match(/^\d+(\-\d+)*$/)) {
        var m = ue.map(b, function(p) {
          return p.idx;
        });
        e.push({
          inst: c,
          node: b[ue.inArray(l, m)].node
        });
      } else if (l.indexOf(":name") !== -1) {
        var h = l.replace(":name", "");
        for (f = 0, g = b.length; f < g; f++)
          b[f].name === h && e.push({
            inst: c,
            node: b[f].node
          });
      } else
        ue(w).filter(l).each(function() {
          e.push({
            inst: c,
            node: this
          });
        });
    else if (typeof l == "object" && l.nodeName) {
      var u = ue.inArray(l, w);
      u !== -1 && e.push({
        inst: c,
        node: w[u]
      });
    }
  }, s = 0, a = t.length; s < a; s++) {
    var o = t[s];
    i(n, o);
  }
  return e;
};
Je.stripData = function(t, n) {
  return t !== null && typeof t == "object" && t.nodeName && t.nodeType && (t = t.innerHTML), typeof t != "string" || (t = Je.stripHtmlScript(t), t = Je.stripHtmlComments(t), (!n || n.stripHtml) && (t = H.util.stripHtml(t)), (!n || n.trim) && (t = t.trim()), (!n || n.stripNewlines) && (t = t.replace(/\n/g, " ")), (!n || n.decodeEntities) && (wl ? t = wl(t) : (Dc.innerHTML = t, t = Dc.value)), (!n || n.escapeExcelFormula) && t.match(/^[=@\t\r]/) && (t = "'" + t)), t;
};
Je.entityDecoder = function(t) {
  wl = t;
};
Je.stripHtmlComments = function(t) {
  var n;
  do
    n = t, t = t.replace(/(<!--.*?--!?>)|(<!--[\S\s]+?--!?>)|(<!--[\S\s]*?$)/g, "");
  while (t !== n);
  return t;
};
Je.stripHtmlScript = function(t) {
  var n;
  do
    n = t, t = t.replace(/<script\b[^<]*(?:(?!<\/script[^>]*>)<[^<]*)*<\/script[^>]*>/gi, "");
  while (t !== n);
  return t;
};
Je.defaults = {
  buttons: ["copy", "excel", "csv", "pdf", "print"],
  name: "main",
  tabIndex: 0,
  dom: {
    container: {
      tag: "div",
      className: "dt-buttons"
    },
    collection: {
      container: {
        // The element used for the dropdown
        className: "dt-button-collection",
        content: {
          className: "",
          tag: "div"
        },
        tag: "div"
      }
      // optionally
      // , button: IButton - buttons inside the collection container
      // , split: ISplit - splits inside the collection container
    },
    button: {
      tag: "button",
      className: "dt-button",
      active: "dt-button-active",
      // class name
      disabled: "disabled",
      // class name
      spacer: {
        className: "dt-button-spacer",
        tag: "span"
      },
      liner: {
        tag: "span",
        className: ""
      },
      dropClass: "",
      dropHtml: '<span class="dt-button-down-arrow">&#x25BC;</span>'
    },
    split: {
      action: {
        // action button
        className: "dt-button-split-drop-button dt-button",
        tag: "button"
      },
      dropdown: {
        // button to trigger the dropdown
        align: "split-right",
        className: "dt-button-split-drop",
        splitAlignClass: "dt-button-split-left",
        tag: "button"
      },
      wrapper: {
        // wrap around both
        className: "dt-button-split",
        tag: "div"
      }
    }
  }
};
Je.version = "3.2.6";
ue.extend(en, {
  collection: {
    text: function(t) {
      return t.i18n("buttons.collection", "Collection");
    },
    className: "buttons-collection",
    closeButton: !1,
    dropIcon: !0,
    init: function(t, n) {
      n.attr("aria-expanded", !1);
    },
    action: function(t, n, e, r) {
      r._collection.parents("body").length ? this.popover(!1, r) : this.popover(r._collection, r), t.type === "keypress" && ue("a, button", r._collection).eq(0).focus();
    },
    attr: {
      "aria-haspopup": "dialog"
    }
    // Also the popover options, defined in Buttons.popover
  },
  split: {
    text: function(t) {
      return t.i18n("buttons.split", "Split");
    },
    className: "buttons-split",
    closeButton: !1,
    init: function(t, n) {
      return n.attr("aria-expanded", !1);
    },
    action: function(t, n, e, r) {
      this.popover(r._collection, r);
    },
    attr: {
      "aria-haspopup": "dialog"
    }
    // Also the popover options, defined in Buttons.popover
  },
  copy: function() {
    if (en.copyHtml5)
      return "copyHtml5";
  },
  csv: function(t, n) {
    if (en.csvHtml5 && en.csvHtml5.available(t, n))
      return "csvHtml5";
  },
  excel: function(t, n) {
    if (en.excelHtml5 && en.excelHtml5.available(t, n))
      return "excelHtml5";
  },
  pdf: function(t, n) {
    if (en.pdfHtml5 && en.pdfHtml5.available(t, n))
      return "pdfHtml5";
  },
  pageLength: function(t) {
    var n = t.settings()[0].aLengthMenu, e = [], r = [], i = function(o) {
      return o.i18n(
        "buttons.pageLength",
        {
          "-1": "Show all rows",
          _: "Show %d rows"
        },
        o.page.len()
      );
    };
    if (Array.isArray(n[0]))
      e = n[0], r = n[1];
    else
      for (var s = 0; s < n.length; s++) {
        var a = n[s];
        ue.isPlainObject(a) ? (e.push(a.value), r.push(a.label)) : (e.push(a), r.push(a));
      }
    return {
      extend: "collection",
      text: i,
      className: "buttons-page-length",
      autoClose: !0,
      buttons: ue.map(e, function(o, l) {
        return {
          text: r[l],
          className: "button-page-length",
          action: function(c, f) {
            f.page.len(o).draw();
          },
          init: function(c, f, g) {
            var b = this, w = function() {
              b.active(c.page.len() === o);
            };
            c.on("length.dt" + g.namespace, w), w();
          },
          destroy: function(c, f, g) {
            c.off("length.dt" + g.namespace);
          }
        };
      }),
      init: function(o, l, c) {
        var f = this;
        o.on("length.dt" + c.namespace, function() {
          f.text(c.text);
        });
      },
      destroy: function(o, l, c) {
        o.off("length.dt" + c.namespace);
      }
    };
  },
  spacer: {
    style: "empty",
    spacer: !0,
    text: function(t) {
      return t.i18n("buttons.spacer", "");
    }
  }
});
H.Api.register("buttons()", function(t, n) {
  n === void 0 && (n = t, t = void 0), this.selector.buttonGroup = t;
  var e = this.iterator(
    !0,
    "table",
    function(r) {
      if (r._buttons)
        return Je.buttonSelector(
          Je.instanceSelector(t, r._buttons),
          n
        );
    },
    !0
  );
  return e._groupSelector = t, e;
});
H.Api.register("button()", function(t, n) {
  var e = this.buttons(t, n);
  return e.length > 1 && e.splice(1, e.length), e;
});
H.Api.registerPlural(
  "buttons().active()",
  "button().active()",
  function(t) {
    return t === void 0 ? this.map(function(n) {
      return n.inst.active(n.node);
    }) : this.each(function(n) {
      n.inst.active(n.node, t);
    });
  }
);
H.Api.registerPlural(
  "buttons().action()",
  "button().action()",
  function(t) {
    return t === void 0 ? this.map(function(n) {
      return n.inst.action(n.node);
    }) : this.each(function(n) {
      n.inst.action(n.node, t);
    });
  }
);
H.Api.registerPlural(
  "buttons().collectionRebuild()",
  "button().collectionRebuild()",
  function(t) {
    return this.each(function(n) {
      for (var e = 0; e < t.length; e++)
        typeof t[e] == "object" && (t[e].parentConf = n);
      n.inst.collectionRebuild(n.node, t);
    });
  }
);
H.Api.register(
  ["buttons().enable()", "button().enable()"],
  function(t) {
    return this.each(function(n) {
      n.inst.enable(n.node, t);
    });
  }
);
H.Api.register(
  ["buttons().disable()", "button().disable()"],
  function() {
    return this.each(function(t) {
      t.inst.disable(t.node);
    });
  }
);
H.Api.register("button().index()", function() {
  var t = null;
  return this.each(function(n) {
    var e = n.inst.index(n.node);
    e !== null && (t = e);
  }), t;
});
H.Api.registerPlural(
  "buttons().nodes()",
  "button().node()",
  function() {
    var t = ue();
    return ue(
      this.each(function(n) {
        t = t.add(n.inst.node(n.node));
      })
    ), t;
  }
);
H.Api.registerPlural(
  "buttons().processing()",
  "button().processing()",
  function(t) {
    return t === void 0 ? this.map(function(n) {
      return n.inst.processing(n.node);
    }) : this.each(function(n) {
      n.inst.processing(n.node, t);
    });
  }
);
H.Api.registerPlural(
  "buttons().text()",
  "button().text()",
  function(t) {
    return t === void 0 ? this.map(function(n) {
      return n.inst.text(n.node);
    }) : this.each(function(n) {
      n.inst.text(n.node, t);
    });
  }
);
H.Api.registerPlural(
  "buttons().trigger()",
  "button().trigger()",
  function() {
    return this.each(function(t) {
      t.inst.node(t.node).trigger("click");
    });
  }
);
H.Api.register("button().popover()", function(t, n) {
  return this.map(function(e) {
    return e.inst._popover(t, this.button(this[0].node), n);
  });
});
H.Api.register("buttons().containers()", function() {
  var t = ue(), n = this._groupSelector;
  return this.iterator(!0, "table", function(e) {
    if (e._buttons)
      for (var r = Je.instanceSelector(n, e._buttons), i = 0, s = r.length; i < s; i++)
        t = t.add(r[i].container());
  }), t;
});
H.Api.register("buttons().container()", function() {
  return this.containers().eq(0);
});
H.Api.register("button().add()", function(t, n, e) {
  var r = this.context, i;
  if (r.length) {
    var s = Je.instanceSelector(
      this._groupSelector,
      r[0]._buttons
    );
    s.length && (i = s[0].add(n, t, e));
  }
  return i ? this.button(this._groupSelector, i) : this;
});
H.Api.register("buttons().destroy()", function() {
  return this.pluck("inst").unique().each(function(t) {
    t.destroy();
  }), this;
});
H.Api.registerPlural(
  "buttons().remove()",
  "buttons().remove()",
  function() {
    return this.each(function(t) {
      t.inst.remove(t.node);
    }), this;
  }
);
var Ji;
H.Api.register("buttons.info()", function(t, n, e) {
  var r = this;
  return t === !1 ? (this.off("destroy.btn-info"), Zl(ue("#datatables_buttons_info"), 400, function() {
    ue(this).remove();
  }), clearTimeout(Ji), Ji = null, this) : (Ji && clearTimeout(Ji), ue("#datatables_buttons_info").length && ue("#datatables_buttons_info").remove(), t = t ? "<h2>" + t + "</h2>" : "", Jl(
    ue('<div id="datatables_buttons_info" class="dt-button-info"/>').html(t).append(
      ue("<div/>")[typeof n == "string" ? "html" : "append"](
        n
      )
    ).css("display", "none").appendTo("body")
  ), e !== void 0 && e !== 0 && (Ji = setTimeout(function() {
    r.buttons.info(!1);
  }, e)), this.on("destroy.btn-info", function() {
    r.buttons.info(!1);
  }), this);
});
H.Api.register("buttons.exportData()", function(t) {
  if (this.context.length)
    return Rg(new H.Api(this.context[0]), t);
});
H.Api.register("buttons.exportInfo()", function(t) {
  return t || (t = {}), {
    filename: Ig(t, this),
    title: Ng(t, this),
    messageTop: Tc(this, t, t.message || t.messageTop, "top"),
    messageBottom: Tc(this, t, t.messageBottom, "bottom")
  };
});
var Ig = function(t, n) {
  var e = t.filename === "*" && t.title !== "*" && t.title !== void 0 && t.title !== null && t.title !== "" ? t.title : t.filename;
  if (typeof e == "function" && (e = e(t, n)), e == null)
    return null;
  e.indexOf("*") !== -1 && (e = e.replace(/\*/g, ue("head > title").text()).trim()), e = e.replace(/[^a-zA-Z0-9_\u00A1-\uFFFF\.,\-_ !\(\)]/g, "");
  var r = eu(t.extension, t, n);
  return r || (r = ""), e + r;
}, eu = function(t, n, e) {
  return t == null ? null : typeof t == "function" ? t(n, e) : t;
}, Ng = function(t, n) {
  var e = eu(t.title, t, n);
  return e === null ? null : e.indexOf("*") !== -1 ? e.replace(/\*/g, ue("head > title").text() || "Exported data") : e;
}, Tc = function(t, n, e, r) {
  var i = eu(e, n, t);
  if (i === null)
    return null;
  var s = ue("caption", t.table().container()).eq(0);
  if (i === "*") {
    var a = s.css("caption-side");
    return a !== r ? null : s.length ? s.text() : "";
  }
  return i;
}, Dc = ue("<textarea/>")[0], Rg = function(t, n) {
  var e = ue.extend(
    !0,
    {},
    {
      rows: null,
      columns: "",
      modifier: {
        search: "applied",
        order: "applied"
      },
      orthogonal: "display",
      stripHtml: !0,
      stripNewlines: !0,
      decodeEntities: !0,
      escapeExcelFormula: !1,
      trim: !0,
      format: {
        header: function(S) {
          return Je.stripData(S, e);
        },
        footer: function(S) {
          return Je.stripData(S, e);
        },
        body: function(S) {
          return Je.stripData(S, e);
        }
      },
      customizeData: null,
      customizeZip: null
    },
    n
  ), r = t.columns(e.columns).indexes().map(function(S) {
    var T = t.column(S);
    return e.format.header(T.title(), S, T.header());
  }).toArray(), i = t.table().footer() ? t.columns(e.columns).indexes().map(function(S) {
    var T = t.column(S).footer(), A = "";
    if (T) {
      var y = ue(".dt-column-title", T);
      A = y.length ? y.html() : ue(T).html();
    }
    return e.format.footer(A, S, T);
  }).toArray() : null, s = ue.extend({}, e.modifier);
  t.select && typeof t.select.info == "function" && s.selected === void 0 && t.rows(e.rows, ue.extend({ selected: !0 }, s)).any() && ue.extend(s, { selected: !0 });
  for (var a = t.rows(e.rows, s).indexes().toArray(), o = t.cells(a, e.columns, {
    order: s.order
  }), l = o.render(e.orthogonal).toArray(), c = o.nodes().toArray(), f = o.indexes().toArray(), g = t.columns(e.columns).count(), b = g > 0 ? l.length / g : 0, w = [], C = 0, m = 0, h = b; m < h; m++) {
    for (var u = [g], p = 0; p < g; p++)
      u[p] = e.format.body(
        l[C],
        f[C].row,
        f[C].column,
        c[C]
      ), C++;
    w[m] = u;
  }
  var v = {
    header: r,
    headerStructure: Ec(
      e.format.header,
      t.table().header.structure(e.columns)
    ),
    footer: i,
    footerStructure: Ec(
      e.format.footer,
      t.table().footer.structure(e.columns)
    ),
    body: w
  };
  return e.customizeData && e.customizeData(v), v;
};
function Ec(t, n) {
  for (var e = 0; e < n.length; e++)
    for (var r = 0; r < n[e].length; r++) {
      var i = n[e][r];
      i && (i.title = t(
        i.title,
        r,
        i.cell
      ));
    }
  return n;
}
ue.fn.dataTable.Buttons = Je;
ue.fn.DataTable.Buttons = Je;
ue(document).on("init.dt plugin-init.dt", function(t, n) {
  if (t.namespace === "dt") {
    var e = n.oInit.buttons || H.defaults.buttons;
    e && !n._buttons && new Je(n, e).container();
  }
});
function lf(t, n) {
  var e = new H.Api(t), r = n || e.init().buttons || H.defaults.buttons;
  return new Je(e, r).container();
}
H.ext.feature.push({
  fnInit: lf,
  cFeature: "B"
});
H.feature && H.feature.register("buttons", lf);
/*! Bootstrap integration for DataTables' Buttons
 * © SpryMedia Ltd - datatables.net/license
 */
let Ac = lt;
Ac.extend(!0, H.Buttons.defaults, {
  dom: {
    container: {
      className: "dt-buttons btn-group flex-wrap"
    },
    button: {
      className: "btn btn-secondary",
      active: "active",
      dropHtml: "",
      dropClass: "dropdown-toggle"
    },
    collection: {
      container: {
        tag: "div",
        className: "dt-button-collection",
        content: {
          tag: "ul",
          className: "dropdown-menu show"
        }
      },
      closeButton: !1,
      button: {
        tag: "li",
        className: "dt-button",
        active: "dt-button-active-a",
        disabled: "disabled",
        liner: {
          tag: "a",
          className: "dropdown-item"
        },
        spacer: {
          className: "divider",
          tag: "li"
        }
      }
    },
    split: {
      action: {
        tag: "a",
        className: "btn btn-secondary dt-button-split-drop-button",
        closeButton: !1
      },
      dropdown: {
        tag: "button",
        className: "btn btn-secondary dt-button-split-drop dropdown-toggle-split",
        closeButton: !1,
        align: "split-left",
        splitAlignClass: "dt-button-split-left"
      },
      wrapper: {
        tag: "div",
        className: "dt-button-split btn-group",
        closeButton: !1
      }
    }
  },
  buttonCreated: function(t, n) {
    return t.buttons ? Ac('<div class="btn-group"/>').append(n) : n;
  }
});
H.ext.buttons.collection.rightAlignClassName = "dropdown-menu-right";
/*! Responsive 3.0.8
 * © SpryMedia Ltd - datatables.net/license
 */
let fe = lt;
var Kt = function(t, n) {
  if (!H.versionCheck || !H.versionCheck("2"))
    throw "DataTables Responsive requires DataTables 2 or newer";
  this.s = {
    childNodeStore: {},
    columns: [],
    current: [],
    dt: new H.Api(t)
  }, !this.s.dt.settings()[0].responsive && (n && typeof n.details == "string" ? n.details = { type: n.details } : n && n.details === !1 ? n.details = { type: !1 } : n && n.details === !0 && (n.details = { type: "inline" }), this.c = fe.extend(
    !0,
    {},
    Kt.defaults,
    H.defaults.responsive,
    n
  ), t.responsive = this, this._constructor());
};
fe.extend(Kt.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Initialise the Responsive instance
   *
   * @private
   */
  _constructor: function() {
    var t = this, n = this.s.dt, e = fe(window).innerWidth();
    n.settings()[0]._responsive = this, fe(window).on(
      "orientationchange.dtr",
      H.util.throttle(function() {
        var i = fe(window).innerWidth();
        i !== e && (t._resize(), e = i);
      })
    ), n.on("row-created.dtr", function(i, s, a, o) {
      fe.inArray(!1, t.s.current) !== -1 && fe(">td, >th", s).each(function(l) {
        var c = n.column.index("toData", l);
        t.s.current[c] === !1 && fe(this).css("display", "none").addClass("dtr-hidden");
      });
    }), n.on("destroy.dtr", function() {
      n.off(".dtr"), fe(n.table().body()).off(".dtr"), fe(window).off("resize.dtr orientationchange.dtr"), n.cells(".dtr-control").nodes().to$().removeClass("dtr-control"), fe(n.table().node()).removeClass("dtr-inline collapsed"), fe.each(t.s.current, function(i, s) {
        s === !1 && t._setColumnVis(i, !0);
      });
    }), this.c.breakpoints.sort(function(i, s) {
      return i.width < s.width ? 1 : i.width > s.width ? -1 : 0;
    }), this._classLogic();
    var r = this.c.details;
    r.type !== !1 && (t._detailsInit(), n.on("column-visibility.dtr", function() {
      t._timer && clearTimeout(t._timer), t._timer = setTimeout(function() {
        t._timer = null, t._classLogic(), t._resizeAuto(), t._resize(!0), t._redrawChildren();
      }, 100);
    }), n.on("draw.dtr", function() {
      t._redrawChildren();
    }), fe(n.table().node()).addClass("dtr-" + r.type)), n.on("column-calc.dt", function(i, s) {
      for (var a = t.s.current, o = 0; o < a.length; o++) {
        var l = s.visible.indexOf(o);
        a[o] === !1 && l >= 0 && s.visible.splice(l, 1);
      }
    }), n.on("preXhr.dtr", function() {
      var i = [];
      n.rows().every(function() {
        this.child.isShown() && i.push(this.id(!0));
      }), n.one("draw.dtr", function() {
        t._resizeAuto(), t._resize(), n.rows(i).every(function() {
          t._detailsDisplay(this, !1);
        });
      });
    }), n.on("draw.dtr", function() {
      n.page.info().serverSide && (t.s.childNodeStore = {}), t._controlClass();
    }).ready(function() {
      t._resizeAuto(), t._resize(), n.on("column-sizing.dtr", function() {
        t._resizeAuto(), t._resize();
      });
    }), n.on("column-reorder.dtr", function(i, s, a) {
      t._classLogic(), t._resizeAuto(), t._resize(!0);
    });
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Insert a `col` tag into the correct location in a `colgroup`.
   *
   * @param {jQuery} colGroup The `colgroup` tag
   * @param {jQuery} colEl The `col` tag
   */
  _colGroupAttach: function(t, n, e) {
    var r = null;
    if (n[e].get(0).parentNode !== t[0]) {
      for (var i = e + 1; i < n.length; i++)
        if (t[0] === n[i].get(0).parentNode) {
          r = i;
          break;
        }
      r !== null ? n[e].insertBefore(n[r][0]) : t.append(n[e]);
    }
  },
  /**
   * Get and store nodes from a cell - use for node moving renderers
   *
   * @param {*} dt DT instance
   * @param {*} row Row index
   * @param {*} col Column index
   */
  _childNodes: function(t, n, e) {
    var r = n + "-" + e;
    if (this.s.childNodeStore[r])
      return this.s.childNodeStore[r];
    for (var i = [], s = t.cell(n, e).node().childNodes, a = 0, o = s.length; a < o; a++)
      i.push(s[a]);
    return this.s.childNodeStore[r] = i, i;
  },
  /**
   * Restore nodes from the cache to a table cell
   *
   * @param {*} dt DT instance
   * @param {*} row Row index
   * @param {*} col Column index
   */
  _childNodesRestore: function(t, n, e) {
    var r = n + "-" + e;
    if (this.s.childNodeStore[r]) {
      var i = t.cell(n, e).node(), s = this.s.childNodeStore[r];
      if (s.length > 0) {
        for (var a = s[0].parentNode, o = a.childNodes, l = [], c = 0, f = o.length; c < f; c++)
          l.push(o[c]);
        for (var g = 0, b = l.length; g < b; g++)
          i.appendChild(l[g]);
      }
      this.s.childNodeStore[r] = void 0;
    }
  },
  /**
   * Calculate the visibility for the columns in a table for a given
   * breakpoint. The result is pre-determined based on the class logic if
   * class names are used to control all columns, but the width of the table
   * is also used if there are columns which are to be automatically shown
   * and hidden.
   *
   * @param  {string} breakpoint Breakpoint name to use for the calculation
   * @return {array} Array of boolean values initiating the visibility of each
   *   column.
   *  @private
   */
  _columnsVisibility: function(t) {
    var n = this.s.dt, e = this.s.columns, r, i, s = e.map(function(m, h) {
      return {
        columnIdx: h,
        priority: m.priority
      };
    }).sort(function(m, h) {
      return m.priority !== h.priority ? m.priority - h.priority : m.columnIdx - h.columnIdx;
    }), a = fe.map(e, function(m, h) {
      return n.column(h).visible() === !1 ? "not-visible" : m.auto && m.minWidth === null ? !1 : m.auto === !0 ? "-" : fe.inArray(t, m.includeIn) !== -1;
    }), o = 0;
    for (r = 0, i = a.length; r < i; r++)
      a[r] === !0 && (o += e[r].minWidth);
    var l = n.settings()[0].oScroll, c = l.sY || l.sX ? l.iBarWidth : 0, f = n.table().container().offsetWidth - c, g = f - o;
    for (r = 0, i = a.length; r < i; r++)
      e[r].control && (g -= e[r].minWidth);
    var b = !1;
    for (r = 0, i = s.length; r < i; r++) {
      var w = s[r].columnIdx;
      a[w] === "-" && !e[w].control && e[w].minWidth && (b || g - e[w].minWidth < 0 ? (b = !0, a[w] = !1) : a[w] = !0, g -= e[w].minWidth);
    }
    var C = !1;
    for (r = 0, i = e.length; r < i; r++)
      if (!e[r].control && !e[r].never && a[r] === !1) {
        C = !0;
        break;
      }
    for (r = 0, i = e.length; r < i; r++)
      e[r].control && (a[r] = C), a[r] === "not-visible" && (a[r] = !1);
    return fe.inArray(!0, a) === -1 && (a[0] = !0), a;
  },
  /**
   * Create the internal `columns` array with information about the columns
   * for the table. This includes determining which breakpoints the column
   * will appear in, based upon class names in the column, which makes up the
   * vast majority of this method.
   *
   * @private
   */
  _classLogic: function() {
    var t = this, n = this.c.breakpoints, e = this.s.dt, r = e.columns().eq(0).map(function(a) {
      var o = this.column(a), l = o.header().className, c = o.init().responsivePriority, f = o.header().getAttribute("data-priority");
      return c === void 0 && (c = f == null ? 1e4 : f * 1), {
        className: l,
        includeIn: [],
        auto: !1,
        control: !1,
        never: !!l.match(/\b(dtr\-)?never\b/),
        priority: c
      };
    }), i = function(a, o) {
      var l = r[a].includeIn;
      fe.inArray(o, l) === -1 && l.push(o);
    }, s = function(a, o, l, c) {
      var f, g, b;
      if (!l)
        r[a].includeIn.push(o);
      else if (l === "max-")
        for (f = t._find(o).width, g = 0, b = n.length; g < b; g++)
          n[g].width <= f && i(a, n[g].name);
      else if (l === "min-")
        for (f = t._find(o).width, g = 0, b = n.length; g < b; g++)
          n[g].width >= f && i(a, n[g].name);
      else if (l === "not-")
        for (g = 0, b = n.length; g < b; g++)
          n[g].name.indexOf(c) === -1 && i(a, n[g].name);
    };
    r.each(function(a, o) {
      for (var l = a.className.split(" "), c = !1, f = 0, g = l.length; f < g; f++) {
        var b = l[f].trim();
        if (b === "all" || b === "dtr-all") {
          c = !0, a.includeIn = fe.map(n, function(w) {
            return w.name;
          });
          return;
        } else if (b === "none" || b === "dtr-none" || a.never) {
          c = !0;
          return;
        } else if (b === "control" || b === "dtr-control") {
          c = !0, a.control = !0;
          return;
        }
        fe.each(n, function(w, C) {
          var m = C.name.split("-"), h = new RegExp(
            "(min\\-|max\\-|not\\-)?(" + m[0] + ")(\\-[_a-zA-Z0-9])?"
          ), u = b.match(h);
          u && (c = !0, u[2] === m[0] && u[3] === "-" + m[1] ? s(
            o,
            C.name,
            u[1],
            u[2] + u[3]
          ) : u[2] === m[0] && !u[3] && s(o, C.name, u[1], u[2]));
        });
      }
      c || (a.auto = !0);
    }), this.s.columns = r;
  },
  /**
   * Update the cells to show the correct control class / button
   * @private
   */
  _controlClass: function() {
    if (this.c.details.type === "inline") {
      var t = this.s.dt, n = this.s.current, e = fe.inArray(!0, n);
      t.cells(
        null,
        function(r) {
          return r !== e;
        },
        { page: "current" }
      ).nodes().to$().filter(".dtr-control").removeClass("dtr-control"), e >= 0 && t.cells(null, e, { page: "current" }).nodes().to$().addClass("dtr-control");
    }
    this._tabIndexes();
  },
  /**
   * Show the details for the child row
   *
   * @param  {DataTables.Api} row    API instance for the row
   * @param  {boolean}        update Update flag
   * @private
   */
  _detailsDisplay: function(t, n) {
    var e = this, r = this.s.dt, i = this.c.details, s = function(l) {
      fe(t.node()).toggleClass("dtr-expanded", l !== !1), fe(r.table().node()).triggerHandler("responsive-display.dt", [
        r,
        t,
        l,
        n
      ]);
    };
    if (i && i.type !== !1) {
      var a = typeof i.renderer == "string" ? Kt.renderer[i.renderer]() : i.renderer, o = i.display(
        t,
        n,
        function() {
          return a.call(
            e,
            r,
            t[0][0],
            e._detailsObj(t[0])
          );
        },
        function() {
          s(!1);
        }
      );
      typeof o == "boolean" && s(o);
    }
  },
  /**
   * Initialisation for the details handler
   *
   * @private
   */
  _detailsInit: function() {
    var t = this, n = this.s.dt, e = this.c.details;
    e.type === "inline" && (e.target = "td.dtr-control, th.dtr-control"), fe(n.table().body()).on("keyup.dtr", "td, th", function(s) {
      let a = document.activeElement.nodeName.toLowerCase();
      s.keyCode === 13 && fe(this).data("dtr-keyboard") && (a === "td" || a === "th") && fe(this).click();
    });
    var r = e.target, i = typeof r == "string" ? r : "td, th";
    (r !== void 0 || r !== null) && fe(n.table().body()).on(
      "click.dtr mousedown.dtr mouseup.dtr",
      i,
      function(s) {
        if (fe(n.table().node()).hasClass("collapsed") && fe.inArray(
          fe(this).closest("tr").get(0),
          n.rows().nodes().toArray()
        ) !== -1) {
          if (typeof r == "number") {
            var a = r < 0 ? n.columns().eq(0).length + r : r;
            if (n.cell(this).index().column !== a)
              return;
          }
          var o = n.row(fe(this).closest("tr"));
          s.type === "click" ? t._detailsDisplay(o, !1) : s.type === "mousedown" ? fe(this).css("outline", "none") : s.type === "mouseup" && fe(this).trigger("blur").css("outline", "");
        }
      }
    );
  },
  /**
   * Get the details to pass to a renderer for a row
   * @param  {int} rowIdx Row index
   * @private
   */
  _detailsObj: function(t) {
    var n = this, e = this.s.dt, r = [];
    let i = e.settings()[0];
    return fe.map(this.s.columns, function(s, a) {
      if (!(s.never || s.control)) {
        var o = i.aoColumns[a];
        return r[a] || (r[a] = e.column(a)), {
          className: o.sClass,
          columnIndex: a,
          data: i.fastData(t, a, n.c.orthogonal),
          hidden: r[a].visible() && !n.s.current[a],
          rowIndex: t,
          title: r[a].title()
        };
      }
    });
  },
  /**
   * Find a breakpoint object from a name
   *
   * @param  {string} name Breakpoint name to find
   * @return {object}      Breakpoint description object
   * @private
   */
  _find: function(t) {
    for (var n = this.c.breakpoints, e = 0, r = n.length; e < r; e++)
      if (n[e].name === t)
        return n[e];
  },
  /**
   * Re-create the contents of the child rows as the display has changed in
   * some way.
   *
   * @private
   */
  _redrawChildren: function() {
    var t = this, n = this.s.dt;
    n.rows({ page: "current" }).iterator("row", function(e, r) {
      t._detailsDisplay(n.row(r), !0);
    });
  },
  /**
   * Alter the table display for a resized viewport. This involves first
   * determining what breakpoint the window currently is in, getting the
   * column visibilities to apply and then setting them.
   *
   * @param  {boolean} forceRedraw Force a redraw
   * @private
   */
  _resize: function(t) {
    var n = this, e = this.s.dt, r = fe(window).innerWidth(), i = this.c.breakpoints, s = i[0].name, a = this.s.columns, o, l, c = this.s.current.slice();
    for (o = i.length - 1; o >= 0; o--)
      if (r <= i[o].width) {
        s = i[o].name;
        break;
      }
    var f = this._columnsVisibility(s);
    this.s.current = f;
    var g = !1;
    for (o = 0, l = a.length; o < l; o++)
      if (f[o] === !1 && !a[o].never && !a[o].control && e.column(o).visible()) {
        g = !0;
        break;
      }
    fe(e.table().node()).toggleClass("collapsed", g);
    var b = !1, w = 0, C = e.settings()[0], m = fe(e.table().node()).children("colgroup"), h = C.aoColumns.map(function(u) {
      return u.colEl;
    });
    e.columns().eq(0).each(function(u, p) {
      e.column(u).visible() && (f[p] === !0 && w++, (t || f[p] !== c[p]) && (b = !0, n._setColumnVis(u, f[p])), f[p] ? n._colGroupAttach(m, h, p) : h[p].detach());
    }), b && (e.columns.adjust(), this._redrawChildren(), fe(e.table().node()).trigger("responsive-resize.dt", [
      e,
      this._responsiveOnlyHidden()
    ]), e.page.info().recordsDisplay === 0 && fe("td", e.table().body()).eq(0).attr("colspan", w)), n._controlClass();
  },
  /**
   * Determine the width of each column in the table so the auto column hiding
   * has that information to work with. This method is never going to be 100%
   * perfect since column widths can change slightly per page, but without
   * seriously compromising performance this is quite effective.
   *
   * @private
   */
  _resizeAuto: function() {
    var t = this.s.dt, n = this.s.columns, e = this, r = t.columns().indexes().filter(function(g) {
      return t.column(g).visible();
    });
    if (this.c.auto && fe.inArray(
      !0,
      fe.map(n, function(g) {
        return g.auto;
      })
    ) !== -1) {
      var i = t.table().node().cloneNode(!1), s = fe(t.table().header().cloneNode(!1)).appendTo(
        i
      ), a = fe(t.table().footer().cloneNode(!1)).appendTo(
        i
      ), o = fe(t.table().body()).clone(!1, !1).empty().appendTo(i);
      i.style.width = "auto", t.table().header.structure(r).forEach((g) => {
        var b = g.filter(function(w) {
          return !!w;
        }).map(function(w) {
          return fe(w.cell).clone(!1).css("display", "table-cell").css("width", "auto").css("min-width", 0);
        });
        fe("<tr/>").append(b).appendTo(s);
      });
      for (var l = fe("<tr/>").appendTo(o), c = 0; c < r.count(); c++)
        l.append("<td/>");
      this.c.details.renderer._responsiveMovesNodes ? t.rows({ page: "current" }).every(function(g) {
        var b = this.node();
        if (b) {
          var w = b.cloneNode(!1);
          t.cells(g, r).every(function(C, m) {
            var h = e.s.childNodeStore[g + "-" + m];
            h ? fe(this.node().cloneNode(!1)).append(fe(h).clone()).appendTo(w) : fe(this.node()).clone(!1).appendTo(w);
          }), o.append(w);
        }
      }) : fe(o).append(fe(t.rows({ page: "current" }).nodes()).clone(!1)).find("th, td").css("display", ""), o.find("th, td").css("display", ""), t.table().footer.structure(r).forEach((g) => {
        var b = g.filter(function(w) {
          return !!w;
        }).map(function(w) {
          return fe(w.cell).clone(!1).css("display", "table-cell").css("width", "auto").css("min-width", 0);
        });
        fe("<tr/>").append(b).appendTo(a);
      }), this.c.details.type === "inline" && fe(i).addClass("dtr-inline collapsed"), fe(i).find("[name]").removeAttr("name"), fe(i).css("position", "relative");
      var f = fe("<div/>").css({
        width: 1,
        height: 1,
        overflow: "hidden",
        clear: "both"
      }).append(i);
      f.insertBefore(t.table().node()), l.children().each(function(g) {
        var b = t.column.index("fromVisible", g);
        n[b].minWidth = this.offsetWidth || 0;
      }), f.remove();
    }
  },
  /**
   * Get the state of the current hidden columns - controlled by Responsive only
   */
  _responsiveOnlyHidden: function() {
    var t = this.s.dt;
    return fe.map(this.s.current, function(n, e) {
      return t.column(e).visible() === !1 ? !0 : n;
    });
  },
  /**
   * Set a column's visibility.
   *
   * We don't use DataTables' column visibility controls in order to ensure
   * that column visibility can Responsive can no-exist. Since only IE8+ is
   * supported (and all evergreen browsers of course) the control of the
   * display attribute works well.
   *
   * @param {integer} col      Column index
   * @param {boolean} showHide Show or hide (true or false)
   * @private
   */
  _setColumnVis: function(t, n) {
    var e = this, r = this.s.dt, i = n ? "" : "none";
    this._setHeaderVis(t, n, r.table().header.structure()), this._setHeaderVis(t, n, r.table().footer.structure()), r.column(t).nodes().to$().css("display", i).toggleClass("dtr-hidden", !n), r.settings()[0].aoColumns[t].responsiveVisible = n, fe.isEmptyObject(this.s.childNodeStore) || r.cells(null, t).indexes().each(function(s) {
      e._childNodesRestore(r, s.row, s.column);
    });
  },
  /**
   * Set a column's visibility, taking into account multiple rows
   * in a header / footer and colspan attributes
   * @param {*} col
   * @param {*} showHide
   * @param {*} structure
   */
  _setHeaderVis: function(t, n, e) {
    var r = this, i = n ? "" : "none";
    e.forEach(function(s, a) {
      for (var o = 0; o < s.length; o++)
        if (s[o] && s[o].rowspan > 1)
          for (var l = s[o].rowspan, c = 1; c < l; c++)
            e[a + c][o] = {};
    }), e.forEach(function(s) {
      if (s[t] && s[t].cell)
        fe(s[t].cell).css("display", i).toggleClass("dtr-hidden", !n);
      else
        for (var a = t; a >= 0; ) {
          if (s[a] && s[a].cell) {
            s[a].cell.colSpan = r._colspan(s, a);
            break;
          }
          a--;
        }
    });
  },
  /**
   * How many columns should this cell span
   *
   * @param {*} row Header structure row
   * @param {*} idx The column index of the cell to span
   */
  _colspan: function(t, n) {
    for (var e = 1, r = n + 1; r < t.length; r++)
      if (t[r] === null && this.s.current[r])
        e++;
      else if (t[r])
        break;
    return e;
  },
  /**
   * Update the cell tab indexes for keyboard accessibility. This is called on
   * every table draw - that is potentially inefficient, but also the least
   * complex option given that column visibility can change on the fly. Its a
   * shame user-focus was removed from CSS 3 UI, as it would have solved this
   * issue with a single CSS statement.
   *
   * @private
   */
  _tabIndexes: function() {
    var t = this.s.dt, n = t.cells({ page: "current" }).nodes().to$(), e = t.settings()[0], r = this.c.details.target;
    if (n.filter("[data-dtr-keyboard]").removeData("[data-dtr-keyboard]"), typeof r == "number")
      t.cells(null, r, { page: "current" }).nodes().to$().attr("tabIndex", e.iTabIndex).data("dtr-keyboard", 1);
    else {
      r === "td:first-child, th:first-child" && (r = ">td:first-child, >th:first-child");
      var i = t.rows({ page: "current" }).nodes(), s = r === "tr" ? fe(i) : fe(r, i);
      s.attr("tabIndex", e.iTabIndex).data("dtr-keyboard", 1);
    }
  }
});
Kt.breakpoints = [
  { name: "desktop", width: 1 / 0 },
  { name: "tablet-l", width: 1024 },
  { name: "tablet-p", width: 768 },
  { name: "mobile-l", width: 480 },
  { name: "mobile-p", width: 320 }
];
Kt.display = {
  childRow: function(t, n, e) {
    var r = fe(t.node());
    if (n) {
      if (r.hasClass("dtr-expanded"))
        return t.child(e(), "child").show(), !0;
    } else {
      if (r.hasClass("dtr-expanded"))
        return t.child(!1), !1;
      var i = e();
      return i === !1 ? !1 : (t.child(i, "child").show(), !0);
    }
  },
  childRowImmediate: function(t, n, e) {
    var r = fe(t.node());
    if (!n && r.hasClass("dtr-expanded") || !t.responsive.hasHidden())
      return t.child(!1), !1;
    var i = e();
    return i === !1 ? !1 : (t.child(i, "child").show(), !0);
  },
  // This is a wrapper so the modal options for Bootstrap and jQuery UI can
  // have options passed into them. This specific one doesn't need to be a
  // function but it is for consistency in the `modal` name
  modal: function(t) {
    return function(n, e, r, i) {
      var s, a = r();
      if (a === !1)
        return !1;
      if (e)
        if (s = fe("div.dtr-modal-content"), s.length && n.index() === s.data("dtr-row-idx"))
          s.empty().append(a);
        else
          return null;
      else {
        var o = function() {
          s.remove(), fe(document).off("keypress.dtr"), fe(n.node()).removeClass("dtr-expanded"), i();
        };
        s = fe('<div class="dtr-modal"/>').append(
          fe('<div class="dtr-modal-display"/>').append(
            fe('<div class="dtr-modal-content"/>').data("dtr-row-idx", n.index()).append(a)
          ).append(
            fe(
              '<div class="dtr-modal-close">&times;</div>'
            ).click(function() {
              o();
            })
          )
        ).append(
          fe('<div class="dtr-modal-background"/>').click(
            function() {
              o();
            }
          )
        ).appendTo("body"), fe(n.node()).addClass("dtr-expanded"), fe(document).on("keyup.dtr", function(l) {
          l.keyCode === 27 && (l.stopPropagation(), o());
        });
      }
      return t && t.header && fe("div.dtr-modal-content").prepend(
        "<h2>" + t.header(n) + "</h2>"
      ), !0;
    };
  }
};
Kt.renderer = {
  listHiddenNodes: function() {
    var t = function(n, e, r) {
      var i = this, s = fe(
        '<ul data-dtr-index="' + e + '" class="dtr-details"/>'
      ), a = !1;
      return fe.each(r, function(o, l) {
        if (l.hidden) {
          var c = l.className ? 'class="' + l.className + '"' : "";
          fe(
            "<li " + c + ' data-dtr-index="' + l.columnIndex + '" data-dt-row="' + l.rowIndex + '" data-dt-column="' + l.columnIndex + '"><span class="dtr-title">' + l.title + "</span> </li>"
          ).append(
            fe('<span class="dtr-data"/>').append(
              i._childNodes(
                n,
                l.rowIndex,
                l.columnIndex
              )
            )
          ).appendTo(s), a = !0;
        }
      }), a ? s : !1;
    };
    return t._responsiveMovesNodes = !0, t;
  },
  listHidden: function() {
    return function(t, n, e) {
      var r = fe.map(e, function(i) {
        var s = i.className ? 'class="' + i.className + '"' : "";
        return i.hidden ? "<li " + s + ' data-dtr-index="' + i.columnIndex + '" data-dt-row="' + i.rowIndex + '" data-dt-column="' + i.columnIndex + '"><span class="dtr-title">' + i.title + '</span> <span class="dtr-data">' + i.data + "</span></li>" : "";
      }).join("");
      return r ? fe(
        '<ul data-dtr-index="' + n + '" class="dtr-details"/>'
      ).append(r) : !1;
    };
  },
  tableAll: function(t) {
    return t = fe.extend(
      {
        tableClass: ""
      },
      t
    ), function(n, e, r) {
      var i = fe.map(r, function(s) {
        var a = s.className ? 'class="' + s.className + '"' : "";
        return "<tr " + a + ' data-dt-row="' + s.rowIndex + '" data-dt-column="' + s.columnIndex + '"><td>' + (s.title !== "" ? s.title + ":" : "") + "</td> <td>" + s.data + "</td></tr>";
      }).join("");
      return fe(
        '<table class="' + t.tableClass + ' dtr-details" width="100%"/>'
      ).append(i);
    };
  }
};
Kt.defaults = {
  /**
   * List of breakpoints for the instance. Note that this means that each
   * instance can have its own breakpoints. Additionally, the breakpoints
   * cannot be changed once an instance has been creased.
   *
   * @type {Array}
   * @default Takes the value of `Responsive.breakpoints`
   */
  breakpoints: Kt.breakpoints,
  /**
   * Enable / disable auto hiding calculations. It can help to increase
   * performance slightly if you disable this option, but all columns would
   * need to have breakpoint classes assigned to them
   *
   * @type {Boolean}
   * @default  `true`
   */
  auto: !0,
  /**
   * Details control. If given as a string value, the `type` property of the
   * default object is set to that value, and the defaults used for the rest
   * of the object - this is for ease of implementation.
   *
   * The object consists of the following properties:
   *
   * * `display` - A function that is used to show and hide the hidden details
   * * `renderer` - function that is called for display of the child row data.
   *   The default function will show the data from the hidden columns
   * * `target` - Used as the selector for what objects to attach the child
   *   open / close to
   * * `type` - `false` to disable the details display, `inline` or `column`
   *   for the two control types
   *
   * @type {Object|string}
   */
  details: {
    display: Kt.display.childRow,
    renderer: Kt.renderer.listHidden(),
    target: 0,
    type: "inline"
  },
  /**
   * Orthogonal data request option. This is used to define the data type
   * requested when Responsive gets the data to show in the child row.
   *
   * @type {String}
   */
  orthogonal: "display"
};
var Ri = fe.fn.dataTable.Api;
Ri.register("responsive()", function() {
  return this;
});
Ri.register("responsive.index()", function(t) {
  return t = fe(t), {
    column: t.data("dtr-index"),
    row: t.parent().data("dtr-index")
  };
});
Ri.register("responsive.rebuild()", function() {
  return this.iterator("table", function(t) {
    t._responsive && t._responsive._classLogic();
  });
});
Ri.register("responsive.recalc()", function() {
  return this.iterator("table", function(t) {
    t._responsive && (t._responsive._resizeAuto(), t._responsive._resize());
  });
});
Ri.register("responsive.hasHidden()", function() {
  var t = this.context[0];
  return t._responsive ? fe.inArray(!1, t._responsive._responsiveOnlyHidden()) !== -1 : !1;
});
Ri.registerPlural(
  "columns().responsiveHidden()",
  "column().responsiveHidden()",
  function() {
    return this.iterator(
      "column",
      function(t, n) {
        return t._responsive ? t._responsive._responsiveOnlyHidden()[n] : !1;
      },
      1
    );
  }
);
Kt.version = "3.0.8";
fe.fn.dataTable.Responsive = Kt;
fe.fn.DataTable.Responsive = Kt;
fe(document).on("preInit.dt.dtr", function(t, n, e) {
  if (t.namespace === "dt" && (fe(n.nTable).hasClass("responsive") || fe(n.nTable).hasClass("dt-responsive") || n.oInit.responsive || H.defaults.responsive)) {
    var r = n.oInit.responsive;
    r !== !1 && new Kt(n, fe.isPlainObject(r) ? r : {});
  }
});
/*! Bootstrap 5 integration for DataTables' Responsive
 * © SpryMedia Ltd - datatables.net/license
 */
let uf = lt;
var cf = H.Responsive.display, Lg = cf.modal, wr = uf(
  '<div class="modal fade dtr-bs-modal" role="dialog"><div class="modal-dialog" role="document"><div class="modal-content"><div class="modal-header"><button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button></div><div class="modal-body"/></div></div></div>'
), Qs, Aa = window.bootstrap;
H.Responsive.bootstrap = function(t) {
  Aa = t;
};
function Mg() {
  let t = H.use("bootstrap");
  if (t)
    return t;
  if (Aa)
    return Aa;
  throw new Error("No Bootstrap library. Set it with `DataTable.use(bootstrap);`");
}
cf.modal = function(t) {
  if (!Qs && Aa.Modal) {
    let n = Mg();
    Qs = new n.Modal(wr[0]);
  }
  return function(n, e, r, i) {
    if (Qs) {
      var s = r();
      if (s === !1)
        return !1;
      if (e)
        if (uf.contains(document, wr[0]) && n.index() === wr.data("dtr-row-idx"))
          wr.find("div.modal-body").empty().append(s);
        else
          return null;
      else {
        if (t && t.header) {
          var a = wr.find("div.modal-header"), o = a.find("button").detach();
          a.empty().append('<h4 class="modal-title">' + t.header(n) + "</h4>").append(o);
        }
        wr.find("div.modal-body").empty().append(s), wr.data("dtr-row-idx", n.index()).one("hidden.bs.modal", i).appendTo("body"), Qs.show();
      }
      return !0;
    } else
      return Lg(n, e, r, i);
  };
};
/*! Select for DataTables 3.1.3
 * © SpryMedia Ltd - datatables.net/license/mit
 */
let Te = lt;
H.select = {};
H.select.classes = {
  checkbox: "dt-select-checkbox"
};
H.select.version = "3.1.3";
H.select.init = function(t) {
  var n = t.settings()[0];
  if (!H.versionCheck("2"))
    throw "Warning: Select requires DataTables 2 or newer";
  if (!n._select) {
    var e = t.state.loaded(), r = function(v, S, T) {
      if (!(T === null || T.select === void 0)) {
        if (t.rows({ selected: !0 }).any() && t.rows().deselect(), T.select.rows !== void 0 && t.rows(T.select.rows).select(), t.columns({ selected: !0 }).any() && t.columns().deselect(), T.select.columns !== void 0 && t.columns(T.select.columns).select(), t.cells({ selected: !0 }).any() && t.cells().deselect(), T.select.cells !== void 0)
          for (var A = 0; A < T.select.cells.length; A++)
            t.cell(T.select.cells[A].row, T.select.cells[A].column).select();
        t.state.save();
      }
    };
    t.on("stateSaveParams", function(v, S, T) {
      T.select = {}, T.select.rows = t.rows({ selected: !0 }).ids(!0).toArray(), T.select.columns = t.columns({ selected: !0 })[0], T.select.cells = t.cells({ selected: !0 })[0].map(function(A) {
        return { row: t.row(A.row).id(!0), column: A.column };
      });
    }).on("stateLoadParams", r).one("init", function() {
      r(void 0, void 0, e);
    });
    var i = n.oInit.select, s = H.defaults.select, a = i === void 0 ? s : i, o = "row", l = "api", c = !1, f = !0, g = null, b = !0, w = "td, th", C = "selected", m = !0, h = !1, u = !1, p = !1;
    n._select = {
      infoEls: []
    }, a === !0 ? (l = "os", h = !0) : typeof a == "string" ? (l = a, h = !0) : Te.isPlainObject(a) && (a.blurable !== void 0 && (c = a.blurable), a.toggleable !== void 0 && (f = a.toggleable), a.info !== void 0 && (b = a.info), a.items !== void 0 && (o = a.items), a.style !== void 0 ? (l = a.style, h = !0) : (l = "os", h = !0), a.selector !== void 0 && (w = a.selector), a.className !== void 0 && (C = a.className), a.headerCheckbox !== void 0 && (m = a.headerCheckbox), a.selectable !== void 0 && (g = a.selectable), a.keys !== void 0 && (u = a.keys), a.keysWrap !== void 0 && (p = a.keysWrap)), t.select.selector(w), t.select.items(o), t.select.style(l), t.select.blurable(c), t.select.toggleable(f), t.select.info(b), t.select.keys(u, p), t.select.selectable(g), n._select.className = C, !h && Te(t.table().node()).hasClass("selectable") && t.select.style("os"), (m || m === "select-page" || m === "select-all") && t.ready(function() {
      Fg(t, m);
    });
  }
};
function kc(t, n, e) {
  var r, i, s, a = function(l, c) {
    if (l > c) {
      var f = c;
      c = l, l = f;
    }
    var g = !1;
    return t.columns(":visible").indexes().filter(function(b) {
      return b === l && (g = !0), b === c ? (g = !1, !0) : g;
    });
  }, o = function(l, c) {
    var f = t.rows({ search: "applied" }).indexes();
    if (f.indexOf(l) > f.indexOf(c)) {
      var g = c;
      c = l, l = g;
    }
    var b = !1;
    return f.filter(function(w) {
      return w === l && (b = !0), w === c ? (b = !1, !0) : b;
    });
  };
  !t.cells({ selected: !0 }).any() && !e ? (i = a(0, n.column), s = o(0, n.row)) : (i = a(e.column, n.column), s = o(e.row, n.row)), r = t.cells(s, i).flatten(), t.cells(n, { selected: !0 }).any() ? t.cells(r).deselect() : t.cells(r).select();
}
function Si(t) {
  var n = H.select.classes.checkbox;
  return t ? n.replace(/ /g, ".") : n;
}
function bi(t) {
  var n = t.settings()[0], e = n._select.selector;
  Te(t.table().container()).off("mousedown.dtSelect", e).off("mouseup.dtSelect", e).off("click.dtSelect", e), Te("body").off("click.dtSelect" + tu(t.table().node()));
}
function df(t) {
  var n = Te(t.table().container()), e = t.settings()[0], r = e._select.selector, i;
  n.on("mousedown.dtSelect", r, function(s) {
    (s.shiftKey || s.metaKey || s.ctrlKey) && n.css("-moz-user-select", "none").one("selectstart.dtSelect", r, function() {
      return !1;
    }), window.getSelection && (i = window.getSelection());
  }).on("mouseup.dtSelect", r, function() {
    n.css("-moz-user-select", "");
  }).on("click.dtSelect", r, function(s) {
    var a = t.select.items(), o;
    if (i) {
      var l = window.getSelection();
      if ((!l.anchorNode || Te(l.anchorNode).closest("table")[0] === t.table().node()) && l !== i)
        return;
    }
    var c = t.settings()[0], f = t.table().container();
    if (Te(s.target).closest("div.dt-container")[0] == f) {
      var g = t.cell(Te(s.target).closest("td, th"));
      if (g.any()) {
        var b = Te.Event("user-select.dt");
        if (Rn(t, b, [a, g, s]), !b.isDefaultPrevented()) {
          var w = g.index();
          a === "row" ? (o = w.row, Lo(s, t, c, "row", o)) : a === "column" ? (o = g.index().column, Lo(s, t, c, "column", o)) : a === "cell" && (o = g.index(), Lo(s, t, c, "cell", o)), c._select_lastCell = w;
        }
      }
    }
  }), Te("body").on("click.dtSelect" + tu(t.table().node()), function(s) {
    if (e._select.blurable) {
      if (Te(s.target).parents().filter(t.table().container()).length || Te(s.target).parents("html").length === 0 || Te(s.target).parents("div.DTE").length)
        return;
      var a = Te.Event("select-blur.dt");
      if (Rn(t, a, [s.target, s]), a.isDefaultPrevented())
        return;
      _s(e, !0);
    }
  });
}
function Rn(t, n, e, r) {
  r && !t.flatten().length || (typeof n == "string" && (n = n + ".dt"), e.unshift(t), Te(t.table().node()).trigger(n, e));
}
function Xa(t) {
  return t.mRender && t.mRender._name === "selectCheckbox";
}
function Pc(t, n) {
  if (!(t.select.style() === "api" || t.select.info() === !1)) {
    var e = t.settings()[0], r = e._select_set;
    if (!t.page.info().serverSide)
      for (var i = r.length - 1; i >= 0; i--)
        e.aIds[r[i]] || r.splice(i, 1);
    var s = r.length ? r.length : t.rows({ selected: !0 }).count(), a = t.columns({ selected: !0 }).count(), o = t.cells({ selected: !0 }).count();
    e._select_mode === "subtractive" && (s = t.page.info().recordsDisplay - r.length);
    var l = function(b, w, C) {
      b.append(
        Te('<span class="select-item"/>').append(
          t.i18n(
            "select." + w + "s",
            { _: "%d " + w + "s selected", 0: "", 1: "1 " + w + " selected" },
            C
          )
        )
      );
    }, c = Te(n), f = Te('<span class="select-info"/>');
    l(f, "row", s), l(f, "column", a), l(f, "cell", o);
    var g = c.children("span.select-info");
    g.length && g.remove(), f.text() !== "" && c.append(f);
  }
}
function Fg(t, n) {
  var e = t.settings()[0], r = e.aoColumns;
  t.columns().iterator("column", function(i, s) {
    var a = r[s];
    if (Xa(a)) {
      var o = t.column(s).header(), l = Te("div.dt-column-header", o);
      if (l.length && (o = l), !Te("input", o).length) {
        var c = Te("<input>").attr({
          class: Si(!1),
          type: "checkbox",
          "aria-label": t.i18n("select.aria.headerCheckbox") || "Select all rows"
        }).appendTo(o).on("change", function() {
          this.checked ? n == "select-page" ? t.rows({ page: "current" }).select() : t.rows({ search: "applied" }).select() : n == "select-page" ? t.rows({ page: "current", selected: !0 }).deselect() : t.rows({ selected: !0 }).deselect();
        }).on("click", function(f) {
          f.stopPropagation();
        });
        t.on("draw select deselect", function(f, g, b) {
          if (b === "row" || !b) {
            var w = Hg(t, n);
            w.search && w.search <= w.count && w.search === w.available ? c.prop("checked", !0).prop("indeterminate", !1) : w.search === 0 && w.count === 0 ? c.prop("checked", !1).prop("indeterminate", !1) : c.prop("checked", !1).prop("indeterminate", !0);
          }
        });
      }
    }
  });
}
function Bg(t) {
  var n = t.settings()[0], e = n._select.keys, r = n._select.keysWrap, i = "dts-keys-" + n.sTableId;
  e ? (Te(t.rows({ page: "current" }).nodes()).attr("tabindex", 0), t.on("draw." + i, function() {
    Te(t.rows({ page: "current" }).nodes()).attr("tabindex", 0);
  }), Te(document).on("keydown." + i, function(s) {
    var a = s.keyCode, o = document.activeElement;
    if ([9, 13, 32, 38, 40].includes(a)) {
      var l = t.rows({ page: "current" }).nodes().toArray(), c = l.indexOf(o), f = !0, g = t.page.info();
      if (c !== -1) {
        if (a === 9)
          s.shift === !1 && c === l.length - 1 ? si(t, "next", ":first-child") : s.shift === !0 && c === 0 ? si(t, "previous", ":last-child") : f = !1;
        else if (a === 13 || a === 32) {
          var b = t.row(o);
          b.selected() ? b.deselect() : b.select();
        } else
          a === 38 ? c > 0 ? l[c - 1].focus() : g.start > 0 ? si(t, "previous", ":last-child") : r && si(t, "last", ":last-child") : c < l.length - 1 ? l[c + 1].focus() : g.page < g.pages - 1 ? si(t, "next", ":first-child") : r && si(t, "first", ":first-child");
        f && (s.stopPropagation(), s.preventDefault());
      }
    }
  })) : (Te(t.rows().nodes()).removeAttr("tabindex"), t.off("draw." + i), Te(document).off("keydown." + i));
}
function si(t, n, e) {
  t.one("draw", function() {
    t.row(e).node().focus();
  }).page(n).draw(!1);
}
function Hg(t, n) {
  var e = t.settings()[0], r = e._select.selectable, i = 0, s = n == "select-page" ? t.rows({ page: "current", selected: !0 }).count() : t.rows({ selected: !0 }).count(), a = n == "select-page" ? t.rows({ page: "current", selected: !0 }).count() : t.rows({ search: "applied", selected: !0 }).count();
  if (!r)
    i = n == "select-page" ? t.rows({ page: "current" }).count() : t.rows({ search: "applied" }).count();
  else
    for (var o = n == "select-page" ? t.rows({ page: "current" }).indexes() : t.rows({ search: "applied" }).indexes(), l = 0; l < o.length; l++) {
      var c = e.aoData[o[l]], f = r(c._aData, c.nTr, o[l]);
      f && i++;
    }
  return {
    available: i,
    count: s,
    search: a
  };
}
function $g(t) {
  var n = new H.Api(t);
  t._select_init = !0, t._select_mode = "additive", t._select_set = [], t.aoRowCreatedCallback.push(
    function(e, r, i) {
      var s, a, o = t.aoData[i], l = n.row(i).id();
      for ((o._select_selected || t._select_mode === "additive" && t._select_set.includes(l) || t._select_mode === "subtractive" && !t._select_set.includes(l)) && (o._select_selected = !0, Te(e).addClass(t._select.className).find("input." + Si(!0)).prop("checked", !0)), s = 0, a = t.aoColumns.length; s < a; s++)
        (t.aoColumns[s]._select_selected || o._selected_cells && o._selected_cells[s]) && Te(o.anCells[s]).addClass(t._select.className);
    }
  ), Vg(n), n.on("info.dt", function(e, r, i) {
    r._select.infoEls.includes(i) || r._select.infoEls.push(i), Pc(n, i);
  }), n.on("select.dtSelect.dt deselect.dtSelect.dt", function() {
    t._select.infoEls.forEach(function(e) {
      Pc(n, e);
    }), n.state.save();
  }), n.on("destroy.dtSelect", function() {
    Te(n.rows({ selected: !0 }).nodes()).removeClass(n.settings()[0]._select.className), Te("input." + Si(!0), n.table().header()).remove(), bi(n), n.off(".dtSelect"), Te("body").off(".dtSelect" + tu(n.table().node()));
  });
}
function Oc(t, n, e, r) {
  var i = t[n + "s"]({ search: "applied" }).indexes(), s = i.indexOf(r), a = i.indexOf(e);
  if (!t[n + "s"]({ selected: !0 }).any() && s === -1)
    i.splice(i.indexOf(e) + 1, i.length);
  else {
    if (s > a) {
      var o = a;
      a = s, s = o;
    }
    i.splice(a + 1, i.length), i.splice(0, s);
  }
  t[n](e, { selected: !0 }).any() ? (i.splice(i.indexOf(e), 1), t[n + "s"](i).deselect()) : t[n + "s"](i).select();
}
function _s(t, n) {
  if (n || t._select.style === "single") {
    var e = new H.Api(t);
    e.rows({ selected: !0 }).deselect(), e.columns({ selected: !0 }).deselect(), e.cells({ selected: !0 }).deselect();
  }
}
function Lo(t, n, e, r, i) {
  var s = n.select.style(), a = n.select.toggleable(), o = n[r](i, { selected: !0 }).any();
  if (!(o && !a))
    if (s === "os")
      if (t.ctrlKey || t.metaKey)
        n[r](i).select(!o);
      else if (t.shiftKey)
        r === "cell" ? kc(n, i, e._select_lastCell || null) : Oc(
          n,
          r,
          i,
          e._select_lastCell ? e._select_lastCell[r] : null
        );
      else {
        var l = n[r + "s"]({ selected: !0 });
        o && l.flatten().length === 1 ? n[r](i).deselect() : (l.deselect(), n[r](i).select());
      }
    else
      s == "multi+shift" && t.shiftKey ? r === "cell" ? kc(n, i, e._select_lastCell || null) : Oc(
        n,
        r,
        i,
        e._select_lastCell ? e._select_lastCell[r] : null
      ) : n[r](i).select(!o);
}
function tu(t) {
  return t.id.replace(/[^a-zA-Z0-9\-\_]/g, "-");
}
function Vg(t) {
  t.on("select", function(n, e, r, i) {
    if (r === "row") {
      var s = t.settings()[0];
      s._select_mode === "additive" ? Ic(t, s._select_set, i) : Nc(t, s._select_set, i);
    }
  }), t.on("deselect", function(n, e, r, i) {
    if (r === "row") {
      var s = t.settings()[0];
      s._select_mode === "additive" ? Nc(t, s._select_set, i) : Ic(t, s._select_set, i);
    }
  });
}
function Ic(t, n, e) {
  for (var r = 0; r < e.length; r++) {
    var i = t.row(e[r]).id();
    i && i !== "undefined" && !n.includes(i) && n.push(i);
  }
}
function Nc(t, n, e) {
  for (var r = 0; r < e.length; r++) {
    var i = t.row(e[r]).id(), s = n.indexOf(i);
    s !== -1 && n.splice(s, 1);
  }
}
Te.each(
  [
    { type: "row", prop: "aoData" },
    { type: "column", prop: "aoColumns" }
  ],
  function(t, n) {
    H.ext.selector[n.type].push(function(e, r, i) {
      var s = r.selected, a, o = [];
      if (s !== !0 && s !== !1)
        return i;
      for (var l = 0, c = i.length; l < c; l++)
        a = e[n.prop][i[l]], a && (s === !0 && a._select_selected === !0 || s === !1 && !a._select_selected) && o.push(i[l]);
      return o;
    });
  }
);
H.ext.selector.cell.push(function(t, n, e) {
  var r = n.selected, i, s = [];
  if (r === void 0)
    return e;
  for (var a = 0, o = e.length; a < o; a++)
    i = t.aoData[e[a].row], i && (r === !0 && i._selected_cells && i._selected_cells[e[a].column] === !0 || r === !1 && (!i._selected_cells || !i._selected_cells[e[a].column])) && s.push(e[a]);
  return s;
});
var Pt = H.Api.register, Li = H.Api.registerPlural;
Pt("select()", function() {
  return this.iterator("table", function(t) {
    H.select.init(new H.Api(t));
  });
});
Pt("select.blurable()", function(t) {
  return t === void 0 ? this.context[0]._select.blurable : this.iterator("table", function(n) {
    n._select.blurable = t;
  });
});
Pt("select.toggleable()", function(t) {
  return t === void 0 ? this.context[0]._select.toggleable : this.iterator("table", function(n) {
    n._select.toggleable = t;
  });
});
Pt("select.info()", function(t) {
  return t === void 0 ? this.context[0]._select.info : this.iterator("table", function(n) {
    n._select.info = t;
  });
});
Pt("select.items()", function(t) {
  return t === void 0 ? this.context[0]._select.items : this.iterator("table", function(n) {
    n._select.items = t, Rn(new H.Api(n), "selectItems", [t]);
  });
});
Pt("select.keys()", function(t, n) {
  return t === void 0 ? this.context[0]._select.keys : this.iterator("table", function(e) {
    e._select || H.select.init(new H.Api(e)), e._select.keys = t, e._select.keysWrap = n, Bg(new H.Api(e));
  });
});
Pt("select.style()", function(t) {
  return t === void 0 ? this.context[0]._select.style : this.iterator("table", function(n) {
    n._select || H.select.init(new H.Api(n)), n._select_init || $g(n), n._select.style = t;
    var e = new H.Api(n);
    t !== "api" ? e.ready(function() {
      bi(e), df(e);
    }) : bi(e), Rn(new H.Api(n), "selectStyle", [t]);
  });
});
Pt("select.selector()", function(t) {
  return t === void 0 ? this.context[0]._select.selector : this.iterator("table", function(n) {
    var e = new H.Api(n), r = n._select.style;
    bi(e), n._select.selector = t, r && r !== "api" ? e.ready(function() {
      bi(e), df(e);
    }) : bi(e);
  });
});
Pt("select.selectable()", function(t) {
  let n = this.context[0];
  return t ? (n._select.selectable = t, this) : n._select.selectable;
});
Pt("select.last()", function(t) {
  let n = this.context[0];
  return t ? (n._select_lastCell = t, this) : n._select_lastCell;
});
Pt("select.cumulative()", function(t) {
  if (t)
    return this.iterator("table", function(e) {
      if (e._select_mode !== t) {
        var r = new H.Api(e);
        if (t === "subtractive") {
          var i = r.rows({ selected: !1 }).ids().toArray();
          e._select_mode = t, e._select_set.length = 0, e._select_set.push.apply(e._select_set, i);
        } else {
          var s = r.rows({ selected: !0 }).ids().toArray();
          e._select_mode = t, e._select_set.length = 0, e._select_set.push.apply(e._select_set, s);
        }
      }
    }).draw(!1);
  let n = this.context[0];
  return n && n._select_set ? {
    mode: n._select_mode,
    rows: n._select_set
  } : null;
});
Li("rows().select()", "row().select()", function(t) {
  var n = this, e = [];
  return t === !1 ? this.deselect() : (this.iterator("row", function(r, i) {
    _s(r);
    var s = r.aoData[i], a = r.aoColumns;
    if (r._select.selectable) {
      var o = r._select.selectable(s._aData, s.nTr, i);
      if (o === !1)
        return;
    }
    Te(s.nTr).addClass(r._select.className), s._select_selected = !0, e.push(i);
    for (var l = 0; l < a.length; l++) {
      var c = a[l];
      if (c.sType === null && n.columns().types(), Xa(c)) {
        var f = s.anCells;
        f && f[l] && Te("input." + Si(!0), f[l]).prop("checked", !0), s._aSortData !== null && (s._aSortData[l] = null);
      }
    }
  }), this.iterator("table", function(r) {
    Rn(n, "select", ["row", e], !0);
  }), this);
});
Pt("row().selected()", function() {
  var t = this.context[0];
  return !!(t && this.length && t.aoData[this[0]] && t.aoData[this[0]]._select_selected);
});
Pt("row().focus()", function() {
  var t = this.context[0];
  t && this.length && t.aoData[this[0]] && t.aoData[this[0]].nTr && t.aoData[this[0]].nTr.focus();
});
Pt("row().blur()", function() {
  var t = this.context[0];
  t && this.length && t.aoData[this[0]] && t.aoData[this[0]].nTr && t.aoData[this[0]].nTr.blur();
});
Li("columns().select()", "column().select()", function(t) {
  var n = this;
  return t === !1 ? this.deselect() : (this.iterator("column", function(e, r) {
    _s(e), e.aoColumns[r]._select_selected = !0;
    var i = new H.Api(e).column(r);
    Te(i.header()).addClass(e._select.className), Te(i.footer()).addClass(e._select.className), i.nodes().to$().addClass(e._select.className);
  }), this.iterator("table", function(e, r) {
    Rn(n, "select", ["column", n[r]], !0);
  }), this);
});
Pt("column().selected()", function() {
  var t = this.context[0];
  return !!(t && this.length && t.aoColumns[this[0]] && t.aoColumns[this[0]]._select_selected);
});
Li("cells().select()", "cell().select()", function(t) {
  var n = this;
  return t === !1 ? this.deselect() : (this.iterator("cell", function(e, r, i) {
    _s(e);
    var s = e.aoData[r];
    s._selected_cells === void 0 && (s._selected_cells = []), s._selected_cells[i] = !0, s.anCells && Te(s.anCells[i]).addClass(e._select.className);
  }), this.iterator("table", function(e, r) {
    Rn(n, "select", ["cell", n.cells(n[r]).indexes().toArray()], !0);
  }), this);
});
Pt("cell().selected()", function() {
  var t = this.context[0];
  if (t && this.length) {
    var n = t.aoData[this[0][0].row];
    if (n && n._selected_cells && n._selected_cells[this[0][0].column])
      return !0;
  }
  return !1;
});
Li("rows().deselect()", "row().deselect()", function() {
  var t = this;
  return this.iterator("row", function(n, e) {
    var r = n.aoData[e], i = n.aoColumns;
    Te(r.nTr).removeClass(n._select.className), r._select_selected = !1, n._select_lastCell = null;
    for (var s = 0; s < i.length; s++) {
      var a = i[s];
      if (a.sType === null && t.columns().types(), Xa(a)) {
        var o = r.anCells;
        o && o[s] && Te("input." + Si(!0), r.anCells[s]).prop("checked", !1), r._aSortData !== null && (r._aSortData[s] = null);
      }
    }
  }), this.iterator("table", function(n, e) {
    Rn(t, "deselect", ["row", t[e]], !0);
  }), this;
});
Li("columns().deselect()", "column().deselect()", function() {
  var t = this;
  return this.iterator("column", function(n, e) {
    n.aoColumns[e]._select_selected = !1;
    var r = new H.Api(n), i = r.column(e);
    Te(i.header()).removeClass(n._select.className), Te(i.footer()).removeClass(n._select.className), r.cells(null, e).indexes().each(function(s) {
      var a = n.aoData[s.row], o = a._selected_cells;
      a.anCells && (!o || !o[s.column]) && Te(a.anCells[s.column]).removeClass(n._select.className);
    });
  }), this.iterator("table", function(n, e) {
    Rn(t, "deselect", ["column", t[e]], !0);
  }), this;
});
Li("cells().deselect()", "cell().deselect()", function() {
  var t = this;
  return this.iterator("cell", function(n, e, r) {
    var i = n.aoData[e];
    i._selected_cells !== void 0 && (i._selected_cells[r] = !1), i.anCells && !n.aoColumns[r]._select_selected && Te(i.anCells[r]).removeClass(n._select.className);
  }), this.iterator("table", function(n, e) {
    Rn(t, "deselect", ["cell", t[e]], !0);
  }), this;
});
function ci(t, n) {
  return function(e) {
    return e.i18n("buttons." + t, n);
  };
}
function Mo(t) {
  var n = t._eventNamespace;
  return "draw.dt.DT" + n + " select.dt.DT" + n + " deselect.dt.DT" + n;
}
function Wg(t, n) {
  return !!(n.limitTo.indexOf("rows") !== -1 && t.rows({ selected: !0 }).any() || n.limitTo.indexOf("columns") !== -1 && t.columns({ selected: !0 }).any() || n.limitTo.indexOf("cells") !== -1 && t.cells({ selected: !0 }).any());
}
var Fo = 0;
Te.extend(H.ext.buttons, {
  selected: {
    text: ci("selected", "Selected"),
    className: "buttons-selected",
    limitTo: ["rows", "columns", "cells"],
    init: function(t, n, e) {
      var r = this;
      e._eventNamespace = ".select" + Fo++, t.on(Mo(e), function() {
        r.enable(Wg(t, e));
      }), this.disable();
    },
    destroy: function(t, n, e) {
      t.off(e._eventNamespace);
    }
  },
  selectedSingle: {
    text: ci("selectedSingle", "Selected single"),
    className: "buttons-selected-single",
    init: function(t, n, e) {
      var r = this;
      e._eventNamespace = ".select" + Fo++, t.on(Mo(e), function() {
        var i = t.rows({ selected: !0 }).flatten().length + t.columns({ selected: !0 }).flatten().length + t.cells({ selected: !0 }).flatten().length;
        r.enable(i === 1);
      }), this.disable();
    },
    destroy: function(t, n, e) {
      t.off(e._eventNamespace);
    }
  },
  selectAll: {
    text: ci("selectAll", "Select all"),
    className: "buttons-select-all",
    action: function(t, n, e, r) {
      var i = this.select.items(), s = r.selectorModifier;
      s ? (typeof s == "function" && (s = s.call(n, t, n, e, r)), this[i + "s"](s).select()) : this[i + "s"]().select();
    }
    // selectorModifier can be specified
  },
  selectNone: {
    text: ci("selectNone", "Deselect all"),
    className: "buttons-select-none",
    action: function() {
      _s(this.settings()[0], !0);
    },
    init: function(t, n, e) {
      var r = this;
      e._eventNamespace = ".select" + Fo++, t.on(Mo(e), function() {
        var i = t.rows({ selected: !0 }).flatten().length + t.columns({ selected: !0 }).flatten().length + t.cells({ selected: !0 }).flatten().length;
        r.enable(i > 0);
      }), this.disable();
    },
    destroy: function(t, n, e) {
      t.off(e._eventNamespace);
    }
  },
  showSelected: {
    text: ci("showSelected", "Show only selected"),
    className: "buttons-show-selected",
    action: function(t, n) {
      if (n.search.fixed("dt-select"))
        n.search.fixed("dt-select", null), this.active(!1);
      else {
        var e = n.settings()[0].aoData;
        n.search.fixed("dt-select", function(r, i, s) {
          return e[s]._select_selected;
        }), this.active(!0);
      }
      n.draw();
    }
  }
});
Te.each(["Row", "Column", "Cell"], function(t, n) {
  var e = n.toLowerCase();
  H.ext.buttons["select" + n + "s"] = {
    text: ci("select" + n + "s", "Select " + e + "s"),
    className: "buttons-select-" + e + "s",
    action: function() {
      this.select.items(e);
    },
    init: function(r) {
      var i = this;
      this.active(r.select.items() === e), r.on("selectItems.dt.DT", function(s, a, o) {
        i.active(o === e);
      });
    }
  };
});
H.type("select-checkbox", {
  className: "dt-select",
  detect: H.versionCheck("2.1") ? {
    oneOf: function() {
      return !1;
    },
    allOf: function() {
      return !1;
    },
    init: function(t, n, e) {
      return Xa(n);
    }
  } : function(t) {
    return t === "select-checkbox" ? t : !1;
  },
  order: {
    pre: function(t) {
      return t === "X" ? -1 : 0;
    }
  }
});
Te.extend(!0, H.defaults.oLanguage, {
  select: {
    aria: {
      rowCheckbox: "Select row"
    }
  }
});
H.render.select = function(t, n) {
  var e = t ? H.util.get(t) : null, r = n ? H.util.get(n) : null, i = function(s, a, o, l) {
    var c = l.settings.aoData[l.row], f = c._select_selected, g = l.settings.oLanguage.select.aria.rowCheckbox, b = l.settings._select.selectable;
    if (a === "display") {
      if (b) {
        var w = b(o, c.nTr, l.row);
        if (w === !1)
          return "";
      }
      return Te("<input>").attr({
        "aria-label": g,
        class: Si(),
        name: r ? r(o) : null,
        type: "checkbox",
        value: e ? e(o) : null,
        checked: f
      }).on("input", function(C) {
        C.preventDefault(), this.checked = Te(this).closest("tr").hasClass("selected");
      })[0];
    } else {
      if (a === "type")
        return "select-checkbox";
      if (a === "filter")
        return "";
    }
    return f ? "X" : "";
  };
  return i._name = "selectCheckbox", i;
};
H.ext.order["select-checkbox"] = function(t, n) {
  return this.api().column(n, { order: "index" }).nodes().map(function(e) {
    return t._select.items === "row" ? Te(e).parent().hasClass(t._select.className).toString() : t._select.items === "cell" ? Te(e).hasClass(t._select.className).toString() : !1;
  });
};
Te.fn.DataTable.select = H.select;
Te(document).on("i18n.dt.dtSelect preInit.dt.dtSelect", function(t, n) {
  t.namespace === "dt" && H.select.init(new H.Api(n));
});
/*! Bootstrap 5 styling wrapper for Select
 * © SpryMedia Ltd - datatables.net/license
 */
H.select.classes.checkbox = "form-check-input dt-select-checkbox";
/*! SearchPanes 2.3.5
 * © SpryMedia Ltd - datatables.net/license
 */
let On = lt;
(function() {
  var t, n;
  function e(F) {
    t = F, n = F.fn.dataTable;
  }
  var r = (
    /** @class */
    function() {
      function F(P, k, R, L, E) {
        var M = this;
        if (E === void 0 && (E = null), !n || !n.versionCheck || !n.versionCheck("1.10.0"))
          throw new Error("SearchPane requires DataTables 1.10 or newer");
        if (!n.select)
          throw new Error("SearchPane requires Select");
        var W = new n.Api(P);
        this.classes = t.extend(!0, {}, F.classes), this.c = t.extend(!0, {}, F.defaults, k, E), k && k.hideCount && k.viewCount === void 0 && (this.c.viewCount = !this.c.hideCount);
        var Y = W.columns().eq(0).toArray().length;
        this.s = {
          colExists: R < Y,
          colOpts: void 0,
          customPaneSettings: E,
          displayed: !1,
          dt: W,
          dtPane: void 0,
          firstSet: !0,
          index: R,
          indexes: [],
          listSet: !1,
          name: void 0,
          rowData: {
            arrayFilter: [],
            arrayOriginal: [],
            bins: {},
            binsOriginal: {},
            filterMap: /* @__PURE__ */ new Map(),
            totalOptions: 0
          },
          scrollTop: 0,
          searchFunction: void 0,
          selections: [],
          serverSelect: [],
          serverSelecting: !1,
          tableLength: null,
          updating: !1
        }, this.s.colOpts = this.s.colExists ? this._getOptions() : this._getBonusOptions(), this.dom = {
          buttonGroup: t("<div/>").addClass(this.classes.buttonGroup),
          clear: t('<button type="button">&#215;</button>').attr("disabled", "true").addClass(this.classes.disabledButton).addClass(this.classes.paneButton).addClass(this.classes.clearButton).attr("aria-label", W.i18n("searchPanes.buttons.clearPane", this.c.i18n.aria.clearPane)).html(this.s.dt.i18n("searchPanes.clearPane", this.c.i18n.clearPane)),
          collapseButton: t('<button type="button"><span class="' + this.classes.caret + '">&#x5e;</span></button>').addClass(this.classes.paneButton).addClass(this.classes.collapseButton).attr("aria-label", W.i18n("searchPanes.buttons.collapse", this.c.i18n.aria.collapse)),
          container: t("<div/>").addClass(this.classes.container).addClass(this.s.colOpts.className).addClass(this.classes.layout + (parseInt(this.c.layout.split("-")[1], 10) < 10 ? this.c.layout : this.c.layout.split("-")[0] + "-9")).addClass(this.s.customPaneSettings && this.s.customPaneSettings.className ? this.s.customPaneSettings.className : ""),
          countButton: t('<button type="button"><span></span></button>').addClass(this.classes.paneButton).addClass(this.classes.countButton).attr("aria-label", W.i18n("searchPanes.buttons.orderByCount", this.c.i18n.aria.orderByCount)),
          dtP: t('<table width="100%"><thead><tr><th></th><th></th></tr></thead></table>'),
          lower: t("<div/>").addClass(this.classes.subRow2).addClass(this.classes.narrowButton),
          nameButton: t('<button type="button"><span></span></button>').addClass(this.classes.paneButton).addClass(this.classes.nameButton).attr("aria-label", W.i18n("searchPanes.buttons.orderByLabel", this.c.i18n.aria.orderByLabel)),
          panesContainer: t(L),
          searchBox: t("<input/>").addClass(this.classes.paneInputButton).addClass(this.classes.search),
          searchButton: t('<button type="button"><span></span></button>').addClass(this.classes.searchIcon).addClass(this.classes.paneButton).attr("aria-label", W.i18n("searchPanes.buttons.clearSearch", this.c.i18n.aria.clearSearch)),
          searchCont: t("<div/>").addClass(this.classes.searchCont),
          searchLabelCont: t("<div/>").addClass(this.classes.searchLabelCont),
          topRow: t("<div/>").addClass(this.classes.topRow),
          upper: t("<div/>").addClass(this.classes.subRow1).addClass(this.classes.narrowSearch)
        };
        var J = "";
        this.s.colExists ? (J = t(this.s.dt.column(this.s.index).header()).text(), this.dom.dtP.find("th").eq(0).text(J)) : (J = this.s.customPaneSettings.header || "Custom Pane", this.dom.dtP.find("th").eq(0).html(J)), this.s.colOpts.name ? this.s.name = this.s.colOpts.name : this.s.customPaneSettings && this.s.customPaneSettings.name ? this.s.name = this.s.customPaneSettings.name : this.s.name = J;
        var X = this.s.dt.table(0).node();
        return this.s.searchFunction = function(Q, ee, ie) {
          if (M.s.selections.length === 0 || Q.nTable !== X)
            return !0;
          var oe = null;
          return M.s.colExists && (oe = ee[M.s.index], M.s.colOpts.orthogonal.filter !== "filter" && (oe = M.s.rowData.filterMap.get(ie), oe instanceof t.fn.dataTable.Api && (oe = oe.toArray()))), M._search(oe, ie);
        }, t.fn.dataTable.ext.search.push(this.s.searchFunction), this.c.clear && this.dom.clear.on("click.dtsp", function() {
          var Q = M.dom.container.find("." + M.classes.search.replace(/\s+/g, "."));
          Q.each(function() {
            t(this).val("").trigger("input");
          }), M.clearPane();
        }), this.s.dt.on("draw.dtsp", function() {
          return M.adjustTopRow();
        }), this.s.dt.on("buttons-action.dtsp", function() {
          return M.adjustTopRow();
        }), this.s.dt.on("column-reorder.dtsp", function(Q, ee, ie) {
          M.s.index = ie.mapping[M.s.index];
        }), this;
      }
      return F.prototype.addRow = function(P, k, R, L, E, M, W) {
        M || (M = this.s.rowData.bins[k] ? this.s.rowData.bins[k] : 0), W || (W = this._getShown(k));
        for (var Y, J = 0, X = this.s.indexes; J < X.length; J++) {
          var Q = X[J];
          Q.filter === k && (Y = Q.index);
        }
        return Y === void 0 && (Y = this.s.indexes.length, this.s.indexes.push({ filter: k, index: Y })), this.s.dtPane.row.add({
          className: E,
          display: P !== "" ? P : this.emptyMessage(),
          filter: k,
          index: Y,
          shown: W,
          sort: R,
          total: M,
          type: L
        });
      }, F.prototype.adjustTopRow = function() {
        var P = this.dom.container.find("." + this.classes.subRowsContainer.replace(/\s+/g, ".")), k = this.dom.container.find("." + this.classes.subRow1.replace(/\s+/g, ".")), R = this.dom.container.find("." + this.classes.subRow2.replace(/\s+/g, ".")), L = this.dom.container.find("." + this.classes.topRow.replace(/\s+/g, "."));
        (t(P[0]).width() < 252 || t(L[0]).width() < 252) && t(P[0]).width() !== 0 ? (t(P[0]).addClass(this.classes.narrow), t(k[0]).addClass(this.classes.narrowSub).removeClass(this.classes.narrowSearch), t(R[0]).addClass(this.classes.narrowSub).removeClass(this.classes.narrowButton)) : (t(P[0]).removeClass(this.classes.narrow), t(k[0]).removeClass(this.classes.narrowSub).addClass(this.classes.narrowSearch), t(R[0]).removeClass(this.classes.narrowSub).addClass(this.classes.narrowButton));
      }, F.prototype.clearData = function() {
        this.s.rowData = {
          arrayFilter: [],
          arrayOriginal: [],
          bins: {},
          binsOriginal: {},
          filterMap: /* @__PURE__ */ new Map(),
          totalOptions: 0
        };
      }, F.prototype.clearPane = function() {
        return this.s.dtPane.rows({ selected: !0 }).deselect(), this.updateTable(), this;
      }, F.prototype.collapse = function() {
        var P = this;
        !this.s.displayed || // If collapsing is disabled globally, and not enabled specifically for this column
        !this.c.collapse && this.s.colOpts.collapse !== !0 || // OR, collapsing could be enabled globally and this column specifically
        // is not to be collapsed.
        // We can't just take !this.s.colOpts.collapse here as if it is undefined
        // then the global should be used
        this.s.colOpts.collapse === !1 || (t(this.s.dtPane.table().container()).addClass(this.classes.hidden), this.dom.topRow.addClass(this.classes.bordered), this.dom.nameButton.addClass(this.classes.disabledButton), this.dom.countButton.addClass(this.classes.disabledButton), this.dom.searchButton.addClass(this.classes.disabledButton), this.dom.collapseButton.addClass(this.classes.rotated), this.dom.topRow.one("click.dtsp", function() {
          return P.show();
        }), this.dom.topRow.trigger("collapse.dtsps"));
      }, F.prototype.destroy = function() {
        this.s.dtPane && this.s.dtPane.off(".dtsp"), this.s.dt.off(".dtsp"), this.dom.clear.off(".dtsp"), this.dom.nameButton.off(".dtsp"), this.dom.countButton.off(".dtsp"), this.dom.searchButton.off(".dtsp"), this.dom.collapseButton.off(".dtsp"), t(this.s.dt.table().node()).off(".dtsp"), this.dom.container.detach();
        for (var P = t.fn.dataTable.ext.search.indexOf(this.s.searchFunction); P !== -1; )
          t.fn.dataTable.ext.search.splice(P, 1), P = t.fn.dataTable.ext.search.indexOf(this.s.searchFunction);
        this.s.dtPane && this.s.dtPane.destroy(), this.s.listSet = !1;
      }, F.prototype.emptyMessage = function() {
        var P = this.c.i18n.emptyMessage;
        return this.c.emptyMessage && (P = this.c.emptyMessage), this.s.colOpts.emptyMessage !== !1 && this.s.colOpts.emptyMessage !== null && (P = this.s.colOpts.emptyMessage), this.s.dt.i18n("searchPanes.emptyMessage", P);
      }, F.prototype.getPaneCount = function() {
        return this.s.dtPane ? this.s.dtPane.rows({ selected: !0 }).data().toArray().length : 0;
      }, F.prototype.rebuildPane = function(P, k) {
        P === void 0 && (P = null), k === void 0 && (k = !1), this.clearData();
        var R = [];
        this.s.serverSelect = [];
        var L = null;
        return this.s.dtPane && (k && (this.s.dt.page.info().serverSide ? this.s.serverSelect = this.s.dtPane.rows({ selected: !0 }).data().toArray() : R = this.s.dtPane.rows({ selected: !0 }).data().toArray()), this.s.dtPane.clear().destroy(), L = this.dom.container.prev(), this.destroy(), this.s.dtPane = void 0, t.fn.dataTable.ext.search.push(this.s.searchFunction)), this.dom.container.removeClass(this.classes.hidden), this.s.displayed = !1, this._buildPane(this.s.dt.page.info().serverSide ? this.s.serverSelect : R, P, L), this;
      }, F.prototype.resize = function(P) {
        this.c.layout = P, this.dom.container.removeClass().addClass(this.classes.show).addClass(this.classes.container).addClass(this.s.colOpts.className).addClass(this.classes.layout + (parseInt(P.split("-")[1], 10) < 10 ? P : P.split("-")[0] + "-9")).addClass(this.s.customPaneSettings !== null && this.s.customPaneSettings.className ? this.s.customPaneSettings.className : ""), this.adjustTopRow(), this.dom.dtP.css("table-layout", "fixed"), this.dom.dtP.css("width", "100%"), this.dom.dtP.find("colgroup col").css("width", "100%");
      }, F.prototype.setListeners = function() {
        var P = this;
        this.s.dtPane && (this.s.dtPane.off("select.dtsp").on("select.dtsp", function() {
          clearTimeout(P.s.deselectTimeout), P._updateSelection(!P.s.updating), P.dom.clear.removeClass(P.classes.disabledButton).removeAttr("disabled");
        }), this.s.dtPane.off("deselect.dtsp").on("deselect.dtsp", function() {
          P.s.deselectTimeout = setTimeout(function() {
            P._updateSelection(!0), P.s.dtPane.rows({ selected: !0 }).data().toArray().length === 0 && P.dom.clear.addClass(P.classes.disabledButton).attr("disabled", "true");
          }, 50);
        }), this.s.firstSet && (this.s.firstSet = !1, this.s.dt.on("stateSaveParams.dtsp", function(k, R, L) {
          if (t.isEmptyObject(L)) {
            P.s.dtPane.state.clear();
            return;
          }
          var E, M, W = [], Y, J, X;
          P.s.dtPane && (W = P.s.dtPane.rows({ selected: !0 }).data().map(function(ee) {
            return ee.filter !== null ? ee.filter.toString() : null;
          }).toArray(), J = P.dom.searchBox.val(), M = P.s.dtPane.order(), E = P.s.rowData.binsOriginal, X = P.s.rowData.arrayOriginal, Y = P.dom.collapseButton.hasClass(P.classes.rotated)), L.searchPanes === void 0 && (L.searchPanes = {}), L.searchPanes.panes === void 0 && (L.searchPanes.panes = []);
          for (var Q = 0; Q < L.searchPanes.panes.length; Q++)
            L.searchPanes.panes[Q].id === P.s.index && (L.searchPanes.panes.splice(Q, 1), Q--);
          L.searchPanes.panes.push({
            arrayFilter: X,
            bins: E,
            collapsed: Y,
            id: P.s.index,
            order: M,
            searchTerm: J,
            selected: W
          });
        })), this.s.dtPane.off("user-select.dtsp").on("user-select.dtsp", function(k, R, L, E, M) {
          M.stopPropagation();
        }), this.s.dtPane.off("draw.dtsp").on("draw.dtsp", function() {
          return P.adjustTopRow();
        }), this.dom.nameButton.off("click.dtsp").on("click.dtsp", function() {
          var k = P.s.dtPane.order()[0][1];
          P.s.dtPane.order([[0, k === "asc" ? "desc" : "asc"]]).draw(), P.s.dt.state.save();
        }), this.dom.countButton.off("click.dtsp").on("click.dtsp", function() {
          var k = P.s.dtPane.order()[0][1], R = k === "asc" ? "desc" : "asc";
          P.s.dtPane.order([[2, R], [1, R]]).draw(), P.s.dt.state.save();
        }), this.dom.collapseButton.off("click.dtsp").on("click.dtsp", function(k) {
          k.stopPropagation();
          var R = t(P.s.dtPane.table().container());
          R.toggleClass(P.classes.hidden), P.dom.topRow.toggleClass(P.classes.bordered), P.dom.nameButton.toggleClass(P.classes.disabledButton), P.dom.countButton.toggleClass(P.classes.disabledButton), P.dom.searchButton.toggleClass(P.classes.disabledButton), P.dom.collapseButton.toggleClass(P.classes.rotated), R.hasClass(P.classes.hidden) ? P.dom.topRow.on("click.dtsp", function() {
            return P.dom.collapseButton.click();
          }) : P.dom.topRow.off("click.dtsp"), P.s.dt.state.save(), P.dom.topRow.trigger("collapse.dtsps");
        }), this.dom.clear.off("click.dtsp").on("click.dtsp", function() {
          var k = P.dom.container.find("." + P.classes.search.replace(/ /g, "."));
          k.each(function() {
            t(this).val("").trigger("input");
          }), P.clearPane();
        }), this.dom.searchButton.off("click.dtsp").on("click.dtsp", function() {
          return P.dom.searchBox.focus();
        }), this.dom.searchBox.off("click.dtsp").on("input.dtsp", function() {
          var k = P.dom.searchBox.val();
          P.s.dtPane.search(k).draw(), typeof k == "string" && (k.length > 0 || k.length === 0 && P.s.dtPane.rows({ selected: !0 }).data().toArray().length > 0) ? P.dom.clear.removeClass(P.classes.disabledButton).removeAttr("disabled") : P.dom.clear.addClass(P.classes.disabledButton).attr("disabled", "true"), P.s.dt.state.save();
        }), this.s.dtPane.select.style(this.s.colOpts.dtOpts && this.s.colOpts.dtOpts.select && this.s.colOpts.dtOpts.select.style ? this.s.colOpts.dtOpts.select.style : this.c.dtOpts && this.c.dtOpts.select && this.c.dtOpts.select.style ? this.c.dtOpts.select.style : "os"));
      }, F.prototype._serverPopulate = function(P) {
        P.tableLength ? (this.s.tableLength = P.tableLength, this.s.rowData.totalOptions = this.s.tableLength) : (this.s.tableLength === null || this.s.dt.rows()[0].length > this.s.tableLength) && (this.s.tableLength = this.s.dt.rows()[0].length, this.s.rowData.totalOptions = this.s.tableLength);
        var k = this.s.dt.column(this.s.index).dataSrc();
        if (P.searchPanes.options[k])
          for (var R = 0, L = P.searchPanes.options[k]; R < L.length; R++) {
            var E = L[R];
            this.s.rowData.arrayFilter.push({
              display: E.label,
              filter: E.value,
              sort: E.label,
              type: E.label
            }), this.s.rowData.bins[E.value] = E.total;
          }
        var M = Object.keys(this.s.rowData.bins).length, W = this._uniqueRatio(M, this.s.tableLength);
        if (this.s.displayed === !1 && ((this.s.colOpts.show === void 0 && this.s.colOpts.threshold === null ? W > this.c.threshold : W > this.s.colOpts.threshold) || this.s.colOpts.show !== !0 && M <= 1)) {
          this.dom.container.addClass(this.classes.hidden), this.s.displayed = !1;
          return;
        }
        this.s.rowData.arrayOriginal = this.s.rowData.arrayFilter, this.s.rowData.binsOriginal = this.s.rowData.bins, this.s.displayed = !0;
      }, F.prototype.show = function() {
        this.s.displayed && (this.dom.topRow.removeClass(this.classes.bordered), this.dom.nameButton.removeClass(this.classes.disabledButton), this.dom.countButton.removeClass(this.classes.disabledButton), this.dom.searchButton.removeClass(this.classes.disabledButton), this.dom.collapseButton.removeClass(this.classes.rotated), t(this.s.dtPane.table().container()).removeClass(this.classes.hidden), this.dom.topRow.trigger("collapse.dtsps"));
      }, F.prototype._uniqueRatio = function(P, k) {
        return k > 0 && (this.s.rowData.totalOptions > 0 && !this.s.dt.page.info().serverSide || this.s.dt.page.info().serverSide && this.s.tableLength > 0) ? P / this.s.rowData.totalOptions : 1;
      }, F.prototype.updateTable = function() {
        var P = this.s.dtPane.rows({ selected: !0 }).data().toArray().map(function(k) {
          return k.filter;
        });
        this.s.selections = P, this._searchExtras();
      }, F.prototype._getComparisonRows = function() {
        var P = this.s.colOpts.options ? this.s.colOpts.options : this.s.customPaneSettings && this.s.customPaneSettings.options ? this.s.customPaneSettings.options : void 0;
        if (P !== void 0) {
          var k = this.s.dt.rows(), R = k.data().toArray(), L = [];
          this.s.dtPane.clear(), this.s.indexes = [];
          for (var E = 0, M = P; E < M.length; E++) {
            var W = M[E], Y = W.label !== "" ? W.label : this.emptyMessage(), J = {
              className: W.className,
              display: Y,
              filter: typeof W.value == "function" ? W.value : [],
              sort: W.order !== void 0 ? W.order : Y,
              total: 0,
              type: Y
            };
            if (typeof W.value == "function") {
              for (var X = 0; X < R.length; X++)
                W.value.call(this.s.dt, R[X], k[0][X]) && J.total++;
              typeof J.filter != "function" && J.filter.push(W.filter);
            }
            L.push(this.addRow(J.display, J.filter, J.sort, J.type, J.className, J.total));
          }
          return L;
        }
      }, F.prototype._getMessage = function(P) {
        return this.s.dt.i18n("searchPanes.count", this.c.i18n.count).replace(/{total}/g, P.total);
      }, F.prototype._getShown = function(P) {
      }, F.prototype._getPaneConfig = function() {
        var P = this, k = n.Scroller, R = this.s.dt.settings()[0].oLanguage;
        return R.url = null, R.sUrl = null, {
          columnDefs: [
            {
              className: "dtsp-nameColumn",
              data: "display",
              render: function(L, E, M) {
                if (E === "sort")
                  return M.sort;
                if (E === "type")
                  return M.type;
                var W = P._getMessage(M), Y = '<span class="' + P.classes.pill + '">' + W + "</span>";
                return (!P.c.viewCount || !P.s.colOpts.viewCount) && (Y = ""), E === "filter" ? typeof L == "string" && L.match(/<[^>]*>/) !== null ? L.replace(/<[^>]*>/g, "") : L : '<div class="' + P.classes.nameCont + '"><span title="' + (typeof L == "string" && L.match(/<[^>]*>/) !== null ? L.replace(/<[^>]*>/g, "") : L) + '" class="' + P.classes.name + '">' + L + "</span>" + Y + "</div>";
              },
              targets: 0,
              // Accessing the private datatables property to set type based on the original table.
              // This is null if not defined by the user, meaning that automatic type detection
              //  would take place
              type: this.s.dt.settings()[0].aoColumns[this.s.index] ? this.s.dt.settings()[0].aoColumns[this.s.index]._sManualType : null
            },
            {
              className: "dtsp-countColumn " + this.classes.badgePill,
              data: "total",
              searchable: !1,
              targets: 1,
              visible: !1
            },
            {
              data: "shown",
              searchable: !1,
              targets: 2,
              visible: !1,
              defaultContent: 0
            }
          ],
          deferRender: !0,
          info: !1,
          language: R,
          paging: !!k,
          scrollX: !1,
          scrollY: "200px",
          scroller: !!k,
          select: !0,
          stateSave: !!this.s.dt.settings()[0].oFeatures.bStateSave
        };
      }, F.prototype._makeSelection = function() {
        this.updateTable(), this.s.updating = !0, this.s.dt.draw(), this.s.updating = !1;
      }, F.prototype._populatePaneArray = function(P, k, R, L) {
        L === void 0 && (L = this.s.rowData.bins);
        var E = R.fastData ? R.fastData : function(Y, J, X) {
          return R.oApi._fnGetCellData(R, Y, J, X);
        };
        if (typeof this.s.colOpts.orthogonal == "string") {
          var M = E(P, this.s.index, this.s.colOpts.orthogonal);
          this.s.rowData.filterMap.set(P, M), this._addOption(M, M, M, M, k, L), this.s.rowData.totalOptions++;
        } else {
          var W = E(P, this.s.index, this.s.colOpts.orthogonal.search);
          W === null && (W = ""), typeof W == "string" && (W = W.replace(/<[^>]*>/g, "")), this.s.rowData.filterMap.set(P, W), L[W] ? (L[W]++, this.s.rowData.totalOptions++) : (this._addOption(W, E(P, this.s.index, this.s.colOpts.orthogonal.display), E(P, this.s.index, this.s.colOpts.orthogonal.sort), E(P, this.s.index, this.s.colOpts.orthogonal.type), k, L), this.s.rowData.totalOptions++);
        }
      }, F.prototype._reloadSelect = function(P) {
        if (P !== void 0) {
          for (var k, R = 0; R < P.searchPanes.panes.length; R++)
            if (P.searchPanes.panes[R].id === this.s.index) {
              k = R;
              break;
            }
          if (k)
            for (var L = this.s.dtPane, E = L.rows({ order: "index" }).data().map(function(X) {
              return X.filter !== null ? X.filter.toString() : null;
            }).toArray(), M = 0, W = P.searchPanes.panes[k].selected; M < W.length; M++) {
              var Y = W[M], J = -1;
              Y !== null && (J = E.indexOf(Y.toString())), J > -1 && (this.s.serverSelecting = !0, L.row(J).select(), this.s.serverSelecting = !1);
            }
        }
      }, F.prototype._updateSelection = function(P) {
        var k = this, R = function(E) {
          if (H.versionCheck("2"))
            k.s.dt.processing(E);
          else {
            var M = k.s.dt.settings()[0], W = M.oApi;
            W._fnProcessingDisplay(M, !1);
          }
        }, L = function() {
          k.s.scrollTop = t(k.s.dtPane.table().node()).parent()[0].scrollTop, k.s.dt.page.info().serverSide && !k.s.updating ? k.s.serverSelecting || (k.s.serverSelect = k.s.dtPane.rows({ selected: !0 }).data().toArray(), k.s.dt.draw(!1)) : P && k._makeSelection(), R(!1);
        };
        R(!0), setTimeout(L, 1);
      }, F.prototype._addOption = function(P, k, R, L, E, M) {
        if (Array.isArray(P) || P instanceof n.Api) {
          if (P instanceof n.Api && (P = P.toArray(), k = k.toArray()), P.length === k.length) {
            for (var W = 0; W < P.length; W++)
              M[P[W]] ? M[P[W]]++ : (M[P[W]] = 1, E.push({
                display: k[W],
                filter: P[W],
                sort: R[W],
                type: L[W]
              })), this.s.rowData.totalOptions++;
            return;
          }
          throw new Error("display and filter not the same length");
        } else
          typeof this.s.colOpts.orthogonal == "string" ? M[P] ? (M[P]++, this.s.rowData.totalOptions++) : (M[P] = 1, E.push({
            display: k,
            filter: P,
            sort: R,
            type: L
          }), this.s.rowData.totalOptions++) : (M[P] = 1, E.push({
            display: k,
            filter: P,
            sort: R,
            type: L
          }));
      }, F.prototype._buildPane = function(P, k, R) {
        var L = this;
        P === void 0 && (P = []), k === void 0 && (k = null), R === void 0 && (R = null), this.s.selections = [];
        var E = this.s.dt.state.loaded(), M;
        if (this.s.listSet && (E = this.s.dt.state()), this.s.colExists) {
          var W = -1;
          if (E && E.searchPanes && E.searchPanes.panes) {
            for (var Y = 0; Y < E.searchPanes.panes.length; Y++)
              if (E.searchPanes.panes[Y].id === this.s.index) {
                W = Y;
                break;
              }
          }
          if ((this.s.colOpts.show === !1 || this.s.colOpts.show !== void 0 && this.s.colOpts.show !== !0) && W === -1)
            return this.dom.container.addClass(this.classes.hidden), this.s.displayed = !1, !1;
          if ((this.s.colOpts.show === !0 || W !== -1) && (this.s.displayed = !0), !this.s.dt.page.info().serverSide && (!k || !k.searchPanes || !k.searchPanes.options)) {
            this.s.rowData.arrayFilter.length === 0 && (this.s.rowData.totalOptions = 0, this._populatePane(), this.s.rowData.arrayOriginal = this.s.rowData.arrayFilter, this.s.rowData.binsOriginal = this.s.rowData.bins);
            var J = Object.keys(this.s.rowData.binsOriginal).length, X = this._uniqueRatio(J, this.s.dt.rows()[0].length);
            if (this.s.displayed === !1 && ((this.s.colOpts.show === void 0 && this.s.colOpts.threshold === null ? X > this.c.threshold : X > this.s.colOpts.threshold) || this.s.colOpts.show !== !0 && J <= 1)) {
              this.dom.container.addClass(this.classes.hidden), this.s.displayed = !1;
              return;
            }
            this.dom.container.addClass(this.classes.show), this.s.displayed = !0;
          } else
            k && k.searchPanes && k.searchPanes.options && this._serverPopulate(k);
        } else
          this.s.displayed = !0;
        this._displayPane(), this.s.listSet || this.dom.dtP.on("stateLoadParams.dtsp", function(mt, St, ut) {
          t.isEmptyObject(L.s.dt.state.loaded()) && t.each(ut, function(Ot) {
            delete ut[Ot];
          });
        }), R !== null && this.dom.panesContainer.has(R).length > 0 ? this.dom.container.insertAfter(R) : this.dom.panesContainer.prepend(this.dom.container);
        var Q = t.fn.dataTable.ext.errMode;
        t.fn.dataTable.ext.errMode = "none", this.dom.dtP.on("init.dt", function(mt, St) {
          var ut = L.dom.dtP.DataTable(), Ot = ut.select.style();
          ut.select.style(Ot);
        });
        var ee;
        if (t.fn.dataTable.versionCheck("2")) {
          var ie = t.extend(!0, {}, t.fn.dataTable.defaults.layout);
          t.each(ie, function(mt, St) {
            ie[mt] = null;
          }), ee = { layout: ie };
        } else
          ee = { dom: "t" };
        this.s.dtPane = this.dom.dtP.DataTable(t.extend(!0, this._getPaneConfig(), this.c.dtOpts, this.s.colOpts ? this.s.colOpts.dtOpts : {}, this.s.colOpts.options || !this.s.colExists ? {
          createdRow: function(mt, St) {
            t(mt).addClass(St.className);
          }
        } : void 0, this.s.customPaneSettings !== null && this.s.customPaneSettings.dtOpts ? this.s.customPaneSettings.dtOpts : {}, ee)), this.dom.dtP.addClass(this.classes.table);
        var oe = "Custom Pane";
        this.s.customPaneSettings && this.s.customPaneSettings.header ? oe = this.s.customPaneSettings.header : this.s.colOpts.header ? oe = this.s.colOpts.header : this.s.colExists && (oe = t.fn.dataTable.versionCheck("2") ? this.s.dt.column(this.s.index).title() : this.s.dt.settings()[0].aoColumns[this.s.index].sTitle), oe = this._escapeHTML(oe);
        var ce = this.s.dt.i18n("searchBuilder.searchTitle", this.c.i18n.searchTitle).replace("{name}", oe);
        if (this.dom.searchBox.attr("placeholder", oe).attr("title", ce), t.fn.dataTable.ext.errMode = Q, this.s.colExists)
          for (var he = 0, we = this.s.rowData.arrayFilter.length; he < we; he++)
            if (this.s.dt.page.info().serverSide) {
              M = this.addRow(this.s.rowData.arrayFilter[he].display, this.s.rowData.arrayFilter[he].filter, this.s.rowData.arrayFilter[he].sort, this.s.rowData.arrayFilter[he].type);
              for (var Oe = 0, Ve = this.s.serverSelect; Oe < Ve.length; Oe++) {
                var Ie = Ve[Oe];
                Ie.filter === this.s.rowData.arrayFilter[he].filter && (this.s.serverSelecting = !0, M.select(), this.s.serverSelecting = !1);
              }
            } else
              !this.s.dt.page.info().serverSide && this.s.rowData.arrayFilter[he] ? this.addRow(this.s.rowData.arrayFilter[he].display, this.s.rowData.arrayFilter[he].filter, this.s.rowData.arrayFilter[he].sort, this.s.rowData.arrayFilter[he].type) : this.s.dt.page.info().serverSide || this.addRow("", "", "", "");
        (this.s.colOpts.options || this.s.customPaneSettings && this.s.customPaneSettings.options) && this._getComparisonRows(), this.s.dtPane.draw(), this.s.dtPane.table().node().parentNode.scrollTop = this.s.scrollTop, this.adjustTopRow(), this.setListeners(), this.s.listSet = !0;
        for (var Ze = 0, et = P; Ze < et.length; Ze++) {
          var He = et[Ze];
          if (He)
            for (var Ke = 0, Ct = this.s.dtPane.rows().indexes().toArray(); Ke < Ct.length; Ke++)
              M = Ct[Ke], this.s.dtPane.row(M).data() && He.filter === this.s.dtPane.row(M).data().filter && (this.s.dt.page.info().serverSide ? (this.s.serverSelecting = !0, this.s.dtPane.row(M).select(), this.s.serverSelecting = !1) : this.s.dtPane.row(M).select());
        }
        if (this.s.dt.page.info().serverSide && this.s.dtPane.search(this.dom.searchBox.val()).draw(), (this.c.initCollapsed && this.s.colOpts.initCollapsed !== !1 || this.s.colOpts.initCollapsed) && (this.c.collapse && this.s.colOpts.collapse !== !1 || this.s.colOpts.collapse) && (this.s.dtPane.settings()[0]._bInitComplete ? this.collapse() : this.s.dtPane.one("init", function() {
          return L.collapse();
        })), E && E.searchPanes && E.searchPanes.panes && (!k || k.draw === 1)) {
          this._reloadSelect(E);
          for (var yt = 0, wt = E.searchPanes.panes; yt < wt.length; yt++) {
            var rt = wt[yt];
            rt.id === this.s.index && (rt.searchTerm && rt.searchTerm.length > 0 && this.dom.searchBox.val(rt.searchTerm).trigger("input"), rt.order && this.s.dtPane.order(rt.order).draw(), rt.collapsed ? this.collapse() : this.show());
          }
        }
        return !0;
      }, F.prototype._displayPane = function() {
        this.dom.dtP.empty(), this.dom.topRow.empty().addClass(this.classes.topRow), parseInt(this.c.layout.split("-")[1], 10) > 3 && this.dom.container.addClass(this.classes.smallGap), this.dom.topRow.addClass(this.classes.subRowsContainer).append(this.dom.upper.append(this.dom.searchCont)).append(this.dom.lower.append(this.dom.buttonGroup)), (this.c.dtOpts.searching === !1 || this.s.colOpts.dtOpts && this.s.colOpts.dtOpts.searching === !1 || !this.c.controls || !this.s.colOpts.controls || this.s.customPaneSettings && this.s.customPaneSettings.dtOpts && this.s.customPaneSettings.dtOpts.searching !== void 0 && !this.s.customPaneSettings.dtOpts.searching) && this.dom.searchBox.removeClass(this.classes.paneInputButton).addClass(this.classes.disabledButton).attr("disabled", "true"), this.dom.searchBox.appendTo(this.dom.searchCont), this._searchContSetup(), this.c.clear && this.c.controls && this.s.colOpts.controls && this.dom.clear.appendTo(this.dom.buttonGroup), this.c.orderable && this.s.colOpts.orderable && this.c.controls && this.s.colOpts.controls && this.dom.nameButton.appendTo(this.dom.buttonGroup), this.c.viewCount && this.s.colOpts.viewCount && this.c.orderable && this.s.colOpts.orderable && this.c.controls && this.s.colOpts.controls && this.dom.countButton.appendTo(this.dom.buttonGroup), (this.c.collapse && this.s.colOpts.collapse !== !1 || this.s.colOpts.collapse) && this.c.controls && this.s.colOpts.controls && this.dom.collapseButton.appendTo(this.dom.buttonGroup), this.dom.container.prepend(this.dom.topRow).append(this.dom.dtP).show();
      }, F.prototype._escapeHTML = function(P) {
        return P.toString().replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&amp;/g, "&");
      }, F.prototype._getBonusOptions = function() {
        var P = {
          threshold: null
        };
        return t.extend(!0, {}, F.defaults, P, this.c ? this.c : {});
      }, F.prototype._getOptions = function() {
        var P = this.s.dt, k = {
          collapse: null,
          emptyMessage: !1,
          initCollapsed: null,
          threshold: null
        }, R = P.settings()[0].aoColumns[this.s.index].searchPanes, L = t.extend(!0, {}, F.defaults, k, R);
        return R && R.hideCount && R.viewCount === void 0 && (L.viewCount = !R.hideCount), L;
      }, F.prototype._populatePane = function() {
        this.s.rowData.arrayFilter = [], this.s.rowData.bins = {};
        var P = this.s.dt.context[0];
        if (!this.s.dt.page.info().serverSide)
          for (var k = 0, R = this.s.dt.rows().indexes().toArray(); k < R.length; k++) {
            var L = R[k];
            this._populatePaneArray(L, this.s.rowData.arrayFilter, P);
          }
      }, F.prototype._search = function(P, k) {
        for (var R = this.s.colOpts, L = this.s.dt, E = 0, M = this.s.selections; E < M.length; E++) {
          var W = M[E];
          if (typeof W == "string" && typeof P == "string" && (W = this._escapeHTML(W)), Array.isArray(P)) {
            if (R.combiner === "and") {
              if (!P.includes(W))
                return !1;
            } else if (P.includes(W))
              return !0;
          } else if (typeof W == "function") {
            if (W.call(L, L.row(k).data(), k)) {
              if (R.combiner === "or")
                return !0;
            } else if (R.combiner === "and")
              return !1;
          } else if (P === W || // Loose type checking incase number type in column comparing to a string
          // eslint-disable-next-line eqeqeq
          !(typeof P == "string" && P.length === 0) && P == W || W === null && typeof P == "string" && P === "")
            return !0;
        }
        return R.combiner === "and";
      }, F.prototype._searchContSetup = function() {
        this.c.controls && this.s.colOpts.controls && this.dom.searchButton.appendTo(this.dom.searchLabelCont), this.c.dtOpts.searching === !1 || this.s.colOpts.dtOpts.searching === !1 || this.s.customPaneSettings && this.s.customPaneSettings.dtOpts && this.s.customPaneSettings.dtOpts.searching !== void 0 && !this.s.customPaneSettings.dtOpts.searching || this.dom.searchLabelCont.appendTo(this.dom.searchCont);
      }, F.prototype._searchExtras = function() {
        var P = this.s.updating;
        this.s.updating = !0;
        var k = this.s.dtPane.rows({ selected: !0 }).data().pluck("filter").toArray(), R = k.indexOf(this.emptyMessage()), L = t(this.s.dtPane.table().container());
        R > -1 && (k[R] = ""), k.length > 0 ? L.addClass(this.classes.selected) : k.length === 0 && L.removeClass(this.classes.selected), this.s.updating = P;
      }, F.version = "2.1.2", F.classes = {
        bordered: "dtsp-bordered",
        buttonGroup: "dtsp-buttonGroup",
        buttonSub: "dtsp-buttonSub",
        caret: "dtsp-caret",
        clear: "dtsp-clear",
        clearAll: "dtsp-clearAll",
        clearButton: "clearButton",
        collapseAll: "dtsp-collapseAll",
        collapseButton: "dtsp-collapseButton",
        container: "dtsp-searchPane",
        countButton: "dtsp-countButton",
        disabledButton: "dtsp-disabledButton",
        hidden: "dtsp-hidden",
        hide: "dtsp-hide",
        layout: "dtsp-",
        name: "dtsp-name",
        nameButton: "dtsp-nameButton",
        nameCont: "dtsp-nameCont",
        narrow: "dtsp-narrow",
        paneButton: "dtsp-paneButton",
        paneInputButton: "dtsp-paneInputButton",
        pill: "dtsp-pill",
        rotated: "dtsp-rotated",
        search: "dtsp-search",
        searchCont: "dtsp-searchCont",
        searchIcon: "dtsp-searchIcon",
        searchLabelCont: "dtsp-searchButtonCont",
        selected: "dtsp-selected",
        smallGap: "dtsp-smallGap",
        subRow1: "dtsp-subRow1",
        subRow2: "dtsp-subRow2",
        subRowsContainer: "dtsp-subRowsContainer",
        title: "dtsp-title",
        topRow: "dtsp-topRow"
      }, F.defaults = {
        clear: !0,
        collapse: !0,
        combiner: "or",
        container: function(P) {
          return P.table().container();
        },
        controls: !0,
        dtOpts: {},
        emptyMessage: null,
        hideCount: !1,
        i18n: {
          aria: {
            clearPane: "Clear selection",
            clearSearch: "Clear search",
            collapse: "Collapse / show pane",
            orderByCount: "Order by count",
            orderByLabel: "Order by label"
          },
          clearPane: "&times;",
          count: "{total}",
          emptyMessage: "<em>Empty</em>",
          searchTitle: "Search: {name}"
        },
        initCollapsed: !1,
        layout: "auto",
        name: void 0,
        orderable: !0,
        orthogonal: {
          display: "display",
          filter: "filter",
          hideCount: !1,
          search: "filter",
          show: void 0,
          sort: "sort",
          threshold: 0.6,
          type: "type",
          viewCount: !0
        },
        preSelect: [],
        threshold: 0.6,
        viewCount: !0
      }, F;
    }()
  ), i = window && window.__extends || /* @__PURE__ */ function() {
    var F = function(P, k) {
      return F = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(R, L) {
        R.__proto__ = L;
      } || function(R, L) {
        for (var E in L)
          L.hasOwnProperty(E) && (R[E] = L[E]);
      }, F(P, k);
    };
    return function(P, k) {
      F(P, k);
      function R() {
        this.constructor = P;
      }
      P.prototype = k === null ? Object.create(k) : (R.prototype = k.prototype, new R());
    };
  }(), s = (
    /** @class */
    function(F) {
      i(P, F);
      function P(k, R, L, E, M) {
        return F.call(this, k, R, L, E, M) || this;
      }
      return P.prototype._emptyPane = function() {
        var k = this.s.dtPane;
        if (H.versionCheck("2")) {
          var R = k.select.last(), L;
          return R && k.row(R.row).any() && (L = k.row(R.row).data().index), k.rows().remove(), function() {
            if (L !== void 0) {
              var E = k.row(function(M, W) {
                return W.index === L;
              }).index();
              k.select.last({ row: E, column: 0 });
            }
          };
        }
        return k.rows().remove(), function() {
        };
      }, P.prototype._serverPopulate = function(k) {
        var R, L, E;
        this.s.rowData.binsShown = {}, this.s.rowData.arrayFilter = [], k.tableLength !== void 0 ? (this.s.tableLength = k.tableLength, this.s.rowData.totalOptions = this.s.tableLength) : (this.s.tableLength === null || this.s.dt.rows()[0].length > this.s.tableLength) && (this.s.tableLength = this.s.dt.rows()[0].length, this.s.rowData.totalOptions = this.s.tableLength);
        var M = this.s.dt.column(this.s.index).dataSrc();
        if (k.searchPanes.options[M] !== void 0)
          for (var W = 0, Y = k.searchPanes.options[M]; W < Y.length; W++) {
            var J = Y[W];
            this.s.rowData.arrayFilter.push({
              display: J.label,
              filter: J.value,
              shown: +J.count,
              sort: J.label,
              total: +J.total,
              type: J.label
            }), this.s.rowData.binsShown[J.value] = +J.count, this.s.rowData.bins[J.value] = +J.total;
          }
        var X = Object.keys(this.s.rowData.bins).length, Q = this._uniqueRatio(X, this.s.tableLength);
        if (!this.s.colOpts.show && this.s.displayed === !1 && ((this.s.colOpts.show === void 0 && this.s.colOpts.threshold === null ? Q > this.c.threshold : Q > this.s.colOpts.threshold) || this.s.colOpts.show !== !0 && X <= 1)) {
          this.dom.container.addClass(this.classes.hidden), this.s.displayed = !1;
          return;
        }
        if (this.s.rowData.arrayOriginal = this.s.rowData.arrayFilter, this.s.rowData.binsOriginal = this.s.rowData.bins, this.s.displayed = !0, this.s.dtPane) {
          for (var ee = this.s.serverSelect, ie = this._emptyPane(), oe = 0, ce = this.s.rowData.arrayFilter; oe < ce.length; oe++)
            if (E = ce[oe], this._shouldAddRow(E)) {
              L = this.addRow(E.display, E.filter, E.sort, E.type);
              for (var he = 0; he < ee.length; he++)
                if (R = ee[he], R.filter === E.filter) {
                  this.s.serverSelecting = !0, L.select(), this.s.serverSelecting = !1, ee.splice(he, 1), this.s.selections.push(E.filter);
                  break;
                }
            }
          for (var we = 0, Oe = ee; we < Oe.length; we++) {
            R = Oe[we];
            for (var Ve = 0, Ie = this.s.rowData.arrayOriginal; Ve < Ie.length; Ve++)
              E = Ie[Ve], E.filter === R.filter && (L = this.addRow(E.display, E.filter, E.sort, E.type), this.s.serverSelecting = !0, L.select(), this.s.serverSelecting = !1, this.s.selections.push(E.filter));
          }
          this.s.serverSelect = this.s.dtPane.rows({ selected: !0 }).data().toArray(), this.s.dtPane.draw(), ie();
        }
      }, P.prototype.updateRows = function() {
        if (!this.s.dt.page.info().serverSide) {
          this.s.rowData.binsShown = {};
          for (var k = 0, R = this.s.dt.rows({ search: "applied" }).indexes().toArray(); k < R.length; k++) {
            var L = R[k];
            this._updateShown(L, this.s.dt.settings()[0], this.s.rowData.binsShown);
          }
        }
        for (var E = function(X) {
          X.shown = typeof M.s.rowData.binsShown[X.filter] == "number" ? M.s.rowData.binsShown[X.filter] : 0, M.s.dtPane.row(function(Q, ee) {
            return ee && ee.index === X.index;
          }).data(X);
        }, M = this, W = 0, Y = this.s.dtPane.rows().data().toArray(); W < Y.length; W++) {
          var J = Y[W];
          E(J);
        }
        this.s.dtPane.draw(), this.s.dtPane.table().node().parentNode.scrollTop = this.s.scrollTop;
      }, P.prototype._makeSelection = function() {
      }, P.prototype._reloadSelect = function() {
      }, P.prototype._shouldAddRow = function(k) {
        return !0;
      }, P.prototype._updateSelection = function() {
        this.s.dt.page.info().serverSide && !this.s.updating && !this.s.serverSelecting && (this.s.serverSelect = this.s.dtPane.rows({ selected: !0 }).data().toArray());
      }, P.prototype._updateShown = function(k, R, L) {
        L === void 0 && (L = this.s.rowData.binsShown);
        var E = typeof this.s.colOpts.orthogonal == "string" ? this.s.colOpts.orthogonal : this.s.colOpts.orthogonal.search, M = R.fastData ? R.fastData : function(ee, ie, oe) {
          return R.oApi._fnGetCellData(R, ee, ie, oe);
        }, W = M(k, this.s.index, E), Y = function(ee) {
          L[ee] ? L[ee]++ : L[ee] = 1;
        };
        if (Array.isArray(W))
          for (var J = 0, X = W; J < X.length; J++) {
            var Q = X[J];
            Y(Q);
          }
        else
          Y(W);
      }, P;
    }(r)
  ), a = window && window.__extends || /* @__PURE__ */ function() {
    var F = function(P, k) {
      return F = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(R, L) {
        R.__proto__ = L;
      } || function(R, L) {
        for (var E in L)
          L.hasOwnProperty(E) && (R[E] = L[E]);
      }, F(P, k);
    };
    return function(P, k) {
      F(P, k);
      function R() {
        this.constructor = P;
      }
      P.prototype = k === null ? Object.create(k) : (R.prototype = k.prototype, new R());
    };
  }(), o;
  function l(F) {
    o = F;
  }
  var c = (
    /** @class */
    function(F) {
      a(P, F);
      function P(k, R, L, E, M) {
        var W = this, Y = {
          i18n: {
            countFiltered: "{shown} ({total})"
          }
        };
        return W = F.call(this, k, o.extend(Y, R), L, E, M) || this, W;
      }
      return P.prototype._getMessage = function(k) {
        var R = this.s.dt.i18n("searchPanes.count", this.c.i18n.count), L = this.s.dt.i18n("searchPanes.countFiltered", this.c.i18n.countFiltered);
        return (this.s.filteringActive ? L : R).replace(/{total}/g, k.total).replace(/{shown}/g, k.shown);
      }, P.prototype._getShown = function(k) {
        return this.s.rowData.binsShown && this.s.rowData.binsShown[k] ? this.s.rowData.binsShown[k] : 0;
      }, P;
    }(s)
  ), f = window && window.__extends || /* @__PURE__ */ function() {
    var F = function(P, k) {
      return F = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(R, L) {
        R.__proto__ = L;
      } || function(R, L) {
        for (var E in L)
          L.hasOwnProperty(E) && (R[E] = L[E]);
      }, F(P, k);
    };
    return function(P, k) {
      F(P, k);
      function R() {
        this.constructor = P;
      }
      P.prototype = k === null ? Object.create(k) : (R.prototype = k.prototype, new R());
    };
  }(), g;
  function b(F) {
    g = F;
  }
  var w = (
    /** @class */
    function(F) {
      f(P, F);
      function P(k, R, L, E, M) {
        var W = this, Y = {
          i18n: {
            count: "{shown}"
          }
        };
        return W = F.call(this, k, g.extend(Y, R), L, E, M) || this, W;
      }
      return P.prototype.updateRows = function() {
        var k = this.s.dtPane.rows({ selected: !0 }).data().toArray(), R;
        if (this.s.colOpts.options || this.s.customPaneSettings && this.s.customPaneSettings.options) {
          this._getComparisonRows();
          for (var L = this.s.dtPane.rows().toArray()[0], E = 0; E < L.length; E++) {
            var M = this.s.dtPane.row(L[E]), W = M.data();
            if (W !== void 0) {
              if (W.shown === 0) {
                M.remove(), L = this.s.dtPane.rows().toArray()[0], E--;
                continue;
              }
              for (var Y = 0, J = k; Y < J.length; Y++)
                if (R = J[Y], W.filter === R.filter) {
                  M.select(), k.splice(E, 1), this.s.selections.push(W.filter);
                  break;
                }
            }
          }
        } else {
          if (!this.s.dt.page.info().serverSide) {
            this._activePopulatePane(), this.s.rowData.binsShown = {};
            for (var X = 0, Q = this.s.dt.rows({ search: "applied" }).indexes().toArray(); X < Q.length; X++) {
              var ee = Q[X];
              this._updateShown(ee, this.s.dt.settings()[0], this.s.rowData.binsShown);
            }
          }
          this.s.dtPane.rows().remove();
          for (var ie = 0, oe = this.s.rowData.arrayFilter; ie < oe.length; ie++) {
            var ce = oe[ie];
            if (ce.shown !== 0)
              for (var he = this.addRow(ce.display, ce.filter, ce.sort, ce.type, void 0), we = 0; we < k.length; we++) {
                var Oe = k[we];
                if (Oe.filter === ce.filter) {
                  he.select(), k.splice(we, 1), this.s.selections.push(ce.filter);
                  break;
                }
              }
          }
          for (var Ve = 0, Ie = k; Ve < Ie.length; Ve++) {
            R = Ie[Ve];
            for (var Ze = 0, et = this.s.rowData.arrayOriginal; Ze < et.length; Ze++) {
              var He = et[Ze];
              if (He.filter === R.filter) {
                var Ke = this.addRow(He.display, He.filter, He.sort, He.type, void 0);
                Ke.select(), this.s.selections.push(He.filter);
              }
            }
          }
        }
        this.s.dtPane.draw(), this.s.dtPane.table().node().parentNode.scrollTop = this.s.scrollTop, this.s.dt.page.info().serverSide || this.s.dt.draw(!1);
      }, P.prototype._activePopulatePane = function() {
        this.s.rowData.arrayFilter = [], this.s.rowData.bins = {};
        var k = this.s.dt.settings()[0];
        if (!this.s.dt.page.info().serverSide)
          for (var R = 0, L = this.s.dt.rows({ search: "applied" }).indexes().toArray(); R < L.length; R++) {
            var E = L[R];
            this._populatePaneArray(E, this.s.rowData.arrayFilter, k);
          }
      }, P.prototype._getComparisonRows = function() {
        var k = this.s.colOpts.options ? this.s.colOpts.options : this.s.customPaneSettings && this.s.customPaneSettings.options ? this.s.customPaneSettings.options : void 0;
        if (k !== void 0) {
          var R = this.s.dt.rows(), L = this.s.dt.rows({ search: "applied" }), E = R.data().toArray(), M = L.data().toArray(), W = [];
          this.s.dtPane.clear(), this.s.indexes = [];
          for (var Y = 0, J = k; Y < J.length; Y++) {
            var X = J[Y], Q = X.label !== "" ? X.label : this.emptyMessage(), ee = {
              className: X.className,
              display: Q,
              filter: typeof X.value == "function" ? X.value : [],
              shown: 0,
              sort: Q,
              total: 0,
              type: Q
            };
            if (typeof X.value == "function") {
              for (var ie = 0; ie < E.length; ie++)
                X.value.call(this.s.dt, E[ie], R[0][ie]) && ee.total++;
              for (var oe = 0; oe < M.length; oe++)
                X.value.call(this.s.dt, M[oe], L[0][oe]) && ee.shown++;
              typeof ee.filter != "function" && ee.filter.push(X.filter);
            }
            W.push(this.addRow(ee.display, ee.filter, ee.sort, ee.type, ee.className, ee.total, ee.shown));
          }
          return W;
        }
      }, P.prototype._getMessage = function(k) {
        return this.s.dt.i18n("searchPanes.count", this.c.i18n.count).replace(/{total}/g, k.total).replace(/{shown}/g, k.shown);
      }, P.prototype._getShown = function(k) {
        return this.s.rowData.binsShown && this.s.rowData.binsShown[k] ? this.s.rowData.binsShown[k] : 0;
      }, P.prototype._shouldAddRow = function(k) {
        return k.shown > 0;
      }, P;
    }(s)
  ), C = window && window.__extends || /* @__PURE__ */ function() {
    var F = function(P, k) {
      return F = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(R, L) {
        R.__proto__ = L;
      } || function(R, L) {
        for (var E in L)
          L.hasOwnProperty(E) && (R[E] = L[E]);
      }, F(P, k);
    };
    return function(P, k) {
      F(P, k);
      function R() {
        this.constructor = P;
      }
      P.prototype = k === null ? Object.create(k) : (R.prototype = k.prototype, new R());
    };
  }(), m;
  function h(F) {
    m = F;
  }
  var u = (
    /** @class */
    function(F) {
      C(P, F);
      function P(k, R, L, E, M) {
        var W = this, Y = {
          i18n: {
            count: "{total}",
            countFiltered: "{shown} ({total})"
          }
        };
        return W = F.call(this, k, m.extend(Y, R), L, E, M) || this, W;
      }
      return P.prototype._activePopulatePane = function() {
        this.s.rowData.arrayFilter = [], this.s.rowData.binsShown = {};
        var k = this.s.dt.settings()[0];
        if (!this.s.dt.page.info().serverSide)
          for (var R = 0, L = this.s.dt.rows({ search: "applied" }).indexes().toArray(); R < L.length; R++) {
            var E = L[R];
            this._populatePaneArray(E, this.s.rowData.arrayFilter, k, this.s.rowData.binsShown);
          }
      }, P.prototype._getMessage = function(k) {
        var R = this.s.dt.i18n("searchPanes.count", this.c.i18n.count), L = this.s.dt.i18n("searchPanes.countFiltered", this.c.i18n.countFiltered);
        return (this.s.filteringActive ? L : R).replace(/{total}/g, k.total).replace(/{shown}/g, k.shown);
      }, P;
    }(w)
  ), p, v;
  function S(F) {
    p = F, v = F.fn.dataTable;
  }
  var T = (
    /** @class */
    function() {
      function F(P, k, R, L) {
        var E = this;
        if (R === void 0 && (R = !1), L === void 0 && (L = r), !v || !v.versionCheck || !v.versionCheck("1.10.0"))
          throw new Error("SearchPane requires DataTables 1.10 or newer");
        if (!v.select)
          throw new Error("SearchPane requires Select");
        var M = new v.Api(P);
        if (this.classes = p.extend(!0, {}, F.classes), this.c = p.extend(!0, {}, F.defaults, k), this.dom = {
          clearAll: p('<button type="button"/>').addClass(this.classes.clearAll).html(M.i18n("searchPanes.clearMessage", this.c.i18n.clearMessage)),
          collapseAll: p('<button type="button"/>').addClass(this.classes.collapseAll).html(M.i18n("searchPanes.collapseMessage", this.c.i18n.collapseMessage)),
          container: p("<div/>").addClass(this.classes.panes).html(M.i18n("searchPanes.loadMessage", this.c.i18n.loadMessage)),
          emptyMessage: p("<div/>").addClass(this.classes.emptyMessage),
          panes: p("<div/>").addClass(this.classes.container),
          showAll: p('<button type="button"/>').addClass(this.classes.showAll).addClass(this.classes.disabledButton).attr("disabled", "true").html(M.i18n("searchPanes.showMessage", this.c.i18n.showMessage)),
          title: p("<div/>").addClass(this.classes.title),
          titleRow: p("<div/>").addClass(this.classes.titleRow)
        }, this.s = {
          colOpts: [],
          dt: M,
          filterCount: 0,
          minPaneWidth: 260,
          page: 0,
          paging: !1,
          pagingST: !1,
          paneClass: L,
          panes: [],
          selectionList: [],
          serverData: {},
          stateRead: !1,
          updating: !1
        }, !M.settings()[0]._searchPanes) {
          if (p(document).on("draw.dt", function(Y) {
            E.dom.container.find(Y.target).length && E._updateFilterCount();
          }), this._getState(), this.s.dt.page.info().serverSide) {
            var W = this.s.dt.settings()[0];
            this.s.dt.on("preXhr.dtsps", function(Y, J, X) {
              if (W === J) {
                X.searchPanes === void 0 && (X.searchPanes = {}), X.searchPanes_null === void 0 && (X.searchPanes_null = {});
                for (var Q, ee = 0, ie = E.s.selectionList; ee < ie.length; ee++) {
                  var oe = ie[ee];
                  Q = E.s.dt.column(oe.column).dataSrc(), X.searchPanes[Q] === void 0 && (X.searchPanes[Q] = {}), X.searchPanes_null[Q] === void 0 && (X.searchPanes_null[Q] = {});
                  for (var ce = 0; ce < oe.rows.length; ce++)
                    X.searchPanes[Q][ce] = oe.rows[ce], X.searchPanes[Q][ce] === null ? X.searchPanes_null[Q][ce] = !0 : X.searchPanes_null[Q][ce] = !1;
                }
                E.s.selectionList.length > 0 && (X.searchPanesLast = Q), X.searchPanes_options = {
                  cascade: E.c.cascadePanes,
                  viewCount: E.c.viewCount,
                  viewTotal: E.c.viewTotal
                };
              }
            });
          }
          return this._setXHR(), M.settings()[0]._searchPanes = this, this.s.dt.settings()[0]._bInitComplete || R ? this._paneDeclare(M, P, k) : M.one("preInit.dtsps", function() {
            E._paneDeclare(M, P, k);
          }), this;
        }
      }
      return F.prototype.clearSelections = function() {
        for (var P, k = 0, R = this.s.panes; k < R.length; k++)
          P = R[k], P.s.dtPane && (P.s.scrollTop = P.s.dtPane.table().node().parentNode.scrollTop);
        var L = this.dom.container.find("." + this.classes.search.replace(/\s+/g, "."));
        L.each(function() {
          p(this).val("").trigger("input");
        }), this.s.selectionList = [];
        for (var E = [], M = 0, W = this.s.panes; M < W.length; M++)
          P = W[M], P.s.dtPane && E.push(P.clearPane());
        return E;
      }, F.prototype.getNode = function() {
        return this.dom.container;
      }, F.prototype.rebuild = function(P, k) {
        P === void 0 && (P = !1), k === void 0 && (k = !1), this.dom.emptyMessage.detach(), P === !1 && this.dom.panes.empty();
        for (var R = [], L = 0, E = this.s.panes; L < E.length; L++) {
          var M = E[L];
          (P === !1 || M.s.index === P) && (M.clearData(), M.rebuildPane(this.s.dt.page.info().serverSide ? this.s.serverData : void 0, k), this.dom.panes.append(M.dom.container), R.push(M));
        }
        return this._updateSelection(), this._updateFilterCount(), this._attachPaneContainer(), this._initSelectionListeners(!1), this.s.dt.draw(!k), this.resizePanes(), R.length === 1 ? R[0] : R;
      }, F.prototype.resizePanes = function() {
        var P;
        if (this.c.layout === "auto") {
          for (var k = p(this.s.dt.searchPanes.container()).width(), R = Math.floor(k / this.s.minPaneWidth), L = 1, E = 0, M = [], W = 0, Y = this.s.panes; W < Y.length; W++)
            P = Y[W], P.s.displayed && M.push(P.s.index);
          var J = M.length;
          if (R === J)
            L = R;
          else
            for (var X = R; X > 1; X--) {
              var Q = J % X;
              if (Q === 0) {
                L = X, E = 0;
                break;
              } else
                Q > E && (L = X, E = Q);
            }
          var ee = E !== 0 ? M.slice(M.length - E, M.length) : [];
          this.s.panes.forEach(function(ce) {
            ce.s.displayed && ce.resize("columns-" + (ee.includes(ce.s.index) ? E : L));
          });
        } else
          for (var ie = 0, oe = this.s.panes; ie < oe.length; ie++)
            P = oe[ie], P.adjustTopRow();
        return this;
      }, F.prototype._initSelectionListeners = function(P) {
      }, F.prototype._serverTotals = function() {
      }, F.prototype._setXHR = function() {
        var P = this, k = this.s.dt.settings()[0], R = function(L) {
          L && L.searchPanes && L.searchPanes.options && (P.s.serverData = L, P.s.serverData.tableLength = L.recordsTotal, P._serverTotals());
        };
        this.s.dt.on("xhr.dtsps", function(L, E, M) {
          k === E && R(M);
        }), R(this.s.dt.ajax.json());
      }, F.prototype._stateLoadListener = function() {
        var P = this, k = this.s.dt.settings()[0];
        this.s.dt.on("stateLoadParams.dtsps", function(R, L, E) {
          if (!(E.searchPanes === void 0 || L !== k)) {
            if (P.clearSelections(), P.s.selectionList = E.searchPanes.selectionList ? E.searchPanes.selectionList : [], E.searchPanes.panes)
              for (var M = 0, W = E.searchPanes.panes; M < W.length; M++)
                for (var Y = W[M], J = 0, X = P.s.panes; J < X.length; J++) {
                  var Q = X[J];
                  Y.id === Q.s.index && Q.s.dtPane && (Q.dom.searchBox.val(Y.searchTerm), Q.s.dtPane.order(Y.order));
                }
            P._makeSelections(P.s.selectionList);
          }
        });
      }, F.prototype._updateSelection = function() {
        this.s.selectionList = [];
        for (var P = 0, k = this.s.panes; P < k.length; P++) {
          var R = k[P];
          if (R.s.dtPane) {
            var L = R.s.dtPane.rows({ selected: !0 }).data().toArray().map(function(E) {
              return E.filter;
            });
            L.length && this.s.selectionList.push({
              column: R.s.index,
              rows: L
            });
          }
        }
      }, F.prototype._attach = function() {
        var P = this;
        this.dom.titleRow.removeClass(this.classes.hide).detach().append(this.dom.title), this.c.clear && this.dom.clearAll.appendTo(this.dom.titleRow).off("click.dtsps").on("click.dtsps", function() {
          return P.clearSelections();
        }), this.c.collapse && (this.dom.showAll.appendTo(this.dom.titleRow), this.dom.collapseAll.appendTo(this.dom.titleRow), this._setCollapseListener());
        for (var k = 0, R = this.s.panes; k < R.length; k++) {
          var L = R[k];
          this.dom.panes.append(L.dom.container);
        }
        this.dom.container[0].childNodes.forEach(function(E) {
          return E.remove();
        }), this.dom.container.removeClass(this.classes.hide).append(this.dom.titleRow).append(this.dom.panes), this.s.panes.forEach(function(E) {
          return E.setListeners();
        }), p("div." + this.classes.container).length === 0 && this.dom.container.prependTo(this.s.dt);
      }, F.prototype._attachMessage = function() {
        var P;
        try {
          P = this.s.dt.i18n("searchPanes.emptyPanes", this.c.i18n.emptyPanes);
        } catch {
          P = null;
        }
        if (P === null) {
          this.dom.container.addClass(this.classes.hide), this.dom.titleRow.removeClass(this.classes.hide);
          return;
        }
        this.dom.container.removeClass(this.classes.hide), this.dom.titleRow.addClass(this.classes.hide), this.dom.emptyMessage.html(P).appendTo(this.dom.container);
      }, F.prototype._attachPaneContainer = function() {
        for (var P = 0, k = this.s.panes; P < k.length; P++) {
          var R = k[P];
          if (R.s.displayed === !0) {
            this._attach();
            return;
          }
        }
        this._attachMessage();
      }, F.prototype._checkCollapse = function() {
        for (var P = !0, k = !0, R = 0, L = this.s.panes; R < L.length; R++) {
          var E = L[R];
          E.s.displayed && (E.dom.collapseButton.hasClass(E.classes.rotated) ? (this.dom.showAll.removeClass(this.classes.disabledButton).removeAttr("disabled"), k = !1) : (this.dom.collapseAll.removeClass(this.classes.disabledButton).removeAttr("disabled"), P = !1));
        }
        P && this.dom.collapseAll.addClass(this.classes.disabledButton).attr("disabled", "true"), k && this.dom.showAll.addClass(this.classes.disabledButton).attr("disabled", "true");
      }, F.prototype._checkMessage = function() {
        for (var P = 0, k = this.s.panes; P < k.length; P++) {
          var R = k[P];
          if (R.s.displayed === !0) {
            this.dom.emptyMessage.detach(), this.dom.titleRow.removeClass(this.classes.hide);
            return;
          }
        }
        this._attachMessage();
      }, F.prototype._collapseAll = function() {
        for (var P = 0, k = this.s.panes; P < k.length; P++) {
          var R = k[P];
          R.collapse();
        }
      }, F.prototype._findPane = function(P) {
        for (var k = 0, R = this.s.panes; k < R.length; k++) {
          var L = R[k];
          if (P === L.s.name)
            return L;
        }
      }, F.prototype._getState = function() {
        var P = this.s.dt.state.loaded();
        P && P.searchPanes && P.searchPanes.selectionList && (this.s.selectionList = P.searchPanes.selectionList);
      }, F.prototype._makeSelections = function(P) {
        for (var k = 0, R = P; k < R.length; k++) {
          for (var L = R[k], E = void 0, M = 0, W = this.s.panes; M < W.length; M++) {
            var Y = W[M];
            if (Y.s.index === L.column) {
              E = Y;
              break;
            }
          }
          if (E && E.s.dtPane) {
            for (var J = 0; J < E.s.dtPane.rows().data().toArray().length; J++)
              L.rows.includes(typeof E.s.dtPane.row(J).data().filter == "function" ? E.s.dtPane.cell(J, 0).data() : E.s.dtPane.row(J).data().filter) && E.s.dtPane.row(J).select();
            E.updateTable();
          }
        }
      }, F.prototype._paneDeclare = function(P, k, R) {
        var L = this;
        P.columns(this.c.columns.length > 0 ? this.c.columns : void 0).eq(0).each(function(Y) {
          L.s.panes.push(new L.s.paneClass(k, R, Y, L.dom.panes));
        });
        for (var E = P.columns().eq(0).toArray().length, M = 0; M < this.c.panes.length; M++) {
          var W = E + M;
          this.s.panes.push(new this.s.paneClass(k, R, W, this.dom.panes, this.c.panes[M]));
        }
        this.c.order.length > 0 && (this.s.panes = this.c.order.map(function(Y) {
          return L._findPane(Y);
        })), this.s.dt.settings()[0]._bInitComplete ? this._startup(P) : v.versionCheck("2") ? this.s.dt.settings()[0].aoInitComplete.push(function() {
          return L._startup(P);
        }) : this.s.dt.settings()[0].aoInitComplete.push({
          fn: function() {
            return L._startup(P);
          }
        });
      }, F.prototype._setCollapseListener = function() {
        var P = this;
        this.dom.collapseAll.off("click.dtsps").on("click.dtsps", function() {
          P._collapseAll(), P.dom.collapseAll.addClass(P.classes.disabledButton).attr("disabled", "true"), P.dom.showAll.removeClass(P.classes.disabledButton).removeAttr("disabled"), P.s.dt.state.save();
        }), this.dom.showAll.off("click.dtsps").on("click.dtsps", function() {
          P._showAll(), P.dom.showAll.addClass(P.classes.disabledButton).attr("disabled", "true"), P.dom.collapseAll.removeClass(P.classes.disabledButton).removeAttr("disabled"), P.s.dt.state.save();
        });
        for (var k = 0, R = this.s.panes; k < R.length; k++) {
          var L = R[k];
          L.dom.topRow.off("collapse.dtsps").on("collapse.dtsps", function() {
            return P._checkCollapse();
          });
        }
        this._checkCollapse();
      }, F.prototype._showAll = function() {
        for (var P = 0, k = this.s.panes; P < k.length; P++) {
          var R = k[P];
          R.show();
        }
      }, F.prototype._startup = function(P) {
        var k = this;
        this._attach(), this.dom.panes.empty();
        for (var R = this.s.dt.settings()[0], L = 0, E = this.s.panes; L < E.length; L++) {
          var M = E[L];
          M.rebuildPane(Object.keys(this.s.serverData).length > 0 ? this.s.serverData : void 0), this.dom.panes.append(M.dom.container);
        }
        this.c.layout === "auto" && this.resizePanes();
        var W = this.s.dt.state.loaded();
        !this.s.stateRead && W && this.s.dt.page(W.start / this.s.dt.page.len()).draw("page"), this.s.stateRead = !0, this._checkMessage(), P.on("preDraw.dtsps", function() {
          !k.s.updating && !k.s.paging && (k._updateFilterCount(), k._updateSelection()), k.s.paging = !1;
        }), p(window).on("resize.dtsps", v.util.throttle(function() {
          return k.resizePanes();
        })), this.s.dt.on("stateSaveParams.dtsps", function(J, X, Q) {
          X === R && (Q.searchPanes === void 0 && (Q.searchPanes = {}), Q.searchPanes.selectionList = k.s.selectionList);
        }), this._stateLoadListener(), P.off("page.dtsps page-nc.dtsps").on("page.dtsps page-nc.dtsps", function(J, X) {
          k.s.paging = !0, k.s.pagingST = !0, k.s.page = k.s.dt.page();
        }), this.s.dt.page.info().serverSide ? P.off("preXhr.dtsps").on("preXhr.dtsps", function(J, X, Q) {
          if (X === R) {
            Q.searchPanes || (Q.searchPanes = {}), Q.searchPanes_null || (Q.searchPanes_null = {});
            for (var ee = 0, ie = 0, oe = k.s.panes; ie < oe.length; ie++) {
              var ce = oe[ie], he = k.s.dt.column(ce.s.index).dataSrc();
              if (Q.searchPanes[he] || (Q.searchPanes[he] = {}), Q.searchPanes_null[he] || (Q.searchPanes_null[he] = {}), ce.s.dtPane)
                for (var we = ce.s.dtPane.rows({ selected: !0 }).data().toArray(), Oe = 0; Oe < we.length; Oe++)
                  Q.searchPanes[he][Oe] = we[Oe].filter, Q.searchPanes[he][Oe] ? Q.searchPanes_null[he][Oe] = !1 : Q.searchPanes_null[he][Oe] = !0, ee++;
            }
            ee > 0 && (ee !== k.s.filterCount && (Q.start = 0, k.s.page = 0, k.s.dt.page(k.s.page)), k.s.filterCount = ee), k.s.selectionList.length > 0 && (Q.searchPanesLast = k.s.dt.column(k.s.selectionList[k.s.selectionList.length - 1].column).dataSrc()), Q.searchPanes_options = {
              cascade: k.c.cascadePanes,
              viewCount: k.c.viewCount,
              viewTotal: k.c.viewTotal
            };
          }
        }) : P.on("preXhr.dtsps", function() {
          return k.s.panes.forEach(function(J) {
            return J.clearData();
          });
        }), this.s.dt.on("xhr.dtsps", function(J, X) {
          if (X.nTable === k.s.dt.table().node() && !k.s.dt.page.info().serverSide) {
            var Q = !1;
            k.s.dt.one("preDraw.dtsps", function() {
              if (!Q) {
                var ee = k.s.dt.page();
                Q = !0, k.s.updating = !0, k.dom.panes.empty();
                for (var ie = 0, oe = k.s.panes; ie < oe.length; ie++) {
                  var ce = oe[ie];
                  ce.clearData(), ce.rebuildPane(void 0, !0), k.dom.panes.append(ce.dom.container);
                }
                k.s.dt.page.info().serverSide || k.s.dt.draw(), k.s.updating = !1, k._updateSelection(), k._checkMessage(), k.s.dt.one("draw.dtsps", function() {
                  k.s.updating = !0, k.s.dt.page(ee).draw(!1), k.s.updating = !1;
                });
              }
            });
          }
        });
        var Y = this.c.preSelect;
        W && W.searchPanes && W.searchPanes.selectionList && (Y = W.searchPanes.selectionList), this._makeSelections(Y), this._updateFilterCount(), P.on("destroy.dtsps", function(J, X) {
          if (X === R) {
            for (var Q = 0, ee = k.s.panes; Q < ee.length; Q++) {
              var ie = ee[Q];
              ie.destroy();
            }
            P.off(".dtsps"), k.dom.showAll.off(".dtsps"), k.dom.clearAll.off(".dtsps"), k.dom.collapseAll.off(".dtsps"), p(P.table().node()).off(".dtsps"), k.dom.container.detach(), k.clearSelections();
          }
        }), this.c.collapse && this._setCollapseListener(), this.c.clear && this.dom.clearAll.off("click.dtsps").on("click.dtsps", function() {
          return k.clearSelections();
        }), R._searchPanes = this, this.s.dt.state.save();
      }, F.prototype._updateFilterCount = function() {
        for (var P = 0, k = 0, R = 0, L = this.s.panes; R < L.length; R++) {
          var E = L[R];
          E.s.dtPane && (P += E.getPaneCount(), E.s.dtPane.search() && k++);
        }
        this.dom.title.html(this.s.dt.i18n("searchPanes.title", this.c.i18n.title, P)), this.c.filterChanged && typeof this.c.filterChanged == "function" && this.c.filterChanged.call(this.s.dt, P), P === 0 && k === 0 ? this.dom.clearAll.addClass(this.classes.disabledButton).attr("disabled", "true") : this.dom.clearAll.removeClass(this.classes.disabledButton).removeAttr("disabled");
      }, F.version = "2.3.5", F.classes = {
        clear: "dtsp-clear",
        clearAll: "dtsp-clearAll",
        collapseAll: "dtsp-collapseAll",
        container: "dtsp-searchPanes",
        disabledButton: "dtsp-disabledButton",
        emptyMessage: "dtsp-emptyMessage",
        hide: "dtsp-hidden",
        panes: "dtsp-panesContainer",
        search: "dtsp-search",
        showAll: "dtsp-showAll",
        title: "dtsp-title",
        titleRow: "dtsp-titleRow"
      }, F.defaults = {
        cascadePanes: !1,
        clear: !0,
        collapse: !0,
        columns: [],
        container: function(P) {
          return P.table().container();
        },
        filterChanged: void 0,
        i18n: {
          clearMessage: "Clear All",
          clearPane: "&times;",
          collapse: {
            0: "SearchPanes",
            _: "SearchPanes (%d)"
          },
          collapseMessage: "Collapse All",
          count: "{total}",
          emptyMessage: "<em>Empty</em>",
          emptyPanes: "No SearchPanes",
          loadMessage: "Loading Search Panes...",
          showMessage: "Show All",
          title: "Filters Active - %d"
        },
        layout: "auto",
        order: [],
        panes: [],
        preSelect: [],
        viewCount: !0,
        viewTotal: !1
      }, F;
    }()
  ), A = window && window.__extends || /* @__PURE__ */ function() {
    var F = function(P, k) {
      return F = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(R, L) {
        R.__proto__ = L;
      } || function(R, L) {
        for (var E in L)
          L.hasOwnProperty(E) && (R[E] = L[E]);
      }, F(P, k);
    };
    return function(P, k) {
      F(P, k);
      function R() {
        this.constructor = P;
      }
      P.prototype = k === null ? Object.create(k) : (R.prototype = k.prototype, new R());
    };
  }(), y = (
    /** @class */
    function(F) {
      A(P, F);
      function P(k, R, L) {
        L === void 0 && (L = !1);
        var E = this, M;
        R.cascadePanes && R.viewTotal ? M = u : R.cascadePanes ? M = w : R.viewTotal && (M = c), E = F.call(this, k, R, L, M) || this;
        var W = E.s.dt, Y = W.state.loaded(), J = function() {
          return E._initSelectionListeners(!0, Y && Y.searchPanes && Y.searchPanes.selectionList ? Y.searchPanes.selectionList : E.c.preSelect);
        };
        return W.settings()[0]._bInitComplete ? J() : W.off("init.dtsps").on("init.dtsps", J), E;
      }
      return P.prototype._initSelectionListeners = function(k, R) {
        k === void 0 && (k = !0), R === void 0 && (R = []), k && (this.s.selectionList = R);
        for (var L = 0, E = this.s.panes; L < E.length; L++) {
          var M = E[L];
          M.s.displayed && M.s.dtPane.off("select.dtsp").on("select.dtsp", this._update(M)).off("deselect.dtsp").on("deselect.dtsp", this._updateTimeout(M));
        }
        this.s.dt.off("draw.dtsps").on("draw.dtsps", this._update()), this._updateSelectionList();
      }, P.prototype._serverTotals = function() {
        for (var k = 0, R = this.s.panes; k < R.length; k++) {
          var L = R[k];
          if (L.s.colOpts.show) {
            var E = this.s.dt.column(L.s.index).dataSrc(), M = !0;
            if (this.s.serverData.searchPanes.options[E])
              for (var W = 0, Y = this.s.serverData.searchPanes.options[E]; W < Y.length; W++) {
                var J = Y[W];
                if (J.total !== J.count) {
                  M = !1;
                  break;
                }
              }
            L.s.filteringActive = !M, L._serverPopulate(this.s.serverData);
          }
        }
      }, P.prototype._stateLoadListener = function() {
        var k = this, R = function(L, E, M) {
          if (M.searchPanes !== void 0) {
            if (k.s.selectionList = M.searchPanes.selectionList ? M.searchPanes.selectionList : [], M.searchPanes.panes)
              for (var W = 0, Y = M.searchPanes.panes; W < Y.length; W++)
                for (var J = Y[W], X = 0, Q = k.s.panes; X < Q.length; X++) {
                  var ee = Q[X];
                  J.id === ee.s.index && ee.s.dtPane && (ee.dom.searchBox.val(J.searchTerm), ee.s.dtPane.order(J.order));
                }
            k._updateSelectionList();
          }
        };
        this.s.dt.off("stateLoadParams.dtsps", R).on("stateLoadParams.dtsps", R);
      }, P.prototype._updateSelection = function() {
      }, P.prototype._update = function(k) {
        var R = this;
        return k === void 0 && (k = void 0), function() {
          k && clearTimeout(k.s.deselectTimeout), R._updateSelectionList(k);
        };
      }, P.prototype._updateTimeout = function(k) {
        var R = this;
        return k === void 0 && (k = void 0), function() {
          return k ? k.s.deselectTimeout = setTimeout(function() {
            return R._updateSelectionList(k);
          }, 50) : R._updateSelectionList();
        };
      }, P.prototype._updateSelectionList = function(k) {
        if (k === void 0 && (k = void 0), this.s.pagingST) {
          this.s.pagingST = !1;
          return;
        } else if (this.s.updating || k && k.s.serverSelecting)
          return;
        if (k !== void 0) {
          this.s.dt.page.info().serverSide && k._updateSelection();
          var R = k.s.dtPane.rows({ selected: !0 }).data().toArray().map(function(L) {
            return L.filter;
          });
          this.s.selectionList = this.s.selectionList.filter(function(L) {
            return L.column !== k.s.index;
          }), R.length > 0 ? (this.s.selectionList.push({
            column: k.s.index,
            rows: R
          }), k.dom.clear.removeClass(this.classes.disabledButton).removeAttr("disabled")) : k.dom.clear.addClass(this.classes.disabledButton).attr("disabled", "true"), this.s.dt.page.info().serverSide && this.s.dt.draw(!1);
        }
        this._remakeSelections(), this._updateFilterCount();
      }, P.prototype._remakeSelections = function() {
        var k, R;
        if (this.s.updating = !0, this.s.dt.page.info().serverSide) {
          this.s.selectionList.length > 0 && (R = this.s.panes[this.s.selectionList[this.s.selectionList.length - 1].column]);
          for (var St = 0, ut = this.s.panes; St < ut.length; St++)
            k = ut[St], k.s.displayed && (!R || k.s.index !== R.s.index) && k.updateRows();
        } else {
          var L = this.s.selectionList, E = !1;
          this.clearSelections(), this.s.dt.draw(!1), this.s.dt.rows().toArray()[0].length > this.s.dt.rows({ search: "applied" }).toArray()[0].length && (E = !0), this.s.selectionList = L;
          for (var M = 0, W = this.s.panes; M < W.length; M++)
            R = W[M], R.s.displayed && (R.s.filteringActive = E, R.updateRows());
          for (var Y = 0, J = this.s.selectionList; Y < J.length; Y++) {
            var X = J[Y];
            R = null;
            for (var Q = 0, ee = this.s.panes; Q < ee.length; Q++) {
              var ie = ee[Q];
              if (ie.s.index === X.column) {
                R = ie;
                break;
              }
            }
            if (R.s.dtPane) {
              for (var oe = R.s.dtPane.rows().indexes().toArray(), ce = 0; ce < X.rows.length; ce++) {
                for (var he = !1, we = 0, Oe = oe; we < Oe.length; we++) {
                  var Ve = Oe[we], Ie = R.s.dtPane.row(Ve), Ze = Ie.data();
                  X.rows[ce] === Ze.filter && (Ie.select(), he = !0);
                }
                he || (X.rows.splice(ce, 1), ce--);
              }
              if (R.s.selections = X.rows, X.rows.length !== 0) {
                this.s.dt.draw();
                for (var et = !1, He = 0, Ke = 0, Ct = 0, yt = 0, wt = this.s.panes; yt < wt.length; yt++)
                  k = wt[yt], k.s.dtPane && (He += k.getPaneCount(), He > Ke && (Ct++, Ke = He));
                et = He > 0;
                for (var rt = 0, mt = this.s.panes; rt < mt.length; rt++)
                  k = mt[rt], k.s.displayed && (E || R.s.index !== k.s.index || !et ? k.s.filteringActive = et || E : Ct === 1 && (k.s.filteringActive = !1), k.s.index !== R.s.index && k.updateRows());
              }
            }
          }
          this.s.dt.draw(!1);
        }
        this.s.updating = !1;
      }, P;
    }(T)
  );
  /*! SearchPanes 2.3.5
   * © SpryMedia Ltd - datatables.net/license
   */
  e(On), S(On), l(On), b(On), h(On);
  var I = On.fn.dataTable;
  I.SearchPanes = T, H.SearchPanes = T, I.SearchPanesST = y, H.SearchPanesST = y, I.SearchPane = r, H.SearchPane = r, I.SearchPaneViewTotal = c, H.SearchPaneViewTotal = c, I.SearchPaneCascade = w, H.SearchPaneCascade = w, I.SearchPaneCascadeViewTotal = u, H.SearchPaneCascadeViewTotal = u;
  var $ = On.fn.dataTable.Api.register;
  $("searchPanes()", function() {
    return this;
  }), $("searchPanes.clearSelections()", function() {
    return this.iterator("table", function(F) {
      F._searchPanes && F._searchPanes.clearSelections();
    });
  }), $("searchPanes.rebuildPane()", function(F, P) {
    return this.iterator("table", function(k) {
      k._searchPanes && k._searchPanes.rebuild(F, P);
    });
  }), $("searchPanes.resizePanes()", function() {
    var F = this.context[0];
    return F._searchPanes ? F._searchPanes.resizePanes() : null;
  }), $("searchPanes.container()", function() {
    var F = this.context[0];
    return F._searchPanes ? F._searchPanes.getNode() : null;
  }), H.ext.buttons.searchPanesClear = {
    action: function(F, P) {
      P.searchPanes.clearSelections();
    },
    text: "Clear Panes"
  }, H.ext.buttons.searchPanes = {
    action: function(F, P, k, R) {
      var L = this, E = this;
      R._panes ? (this.popover(R._panes.getNode(), {
        align: "container",
        span: "container"
      }), R._panes.rebuild(void 0, !0)) : (this.processing(!0), setTimeout(function() {
        V(P, k, R), L.popover(R._panes.getNode(), {
          align: "container",
          span: "container"
        }), R._panes.rebuild(void 0, !0), On("table.dataTable", R._panes.getNode()).DataTable().columns.adjust(), E.processing(!1);
      }, 10));
    },
    init: function(F, P, k) {
      F.button(P).text(k.text || F.i18n("searchPanes.collapse", "SearchPanes", 0)), (F.init().stateSave || k.delayInit === !1) && V(F, P, k);
    },
    config: {},
    text: "",
    delayInit: !0
  };
  function V(F, P, k) {
    var R = On.extend({
      filterChanged: function(E) {
        F.button(P).text(F.i18n("searchPanes.collapse", F.context[0].oLanguage.searchPanes !== void 0 ? F.context[0].oLanguage.searchPanes.collapse : F.context[0]._searchPanes.c.i18n.collapse, E));
      }
    }, k.config), L = R && (R.cascadePanes || R.viewTotal) ? new H.SearchPanesST(F, R) : new H.SearchPanes(F, R);
    F.button(P).text(k.text || F.i18n("searchPanes.collapse", L.c.i18n.collapse, 0)), k._panes = L;
  }
  function z(F, P, k) {
    P === void 0 && (P = null), k === void 0 && (k = !1);
    var R = new I.Api(F), L = P || R.init().searchPanes || I.defaults.searchPanes, E = L && (L.cascadePanes || L.viewTotal) ? new y(R, L, k) : new T(R, L, k), M = E.getNode();
    return M;
  }
  On(document).on("preInit.dt.dtsp", function(F, P) {
    F.namespace === "dt" && (P.oInit.searchPanes || H.defaults.searchPanes) && (P._searchPanes || z(P, null, !0));
  }), H.ext.feature.push({
    cFeature: "P",
    fnInit: z
  }), H.feature && H.feature.register("searchPanes", z);
})();
/*! Bootstrap 5 integration for DataTables' SearchPanes
 * © SpryMedia Ltd - datatables.net/license
 */
let ff = lt;
ff.extend(!0, H.SearchPane.classes, {
  buttonGroup: "btn-group",
  disabledButton: "disabled",
  narrow: "col",
  pane: {
    container: "table"
  },
  paneButton: "btn btn-subtle",
  pill: "badge rounded-pill bg-secondary",
  search: "form-control search",
  table: "table table-sm table-borderless",
  topRow: "dtsp-topRow"
});
ff.extend(!0, H.SearchPanes.classes, {
  clearAll: "dtsp-clearAll btn btn-subtle",
  collapseAll: "dtsp-collapseAll btn btn-subtle",
  container: "dtsp-searchPanes",
  disabledButton: "disabled",
  panes: "dtsp-panes dtsp-panesContainer",
  search: H.SearchPane.classes.search,
  showAll: "dtsp-showAll btn btn-subtle",
  title: "dtsp-title",
  titleRow: "dtsp-titleRow"
});
/*! SearchBuilder 1.8.4
 * ©SpryMedia Ltd - datatables.net/license/mit
 */
let _r = lt;
(function() {
  var t, n;
  function e() {
    var h = H.use("moment");
    return h || window.moment;
  }
  function r() {
    var h = H.use("luxon");
    return h || window.luxon;
  }
  function i(h) {
    t = h, n = h.fn.dataTable;
  }
  var s = (
    /** @class */
    function() {
      function h(u, p, v, S, T, A, y) {
        S === void 0 && (S = 0), T === void 0 && (T = 1), A === void 0 && (A = void 0), y === void 0 && (y = !1);
        var I = this;
        this.classes = t.extend(!0, {}, h.classes), this.c = t.extend(!0, {}, h.defaults, t.fn.dataTable.ext.searchBuilder, p);
        var $ = this.c.i18n;
        if (this.s = {
          condition: void 0,
          conditions: {},
          data: void 0,
          dataIdx: -1,
          dataPoints: [],
          dateFormat: !1,
          depth: T,
          dt: u,
          filled: !1,
          index: S,
          liveSearch: y,
          origData: void 0,
          preventRedraw: !1,
          serverData: A,
          topGroup: v,
          type: "",
          value: []
        }, this.dom = {
          buttons: t("<div/>").addClass(this.classes.buttonContainer),
          condition: t("<select disabled/>").addClass(this.classes.condition).addClass(this.classes.dropDown).addClass(this.classes.italic).attr("autocomplete", "hacking"),
          conditionTitle: t('<option value="" disabled selected hidden/>').html(this.s.dt.i18n("searchBuilder.condition", $.condition)),
          container: t("<div/>").addClass(this.classes.container),
          data: t("<select/>").addClass(this.classes.data).addClass(this.classes.dropDown).addClass(this.classes.italic),
          dataTitle: t('<option value="" disabled selected hidden/>').html(this.s.dt.i18n("searchBuilder.data", $.data)),
          defaultValue: t("<select disabled/>").addClass(this.classes.value).addClass(this.classes.dropDown).addClass(this.classes.select).addClass(this.classes.italic),
          delete: t("<button/>").html(this.s.dt.i18n("searchBuilder.delete", $.delete)).addClass(this.classes.delete).addClass(this.classes.button).attr("title", this.s.dt.i18n("searchBuilder.deleteTitle", $.deleteTitle)).attr("type", "button"),
          inputCont: t("<div/>").addClass(this.classes.inputCont),
          // eslint-disable-next-line no-useless-escape
          left: t("<button/>").html(this.s.dt.i18n("searchBuilder.left", $.left)).addClass(this.classes.left).addClass(this.classes.button).attr("title", this.s.dt.i18n("searchBuilder.leftTitle", $.leftTitle)).attr("type", "button"),
          // eslint-disable-next-line no-useless-escape
          right: t("<button/>").html(this.s.dt.i18n("searchBuilder.right", $.right)).addClass(this.classes.right).addClass(this.classes.button).attr("title", this.s.dt.i18n("searchBuilder.rightTitle", $.rightTitle)).attr("type", "button"),
          value: [
            t("<select disabled/>").addClass(this.classes.value).addClass(this.classes.dropDown).addClass(this.classes.italic).addClass(this.classes.select)
          ],
          valueTitle: t('<option value="--valueTitle--" disabled selected hidden/>').html(this.s.dt.i18n("searchBuilder.value", $.value))
        }, this.c.greyscale) {
          this.dom.data.addClass(this.classes.greyscale), this.dom.condition.addClass(this.classes.greyscale), this.dom.defaultValue.addClass(this.classes.greyscale);
          for (var V = 0, z = this.dom.value; V < z.length; V++) {
            var F = z[V];
            F.addClass(this.classes.greyscale);
          }
        }
        return t(window).on("resize.dtsb", n.util.throttle(function() {
          I.s.topGroup.trigger("dtsb-redrawLogic");
        })), this._buildCriteria(), this;
      }
      return h._escapeHTML = function(u) {
        return u.toString().replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&amp;/g, "&");
      }, h.prototype.doSearch = function() {
        this.c.liveSearch && this.s.dt.draw();
      }, h.parseNumFmt = function(u) {
        return +u.replace(/(?!^-)[^0-9.]/g, "");
      }, h.prototype.updateArrows = function(u) {
        u === void 0 && (u = !1), this.dom.container.children().detach(), this.dom.container.append(this.dom.data).append(this.dom.condition).append(this.dom.inputCont), this.setListeners(), this.dom.value[0] !== void 0 && t(this.dom.value[0]).trigger("dtsb-inserted");
        for (var p = 1; p < this.dom.value.length; p++)
          this.dom.inputCont.append(this.dom.value[p]), t(this.dom.value[p]).trigger("dtsb-inserted");
        this.s.depth > 1 && this.dom.buttons.append(this.dom.left), (this.c.depthLimit === !1 || this.s.depth < this.c.depthLimit) && u ? this.dom.buttons.append(this.dom.right) : this.dom.right.remove(), this.dom.buttons.append(this.dom.delete), this.dom.container.append(this.dom.buttons);
      }, h.prototype.destroy = function() {
        this.dom.data.off(".dtsb"), this.dom.condition.off(".dtsb"), this.dom.delete.off(".dtsb");
        for (var u = 0, p = this.dom.value; u < p.length; u++) {
          var v = p[u];
          v.off(".dtsb");
        }
        this.dom.container.remove();
      }, h.prototype.search = function(u, p) {
        var v = this.s.dt.settings()[0], S = this.s.conditions[this.s.condition];
        if (this.s.condition !== void 0 && S !== void 0) {
          var T = u[this.s.dataIdx];
          if (this.s.type && this.s.type.includes("num") && (v.oLanguage.sDecimal !== "" || v.oLanguage.sThousands !== "")) {
            var A = [u[this.s.dataIdx]];
            if (v.oLanguage.sDecimal !== "" && (A = u[this.s.dataIdx].split(v.oLanguage.sDecimal)), v.oLanguage.sThousands !== "")
              for (var y = 0; y < A.length; y++)
                A[y] = A[y].replace(v.oLanguage.sThousands, ",");
            T = A.join(".");
          }
          if (this.c.orthogonal.search !== "filter" && (T = v.fastData(p, this.s.dataIdx, typeof this.c.orthogonal == "string" ? this.c.orthogonal : this.c.orthogonal.search)), this.s.type === "array") {
            Array.isArray(T) || (T = [T]), T.sort();
            for (var I = 0, $ = T; I < $.length; I++) {
              var V = $[I];
              V && typeof V == "string" && (V = V.replace(/[\r\n\u2028]/g, " "));
            }
          } else
            T !== null && typeof T == "string" && (T = T.replace(/[\r\n\u2028]/g, " "));
          return this.s.type.includes("html") && typeof T == "string" && (T = T.replace(/(<([^>]+)>)/ig, "")), T === null && (T = ""), S.search(T, this.s.value, this);
        }
      }, h.prototype.getDetails = function(u) {
        u === void 0 && (u = !1);
        var p, v = this.s.dt.settings()[0];
        if (this.s.type !== null && ["num", "num-fmt", "html-num", "html-num-fmt"].includes(this.s.type) && (v.oLanguage.sDecimal !== "" || v.oLanguage.sThousands !== ""))
          for (p = 0; p < this.s.value.length; p++) {
            var S = [this.s.value[p].toString()];
            if (v.oLanguage.sDecimal !== "" && (S = this.s.value[p].split(v.oLanguage.sDecimal)), v.oLanguage.sThousands !== "")
              for (var T = 0; T < S.length; T++)
                S[T] = S[T].replace(v.oLanguage.sThousands, ",");
            this.s.value[p] = S.join(".");
          }
        else if (this.s.type !== null && u) {
          var A = e(), y = r();
          if ((this.s.type.includes("date") || this.s.type.includes("time")) && !e && !r)
            for (p = 0; p < this.s.value.length; p++)
              this.s.value[p].match(/^\d{4}-([0]\d|1[0-2])-([0-2]\d|3[01])$/g) === null && (this.s.value[p] = "");
          else if (this.s.type.includes("moment") || this.s.type.includes("datetime") && e)
            for (p = 0; p < this.s.value.length; p++)
              this.s.value[p] && this.s.value[p].length > 0 && A(this.s.value[p], this.s.dateFormat, !0).isValid() && (this.s.value[p] = A(this.s.value[p], this.s.dateFormat).format("YYYY-MM-DD HH:mm:ss"));
          else if (this.s.type.includes("luxon") || this.s.type.includes("datetime") && r)
            for (p = 0; p < this.s.value.length; p++)
              this.s.value[p] && this.s.value[p].length > 0 && y.DateTime.fromFormat(this.s.value[p], this.s.dateFormat).invalid === null && (this.s.value[p] = y.DateTime.fromFormat(this.s.value[p], this.s.dateFormat).toFormat("yyyy-MM-dd HH:mm:ss"));
        }
        if (this.s.type && this.s.type.includes("num") && this.s.dt.page.info().serverSide)
          for (p = 0; p < this.s.value.length; p++)
            this.s.value[p] = this.s.value[p].replace(/[^0-9.\-]/g, "");
        return {
          condition: this.s.condition,
          data: this.s.data,
          origData: this.s.origData,
          type: this.s.type,
          value: this.s.value.map(function(I) {
            return I != null ? I.toString() : I;
          })
        };
      }, h.prototype.getNode = function() {
        return this.dom.container;
      }, h.prototype.parseNumber = function(u) {
        var p = this.s.dt.i18n("decimal");
        return p && p !== "." && (u = u.replace(/\./g, "").replace(p, ".")), +u.replace(/(?!^-)[^0-9.]/g, "");
      }, h.prototype.populate = function() {
        this._populateData(), this.s.dataIdx !== -1 && (this._populateCondition(), this.s.condition !== void 0 && this._populateValue());
      }, h.prototype.rebuild = function(u) {
        var p = !1, v, S;
        if (this._populateData(), u.data !== void 0) {
          var T = this.classes.italic, A = this.dom.data;
          this.dom.data.children("option").each(function() {
            !p && (t(this).text() === u.data || u.origData && t(this).prop("origData") === u.origData) ? (t(this).prop("selected", !0), A.removeClass(T), p = !0, v = parseInt(t(this).val(), 10)) : t(this).removeProp("selected");
          });
        }
        if (p) {
          this.s.data = u.data, this.s.origData = u.origData, this.s.dataIdx = v, this.c.orthogonal = this._getOptions().orthogonal, this.dom.dataTitle.remove(), this._populateCondition(), this.dom.conditionTitle.remove();
          var y = void 0, I = this.dom.condition.children("option");
          for (S = 0; S < I.length; S++) {
            var $ = t(I[S]);
            u.condition !== void 0 && $.val() === u.condition && typeof u.condition == "string" ? ($.prop("selected", !0), y = $.val()) : $.removeProp("selected");
          }
          if (this.s.condition = y, this.s.condition !== void 0) {
            for (this.dom.conditionTitle.removeProp("selected"), this.dom.conditionTitle.remove(), this.dom.condition.removeClass(this.classes.italic), S = 0; S < I.length; S++) {
              var V = t(I[S]);
              V.val() !== this.s.condition && V.removeProp("selected");
            }
            this._populateValue(u);
          } else
            this.dom.conditionTitle.prependTo(this.dom.condition).prop("selected", !0);
        }
      }, h.prototype.setListeners = function() {
        var u = this;
        this.dom.data.unbind("change").on("change.dtsb", function() {
          u.dom.dataTitle.removeProp("selected");
          for (var p = u.dom.data.children("option." + u.classes.option), v = 0; v < p.length; v++) {
            var S = t(p[v]);
            S.val() === u.dom.data.val() ? (u.dom.data.removeClass(u.classes.italic), S.prop("selected", !0), u.s.dataIdx = +S.val(), u.s.data = S.text(), u.s.origData = S.prop("origData"), u.c.orthogonal = u._getOptions().orthogonal, u._clearCondition(), u._clearValue(), u._populateCondition(), u.s.filled && (u.s.filled = !1, u.doSearch(), u.setListeners()), u.s.dt.state.save()) : S.removeProp("selected");
          }
        }), this.dom.condition.unbind("change").on("change.dtsb", function() {
          u.dom.conditionTitle.removeProp("selected");
          for (var p = u.dom.condition.children("option." + u.classes.option), v = 0; v < p.length; v++) {
            var S = t(p[v]);
            if (S.val() === u.dom.condition.val()) {
              u.dom.condition.removeClass(u.classes.italic), S.prop("selected", !0);
              for (var T = S.val(), A = 0, y = Object.keys(u.s.conditions); A < y.length; A++) {
                var I = y[A];
                if (I === T) {
                  u.s.condition = T;
                  break;
                }
              }
              u._clearValue(), u._populateValue();
              for (var $ = 0, V = u.dom.value; $ < V.length; $++) {
                var z = V[$];
                u.s.filled && z !== void 0 && u.dom.inputCont.has(z[0]).length !== 0 && (u.s.filled = !1, u.doSearch(), u.setListeners());
              }
              (u.dom.value.length === 0 || u.dom.value.length === 1 && u.dom.value[0] === void 0) && u.doSearch();
            } else
              S.removeProp("selected");
          }
        });
      }, h.prototype.setupButtons = function() {
        if (window.innerWidth > 550) {
          this.dom.container.removeClass(this.classes.vertical), this.dom.buttons.css("left", null), this.dom.buttons.css("top", null);
          return;
        }
        this.dom.container.addClass(this.classes.vertical), this.dom.buttons.css("left", this.dom.data.innerWidth()), this.dom.buttons.css("top", this.dom.data.position().top);
      }, h.prototype._buildCriteria = function() {
        this.dom.data.append(this.dom.dataTitle), this.dom.condition.append(this.dom.conditionTitle), this.dom.container.append(this.dom.data).append(this.dom.condition), this.dom.inputCont.empty();
        for (var u = 0, p = this.dom.value; u < p.length; u++) {
          var v = p[u];
          v.append(this.dom.valueTitle), this.dom.inputCont.append(v);
        }
        this.dom.buttons.append(this.dom.delete).append(this.dom.right), this.dom.container.append(this.dom.inputCont).append(this.dom.buttons), this.setListeners();
      }, h.prototype._clearCondition = function() {
        this.dom.condition.empty(), this.dom.conditionTitle.prop("selected", !0).attr("disabled", "true"), this.dom.condition.prepend(this.dom.conditionTitle).prop("selectedIndex", 0), this.s.conditions = {}, this.s.condition = void 0;
      }, h.prototype._clearValue = function() {
        var u;
        if (this.s.condition !== void 0) {
          if (this.dom.value.length > 0 && this.dom.value[0] !== void 0)
            for (var p = 0, v = this.dom.value; p < v.length; p++)
              u = v[p], u !== void 0 && setTimeout(function() {
                u.remove();
              }, 50);
          if (this.dom.value = [].concat(this.s.conditions[this.s.condition].init(this, h.updateListener)), this.dom.value.length > 0 && this.dom.value[0] !== void 0) {
            this.dom.inputCont.empty().append(this.dom.value[0]).insertAfter(this.dom.condition), t(this.dom.value[0]).trigger("dtsb-inserted");
            for (var S = 1; S < this.dom.value.length; S++)
              this.dom.inputCont.append(this.dom.value[S]), t(this.dom.value[S]).trigger("dtsb-inserted");
          }
        } else {
          for (var T = 0, A = this.dom.value; T < A.length; T++)
            u = A[T], u !== void 0 && setTimeout(function() {
              u.remove();
            }, 50);
          this.dom.valueTitle.prop("selected", !0), this.dom.defaultValue.append(this.dom.valueTitle).insertAfter(this.dom.condition);
        }
        this.s.value = [], this.dom.value = [
          t("<select disabled/>").addClass(this.classes.value).addClass(this.classes.dropDown).addClass(this.classes.italic).addClass(this.classes.select).append(this.dom.valueTitle.clone())
        ];
      }, h.prototype._getOptions = function() {
        var u = this.s.dt;
        return t.extend(!0, {}, h.defaults, u.settings()[0].aoColumns[this.s.dataIdx].searchBuilder);
      }, h.prototype._populateCondition = function() {
        var u = [], p = Object.keys(this.s.conditions).length, v = this.s.dt, S = v.settings()[0].aoColumns, T = +this.dom.data.children("option:selected").val(), A, y;
        if (p === 0) {
          if (this.s.type = v.column(T).type(), S !== void 0) {
            var I = S[T];
            I.searchBuilderType !== void 0 && I.searchBuilderType !== null ? this.s.type = I.searchBuilderType : (this.s.type === void 0 || this.s.type === null) && (this.s.type = I.sType);
          }
          (this.s.type === null || this.s.type === void 0) && (t.fn.dataTable.ext.oApi && t.fn.dataTable.ext.oApi._fnColumnTypes(v.settings()[0]), this.s.type = v.column(T).type()), this.dom.condition.removeAttr("disabled").empty().append(this.dom.conditionTitle).addClass(this.classes.italic), this.dom.conditionTitle.prop("selected", !0);
          var $ = v.settings()[0].oLanguage.sDecimal;
          $ !== "" && this.s.type && this.s.type.indexOf($) === this.s.type.length - $.length && (this.s.type.includes("num-fmt") ? this.s.type = this.s.type.replace($, "") : this.s.type.includes("num") && (this.s.type = this.s.type.replace($, "")));
          var V = void 0;
          if (this.c.conditions[this.s.type] !== void 0)
            V = this.c.conditions[this.s.type];
          else if (this.s.type && this.s.type === "datetime") {
            var z = H.use("moment"), F = H.use("luxon");
            z && (V = this.c.conditions.moment, this.s.dateFormat = z().creationData().locale._longDateFormat.L), F && (V = this.c.conditions.luxon, this.s.dateFormat = F.DateTime.DATE_SHORT);
          } else
            this.s.type && this.s.type.includes("datetime-") ? (V = H.use("moment") ? this.c.conditions.moment : this.c.conditions.luxon, this.s.dateFormat = this.s.type.replace(/datetime-/g, "")) : this.s.type && this.s.type.includes("moment") ? (V = this.c.conditions.moment, this.s.dateFormat = this.s.type.replace(/moment-/g, "")) : this.s.type && this.s.type.includes("luxon") ? (V = this.c.conditions.luxon, this.s.dateFormat = this.s.type.replace(/luxon-/g, "")) : V = this.c.conditions.string;
          for (var P = 0, k = Object.keys(V); P < k.length; P++)
            if (A = k[P], V[A] !== null) {
              if (v.page.info().serverSide && V[A].init === h.initSelect) {
                var R = S[T];
                this.s.serverData && this.s.serverData[R.data] ? (V[A].init = h.initSelectSSP, V[A].inputValue = h.inputValueSelect, V[A].isInputValid = h.isInputValidSelect) : (V[A].init = h.initInput, V[A].inputValue = h.inputValueInput, V[A].isInputValid = h.isInputValidInput);
              }
              this.s.conditions[A] = V[A], y = V[A].conditionName, typeof y == "function" && (y = y(v, this.c.i18n)), u.push(t("<option>", {
                text: y,
                value: A
              }).addClass(this.classes.option).addClass(this.classes.notItalic));
            }
        } else if (p > 0) {
          this.dom.condition.empty().removeAttr("disabled").addClass(this.classes.italic);
          for (var L = 0, E = Object.keys(this.s.conditions); L < E.length; L++) {
            A = E[L];
            var M = this.s.conditions[A].conditionName;
            typeof M == "function" && (M = M(v, this.c.i18n));
            var W = t("<option>", {
              text: M,
              value: A
            }).addClass(this.classes.option).addClass(this.classes.notItalic);
            this.s.condition !== void 0 && this.s.condition === M && (W.prop("selected", !0), this.dom.condition.removeClass(this.classes.italic)), u.push(W);
          }
        } else {
          this.dom.condition.attr("disabled", "true").addClass(this.classes.italic);
          return;
        }
        for (var Y = 0, J = u; Y < J.length; Y++) {
          var X = J[Y];
          this.dom.condition.append(X);
        }
        if (S[T].searchBuilder && S[T].searchBuilder.defaultCondition) {
          var Q = S[T].searchBuilder.defaultCondition;
          if (typeof Q == "number")
            this.dom.condition.prop("selectedIndex", Q), this.dom.condition.trigger("change");
          else if (typeof Q == "string")
            for (var ee = 0; ee < u.length; ee++)
              for (var ie = 0, oe = Object.keys(this.s.conditions); ie < oe.length; ie++) {
                var ce = oe[ie];
                if (y = this.s.conditions[ce].conditionName, // If the conditionName matches the text of the option
                (typeof y == "string" ? y : y(v, this.c.i18n)) === u[ee].text() && // and the tokens match
                ce === Q) {
                  this.dom.condition.prop("selectedIndex", this.dom.condition.children().toArray().indexOf(u[ee][0])).removeClass(this.classes.italic), this.dom.condition.trigger("change"), ee = u.length;
                  break;
                }
              }
        } else
          this.dom.condition.prop("selectedIndex", 0);
      }, h.prototype._populateData = function() {
        var u = this.s.dt.settings()[0].aoColumns, p = this.s.dt.columns(this.c.columns).indexes().toArray();
        this.dom.data.empty().append(this.dom.dataTitle);
        for (var v = 0; v < u.length; v++)
          if (this.c.columns === !0 || p.includes(v)) {
            var S = u[v], T = {
              index: v,
              origData: S.data,
              text: (S.searchBuilderTitle || S.sTitle).replace(/(<([^>]+)>)/ig, "")
            };
            this.dom.data.append(t("<option>", {
              text: T.text,
              value: T.index
            }).addClass(this.classes.option).addClass(this.classes.notItalic).prop("origData", S.data).prop("selected", this.s.dataIdx === T.index)), this.s.dataIdx === T.index && this.dom.dataTitle.removeProp("selected");
          }
      }, h.prototype._populateValue = function(u) {
        var p = this, v = this.s.filled, S;
        this.s.filled = !1, setTimeout(function() {
          p.dom.defaultValue.remove();
        }, 50);
        for (var T = function(V) {
          setTimeout(function() {
            V !== void 0 && V.remove();
          }, 50);
        }, A = 0, y = this.dom.value; A < y.length; A++) {
          var I = y[A];
          T(I);
        }
        var $ = this.dom.inputCont.children();
        if ($.length > 1)
          for (S = 0; S < $.length; S++)
            t($[S]).remove();
        for (u !== void 0 && this.s.dt.columns().every(function(V) {
          p.s.dt.settings()[0].aoColumns[V].sTitle === u.data && (p.s.dataIdx = V);
        }), this.dom.value = [].concat(this.s.conditions[this.s.condition].init(this, h.updateListener, u !== void 0 ? u.value : void 0)), u !== void 0 && u.value !== void 0 && (this.s.value = u.value), this.dom.inputCont.empty(), this.dom.value[0] !== void 0 && t(this.dom.value[0]).appendTo(this.dom.inputCont).trigger("dtsb-inserted"), S = 1; S < this.dom.value.length; S++)
          t(this.dom.value[S]).insertAfter(this.dom.value[S - 1]).trigger("dtsb-inserted");
        this.s.filled = this.s.conditions[this.s.condition].isInputValid(this.dom.value, this), this.setListeners(), !this.s.preventRedraw && v !== this.s.filled && (this.s.dt.page.info().serverSide || this.doSearch(), this.setListeners());
      }, h.prototype._throttle = function(u, p) {
        p === void 0 && (p = 200);
        var v = null, S = null, T = this;
        return p === null && (p = 200), function() {
          for (var A = [], y = 0; y < arguments.length; y++)
            A[y] = arguments[y];
          var I = +/* @__PURE__ */ new Date();
          v !== null && I < v + p ? clearTimeout(S) : v = I, S = setTimeout(function() {
            v = null, u.apply(T, A);
          }, p);
        };
      }, h.version = "1.1.0", h.classes = {
        button: "dtsb-button",
        buttonContainer: "dtsb-buttonContainer",
        condition: "dtsb-condition",
        container: "dtsb-criteria",
        data: "dtsb-data",
        delete: "dtsb-delete",
        dropDown: "dtsb-dropDown",
        greyscale: "dtsb-greyscale",
        input: "dtsb-input",
        inputCont: "dtsb-inputCont",
        italic: "dtsb-italic",
        joiner: "dtsb-joiner",
        left: "dtsb-left",
        notItalic: "dtsb-notItalic",
        option: "dtsb-option",
        right: "dtsb-right",
        select: "dtsb-select",
        value: "dtsb-value",
        vertical: "dtsb-vertical"
      }, h.initSelect = function(u, p, v, S) {
        v === void 0 && (v = null), S === void 0 && (S = !1);
        var T = u.dom.data.children("option:selected").val(), A = u.s.dt.rows().indexes().toArray(), y = u.s.dt.settings()[0].fastData;
        u.dom.valueTitle.prop("selected", !0);
        var I = t("<select/>").addClass(h.classes.value).addClass(h.classes.dropDown).addClass(h.classes.italic).addClass(h.classes.select).append(u.dom.valueTitle).on("change.dtsb", function() {
          t(this).removeClass(h.classes.italic), p(u, this);
        });
        u.c.greyscale && I.addClass(h.classes.greyscale);
        for (var $ = [], V = [], z = 0, F = A; z < F.length; z++) {
          var P = F[z], k = y(P, T, typeof u.c.orthogonal == "string" ? u.c.orthogonal : u.c.orthogonal.search), R = {
            filter: typeof k == "string" ? k.replace(/[\r\n\u2028]/g, " ") : (
              // Need to replace certain characters to match search values
              k
            ),
            index: P,
            text: y(P, T, typeof u.c.orthogonal == "string" ? u.c.orthogonal : u.c.orthogonal.display)
          };
          u.s.type === "array" && (R.filter = Array.isArray(R.filter) ? R.filter : [R.filter], R.text = Array.isArray(R.text) ? R.text : [R.text]);
          var L = function(J, X) {
            u.s.type.includes("html") && J !== null && typeof J == "string" && J.replace(/(<([^>]+)>)/ig, "");
            var Q = t("<option>", {
              type: Array.isArray(J) ? "Array" : "String",
              value: J
            }).data("sbv", J).addClass(u.classes.option).addClass(u.classes.notItalic).html(typeof X == "string" ? X.replace(/(<([^>]+)>)/ig, "") : X), ee = Q.val();
            $.indexOf(ee) === -1 && ($.push(ee), V.push(Q), v !== null && Array.isArray(v[0]) && (v[0] = v[0].sort().join(",")), v !== null && Q.val() === v[0] && (Q.prop("selected", !0), I.removeClass(h.classes.italic), u.dom.valueTitle.removeProp("selected")));
          };
          if (S)
            for (var E = 0; E < R.filter.length; E++)
              L(R.filter[E], R.text[E]);
          else
            L(R.filter, Array.isArray(R.text) ? R.text.join(", ") : R.text);
        }
        V.sort(function(J, X) {
          if (u.s.type === "array" || u.s.type === "string" || u.s.type === "html")
            return J.val() < X.val() ? -1 : J.val() > X.val() ? 1 : 0;
          if (u.s.type === "num" || u.s.type === "html-num")
            return +J.val().replace(/(<([^>]+)>)/ig, "") < +X.val().replace(/(<([^>]+)>)/ig, "") ? -1 : +J.val().replace(/(<([^>]+)>)/ig, "") > +X.val().replace(/(<([^>]+)>)/ig, "") ? 1 : 0;
          if (u.s.type === "num-fmt" || u.s.type === "html-num-fmt")
            return +J.val().replace(/[^0-9.]/g, "") < +X.val().replace(/[^0-9.]/g, "") ? -1 : +J.val().replace(/[^0-9.]/g, "") > +X.val().replace(/[^0-9.]/g, "") ? 1 : 0;
        });
        for (var M = 0, W = V; M < W.length; M++) {
          var Y = W[M];
          I.append(Y);
        }
        return I;
      }, h.initSelectSSP = function(u, p, v) {
        v === void 0 && (v = null), u.dom.valueTitle.prop("selected", !0);
        var S = t("<select/>").addClass(h.classes.value).addClass(h.classes.dropDown).addClass(h.classes.italic).addClass(h.classes.select).append(u.dom.valueTitle).on("change.dtsb", function() {
          t(this).removeClass(h.classes.italic), p(u, this);
        });
        u.c.greyscale && S.addClass(h.classes.greyscale);
        for (var T = [], A = 0, y = u.s.serverData[u.s.origData]; A < y.length; A++) {
          var I = y[A], $ = I.value, V = I.label, z = function(R, L) {
            u.s.type.includes("html") && R !== null && typeof R == "string" && R.replace(/(<([^>]+)>)/ig, "");
            var E = t("<option>", {
              type: Array.isArray(R) ? "Array" : "String",
              value: R
            }).data("sbv", R).addClass(u.classes.option).addClass(u.classes.notItalic).html(typeof L == "string" ? L.replace(/(<([^>]+)>)/ig, "") : L);
            T.push(E), v !== null && E.val() === v[0] && (E.prop("selected", !0), S.removeClass(h.classes.italic), u.dom.valueTitle.removeProp("selected"));
          };
          z($, V);
        }
        for (var F = 0, P = T; F < P.length; F++) {
          var k = P[F];
          S.append(k);
        }
        return S;
      }, h.initSelectArray = function(u, p, v) {
        return v === void 0 && (v = null), h.initSelect(u, p, v, !0);
      }, h.initInput = function(u, p, v) {
        v === void 0 && (v = null);
        var S = u.s.dt.settings()[0].searchDelay, T = t("<input/>").addClass(h.classes.value).addClass(h.classes.input).on("input.dtsb keypress.dtsb", u._throttle(function(A) {
          var y = A.keyCode || A.which;
          return p(u, this, y);
        }, S === null ? 100 : S));
        return u.c.greyscale && T.addClass(h.classes.greyscale), v !== null && T.val(v[0]), u.s.dt.one("draw.dtsb", function() {
          u.s.topGroup.trigger("dtsb-redrawLogic");
        }), T;
      }, h.init2Input = function(u, p, v) {
        v === void 0 && (v = null);
        var S = u.s.dt.settings()[0].searchDelay, T = [
          t("<input/>").addClass(h.classes.value).addClass(h.classes.input).on("input.dtsb keypress.dtsb", u._throttle(function(A) {
            var y = A.keyCode || A.which;
            return p(u, this, y);
          }, S === null ? 100 : S)),
          t("<span>").addClass(u.classes.joiner).html(u.s.dt.i18n("searchBuilder.valueJoiner", u.c.i18n.valueJoiner)),
          t("<input/>").addClass(h.classes.value).addClass(h.classes.input).on("input.dtsb keypress.dtsb", u._throttle(function(A) {
            var y = A.keyCode || A.which;
            return p(u, this, y);
          }, S === null ? 100 : S))
        ];
        return u.c.greyscale && (T[0].addClass(h.classes.greyscale), T[2].addClass(h.classes.greyscale)), v !== null && (T[0].val(v[0]), T[2].val(v[1])), u.s.dt.one("draw.dtsb", function() {
          u.s.topGroup.trigger("dtsb-redrawLogic");
        }), T;
      }, h.initDate = function(u, p, v) {
        v === void 0 && (v = null);
        var S = u.s.dt.settings()[0].searchDelay, T = u.s.dt.i18n("datetime", {}, !1), A = t("<input/>").addClass(h.classes.value).addClass(h.classes.input).dtDateTime({
          format: u.s.dateFormat ? u.s.dateFormat : void 0,
          i18n: T
        }).on("change.dtsb", u._throttle(function() {
          return p(u, this);
        }, S === null ? 100 : S)).on("input.dtsb keypress.dtsb", function(y) {
          u._throttle(function() {
            var I = y.keyCode || y.which;
            return p(u, this, I);
          }, S === null ? 100 : S);
        });
        return u.c.greyscale && A.addClass(h.classes.greyscale), v !== null && A.val(v[0]), u.s.dt.one("draw.dtsb", function() {
          u.s.topGroup.trigger("dtsb-redrawLogic");
        }), A;
      }, h.initNoValue = function(u) {
        return u.s.dt.one("draw.dtsb", function() {
          u.s.topGroup.trigger("dtsb-redrawLogic");
        }), [];
      }, h.init2Date = function(u, p, v) {
        var S = this;
        v === void 0 && (v = null);
        var T = u.s.dt.settings()[0].searchDelay, A = u.s.dt.i18n("datetime", {}, !1), y = [
          t("<input/>").addClass(h.classes.value).addClass(h.classes.input).dtDateTime({
            format: u.s.dateFormat ? u.s.dateFormat : void 0,
            i18n: A
          }).on("change.dtsb", T !== null ? H.util.throttle(function() {
            return p(u, this);
          }, T) : function() {
            p(u, S);
          }).on("input.dtsb keypress.dtsb", function(I) {
            H.util.throttle(function() {
              var $ = I.keyCode || I.which;
              return p(u, this, $);
            }, T === null ? 0 : T);
          }),
          t("<span>").addClass(u.classes.joiner).html(u.s.dt.i18n("searchBuilder.valueJoiner", u.c.i18n.valueJoiner)),
          t("<input/>").addClass(h.classes.value).addClass(h.classes.input).dtDateTime({
            format: u.s.dateFormat ? u.s.dateFormat : void 0,
            i18n: A
          }).on("change.dtsb", T !== null ? H.util.throttle(function() {
            return p(u, this);
          }, T) : function() {
            p(u, S);
          }).on("input.dtsb keypress.dtsb", !u.c.enterSearch && !(u.s.dt.settings()[0].oInit.search !== void 0 && u.s.dt.settings()[0].oInit.search.return) && T !== null ? H.util.throttle(function() {
            return p(u, this);
          }, T) : function(I) {
            var $ = I.keyCode || I.which;
            p(u, S, $);
          })
        ];
        return u.c.greyscale && (y[0].addClass(h.classes.greyscale), y[2].addClass(h.classes.greyscale)), v !== null && v.length > 0 && (y[0].val(v[0]), y[2].val(v[1])), u.s.dt.one("draw.dtsb", function() {
          u.s.topGroup.trigger("dtsb-redrawLogic");
        }), y;
      }, h.isInputValidSelect = function(u) {
        for (var p = !0, v = 0, S = u; v < S.length; v++) {
          var T = S[v];
          T.children("option:selected").length === T.children("option").length - T.children("option." + h.classes.notItalic).length && T.children("option:selected").length === 1 && T.children("option:selected")[0] === T.children("option")[0] && (p = !1);
        }
        return p;
      }, h.isInputValidInput = function(u) {
        for (var p = !0, v = 0, S = u; v < S.length; v++) {
          var T = S[v];
          T.is("input") && T.val().length === 0 && (p = !1);
        }
        return p;
      }, h.inputValueSelect = function(u) {
        for (var p = [], v = 0, S = u; v < S.length; v++) {
          var T = S[v];
          if (T.is("select")) {
            var A = [].concat(T.children("option:selected").data("sbv")).map(function(y) {
              return h._escapeHTML(y);
            });
            p.push.apply(p, A);
          }
        }
        return p;
      }, h.inputValueInput = function(u) {
        for (var p = [], v = 0, S = u; v < S.length; v++) {
          var T = S[v];
          T.is("input") && p.push(h._escapeHTML(T.val()));
        }
        return p.map(n.util.diacritics);
      }, h.updateListener = function(u, p, v) {
        var S = u.s.conditions[u.s.condition], T;
        if (u.s.filled = S.isInputValid(u.dom.value, u), u.s.value = S.inputValue(u.dom.value, u), !u.s.filled) {
          (!u.c.enterSearch && !(u.s.dt.settings()[0].oInit.search !== void 0 && u.s.dt.settings()[0].oInit.search.return) || v === 13) && u.doSearch();
          return;
        }
        for (Array.isArray(u.s.value) || (u.s.value = [u.s.value]), T = 0; T < u.s.value.length; T++)
          Array.isArray(u.s.value[T]) && u.s.value[T].sort();
        var A = null, y = null;
        for (T = 0; T < u.dom.value.length; T++)
          p === u.dom.value[T][0] && (A = T, p.selectionStart !== void 0 && (y = p.selectionStart));
        (!u.c.enterSearch && !(u.s.dt.settings()[0].oInit.search !== void 0 && u.s.dt.settings()[0].oInit.search.return) || v === 13 || p.nodeName.toLowerCase() === "select") && u.doSearch(), A !== null && (u.dom.value[A].removeClass(u.classes.italic), u.dom.value[A].focus(), y !== null && u.dom.value[A][0].setSelectionRange(y, y));
      }, h.dateConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.equals", p.conditions.date.equals);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), u === p[0];
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.not", p.conditions.date.not);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), u !== p[0];
          }
        },
        "<": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.before", p.conditions.date.before);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), u < p[0];
          }
        },
        ">": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.after", p.conditions.date.after);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), u > p[0];
          }
        },
        between: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.between", p.conditions.date.between);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), p[0] < p[1] ? p[0] <= u && u <= p[1] : p[1] <= u && u <= p[0];
          }
        },
        "!between": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notBetween", p.conditions.date.notBetween);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u = u.replace(/(\/|-|,)/g, "-"), p[0] < p[1] ? !(p[0] <= u && u <= p[1]) : !(p[1] <= u && u <= p[0]);
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.empty", p.conditions.date.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notEmpty", p.conditions.date.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.momentDateConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.equals", p.conditions.date.equals);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return e()(u, v.s.dateFormat).valueOf() === e()(p[0], v.s.dateFormat).valueOf();
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.not", p.conditions.date.not);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return e()(u, v.s.dateFormat).valueOf() !== e()(p[0], v.s.dateFormat).valueOf();
          }
        },
        "<": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.before", p.conditions.date.before);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return e()(u, v.s.dateFormat).valueOf() < e()(p[0], v.s.dateFormat).valueOf();
          }
        },
        ">": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.after", p.conditions.date.after);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return e()(u, v.s.dateFormat).valueOf() > e()(p[0], v.s.dateFormat).valueOf();
          }
        },
        between: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.between", p.conditions.date.between);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = e()(u, v.s.dateFormat).valueOf(), T = e()(p[0], v.s.dateFormat).valueOf(), A = e()(p[1], v.s.dateFormat).valueOf();
            return T < A ? T <= S && S <= A : A <= S && S <= T;
          }
        },
        "!between": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notBetween", p.conditions.date.notBetween);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = e()(u, v.s.dateFormat).valueOf(), T = e()(p[0], v.s.dateFormat).valueOf(), A = e()(p[1], v.s.dateFormat).valueOf();
            return T < A ? !(+T <= +S && +S <= +A) : !(+A <= +S && +S <= +T);
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.empty", p.conditions.date.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notEmpty", p.conditions.date.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.luxonDateConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.equals", p.conditions.date.equals);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return r().DateTime.fromFormat(u, v.s.dateFormat).ts === r().DateTime.fromFormat(p[0], v.s.dateFormat).ts;
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.not", p.conditions.date.not);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return r().DateTime.fromFormat(u, v.s.dateFormat).ts !== r().DateTime.fromFormat(p[0], v.s.dateFormat).ts;
          }
        },
        "<": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.before", p.conditions.date.before);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return r().DateTime.fromFormat(u, v.s.dateFormat).ts < r().DateTime.fromFormat(p[0], v.s.dateFormat).ts;
          }
        },
        ">": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.after", p.conditions.date.after);
          },
          init: h.initDate,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return r().DateTime.fromFormat(u, v.s.dateFormat).ts > r().DateTime.fromFormat(p[0], v.s.dateFormat).ts;
          }
        },
        between: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.between", p.conditions.date.between);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = r().DateTime.fromFormat(u, v.s.dateFormat).ts, T = r().DateTime.fromFormat(p[0], v.s.dateFormat).ts, A = r().DateTime.fromFormat(p[1], v.s.dateFormat).ts;
            return T < A ? T <= S && S <= A : A <= S && S <= T;
          }
        },
        "!between": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notBetween", p.conditions.date.notBetween);
          },
          init: h.init2Date,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = r().DateTime.fromFormat(u, v.s.dateFormat).ts, T = r().DateTime.fromFormat(p[0], v.s.dateFormat).ts, A = r().DateTime.fromFormat(p[1], v.s.dateFormat).ts;
            return T < A ? !(+T <= +S && +S <= +A) : !(+A <= +S && +S <= +T);
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.empty", p.conditions.date.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.date.notEmpty", p.conditions.date.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.numConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.equals", p.conditions.number.equals);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            return +u == +p[0];
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.not", p.conditions.number.not);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            return +u != +p[0];
          }
        },
        "<": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.lt", p.conditions.number.lt);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +u < +p[0];
          }
        },
        "<=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.lte", p.conditions.number.lte);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +u <= +p[0];
          }
        },
        ">=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.gte", p.conditions.number.gte);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +u >= +p[0];
          }
        },
        ">": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.gt", p.conditions.number.gt);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +u > +p[0];
          }
        },
        between: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.between", p.conditions.number.between);
          },
          init: h.init2Input,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +p[0] < +p[1] ? +p[0] <= +u && +u <= +p[1] : +p[1] <= +u && +u <= +p[0];
          }
        },
        "!between": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.notBetween", p.conditions.number.notBetween);
          },
          init: h.init2Input,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return +p[0] < +p[1] ? !(+p[0] <= +u && +u <= +p[1]) : !(+p[1] <= +u && +u <= +p[0]);
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.empty", p.conditions.number.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.notEmpty", p.conditions.number.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.numFmtConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.equals", p.conditions.number.equals);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p, v) {
            return v.parseNumber(u) === v.parseNumber(p[0]);
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.not", p.conditions.number.not);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p, v) {
            return v.parseNumber(u) !== v.parseNumber(p[0]);
          }
        },
        "<": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.lt", p.conditions.number.lt);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return v.parseNumber(u) < v.parseNumber(p[0]);
          }
        },
        "<=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.lte", p.conditions.number.lte);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return v.parseNumber(u) <= v.parseNumber(p[0]);
          }
        },
        ">=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.gte", p.conditions.number.gte);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return v.parseNumber(u) >= v.parseNumber(p[0]);
          }
        },
        ">": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.gt", p.conditions.number.gt);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            return v.parseNumber(u) > v.parseNumber(p[0]);
          }
        },
        between: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.between", p.conditions.number.between);
          },
          init: h.init2Input,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = v.parseNumber(u), T = v.parseNumber(p[0]), A = v.parseNumber(p[1]);
            return +T < +A ? +T <= +S && +S <= +A : +A <= +S && +S <= +T;
          }
        },
        "!between": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.notBetween", p.conditions.number.notBetween);
          },
          init: h.init2Input,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p, v) {
            var S = v.parseNumber(u), T = v.parseNumber(p[0]), A = v.parseNumber(p[1]);
            return +T < +A ? !(+T <= +S && +S <= +A) : !(+A <= +S && +S <= +T);
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.empty", p.conditions.number.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.number.notEmpty", p.conditions.number.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.stringConditions = {
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.equals", p.conditions.string.equals);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            return u === p[0];
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.not", p.conditions.string.not);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u !== p[0];
          }
        },
        starts: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.startsWith", p.conditions.string.startsWith);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u.toLowerCase().indexOf(p[0].toLowerCase()) === 0;
          }
        },
        "!starts": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.notStartsWith", p.conditions.string.notStartsWith);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u.toLowerCase().indexOf(p[0].toLowerCase()) !== 0;
          }
        },
        contains: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.contains", p.conditions.string.contains);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u.toLowerCase().includes(p[0].toLowerCase());
          }
        },
        "!contains": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.notContains", p.conditions.string.notContains);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return !u.toLowerCase().includes(p[0].toLowerCase());
          }
        },
        ends: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.endsWith", p.conditions.string.endsWith);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return u.toLowerCase().endsWith(p[0].toLowerCase());
          }
        },
        "!ends": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.notEndsWith", p.conditions.string.notEndsWith);
          },
          init: h.initInput,
          inputValue: h.inputValueInput,
          isInputValid: h.isInputValidInput,
          search: function(u, p) {
            return !u.toLowerCase().endsWith(p[0].toLowerCase());
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.empty", p.conditions.string.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.string.notEmpty", p.conditions.string.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return !(u == null || u.length === 0);
          }
        }
      }, h.arrayConditions = {
        contains: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.contains", p.conditions.array.contains);
          },
          init: h.initSelectArray,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            return u.includes(p[0]);
          }
        },
        without: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.without", p.conditions.array.without);
          },
          init: h.initSelectArray,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            return u.indexOf(p[0]) === -1;
          }
        },
        "=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.equals", p.conditions.array.equals);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            if (u.length === p.length) {
              p.sort();
              for (var v = 0; v < u.length; v++)
                if (u[v] !== p[v])
                  return !1;
              return !0;
            }
            return !1;
          }
        },
        "!=": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.not", p.conditions.array.not);
          },
          init: h.initSelect,
          inputValue: h.inputValueSelect,
          isInputValid: h.isInputValidSelect,
          search: function(u, p) {
            if (u.length === p.length) {
              p.sort();
              for (var v = 0; v < u.length; v++)
                if (u[v] !== p[v])
                  return !0;
              return !1;
            }
            return !0;
          }
        },
        null: {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.empty", p.conditions.array.empty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u == null || u.length === 0;
          }
        },
        "!null": {
          conditionName: function(u, p) {
            return u.i18n("searchBuilder.conditions.array.notEmpty", p.conditions.array.notEmpty);
          },
          init: h.initNoValue,
          inputValue: function() {
          },
          isInputValid: function() {
            return !0;
          },
          search: function(u) {
            return u != null && u.length !== 0;
          }
        }
      }, h.defaults = {
        columns: !0,
        conditions: {
          array: h.arrayConditions,
          date: h.dateConditions,
          html: h.stringConditions,
          "html-num": h.numConditions,
          "html-num-fmt": h.numFmtConditions,
          luxon: h.luxonDateConditions,
          moment: h.momentDateConditions,
          num: h.numConditions,
          "num-fmt": h.numFmtConditions,
          string: h.stringConditions
        },
        depthLimit: !1,
        enterSearch: !1,
        filterChanged: void 0,
        greyscale: !1,
        i18n: {
          add: "Add Condition",
          button: {
            0: "Search Builder",
            _: "Search Builder (%d)"
          },
          clearAll: "Clear All",
          condition: "Condition",
          data: "Data",
          delete: "&times",
          deleteTitle: "Delete filtering rule",
          left: "<",
          leftTitle: "Outdent criteria",
          logicAnd: "And",
          logicOr: "Or",
          right: ">",
          rightTitle: "Indent criteria",
          search: "Search",
          title: {
            0: "Custom Search Builder",
            _: "Custom Search Builder (%d)"
          },
          value: "Value",
          valueJoiner: "and"
        },
        liveSearch: !0,
        logic: "AND",
        orthogonal: {
          display: "display",
          search: "filter"
        },
        preDefined: !1
      }, h;
    }()
  ), a;
  function o(h) {
    a = h, h.fn.dataTable;
  }
  var l = (
    /** @class */
    function() {
      function h(u, p, v, S, T, A, y) {
        return S === void 0 && (S = 0), T === void 0 && (T = !1), A === void 0 && (A = 1), y === void 0 && (y = void 0), this.classes = a.extend(!0, {}, h.classes), this.c = a.extend(!0, {}, h.defaults, p), this.s = {
          criteria: [],
          depth: A,
          dt: u,
          index: S,
          isChild: T,
          logic: void 0,
          opts: p,
          preventRedraw: !1,
          serverData: y,
          toDrop: void 0,
          topGroup: v
        }, this.dom = {
          add: a("<button/>").addClass(this.classes.add).addClass(this.classes.button).attr("type", "button"),
          clear: a("<button>&times</button>").addClass(this.classes.button).addClass(this.classes.clearGroup).attr("type", "button"),
          container: a("<div/>").addClass(this.classes.group),
          logic: a("<button><div/></button>").addClass(this.classes.logic).addClass(this.classes.button).attr("type", "button"),
          logicContainer: a("<div/>").addClass(this.classes.logicContainer),
          search: a("<button/>").addClass(this.classes.search).addClass(this.classes.button).attr("type", "button").css("display", "none")
        }, this.s.topGroup === void 0 && (this.s.topGroup = this.dom.container), this._setup(), this;
      }
      return h.prototype.destroy = function() {
        this.dom.add.off(".dtsb"), this.dom.logic.off(".dtsb"), this.dom.search.off(".dtsb"), this.dom.container.trigger("dtsb-destroy").remove(), this.s.criteria = [];
      }, h.prototype.getDetails = function(u) {
        if (u === void 0 && (u = !1), this.s.criteria.length === 0)
          return {};
        for (var p = {
          criteria: [],
          logic: this.s.logic
        }, v = 0, S = this.s.criteria; v < S.length; v++) {
          var T = S[v];
          p.criteria.push(T.criteria.getDetails(u));
        }
        return p;
      }, h.prototype.getNode = function() {
        return this.dom.container;
      }, h.prototype.rebuild = function(u) {
        var p;
        if (!(u.criteria === void 0 || u.criteria === null || Array.isArray(u.criteria) && u.criteria.length === 0)) {
          if (this.s.logic = u.logic, this.dom.logic.children().first().html(this.s.logic === "OR" ? this.s.dt.i18n("searchBuilder.logicOr", this.c.i18n.logicOr) : this.s.dt.i18n("searchBuilder.logicAnd", this.c.i18n.logicAnd)), Array.isArray(u.criteria))
            for (var v = 0, S = u.criteria; v < S.length; v++)
              p = S[v], p.logic !== void 0 ? this._addPrevGroup(p) : p.logic === void 0 && this._addPrevCriteria(p);
          for (var T = 0, A = this.s.criteria; T < A.length; T++)
            p = A[T], p.criteria instanceof s && (p.criteria.updateArrows(this.s.criteria.length > 1), this._setCriteriaListeners(p.criteria));
        }
      }, h.prototype.redrawContents = function() {
        if (!this.s.preventRedraw) {
          this.dom.container.children().detach(), this.dom.container.append(this.dom.logicContainer).append(this.dom.add), this.c.liveSearch || this.dom.container.append(this.dom.search), this.s.criteria.sort(function(v, S) {
            return v.criteria.s.index < S.criteria.s.index ? -1 : v.criteria.s.index > S.criteria.s.index ? 1 : 0;
          }), this.setListeners();
          for (var u = 0; u < this.s.criteria.length; u++) {
            var p = this.s.criteria[u].criteria;
            p instanceof s ? (this.s.criteria[u].index = u, this.s.criteria[u].criteria.s.index = u, this.s.criteria[u].criteria.dom.container.insertBefore(this.dom.add), this._setCriteriaListeners(p), this.s.criteria[u].criteria.s.preventRedraw = this.s.preventRedraw, this.s.criteria[u].criteria.rebuild(this.s.criteria[u].criteria.getDetails()), this.s.criteria[u].criteria.s.preventRedraw = !1) : p instanceof h && p.s.criteria.length > 0 ? (this.s.criteria[u].index = u, this.s.criteria[u].criteria.s.index = u, this.s.criteria[u].criteria.dom.container.insertBefore(this.dom.add), p.s.preventRedraw = this.s.preventRedraw, p.redrawContents(), p.s.preventRedraw = !1, this._setGroupListeners(p)) : (this.s.criteria.splice(u, 1), u--);
          }
          this.setupLogic();
        }
      }, h.prototype.redrawLogic = function() {
        for (var u = 0, p = this.s.criteria; u < p.length; u++) {
          var v = p[u];
          v.criteria instanceof h && v.criteria.redrawLogic();
        }
        this.setupLogic();
      }, h.prototype.search = function(u, p) {
        return this.s.logic === "AND" ? this._andSearch(u, p) : this.s.logic === "OR" ? this._orSearch(u, p) : !0;
      }, h.prototype.setupLogic = function() {
        if (this.dom.logicContainer.remove(), this.dom.clear.remove(), this.s.criteria.length < 1) {
          this.s.isChild || (this.dom.container.trigger("dtsb-destroy"), this.dom.container.css("margin-left", 0)), this.dom.search.css("display", "none");
          return;
        }
        this.dom.clear.height("0px"), this.dom.logicContainer.append(this.dom.clear), this.s.isChild || this.dom.search.css("display", "inline-block"), this.dom.container.prepend(this.dom.logicContainer);
        for (var u = 0, p = this.s.criteria; u < p.length; u++) {
          var v = p[u];
          v.criteria instanceof s && v.criteria.setupButtons();
        }
        var S = this.dom.container.outerHeight() - 1;
        this.dom.logicContainer.width(S), this._setLogicListener(), this.dom.container.css("margin-left", this.dom.logicContainer.outerHeight(!0));
        var T = this.dom.logicContainer.offset(), A = T.left, y = this.dom.container.offset().left, I = A - y, $ = A - I - this.dom.logicContainer.outerHeight(!0);
        this.dom.logicContainer.offset({ left: $ });
        var V = this.dom.logicContainer.next(), z = T.top, F = a(V).offset().top, P = z - F, k = z - P;
        this.dom.logicContainer.offset({ top: k }), this.dom.clear.outerHeight(this.dom.logicContainer.height()), this._setClearListener();
      }, h.prototype.setListeners = function() {
        var u = this;
        this.dom.add.unbind("click"), this.dom.add.on("click.dtsb", function() {
          return u.s.isChild || u.dom.container.prepend(u.dom.logicContainer), u.addCriteria(), u.dom.container.trigger("dtsb-add"), u.s.dt.state.save(), !1;
        }), this.dom.search.off("click.dtsb").on("click.dtsb", function() {
          u.s.dt.draw();
        });
        for (var p = 0, v = this.s.criteria; p < v.length; p++) {
          var S = v[p];
          S.criteria.setListeners();
        }
        this._setClearListener(), this._setLogicListener();
      }, h.prototype.addCriteria = function(u) {
        u === void 0 && (u = null);
        var p = u === null ? this.s.criteria.length : u.s.index, v = new s(this.s.dt, this.s.opts, this.s.topGroup, p, this.s.depth, this.s.serverData, this.c.liveSearch);
        u !== null && (v.c = u.c, v.s = u.s, v.s.depth = this.s.depth, v.classes = u.classes), v.populate();
        for (var S = !1, T = 0; T < this.s.criteria.length; T++)
          T === 0 && this.s.criteria[T].criteria.s.index > v.s.index ? (v.getNode().insertBefore(this.s.criteria[T].criteria.dom.container), S = !0) : T < this.s.criteria.length - 1 && this.s.criteria[T].criteria.s.index < v.s.index && this.s.criteria[T + 1].criteria.s.index > v.s.index && (v.getNode().insertAfter(this.s.criteria[T].criteria.dom.container), S = !0);
        S || v.getNode().insertBefore(this.dom.add), this.s.criteria.push({
          criteria: v,
          index: p
        }), this.s.criteria = this.s.criteria.sort(function($, V) {
          return $.criteria.s.index - V.criteria.s.index;
        });
        for (var A = 0, y = this.s.criteria; A < y.length; A++) {
          var I = y[A];
          I.criteria instanceof s && I.criteria.updateArrows(this.s.criteria.length > 1);
        }
        this._setCriteriaListeners(v), v.setListeners(), this.setupLogic();
      }, h.prototype.checkFilled = function() {
        for (var u = 0, p = this.s.criteria; u < p.length; u++) {
          var v = p[u];
          if (v.criteria instanceof s && v.criteria.s.filled || v.criteria instanceof h && v.criteria.checkFilled())
            return !0;
        }
        return !1;
      }, h.prototype.count = function() {
        for (var u = 0, p = 0, v = this.s.criteria; p < v.length; p++) {
          var S = v[p];
          S.criteria instanceof h ? u += S.criteria.count() : u++;
        }
        return u;
      }, h.prototype._addPrevGroup = function(u) {
        var p = this.s.criteria.length, v = new h(this.s.dt, this.c, this.s.topGroup, p, !0, this.s.depth + 1, this.s.serverData);
        this.s.criteria.push({
          criteria: v,
          index: p,
          logic: v.s.logic
        }), v.rebuild(u), this.s.criteria[p].criteria = v, this.s.topGroup.trigger("dtsb-redrawContents"), this._setGroupListeners(v);
      }, h.prototype._addPrevCriteria = function(u) {
        var p = this.s.criteria.length, v = new s(this.s.dt, this.s.opts, this.s.topGroup, p, this.s.depth, this.s.serverData);
        v.populate(), this.s.criteria.push({
          criteria: v,
          index: p
        }), v.s.preventRedraw = this.s.preventRedraw, v.rebuild(u), v.s.preventRedraw = !1, this.s.criteria[p].criteria = v, this.s.preventRedraw || this.s.topGroup.trigger("dtsb-redrawContents");
      }, h.prototype._andSearch = function(u, p) {
        if (this.s.criteria.length === 0)
          return !0;
        for (var v = 0, S = this.s.criteria; v < S.length; v++) {
          var T = S[v];
          if (!(T.criteria instanceof s && !T.criteria.s.filled) && !T.criteria.search(u, p))
            return !1;
        }
        return !0;
      }, h.prototype._orSearch = function(u, p) {
        if (this.s.criteria.length === 0)
          return !0;
        for (var v = !1, S = 0, T = this.s.criteria; S < T.length; S++) {
          var A = T[S];
          if (A.criteria instanceof s && A.criteria.s.filled) {
            if (v = !0, A.criteria.search(u, p))
              return !0;
          } else if (A.criteria instanceof h && A.criteria.checkFilled() && (v = !0, A.criteria.search(u, p)))
            return !0;
        }
        return !v;
      }, h.prototype._removeCriteria = function(u, p) {
        p === void 0 && (p = !1);
        var v;
        if (this.s.criteria.length <= 1 && this.s.isChild)
          this.destroy();
        else {
          var S = void 0;
          for (v = 0; v < this.s.criteria.length; v++)
            this.s.criteria[v].index === u.s.index && (!p || this.s.criteria[v].criteria instanceof h) && (S = v);
          for (S !== void 0 && this.s.criteria.splice(S, 1), v = 0; v < this.s.criteria.length; v++)
            this.s.criteria[v].index = v, this.s.criteria[v].criteria.s.index = v;
        }
      }, h.prototype._setCriteriaListeners = function(u) {
        var p = this;
        u.dom.delete.unbind("click").on("click.dtsb", function() {
          p._removeCriteria(u), u.dom.container.remove();
          for (var v = 0, S = p.s.criteria; v < S.length; v++) {
            var T = S[v];
            T.criteria instanceof s && T.criteria.updateArrows(p.s.criteria.length > 1);
          }
          return u.destroy(), p.s.dt.draw(), p.s.topGroup.trigger("dtsb-redrawContents"), !1;
        }), u.dom.right.unbind("click").on("click.dtsb", function() {
          var v = u.s.index, S = new h(p.s.dt, p.s.opts, p.s.topGroup, u.s.index, !0, p.s.depth + 1, p.s.serverData);
          return S.addCriteria(u), p.s.criteria[v].criteria = S, p.s.criteria[v].logic = "AND", p.s.topGroup.trigger("dtsb-redrawContents"), p._setGroupListeners(S), !1;
        }), u.dom.left.unbind("click").on("click.dtsb", function() {
          p.s.toDrop = new s(p.s.dt, p.s.opts, p.s.topGroup, u.s.index, void 0, p.s.serverData), p.s.toDrop.s = u.s, p.s.toDrop.c = u.c, p.s.toDrop.classes = u.classes, p.s.toDrop.populate();
          var v = p.s.toDrop.s.index;
          return p.dom.container.trigger("dtsb-dropCriteria"), u.s.index = v, p._removeCriteria(u), p.s.topGroup.trigger("dtsb-redrawContents"), p.s.dt.draw(), !1;
        });
      }, h.prototype._setClearListener = function() {
        var u = this;
        this.dom.clear.unbind("click").on("click.dtsb", function() {
          return u.s.isChild ? (u.destroy(), u.s.topGroup.trigger("dtsb-redrawContents"), !1) : (u.dom.container.trigger("dtsb-clearContents"), !1);
        });
      }, h.prototype._setGroupListeners = function(u) {
        var p = this;
        u.dom.add.unbind("click").on("click.dtsb", function() {
          return p.setupLogic(), p.dom.container.trigger("dtsb-add"), !1;
        }), u.dom.container.unbind("dtsb-add").on("dtsb-add.dtsb", function() {
          return p.setupLogic(), p.dom.container.trigger("dtsb-add"), !1;
        }), u.dom.container.unbind("dtsb-destroy").on("dtsb-destroy.dtsb", function() {
          return p._removeCriteria(u, !0), u.dom.container.remove(), p.setupLogic(), !1;
        }), u.dom.container.unbind("dtsb-dropCriteria").on("dtsb-dropCriteria.dtsb", function() {
          var v = u.s.toDrop;
          return v.s.index = u.s.index, v.updateArrows(p.s.criteria.length > 1), p.addCriteria(v), !1;
        }), u.setListeners();
      }, h.prototype._setup = function() {
        this.setListeners(), this.dom.add.html(this.s.dt.i18n("searchBuilder.add", this.c.i18n.add)), this.dom.search.html(this.s.dt.i18n("searchBuilder.search", this.c.i18n.search)), this.dom.logic.children().first().html(this.c.logic === "OR" ? this.s.dt.i18n("searchBuilder.logicOr", this.c.i18n.logicOr) : this.s.dt.i18n("searchBuilder.logicAnd", this.c.i18n.logicAnd)), this.s.logic = this.c.logic === "OR" ? "OR" : "AND", this.c.greyscale && this.dom.logic.addClass(this.classes.greyscale), this.dom.logicContainer.append(this.dom.logic).append(this.dom.clear), this.s.isChild && this.dom.container.append(this.dom.logicContainer), this.dom.container.append(this.dom.add), this.c.liveSearch || this.dom.container.append(this.dom.search);
      }, h.prototype._setLogicListener = function() {
        var u = this;
        this.dom.logic.unbind("click").on("click.dtsb", function() {
          u._toggleLogic(), u.s.dt.draw();
          for (var p = 0, v = u.s.criteria; p < v.length; p++) {
            var S = v[p];
            S.criteria.setListeners();
          }
        });
      }, h.prototype._toggleLogic = function() {
        this.s.logic === "OR" ? (this.s.logic = "AND", this.dom.logic.children().first().html(this.s.dt.i18n("searchBuilder.logicAnd", this.c.i18n.logicAnd))) : this.s.logic === "AND" && (this.s.logic = "OR", this.dom.logic.children().first().html(this.s.dt.i18n("searchBuilder.logicOr", this.c.i18n.logicOr)));
      }, h.version = "1.1.0", h.classes = {
        add: "dtsb-add",
        button: "dtsb-button",
        clearGroup: "dtsb-clearGroup",
        greyscale: "dtsb-greyscale",
        group: "dtsb-group",
        inputButton: "dtsb-iptbtn",
        logic: "dtsb-logic",
        logicContainer: "dtsb-logicContainer",
        search: "dtsb-search"
      }, h.defaults = {
        columns: !0,
        conditions: {
          date: s.dateConditions,
          html: s.stringConditions,
          "html-num": s.numConditions,
          "html-num-fmt": s.numFmtConditions,
          luxon: s.luxonDateConditions,
          moment: s.momentDateConditions,
          num: s.numConditions,
          "num-fmt": s.numFmtConditions,
          string: s.stringConditions
        },
        depthLimit: !1,
        enterSearch: !1,
        filterChanged: void 0,
        greyscale: !1,
        liveSearch: !0,
        i18n: {
          add: "Add Condition",
          button: {
            0: "Search Builder",
            _: "Search Builder (%d)"
          },
          clearAll: "Clear All",
          condition: "Condition",
          data: "Data",
          delete: "&times",
          deleteTitle: "Delete filtering rule",
          left: "<",
          leftTitle: "Outdent criteria",
          logicAnd: "And",
          logicOr: "Or",
          right: ">",
          rightTitle: "Indent criteria",
          search: "Search",
          title: {
            0: "Custom Search Builder",
            _: "Custom Search Builder (%d)"
          },
          value: "Value",
          valueJoiner: "and"
        },
        logic: "AND",
        orthogonal: {
          display: "display",
          search: "filter"
        },
        preDefined: !1
      }, h;
    }()
  ), c, f;
  function g(h) {
    c = h, f = h.fn.DataTable;
  }
  var b = (
    /** @class */
    function() {
      function h(u, p) {
        var v = this;
        if (!f || !f.versionCheck || !f.versionCheck("2"))
          throw new Error("SearchBuilder requires DataTables 2 or newer");
        var S = new f.Api(u);
        if (this.classes = c.extend(!0, {}, h.classes), this.c = c.extend(!0, {}, h.defaults, p), this.dom = {
          clearAll: c('<button type="button">' + S.i18n("searchBuilder.clearAll", this.c.i18n.clearAll) + "</button>").addClass(this.classes.clearAll).addClass(this.classes.button).attr("type", "button"),
          container: c("<div/>").addClass(this.classes.container),
          title: c("<div/>").addClass(this.classes.title),
          titleRow: c("<div/>").addClass(this.classes.titleRow),
          topGroup: void 0
        }, this.s = {
          dt: S,
          opts: p,
          search: void 0,
          serverData: void 0,
          topGroup: void 0
        }, S.settings()[0]._searchBuilder === void 0)
          return S.settings()[0]._searchBuilder = this, this.s.dt.page.info().serverSide && (this.s.dt.on("preXhr.dtsb", function(T, A, y) {
            var I = v.s.dt.state.loaded();
            I && I.searchBuilder && (y.searchBuilder = v._collapseArray(I.searchBuilder));
          }), this.s.dt.on("xhr.dtsb", function(T, A, y) {
            y && y.searchBuilder && y.searchBuilder.options && (v.s.serverData = y.searchBuilder.options);
          })), this.s.dt.settings()[0]._bInitComplete ? this._setUp() : S.one("init.dt", function() {
            v._setUp();
          }), this;
      }
      return h.prototype.getDetails = function(u) {
        return u === void 0 && (u = !1), this.s.topGroup ? this.s.topGroup.getDetails(u) : {};
      }, h.prototype.getNode = function() {
        return this.dom.container;
      }, h.prototype.rebuild = function(u, p) {
        return p === void 0 && (p = !0), this.dom.clearAll.trigger("click", !1), u == null ? this : (this.s.topGroup.s.preventRedraw = !0, this.s.topGroup.rebuild(u), this.s.topGroup.s.preventRedraw = !1, this._checkClear(), this._updateTitle(this.s.topGroup.count()), this.s.topGroup.redrawContents(), p && this.s.dt.draw(!1), this.s.topGroup.setListeners(), this);
      }, h.prototype._applyPreDefDefaults = function(u) {
        var p = this;
        u.criteria !== void 0 && u.logic === void 0 && (u.logic = "AND");
        for (var v = function(I) {
          I.criteria !== void 0 ? I = S._applyPreDefDefaults(I) : S.s.dt.columns().every(function($) {
            p.s.dt.settings()[0].aoColumns[$].sTitle === I.data && (I.dataIdx = $);
          });
        }, S = this, T = 0, A = u.criteria; T < A.length; T++) {
          var y = A[T];
          v(y);
        }
        return u;
      }, h.prototype._setUp = function(u) {
        var p = this;
        if (u === void 0 && (u = !0), typeof this.s.dt.column().type != "function" && H.Api.registerPlural("columns().types()", "column().type()", function() {
          return this.iterator("column", function(F, P) {
            return F.aoColumns[P].sType;
          }, 1);
        }), !f.DateTime) {
          var v = this.s.dt.columns().types().toArray();
          if (v === void 0 || v.includes(void 0) || v.includes(null)) {
            v = [];
            for (var S = 0, T = this.s.dt.settings()[0].aoColumns; S < T.length; S++) {
              var A = T[S];
              v.push(A.searchBuilderType !== void 0 ? A.searchBuilderType : A.sType);
            }
          }
          var y = this.s.dt.columns().toArray();
          (v === void 0 || v.includes(void 0) || v.includes(null)) && (c.fn.dataTable.ext.oApi && c.fn.dataTable.ext.oApi._fnColumnTypes(this.s.dt.settings()[0]), v = this.s.dt.columns().types().toArray());
          for (var I = 0; I < y[0].length; I++) {
            var $ = y[0][I], V = v[$];
            if (
              // Check if this column can be filtered
              (this.c.columns === !0 || Array.isArray(this.c.columns) && this.c.columns.includes(I)) && // Check if the type is one of the restricted types
              (V.includes("date") || V.includes("moment") || V.includes("luxon"))
            )
              throw alert("SearchBuilder Requires DateTime when used with dates."), new Error("SearchBuilder requires DateTime");
          }
        }
        if (this.s.topGroup = new l(this.s.dt, this.c, void 0, void 0, void 0, void 0, this.s.serverData), this._setClearListener(), this.s.dt.on("stateSaveParams.dtsb", function(F, P, k) {
          k.searchBuilder = p.getDetails(), k.scroller ? k.start = p.s.dt.state().start : k.page = p.s.dt.page();
        }), this.s.dt.on("stateLoadParams.dtsb", function(F, P, k) {
          p.rebuild(k.searchBuilder);
        }), this._build(), this.s.dt.on("preXhr.dtsb", function(F, P, k) {
          p.s.dt.page.info().serverSide && (k.searchBuilder = p._collapseArray(p.getDetails(!0)));
        }), this.s.dt.on("columns-reordered", function() {
          p.rebuild(p.getDetails());
        }), u) {
          var z = this.s.dt.state.loaded();
          z !== null && z.searchBuilder !== void 0 ? (this.s.topGroup.rebuild(z.searchBuilder), this.s.topGroup.dom.container.trigger("dtsb-redrawContents"), this.s.dt.page.info().serverSide || (z.page ? this.s.dt.page(z.page).draw("page") : this.s.dt.scroller && z.scroller && this.s.dt.scroller().scrollToRow(z.scroller.topRow)), this.s.topGroup.setListeners()) : this.c.preDefined !== !1 && (this.c.preDefined = this._applyPreDefDefaults(this.c.preDefined), this.rebuild(this.c.preDefined));
        }
        this._setEmptyListener(), this.s.dt.state.save();
      }, h.prototype._collapseArray = function(u) {
        if (u.logic === void 0)
          u.value !== void 0 && (u.value.sort(function(v, S) {
            return isNaN(+v) || (v = +v, S = +S), v < S ? -1 : S < v ? 1 : 0;
          }), u.value1 = u.value[0], u.value2 = u.value[1]);
        else
          for (var p = 0; p < u.criteria.length; p++)
            u.criteria[p] = this._collapseArray(u.criteria[p]);
        return u;
      }, h.prototype._updateTitle = function(u) {
        this.dom.title.html(this.s.dt.i18n("searchBuilder.title", this.c.i18n.title, u));
      }, h.prototype._build = function() {
        var u = this;
        this.dom.clearAll.remove(), this.dom.container.empty();
        var p = this.s.topGroup.count();
        this._updateTitle(p), this.dom.titleRow.append(this.dom.title), this.dom.container.append(this.dom.titleRow), this.dom.topGroup = this.s.topGroup.getNode(), this.dom.container.append(this.dom.topGroup), this._setRedrawListener();
        var v = this.s.dt.table(0).node();
        c.fn.dataTable.ext.search.includes(this.s.search) || (this.s.search = function(S, T, A) {
          return S.nTable !== v ? !0 : u.s.topGroup.search(T, A);
        }, c.fn.dataTable.ext.search.push(this.s.search)), this.s.dt.on("destroy.dtsb", function() {
          u.dom.container.remove(), u.dom.clearAll.remove();
          for (var S = c.fn.dataTable.ext.search.indexOf(u.s.search); S !== -1; )
            c.fn.dataTable.ext.search.splice(S, 1), S = c.fn.dataTable.ext.search.indexOf(u.s.search);
          u.s.dt.off(".dtsb"), c(u.s.dt.table().node()).off(".dtsb");
        });
      }, h.prototype._checkClear = function() {
        this.s.topGroup.s.criteria.length > 0 ? (this.dom.clearAll.insertAfter(this.dom.title), this._setClearListener()) : this.dom.clearAll.remove();
      }, h.prototype._filterChanged = function(u) {
        var p = this.c.filterChanged;
        typeof p == "function" && p(u, this.s.dt.i18n("searchBuilder.button", this.c.i18n.button, u));
      }, h.prototype._setClearListener = function() {
        var u = this;
        this.dom.clearAll.unbind("click"), this.dom.clearAll.on("click.dtsb", function(p, v) {
          return u.s.topGroup = new l(u.s.dt, u.c, void 0, void 0, void 0, void 0, u.s.serverData), u._build(), v !== !1 && u.s.dt.draw(), u.s.topGroup.setListeners(), u.dom.clearAll.remove(), u._setEmptyListener(), u._filterChanged(0), !1;
        });
      }, h.prototype._setRedrawListener = function() {
        var u = this;
        this.s.topGroup.dom.container.unbind("dtsb-redrawContents"), this.s.topGroup.dom.container.on("dtsb-redrawContents.dtsb", function() {
          u._checkClear(), u.s.topGroup.redrawContents(), u.s.topGroup.setupLogic(), u._setEmptyListener();
          var p = u.s.topGroup.count();
          u._updateTitle(p), u._filterChanged(p), u.s.dt.page.info().serverSide || u.s.dt.draw(), u.s.dt.state.save();
        }), this.s.topGroup.dom.container.unbind("dtsb-redrawContents-noDraw"), this.s.topGroup.dom.container.on("dtsb-redrawContents-noDraw.dtsb", function() {
          u._checkClear(), u.s.topGroup.s.preventRedraw = !0, u.s.topGroup.redrawContents(), u.s.topGroup.s.preventRedraw = !1, u.s.topGroup.setupLogic(), u._setEmptyListener();
          var p = u.s.topGroup.count();
          u._updateTitle(p), u._filterChanged(p);
        }), this.s.topGroup.dom.container.unbind("dtsb-redrawLogic"), this.s.topGroup.dom.container.on("dtsb-redrawLogic.dtsb", function() {
          u.s.topGroup.redrawLogic();
          var p = u.s.topGroup.count();
          u._updateTitle(p), u._filterChanged(p);
        }), this.s.topGroup.dom.container.unbind("dtsb-add"), this.s.topGroup.dom.container.on("dtsb-add.dtsb", function() {
          var p = u.s.topGroup.count();
          u._updateTitle(p), u._filterChanged(p), u._checkClear();
        }), this.s.dt.on("postEdit.dtsb postCreate.dtsb postRemove.dtsb", function() {
          u.s.topGroup.redrawContents();
        }), this.s.topGroup.dom.container.unbind("dtsb-clearContents"), this.s.topGroup.dom.container.on("dtsb-clearContents.dtsb", function() {
          u._setUp(!1), u._filterChanged(0), u.s.dt.draw();
        });
      }, h.prototype._setEmptyListener = function() {
        var u = this;
        this.s.topGroup.dom.add.on("click.dtsb", function() {
          u._checkClear();
        }), this.s.topGroup.dom.container.on("dtsb-destroy.dtsb", function() {
          u.dom.clearAll.remove();
        });
      }, h.version = "1.8.4", h.classes = {
        button: "dtsb-button",
        clearAll: "dtsb-clearAll",
        container: "dtsb-searchBuilder",
        inputButton: "dtsb-iptbtn",
        title: "dtsb-title",
        titleRow: "dtsb-titleRow"
      }, h.defaults = {
        columns: !0,
        conditions: {
          date: s.dateConditions,
          html: s.stringConditions,
          "html-num": s.numConditions,
          "html-num-fmt": s.numFmtConditions,
          luxon: s.luxonDateConditions,
          moment: s.momentDateConditions,
          num: s.numConditions,
          "num-fmt": s.numFmtConditions,
          string: s.stringConditions
        },
        depthLimit: !1,
        enterSearch: !1,
        filterChanged: void 0,
        greyscale: !1,
        liveSearch: !0,
        i18n: {
          add: "Add Condition",
          button: {
            0: "Search Builder",
            _: "Search Builder (%d)"
          },
          clearAll: "Clear All",
          condition: "Condition",
          conditions: {
            array: {
              contains: "Contains",
              empty: "Empty",
              equals: "Equals",
              not: "Not",
              notEmpty: "Not Empty",
              without: "Without"
            },
            date: {
              after: "After",
              before: "Before",
              between: "Between",
              empty: "Empty",
              equals: "Equals",
              not: "Not",
              notBetween: "Not Between",
              notEmpty: "Not Empty"
            },
            // eslint-disable-next-line id-blacklist
            number: {
              between: "Between",
              empty: "Empty",
              equals: "Equals",
              gt: "Greater Than",
              gte: "Greater Than Equal To",
              lt: "Less Than",
              lte: "Less Than Equal To",
              not: "Not",
              notBetween: "Not Between",
              notEmpty: "Not Empty"
            },
            // eslint-disable-next-line id-blacklist
            string: {
              contains: "Contains",
              empty: "Empty",
              endsWith: "Ends With",
              equals: "Equals",
              not: "Not",
              notContains: "Does Not Contain",
              notEmpty: "Not Empty",
              notEndsWith: "Does Not End With",
              notStartsWith: "Does Not Start With",
              startsWith: "Starts With"
            }
          },
          data: "Data",
          delete: "&times",
          deleteTitle: "Delete filtering rule",
          left: "<",
          leftTitle: "Outdent criteria",
          logicAnd: "And",
          logicOr: "Or",
          right: ">",
          rightTitle: "Indent criteria",
          search: "Search",
          title: {
            0: "Custom Search Builder",
            _: "Custom Search Builder (%d)"
          },
          value: "Value",
          valueJoiner: "and"
        },
        logic: "AND",
        orthogonal: {
          display: "display",
          search: "filter"
        },
        preDefined: !1
      }, h;
    }()
  );
  /*! SearchBuilder 1.8.4
   * ©SpryMedia Ltd - datatables.net/license/mit
   */
  g(_r), o(_r), i(_r);
  var w = _r.fn.dataTable;
  H.SearchBuilder = b, w.SearchBuilder = b, H.Group = l, w.Group = l, H.Criteria = s, w.Criteria = s;
  var C = H.Api.register;
  H.ext.searchBuilder = {
    conditions: {}
  }, H.ext.buttons.searchBuilder = {
    action: function(h, u, p, v) {
      this.popover(v._searchBuilder.getNode(), {
        align: "container",
        span: "container"
      });
      var S = v._searchBuilder.s.topGroup;
      S !== void 0 && S.dom.container.trigger("dtsb-redrawContents-noDraw"), S.s.criteria.length === 0 && _r("." + _r.fn.dataTable.Group.classes.add.replace(/ /g, ".")).click();
    },
    config: {},
    init: function(h, u, p) {
      var v = this, S = new H.SearchBuilder(h, p.config);
      h.on("draw", function() {
        var T = S.s.topGroup ? S.s.topGroup.count() : 0;
        v.text(h.i18n("searchBuilder.button", S.c.i18n.button, T));
      }), v.text(p.text || h.i18n("searchBuilder.button", S.c.i18n.button, 0)), p._searchBuilder = S;
    },
    text: null
  }, C("searchBuilder.getDetails()", function(h) {
    h === void 0 && (h = !1);
    var u = this.context[0];
    return u._searchBuilder ? u._searchBuilder.getDetails(h) : null;
  }), C("searchBuilder.rebuild()", function(h, u) {
    u === void 0 && (u = !0);
    var p = this.context[0];
    return p._searchBuilder === void 0 ? null : (p._searchBuilder.rebuild(h, u), this);
  }), C("searchBuilder.container()", function() {
    var h = this.context[0];
    return h._searchBuilder ? h._searchBuilder.getNode() : null;
  });
  function m(h, u) {
    var p = new H.Api(h), v = u || p.init().searchBuilder || H.defaults.searchBuilder, S = new b(p, v), T = S.getNode();
    return T;
  }
  _r(document).on("preInit.dt.dtsp", function(h, u) {
    h.namespace === "dt" && (u.oInit.searchBuilder || H.defaults.searchBuilder) && (u._searchBuilder || m(u));
  }), H.ext.feature.push({
    cFeature: "Q",
    fnInit: m
  }), H.feature && H.feature.register("searchBuilder", m);
})();
/*! Bootstrap 5 ui integration for DataTables' SearchBuilder
 * © SpryMedia Ltd - datatables.net/license
 */
let nu = lt;
nu.extend(!0, H.SearchBuilder.classes, {
  clearAll: "btn btn-secondary dtsb-clearAll"
});
nu.extend(!0, H.Group.classes, {
  add: "btn btn-secondary dtsb-add",
  clearGroup: "btn btn-secondary dtsb-clearGroup",
  logic: "btn btn-secondary dtsb-logic",
  search: "btn btn-secondary dtsb-search"
});
nu.extend(!0, H.Criteria.classes, {
  condition: "form-select dtsb-condition",
  data: "dtsb-data form-select",
  delete: "btn btn-secondary dtsb-delete",
  input: "form-control dtsb-input",
  left: "btn btn-secondary dtsb-left",
  right: "btn btn-secondary dtsb-right",
  select: "form-select",
  value: "dtsb-value"
});
/*! RowGroup 1.6.0
 * © SpryMedia Ltd - datatables.net/license
 */
let tn = lt;
var dr = function(t, n) {
  if (!H.versionCheck || !H.versionCheck("2"))
    throw new Error("RowGroup requires DataTables 2 or newer");
  this.c = tn.extend(!0, {}, H.defaults.rowGroup, dr.defaults, n), this.s = {
    dt: new H.Api(t)
  }, this.dom = {};
  var e = this.s.dt.settings()[0], r = e.rowGroup;
  if (r)
    return r;
  e.rowGroup = this, this._constructor();
};
tn.extend(dr.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * API methods for DataTables API interface
   */
  /**
   * Get/set the grouping data source - need to call draw after this is
   * executed as a setter
   * @returns string~RowGroup
   */
  dataSrc: function(t) {
    if (t === void 0)
      return this.c.dataSrc;
    var n = this.s.dt;
    return this.c.dataSrc = t, tn(n.table().node()).triggerHandler("rowgroup-datasrc.dt", [n, t]), this;
  },
  /**
   * Disable - need to call draw after this is executed
   * @returns RowGroup
   */
  disable: function() {
    return this.c.enable = !1, this;
  },
  /**
   * Enable - need to call draw after this is executed
   * @returns RowGroup
   */
  enable: function(t) {
    return t === !1 ? this.disable() : (this.c.enable = !0, this);
  },
  /**
   * Get enabled flag
   * @returns boolean
   */
  enabled: function() {
    return this.c.enable;
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  _constructor: function() {
    var t = this, n = this.s.dt, e = n.settings()[0], r = tn("div.dt-scroll-body", n.table().container());
    n.on("draw.dtrg", function(s, a) {
      t.c.enable && e === a && (t._draw(), i && r.scrollTop() && (r.scrollTop(i), i = null));
    }), n.on("column-visibility.dt.dtrg responsive-resize.dt.dtrg", function() {
      t._adjustColspan();
    }), n.on("destroy", function() {
      n.off(".dtrg");
    });
    var i = null;
    r.length && n.on("preDraw", function() {
      i = r.scrollTop();
    });
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Adjust column span when column visibility changes
   * @private
   */
  _adjustColspan: function() {
    let t = tn("tr." + this.c.className, this.s.dt.table().body()).find("th:visible, td:visible");
    t.length === 1 && t.attr("colspan", this._colspan());
  },
  /**
   * Get the number of columns that a grouping row should span
   * @private
   */
  _colspan: function() {
    return this.s.dt.columns().visible().reduce(function(t, n) {
      return t + n;
    }, 0);
  },
  /**
   * Update function that is called whenever we need to draw the grouping rows.
   * This is basically a bootstrap for the self iterative _group and _groupDisplay
   * methods
   * @private
   */
  _draw: function() {
    var t = this.s.dt;
    if (!(this.c.dataSrc === null || Array.isArray(this.c.dataSrc) && this.c.dataSrc.length === 0)) {
      var n = this._group(0, t.rows({ page: "current" }).indexes());
      this._groupDisplay(0, n);
    }
  },
  /**
   * Get the grouping information from a data set (index) of rows
   * @param {number} level Nesting level
   * @param {DataTables.Api} rows API of the rows to consider for this group
   * @returns {object[]} Nested grouping information - it is structured like this:
   *	{
   *		dataPoint: 'Edinburgh',
   *		rows: [ 1,2,3,4,5,6,7 ],
   *		children: [ {
   *			dataPoint: 'developer'
   *			rows: [ 1, 2, 3 ]
   *		},
   *		{
   *			dataPoint: 'support',
   *			rows: [ 4, 5, 6, 7 ]
   *		} ]
   *	}
   * @private
   */
  _group: function(t, n) {
    var e = Array.isArray(this.c.dataSrc) ? this.c.dataSrc : [this.c.dataSrc], r = H.util.get(e[t]), i = this.s.dt, s, a, o, l, c = [], f = this;
    for (o = 0, l = n.length; o < l; o++) {
      var g = n[o], b = i.row(g).data();
      s = r(b, t), s == null && (s = f.c.emptyDataGroup), (a === void 0 || s !== a) && (c.push({
        dataPoint: s,
        rows: []
      }), a = s), c[c.length - 1].rows.push(g);
    }
    if (e[t + 1] !== void 0)
      for (o = 0, l = c.length; o < l; o++)
        c[o].children = this._group(t + 1, c[o].rows);
    return c;
  },
  /**
   * Row group display - insert the rows into the document
   * @param {number} level Nesting level
   * @param {object[]} groups Takes the nested array from `_group`
   * @private
   */
  _groupDisplay: function(t, n) {
    for (var e = this.s.dt, r, i = 0, s = n.length; i < s; i++) {
      var a = n[i], o = a.dataPoint, l, c = a.rows;
      this.c.startRender && (r = this.c.startRender.call(this, e.rows(c), o, t), l = this._rowWrap(r, this.c.startClassName, t), l && l.insertBefore(e.row(c[0]).node())), this.c.endRender && (r = this.c.endRender.call(this, e.rows(c), o, t), l = this._rowWrap(r, this.c.endClassName, t), l && l.insertAfter(e.row(c[c.length - 1]).node())), a.children && this._groupDisplay(t + 1, a.children);
    }
  },
  /**
   * Take a rendered value from an end user and make it suitable for display
   * as a row, by wrapping it in a row, or detecting that it is a row.
   * @param {node|jQuery|string} display Display value
   * @param {string} className Class to add to the row
   * @param {array} group
   * @param {number} group level
   * @private
   */
  _rowWrap: function(t, n, e) {
    var r;
    return (t === null || t === "") && (t = this.c.emptyDataGroup), t == null ? null : (typeof t == "object" && t.nodeName && t.nodeName.toLowerCase() === "tr" ? r = tn(t) : t instanceof tn && t.length && t[0].nodeName.toLowerCase() === "tr" ? r = t : r = tn("<tr/>").append(
      tn("<th/>").attr("colspan", this._colspan()).attr("scope", "row").append(t)
    ), r.addClass(this.c.className).addClass(n).addClass("dtrg-level-" + e));
  }
});
dr.defaults = {
  /**
   * Class to apply to grouping rows - applied to both the start and
   * end grouping rows.
   * @type string
   */
  className: "dtrg-group",
  /**
   * Data property from which to read the grouping information
   * @type string|integer|array
   */
  dataSrc: 0,
  /**
   * Text to show if no data is found for a group
   * @type string
   */
  emptyDataGroup: "No group",
  /**
   * Initial enablement state
   * @boolean
   */
  enable: !0,
  /**
   * Class name to give to the end grouping row
   * @type string
   */
  endClassName: "dtrg-end",
  /**
   * End grouping label function
   * @function
   */
  endRender: null,
  /**
   * Class name to give to the start grouping row
   * @type string
   */
  startClassName: "dtrg-start",
  /**
   * Start grouping label function
   * @function
   */
  startRender: function(t, n) {
    return n;
  }
};
dr.version = "1.6.0";
tn.fn.dataTable.RowGroup = dr;
tn.fn.DataTable.RowGroup = dr;
H.Api.register("rowGroup()", function() {
  return this;
});
H.Api.register("rowGroup().disable()", function() {
  return this.iterator("table", function(t) {
    t.rowGroup && t.rowGroup.enable(!1);
  });
});
H.Api.register("rowGroup().enable()", function(t) {
  return this.iterator("table", function(n) {
    n.rowGroup && n.rowGroup.enable(t === void 0 ? !0 : t);
  });
});
H.Api.register("rowGroup().enabled()", function() {
  var t = this.context;
  return t.length && t[0].rowGroup ? t[0].rowGroup.enabled() : !1;
});
H.Api.register("rowGroup().dataSrc()", function(t) {
  if (t === void 0) {
    let n = this.context[0].rowGroup;
    return n ? n.dataSrc() : [];
  }
  return this.iterator("table", function(n) {
    n.rowGroup || new dr(this.context[0]), n.rowGroup.dataSrc(t);
  });
});
tn(document).on("preInit.dt.dtrg", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = n.oInit.rowGroup, i = H.defaults.rowGroup;
    if (r || i) {
      var s = tn.extend({}, i, r);
      r !== !1 && new dr(n, s);
    }
  }
});
/*! RowReorder 1.5.1
 * © SpryMedia Ltd - datatables.net/license
 */
let Ce = lt;
var $r = function(t, n) {
  if (!H.versionCheck || !H.versionCheck("1.11"))
    throw "DataTables RowReorder requires DataTables 1.11 or newer";
  this.c = Ce.extend(!0, {}, H.defaults.rowReorder, $r.defaults, n), this.s = {
    /** @type {integer} Scroll body top cache */
    bodyTop: null,
    /** @type {DataTable.Api} DataTables' API instance */
    dt: new H.Api(t),
    /** @type {function} Data fetch function */
    getDataFn: H.util.get(this.c.dataSrc),
    /** @type {array} Pixel positions for row insertion calculation */
    middles: null,
    /** @type {Object} Cached dimension information for use in the mouse move event handler */
    scroll: {},
    /** @type {integer} Interval object used for smooth scrolling */
    scrollInterval: null,
    /** @type {function} Data set function */
    setDataFn: H.util.set(this.c.dataSrc),
    /** @type {Object} Mouse down information */
    start: {
      top: 0,
      left: 0,
      offsetTop: 0,
      offsetLeft: 0,
      nodes: [],
      rowIndex: 0
    },
    /** @type {integer} Window height cached value */
    windowHeight: 0,
    /** @type {integer} Document outer height cached value */
    documentOuterHeight: 0,
    /** @type {integer} DOM clone outer height cached value */
    domCloneOuterHeight: 0,
    /** @type {integer} Flag used for signing if the drop is enabled or not */
    dropAllowed: !0
  }, this.dom = {
    /** @type {jQuery} Cloned row being moved around */
    clone: null,
    cloneParent: null,
    /** @type {jQuery} DataTables scrolling container */
    dtScroll: Ce("div.dataTables_scrollBody, div.dt-scroll-body", this.s.dt.table().container())
  };
  var e = this.s.dt.settings()[0], r = e.rowreorder;
  if (r)
    return r;
  this.dom.dtScroll.length || (this.dom.dtScroll = Ce(this.s.dt.table().container(), "tbody")), e.rowreorder = this, this._constructor();
};
Ce.extend($r.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Initialise the RowReorder instance
   *
   * @private
   */
  _constructor: function() {
    var t = this, n = this.s.dt, e = Ce(n.table().node());
    e.css("position") === "static" && e.css("position", "relative"), Ce(n.table().container()).on(
      "mousedown.rowReorder touchstart.rowReorder",
      this.c.selector,
      function(r) {
        if (t.c.enable) {
          if (Ce(r.target).is(t.c.excludedChildren))
            return !0;
          var i = Ce(this).closest("tr"), s = n.row(i);
          if (s.any())
            return t._emitEvent("pre-row-reorder", {
              node: s.node(),
              index: s.index()
            }), t._mouseDown(r, i), !1;
        }
      }
    ), n.on("destroy.rowReorder", function() {
      Ce(n.table().container()).off(".rowReorder"), n.off(".rowReorder");
    }), this._keyup = this._keyup.bind(this);
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Cache the measurements that RowReorder needs in the mouse move handler
   * to attempt to speed things up, rather than reading from the DOM.
   *
   * @private
   */
  _cachePositions: function() {
    var t = this.s.dt, n = Ce(t.table().node()).find("thead").outerHeight(), e = Ce.uniqueSort(t.rows({ page: "current" }).nodes().toArray()), r = Ce.map(e, function(i, s) {
      var a = Ce(i).position().top - n;
      return (a + a + Ce(i).outerHeight()) / 2;
    });
    this.s.middles = r, this.s.bodyTop = Ce(t.table().body()).offset().top, this.s.windowHeight = Ce(window).height(), this.s.documentOuterHeight = Ce(document).outerHeight(), this.s.bodyArea = this._calcBodyArea();
  },
  /**
   * Clone a row so it can be floated around the screen
   *
   * @param  {jQuery} target Node to be cloned
   * @private
   */
  _clone: function(t) {
    var n = this.s.dt, e = Ce(n.table().node().cloneNode(!1)).addClass("dt-rowReorder-float").append("<tbody/>").append(t.clone(!1)), r = t.outerWidth(), i = t.outerHeight(), s = Ce(Ce(this.s.dt.table().node()).parent()), a = s.width(), o = s.scrollLeft(), l = t.children().map(function() {
      return Ce(this).width();
    });
    e.width(r).height(i).find("tr").children().each(function(f) {
      this.style.width = l[f] + "px";
    });
    var c = Ce("<div>").addClass("dt-rowReorder-float-parent").width(a).append(e).appendTo("body").scrollLeft(o);
    this.dom.clone = e, this.dom.cloneParent = c, this.s.domCloneOuterHeight = e.outerHeight();
  },
  /**
   * Update the cloned item's position in the document
   *
   * @param  {object} e Event giving the mouse's position
   * @private
   */
  _clonePosition: function(t) {
    var n = this.s.start, e = this._eventToPage(t, "Y") - n.top, r = this._eventToPage(t, "X") - n.left, i = this.c.snapX, s, a = e + n.offsetTop;
    i === !0 ? s = n.offsetLeft : typeof i == "number" ? s = n.offsetLeft + i : s = r + n.offsetLeft + this.dom.cloneParent.scrollLeft(), a < 0 ? a = 0 : a + this.s.domCloneOuterHeight > this.s.documentOuterHeight && (a = this.s.documentOuterHeight - this.s.domCloneOuterHeight), this.dom.cloneParent.css({
      top: a,
      left: s
    });
  },
  /**
   * Emit an event on the DataTable for listeners
   *
   * @param  {string} name Event name
   * @param  {array} args Event arguments
   * @private
   */
  _emitEvent: function(t, n) {
    var e;
    return this.s.dt.iterator("table", function(r, i) {
      var s = Ce(r.nTable).triggerHandler(t + ".dt", n);
      s !== void 0 && (e = s);
    }), e;
  },
  /**
   * Get pageX/Y position from an event, regardless of if it is a mouse or
   * touch event.
   *
   * @param  {object} e Event
   * @param  {string} pos X or Y (must be a capital)
   * @private
   */
  _eventToPage: function(t, n) {
    return t.type.indexOf("touch") !== -1 ? t.originalEvent.touches[0]["page" + n] : t["page" + n];
  },
  /**
   * Mouse down event handler. Read initial positions and add event handlers
   * for the move.
   *
   * @param  {object} e      Mouse event
   * @param  {jQuery} target TR element that is to be moved
   * @private
   */
  _mouseDown: function(t, n) {
    var e = this, r = this.s.dt, i = this.s.start, s = this.c.cancelable, a = n.offset();
    i.top = this._eventToPage(t, "Y"), i.left = this._eventToPage(t, "X"), i.offsetTop = a.top, i.offsetLeft = a.left, i.nodes = Ce.uniqueSort(r.rows({ page: "current" }).nodes().toArray()), this._cachePositions(), this._clone(n), this._clonePosition(t);
    var o = this._eventToPage(t, "Y") - this.s.bodyTop;
    i.rowIndex = this._calcRowIndexByPos(o), this.dom.target = n, n.addClass("dt-rowReorder-moving"), Ce(document).on("mouseup.rowReorder touchend.rowReorder", function(c) {
      e._mouseUp(c);
    }).on("mousemove.rowReorder touchmove.rowReorder", function(c) {
      e._mouseMove(c);
    }), Ce(window).width() === Ce(document).width() && Ce(document.body).addClass("dt-rowReorder-noOverflow");
    var l = this.dom.dtScroll;
    this.s.scroll = {
      windowHeight: Ce(window).height(),
      windowWidth: Ce(window).width(),
      dtTop: l.length ? l.offset().top : null,
      dtLeft: l.length ? l.offset().left : null,
      dtHeight: l.length ? l.outerHeight() : null,
      dtWidth: l.length ? l.outerWidth() : null
    }, s && Ce(document).on("keyup", this._keyup);
  },
  /**
   * Mouse move event handler - move the cloned row and shuffle the table's
   * rows if required.
   *
   * @param  {object} e Mouse event
   * @private
   */
  _mouseMove: function(t) {
    this._clonePosition(t);
    var n = this.s.start, e = this.c.cancelable;
    if (e) {
      var r = this.s.bodyArea, i = this._calcCloneParentArea();
      this.s.dropAllowed = this._rectanglesIntersect(r, i), Ce(this.dom.cloneParent).toggleClass("drop-not-allowed", !this.s.dropAllowed);
    }
    for (var s = this._eventToPage(t, "Y") - this.s.bodyTop, a = this.s.middles, o = null, l = 0, c = a.length; l < c; l++)
      if (s < a[l]) {
        o = l;
        break;
      }
    o === null && (o = a.length), e && (this.s.dropAllowed || (o = n.rowIndex > this.s.lastInsert ? n.rowIndex + 1 : n.rowIndex), this.dom.target.toggleClass("dt-rowReorder-moving", this.s.dropAllowed)), this._moveTargetIntoPosition(o), this._shiftScroll(t);
  },
  /**
   * Mouse up event handler - release the event handlers and perform the
   * table updates
   *
   * @param  {object} e Mouse event
   * @private
   */
  _mouseUp: function(t) {
    var n = this, e = this.s.dt, r, i, s = this.c.dataSrc, a = this.s.dropAllowed;
    if (!a) {
      n._cancel();
      return;
    }
    var o = this.s.start.nodes, l = Ce.uniqueSort(e.rows({ page: "current" }).nodes().toArray()), c = {}, f = [], g = [], b = this.s.getDataFn, w = this.s.setDataFn;
    for (r = 0, i = o.length; r < i; r++)
      if (o[r] !== l[r]) {
        var C = e.row(l[r]).id(), m = e.row(l[r]).data(), h = e.row(o[r]).data();
        C && (c[C] = b(h)), f.push({
          node: l[r],
          oldData: b(m),
          newData: b(h),
          newPosition: r,
          oldPosition: Ce.inArray(l[r], o)
        }), g.push(l[r]);
      }
    var u = [
      f,
      {
        dataSrc: s,
        nodes: g,
        values: c,
        triggerRow: e.row(this.dom.target),
        originalEvent: t
      }
    ], p = this._emitEvent("row-reorder", u);
    if (p === !1) {
      n._cancel();
      return;
    }
    this._cleanupDragging();
    var v = function() {
      if (n.c.update) {
        for (r = 0, i = f.length; r < i; r++) {
          var S = e.row(f[r].node), T = S.data();
          w(T, f[r].newData), e.columns().every(function() {
            this.dataSrc() === s && e.cell(f[r].node, this.index()).invalidate("data");
          });
        }
        n._emitEvent("row-reordered", u), e.draw(!1);
      }
    };
    this.c.editor ? (this.c.enable = !1, this.c.editor.edit(g, !1, Ce.extend({ submit: "changed" }, this.c.formOptions)).multiSet(s, c).one("preSubmitCancelled.rowReorder", function() {
      n.c.enable = !0, n.c.editor.off(".rowReorder"), e.draw(!1);
    }).one("submitUnsuccessful.rowReorder", function() {
      e.draw(!1);
    }).one("submitSuccess.rowReorder", function() {
      v();
    }).one("submitComplete", function() {
      n.c.enable = !0, n.c.editor.off(".rowReorder");
    }).submit()) : v();
  },
  /**
   * Moves the current target into the given position within the table
   * and caches the new positions
   *
   * @param  {integer} insertPoint Position
   * @private
   */
  _moveTargetIntoPosition: function(t) {
    var n = this.s.dt;
    if (this.s.lastInsert === null || this.s.lastInsert !== t) {
      var e = Ce.uniqueSort(n.rows({ page: "current" }).nodes().toArray()), r = "";
      t > this.s.lastInsert ? (this.dom.target.insertAfter(e[t - 1]), r = "after") : (this.dom.target.insertBefore(e[t]), r = "before"), this._cachePositions(), this.s.lastInsert = t, this._emitEvent("row-reorder-changed", {
        insertPlacement: r,
        insertPoint: t,
        row: n.row(this.dom.target)
      });
    }
  },
  /**
   * Removes the cloned elements, event handlers, scrolling intervals, etc
   *
   * @private
   */
  _cleanupDragging: function() {
    var t = this.c.cancelable;
    this.dom.clone.remove(), this.dom.cloneParent.remove(), this.dom.clone = null, this.dom.cloneParent = null, this.dom.target.removeClass("dt-rowReorder-moving"), Ce(document).off(".rowReorder"), Ce(document.body).removeClass("dt-rowReorder-noOverflow"), clearInterval(this.s.scrollInterval), this.s.scrollInterval = null, t && Ce(document).off("keyup", this._keyup);
  },
  /**
   * Move the window and DataTables scrolling during a drag to scroll new
   * content into view.
   *
   * This matches the `_shiftScroll` method used in AutoFill, but only
   * horizontal scrolling is considered here.
   *
   * @param  {object} e Mouse move event object
   * @private
   */
  _shiftScroll: function(t) {
    var n = this, e = this.s.scroll, r = !1, i = 5, s = 65, a = t.pageY - document.body.scrollTop, o, l;
    a < Ce(window).scrollTop() + s ? o = i * -1 : a > e.windowHeight + Ce(window).scrollTop() - s && (o = i), e.dtTop !== null && t.pageY < e.dtTop + s ? l = i * -1 : e.dtTop !== null && t.pageY > e.dtTop + e.dtHeight - s && (l = i), o || l ? (e.windowVert = o, e.dtVert = l, r = !0) : this.s.scrollInterval && (clearInterval(this.s.scrollInterval), this.s.scrollInterval = null), !this.s.scrollInterval && r && (this.s.scrollInterval = setInterval(function() {
      if (e.windowVert) {
        var c = Ce(document).scrollTop();
        if (Ce(document).scrollTop(c + e.windowVert), c !== Ce(document).scrollTop()) {
          var f = parseFloat(n.dom.cloneParent.css("top"));
          n.dom.cloneParent.css("top", f + e.windowVert);
        }
      }
      if (e.dtVert) {
        var g = n.dom.dtScroll[0];
        e.dtVert && (g.scrollTop += e.dtVert);
      }
    }, 20));
  },
  /**
   * Calculates the current area of the table body and returns it as a rectangle
   *
   * @private
   */
  _calcBodyArea: function(t) {
    var n = this.s.dt, e = Ce(n.table().body()).offset(), r = {
      left: e.left,
      top: e.top,
      right: e.left + Ce(n.table().body()).width(),
      bottom: e.top + Ce(n.table().body()).height()
    };
    return r;
  },
  /**
   * Calculates the current area of the cloned parent element and returns it as a rectangle
   *
   * @private
   */
  _calcCloneParentArea: function(t) {
    var n = Ce(this.dom.cloneParent).offset(), e = {
      left: n.left,
      top: n.top,
      right: n.left + Ce(this.dom.cloneParent).width(),
      bottom: n.top + Ce(this.dom.cloneParent).height()
    };
    return e;
  },
  /**
   * Returns whether the given reactangles intersect or not
   *
   * @private
   */
  _rectanglesIntersect: function(t, n) {
    var e = t.left >= n.right || n.left >= t.right || t.top >= n.bottom || n.top >= t.bottom;
    return !e;
  },
  /**
   * Calculates the index of the row which lays under the given Y position or
   * returns -1 if no such row
   *
   * @param  {integer} insertPoint Position
   * @private
   */
  _calcRowIndexByPos: function(t) {
    var n = this.s.dt, e = Ce.uniqueSort(n.rows({ page: "current" }).nodes().toArray()), r = -1, i = Ce(n.table().node()).find("thead").outerHeight();
    return Ce.each(e, function(s, a) {
      var o = Ce(a).position().top - i, l = o + Ce(a).outerHeight();
      t >= o && t <= l && (r = s);
    }), r;
  },
  /**
   * Handles key up events and cancels the dragging if ESC key is pressed
   *
   * @param  {object} e Mouse move event object
   * @private
   */
  _keyup: function(t) {
    var n = this.c.cancelable;
    n && t.which === 27 && (t.preventDefault(), this._cancel());
  },
  /**
   * Cancels the dragging, moves target back into its original position
   * and cleans up the dragging
   *
   * @param  {object} e Mouse move event object
   * @private
   */
  _cancel: function() {
    var t = this.s.start, n = t.rowIndex > this.s.lastInsert ? t.rowIndex + 1 : t.rowIndex;
    this._moveTargetIntoPosition(n), this._cleanupDragging(), this._emitEvent("row-reorder-canceled", [this.s.start.rowIndex]);
  }
});
$r.defaults = {
  /**
   * Data point in the host row's data source object for where to get and set
   * the data to reorder. This will normally also be the sorting column.
   *
   * @type {Number}
   */
  dataSrc: 0,
  /**
   * Editor instance that will be used to perform the update
   *
   * @type {DataTable.Editor}
   */
  editor: null,
  /**
   * Enable / disable RowReorder's user interaction
   * @type {Boolean}
   */
  enable: !0,
  /**
   * Form options to pass to Editor when submitting a change in the row order.
   * See the Editor `from-options` object for details of the options
   * available.
   * @type {Object}
   */
  formOptions: {},
  /**
   * Drag handle selector. This defines the element that when dragged will
   * reorder a row.
   *
   * @type {String}
   */
  selector: "td:first-child",
  /**
   * Optionally lock the dragged row's x-position. This can be `true` to
   * fix the position match the host table's, `false` to allow free movement
   * of the row, or a number to define an offset from the host table.
   *
   * @type {Boolean|number}
   */
  snapX: !1,
  /**
   * Update the table's data on drop
   *
   * @type {Boolean}
   */
  update: !0,
  /**
   * Selector for children of the drag handle selector that mouseDown events
   * will be passed through to and drag will not activate
   *
   * @type {String}
   */
  excludedChildren: "a",
  /**
   * Enable / disable the canceling of the drag & drop interaction
   *
   * @type {Boolean}
   */
  cancelable: !1
};
var ru = Ce.fn.dataTable.Api;
ru.register("rowReorder()", function() {
  return this;
});
ru.register("rowReorder.enable()", function(t) {
  return t === void 0 && (t = !0), this.iterator("table", function(n) {
    n.rowreorder && (n.rowreorder.c.enable = t);
  });
});
ru.register("rowReorder.disable()", function() {
  return this.iterator("table", function(t) {
    t.rowreorder && (t.rowreorder.c.enable = !1);
  });
});
$r.version = "1.5.1";
Ce.fn.dataTable.RowReorder = $r;
Ce.fn.DataTable.RowReorder = $r;
Ce(document).on("init.dt.dtr", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = n.oInit.rowReorder, i = H.defaults.rowReorder;
    if (r || i) {
      var s = Ce.extend({}, r, i);
      r !== !1 && new $r(n, s);
    }
  }
});
/*! FixedHeader 4.0.6
 * © SpryMedia Ltd - datatables.net/license
 */
let xe = lt;
var jg = 0, lr = function(t, n) {
  if (!H.versionCheck("2"))
    throw "Warning: FixedHeader requires DataTables 2 or newer";
  if (!(this instanceof lr))
    throw "FixedHeader must be initialised with the 'new' keyword.";
  n === !0 && (n = {}), t = new H.Api(t), this.c = xe.extend(!0, {}, lr.defaults, n), this.s = {
    dt: t,
    position: {
      theadTop: 0,
      tbodyTop: 0,
      tfootTop: 0,
      tfootBottom: 0,
      width: 0,
      left: 0,
      tfootHeight: 0,
      theadHeight: 0,
      windowHeight: xe(window).height(),
      visible: !0
    },
    headerMode: null,
    footerMode: null,
    autoWidth: t.settings()[0].oFeatures.bAutoWidth,
    namespace: ".dtfc" + jg++,
    scrollLeft: {
      header: -1,
      footer: -1
    },
    enable: !0,
    autoDisable: !1
  }, this.dom = {
    floatingHeader: null,
    thead: xe(t.table().header()),
    tbody: xe(t.table().body()),
    tfoot: xe(t.table().footer()),
    header: {
      host: null,
      scrollAdjust: null,
      floating: null,
      floatingParent: xe(
        '<div class="dtfh-floatingparent"><div class="dtfh-floating-limiter"><div></div></div></div>'
      ),
      limiter: null,
      placeholder: null
    },
    footer: {
      host: null,
      scrollAdjust: null,
      floating: null,
      floatingParent: xe(
        '<div class="dtfh-floatingparent"><div class="dtfh-floating-limiter"><div></div></div></div>'
      ),
      limiter: null,
      placeholder: null
    }
  };
  var e = this.dom;
  e.header.host = e.thead.parent(), e.header.limiter = e.header.floatingParent.children(), e.header.scrollAdjust = e.header.limiter.children(), e.footer.host = e.tfoot.parent(), e.footer.limiter = e.footer.floatingParent.children(), e.footer.scrollAdjust = e.footer.limiter.children();
  var r = t.settings()[0];
  if (r._fixedHeader)
    throw "FixedHeader already initialised on table " + r.nTable.id;
  r._fixedHeader = this, this._constructor();
};
xe.extend(lr.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * API methods
   */
  /**
   * Kill off FH and any events
   */
  destroy: function() {
    var t = this.dom;
    this.s.dt.off(".dtfc"), xe("body").off(".dtfc"), xe(window).off(this.s.namespace), t.header.rightBlocker && t.header.rightBlocker.remove(), t.header.leftBlocker && t.header.leftBlocker.remove(), t.footer.rightBlocker && t.footer.rightBlocker.remove(), t.footer.leftBlocker && t.footer.leftBlocker.remove(), this.c.header && this._modeChange("in-place", "header", !0), this.c.footer && t.tfoot.length && this._modeChange("in-place", "footer", !0);
  },
  /**
   * Enable / disable the fixed elements
   *
   * @param  {boolean} enable `true` to enable, `false` to disable
   */
  enable: function(t, n, e) {
    this.s.enable = t, this.s.enableType = e, (n || n === void 0) && (this._positions(), this._scroll(!0));
  },
  /**
   * Get enabled status
   */
  enabled: function() {
    return this.s.enable;
  },
  /**
   * Set header offset
   *
   * @param  {int} new value for headerOffset
   */
  headerOffset: function(t) {
    return t !== void 0 && (this.c.headerOffset = t, this.update()), this.c.headerOffset;
  },
  /**
   * Set footer offset
   *
   * @param  {int} new value for footerOffset
   */
  footerOffset: function(t) {
    return t !== void 0 && (this.c.footerOffset = t, this.update()), this.c.footerOffset;
  },
  /**
   * Recalculate the position of the fixed elements and force them into place
   */
  update: function(t) {
    var n = this.s.dt.table().node();
    !this.s.enable && !this.s.autoDisable || (xe(n).is(":visible") ? (this.s.autoDisable = !1, this.enable(!0, !1)) : (this.s.autoDisable = !0, this.enable(!1, !1)), xe(n).children("thead").length !== 0 && (this._positions(), this._scroll(t !== void 0 ? t : !0), this._widths(this.dom.header), this._widths(this.dom.footer)));
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * FixedHeader constructor - adding the required event listeners and
   * simple initialisation
   *
   * @private
   */
  _constructor: function() {
    var t = this, n = this.s.dt;
    xe(window).on("scroll" + this.s.namespace, function() {
      t._scroll();
    }).on(
      "resize" + this.s.namespace,
      H.util.throttle(function() {
        t.s.position.windowHeight = xe(window).height(), t.update();
      }, 50)
    );
    var e = xe(".fh-fixedHeader");
    !this.c.headerOffset && e.length && (this.c.headerOffset = e.outerHeight());
    var r = xe(".fh-fixedFooter");
    !this.c.footerOffset && r.length && (this.c.footerOffset = r.outerHeight()), n.on(
      "column-reorder.dt.dtfc column-visibility.dt.dtfc column-sizing.dt.dtfc responsive-display.dt.dtfc",
      function(i, s) {
        t.update();
      }
    ), xe("body").on("draw.dt.dtfc", function(i, s) {
      t.update(s !== n.settings()[0]);
    }), n.on("destroy.dtfc", function() {
      t.destroy();
    }), this._positions(), this._scroll();
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Clone a fixed item to act as a place holder for the original element
   * which is moved into a clone of the table element, and moved around the
   * document to give the fixed effect.
   *
   * @param  {string}  item  'header' or 'footer'
   * @param  {boolean} force Force the clone to happen, or allow automatic
   *   decision (reuse existing if available)
   * @private
   */
  _clone: function(t, n) {
    var e = this, r = this.s.dt, i = this.dom[t], s = t === "header" ? this.dom.thead : this.dom.tfoot;
    if (!(t === "footer" && this._scrollEnabled()))
      if (!n && i.floating)
        i.floating.removeClass(
          "fixedHeader-floating fixedHeader-locked"
        );
      else {
        i.floating && (i.placeholder !== null && i.placeholder.detach(), i.floating.detach());
        var a = xe(r.table().node()), o = xe(a.parent()), l = this._scrollEnabled();
        i.floating = xe(r.table().node().cloneNode(!1)).attr("aria-hidden", "true").css({
          top: 0,
          left: 0
        }).removeAttr("id"), i.floatingParent.css({
          width: o[0].offsetWidth,
          position: "fixed",
          left: l ? a.offset().left + o.scrollLeft() : 0
        }).css(
          t === "header" ? {
            top: this.c.headerOffset,
            bottom: ""
          } : {
            top: "",
            bottom: this.c.footerOffset
          }
        ).addClass(
          t === "footer" ? "dtfh-floatingparent-foot" : "dtfh-floatingparent-head"
        ).appendTo("body").children().eq(0), i.limiter.css({
          width: "100%",
          overflow: "hidden",
          height: "fit-content"
        }), i.scrollAdjust.append(i.floating), this._stickyPosition(i.floating, "-");
        var c = function() {
          var g = o.scrollLeft();
          e.s.scrollLeft = { footer: g, header: g }, i.limiter.scrollLeft(e.s.scrollLeft.header);
        };
        c(), o.off("scroll.dtfh").on("scroll.dtfh", c), i.scrollAdjust.css({
          width: "fit-content",
          paddingRight: e.s.dt.settings()[0].oBrowser.barWidth
        });
        let f = xe(
          t === "footer" ? "div.dtfc-bottom-blocker" : "div.dtfc-top-blocker",
          r.table().container()
        );
        return f.length && f.clone().appendTo(i.floatingParent).css({
          position: "fixed",
          right: f.width()
        }), i.placeholder = s.clone(!1), i.placeholder.find("*[id]").removeAttr("id"), xe(i.placeholder).insertAfter(
          xe(t === "header" ? "colgroup" : "tbody", i.host)
        ), i.floating.append(s), this._widths(i), c;
      }
  },
  /**
   * This method sets the sticky position of the header elements to match fixed columns
   * @param {JQuery<HTMLElement>} el
   * @param {string} sign
   */
  _stickyPosition: function(t, n) {
    if (this._scrollEnabled()) {
      var e = this, r = xe(e.s.dt.table().node()).css("direction") === "rtl";
      t.find("th").each(function() {
        if (xe(this).css("position") === "sticky") {
          var i = xe(this).css("right"), s = xe(this).css("left"), a;
          i !== "auto" && !r ? (a = +i.replace(/px/g, ""), xe(this).css("right", a > 0 ? a : 0)) : s !== "auto" && r && (a = +s.replace(/px/g, ""), xe(this).css("left", a > 0 ? a : 0));
        }
      });
    }
  },
  /**
   * Reposition the floating elements to take account of horizontal page
   * scroll
   *
   * @param  {string} item       The `header` or `footer`
   * @param  {int}    scrollLeft Document scrollLeft
   * @private
   */
  _horizontal: function(t, n) {
    var e = this.dom[t], r = this.s.scrollLeft;
    if (e.floating && r[t] !== n) {
      if (this._scrollEnabled()) {
        var i = xe(
          xe(this.s.dt.table().node()).parent()
        ).scrollLeft();
        e.floating.scrollLeft(i), e.floatingParent.scrollLeft(i);
      }
      r[t] = n;
    }
  },
  /**
   * Change from one display mode to another. Each fixed item can be in one
   * of:
   *
   * * `in-place` - In the main DataTable
   * * `in` - Floating over the DataTable
   * * `below` - (Header only) Fixed to the bottom of the table body
   * * `above` - (Footer only) Fixed to the top of the table body
   *
   * @param  {string}  mode        Mode that the item should be shown in
   * @param  {string}  item        'header' or 'footer'
   * @param  {boolean} forceChange Force a redraw of the mode, even if already
   *     in that mode.
   * @private
   */
  _modeChange: function(t, n, e) {
    var r = this.s.dt, i = this.dom[n], s = this.s.position, a = this._scrollEnabled();
    if (!(n === "footer" && a)) {
      var o = function(S) {
        i.floating[0].style.setProperty("width", S + "px", "important"), a || i.floatingParent[0].style.setProperty("width", S + "px", "important");
      }, l = this.dom[n === "footer" ? "tfoot" : "thead"], c = xe.contains(l[0], document.activeElement) ? document.activeElement : null, f = xe(xe(this.s.dt.table().node()).parent());
      if (t === "in-place")
        i.placeholder && (i.placeholder.remove(), i.placeholder = null), xe.contains(i.host[0], l[0]) || (n === "header" ? l.insertAfter(xe("colgroup", i.host)) : i.host.append(l)), i.floating && (i.floating.remove(), i.floating = null, this._stickyPosition(i.host, "+")), i.floatingParent && (i.floatingParent.find("div.dtfc-top-blocker").remove(), i.floatingParent.remove()), xe(xe(i.host.parent()).parent()).scrollLeft(
          f.scrollLeft()
        );
      else if (t === "in") {
        let S = this._clone(n, e);
        var g = f.offset(), b = xe(document).scrollTop(), w = xe(window).height(), C = b + w, m = a ? g.top : s.tbodyTop, h = a ? g.top + f.outerHeight() : s.tfootTop, u;
        n === "footer" ? u = m > C ? s.tfootHeight : m + s.tfootHeight - C : u = b + this.c.headerOffset + s.theadHeight - h;
        var p = n === "header" ? "top" : "bottom", v = this.c[n + "Offset"] - (u > 0 ? u : 0);
        i.floating.addClass("fixedHeader-floating"), i.floatingParent.css(p, v).css({
          left: s.left,
          "z-index": 3
        }), o(s.width), S && S(), n === "footer" && i.floating.css("top", "");
      } else
        t === "below" ? (this._clone(n, e), i.floating.addClass("fixedHeader-locked"), i.floatingParent.css({
          position: "absolute",
          top: s.tfootTop - s.theadHeight,
          left: s.left + "px"
        }), o(s.width)) : t === "above" && (this._clone(n, e), i.floating.addClass("fixedHeader-locked"), i.floatingParent.css({
          position: "absolute",
          top: s.tbodyTop,
          left: s.left + "px"
        }), o(s.width));
      c && c !== document.activeElement && setTimeout(function() {
        c.focus();
      }, 10), this.s.scrollLeft.header = -1, this.s.scrollLeft.footer = -1, this.s[n + "Mode"] = t, r.trigger("fixedheader-mode", [t, n]);
    }
  },
  /**
   * Cache the positional information that is required for the mode
   * calculations that FixedHeader performs.
   *
   * @private
   */
  _positions: function() {
    var t = this.s.dt, n = t.table(), e = this.s.position, r = this.dom, i = xe(n.node()), s = this._scrollEnabled(), a = xe(t.table().header()), o = xe(t.table().footer()), l = r.tbody, c = i.parent();
    e.visible = i.is(":visible"), e.width = i.outerWidth(), e.left = i.offset().left, e.theadTop = a.offset().top, e.tbodyTop = s ? c.offset().top : l.offset().top, e.tbodyHeight = s ? c.outerHeight() : l.outerHeight(), e.theadHeight = a.outerHeight(), e.theadBottom = e.theadTop + e.theadHeight, e.tfootTop = e.tbodyTop + e.tbodyHeight, o.length ? (e.tfootBottom = e.tfootTop + o.outerHeight(), e.tfootHeight = o.outerHeight()) : (e.tfootBottom = e.tfootTop, e.tfootHeight = 0);
  },
  /**
   * Mode calculation - determine what mode the fixed items should be placed
   * into.
   *
   * @param  {boolean} forceChange Force a redraw of the mode, even if already
   *     in that mode.
   * @private
   */
  _scroll: function(t) {
    if (!this.s.dt.settings()[0].bDestroying) {
      var n = this._scrollEnabled(), e = xe(this.s.dt.table().node()).parent(), r = e.offset(), i = e.outerHeight(), s = xe(document).scrollLeft(), a = xe(document).scrollTop(), o = xe(window).height(), l = o + a, c = this.s.position, f, g, b = n ? r.top : c.tbodyTop, w = n ? r.left : c.left, C = n ? r.top + i : c.tfootTop, m = n ? e.outerWidth() : c.tbodyWidth;
      if (this.c.header) {
        if (!this.s.enable)
          f = "in-place";
        else if (!c.visible || a + this.c.headerOffset + c.theadHeight <= b)
          f = "in-place";
        else if (
          // The scrolling plus the header offset plus the height of the header is lower than the top of the body
          a + this.c.headerOffset + c.theadHeight > b && // And the scrolling at the top plus the header offset is above the bottom of the body
          a + this.c.headerOffset + c.theadHeight < C
        )
          if (f = "in", a + this.c.headerOffset + c.theadHeight > C || this.dom.header.floatingParent === void 0)
            t = !0;
          else {
            var h = this.dom.header.floatingParent.css({
              top: this.c.headerOffset,
              position: "fixed"
            }).children().eq(0);
            h.find(this.dom.header.floating).length === 0 && h.append(this.dom.header.floating);
          }
        else
          f = "below";
        (t || f !== this.s.headerMode) && this._modeChange(f, "header", t), this._horizontal("header", s);
      }
      var u = {
        offset: { top: 0, left: 0 },
        height: 0
      }, p = {
        offset: { top: 0, left: 0 },
        height: 0
      };
      if (this.c.footer && this.dom.tfoot.length && this.dom.tfoot.find("th, td").length) {
        this.s.enable ? !c.visible || c.tfootBottom + this.c.footerOffset <= l ? g = "in-place" : C + c.tfootHeight + this.c.footerOffset > l && b + this.c.footerOffset < l ? (g = "in", t = !0) : g = "above" : g = "in-place", (t || g !== this.s.footerMode) && this._modeChange(g, "footer", t), this._horizontal("footer", s);
        var v = function(y) {
          return {
            offset: y.offset(),
            height: y.outerHeight()
          };
        };
        if (u = this.dom.header.floating ? v(this.dom.header.floating) : v(this.dom.thead), p = this.dom.footer.floating ? v(this.dom.footer.floating) : v(this.dom.tfoot), n && p.offset.top > a) {
          var S = a - r.top, T = l + // If the gap between the top of the scrollbody and the window is more than
          //  the height of the header then the top of the table is still visible so add that gap
          // Doing this has effectively calculated the height from the top of the table to the bottom of the current page
          (S > -u.height ? S : 0) - // Take from that
          // The top of the header plus
          (u.offset.top + // The header height if the standard header is present
          (S < -u.height ? u.height : 0) + // And the height of the footer
          p.height);
          T < 0 && (T = 0), e.outerHeight(T), Math.round(e.outerHeight()) >= Math.round(T) ? xe(this.dom.tfoot.parent()).addClass("fixedHeader-floating") : xe(this.dom.tfoot.parent()).removeClass(
            "fixedHeader-floating"
          );
        }
      }
      if (this.dom.header.floating && this.dom.header.floatingParent.css("left", w - s), this.dom.footer.floating && this.dom.footer.floatingParent.css("left", w - s), this.s.dt.settings()[0]._fixedColumns !== void 0) {
        var A = function(y, I, $) {
          if ($ === void 0) {
            var V = xe(
              "div.dtfc-" + y + "-" + I + "-blocker"
            );
            $ = V.length === 0 ? null : V.clone().css("z-index", 1);
          }
          return $ !== null && (f === "in" || f === "below" ? $.appendTo("body").css({
            top: I === "top" ? u.offset.top : p.offset.top,
            left: y === "right" ? w + m - $.width() : w
          }) : $.detach()), $;
        };
        this.dom.header.rightBlocker = A(
          "right",
          "top",
          this.dom.header.rightBlocker
        ), this.dom.header.leftBlocker = A(
          "left",
          "top",
          this.dom.header.leftBlocker
        ), this.dom.footer.rightBlocker = A(
          "right",
          "bottom",
          this.dom.footer.rightBlocker
        ), this.dom.footer.leftBlocker = A(
          "left",
          "bottom",
          this.dom.footer.leftBlocker
        );
      }
    }
  },
  /**
   * Function to check if scrolling is enabled on the table or not
   * @returns Boolean value indicating if scrolling on the table is enabled or not
   */
  _scrollEnabled: function() {
    var t = this.s.dt.settings()[0].oScroll;
    return t.sY !== "" || t.sX !== "";
  },
  /**
   * Realign columns by using the colgroup tag and
   * checking column widths
   */
  _widths: function(t) {
    if (!(!t || !t.placeholder)) {
      var n = xe(this.s.dt.table().node()), e = xe(n.parent());
      t.floatingParent.css("width", e[0].offsetWidth), t.floating.css("width", n[0].offsetWidth), xe("colgroup", t.floating).remove();
      for (var r = t.placeholder.parent().find("colgroup").clone().appendTo(t.floating).find("col"), i = this.s.dt.columns(":visible").widths(), s = 0; s < i.length; s++)
        r.eq(s).css("width", i[s]);
    }
  }
});
lr.version = "4.0.6";
lr.defaults = {
  header: !0,
  footer: !1,
  headerOffset: 0,
  footerOffset: 0
};
xe.fn.dataTable.FixedHeader = lr;
xe.fn.DataTable.FixedHeader = lr;
xe(document).on("init.dt.dtfh", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = n.oInit.fixedHeader, i = H.defaults.fixedHeader;
    if ((r || i) && !n._fixedHeader) {
      var s = xe.extend({}, i, r);
      r !== !1 && new lr(n, s);
    }
  }
});
H.Api.register("fixedHeader()", function() {
});
H.Api.register("fixedHeader.adjust()", function() {
  return this.iterator("table", function(t) {
    var n = t._fixedHeader;
    n && n.update();
  });
});
H.Api.register("fixedHeader.enable()", function(t) {
  return this.iterator("table", function(n) {
    var e = n._fixedHeader;
    t = t !== void 0 ? t : !0, e && t !== e.enabled() && e.enable(t);
  });
});
H.Api.register("fixedHeader.enabled()", function() {
  if (this.context.length) {
    var t = this.context[0]._fixedHeader;
    if (t)
      return t.enabled();
  }
  return !1;
});
H.Api.register("fixedHeader.disable()", function() {
  return this.iterator("table", function(t) {
    var n = t._fixedHeader;
    n && n.enabled() && n.enable(!1);
  });
});
xe.each(["header", "footer"], function(t, n) {
  H.Api.register("fixedHeader." + n + "Offset()", function(e) {
    var r = this.context;
    return e === void 0 ? r.length && r[0]._fixedHeader ? r[0]._fixedHeader[n + "Offset"]() : void 0 : this.iterator("table", function(i) {
      var s = i._fixedHeader;
      s && s[n + "Offset"](e);
    });
  });
});
/*! FixedColumns 5.0.5
 * © SpryMedia Ltd - datatables.net/license
 */
let Jn = lt;
(function() {
  var t, n;
  function e(a) {
    t = a, n = t.fn.dataTable;
  }
  var r = (
    /** @class */
    function() {
      function a(o, l) {
        var c = this;
        if (!n || !n.versionCheck || !n.versionCheck("2"))
          throw new Error("FixedColumns requires DataTables 2 or newer");
        var f = new n.Api(o);
        this.classes = t.extend(!0, {}, a.classes), this.c = t.extend(!0, {}, a.defaults, l), this.s = {
          dt: f,
          rtl: t(f.table().node()).css("direction") === "rtl"
        }, l && l.leftColumns !== void 0 && (l.left = l.leftColumns), l && l.left !== void 0 && (this.c[this.s.rtl ? "end" : "start"] = l.left), l && l.rightColumns !== void 0 && (l.right = l.rightColumns), l && l.right !== void 0 && (this.c[this.s.rtl ? "start" : "end"] = l.right), this.dom = {
          bottomBlocker: t("<div>").addClass(this.classes.bottomBlocker),
          topBlocker: t("<div>").addClass(this.classes.topBlocker),
          scroller: t("div.dt-scroll-body", this.s.dt.table().container())
        }, this.s.dt.settings()[0]._bInitComplete ? (this._addStyles(), this._setKeyTableListener()) : f.one("init.dt.dtfc", function() {
          c._addStyles(), c._setKeyTableListener();
        }), f.on("column-sizing.dt.dtfc column-reorder.dt.dtfc draw.dt.dtfc", function() {
          return c._addStyles();
        });
        var g = n.util.debounce(function() {
          c._addStyles();
        }, 50);
        return f.on("column-visibility.dt.dtfc", function() {
          g();
        }), this.dom.scroller.on("scroll.dtfc", function() {
          return c._scroll();
        }), this._scroll(), f.settings()[0]._fixedColumns = this, f.on("destroy", function() {
          return c._destroy();
        }), this;
      }
      return a.prototype.end = function(o) {
        return o !== void 0 ? (o >= 0 && o <= this.s.dt.columns().count() && (this.c.end = o, this._addStyles()), this) : this.c.end;
      }, a.prototype.left = function(o) {
        return this.s.rtl ? this.end(o) : this.start(o);
      }, a.prototype.right = function(o) {
        return this.s.rtl ? this.start(o) : this.end(o);
      }, a.prototype.start = function(o) {
        return o !== void 0 ? (o >= 0 && o <= this.s.dt.columns().count() && (this.c.start = o, this._addStyles()), this) : this.c.start;
      }, a.prototype._addStyles = function() {
        var o = this.s.dt, l = this, c = this.s.dt.columns(":visible").count(), f = o.table().header.structure(":visible"), g = o.table().footer.structure(":visible"), b = o.columns(":visible").widths().toArray(), w = t(o.table().node()).closest("div.dt-scroll"), C = t(o.table().node()).closest("div.dt-scroll-body")[0], m = this.s.rtl, h = this.c.start, u = this.c.end, p = m ? u : h, v = m ? h : u, S = o.settings()[0].oBrowser.barWidth;
        if (w.length === 0)
          return this;
        C.offsetWidth === C.clientWidth && (S = 0), o.columns().every(function($) {
          var V = o.column.index("toVisible", $), z;
          V !== null && (V < h ? (z = l._sum(b, V), l._fixColumn(V, z, "start", f, g, S)) : V >= c - u ? (z = l._sum(b, c - V - 1, !0), l._fixColumn(V, z, "end", f, g, S)) : l._fixColumn(V, 0, "none", f, g, S));
        }), t(o.table().node()).toggleClass(l.classes.tableFixedStart, h > 0).toggleClass(l.classes.tableFixedEnd, u > 0).toggleClass(l.classes.tableFixedLeft, p > 0).toggleClass(l.classes.tableFixedRight, v > 0);
        var T = o.table().header(), A = o.table().footer(), y = t(T).outerHeight(), I = t(A).outerHeight();
        this.dom.topBlocker.appendTo(w).css("top", 0).css(this.s.rtl ? "left" : "right", 0).css("height", y).css("width", S + 1).css("display", S ? "block" : "none"), A && this.dom.bottomBlocker.appendTo(w).css("bottom", 0).css(this.s.rtl ? "left" : "right", 0).css("height", I).css("width", S + 1).css("display", S ? "block" : "none");
      }, a.prototype._destroy = function() {
        this.s.dt.off(".dtfc"), this.dom.scroller.off(".dtfc"), t(this.s.dt.table().node()).removeClass(this.classes.tableScrollingEnd + " " + this.classes.tableScrollingLeft + " " + this.classes.tableScrollingStart + " " + this.classes.tableScrollingRight), this.dom.bottomBlocker.remove(), this.dom.topBlocker.remove();
      }, a.prototype._fixColumn = function(o, l, c, f, g, b) {
        var w = this, C = this.s.dt, m = function(h, u) {
          if (c === "none")
            h.css("position", "").css("left", "").css("right", "").removeClass(w.classes.fixedEnd + " " + w.classes.fixedLeft + " " + w.classes.fixedRight + " " + w.classes.fixedStart);
          else {
            var p = c === "start" ? "left" : "right";
            w.s.rtl && (p = c === "start" ? "right" : "left");
            var v = l;
            c === "end" && (u === "header" || u === "footer") && (v += b), h.css("position", "sticky").css(p, v).addClass(c === "start" ? w.classes.fixedStart : w.classes.fixedEnd).addClass(p === "left" ? w.classes.fixedLeft : w.classes.fixedRight);
          }
        };
        f.forEach(function(h) {
          h[o] && m(t(h[o].cell), "header");
        }), m(C.column(o + ":visible", { page: "current" }).nodes().to$(), "body"), g && g.forEach(function(h) {
          h[o] && m(t(h[o].cell), "footer");
        });
      }, a.prototype._scroll = function() {
        var o = this.dom.scroller[0];
        if (o) {
          var l = t(this.s.dt.table().node()).add(this.s.dt.table().header().parentNode).add(this.s.dt.table().footer().parentNode).add("div.dt-scroll-headInner table", this.s.dt.table().container()).add("div.dt-scroll-footInner table", this.s.dt.table().container()), c = o.scrollLeft, f = !this.s.rtl, g = c !== 0, b = o.scrollWidth > o.clientWidth + Math.abs(c) + 1;
          l.toggleClass(this.classes.tableScrollingStart, g), l.toggleClass(this.classes.tableScrollingEnd, b), l.toggleClass(this.classes.tableScrollingLeft, g && f || b && !f), l.toggleClass(this.classes.tableScrollingRight, b && f || g && !f);
        }
      }, a.prototype._setKeyTableListener = function() {
        var o = this;
        this.s.dt.on("key-focus.dt.dtfc", function(l, c, f) {
          var g, b = t(f.node()).offset(), w = o.dom.scroller[0], C = t(t(o.s.dt.table().node()).closest("div.dt-scroll-body"));
          if (o.c.start > 0) {
            var m = t(o.s.dt.column(o.c.start - 1).header()), h = m.offset(), u = m.outerWidth();
            t(f.node()).hasClass(o.classes.fixedLeft) ? C.scrollLeft(0) : b.left < h.left + u && (g = C.scrollLeft(), C.scrollLeft(g - (h.left + u - b.left)));
          }
          if (o.c.end > 0) {
            var p = o.s.dt.columns().data().toArray().length, v = t(f.node()).outerWidth(), S = t(o.s.dt.column(p - o.c.end).header()), T = S.offset();
            t(f.node()).hasClass(o.classes.fixedRight) ? C.scrollLeft(w.scrollWidth - w.clientWidth) : b.left + v > T.left && (g = C.scrollLeft(), C.scrollLeft(g - (T.left - (b.left + v))));
          }
        });
      }, a.prototype._sum = function(o, l, c) {
        return c === void 0 && (c = !1), c && (o = o.slice().reverse()), o.slice(0, l).reduce(function(f, g) {
          return f + g;
        }, 0);
      }, a.version = "5.0.5", a.classes = {
        bottomBlocker: "dtfc-bottom-blocker",
        fixedEnd: "dtfc-fixed-end",
        fixedLeft: "dtfc-fixed-left",
        fixedRight: "dtfc-fixed-right",
        fixedStart: "dtfc-fixed-start",
        tableFixedEnd: "dtfc-has-end",
        tableFixedLeft: "dtfc-has-left",
        tableFixedRight: "dtfc-has-right",
        tableFixedStart: "dtfc-has-start",
        tableScrollingEnd: "dtfc-scrolling-end",
        tableScrollingLeft: "dtfc-scrolling-left",
        tableScrollingRight: "dtfc-scrolling-right",
        tableScrollingStart: "dtfc-scrolling-start",
        topBlocker: "dtfc-top-blocker"
      }, a.defaults = {
        i18n: {
          button: "FixedColumns"
        },
        start: 1,
        end: 0
      }, a;
    }()
  );
  /*! FixedColumns 5.0.5
   * © SpryMedia Ltd - datatables.net/license
   */
  e(Jn), Jn.fn.dataTable.FixedColumns = r, Jn.fn.DataTable.FixedColumns = r;
  var i = H.Api.register;
  i("fixedColumns()", function() {
    return this;
  }), i("fixedColumns().start()", function(a) {
    var o = this.context[0];
    return a !== void 0 ? (o._fixedColumns.start(a), this) : o._fixedColumns.start();
  }), i("fixedColumns().end()", function(a) {
    var o = this.context[0];
    return a !== void 0 ? (o._fixedColumns.end(a), this) : o._fixedColumns.end();
  }), i("fixedColumns().left()", function(a) {
    var o = this.context[0];
    return a !== void 0 ? (o._fixedColumns.left(a), this) : o._fixedColumns.left();
  }), i("fixedColumns().right()", function(a) {
    var o = this.context[0];
    return a !== void 0 ? (o._fixedColumns.right(a), this) : o._fixedColumns.right();
  }), H.ext.buttons.fixedColumns = {
    action: function(a, o, l, c) {
      Jn(l).attr("active") ? (Jn(l).removeAttr("active").removeClass("active"), o.fixedColumns().start(0), o.fixedColumns().end(0)) : (Jn(l).attr("active", "true").addClass("active"), o.fixedColumns().start(c.config.start), o.fixedColumns().end(c.config.end));
    },
    config: {
      start: 1,
      end: 0
    },
    init: function(a, o, l) {
      a.settings()[0]._fixedColumns === void 0 && s(a.settings(), l.config), Jn(o).attr("active", "true").addClass("active"), a.button(o).text(l.text || a.i18n("buttons.fixedColumns", a.settings()[0]._fixedColumns.c.i18n.button));
    },
    text: null
  };
  function s(a, o) {
    o === void 0 && (o = null);
    var l = new H.Api(a), c = o || l.init().fixedColumns || H.defaults.fixedColumns, f = new r(l, c);
    return f;
  }
  Jn(document).on("plugin-init.dt", function(a, o) {
    a.namespace === "dt" && (o.oInit.fixedColumns || H.defaults.fixedColumns) && (o._fixedColumns || s(o, null));
  });
})();
/*! ColReorder 2.1.2
 * © SpryMedia Ltd - datatables.net/license
 */
let ot = lt;
function Ar(t, n, e, r) {
  var i = t.splice(n, e);
  i.unshift(0), i.unshift(r < n ? r : r - e + 1), t.splice.apply(t, i);
}
function hf(t) {
  t.rows().invalidate("data"), t.column(0).visible(t.column(0).visible()), t.columns.adjust();
  var n = t.colReorder.order();
  t.trigger("columns-reordered", [
    {
      order: n,
      mapping: iu(n)
    }
  ]);
}
function pf(t) {
  return t.settings()[0].aoColumns.map(function(n) {
    return n._crOriginalIdx;
  });
}
function Rc(t, n, e, r) {
  for (var i = [], s = 0; s < t.length; s++) {
    var a = t[s];
    Ar(a, e[0], e.length, r);
    for (var o = 0; o < a.length; o++) {
      var l = a[o].cell;
      if (!i.includes(l)) {
        var c = l.getAttribute("data-dt-column").split(","), f = c.map(function(g) {
          return n[g];
        }).join(",");
        l.setAttribute("data-dt-column", f), i.push(l);
      }
    }
  }
}
function Cs(t) {
  t.columns().iterator("column", function(n, e) {
    var r = n.aoColumns;
    r[e]._crOriginalIdx === void 0 && (r[e]._crOriginalIdx = e);
  });
}
function iu(t) {
  for (var n = [], e = 0; e < t.length; e++)
    n[t[e]] = e;
  return n;
}
function gf(t, n, e) {
  var r, i, s = t.settings()[0], a = s.aoColumns, o = a.map(function(b, w) {
    return w;
  });
  if (!n.includes(e)) {
    Ar(o, n[0], n.length, e);
    var l = iu(o);
    for (Ar(a, n[0], n.length, e), r = 0; r < s.aoData.length; r++) {
      var c = s.aoData[r];
      if (c) {
        var f = c.anCells;
        if (f)
          for (Ar(f, n[0], n.length, e), i = 0; i < f.length; i++)
            c.nTr && f[i] && a[i].bVisible && c.nTr.appendChild(f[i]), f[i] && f[i]._DT_CellIndex && (f[i]._DT_CellIndex.column = i);
      }
    }
    for (r = 0; r < a.length; r++) {
      var g = a[r];
      for (i = 0; i < g.aDataSort.length; i++)
        g.aDataSort[i] = l[g.aDataSort[i]];
      g.idx = l[g.idx], g.bVisible && s.colgroup.append(g.colEl);
    }
    Rc(s.aoHeader, l, n, e), Rc(s.aoFooter, l, n, e), Ar(s.aoPreSearchCols, n[0], n.length, e), ts(l, s.aaSorting), Array.isArray(s.aaSortingFixed) ? ts(l, s.aaSortingFixed) : (s.aaSortingFixed.pre || s.aaSortingFixed.post) && ts(l, s.aaSortingFixed.pre), s.aLastSort.forEach(function(b) {
      b.src = l[b.src];
    }), t.trigger("column-reorder", [
      t.settings()[0],
      {
        from: n,
        to: e,
        mapping: l
      }
    ]);
  }
}
function ts(t, n) {
  if (n)
    for (var e = 0; e < n.length; e++) {
      var r = n[e];
      typeof r == "number" ? n[e] = t[r] : ot.isPlainObject(r) && r.idx !== void 0 ? r.idx = t[r.idx] : Array.isArray(r) && typeof r[0] == "number" && (r[0] = t[r[0]]);
    }
}
function Qa(t, n, e) {
  var r = !1, i;
  if (n.length !== t.columns().count()) {
    t.error("ColReorder - column count mismatch");
    return;
  }
  e && (n = mf(t, n, "toCurrent"));
  var s = iu(n);
  for (i = 0; i < s.length; i++) {
    var a = s.indexOf(i);
    i !== a && (Ar(s, a, 1, i), gf(t, [a], i), r = !0);
  }
  r && hf(t);
}
function zg(t) {
  for (var n = [], e = 0; e < t.length; e++) {
    n.push([]);
    for (var r = 0; r < t[e].length; r++) {
      var i = t[e][r];
      if (i)
        for (var s = 0; s < i.rowspan; s++) {
          n[e + s] || (n[e + s] = []);
          for (var a = 0; a < i.colspan; a++)
            n[e + s][r + a] = i.cell;
        }
    }
  }
  return n;
}
function mf(t, n, e) {
  var r = t.colReorder.order(), i = t.settings()[0].aoColumns;
  return e === "toCurrent" || e === "fromOriginal" ? Array.isArray(n) ? n.map(function(s) {
    return r.indexOf(s);
  }) : r.indexOf(n) : Array.isArray(n) ? n.map(function(s) {
    return i[s]._crOriginalIdx;
  }) : i[n]._crOriginalIdx;
}
function vf(t, n, e) {
  var r = t.columns().count();
  return n[0] < e && e < n[n.length] || n[0] < 0 && n[n.length - 1] > r || e < 0 && e > r ? !1 : n.includes(e) ? !0 : !(!Lc(t.table().header.structure(), n, e) || !Lc(t.table().footer.structure(), n, e));
}
function Lc(t, n, e) {
  var r = zg(t), i;
  for (i = 0; i < r.length; i++)
    Ar(r[i], n[0], n.length, e);
  for (i = 0; i < r.length; i++)
    for (var s = [], a = 0; a < r[i].length; a++) {
      var o = r[i][a];
      if (!s.includes(o))
        s.push(o);
      else if (s[s.length - 1] !== o)
        return !1;
    }
  return !0;
}
var bf = (
  /** @class */
  function() {
    function t(n, e) {
      this.dom = {
        drag: null
      }, this.c = {
        columns: null,
        enable: null,
        headerRows: null,
        order: null
      }, this.s = {
        dropZones: [],
        mouse: {
          absLeft: -1,
          offset: {
            x: -1,
            y: -1
          },
          start: {
            x: -1,
            y: -1
          },
          target: null,
          targets: []
        },
        scrollInterval: null
      };
      var r = this, i = n.settings()[0];
      if (!i._colReorder) {
        n.settings()[0]._colReorder = this, this.dt = n, ot.extend(this.c, t.defaults, e), Cs(n), n.on("stateSaveParams", function(o, l, c) {
          c.colReorder = pf(n);
        }), n.on("destroy", function() {
          n.off(".colReorder"), n.colReorder.reset();
        });
        var s = n.state.loaded(), a = this.c.order;
        s && s.colReorder && n.columns().count() === s.colReorder.length && (a = s.colReorder), a && n.ready(function() {
          Qa(n, a, !0);
        }), n.table().header.structure().forEach(function(o, l) {
          for (var c = e.headerRows, f = 0; f < o.length; f++)
            (!c || c.includes(l)) && o[f] && o[f].cell && r._addListener(o[f].cell);
        });
      }
    }
    return t.prototype.disable = function() {
      return this.c.enable = !1, this;
    }, t.prototype.enable = function(n) {
      return n === void 0 && (n = !0), n === !1 ? this.disable() : (this.c.enable = !0, this);
    }, t.prototype._addListener = function(n) {
      var e = this;
      ot(n).on("selectstart.colReorder", function() {
        return !1;
      }).on("mousedown.colReorder touchstart.colReorder", function(r) {
        if (!(r.type === "mousedown" && r.which !== 1) && e.c.enable) {
          var i = ot("button.dtcc-button_reorder", this);
          i.length && r.target !== i[0] && i.find(r.target).length === 0 || e._mouseDown(r, this);
        }
      });
    }, t.prototype._createDragNode = function() {
      var n = this.s.mouse.target, e = n.parent(), r = e.parent(), i = r.parent(), s = n.clone();
      this.dom.drag = ot(i[0].cloneNode(!1)).addClass("dtcr-cloned").append(
        ot(r[0].cloneNode(!1)).append(ot(e[0].cloneNode(!1)).append(s[0]))
        // Not sure why  it doesn't want to append a jQuery node
      ).css({
        position: "absolute",
        top: 0,
        left: 0,
        width: ot(n).outerWidth(),
        height: ot(n).outerHeight()
      }).appendTo("body");
    }, t.prototype._cursorPosition = function(n, e) {
      return n.type.indexOf("touch") !== -1 ? n.originalEvent.touches[0][e] : n[e];
    }, t.prototype._mouseDown = function(n, e) {
      for (var r = this, i = ot(n.target).closest("th, td"), s = i.offset(), a = this.dt.columns(this.c.columns).indexes().toArray(), o = ot(e).attr("data-dt-column").split(",").map(function(b) {
        return parseInt(b, 10);
      }), l = 0; l < o.length; l++)
        if (!a.includes(o[l]))
          return !1;
      this.s.mouse.start.x = this._cursorPosition(n, "pageX"), this.s.mouse.start.y = this._cursorPosition(n, "pageY"), this.s.mouse.offset.x = this._cursorPosition(n, "pageX") - s.left, this.s.mouse.offset.y = this._cursorPosition(n, "pageY") - s.top, this.s.mouse.target = i, this.s.mouse.targets = o;
      for (var c = 0; c < o.length; c++) {
        var f = this.dt.cells(null, o[c], { page: "current" }).nodes().to$(), g = "dtcr-moving";
        c === 0 && (g += " dtcr-moving-first"), c === o.length - 1 && (g += " dtcr-moving-last"), f.addClass(g);
      }
      this._regions(o), this._scrollRegions(), ot(document).on("mousemove.colReorder touchmove.colReorder", function(b) {
        r._mouseMove(b);
      }).on("mouseup.colReorder touchend.colReorder", function(b) {
        r._mouseUp(b);
      });
    }, t.prototype._mouseMove = function(n) {
      if (this.dom.drag === null) {
        if (Math.pow(Math.pow(this._cursorPosition(n, "pageX") - this.s.mouse.start.x, 2) + Math.pow(this._cursorPosition(n, "pageY") - this.s.mouse.start.y, 2), 0.5) < 5)
          return;
        ot(document.body).addClass("dtcr-dragging"), this._createDragNode();
      }
      this.dom.drag.css({
        left: this._cursorPosition(n, "pageX") - this.s.mouse.offset.x,
        top: this._cursorPosition(n, "pageY") - this.s.mouse.offset.y
      });
      var e = this.dt.table().node(), r = ot(e).offset().left, i = this._cursorPosition(n, "pageX") - r, s;
      if (this._isRtl()) {
        var a = e.clientWidth;
        s = a - i;
      } else
        s = i;
      var o = this.s.dropZones.find(function(l) {
        return l.inlineStart <= s && s <= l.inlineStart + l.width;
      });
      this.s.mouse.absLeft = this._cursorPosition(n, "pageX"), o && (o.self || this._move(o, s));
    }, t.prototype._mouseUp = function(n) {
      var e = this;
      ot(document).off(".colReorder"), ot(document.body).removeClass("dtcr-dragging"), this.dom.drag && (this.dom.drag.remove(), this.dom.drag = null, this.s.mouse.target.on("click.dtcr", function(r) {
        return !1;
      }), setTimeout(function() {
        e.s.mouse.target.off(".dtcr");
      }, 10)), this.s.scrollInterval && clearInterval(this.s.scrollInterval), this.dt.cells(".dtcr-moving").nodes().to$().removeClass("dtcr-moving dtcr-moving-first dtcr-moving-last");
    }, t.prototype._move = function(n, e) {
      var r = this;
      this.dt.colReorder.move(this.s.mouse.targets, n.colIdx), this.s.mouse.targets = ot(this.s.mouse.target).attr("data-dt-column").split(",").map(function(g) {
        return parseInt(g, 10);
      }), this._regions(this.s.mouse.targets);
      var i = this.s.mouse.targets.filter(function(g) {
        return r.dt.column(g).visible();
      }), s = this.s.dropZones.find(function(g) {
        return g.colIdx === i[0];
      }), a = this.s.dropZones.indexOf(s);
      if (s.inlineStart > e) {
        var o = s.inlineStart - e, l = this.s.dropZones[a - 1];
        s.inlineStart -= o, s.width += o, l && (l.width -= o);
      }
      if (s = this.s.dropZones.find(function(g) {
        return g.colIdx === i[i.length - 1];
      }), s.inlineStart + s.width < e) {
        var c = e - (s.inlineStart + s.width), f = this.s.dropZones[a + 1];
        s.width += c, f && (f.inlineStart += c, f.width -= c);
      }
    }, t.prototype._regions = function(n) {
      var e = this, r = [], i = 0, s = 0, a = this.dt.columns(this.c.columns).indexes().toArray(), o = this.dt.columns().widths();
      this.dt.columns().every(function(l, c, f) {
        if (this.visible()) {
          var g = o[l];
          if (!a.includes(l)) {
            i += g;
            return;
          }
          var b = vf(e.dt, n, l);
          b ? r.push({
            colIdx: l,
            inlineStart: i - s,
            self: n[0] <= l && l <= n[n.length - 1],
            width: g + s
          }) : l < n[0] ? r.length && (r[r.length - 1].width += g) : l > n[n.length - 1] && (s += g), i += g;
        }
      }), this.s.dropZones = r;
    }, t.prototype._isScrolling = function() {
      return this.dt.table().body().parentNode !== this.dt.table().header().parentNode;
    }, t.prototype._scrollRegions = function() {
      if (this._isScrolling()) {
        var n = this, e = ot(this.dt.table().container()).offset().left, r = ot(this.dt.table().container()).outerWidth(), i = 75, s = this.dt.table().body().parentElement.parentElement;
        this.s.scrollInterval = setInterval(function() {
          var a = n.s.mouse.absLeft;
          a !== -1 && (a < e + i && s.scrollLeft ? s.scrollLeft -= 5 : a > e + r - i && s.scrollLeft < s.scrollWidth && (s.scrollLeft += 5));
        }, 25);
      }
    }, t.prototype._isRtl = function() {
      return ot(this.dt.table().node()).css("direction") === "rtl";
    }, t.defaults = {
      columns: "",
      enable: !0,
      headerRows: null,
      order: null
    }, t.version = "2.1.2", t;
  }()
);
/*! ColReorder 2.1.2
 * © SpryMedia Ltd - datatables.net/license
 */
H.Api.register("colReorder.enable()", function(t) {
  return this.iterator("table", function(n) {
    n._colReorder && n._colReorder.enable(t);
  });
});
H.Api.register("colReorder.disable()", function() {
  return this.iterator("table", function(t) {
    t._colReorder && t._colReorder.disable();
  });
});
H.Api.register("colReorder.move()", function(t, n) {
  return Cs(this), Array.isArray(t) || (t = [t]), vf(this, t, n) ? this.tables().every(function() {
    gf(this, t, n), hf(this);
  }) : (this.error("ColReorder - invalid move"), this);
});
H.Api.register("colReorder.order()", function(t, n) {
  return Cs(this), t ? this.tables().every(function() {
    Qa(this, t, n);
  }) : this.context.length ? pf(this) : null;
});
H.Api.register("colReorder.reset()", function() {
  return Cs(this), this.tables().every(function() {
    var t = this.columns().every(function(n) {
      return n;
    }).flatten().toArray();
    Qa(this, t, !0);
  });
});
H.Api.register("colReorder.transpose()", function(t, n) {
  return Cs(this), n || (n = "toCurrent"), mf(this, t, n);
});
H.ColReorder = bf;
ot(document).on("stateLoadInit.dt", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = new H.Api(n);
    if (e.colReorder && r.columns().count() === e.colReorder.length) {
      if (r.ready())
        Qa(r, e.colReorder, !0);
      else if (ts(e.colReorder, e.order), e.columns) {
        for (var i = 0; i < e.columns.length; i++)
          e.columns[i]._cr_sort = e.colReorder[i];
        e.columns.sort(function(s, a) {
          return s._cr_sort - a._cr_sort;
        });
      }
    }
  }
});
ot(document).on("preInit.dt", function(t, n) {
  if (t.namespace === "dt") {
    var e = n.oInit.colReorder, r = H.defaults.colReorder;
    if (e || r) {
      var i = ot.extend({}, r, e);
      if (e !== !1) {
        var s = new H.Api(n);
        new bf(s, i);
      }
    }
  }
});
/*! KeyTable 2.12.2
 * © SpryMedia Ltd - datatables.net/license
 */
let $e = lt;
var Yg = 0, qg = 0, Vr = function(t, n) {
  if (!H.versionCheck || !H.versionCheck("1.10.8"))
    throw "KeyTable requires DataTables 1.10.8 or newer";
  this.c = $e.extend(!0, {}, H.defaults.keyTable, Vr.defaults, n), this.s = {
    /** @type {DataTable.Api} DataTables' API instance */
    dt: new H.Api(t),
    /** Indicate when the DataTable is redrawing - take no action on key presses */
    dtDrawing: !1,
    enable: !0,
    /** @type {bool} Flag for if a draw is triggered by focus */
    focusDraw: !1,
    /** @type {bool} Flag to indicate when waiting for a draw to happen.
     *   Will ignore key presses at this point
     */
    waitingForDraw: !1,
    /** @type {object} Information about the last cell that was focused */
    lastFocus: null,
    /** @type {string} Unique namespace per instance */
    namespace: ".keyTable-" + Yg++,
    /** @type {Node} Input element for tabbing into the table */
    tabInput: null
  }, this.dom = {};
  var e = this.s.dt.settings()[0], r = e.keytable;
  if (r)
    return r;
  e.keytable = this, this._constructor();
};
$e.extend(Vr.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * API methods for DataTables API interface
   */
  /**
   * Blur the table's cell focus
   */
  blur: function() {
    this._blur();
  },
  /**
   * Enable cell focus for the table
   *
   * @param  {string} state Can be `true`, `false` or `-string navigation-only`
   */
  enable: function(t) {
    this.s.enable = t;
  },
  /**
   * Get enable status
   */
  enabled: function() {
    return this.s.enable;
  },
  /**
   * Focus on a cell
   * @param  {integer} row    Row index
   * @param  {integer} column Column index
   */
  focus: function(t, n) {
    this._focus(this.s.dt.cell(t, n));
  },
  /**
   * Is the cell focused
   * @param  {object} cell Cell index to check
   * @returns {boolean} true if focused, false otherwise
   */
  focused: function(t) {
    var n = this.s.lastFocus;
    if (!n)
      return !1;
    var e = this.s.lastFocus.cell.index();
    return t.row === e.row && t.column === e.column;
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Initialise the KeyTable instance
   *
   * @private
   */
  _constructor: function() {
    this._tabInput();
    var t = this, n = this.s.dt, e = $e(n.table().node()), r = this.s.namespace, i = !1;
    if (e.css("position") === "static" && e.css("position", "relative"), $e(n.table().body()).on("click" + r, "th, td", function(o) {
      if (t.s.enable !== !1) {
        var l = n.cell(this);
        l.any() && t._focus(l, null, !1, o);
      }
    }), $e(document).on("keydown" + r, function(o) {
      !i && !t.s.dtDrawing ? t._key(o) : o.preventDefault();
    }), this.c.blurable && $e(document).on("mousedown" + r, function(o) {
      $e(o.target).parents(".dataTables_filter, .dt-search").length && t._blur(), !$e(o.target).parents().filter(n.table().container()).length && ($e(o.target).parents("div.DTE").length || $e(o.target).parents("div.editor-datetime").length || $e(o.target).parents("div.dt-datetime").length || $e(o.target).parents().filter(".DTFC_Cloned").length || t._blur());
    }), this.c.editor) {
      var s = this.c.editor;
      s.on("open.keyTableMain", function(o, l, c) {
        l !== "inline" && t.s.enable && (t.enable(!1), s.one("close" + r, function() {
          t.enable(!0);
        }));
      }), this.c.editOnFocus && n.on(
        "key-focus" + r + " key-refocus" + r,
        function(o, l, c, f) {
          t._editor(null, f, !0);
        }
      ), n.on("key" + r, function(o, l, c, f, g) {
        t._editor(c, g, !1);
      }), $e(n.table().body()).on("dblclick" + r, "th, td", function(o) {
        if (t.s.enable !== !1) {
          var l = n.cell(this);
          l.any() && (t.s.lastFocus && this !== t.s.lastFocus.cell.node() || t._editor(null, o, !0));
        }
      }), s.on("preSubmit", function() {
        i = !0;
      }).on("preSubmitCancelled", function() {
        i = !1;
      }).on("submitComplete", function() {
        i = !1;
      });
    }
    n.on("stateSaveParams" + r, function(o, l, c) {
      c.keyTable = t.s.lastFocus ? t.s.lastFocus.cell.index() : null;
    }), n.on("column-visibility" + r, function(o) {
      t._tabInput();
    }), n.on("column-reorder" + r, function(o, l, c) {
      var f = t.s.lastFocus;
      if (f && f.cell) {
        var g = f.relative.column;
        f.cell[0][0].column = c.mapping.indexOf(g), f.relative.column = c.mapping.indexOf(g);
      }
    }), n.on("preDraw" + r + " scroller-will-draw" + r, function(o) {
      t.s.dtDrawing = !0;
    }), n.on("draw" + r, function(o) {
      if (t.s.dtDrawing = !1, t._tabInput(), !t.s.focusDraw) {
        var l = t.s.lastFocus;
        if (l) {
          var c = t.s.lastFocus.relative, f = n.page.info(), g = c.row;
          if (f.recordsDisplay === 0 || g < f.start || g > f.start + f.length)
            return;
          g >= f.recordsDisplay && (g = f.recordsDisplay - 1), t._focus(g, c.column, !0, o);
        }
      }
    }), this.c.clipboard && this._clipboard(), n.on("destroy" + r, function() {
      t._blur(!0), n.off(r), $e(n.table().body()).off("click" + r, "th, td").off("dblclick" + r, "th, td"), $e(document).off("mousedown" + r).off("keydown" + r).off("copy" + r).off("paste" + r);
    });
    var a = n.state.loaded();
    a && a.keyTable ? n.one("init", function() {
      var o = n.cell(a.keyTable);
      o.any() && o.focus();
    }) : this.c.focus && n.cell(this.c.focus).focus();
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Blur the control
   *
   * @param {boolean} [noEvents=false] Don't trigger updates / events (for destroying)
   * @private
   */
  _blur: function(t) {
    if (!(!this.s.enable || !this.s.lastFocus)) {
      var n = this.s.lastFocus.cell;
      $e(n.node()).removeClass(this.c.className), this.s.lastFocus = null, t || (this._updateFixedColumns(n.index().column), this._emitEvent("key-blur", [this.s.dt, n]));
    }
  },
  /**
   * Clipboard interaction handlers
   *
   * @private
   */
  _clipboard: function() {
    var t = this.s.dt, n = this, e = this.s.namespace, r = this.c.clipboard;
    window.getSelection && ((r === !0 || r.copy) && $e(document).on("copy" + e, function(i) {
      var s = i.originalEvent, a = window.getSelection().toString(), o = n.s.lastFocus;
      !a && o && (s.clipboardData.setData(
        "text/plain",
        o.cell.render(n.c.clipboardOrthogonal)
      ), s.preventDefault());
    }), (r === !0 || r.paste) && $e(document).on("paste" + e, function(i) {
      var s = i.originalEvent, a = n.s.lastFocus, o = document.activeElement, l = n.c.editor, c;
      if (a && (!o || o.nodeName.toLowerCase() === "body"))
        if (s.preventDefault(), window.clipboardData && window.clipboardData.getData ? c = window.clipboardData.getData("Text") : s.clipboardData && s.clipboardData.getData && (c = s.clipboardData.getData("text/plain")), l) {
          var f = n._inlineOptions(a.cell.index());
          l.inline(f.cell, f.field, f.options).set(l.displayed()[0], c).submit();
        } else
          a.cell.data(c), t.draw(!1);
    }));
  },
  /**
   * Get an array of the column indexes that KeyTable can operate on. This
   * is a merge of the user supplied columns and the visible columns.
   *
   * @private
   */
  _columns: function() {
    var t = this.s.dt, n = t.columns(this.c.columns).indexes(), e = [];
    return t.columns(":visible").every(function(r) {
      n.indexOf(r) !== -1 && e.push(r);
    }), e;
  },
  /**
   * Perform excel like navigation for Editor by triggering an edit on key
   * press
   *
   * @param  {integer} key Key code for the pressed key
   * @param  {object} orig Original event
   * @private
   */
  _editor: function(t, n, e) {
    if (this.s.lastFocus && !(n && n.type === "draw")) {
      var r = this, i = this.s.dt, s = this.c.editor, a = this.s.lastFocus.cell, o = this.s.namespace + "e" + qg++;
      if (!$e("div.DTE", a.node()).length && !(t !== null && (t >= 0 && t <= 9 || t === 11 || t === 12 || t >= 14 && t <= 31 || t >= 112 && t <= 123 || t >= 127 && t <= 159))) {
        n && (n.stopPropagation(), t === 13 && n.preventDefault());
        var l = function() {
          var c = r._inlineOptions(a.index());
          s.one("open" + o, function() {
            s.off("cancelOpen" + o), e || $e(
              "div.DTE_Field_InputControl input, div.DTE_Field_InputControl textarea"
            ).select(), i.keys.enable(e ? "tab-only" : "navigation-only"), i.on("key-blur.editor", function(f, g, b) {
              s.s.editOpts.onBlur !== "submit" && s.displayed() && b.node() === a.node() && s.submit();
            }), e && $e(i.table().container()).addClass("dtk-focus-alt"), s.on("preSubmitCancelled" + o, function() {
              setTimeout(function() {
                r._focus(a, null, !1);
              }, 50);
            }), s.on("submitUnsuccessful" + o, function() {
              r._focus(a, null, !1);
            }), s.one("close" + o, function() {
              i.keys.enable(!0), i.off("key-blur.editor"), s.off(o), $e(i.table().container()).removeClass("dtk-focus-alt"), r.s.returnSubmit && (r.s.returnSubmit = !1, r._emitEvent("key-return-submit", [i, a]));
            });
          }).one("cancelOpen" + o, function() {
            s.off(o);
          }).inline(c.cell, c.field, c.options);
        };
        t === 13 ? (e = !0, $e(document).one("keyup", function() {
          l();
        })) : l();
      }
    }
  },
  _inlineOptions: function(t) {
    return this.c.editorOptions ? this.c.editorOptions(t) : {
      cell: t,
      field: void 0,
      options: void 0
    };
  },
  /**
   * Emit an event on the DataTable for listeners
   *
   * @param  {string} name Event name
   * @param  {array} args Event arguments
   * @private
   */
  _emitEvent: function(t, n) {
    return this.s.dt.iterator("table", function(e, r) {
      return $e(e.nTable).triggerHandler(t, n);
    });
  },
  /**
   * Focus on a particular cell, shifting the table's paging if required
   *
   * @param  {DataTables.Api|integer} row Can be given as an API instance that
   *   contains the cell to focus or as an integer. As the latter it is the
   *   visible row index (from the whole data set) - NOT the data index
   * @param  {integer} [column] Not required if a cell is given as the first
   *   parameter. Otherwise this is the column data index for the cell to
   *   focus on
   * @param {boolean} [shift=true] Should the viewport be moved to show cell
   * @private
   */
  _focus: function(t, n, e, r) {
    var i = this, s = this.s.dt, a = s.page.info(), o = this.s.lastFocus;
    if (r || (r = null), !!this.s.enable) {
      if (typeof t != "number") {
        if (!t.any())
          return;
        var l = t.index();
        if (n = l.column, t = s.rows({ filter: "applied", order: "applied" }).indexes().indexOf(l.row), t < 0)
          return;
        a.serverSide && (t += a.start);
      }
      if (a.length !== -1 && (t < a.start || t >= a.start + a.length)) {
        this.s.focusDraw = !0, this.s.waitingForDraw = !0, s.one("draw", function() {
          i.s.focusDraw = !1, i.s.waitingForDraw = !1, i._focus(t, n, void 0, r);
        }).page(Math.floor(t / a.length)).draw(!1);
        return;
      }
      if ($e.inArray(n, this._columns()) !== -1) {
        a.serverSide && (t -= a.start);
        var c = s.cells(null, n, { search: "applied", order: "applied" }).flatten(), f = s.cell(c[t]), g = this._emitEvent("key-prefocus", [this.s.dt, f, r || null]);
        if (g.indexOf(!1) === -1) {
          if (o) {
            if (o.node === f.node()) {
              this._emitEvent("key-refocus", [this.s.dt, f, r || null]);
              return;
            }
            this._blur();
          }
          this._removeOtherFocus();
          var b = $e(f.node());
          if (b.addClass(this.c.className), this._updateFixedColumns(n), e === void 0 || e === !0) {
            this._scroll($e(window), $e(document.body), b, "offset");
            var w = s.table().body().parentNode;
            if (w !== s.table().header().parentNode) {
              var C = $e(w.parentNode);
              this._scroll(C, C, b, "position");
            }
          }
          var m = s.page.info();
          this.s.lastFocus = {
            cell: f,
            node: f.node(),
            relative: {
              row: m.start + s.rows({ page: "current" }).indexes().indexOf(f.index().row),
              column: f.index().column
            }
          }, this._emitEvent("key-focus", [this.s.dt, f, r || null]), s.state.save();
        }
      }
    }
  },
  /**
   * Handle key press
   *
   * @param  {object} e Event
   * @private
   */
  _key: function(t) {
    if (this.s.waitingForDraw) {
      t.preventDefault();
      return;
    }
    if (!$e(t.target).closest(".dte-inlineAdd").length) {
      var n = this.s.enable;
      this.s.returnSubmit = (n === "navigation-only" || n === "tab-only") && t.keyCode === 13;
      var e = n === !0 || n === "navigation-only";
      if (n && !((t.keyCode === 0 || t.ctrlKey || t.metaKey || t.altKey) && !(t.ctrlKey && t.altKey))) {
        var r = this.s.lastFocus;
        if (r) {
          if (!this.s.dt.cell(r.node).any()) {
            this.s.lastFocus = null;
            return;
          }
          var i = this, s = this.s.dt, a = !!this.s.dt.settings()[0].oScroll.sY;
          if (!(this.c.keys && $e.inArray(t.keyCode, this.c.keys) === -1))
            switch (t.keyCode) {
              case 9:
                t.preventDefault(), this._keyAction(function() {
                  i._shift(t, t.shiftKey ? "left" : "right", !0);
                });
                break;
              case 27:
                if ($e(r.node).find("div.DTE").length)
                  return;
                this.c.blurable && n === !0 && this._blur();
                break;
              case 33:
              case 34:
                e && !a && (t.preventDefault(), this._keyAction(function() {
                  s.page(t.keyCode === 33 ? "previous" : "next").draw(!1);
                }));
                break;
              case 35:
              case 36:
                e && (t.preventDefault(), this._keyAction(function() {
                  var o = s.cells({ page: "current" }).indexes(), l = i._columns();
                  i._focus(
                    s.cell(o[t.keyCode === 35 ? o.length - 1 : l[0]]),
                    null,
                    !0,
                    t
                  );
                }));
                break;
              case 37:
                e && this._keyAction(function() {
                  i._shift(t, "left");
                });
                break;
              case 38:
                e && this._keyAction(function() {
                  i._shift(t, "up");
                });
                break;
              case 39:
                e && this._keyAction(function() {
                  i._shift(t, "right");
                });
                break;
              case 40:
                e && this._keyAction(function() {
                  i._shift(t, "down");
                });
                break;
              case 113:
                if (this.c.editor) {
                  this._editor(null, t, !0);
                  break;
                }
              default:
                n === !0 && this._emitEvent("key", [s, t.keyCode, this.s.lastFocus.cell, t]);
                break;
            }
        }
      }
    }
  },
  /**
   * Whether we perform a key shift action immediately or not depends
   * upon if Editor is being used. If it is, then we wait until it
   * completes its action
   * @param {*} action Function to trigger when ready
   */
  _keyAction: function(t) {
    var n = this.c.editor;
    n && n.mode() && n.display() ? n.submit(t) : t();
  },
  /**
   * Remove focus from all tables other than this one
   */
  _removeOtherFocus: function() {
    var t = this.s.dt.table().node();
    $e.fn.dataTable.tables({ api: !0 }).iterator("table", function(n) {
      this.table().node() !== t && this.cell.blur();
    });
  },
  /**
   * Scroll a container to make a cell visible in it. This can be used for
   * both DataTables scrolling and native window scrolling.
   *
   * @param  {jQuery} container Scrolling container
   * @param  {jQuery} scroller  Item being scrolled
   * @param  {jQuery} cell      Cell in the scroller
   * @param  {string} posOff    `position` or `offset` - which to use for the
   *   calculation. `offset` for the document, otherwise `position`
   * @private
   */
  _scroll: function(t, n, e, r) {
    var i = e[r](), s = e.outerHeight(), a = e.outerWidth(), o = n.scrollTop(), l = n.scrollLeft(), c = t.height(), f = t.width();
    r === "position" && (i.top += parseInt(e.closest("table").css("top"), 10)), i.top < o && i.top + s > o - 5 && n.scrollTop(i.top), i.left < l && n.scrollLeft(i.left), i.top + s > o + c && i.top < o + c + 5 && s < c && n.scrollTop(i.top + s - c), i.left + a > l + f && a < f && n.scrollLeft(i.left + a - f);
  },
  /**
   * Calculate a single offset movement in the table - up, down, left and
   * right and then perform the focus if possible
   *
   * @param  {object}  e           Event object
   * @param  {string}  direction   Movement direction
   * @param  {boolean} keyBlurable `true` if the key press can result in the
   *   table being blurred. This is so arrow keys won't blur the table, but
   *   tab will.
   * @private
   */
  _shift: function(t, n, e) {
    var r = this.s.dt, i = r.page.info(), s = i.recordsDisplay, a = this._columns(), o = this.s.lastFocus;
    if (o) {
      var l = o.cell;
      if (l) {
        var c = r.rows({ filter: "applied", order: "applied" }).indexes().indexOf(l.index().row);
        i.serverSide && (c += i.start);
        var f = r.columns(a).indexes().indexOf(l.index().column), g = c, b = a[f];
        $e(r.table().node()).css("direction") === "rtl" && (n === "right" ? n = "left" : n === "left" && (n = "right")), n === "right" ? f >= a.length - 1 ? (g++, b = a[0]) : b = a[f + 1] : n === "left" ? f === 0 ? (g--, b = a[a.length - 1]) : b = a[f - 1] : n === "up" ? g-- : n === "down" && g++, g >= 0 && g < s && $e.inArray(b, a) !== -1 ? (t && t.preventDefault(), this._focus(g, b, !0, t)) : !e || !this.c.blurable ? t && t.preventDefault() : this._blur();
      }
    }
  },
  /**
   * Create and insert a hidden input element that can receive focus on behalf
   * of the table
   *
   * @private
   */
  _tabInput: function() {
    var t = this, n = this.s.dt, e = this.c.tabIndex !== null ? this.c.tabIndex : n.settings()[0].iTabIndex;
    if (e != -1) {
      if (!this.s.tabInput) {
        var r = "keytable-focus-capture-" + this.s.namespace.split("-")[1], i = '<input id="' + r + '" type="text" tabindex="' + e + '"/>', s = $e('<div><label for="' + r + '">' + i + "</label></div>").css({
          position: "absolute",
          height: 1,
          width: 0,
          overflow: "hidden"
        });
        s.find("input").on("focus", function(o) {
          var l = n.cell(":eq(0)", t._columns(), { page: "current" });
          l.any() && t._focus(l, null, !0, o);
        }), this.s.tabInput = s;
      }
      var a = this.s.dt.cell(":eq(0)", "0:visible", { page: "current", order: "current" }).node();
      a && $e(a).prepend(this.s.tabInput);
    }
  },
  /**
   * Update fixed columns if they are enabled and if the cell we are
   * focusing is inside a fixed column
   * @param  {integer} column Index of the column being changed
   * @private
   */
  _updateFixedColumns: function(t) {
    var n = this.s.dt, e = n.settings()[0];
    if (e._oFixedColumns) {
      var r = e._oFixedColumns.s.iLeftColumns, i = e.aoColumns.length - e._oFixedColumns.s.iRightColumns;
      (t < r || t >= i) && n.fixedColumns().update();
    }
  }
});
Vr.defaults = {
  /**
   * Can focus be removed from the table
   * @type {Boolean}
   */
  blurable: !0,
  /**
   * Class to give to the focused cell
   * @type {String}
   */
  className: "focus",
  /**
   * Enable or disable clipboard support
   * @type {Boolean}
   */
  clipboard: !0,
  /**
   * Orthogonal data that should be copied to clipboard
   * @type {string}
   */
  clipboardOrthogonal: "display",
  /**
   * Columns that can be focused. This is automatically merged with the
   * visible columns as only visible columns can gain focus.
   * @type {String}
   */
  columns: "",
  // all
  /**
   * Editor instance to automatically perform Excel like navigation
   * @type {Editor}
   */
  editor: null,
  /**
   * Trigger editing immediately on focus
   * @type {boolean}
   */
  editOnFocus: !1,
  /**
   * Options to pass to Editor's inline method
   * @type {function}
   */
  editorOptions: null,
  /**
   * Select a cell to automatically select on start up. `null` for no
   * automatic selection
   * @type {cell-selector}
   */
  focus: null,
  /**
   * Array of keys to listen for
   * @type {null|array}
   */
  keys: null,
  /**
   * Tab index for where the table should sit in the document's tab flow
   * @type {integer|null}
   */
  tabIndex: null
};
Vr.version = "2.12.2";
$e.fn.dataTable.KeyTable = Vr;
$e.fn.DataTable.KeyTable = Vr;
H.Api.register("cell.blur()", function() {
  return this.iterator("table", function(t) {
    t.keytable && t.keytable.blur();
  });
});
H.Api.register("cell().focus()", function() {
  return this.iterator("cell", function(t, n, e) {
    t.keytable && t.keytable.focus(n, e);
  });
});
H.Api.register("keys.disable()", function() {
  return this.iterator("table", function(t) {
    t.keytable && t.keytable.enable(!1);
  });
});
H.Api.register("keys.enable()", function(t) {
  return this.iterator("table", function(n) {
    n.keytable && n.keytable.enable(t === void 0 ? !0 : t);
  });
});
H.Api.register("keys.enabled()", function(t) {
  var n = this.context;
  return n.length && n[0].keytable ? n[0].keytable.enabled() : !1;
});
H.Api.register("keys.move()", function(t) {
  return this.iterator("table", function(n) {
    n.keytable && n.keytable._shift(null, t, !1);
  });
});
H.ext.selector.cell.push(function(t, n, e) {
  var r = n.focused, i = t.keytable, s = [];
  if (!i || r === void 0)
    return e;
  for (var a = 0, o = e.length; a < o; a++)
    (r === !0 && i.focused(e[a]) || r === !1 && !i.focused(e[a])) && s.push(e[a]);
  return s;
});
$e(document).on("preInit.dt.dtk", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = n.oInit.keys, i = H.defaults.keys;
    if (r || i) {
      var s = $e.extend({}, i, r);
      r !== !1 && new Vr(n, s);
    }
  }
});
/*! Scroller 2.4.3
 * © SpryMedia Ltd - datatables.net/license
 */
let Be = lt;
var Ln = function(t, n) {
  if (!(this instanceof Ln)) {
    alert(
      "Scroller warning: Scroller must be initialised with the 'new' keyword."
    );
    return;
  }
  n === void 0 && (n = {});
  var e = Be.fn.dataTable.Api(t);
  this.s = {
    /**
     * DataTables settings object
     *  @type     object
     *  @default  Passed in as first parameter to constructor
     */
    dt: e.settings()[0],
    /**
     * DataTables API instance
     *  @type     DataTable.Api
     */
    dtApi: e,
    /**
     * Pixel location of the top of the drawn table in the viewport
     *  @type     int
     *  @default  0
     */
    tableTop: 0,
    /**
     * Pixel location of the bottom of the drawn table in the viewport
     *  @type     int
     *  @default  0
     */
    tableBottom: 0,
    /**
     * Pixel location of the boundary for when the next data set should be loaded and drawn
     * when scrolling up the way.
     *  @type     int
     *  @default  0
     *  @private
     */
    redrawTop: 0,
    /**
     * Pixel location of the boundary for when the next data set should be loaded and drawn
     * when scrolling down the way. Note that this is actually calculated as the offset from
     * the top.
     *  @type     int
     *  @default  0
     *  @private
     */
    redrawBottom: 0,
    /**
     * Auto row height or not indicator
     *  @type     bool
     *  @default  0
     */
    autoHeight: !0,
    /**
     * Number of rows calculated as visible in the visible viewport
     *  @type     int
     *  @default  0
     */
    viewportRows: 0,
    /**
     * setTimeout reference for state saving, used when state saving is enabled in the DataTable
     * and when the user scrolls the viewport in order to stop the cookie set taking too much
     * CPU!
     *  @type     int
     *  @default  0
     */
    stateTO: null,
    stateSaveThrottle: function() {
    },
    /**
     * setTimeout reference for the redraw, used when server-side processing is enabled in the
     * DataTables in order to prevent DoSing the server
     *  @type     int
     *  @default  null
     */
    drawTO: null,
    heights: {
      jump: null,
      page: null,
      virtual: null,
      scroll: null,
      /**
       * Height of rows in the table
       *  @type     int
       *  @default  0
       */
      row: null,
      /**
       * Pixel height of the viewport
       *  @type     int
       *  @default  0
       */
      viewport: null,
      labelHeight: 0,
      xbar: 0
    },
    topRowFloat: 0,
    scrollDrawDiff: null,
    loaderVisible: !1,
    forceReposition: !1,
    baseRowTop: 0,
    baseScrollTop: 0,
    mousedown: !1,
    lastScrollTop: 0
  }, this.s = Be.extend(this.s, Ln.oDefaults, n), this.s.heights.row = this.s.rowHeight, this.dom = {
    force: document.createElement("div"),
    label: Be('<div class="dts_label">0</div>'),
    scroller: null,
    table: null,
    loader: null
  }, !this.s.dt.oScroller && (this.s.dt.oScroller = this, this.construct());
};
Be.extend(Ln.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Public methods - to be exposed via the DataTables API
   */
  /**
   * Calculate and store information about how many rows are to be displayed
   * in the scrolling viewport, based on current dimensions in the browser's
   * rendering. This can be particularly useful if the table is initially
   * drawn in a hidden element - for example in a tab.
   *  @param {bool} [redraw=true] Redraw the table automatically after the recalculation, with
   *    the new dimensions forming the basis for the draw.
   *  @returns {void}
   */
  measure: function(t) {
    this.s.autoHeight && this._calcRowHeight();
    var n = this.s.heights;
    n.row && (n.viewport = this._parseHeight(
      Be(this.dom.scroller).css("max-height")
    ), this.s.viewportRows = parseInt(n.viewport / n.row, 10) + 1, this.s.dt._iDisplayLength = this.s.viewportRows * this.s.displayBuffer);
    var e = this.dom.label.outerHeight();
    n.xbar = this.dom.scroller.offsetHeight - this.dom.scroller.clientHeight, n.labelHeight = e, (t === void 0 || t) && this.s.dtApi.draw(!1);
  },
  /**
   * Get information about current displayed record range. This corresponds to
   * the information usually displayed in the "Info" block of the table.
   *
   * @returns {object} info as an object:
   *  {
   *      start: {int}, // the 0-indexed record at the top of the viewport
   *      end:   {int}, // the 0-indexed record at the bottom of the viewport
   *  }
   */
  pageInfo: function() {
    var t = this.s.dt, n = this.dom.scroller.scrollTop, e = t.fnRecordsDisplay(), r = Math.ceil(
      this.pixelsToRow(
        n + this.s.heights.viewport,
        !1,
        this.s.ani
      )
    );
    return {
      start: Math.floor(this.pixelsToRow(n, !1, this.s.ani)),
      end: e < r ? e - 1 : r - 1
    };
  },
  /**
   * Calculate the row number that will be found at the given pixel position
   * (y-scroll).
   *
   * Please note that when the height of the full table exceeds 1 million
   * pixels, Scroller switches into a non-linear mode for the scrollbar to fit
   * all of the records into a finite area, but this function returns a linear
   * value (relative to the last non-linear positioning).
   *  @param {int} pixels Offset from top to calculate the row number of
   *  @param {int} [intParse=true] If an integer value should be returned
   *  @param {int} [virtual=false] Perform the calculations in the virtual domain
   *  @returns {int} Row index
   */
  pixelsToRow: function(t, n, e) {
    var r = t - this.s.baseScrollTop, i = e ? (this._domain("physicalToVirtual", this.s.baseScrollTop) + r) / this.s.heights.row : r / this.s.heights.row + this.s.baseRowTop;
    return n || n === void 0 ? parseInt(i, 10) : i;
  },
  /**
   * Calculate the pixel position from the top of the scrolling container for
   * a given row
   *  @param {int} iRow Row number to calculate the position of
   *  @returns {int} Pixels
   */
  rowToPixels: function(t, n, e) {
    var r, i = t - this.s.baseRowTop;
    return e ? (r = this._domain("virtualToPhysical", this.s.baseScrollTop), r += i * this.s.heights.row) : (r = this.s.baseScrollTop, r += i * this.s.heights.row), n || n === void 0 ? parseInt(r, 10) : r;
  },
  /**
   * Calculate the row number that will be found at the given pixel position (y-scroll)
   *  @param {int} row Row index to scroll to
   *  @param {bool} [animate=true] Animate the transition or not
   *  @returns {void}
   */
  scrollToRow: function(t, n) {
    var e = this, r = !1, i = this.rowToPixels(t), s = (this.s.displayBuffer - 1) / 2 * this.s.viewportRows, a = t - s;
    a < 0 && (a = 0), (i > this.s.redrawBottom || i < this.s.redrawTop) && this.s.dt._iDisplayStart !== a && (r = !0, i = this._domain("virtualToPhysical", t * this.s.heights.row), this.s.redrawTop < i && i < this.s.redrawBottom && (this.s.forceReposition = !0, n = !1)), n === void 0 || n ? (this.s.ani = r, Be(this.dom.scroller).animate(
      {
        scrollTop: i
      },
      function() {
        setTimeout(function() {
          e.s.ani = !1;
        }, 250);
      }
    )) : Be(this.dom.scroller).scrollTop(i);
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Initialisation for Scroller
   *  @returns {void}
   *  @private
   */
  construct: function() {
    var t = this, n = this.s.dtApi;
    if (!this.s.dt.oFeatures.bPaginate)
      throw new Error(
        "Pagination must be enabled for Scroller to operate"
      );
    this.dom.force.style.position = "relative", this.dom.force.style.top = "0px", this.dom.force.style.left = "0px", this.dom.force.style.width = "1px", this.dom.scroller = n.table().node().parentNode, this.dom.scroller.appendChild(this.dom.force), this.dom.scroller.style.position = "relative", this.dom.table = Be(">table", this.dom.scroller)[0], this.dom.table.style.position = "absolute", this.dom.table.style.top = "0px", this.dom.table.style.left = "0px", Be(n.table().container()).addClass("dts DTS"), this.dom.label.appendTo(this.dom.scroller), this.s.heights.row && this.s.heights.row != "auto" && (this.s.autoHeight = !1), this.s.ingnoreScroll = !0, Be(this.dom.scroller).on("scroll.dt-scroller", function(i) {
      t._scroll.call(t);
    }), Be(this.dom.scroller).on("touchstart.dt-scroller", function() {
      t._scroll.call(t);
    }), Be(this.dom.scroller).on("mousedown.dt-scroller", function() {
      t.s.mousedown = !0;
    }).on("mouseup.dt-scroller", function() {
      t.s.labelVisible = !1, t.s.mousedown = !1, t.dom.label.css("display", "none");
    }), Be(window).on("resize.dt-scroller", function() {
      t.measure(!1), t._info();
    });
    var e = !0, r = n.state.loaded();
    n.on("stateSaveParams.scroller", function(i, s, a) {
      e && r ? (a.scroller = r.scroller, e = !1, a.scroller && (t.s.lastScrollTop = a.scroller.scrollTop)) : a.scroller = {
        topRow: t.s.topRowFloat,
        baseRowTop: t.s.baseRowTop
      };
    }), n.on("stateLoadParams.scroller", function(i, s, a) {
      a.scroller !== void 0 && t.scrollToRow(a.scroller.topRow);
    }), this.measure(!1), r && r.scroller && (this.s.topRowFloat = r.scroller.topRow, this.s.baseRowTop = r.scroller.baseRowTop, this.s.baseScrollTop = this.s.baseRowTop * this.s.heights.row, r.scroller.scrollTop = this._domain("physicalToVirtual", this.s.topRowFloat * this.s.heights.row)), t.s.stateSaveThrottle = H.util.throttle(function() {
      t.s.dtApi.state.save();
    }, 500), n.on("init.scroller", function() {
      t.measure(!1), t.s.scrollType = "jump", t._draw(), n.on("draw.scroller", function() {
        t._draw();
      });
    }), n.on("preDraw.dt.scroller", function() {
      t._scrollForce();
    }), n.on("destroy.scroller", function() {
      Be(window).off("resize.dt-scroller"), Be(t.dom.scroller).off(".dt-scroller"), Be(t.s.dt.nTable).off(".scroller"), Be(t.s.dt.nTableWrapper).removeClass("DTS"), Be("div.DTS_Loading", t.dom.scroller.parentNode).remove(), t.dom.table.style.position = "", t.dom.table.style.top = "", t.dom.table.style.left = "";
    });
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Automatic calculation of table row height. This is just a little tricky here as using
   * initialisation DataTables has tale the table out of the document, so we need to create
   * a new table and insert it into the document, calculate the row height and then whip the
   * table out.
   *  @returns {void}
   *  @private
   */
  _calcRowHeight: function() {
    var t = this.s.dt, n = t.nTable, e = n.cloneNode(!1), r = Be("<tbody/>").appendTo(e), i = t.oClasses, s = H.versionCheck("2") ? {
      container: i.container,
      scroller: i.scrolling.container,
      body: i.scrolling.body
    } : {
      container: i.sWrapper,
      scroller: i.sScrollWrapper,
      body: i.sScrollBody
    }, a = Be(
      '<div class="' + s.container + ' DTS"><div class="' + s.scroller + '"><div class="' + s.body + '"></div></div></div>'
    );
    Be("tbody tr:lt(4)", n).clone().appendTo(r);
    var o = Be("tr", r).length;
    if (o === 1)
      r.prepend("<tr><td>&#160;</td></tr>"), r.append("<tr><td>&#160;</td></tr>");
    else
      for (; o < 3; o++)
        r.append("<tr><td>&#160;</td></tr>");
    Be("div." + s.body, a).append(e);
    var l = this.s.dt.nHolding || n.parentNode;
    Be(l).is(":visible") || (l = "body"), a.find("input").removeAttr("name"), a.appendTo(l), this.s.heights.row = Be("tr", r).eq(1).outerHeight(), a.remove();
  },
  /**
   * Draw callback function which is fired when the DataTable is redrawn. The main function of
   * this method is to position the drawn table correctly the scrolling container for the rows
   * that is displays as a result of the scrolling position.
   *  @returns {void}
   *  @private
   */
  _draw: function() {
    var t = this, n = this.s.heights, e = this.dom.scroller.scrollTop, r = Be(this.s.dt.nTable).height(), i = this.s.dt._iDisplayStart, s = this.s.dt._iDisplayLength, a = this.s.dt.fnRecordsDisplay(), o = e + n.viewport;
    this.s.skip = !0, (this.s.dt.bSorted || this.s.dt.bFiltered) && i === 0 && !this.s.dt._drawHold && (this.s.topRowFloat = 0), e = this.s.scrollType === "jump" ? this._domain(
      "virtualToPhysical",
      this.s.topRowFloat * n.row
    ) : e, this.s.baseScrollTop = e, this.s.baseRowTop = this.s.topRowFloat;
    var l = e - (this.s.topRowFloat - i) * n.row;
    if (i === 0)
      l = 0;
    else if (i + s >= a)
      l = n.scroll - r;
    else {
      var c = l + r;
      if (c < o) {
        var f = o - r, g = f - l;
        this.s.baseScrollTop += g + 1, l = f;
      }
    }
    this.dom.table.style.top = l + "px", this.s.tableTop = l, this.s.tableBottom = r + this.s.tableTop;
    var b = (e - this.s.tableTop) * this.s.boundaryScale;
    if (this.s.redrawTop = e - b, this.s.redrawBottom = e + b > n.scroll - n.viewport - n.row ? n.scroll - n.viewport - n.row : e + b, this.s.skip = !1, t.s.ingnoreScroll)
      if (this.s.dt.oFeatures.bStateSave && this.s.dt.oLoadedState !== null && typeof this.s.dt.oLoadedState.scroller < "u") {
        var w = !!((this.s.dt.sAjaxSource || t.s.dt.ajax) && !this.s.dt.oFeatures.bServerSide);
        (w && this.s.dt.iDraw >= 2 || !w && this.s.dt.iDraw >= 1) && setTimeout(function() {
          Be(t.dom.scroller).scrollTop(
            t.s.dt.oLoadedState.scroller.scrollTop
          ), setTimeout(function() {
            t.s.ingnoreScroll = !1;
          }, 0);
        }, 0);
      } else
        t.s.ingnoreScroll = !1;
    this.s.dt.oFeatures.bInfo && setTimeout(function() {
      t._info.call(t);
    }, 0), Be(this.s.dt.nTable).triggerHandler("position.dts.dt", l);
  },
  /**
   * Convert from one domain to another. The physical domain is the actual
   * pixel count on the screen, while the virtual is if we had browsers which
   * had scrolling containers of infinite height (i.e. the absolute value)
   *
   *  @param {string} dir Domain transform direction, `virtualToPhysical` or
   *    `physicalToVirtual`
   *  @returns {number} Calculated transform
   *  @private
   */
  _domain: function(t, n) {
    var e = this.s.heights, r, i = 1e4;
    if (e.virtual === e.scroll || n < i)
      return n;
    if (t === "virtualToPhysical" && n >= e.virtual - i)
      return r = e.virtual - n, e.scroll - r;
    if (t === "physicalToVirtual" && n >= e.scroll - i)
      return r = e.scroll - n, e.virtual - r;
    var s = (e.virtual - i - i) / (e.scroll - i - i), a = i - s * i;
    return t === "virtualToPhysical" ? (n - a) / s : s * n + a;
  },
  /**
   * Update any information elements that are controlled by the DataTable based on the scrolling
   * viewport and what rows are visible in it. This function basically acts in the same way as
   * _fnUpdateInfo in DataTables, and effectively replaces that function.
   *  @returns {void}
   *  @private
   */
  _info: function() {
    if (this.s.dt.oFeatures.bInfo) {
      var t = this.s.dt, n = this.s.dtApi, e = t.oLanguage, r = n.page.info(), i = r.recordsDisplay, s = r.recordsTotal, a = (this.s.lastScrollTop - this.s.baseScrollTop) / this.s.heights.row, o = Math.floor(this.s.baseRowTop + a) + 1;
      this.s.scrollType === "jump" && (o = Math.floor(this.s.topRowFloat) + 1);
      var l = o + Math.floor(this.s.heights.viewport / this.s.heights.row), c = l > i ? i : l, f;
      i === 0 && i == s ? f = e.sInfoEmpty + e.sInfoPostFix : i === 0 ? f = e.sInfoEmpty + " " + e.sInfoFiltered + e.sInfoPostFix : i == s ? f = e.sInfo + e.sInfoPostFix : f = e.sInfo + " " + e.sInfoFiltered + e.sInfoPostFix, f = this._macros(f, o, c, s, i);
      var g = e.fnInfoCallback;
      g && (f = g.call(
        t.oInstance,
        t,
        o,
        c,
        s,
        i,
        f
      ));
      var b = t.aanFeatures.i;
      if (typeof b < "u") {
        for (var w = 0, C = b.length; w < C; w++)
          Be(b[w]).html(f);
        Be(t.nTable).triggerHandler("info.dt");
      }
      Be("div.dt-info", n.table().container()).each(function() {
        Be(this).html(f), n.trigger("info", [n.settings()[0], this, f]);
      });
    }
  },
  /**
   * String replacement for info display. Basically the same as what DataTables does.
   *
   * @param {*} str
   * @param {*} start
   * @param {*} end
   * @param {*} max
   * @param {*} total
   * @returns Formatted string
   */
  _macros: function(t, n, e, r, i) {
    var s = this.s.dtApi, a = this.s.dt, o = a.fnFormatNumber;
    return t.replace(/_START_/g, o.call(a, n)).replace(/_END_/g, o.call(a, e)).replace(/_MAX_/g, o.call(a, r)).replace(/_TOTAL_/g, o.call(a, i)).replace(/_ENTRIES_/g, s.i18n("entries", "")).replace(/_ENTRIES-MAX_/g, s.i18n("entries", "", r)).replace(/_ENTRIES-TOTAL_/g, s.i18n("entries", "", i));
  },
  /**
   * Parse CSS height property string as number
   *
   * An attempt is made to parse the string as a number. Currently supported units are 'px',
   * 'vh', and 'rem'. 'em' is partially supported; it works as long as the parent element's
   * font size matches the body element. Zero is returned for unrecognized strings.
   *  @param {string} cssHeight CSS height property string
   *  @returns {number} height
   *  @private
   */
  _parseHeight: function(t) {
    var n, e = /^([+-]?(?:\d+(?:\.\d+)?|\.\d+))(px|em|rem|vh)$/.exec(
      t
    );
    if (e === null)
      return 0;
    var r = parseFloat(e[1]), i = e[2];
    return i === "px" ? n = r : i === "vh" ? n = r / 100 * Be(window).height() : i === "rem" ? n = r * parseFloat(Be(":root").css("font-size")) : i === "em" && (n = r * parseFloat(Be("body").css("font-size"))), n || 0;
  },
  /**
   * Scrolling function - fired whenever the scrolling position is changed.
   * This method needs to use the stored values to see if the table should be
   * redrawn as we are moving towards the end of the information that is
   * currently drawn or not. If needed, then it will redraw the table based on
   * the new position.
   *  @returns {void}
   *  @private
   */
  _scroll: function() {
    var t = this, n = this.s.heights, e = this.dom.scroller.scrollTop, r;
    if (!this.s.skip && !this.s.ingnoreScroll && e !== this.s.lastScrollTop) {
      if (this.s.dt.bFiltered || this.s.dt.bSorted) {
        this.s.lastScrollTop = 0;
        return;
      }
      if (clearTimeout(this.s.stateTO), this.s.stateTO = setTimeout(function() {
        t.s.dtApi.state.save(), t._info();
      }, 250), this.s.scrollType = Math.abs(e - this.s.lastScrollTop) > n.viewport ? "jump" : "cont", this.s.topRowFloat = this.s.scrollType === "cont" ? this.pixelsToRow(e, !1, !1) : this._domain("physicalToVirtual", e) / n.row, this.s.topRowFloat < 0 && (this.s.topRowFloat = 0), this.s.forceReposition || e < this.s.redrawTop || e > this.s.redrawBottom) {
        var i = Math.ceil(
          (this.s.displayBuffer - 1) / 2 * this.s.viewportRows
        );
        if (r = parseInt(this.s.topRowFloat, 10) - i, this.s.forceReposition = !1, r <= 0 ? r = 0 : r + this.s.dt._iDisplayLength > this.s.dt.fnRecordsDisplay() ? (r = this.s.dt.fnRecordsDisplay() - this.s.dt._iDisplayLength, r < 0 && (r = 0)) : r % 2 !== 0 && r++, this.s.targetTop = r, r != this.s.dt._iDisplayStart) {
          this.s.tableTop = Be(this.s.dt.nTable).offset().top, this.s.tableBottom = Be(this.s.dt.nTable).height() + this.s.tableTop;
          var s = function() {
            t.s.dt._iDisplayStart = t.s.targetTop, t.s.dtApi.draw("page");
          };
          this.s.dt.oFeatures.bServerSide ? (this.s.forceReposition = !0, Be(this.s.dt.nTable).triggerHandler("scroller-will-draw.dt"), H.versionCheck("2") ? t.s.dtApi.processing(!0) : this.s.dt.oApi._fnProcessingDisplay(this.s.dt, !0), clearTimeout(this.s.drawTO), this.s.drawTO = setTimeout(s, this.s.serverWait)) : s();
        }
      } else
        this.s.topRowFloat = this.pixelsToRow(e, !1, !0);
      if (this._info(), this.s.lastScrollTop = e, this.s.stateSaveThrottle(), this.s.scrollType === "jump" && this.s.mousedown && (this.s.labelVisible = !0), this.s.labelVisible) {
        var a = (n.viewport - n.labelHeight - n.xbar) / n.scroll;
        this.dom.label.html(
          this.s.dt.fnFormatNumber(
            parseInt(this.s.topRowFloat, 10) + 1
          )
        ).css("top", e + e * a).css("display", "block");
      }
    }
  },
  /**
   * Force the scrolling container to have height beyond that of just the
   * table that has been drawn so the user can scroll the whole data set.
   *
   * Note that if the calculated required scrolling height exceeds a maximum
   * value (1 million pixels - hard-coded) the forcing element will be set
   * only to that maximum value and virtual / physical domain transforms will
   * be used to allow Scroller to display tables of any number of records.
   *  @returns {void}
   *  @private
   */
  _scrollForce: function() {
    var t = this.s.heights, n = 1e6;
    t.virtual = t.row * this.s.dt.fnRecordsDisplay(), t.scroll = t.virtual, t.scroll > n && (t.scroll = n), this.dom.force.style.height = t.scroll > this.s.heights.row ? t.scroll + "px" : this.s.heights.row + "px";
  }
});
Ln.defaults = {
  /**
   * Scroller uses the boundary scaling factor to decide when to redraw the table - which it
   * typically does before you reach the end of the currently loaded data set (in order to
   * allow the data to look continuous to a user scrolling through the data). If given as 0
   * then the table will be redrawn whenever the viewport is scrolled, while 1 would not
   * redraw the table until the currently loaded data has all been shown. You will want
   * something in the middle - the default factor of 0.5 is usually suitable.
   *  @type     float
   *  @default  0.5
   *  @static
   */
  boundaryScale: 0.5,
  /**
   * The display buffer is what Scroller uses to calculate how many rows it should pre-fetch
   * for scrolling. Scroller automatically adjusts DataTables' display length to pre-fetch
   * rows that will be shown in "near scrolling" (i.e. just beyond the current display area).
   * The value is based upon the number of rows that can be displayed in the viewport (i.e.
   * a value of 1), and will apply the display range to records before before and after the
   * current viewport - i.e. a factor of 3 will allow Scroller to pre-fetch 1 viewport's worth
   * of rows before the current viewport, the current viewport's rows and 1 viewport's worth
   * of rows after the current viewport. Adjusting this value can be useful for ensuring
   * smooth scrolling based on your data set.
   *  @type     int
   *  @default  9
   *  @static
   */
  displayBuffer: 9,
  /**
   * Scroller will attempt to automatically calculate the height of rows for it's internal
   * calculations. However the height that is used can be overridden using this parameter.
   *  @type     int|string
   *  @default  auto
   *  @static
   */
  rowHeight: "auto",
  /**
   * When using server-side processing, Scroller will wait a small amount of time to allow
   * the scrolling to finish before requesting more data from the server. This prevents
   * you from DoSing your own server! The wait time can be configured by this parameter.
   *  @type     int
   *  @default  200
   *  @static
   */
  serverWait: 200
};
Ln.oDefaults = Ln.defaults;
Ln.version = "2.4.3";
Be(document).on("preInit.dt.dtscroller", function(t, n) {
  if (t.namespace === "dt") {
    var e = n.oInit.scroller, r = H.defaults.scroller;
    if (e || r) {
      var i = Be.extend({}, e, r);
      e !== !1 && new Ln(n, i);
    }
  }
});
Be.fn.dataTable.Scroller = Ln;
Be.fn.DataTable.Scroller = Ln;
var Wr = Be.fn.dataTable.Api;
Wr.register("scroller()", function() {
  return this;
});
Wr.register("scroller().rowToPixels()", function(t, n, e) {
  var r = this.context;
  if (r.length && r[0].oScroller)
    return r[0].oScroller.rowToPixels(t, n, e);
});
Wr.register("scroller().pixelsToRow()", function(t, n, e) {
  var r = this.context;
  if (r.length && r[0].oScroller)
    return r[0].oScroller.pixelsToRow(t, n, e);
});
Wr.register(
  ["scroller().scrollToRow()", "scroller.toPosition()"],
  function(t, n) {
    return this.iterator("table", function(e) {
      e.oScroller && e.oScroller.scrollToRow(t, n);
    }), this;
  }
);
Wr.register("row().scrollTo()", function(t) {
  var n = this;
  return this.iterator("row", function(e, r) {
    if (e.oScroller) {
      var i = n.rows({ order: "applied", search: "applied" }).indexes().indexOf(r);
      e.oScroller.scrollToRow(i, t);
    }
  }), this;
});
Wr.register("scroller.measure()", function(t) {
  return this.iterator("table", function(n) {
    n.oScroller && n.oScroller.measure(t);
  }), this;
});
Wr.register("scroller.page()", function() {
  var t = this.context;
  if (t.length && t[0].oScroller)
    return t[0].oScroller.pageInfo();
});
/*! AutoFill 2.7.1
 * © SpryMedia Ltd - datatables.net/license
 */
let Ae = lt;
var Gg = 0, En = function(t, n) {
  if (!H.versionCheck || !H.versionCheck("1.11"))
    throw "Warning: AutoFill requires DataTables 1.11 or greater";
  this.c = Ae.extend(
    !0,
    {},
    H.defaults.autoFill,
    En.defaults,
    n
  ), this.s = {
    /** @type {DataTable.Api} DataTables' API instance */
    dt: new H.Api(t),
    /** @type {String} Unique namespace for events attached to the document */
    namespace: ".autoFill" + Gg++,
    /** @type {Object} Cached dimension information for use in the mouse move event handler */
    scroll: {},
    /** @type {integer} Interval object used for smooth scrolling */
    scrollInterval: null,
    handle: {
      height: 0,
      width: 0
    },
    /**
     * Enabled setting
     * @type {Boolean}
     */
    enabled: !1
  }, this.dom = {
    closeButton: Ae('<div class="dtaf-popover-close">&times;</div>'),
    /** @type {jQuery} AutoFill handle */
    handle: Ae('<div class="dt-autofill-handle"/>'),
    /**
     * @type {Object} Selected cells outline - Need to use 4 elements,
     *   otherwise the mouse over if you back into the selected rectangle
     *   will be over that element, rather than the cells!
     */
    select: {
      top: Ae('<div class="dt-autofill-select top"/>'),
      right: Ae('<div class="dt-autofill-select right"/>'),
      bottom: Ae('<div class="dt-autofill-select bottom"/>'),
      left: Ae('<div class="dt-autofill-select left"/>')
    },
    /** @type {jQuery} Fill type chooser background */
    background: Ae('<div class="dt-autofill-background"/>'),
    /** @type {jQuery} Fill type chooser */
    list: Ae('<div class="dt-autofill-list">' + this.s.dt.i18n("autoFill.info", "") + "</div>").attr("aria-modal", !0).attr("role", "dialog").append('<div class="dt-autofill-list-items"></div>'),
    /** @type {jQuery} DataTables scrolling container */
    dtScroll: null,
    /** @type {jQuery} Offset parent element */
    offsetParent: null
  }, this._constructor();
};
Ae.extend(En.prototype, {
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Public methods (exposed via the DataTables API below)
   */
  enabled: function() {
    return this.s.enabled;
  },
  enable: function(t) {
    var n = this;
    if (t === !1)
      return this.disable();
    this.s.enabled = !0, this._focusListener(), this.dom.handle.on("mousedown touchstart", function(r) {
      return n._mousedown(r), !1;
    }), Ae(window).on("resize", function() {
      var r = Ae("div.dt-autofill-handle");
      r.length > 0 && n.dom.attachedTo !== void 0 && n._attach(n.dom.attachedTo);
    });
    let e = function() {
      n.s.handle = {
        height: !1,
        width: !1
      }, Ae(n.dom.handle).css({
        height: "",
        width: ""
      }), n.dom.attachedTo !== void 0 && n._attach(n.dom.attachedTo);
    };
    return Ae(window).on("orientationchange", function() {
      setTimeout(function() {
        e(), setTimeout(e, 150);
      }, 50);
    }), this;
  },
  disable: function() {
    return this.s.enabled = !1, this._focusListenerRemove(), this;
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Constructor
   */
  /**
   * Initialise the RowReorder instance
   *
   * @private
   */
  _constructor: function() {
    var t = this, n = this.s.dt, e = Ae("div.dataTables_scrollBody, div.dt-scroll-body", this.s.dt.table().container());
    n.settings()[0].autoFill = this, e.length && (this.dom.dtScroll = e, e.css("position") === "static" && e.css("position", "relative")), this.c.enable !== !1 && this.enable(), n.on("destroy.autoFill", function() {
      t._focusListenerRemove();
    });
  },
  /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
   * Private methods
   */
  /**
   * Display the AutoFill drag handle by appending it to a table cell. This
   * is the opposite of the _detach method.
   *
   * @param  {node} node TD/TH cell to insert the handle into
   * @private
   */
  _attach: function(t) {
    var n = this.s.dt, e = n.cell(t).index(), r = this.dom.handle, i = this.s.handle;
    if (!e || n.columns(this.c.columns).indexes().indexOf(e.column) === -1) {
      this._detach();
      return;
    }
    this.dom.offsetParent || (this.dom.offsetParent = Ae(n.table().node()).offsetParent()), (!i.height || !i.width) && (r.appendTo("body"), i.height = r.outerHeight(), i.width = r.outerWidth());
    var s = this._getPosition(t, this.dom.offsetParent);
    this.dom.attachedTo = t, r.css({
      top: s.top + t.offsetHeight - i.height,
      left: s.left + t.offsetWidth - i.width
    }).appendTo(this.dom.offsetParent);
  },
  /**
   * Determine can the fill type should be. This can be automatic, or ask the
   * end user.
   *
   * @param {array} cells Information about the selected cells from the key
   *     up function
   * @private
   */
  _actionSelector: function(t) {
    var n = this, e = this.s.dt, r = En.actions, i = [];
    if (Ae.each(r, function(o, l) {
      l.available(e, t) && i.push(o);
    }), i.length === 1 && this.c.alwaysAsk === !1) {
      var s = r[i[0]].execute(e, t);
      this._update(s, t);
    } else if (i.length > 1 || this.c.alwaysAsk) {
      var a = this.dom.list.children("div.dt-autofill-list-items").empty();
      i.push("cancel"), Ae.each(i, function(o, l) {
        a.append(
          Ae("<button/>").html(r[l].option(e, t)).append(Ae('<span class="dt-autofill-button"/>').html(e.i18n("autoFill.button", "&gt;"))).on("click", function(c) {
            if (c.target.nodeName.toLowerCase() === "button") {
              var f = r[l].execute(
                e,
                t,
                Ae(this).closest("button")
              );
              n._update(f, t), n.dom.background.remove(), n.dom.list.remove();
            }
          })
        );
      }), this.dom.background.appendTo("body"), this.dom.background.one("click", function() {
        n.dom.background.remove(), n.dom.list.remove();
      }), this.dom.list.appendTo("body"), this.c.closeButton && (this.dom.list.prepend(this.dom.closeButton).addClass(En.classes.closeable), this.dom.closeButton.on("click", function() {
        return n.dom.background.click();
      })), this.dom.list.css("margin-top", this.dom.list.outerHeight() / 2 * -1);
    }
  },
  /**
   * Remove the AutoFill handle from the document
   *
   * @private
   */
  _detach: function() {
    this.dom.attachedTo = null, this.dom.handle.detach();
  },
  /**
   * Draw the selection outline by calculating the range between the start
   * and end cells, then placing the highlighting elements to draw a rectangle
   *
   * @param  {node}   target End cell
   * @param  {object} e      Originating event
   * @private
   */
  _drawSelection: function(t, n) {
    var e = this.s.dt, r = this.s.start, i = Ae(this.dom.start), s = {
      row: this.c.vertical ? e.rows({ page: "current" }).nodes().indexOf(t.parentNode) : r.row,
      column: this.c.horizontal ? Ae(t).index() : r.column
    }, a = e.column.index("toData", s.column), o = e.row(":eq(" + s.row + ")", { page: "current" }), l = Ae(e.cell(o.index(), a).node());
    if (e.cell(l).any() && !(e.columns(this.c.columns).indexes().indexOf(a) === -1 || s.row === -1)) {
      this.s.end = s;
      var c, f, g, b, w, C;
      c = r.row < s.row ? i : l, f = r.row < s.row ? l : i, g = r.column < s.column ? i : l, b = r.column < s.column ? l : i, c = this._getPosition(c.get(0)).top, g = this._getPosition(g.get(0)).left, w = this._getPosition(f.get(0)).top + f.outerHeight() - c, C = this._getPosition(b.get(0)).left + b.outerWidth() - g;
      var m = this.dom.select;
      m.top.css({
        top: c,
        left: g,
        width: C
      }), m.left.css({
        top: c,
        left: g,
        height: w
      }), m.bottom.css({
        top: c + w,
        left: g,
        width: C
      }), m.right.css({
        top: c,
        left: g + C,
        height: w
      });
    }
  },
  /**
   * Use the Editor API to perform an update based on the new data for the
   * cells
   *
   * @param {array} cells Information about the selected cells from the key
   *     up function
   * @private
   */
  _editor: function(t) {
    var n = this.s.dt, e = this.c.editor;
    if (e) {
      for (var r = {}, i = [], s = e.fields(), a = 0, o = t.length; a < o; a++)
        for (var l = 0, c = t[a].length; l < c; l++) {
          var f = t[a][l], g = n.settings()[0].aoColumns[f.index.column], b = g.editField;
          if (b === void 0)
            for (var w = g.mData, C = 0, m = s.length; C < m; C++) {
              var h = e.field(s[C]);
              if (h.dataSrc() === w) {
                b = h.name();
                break;
              }
            }
          if (!b)
            throw "Could not automatically determine field data. Please see https://datatables.net/tn/11";
          r[b] || (r[b] = {});
          var u = n.row(f.index.row).id();
          r[b][u] = f.set, i.push(f.index);
        }
      e.bubble(i, !1).multiSet(r).submit(null, function() {
        e.close();
      });
    }
  },
  /**
   * Emit an event on the DataTable for listeners
   *
   * @param  {string} name Event name
   * @param  {array} args Event arguments
   * @private
   */
  _emitEvent: function(t, n) {
    this.s.dt.iterator("table", function(e, r) {
      Ae(e.nTable).triggerHandler(t + ".dt", n);
    });
  },
  /**
   * Attach suitable listeners (based on the configuration) that will attach
   * and detach the AutoFill handle in the document.
   *
   * @private
   */
  _focusListener: function() {
    var t = this, n = this.s.dt, e = this.s.namespace, r = this.c.focus !== null ? this.c.focus : n.init().keys || n.settings()[0].keytable ? "focus" : "hover";
    r === "focus" ? n.on("key-focus.autoFill", function(i, s, a) {
      t._attach(a.node());
    }).on("key-blur.autoFill", function(i, s, a) {
      t._detach();
    }) : r === "click" ? (Ae(n.table().body()).on("click" + e, "td, th", function(i) {
      t._attach(this);
    }), Ae(document.body).on("click" + e, function(i) {
      Ae(i.target).parents().filter(n.table().body()).length || t._detach();
    })) : Ae(n.table().body()).on("mouseenter" + e + " touchstart" + e, "td, th", function(i) {
      t._attach(this);
    }).on("mouseleave" + e + "touchend" + e, function(i) {
      Ae(i.relatedTarget).hasClass("dt-autofill-handle") || t._detach();
    });
  },
  _focusListenerRemove: function() {
    var t = this.s.dt;
    t.off(".autoFill"), Ae(t.table().body()).off(this.s.namespace), Ae(document.body).off(this.s.namespace);
  },
  /**
   * Get the position of a node, relative to another, including any scrolling
   * offsets.
   * @param  {Node}   node         Node to get the position of
   * @param  {jQuery} targetParent Node to use as the parent
   * @return {object}              Offset calculation
   * @private
   */
  _getPosition: function(t, n) {
    var e = t, r, i = 0, s = 0;
    n || (n = Ae(Ae(this.s.dt.table().node())[0].offsetParent));
    do {
      var a = e.offsetTop, o = e.offsetLeft;
      if (r = Ae(e.offsetParent), i += a + parseInt(r.css("border-top-width") || 0) * 1, s += o + parseInt(r.css("border-left-width") || 0) * 1, e.nodeName.toLowerCase() === "body")
        break;
      e = r.get(0);
    } while (r.get(0) !== n.get(0));
    return {
      top: i,
      left: s
    };
  },
  /**
   * Start mouse drag - selects the start cell
   *
   * @param  {object} e Mouse down event
   * @private
   */
  _mousedown: function(t) {
    var n = this, e = this.s.dt;
    this.dom.start = this.dom.attachedTo, this.s.start = {
      row: e.rows({ page: "current" }).nodes().indexOf(Ae(this.dom.start).parent()[0]),
      column: Ae(this.dom.start).index()
    }, Ae(document.body).on("mousemove.autoFill touchmove.autoFill", function(a) {
      n._mousemove(a), a.type === "touchmove" && Ae(document.body).one("touchend.autoFill", function() {
        n._detach();
      });
    }).on("mouseup.autoFill touchend.autoFill", function(a) {
      n._mouseup(a);
    });
    var r = this.dom.select, i = Ae(e.table().node()).offsetParent();
    r.top.appendTo(i), r.left.appendTo(i), r.right.appendTo(i), r.bottom.appendTo(i), this._drawSelection(this.dom.start, t), this.dom.handle.css("display", "none");
    var s = this.dom.dtScroll;
    this.s.scroll = {
      windowHeight: Ae(window).height(),
      windowWidth: Ae(window).width(),
      dtTop: s ? s.offset().top : null,
      dtLeft: s ? s.offset().left : null,
      dtHeight: s ? s.outerHeight() : null,
      dtWidth: s ? s.outerWidth() : null
    };
  },
  /**
   * Mouse drag - selects the end cell and update the selection display for
   * the end user
   *
   * @param  {object} e Mouse move event
   * @private
   */
  _mousemove: function(t) {
    var n = t.touches && t.touches.length ? document.elementFromPoint(t.touches[0].clientX, t.touches[0].clientY) : t.target, e = n.nodeName.toLowerCase();
    e !== "td" && e !== "th" || (this._drawSelection(n, t), this._shiftScroll(t));
  },
  /**
   * End mouse drag - perform the update actions
   *
   * @param  {object} e Mouse up event
   * @private
   */
  _mouseup: function(t) {
    Ae(document.body).off(".autoFill");
    var n = this, e = this.s.dt, r = this.dom.select;
    r.top.remove(), r.left.remove(), r.right.remove(), r.bottom.remove(), this.dom.handle.css("display", "block");
    var i = this.s.start, s = this.s.end;
    if (!(i.row === s.row && i.column === s.column)) {
      var a = e.cell(":eq(" + i.row + ")", i.column + ":visible", { page: "current" });
      if (Ae("div.DTE", a.node()).length) {
        var o = e.editor();
        o.on("submitSuccess.dtaf close.dtaf", function() {
          o.off(".dtaf"), setTimeout(function() {
            n._mouseup(t);
          }, 100);
        }).on("submitComplete.dtaf preSubmitCancelled.dtaf close.dtaf", function() {
          o.off(".dtaf");
        }), o.submit();
        return;
      }
      for (var l = this._range(i.row, s.row), c = this._range(i.column, s.column), f = [], g = e.settings()[0], b = g.aoColumns, w = e.columns(this.c.columns).indexes(), C = 0; C < l.length; C++)
        f.push(
          Ae.map(c, function(m) {
            var h = e.row(":eq(" + l[C] + ")", { page: "current" }), u = e.cell(h.index(), m + ":visible"), p = u.data(), v = u.index(), S = b[v.column].editField;
            if (S !== void 0 && (p = H.util.get(S)(e.row(v.row).data())), w.indexOf(v.column) !== -1)
              return {
                cell: u,
                data: p,
                label: u.data(),
                index: v
              };
          })
        );
      this._actionSelector(f), clearInterval(this.s.scrollInterval), this.s.scrollInterval = null;
    }
  },
  /**
   * Create an array with a range of numbers defined by the start and end
   * parameters passed in (inclusive!).
   * 
   * @param  {integer} start Start
   * @param  {integer} end   End
   * @private
   */
  _range: function(t, n) {
    var e = [], r;
    if (t <= n)
      for (r = t; r <= n; r++)
        e.push(r);
    else
      for (r = t; r >= n; r--)
        e.push(r);
    return e;
  },
  /**
   * Move the window and DataTables scrolling during a drag to scroll new
   * content into view. This is done by proximity to the edge of the scrolling
   * container of the mouse - for example near the top edge of the window
   * should scroll up. This is a little complicated as there are two elements
   * that can be scrolled - the window and the DataTables scrolling view port
   * (if scrollX and / or scrollY is enabled).
   *
   * @param  {object} e Mouse move event object
   * @private
   */
  _shiftScroll: function(t) {
    var n = this, e = this.s.scroll, r = !1, i = 5, s = 65, a = t.type.includes("touch") ? t.touches[0].clientX : t.pageX - window.scrollX, o = t.type.includes("touch") ? t.touches[0].clientY : t.pageY - window.scrollY, l = o, c = a, f, g, b, w;
    l < s ? f = i * -1 : l > e.windowHeight - s && (f = i), c < s ? g = i * -1 : c > e.windowWidth - s && (g = i), e.dtTop !== null && o < e.dtTop + s ? b = i * -1 : e.dtTop !== null && o > e.dtTop + e.dtHeight - s && (b = i), e.dtLeft !== null && a < e.dtLeft + s ? w = i * -1 : e.dtLeft !== null && a > e.dtLeft + e.dtWidth - s && (w = i), f || g || b || w ? (e.windowVert = f, e.windowHoriz = g, e.dtVert = b, e.dtHoriz = w, r = !0) : this.s.scrollInterval && (clearInterval(this.s.scrollInterval), this.s.scrollInterval = null), !this.s.scrollInterval && r && (this.s.scrollInterval = setInterval(function() {
      if (window.scrollTo(window.scrollX + (e.windowHoriz ? e.windowHoriz : 0), window.scrollY + (e.windowVert ? e.windowVert : 0)), e.dtVert || e.dtHoriz) {
        var C = n.dom.dtScroll[0];
        e.dtVert && (C.scrollTop += e.dtVert), e.dtHoriz && (C.scrollLeft += e.dtHoriz);
      }
    }, 20));
  },
  /**
   * Update the DataTable after the user has selected what they want to do
   *
   * @param  {false|undefined} result Return from the `execute` method - can
   *   be false internally to do nothing. This is not documented for plug-ins
   *   and is used only by the cancel option.
   * @param {array} cells Information about the selected cells from the key
   *     up function, argumented with the set values
   * @private
   */
  _update: function(t, n) {
    if (t !== !1) {
      var e = this.s.dt, r, i = e.columns(this.c.columns).indexes();
      this._emitEvent("preAutoFill", [e, n]), this._editor(n);
      var s = this.c.update !== null ? this.c.update : !this.c.editor;
      if (s) {
        for (var a = 0, o = n.length; a < o; a++)
          for (var l = 0, c = n[a].length; l < c; l++)
            r = n[a][l], i.indexOf(r.index.column) !== -1 && r.cell.data(r.set);
        e.draw(!1);
      }
      this._emitEvent("autoFill", [e, n]);
    }
  }
});
En.actions = {
  increment: {
    available: function(t, n) {
      var e = n[0][0].label;
      return !isNaN(e - parseFloat(e));
    },
    option: function(t, n) {
      return t.i18n(
        "autoFill.increment",
        'Increment / decrement each cell by: <input type="number" value="1">'
      );
    },
    execute: function(t, n, e) {
      for (var r = n[0][0].data * 1, i = Ae("input", e).val() * 1, s = 0, a = n.length; s < a; s++)
        for (var o = 0, l = n[s].length; o < l; o++)
          n[s][o].set = r, r += i;
    }
  },
  fill: {
    available: function(t, n) {
      return !0;
    },
    option: function(t, n) {
      return t.i18n("autoFill.fill", "Fill all cells with <i>%d</i>", n[0][0].label);
    },
    execute: function(t, n, e) {
      for (var r = n[0][0].data, i = 0, s = n.length; i < s; i++)
        for (var a = 0, o = n[i].length; a < o; a++)
          n[i][a].set = r;
    }
  },
  fillHorizontal: {
    available: function(t, n) {
      return n.length > 1 && n[0].length > 1;
    },
    option: function(t, n) {
      return t.i18n("autoFill.fillHorizontal", "Fill cells horizontally");
    },
    execute: function(t, n, e) {
      for (var r = 0, i = n.length; r < i; r++)
        for (var s = 0, a = n[r].length; s < a; s++)
          n[r][s].set = n[r][0].data;
    }
  },
  fillVertical: {
    available: function(t, n) {
      return n.length > 1 && n[0].length > 1;
    },
    option: function(t, n) {
      return t.i18n("autoFill.fillVertical", "Fill cells vertically");
    },
    execute: function(t, n, e) {
      for (var r = 0, i = n.length; r < i; r++)
        for (var s = 0, a = n[r].length; s < a; s++)
          n[r][s].set = n[0][s].data;
    }
  },
  // Special type that does not make itself available, but is added
  // automatically by AutoFill if a multi-choice list is shown. This allows
  // sensible code reuse
  cancel: {
    available: function() {
      return !1;
    },
    option: function(t) {
      return t.i18n("autoFill.cancel", "Cancel");
    },
    execute: function() {
      return !1;
    }
  }
};
En.version = "2.7.1";
En.defaults = {
  /** @type {Boolean} Ask user what they want to do, even for a single option */
  alwaysAsk: !1,
  closeButton: !0,
  /** @type {string|null} What will trigger a focus */
  focus: null,
  // focus, click, hover
  /** @type {column-selector} Columns to provide auto fill for */
  columns: "",
  // all
  /** @type {Boolean} Enable AutoFill on load */
  enable: !0,
  /** @type {boolean|null} Update the cells after a drag */
  update: null,
  // false is editor given, true otherwise
  /** @type {DataTable.Editor} Editor instance for automatic submission */
  editor: null,
  /** @type {boolean} Enable vertical fill */
  vertical: !0,
  /** @type {boolean} Enable horizontal fill */
  horizontal: !0
};
En.classes = {
  /** @type {String} Class used by the selection button */
  btn: "btn",
  closeable: "dtaf-popover-closeable"
};
var Ja = Ae.fn.dataTable.Api;
Ja.register("autoFill()", function() {
  return this;
});
Ja.register("autoFill().enabled()", function() {
  var t = this.context[0];
  return t.autoFill ? t.autoFill.enabled() : !1;
});
Ja.register("autoFill().enable()", function(t) {
  return this.iterator("table", function(n) {
    n.autoFill && n.autoFill.enable(t);
  });
});
Ja.register("autoFill().disable()", function() {
  return this.iterator("table", function(t) {
    t.autoFill && t.autoFill.disable();
  });
});
Ae(document).on("preInit.dt.autofill", function(t, n, e) {
  if (t.namespace === "dt") {
    var r = n.oInit.autoFill, i = H.defaults.autoFill;
    if (r || i) {
      var s = Ae.extend({}, r, i);
      r !== !1 && new En(n, s);
    }
  }
});
H.AutoFill = En;
Ae.fn.DataTable.AutoFill = En;
/*! Bootstrap integration for DataTables' AutoFill
 * ©2015 SpryMedia Ltd - datatables.net/license
 */
H.AutoFill.classes.btn = "btn btn-primary";
/*! StateRestore 1.4.3
 * © SpryMedia Ltd - datatables.net/license
 */
let Sn = lt;
(function() {
  var t, n;
  function e(b) {
    t = b, n = b.fn.dataTable;
  }
  var r = (
    /** @class */
    function() {
      function b(w, C, m, h, u, p) {
        if (h === void 0 && (h = void 0), u === void 0 && (u = !1), p === void 0 && (p = function() {
          return null;
        }), !n || !n.versionCheck || !n.versionCheck("1.10.0"))
          throw new Error("StateRestore requires DataTables 1.10 or newer");
        if (!n.Buttons)
          throw new Error("StateRestore requires Buttons");
        var v = new n.Api(w);
        this.classes = t.extend(!0, {}, b.classes), this.c = t.extend(!0, {}, b.defaults, C), this.s = {
          dt: v,
          identifier: m,
          isPreDefined: u,
          savedState: h,
          tableId: h && h.stateRestore ? h.stateRestore.tableId : void 0
        }, this.dom = {
          background: t('<div class="' + this.classes.background + '"/>'),
          closeButton: t('<div class="' + this.classes.closeButton + '">&times;</div>'),
          confirmation: t('<div class="' + this.classes.confirmation + '"/>'),
          confirmationButton: t('<button class="' + this.classes.confirmationButton + " " + this.classes.dtButton + '">'),
          confirmationTitleRow: t('<div class="' + this.classes.confirmationTitleRow + '"></div>'),
          dtContainer: t(this.s.dt.table().container()),
          duplicateError: t('<span class="' + this.classes.modalError + '">' + this.s.dt.i18n("stateRestore.duplicateError", this.c.i18n.duplicateError) + "</span>"),
          emptyError: t('<span class="' + this.classes.modalError + '">' + this.s.dt.i18n("stateRestore.emptyError", this.c.i18n.emptyError) + "</span>"),
          removeContents: t('<div class="' + this.classes.confirmationText + '"><span>' + this.s.dt.i18n("stateRestore.removeConfirm", this.c.i18n.removeConfirm).replace(/%s/g, b.entityEncode(this.s.identifier)) + "</span></div>"),
          removeError: t('<span class="' + this.classes.modalError + '">' + this.s.dt.i18n("stateRestore.removeError", this.c.i18n.removeError) + "</span>"),
          removeTitle: t('<h2 class="' + this.classes.confirmationTitle + '">' + this.s.dt.i18n("stateRestore.removeTitle", this.c.i18n.removeTitle) + "</h2>"),
          renameContents: t('<div class="' + this.classes.confirmationText + " " + this.classes.renameModal + '"><label class="' + this.classes.confirmationMessage + '">' + this.s.dt.i18n("stateRestore.renameLabel", this.c.i18n.renameLabel).replace(/%s/g, b.entityEncode(this.s.identifier)) + "</label></div>"),
          renameInput: t('<input class="' + this.classes.input + '" type="text"></input>'),
          renameTitle: t('<h2 class="' + this.classes.confirmationTitle + '">' + this.s.dt.i18n("stateRestore.renameTitle", this.c.i18n.renameTitle) + "</h2>")
        }, this.save(h, p, !u);
      }
      return b.prototype.remove = function(w) {
        var C, m = this;
        if (w === void 0 && (w = !1), !this.c.remove)
          return !1;
        var h, u = {
          action: "remove",
          stateRestore: (C = {}, C[this.s.identifier] = this.s.savedState, C)
        }, p = function() {
          m.dom.confirmation.trigger("dtsr-remove"), t(m.s.dt.table().node()).trigger("stateRestore-change"), m.dom.background.click(), m.dom.confirmation.remove(), t(document).unbind("keyup", function(v) {
            return m._keyupFunction(v);
          }), m.dom.confirmationButton.off("click");
        };
        return this.c.ajax ? typeof this.c.ajax == "string" && this.s.dt.settings()[0]._bInitComplete ? h = function() {
          return t.ajax({
            data: u,
            success: p,
            type: "POST",
            url: m.c.ajax
          }), !0;
        } : typeof this.c.ajax == "function" && (h = function() {
          return typeof m.c.ajax == "function" && m.c.ajax.call(m.s.dt, u, p), !0;
        }) : h = function() {
          try {
            localStorage.removeItem("DataTables_stateRestore_" + m.s.identifier + "_" + location.pathname + (m.s.tableId ? "_" + m.s.tableId : "")), p();
          } catch {
            return m.dom.confirmation.children("." + m.classes.modalError).remove(), m.dom.confirmation.append(m.dom.removeError), "remove";
          }
          return !0;
        }, w ? (this.dom.confirmation.appendTo(this.dom.dtContainer), t(this.s.dt.table().node()).trigger("dtsr-modal-inserted"), h(), this.dom.confirmation.remove()) : this._newModal(this.dom.removeTitle, this.s.dt.i18n("stateRestore.removeSubmit", this.c.i18n.removeSubmit), h, this.dom.removeContents), !0;
      }, b.prototype.compare = function(w) {
        if (this.c.saveState.order || (w.order = void 0), this.c.saveState.search || (w.search = void 0), this.c.saveState.columns && w.columns)
          for (var C = 0, m = w.columns.length; C < m; C++)
            typeof this.c.saveState.columns != "boolean" && !this.c.saveState.columns.visible && (w.columns[C].visible = void 0), typeof this.c.saveState.columns != "boolean" && !this.c.saveState.columns.search && (w.columns[C].search = void 0);
        else
          this.c.saveState.columns || (w.columns = void 0);
        this.c.saveState.paging || (w.page = void 0), this.c.saveState.searchBuilder || (w.searchBuilder = void 0), this.c.saveState.searchPanes || (w.searchPanes = void 0), this.c.saveState.select || (w.select = void 0), this.c.saveState.colReorder || (w.ColReorder = void 0), this.c.saveState.scroller || (w.scroller = void 0, n.Scroller !== void 0 && (w.start = 0)), this.c.saveState.paging || (w.start = 0), this.c.saveState.length || (w.length = void 0), delete w.time;
        var h = this.s.savedState;
        return delete h.time, delete h.c, delete h.stateRestore, this._deepCompare(w, h);
      }, b.prototype.destroy = function() {
        t.each(this.dom, function(w, C) {
          C.off().remove();
        });
      }, b.prototype.load = function() {
        var w = this, C = this.s.savedState, m = this.s.dt.settings()[0];
        C.time = +/* @__PURE__ */ new Date(), m.oLoadedState = t.extend(!0, {}, C), t("div.dt-button-background").click();
        var h = function() {
          var u = function(p, v) {
            setTimeout(function() {
              var S = v._iDisplayStart / v._iDisplayLength, T = C.start / C.length;
              S >= 0 && T >= 0 && S !== T && w.s.dt.page(T).draw(!1);
            }, 50);
          };
          w.s.dt.one("preDraw", u), w.s.dt.draw(!1);
        };
        return H.versionCheck("2") ? (this.s.dt.state(C), h()) : H.ext.oApi._fnImplementState(m, C, h), C;
      }, b.prototype.rename = function(w, C) {
        var m = this;
        if (w === void 0 && (w = null), !!this.c.rename) {
          var h = function() {
            var u;
            if (w === null) {
              var p = t("input." + m.classes.input.replace(/ /g, ".")).val();
              if (p.length === 0)
                return m.dom.confirmation.children("." + m.classes.modalError).remove(), m.dom.confirmation.append(m.dom.emptyError), "empty";
              if (C.includes(p))
                return m.dom.confirmation.children("." + m.classes.modalError).remove(), m.dom.confirmation.append(m.dom.duplicateError), "duplicate";
              w = p;
            }
            var v = {
              action: "rename",
              stateRestore: (u = {}, u[m.s.identifier] = w, u)
            }, S = function() {
              m.s.identifier = w, m.save(m.s.savedState, function() {
                return null;
              }, !1), m.dom.removeContents = t('<div class="' + m.classes.confirmationText + '"><span>' + m.s.dt.i18n("stateRestore.removeConfirm", m.c.i18n.removeConfirm).replace(/%s/g, m.s.identifier) + "</span></div>"), m.dom.confirmation.trigger("dtsr-rename"), m.dom.background.click(), m.dom.confirmation.remove(), t(document).unbind("keyup", function(T) {
                return m._keyupFunction(T);
              }), m.dom.confirmationButton.off("click");
            };
            if (m.c.ajax)
              typeof m.c.ajax == "string" && m.s.dt.settings()[0]._bInitComplete ? t.ajax({
                data: v,
                success: S,
                type: "POST",
                url: m.c.ajax
              }) : typeof m.c.ajax == "function" && m.c.ajax.call(m.s.dt, v, S);
            else
              try {
                localStorage.removeItem("DataTables_stateRestore_" + m.s.identifier + "_" + location.pathname + (m.s.tableId ? "_" + m.s.tableId : "")), S();
              } catch {
                return m.dom.confirmation.children("." + m.classes.modalError).remove(), m.dom.confirmation.append(m.dom.removeError), !1;
              }
            return !0;
          };
          if (w !== null) {
            if (C.includes(w))
              throw new Error(this.s.dt.i18n("stateRestore.duplicateError", this.c.i18n.duplicateError));
            if (w.length === 0)
              throw new Error(this.s.dt.i18n("stateRestore.emptyError", this.c.i18n.emptyError));
            this.dom.confirmation.appendTo(this.dom.dtContainer), t(this.s.dt.table().node()).trigger("dtsr-modal-inserted"), h(), this.dom.confirmation.remove();
          } else
            this.dom.renameInput.val(this.s.identifier), this.dom.renameContents.append(this.dom.renameInput), this._newModal(this.dom.renameTitle, this.s.dt.i18n("stateRestore.renameButton", this.c.i18n.renameButton), h, this.dom.renameContents);
        }
      }, b.prototype.save = function(w, C, m) {
        var h, u = this;
        if (m === void 0 && (m = !0), !this.c.save) {
          C && C.call(this);
          return;
        }
        var p;
        if (this.s.dt.state.save(), w === void 0)
          p = this.s.dt.state();
        else {
          if (typeof w != "object")
            return;
          p = w;
        }
        if (p.stateRestore ? (p.stateRestore.isPreDefined = this.s.isPreDefined, p.stateRestore.state = this.s.identifier, p.stateRestore.tableId = this.s.tableId) : p.stateRestore = {
          isPreDefined: this.s.isPreDefined,
          state: this.s.identifier,
          tableId: this.s.tableId
        }, this.s.savedState = p, this.c.saveState.order || (this.s.savedState.order = void 0), this.c.saveState.search || (this.s.savedState.search = void 0), this.c.saveState.columns && this.s.savedState.columns)
          for (var v = 0, S = this.s.savedState.columns.length; v < S; v++)
            typeof this.c.saveState.columns != "boolean" && !this.c.saveState.columns.visible && (this.s.savedState.columns[v].visible = void 0), typeof this.c.saveState.columns != "boolean" && !this.c.saveState.columns.search && (this.s.savedState.columns[v].search = void 0);
        else
          this.c.saveState.columns || (this.s.savedState.columns = void 0);
        if (this.c.saveState.searchBuilder || (this.s.savedState.searchBuilder = void 0), this.c.saveState.searchPanes || (this.s.savedState.searchPanes = void 0), this.c.saveState.select || (this.s.savedState.select = void 0), this.c.saveState.colReorder || (this.s.savedState.ColReorder = void 0), this.c.saveState.scroller || (this.s.savedState.scroller = void 0, n.Scroller !== void 0 && (this.s.savedState.start = 0)), this.c.saveState.paging || (this.s.savedState.start = 0), this.c.saveState.length || (this.s.savedState.length = void 0), this.s.savedState.c = this.c, this.s.savedState.c.splitSecondaries.length)
          for (var T = 0, A = this.s.savedState.c.splitSecondaries; T < A.length; T++) {
            var y = A[T];
            y.parent && (y.parent = void 0);
          }
        var I = {
          action: "save",
          stateRestore: (h = {}, h[this.s.identifier] = this.s.savedState, h)
        }, $ = function() {
          C && C.call(u), u.dom.confirmation.trigger("dtsr-save"), t(u.s.dt.table().node()).trigger("stateRestore-change");
        };
        this.c.ajax ? typeof this.c.ajax == "string" && m ? this.s.dt.settings()[0]._bInitComplete ? t.ajax({
          data: I,
          success: $,
          type: "POST",
          url: this.c.ajax
        }) : this.s.dt.one("init", function() {
          t.ajax({
            data: I,
            success: $,
            type: "POST",
            url: u.c.ajax
          });
        }) : typeof this.c.ajax == "function" && m ? this.c.ajax.call(this.s.dt, I, $) : m || $() : (localStorage.setItem("DataTables_stateRestore_" + this.s.identifier + "_" + location.pathname + (this.s.tableId ? "_" + this.s.tableId : ""), JSON.stringify(this.s.savedState)), $());
      }, b.entityEncode = function(w) {
        return typeof w == "string" ? w.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;") : w;
      }, b.prototype._deepCompare = function(w, C) {
        if (w === null && C === null)
          return !0;
        if (w === null || C === null)
          return !1;
        var m = [w, C], h = [Object.keys(w).sort(), Object.keys(C).sort()], u, p;
        for (h[0].includes("scroller") && (u = h[0].indexOf("start"), u && h[0].splice(u, 1)), h[1].includes("scroller") && (u = h[1].indexOf("start"), u && h[1].splice(u, 1)), p = 0; p < h[0].length; p++) {
          if (h[0][p].indexOf("_") === 0) {
            h[0].splice(p, 1), p--;
            continue;
          }
          if (h[0][p] === "baseRowTop" || h[0][p] === "baseScrollTop" || h[0][p] === "scrollTop" || !this.c.saveState.paging && h[0][p] === "page") {
            h[0].splice(p, 1), p--;
            continue;
          }
        }
        for (p = 0; p < h[1].length; p++) {
          if (h[1][p].indexOf("_") === 0) {
            h[1].splice(p, 1), p--;
            continue;
          }
          if (h[1][p] === "baseRowTop" || h[1][p] === "baseScrollTop" || h[1][p] === "scrollTop" || !this.c.saveState.paging && h[0][p] === "page") {
            h[1].splice(p, 1), p--;
            continue;
          }
        }
        if (h[0].length === 0 && h[1].length > 0 || h[1].length === 0 && h[0].length > 0)
          return !1;
        for (p = 0; p < h[0].length; p++)
          h[1].includes(h[0][p]) || (h[0].splice(p, 1), p--);
        for (p = 0; p < h[1].length; p++)
          h[0].includes(h[1][p]) || (h[1].splice(p, 1), p--);
        for (p = 0; p < h[0].length; p++) {
          if (h[0][p] !== h[1][p] || typeof m[0][h[0][p]] != typeof m[1][h[1][p]])
            return !1;
          if (typeof m[0][h[0][p]] == "object") {
            if (Array.isArray(m[0][h[0][p]]) && Array.isArray(m[1][h[1][p]]) && m[0][h[0][p]].length !== m[1][h[0][p]].length || !this._deepCompare(m[0][h[0][p]], m[1][h[1][p]]))
              return !1;
          } else if (typeof m[0][h[0][p]] == "number" && typeof m[1][h[1][p]] == "number") {
            if (Math.round(m[0][h[0][p]]) !== Math.round(m[1][h[1][p]]))
              return !1;
          } else if (m[0][h[0][p]] !== m[1][h[1][p]])
            return !1;
        }
        return !0;
      }, b.prototype._keyupFunction = function(w) {
        w.key === "Enter" ? this.dom.confirmationButton.click() : w.key === "Escape" && t("div." + this.classes.background.replace(/ /g, ".")).click();
      }, b.prototype._newModal = function(w, C, m, h) {
        var u = this;
        this.dom.background.appendTo(this.dom.dtContainer), this.dom.confirmationTitleRow.empty().append(w), this.dom.confirmationButton.html(C), this.dom.confirmation.empty().append(this.dom.confirmationTitleRow).append(h).append(t('<div class="' + this.classes.confirmationButtons + '"></div>').append(this.dom.confirmationButton)).appendTo(this.dom.dtContainer), t(this.s.dt.table().node()).trigger("dtsr-modal-inserted");
        var p = h.children("input");
        p.length > 0 ? t(p[0]).focus() : this.dom.confirmationButton.focus();
        var v = t("div." + this.classes.background.replace(/ /g, "."));
        this.c.modalCloseButton && (this.dom.confirmation.append(this.dom.closeButton), this.dom.closeButton.on("click", function() {
          return v.click();
        })), this.dom.confirmationButton.on("click", function() {
          return m();
        }), this.dom.confirmation.on("click", function(S) {
          S.stopPropagation();
        }), v.one("click", function() {
          u.dom.background.remove(), u.dom.confirmation.remove(), t(document).unbind("keyup", function(S) {
            return u._keyupFunction(S);
          });
        }), t(document).on("keyup", function(S) {
          return u._keyupFunction(S);
        });
      }, b.version = "1.4.3", b.classes = {
        background: "dtsr-background",
        closeButton: "dtsr-popover-close",
        confirmation: "dtsr-confirmation",
        confirmationButton: "dtsr-confirmation-button",
        confirmationButtons: "dtsr-confirmation-buttons",
        confirmationMessage: "dtsr-confirmation-message dtsr-name-label",
        confirmationText: "dtsr-confirmation-text",
        confirmationTitle: "dtsr-confirmation-title",
        confirmationTitleRow: "dtsr-confirmation-title-row",
        dtButton: "dt-button",
        input: "dtsr-input",
        modalError: "dtsr-modal-error",
        renameModal: "dtsr-rename-modal"
      }, b.defaults = {
        _createInSaved: !1,
        ajax: !1,
        create: !0,
        creationModal: !1,
        i18n: {
          creationModal: {
            button: "Create",
            colReorder: "Column Order:",
            columns: {
              search: "Column Search:",
              visible: "Column Visibility:"
            },
            length: "Page Length:",
            name: "Name:",
            order: "Sorting:",
            paging: "Paging:",
            scroller: "Scroll Position:",
            search: "Search:",
            searchBuilder: "SearchBuilder:",
            searchPanes: "SearchPanes:",
            select: "Select:",
            title: "Create New State",
            toggleLabel: "Includes:"
          },
          duplicateError: "A state with this name already exists.",
          emptyError: "Name cannot be empty.",
          emptyStates: "No saved states",
          removeConfirm: 'Are you sure you want to remove "%s"?',
          removeError: "Failed to remove state.",
          removeJoiner: " and ",
          removeSubmit: "Remove",
          removeTitle: "Remove State",
          renameButton: "Rename",
          renameLabel: 'New Name for "%s":',
          renameTitle: "Rename State"
        },
        modalCloseButton: !0,
        remove: !0,
        rename: !0,
        save: !0,
        saveState: {
          colReorder: !0,
          columns: {
            search: !0,
            visible: !0
          },
          length: !0,
          order: !0,
          paging: !0,
          scroller: !0,
          search: !0,
          searchBuilder: !0,
          searchPanes: !0,
          select: !0
        },
        splitSecondaries: [
          "updateState",
          "renameState",
          "removeState"
        ],
        toggle: {
          colReorder: !1,
          columns: {
            search: !1,
            visible: !1
          },
          length: !1,
          order: !1,
          paging: !1,
          scroller: !1,
          search: !1,
          searchBuilder: !1,
          searchPanes: !1,
          select: !1
        },
        createButton: null,
        createState: null
      }, b;
    }()
  ), i, s;
  function a(b) {
    i = b, s = b.fn.dataTable;
  }
  var o = (
    /** @class */
    function() {
      function b(w, C) {
        var m = this;
        if (!s || !s.versionCheck || !s.versionCheck("1.10.0"))
          throw new Error("StateRestore requires DataTables 1.10 or newer");
        if (!s.Buttons)
          throw new Error("StateRestore requires Buttons");
        var h = new s.Api(w);
        if (this.classes = i.extend(!0, {}, b.classes), h.settings()[0]._stateRestore === void 0) {
          this.c = i.extend(!0, {}, b.defaults, C), this.s = {
            dt: h,
            hasColReorder: s.ColReorder !== void 0,
            hasScroller: s.Scroller !== void 0,
            hasSearchBuilder: s.SearchBuilder !== void 0,
            hasSearchPanes: s.SearchPanes !== void 0,
            hasSelect: s.select !== void 0,
            states: []
          }, this.s.dt.on("xhr", function(v, S, T) {
            T && T.stateRestore && m._addPreDefined(T.stateRestore);
          }), this.dom = {
            background: i('<div class="' + this.classes.background + '"/>'),
            checkboxInputRow: i('<div class="' + this.classes.formRow + '"><label class="' + this.classes.nameLabel + '">' + this.s.dt.i18n("stateRestore.creationModal.toggleLabel", this.c.i18n.creationModal.toggleLabel) + '</label><div class="dtsr-input"></div></div>'),
            closeButton: i('<div class="' + this.classes.closeButton + '">x</div>'),
            colReorderToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.colReorderToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.colReorder", this.c.i18n.creationModal.colReorder) + "</div>"),
            columnsSearchToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.columnsSearchToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.columns.search", this.c.i18n.creationModal.columns.search) + "</div>"),
            columnsVisibleToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.columnsVisibleToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.columns.visible", this.c.i18n.creationModal.columns.visible) + "</div>"),
            confirmation: i('<div class="' + this.classes.confirmation + '"/>'),
            confirmationTitleRow: i('<div class="' + this.classes.confirmationTitleRow + '"></div>'),
            createButtonRow: i('<div class="' + this.classes.formRow + " " + this.classes.modalFoot + '"><button class="' + this.classes.creationButton + " " + this.classes.dtButton + '">' + this.s.dt.i18n("stateRestore.creationModal.button", this.c.i18n.creationModal.button) + "</button></div>"),
            creation: i('<div class="' + this.classes.creation + '"/>'),
            creationForm: i('<div class="' + this.classes.creationForm + '"/>'),
            creationTitle: i('<div class="' + this.classes.creationText + '"><h2 class="' + this.classes.creationTitle + '">' + this.s.dt.i18n("stateRestore.creationModal.title", this.c.i18n.creationModal.title) + "</h2></div>"),
            dtContainer: i(this.s.dt.table().container()),
            duplicateError: i('<span class="' + this.classes.modalError + '">' + this.s.dt.i18n("stateRestore.duplicateError", this.c.i18n.duplicateError) + "</span>"),
            emptyError: i('<span class="' + this.classes.modalError + '">' + this.s.dt.i18n("stateRestore.emptyError", this.c.i18n.emptyError) + "</span>"),
            lengthToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.lengthToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.length", this.c.i18n.creationModal.length) + "</div>"),
            nameInputRow: i('<div class="' + this.classes.formRow + '"><label class="' + this.classes.nameLabel + '">' + this.s.dt.i18n("stateRestore.creationModal.name", this.c.i18n.creationModal.name) + '</label><div class="dtsr-input"><input class="' + this.classes.nameInput + '" type="text"></div></div>'),
            orderToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.orderToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.order", this.c.i18n.creationModal.order) + "</div>"),
            pagingToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.pagingToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.paging", this.c.i18n.creationModal.paging) + "</div>"),
            removeContents: i('<div class="' + this.classes.confirmationText + '"><span></span></div>'),
            removeTitle: i('<div class="' + this.classes.creationText + '"><h2 class="' + this.classes.creationTitle + '">' + this.s.dt.i18n("stateRestore.removeTitle", this.c.i18n.removeTitle) + "</h2></div>"),
            scrollerToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.scrollerToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.scroller", this.c.i18n.creationModal.scroller) + "</div>"),
            searchBuilderToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.searchBuilderToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.searchBuilder", this.c.i18n.creationModal.searchBuilder) + "</div>"),
            searchPanesToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.searchPanesToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.searchPanes", this.c.i18n.creationModal.searchPanes) + "</div>"),
            searchToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.searchToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.search", this.c.i18n.creationModal.search) + "</div>"),
            selectToggle: i('<div class="' + this.classes.checkLabel + '"><input type="checkbox" class="' + this.classes.selectToggle + " " + this.classes.checkBox + '" checked>' + this.s.dt.i18n("stateRestore.creationModal.select", this.c.i18n.creationModal.select) + "</div>")
          }, h.settings()[0]._stateRestore = this, this._searchForStates(), this._addPreDefined(this.c.preDefined);
          var u, p = {
            action: "load"
          };
          return typeof this.c.ajax == "function" ? u = function() {
            typeof m.c.ajax == "function" && m.c.ajax.call(m.s.dt, p, function(v) {
              return m._addPreDefined(v);
            });
          } : typeof this.c.ajax == "string" && (u = function() {
            i.ajax({
              data: p,
              dataType: "json",
              success: function(v) {
                m._addPreDefined(v);
              },
              type: "POST",
              url: m.c.ajax
            });
          }), typeof u == "function" && (this.s.dt.settings()[0]._bInitComplete ? u() : this.s.dt.one("preInit.dtsr", function() {
            u();
          })), this.s.dt.on("destroy.dtsr", function() {
            m.destroy();
          }), this.s.dt.on("draw.dtsr buttons-action.dtsr", function() {
            return m.findActive();
          }), this;
        }
      }
      return b.prototype.addState = function(w, C, m) {
        var h = this;
        if (!(!this.c.create || !this.c.save)) {
          var u = this.getState(w), p = function(S, T) {
            if (S.length === 0)
              return "empty";
            if (C.includes(S))
              return "duplicate";
            h.s.dt.state.save();
            var A = h, y = function() {
              A.s.states.push(this), A._collectionRebuild();
            }, I = h.s.dt.state();
            if (I.stateRestore = {
              isPredefined: !1,
              state: S,
              tableId: h.s.dt.table().node().id
            }, T.saveState) {
              for (var $ = h.c.saveState, V = 0, z = Object.keys(T.saveState); V < z.length; V++) {
                var F = z[V];
                if (typeof T.saveState[F] == "object")
                  for (var P = 0, k = Object.keys(T.saveState[F]); P < k.length; P++) {
                    var R = k[P];
                    T.saveState[F][R] || ($[F][R] = !1);
                  }
                else
                  T.saveState[F] || ($[F] = !1);
              }
              h.c.saveState = $;
            }
            var L = new r(h.s.dt.settings()[0], i.extend(!0, {}, h.c, m), S, I, !1, y);
            return i(h.s.dt.table().node()).on("dtsr-modal-inserted", function() {
              L.dom.confirmation.one("dtsr-remove", function() {
                return h._removeCallback(L.s.identifier);
              }), L.dom.confirmation.one("dtsr-rename", function() {
                return h._collectionRebuild();
              }), L.dom.confirmation.one("dtsr-save", function() {
                return h._collectionRebuild();
              });
            }), !0;
          };
          if (u === null)
            if (this.c.creationModal || m !== void 0 && m.creationModal)
              this._creationModal(p, w, m);
            else {
              var v = p(w, {});
              if (v === "empty")
                throw new Error(this.s.dt.i18n("stateRestore.emptyError", this.c.i18n.emptyError));
              if (v === "duplicate")
                throw new Error(this.s.dt.i18n("stateRestore.duplicateError", this.c.i18n.duplicateError));
            }
          else
            throw new Error(this.s.dt.i18n("stateRestore.duplicateError", this.c.i18n.duplicateError));
        }
      }, b.prototype.removeAll = function(w) {
        if (this.s.states.length !== 0) {
          var C = this.s.states.map(function(h) {
            return h.s.identifier;
          }), m = C[0];
          C.length > 1 && (m = C.slice(0, -1).join(", ") + this.s.dt.i18n("stateRestore.removeJoiner", this.c.i18n.removeJoiner) + C.slice(-1)), i(this.dom.removeContents.children("span")).html(this.s.dt.i18n("stateRestore.removeConfirm", this.c.i18n.removeConfirm).replace(/%s/g, m)), this._newModal(this.dom.removeTitle, this.s.dt.i18n("stateRestore.removeSubmit", this.c.i18n.removeSubmit), w, this.dom.removeContents);
        }
      }, b.prototype.destroy = function() {
        for (var w = 0, C = this.s.states; w < C.length; w++) {
          var m = C[w];
          m.destroy();
        }
        i.each(this.dom, function(h, u) {
          u.off().remove();
        }), this.s.states = [], this.s.dt.off(".dtsr"), i(this.s.dt.table().node()).off(".dtsr");
      }, b.prototype.findActive = function() {
        this.s.dt.state.save();
        for (var w = this.s.dt.state(), C, m = this.s.dt.buttons().nodes(), h = 0, u = m; h < u.length; h++)
          C = u[h], (i(C).hasClass("dtsr-state") || i(C).children().hasClass("dtsr-state")) && this.s.dt.button(C).active(!1);
        for (var p = [], v = 0, S = this.s.states; v < S.length; v++) {
          var T = S[v];
          if (T.compare(w)) {
            p.push({
              data: T.s.savedState,
              name: T.s.identifier
            });
            for (var A = 0, y = m; A < y.length; A++) {
              C = y[A];
              var I = this.s.dt.button(C);
              if (I.text() === T.s.identifier) {
                I.active(!0);
                break;
              }
            }
          }
        }
        return p;
      }, b.prototype.getState = function(w) {
        for (var C = 0, m = this.s.states; C < m.length; C++) {
          var h = m[C];
          if (h.s.identifier === w)
            return h;
        }
        return null;
      }, b.prototype.getStates = function(w) {
        if (w === void 0)
          return this.s.states;
        for (var C = [], m = 0, h = w; m < h.length; m++) {
          for (var u = h[m], p = !1, v = 0, S = this.s.states; v < S.length; v++) {
            var T = S[v];
            if (u === T.s.identifier) {
              C.push(T), p = !0;
              break;
            }
          }
          p || C.push(void 0);
        }
        return C;
      }, b.prototype._addPreDefined = function(w) {
        for (var C = this, m = Object.keys(w).sort(function(T, A) {
          return T > A ? 1 : T < A ? -1 : 0;
        }), h = function(T) {
          for (var A = 0; A < u.s.states.length; A++)
            u.s.states[A].s.identifier === T && u.s.states.splice(A, 1);
          var y = u, I = function() {
            y.s.states.push(this), y._collectionRebuild();
          }, $ = u._fixTypes(w[T]), V = i.extend(!0, {}, u.c, $.c !== void 0 ? {
            saveState: $.c.saveState,
            remove: $.c.remove,
            rename: $.c.rename,
            save: $.c.save
          } : void 0, !0);
          u.c.createState && u.c.createState(V, $);
          var z = new r(u.s.dt, V, T, $, !0, I);
          i(u.s.dt.table().node()).on("dtsr-modal-inserted", function() {
            z.dom.confirmation.one("dtsr-remove", function() {
              return C._removeCallback(z.s.identifier);
            }), z.dom.confirmation.one("dtsr-rename", function() {
              return C._collectionRebuild();
            }), z.dom.confirmation.one("dtsr-save", function() {
              return C._collectionRebuild();
            });
          });
        }, u = this, p = 0, v = m; p < v.length; p++) {
          var S = v[p];
          h(S);
        }
      }, b.prototype._collectionRebuild = function() {
        var w = this.s.dt.button("SaveStateRestore:name"), C = [], m;
        if (w[0]) {
          var h = w.index().split("-");
          for (C = w[0].inst.c.buttons, m = 0; m < h.length; m++)
            if (C[h[m]].buttons)
              C = C[h[m]].buttons;
            else {
              C = [];
              break;
            }
        }
        for (m = 0; m < C.length; m++)
          C[m].extend === "stateRestore" && (C.splice(m, 1), m--);
        this.c._createInSaved && C.push("createState");
        var u = '<span class="' + this.classes.emptyStates + '">' + this.s.dt.i18n("stateRestore.emptyStates", this.c.i18n.emptyStates) + "</span>";
        if (this.s.states.length === 0)
          C.includes(u) || C.push(u);
        else {
          for (; C.includes(u); )
            C.splice(C.indexOf(u), 1);
          this.s.states = this.s.states.sort(function(z, F) {
            var P = z.s.identifier, k = F.s.identifier;
            return P > k ? 1 : P < k ? -1 : 0;
          });
          for (var p = 0, v = this.s.states; p < v.length; p++) {
            var S = v[p], T = this.c.splitSecondaries.slice();
            T.includes("updateState") && (!this.c.save || !S.c.save) && T.splice(T.indexOf("updateState"), 1), T.includes("renameState") && (!this.c.save || !S.c.save || !this.c.rename || !S.c.rename) && T.splice(T.indexOf("renameState"), 1), T.includes("removeState") && (!this.c.remove || !S.c.remove) && T.splice(T.indexOf("removeState"), 1);
            var A = {
              _stateRestore: S,
              attr: {
                title: S.s.identifier
              },
              config: {
                split: T
              },
              extend: "stateRestore",
              text: r.entityEncode(S.s.identifier),
              popoverTitle: r.entityEncode(S.s.identifier)
            };
            this.c.createButton && this.c.createButton(A, S.s.savedState), C.push(A);
          }
        }
        w.collectionRebuild(C);
        for (var y = this.s.dt.buttons(), I = 0, $ = y; I < $.length; I++) {
          var V = $[I];
          i(V.node).hasClass("dtsr-removeAllStates") && (this.s.states.length === 0 ? this.s.dt.button(V.node).disable() : this.s.dt.button(V.node).enable());
        }
      }, b.prototype._creationModal = function(w, C, m) {
        var h = this;
        this.dom.creation.empty(), this.dom.creationForm.empty(), this.dom.nameInputRow.find("input").val(C), this.dom.creationForm.append(this.dom.nameInputRow);
        var u = this.s.dt.settings()[0].oInit, p, v = [], S = m !== void 0 && m.toggle !== void 0;
        ((!S || m.toggle.order === void 0) && this.c.toggle.order || S && m.toggle.order) && this.c.saveState.order && (u.ordering === void 0 || u.ordering) && v.push(this.dom.orderToggle), ((!S || m.toggle.search === void 0) && this.c.toggle.search || S && m.toggle.search) && this.c.saveState.search && (u.searching === void 0 || u.searching) && v.push(this.dom.searchToggle), ((!S || m.toggle.paging === void 0) && this.c.toggle.paging || S && m.toggle.paging) && this.c.saveState.paging && (u.paging === void 0 || u.paging) && v.push(this.dom.pagingToggle), ((!S || m.toggle.length === void 0) && this.c.toggle.length || S && m.toggle.length) && this.c.saveState.length && (u.length === void 0 || u.length) && v.push(this.dom.lengthToggle), this.s.hasColReorder && ((!S || m.toggle.colReorder === void 0) && this.c.toggle.colReorder || S && m.toggle.colReorder) && this.c.saveState.colReorder && v.push(this.dom.colReorderToggle), this.s.hasScroller && ((!S || m.toggle.scroller === void 0) && this.c.toggle.scroller || S && m.toggle.scroller) && this.c.saveState.scroller && v.push(this.dom.scrollerToggle), this.s.hasSearchBuilder && ((!S || m.toggle.searchBuilder === void 0) && this.c.toggle.searchBuilder || S && m.toggle.searchBuilder) && this.c.saveState.searchBuilder && v.push(this.dom.searchBuilderToggle), this.s.hasSearchPanes && ((!S || m.toggle.searchPanes === void 0) && this.c.toggle.searchPanes || S && m.toggle.searchPanes) && this.c.saveState.searchPanes && v.push(this.dom.searchPanesToggle), this.s.hasSelect && ((!S || m.toggle.select === void 0) && this.c.toggle.select || S && m.toggle.select) && this.c.saveState.select && v.push(this.dom.selectToggle), typeof this.c.toggle.columns == "boolean" && ((!S || m.toggle.order === void 0) && this.c.toggle.columns || S && m.toggle.order) && this.c.saveState.columns ? (v.push(this.dom.columnsSearchToggle), v.push(this.dom.columnsVisibleToggle)) : ((!S || m.toggle.columns === void 0) && typeof this.c.toggle.columns != "boolean" || typeof m.toggle.order != "boolean") && (typeof this.c.saveState.columns != "boolean" && this.c.saveState.columns ? (
          // columns.search is defined when passed in
          ((S && m.toggle.columns !== void 0 && typeof m.toggle.columns != "boolean" && m.toggle.columns.search || // Columns search is not defined when passed in but is in defaults
          (!S || m.toggle.columns === void 0 || typeof m.toggle.columns != "boolean" && m.toggle.columns.search === void 0) && typeof this.c.toggle.columns != "boolean" && this.c.toggle.columns.search) && this.c.saveState.columns.search && v.push(this.dom.columnsSearchToggle), // columns.visible is defined when passed in
          (S && m.toggle.columns !== void 0 && typeof m.toggle.columns != "boolean" && m.toggle.columns.visible || // Columns visible is not defined when passed in but is in defaults
          (!S || m.toggle.columns === void 0 || typeof m.toggle.columns != "boolean" && m.toggle.columns.visible === void 0) && typeof this.c.toggle.columns != "boolean" && this.c.toggle.columns.visible) && this.c.saveState.columns.visible && v.push(this.dom.columnsVisibleToggle))
        ) : this.c.saveState.columns && (v.push(this.dom.columnsSearchToggle), v.push(this.dom.columnsVisibleToggle))), v.sort(function(k, R) {
          var L = k.text(), E = R.text();
          return L < E ? -1 : L > E ? 1 : 0;
        });
        for (var T = this.dom.checkboxInputRow.css("display", v.length ? "block" : "none").appendTo(this.dom.creationForm).find("div.dtsr-input").empty(), A = 0, y = v; A < y.length; A++)
          p = y[A], T.append(p);
        this.dom.background.appendTo(this.dom.dtContainer), this.dom.creation.append(this.dom.creationTitle).append(this.dom.creationForm).append(this.dom.createButtonRow).appendTo(this.dom.dtContainer), i(this.s.dt.table().node()).trigger("dtsr-modal-inserted");
        for (var I = 0, $ = v; I < $.length; I++)
          p = $[I], i(p.children("label:last-child")).on("click", function() {
            p.children("input").prop("checked", !p.children("input").prop("checked"));
          });
        var V = i("button." + this.classes.creationButton.replace(/ /g, ".")), z = this.dom.creationForm.find("input");
        z.length > 0 ? i(z[0]).focus() : V.focus();
        var F = i("div." + this.classes.background.replace(/ /g, ".")), P = function(k) {
          k.key === "Enter" ? V.click() : k.key === "Escape" && F.click();
        };
        this.c.modalCloseButton && (this.dom.creation.append(this.dom.closeButton), this.dom.closeButton.on("click", function() {
          return F.click();
        })), V.on("click", function() {
          var k = {
            colReorder: h.dom.colReorderToggle.find("input").is(":checked"),
            columns: {
              search: h.dom.columnsSearchToggle.find("input").is(":checked"),
              visible: h.dom.columnsVisibleToggle.find("input").is(":checked")
            },
            length: h.dom.lengthToggle.find("input").is(":checked"),
            order: h.dom.orderToggle.find("input").is(":checked"),
            paging: h.dom.pagingToggle.find("input").is(":checked"),
            scroller: h.dom.scrollerToggle.find("input").is(":checked"),
            search: h.dom.searchToggle.find("input").is(":checked"),
            searchBuilder: h.dom.searchBuilderToggle.find("input").is(":checked"),
            searchPanes: h.dom.searchPanesToggle.find("input").is(":checked"),
            select: h.dom.selectToggle.find("input").is(":checked")
          }, R = w(i("input." + h.classes.nameInput.replace(/ /g, ".")).val(), { saveState: k });
          R === !0 ? (h.dom.background.remove(), h.dom.creation.remove(), i(document).unbind("keyup", P)) : (h.dom.creation.children("." + h.classes.modalError).remove(), h.dom.creation.append(h.dom[R + "Error"]));
        }), F.one("click", function() {
          h.dom.background.remove(), h.dom.creation.remove(), i(document).unbind("keyup", P), h._collectionRebuild();
        }), i(document).on("keyup", P), this.s.dt.state.save();
      }, b.prototype._fixTypes = function(w) {
        var C, m = function(u, p) {
          var v = u[p];
          v !== void 0 && (u[p] = typeof v == "number" ? v : parseInt(v));
        }, h = function(u, p) {
          var v = u[p];
          v !== void 0 && (u[p] = typeof v != "string" ? v : v === "true");
        };
        if (m(w, "start"), m(w, "length"), m(w, "time"), w.order)
          for (C = 0; C < w.order.length; C++)
            m(w.order[C], 0);
        if (w.search && (h(w.search, "caseInsensitive"), h(w.search, "regex"), h(w.search, "smart"), h(w.search, "visible"), h(w.search, "return")), w.columns)
          for (C = 0; C < w.columns.length; C++)
            h(w.columns[C], "caseInsensitive"), h(w.columns[C], "regex"), h(w.columns[C], "smart"), h(w.columns[C], "visible");
        if (w.colReorder)
          for (C = 0; C < w.colReorder.length; C++)
            m(w.colReorder, C);
        return w;
      }, b.prototype._removeCallback = function(w) {
        for (var C = 0; C < this.s.states.length; C++)
          this.s.states[C].s.identifier === w && (this.s.states.splice(C, 1), C--);
        return this._collectionRebuild(), !0;
      }, b.prototype._newModal = function(w, C, m, h) {
        var u = this;
        this.dom.background.appendTo(this.dom.dtContainer), this.dom.confirmationTitleRow.empty().append(w);
        var p = i('<button class="' + this.classes.confirmationButton + " " + this.classes.dtButton + '">' + C + "</button>");
        this.dom.confirmation.empty().append(this.dom.confirmationTitleRow).append(h).append(i('<div class="' + this.classes.confirmationButtons + '"></div>').append(p)).appendTo(this.dom.dtContainer), i(this.s.dt.table().node()).trigger("dtsr-modal-inserted");
        var v = h.children("input");
        v.length > 0 ? i(v[0]).focus() : p.focus();
        var S = i("div." + this.classes.background.replace(/ /g, ".")), T = function(A) {
          A.key === "Enter" ? p.click() : A.key === "Escape" && S.click();
        };
        p.on("click", function() {
          var A = m(!0);
          A === !0 ? (u.dom.background.remove(), u.dom.confirmation.remove(), i(document).unbind("keyup", T), p.off("click")) : (u.dom.confirmation.children("." + u.classes.modalError).remove(), u.dom.confirmation.append(u.dom[A + "Error"]));
        }), this.dom.confirmation.on("click", function(A) {
          A.stopPropagation();
        }), S.one("click", function() {
          u.dom.background.remove(), u.dom.confirmation.remove(), i(document).unbind("keyup", T);
        }), i(document).on("keyup", T);
      }, b.prototype._searchForStates = function() {
        for (var w = this, C = Object.keys(localStorage), m = function(S) {
          if (S.startsWith("DataTables_stateRestore_") && (S.endsWith(location.pathname) || S.endsWith(location.pathname + "_" + h.s.dt.table().node().id))) {
            var T = JSON.parse(localStorage.getItem(S));
            if (T.stateRestore.isPreDefined || T.stateRestore.tableId && T.stateRestore.tableId !== h.s.dt.table().node().id)
              return "continue";
            var A = h, y = function() {
              this.s.savedState = T, A.s.states.push(this), A._collectionRebuild();
            }, I = new r(h.s.dt, i.extend(!0, {}, h.c, { saveState: T.c.saveState }), T.stateRestore.state, T, !1, y);
            i(h.s.dt.table().node()).on("dtsr-modal-inserted", function() {
              I.dom.confirmation.one("dtsr-remove", function() {
                return w._removeCallback(I.s.identifier);
              }), I.dom.confirmation.one("dtsr-rename", function() {
                return w._collectionRebuild();
              }), I.dom.confirmation.one("dtsr-save", function() {
                return w._collectionRebuild();
              });
            });
          }
        }, h = this, u = 0, p = C; u < p.length; u++) {
          var v = p[u];
          m(v);
        }
      }, b.version = "1.0.0", b.classes = {
        background: "dtsr-background",
        checkBox: "dtsr-check-box",
        checkLabel: "dtsr-check-label",
        checkRow: "dtsr-check-row",
        closeButton: "dtsr-popover-close",
        colReorderToggle: "dtsr-colReorder-toggle",
        columnsSearchToggle: "dtsr-columns-search-toggle",
        columnsVisibleToggle: "dtsr-columns-visible-toggle",
        confirmation: "dtsr-confirmation",
        confirmationButton: "dtsr-confirmation-button",
        confirmationButtons: "dtsr-confirmation-buttons",
        confirmationMessage: "dtsr-confirmation-message dtsr-name-label",
        confirmationText: "dtsr-confirmation-text",
        confirmationTitle: "dtsr-confirmation-title",
        confirmationTitleRow: "dtsr-confirmation-title-row",
        creation: "dtsr-creation",
        creationButton: "dtsr-creation-button",
        creationForm: "dtsr-creation-form",
        creationText: "dtsr-creation-text",
        creationTitle: "dtsr-creation-title",
        dtButton: "dt-button",
        emptyStates: "dtsr-emptyStates",
        formRow: "dtsr-form-row",
        leftSide: "dtsr-left",
        lengthToggle: "dtsr-length-toggle",
        modalError: "dtsr-modal-error",
        modalFoot: "dtsr-modal-foot",
        nameInput: "dtsr-name-input",
        nameLabel: "dtsr-name-label",
        orderToggle: "dtsr-order-toggle",
        pagingToggle: "dtsr-paging-toggle",
        rightSide: "dtsr-right",
        scrollerToggle: "dtsr-scroller-toggle",
        searchBuilderToggle: "dtsr-searchBuilder-toggle",
        searchPanesToggle: "dtsr-searchPanes-toggle",
        searchToggle: "dtsr-search-toggle",
        selectToggle: "dtsr-select-toggle",
        toggleLabel: "dtsr-toggle-title"
      }, b.defaults = {
        _createInSaved: !1,
        ajax: !1,
        create: !0,
        creationModal: !1,
        i18n: {
          creationModal: {
            button: "Create",
            colReorder: "Column Order",
            columns: {
              search: "Column Search",
              visible: "Column Visibility"
            },
            length: "Page Length",
            name: "Name:",
            order: "Sorting",
            paging: "Paging",
            scroller: "Scroll Position",
            search: "Search",
            searchBuilder: "SearchBuilder",
            searchPanes: "SearchPanes",
            select: "Select",
            title: "Create New State",
            toggleLabel: "Include:"
          },
          duplicateError: "A state with this name already exists.",
          emptyError: "Name cannot be empty.",
          emptyStates: "No saved states",
          removeConfirm: "Are you sure you want to remove %s?",
          removeError: "Failed to remove state.",
          removeJoiner: " and ",
          removeSubmit: "Remove",
          removeTitle: "Remove State",
          renameButton: "Rename",
          renameLabel: "New Name for %s:",
          renameTitle: "Rename State"
        },
        modalCloseButton: !0,
        preDefined: {},
        remove: !0,
        rename: !0,
        save: !0,
        saveState: {
          colReorder: !0,
          columns: {
            search: !0,
            visible: !0
          },
          length: !0,
          order: !0,
          paging: !0,
          scroller: !0,
          search: !0,
          searchBuilder: !0,
          searchPanes: !0,
          select: !0
        },
        splitSecondaries: [
          "updateState",
          "renameState",
          "removeState"
        ],
        toggle: {
          colReorder: !1,
          columns: {
            search: !1,
            visible: !1
          },
          length: !1,
          order: !1,
          paging: !1,
          scroller: !1,
          search: !1,
          searchBuilder: !1,
          searchPanes: !1,
          select: !1
        },
        createButton: null,
        createState: null
      }, b;
    }()
  );
  /*! StateRestore 1.4.3
   * © SpryMedia Ltd - datatables.net/license
   */
  e(Sn), a(Sn), Sn.fn.dataTable.StateRestore = r, Sn.fn.DataTable.StateRestore = r, Sn.fn.dataTable.StateRestoreCollection = o, Sn.fn.DataTable.StateRestoreCollection = o;
  var l = H.Api.register;
  l("stateRestore()", function() {
    return this;
  }), l("stateRestore.state()", function(b) {
    var w = this.context[0];
    if (!w._stateRestore) {
      var C = H.Api(w), m = new H.StateRestoreCollection(C, {});
      g(C, m);
    }
    return this[0] = w._stateRestore.getState(b), this;
  }), l("stateRestore.state.add()", function(b, w) {
    var C = this.context[0];
    if (!C._stateRestore) {
      var m = H.Api(C), h = new H.StateRestoreCollection(m, {});
      g(m, h);
    }
    if (!C._stateRestore.c.create)
      return this;
    if (C._stateRestore.addState) {
      for (var u = C._stateRestore.s.states, p = [], v = 0, S = u; v < S.length; v++) {
        var T = S[v];
        p.push(T.s.identifier);
      }
      return C._stateRestore.addState(b, p, w), this;
    }
  }), l("stateRestore.states()", function(b) {
    var w = this.context[0];
    if (!w._stateRestore) {
      var C = H.Api(w), m = new H.StateRestoreCollection(C, {});
      g(C, m);
    }
    return this.length = 0, this.push.apply(this, w._stateRestore.getStates(b)), this;
  }), l("stateRestore.state().save()", function() {
    var b = this[0];
    return b.c.save && b.save(), this;
  }), l("stateRestore.state().rename()", function(b) {
    var w = this.context[0], C = this[0];
    if (C.c.save) {
      for (var m = w._stateRestore.s.states, h = [], u = 0, p = m; u < p.length; u++) {
        var v = p[u];
        h.push(v.s.identifier);
      }
      C.rename(b, h);
    }
    return this;
  }), l("stateRestore.state().load()", function() {
    var b = this[0];
    return b.load(), this;
  }), l("stateRestore.state().remove()", function(b) {
    var w = this[0];
    return w.c.remove && w.remove(b), this;
  }), l("stateRestore.states().remove()", function(b) {
    var w = this, C = function(m) {
      for (var h = !0, u = w.toArray(); u.length > 0; ) {
        var p = u[0];
        if (p !== void 0 && p.c.remove) {
          var v = p.remove(m);
          v !== !0 ? h = v : u.splice(0, 1);
        } else
          break;
      }
      return h;
    };
    return this.context[0]._stateRestore && this.context[0]._stateRestore.c.remove && (b ? C(b) : this.context[0]._stateRestore.removeAll(C)), this;
  }), l("stateRestore.activeStates()", function() {
    var b = this.context[0];
    if (this.length = 0, !b._stateRestore) {
      var w = H.Api(b), C = new H.StateRestoreCollection(w, {});
      g(w, C);
    }
    return b._stateRestore && this.push.apply(this, b._stateRestore.findActive()), this;
  }), H.ext.buttons.stateRestore = {
    action: function(b, w, C, m) {
      m._stateRestore.load(), C.blur();
    },
    className: "dtsr-state",
    config: {
      split: ["updateState", "renameState", "removeState"]
    },
    text: function(b) {
      return b.i18n("buttons.stateRestore", "State %d", b.stateRestore.states()[0].length + 1);
    }
  }, H.ext.buttons.updateState = {
    action: function(b, w, C, m) {
      Sn("div.dt-button-background").click(), m.parent._stateRestore.save();
    },
    text: function(b) {
      return b.i18n("buttons.updateState", "Update");
    }
  }, H.ext.buttons.savedStates = {
    buttons: [],
    extend: "collection",
    init: function(b, w, C) {
      b.on("stateRestore-change", function() {
        b.button(w).text(b.i18n("buttons.savedStates", "Saved States", b.stateRestore.states().length));
      }), b.settings()[0]._stateRestore === void 0 && f(b, C);
    },
    name: "SaveStateRestore",
    text: function(b) {
      return b.i18n("buttons.savedStates", "Saved States", 0);
    }
  }, H.ext.buttons.savedStatesCreate = {
    buttons: [],
    extend: "collection",
    init: function(b, w, C) {
      b.on("stateRestore-change", function() {
        b.button(w).text(b.i18n("buttons.savedStates", "Saved States", b.stateRestore.states().length));
      }), b.settings()[0]._stateRestore === void 0 && (C.config === void 0 && (C.config = {}), C.config._createInSaved = !0, f(b, C));
    },
    name: "SaveStateRestore",
    text: function(b) {
      return b.i18n("buttons.savedStates", "Saved States", 0);
    }
  }, H.ext.buttons.createState = {
    action: function(b, w, C, m) {
      b.stopPropagation();
      var h = w.settings()[0]._stateRestore.c, u = w.settings()[0].oLanguage;
      if (!(!h.create || !h.save)) {
        var p = w.stateRestore.states().toArray(), v = u.buttons !== void 0 && u.buttons.stateRestore !== void 0 ? u.buttons.stateRestore : "State ", S;
        if (v.indexOf("%d") === v.length - 3)
          S = new RegExp(v.replace(/%d/g, ""));
        else {
          var T = v.split("%d");
          S = [];
          for (var A = 0, y = T; A < y.length; A++) {
            var I = y[A];
            S.push(new RegExp(I));
          }
        }
        var $ = function(ie) {
          var oe;
          if (Array.isArray(S)) {
            oe = ie;
            for (var ce = 0, he = S; ce < he.length; ce++) {
              var we = he[ce];
              oe = oe.replace(we, "");
            }
          } else
            oe = ie.replace(S, "");
          return isNaN(+oe) || oe.length === ie ? 0 : +oe;
        }, V = p.map(function(ie) {
          return $(ie.s.identifier);
        }).sort(function(ie, oe) {
          return +ie < +oe ? 1 : +ie > +oe ? -1 : 0;
        }), z = V[0];
        w.stateRestore.state.add(w.i18n("buttons.stateRestore", "State %d", z !== void 0 ? z + 1 : 1), m.config);
        for (var F = w.stateRestore.states().sort(function(ie, oe) {
          var ce = +$(ie.s.identifier), he = +$(oe.s.identifier);
          return ce > he ? 1 : ce < he ? -1 : 0;
        }), P = w.button("SaveStateRestore:name"), k = parseInt(P.index()), R = P[0] !== void 0 && P[0].inst.c.buttons[k].buttons !== void 0 ? P[0].inst.c.buttons[k].buttons : [], L = 0; L < R.length; L++)
          R[L].extend === "stateRestore" && (R.splice(L, 1), L--);
        h._createInSaved && R.push("createState");
        for (var E = 0, M = F; E < M.length; E++) {
          var W = M[E], Y = h.splitSecondaries.slice();
          Y.includes("updateState") && !h.save && Y.splice(Y.indexOf("updateState"), 1), Y.includes("renameState") && (!h.save || !h.rename) && Y.splice(Y.indexOf("renameState"), 1), Y.includes("removeState") && !h.remove && Y.splice(Y.indexOf("removeState"), 1), R.push({
            _stateRestore: W,
            attr: {
              title: W.s.identifier
            },
            config: {
              split: Y
            },
            extend: "stateRestore",
            text: r.entityEncode(W.s.identifier),
            popoverTitle: r.entityEncode(W.s.identifier)
          });
        }
        w.button("SaveStateRestore:name").collectionRebuild(R), C.blur();
        for (var J = w.buttons(), X = 0, Q = J; X < Q.length; X++) {
          var ee = Q[X];
          Sn(ee.node).hasClass("dtsr-removeAllStates") && (F.length === 0 ? w.button(ee.node).disable() : w.button(ee.node).enable());
        }
      }
    },
    init: function(b, w, C) {
      b.settings()[0]._stateRestore === void 0 && b.button("SaveStateRestore:name").length > 1 && f(b, C);
    },
    text: function(b) {
      return b.i18n("buttons.createState", "Create State");
    }
  }, H.ext.buttons.removeState = {
    action: function(b, w, C, m) {
      m.parent._stateRestore.remove(), C.blur();
    },
    text: function(b) {
      return b.i18n("buttons.removeState", "Remove");
    }
  }, H.ext.buttons.removeAllStates = {
    action: function(b, w, C) {
      w.stateRestore.states().remove(!0), C.blur();
    },
    className: "dt-button dtsr-removeAllStates",
    init: function(b, w) {
      (!b.settings()[0]._stateRestore || b.stateRestore.states().length === 0) && Sn(w).addClass("disabled");
    },
    text: function(b) {
      return b.i18n("buttons.removeAllStates", "Remove All States");
    }
  }, H.ext.buttons.renameState = {
    action: function(b, w, C, m) {
      for (var h = w.settings()[0]._stateRestore.s.states, u = [], p = 0, v = h; p < v.length; p++) {
        var S = v[p];
        u.push(S.s.identifier);
      }
      m.parent._stateRestore.rename(void 0, u), C.blur();
    },
    text: function(b) {
      return b.i18n("buttons.renameState", "Rename");
    }
  };
  function c(b, w) {
    w === void 0 && (w = null);
    var C = new H.Api(b), m = w || C.init().stateRestore || H.defaults.stateRestore, h = new o(C, m);
    return g(C, h), h;
  }
  function f(b, w) {
    var C = new H.StateRestoreCollection(b, w.config);
    g(b, C);
  }
  function g(b, w) {
    var C = b.stateRestore.states(), m = b.button("SaveStateRestore:name"), h = [], u;
    if (m[0]) {
      var p = m.index().split("-");
      for (h = m[0].inst.c.buttons, u = 0; u < p.length; u++)
        if (h[p[u]].buttons)
          h = h[p[u]].buttons;
        else {
          h = [];
          break;
        }
    }
    var v = b.settings()[0]._stateRestore.c;
    for (u = 0; u < h.length; u++)
      h[u].extend === "stateRestore" && (h.splice(u, 1), u--);
    if (v._createInSaved && h.push("createState"), C === void 0 || C.length === 0)
      h.push('<span class="' + w.classes.emptyStates + '">' + b.i18n("stateRestore.emptyStates", w.c.i18n.emptyStates) + "</span>");
    else
      for (var S = 0, T = C; S < T.length; S++) {
        var A = T[S], y = v.splitSecondaries.slice();
        y.includes("updateState") && !v.save && y.splice(y.indexOf("updateState"), 1), y.includes("renameState") && (!v.save || !v.rename) && y.splice(y.indexOf("renameState"), 1), y.includes("removeState") && !v.remove && y.splice(y.indexOf("removeState"), 1), h.push({
          _stateRestore: A,
          attr: {
            title: A.s.identifier
          },
          config: {
            split: y
          },
          extend: "stateRestore",
          text: r.entityEncode(A.s.identifier),
          popoverTitle: r.entityEncode(A.s.identifier)
        });
      }
    b.button("SaveStateRestore:name").collectionRebuild(h);
    for (var I = b.buttons(), $ = 0, V = I; $ < V.length; $++) {
      var z = V[$];
      Sn(z.node).hasClass("dtsr-removeAllStates") && (C.length === 0 ? b.button(z.node).disable() : b.button(z.node).enable());
    }
  }
  Sn(document).on("preInit.dt.dtsr", function(b, w) {
    b.namespace === "dt" && (w.oInit.stateRestore || H.defaults.stateRestore) && (w._stateRestore || c(w, null));
  });
})();
/*! Bootstrap integration for DataTables' StateRestore
 * © SpryMedia Ltd - datatables.net/license
 */
let yf = lt;
yf.extend(!0, H.StateRestoreCollection.classes, {
  checkBox: "dtsr-check-box form-check-input",
  checkLabel: "dtsr-check-label form-check-label",
  checkRow: "dtsr-check-row form-check",
  creationButton: "dtsr-creation-button btn btn-secondary",
  creationForm: "dtsr-creation-form modal-body",
  creationText: "dtsr-creation-text modal-header",
  creationTitle: "dtsr-creation-title modal-title",
  nameInput: "dtsr-name-input form-control",
  nameLabel: "dtsr-name-label form-label"
});
yf.extend(!0, H.StateRestore.classes, {
  confirmationButton: "dtsr-confirmation-button btn btn-secondary",
  input: "dtsr-input form-control"
});
var Ft = "top", on = "bottom", ln = "right", Bt = "left", Za = "auto", Mi = [Ft, on, ln, Bt], Lr = "start", Ti = "end", wf = "clippingParents", su = "viewport", di = "popper", _f = "reference", _l = /* @__PURE__ */ Mi.reduce(function(t, n) {
  return t.concat([n + "-" + Lr, n + "-" + Ti]);
}, []), au = /* @__PURE__ */ [].concat(Mi, [Za]).reduce(function(t, n) {
  return t.concat([n, n + "-" + Lr, n + "-" + Ti]);
}, []), Cf = "beforeRead", xf = "read", Sf = "afterRead", Tf = "beforeMain", Df = "main", Ef = "afterMain", Af = "beforeWrite", kf = "write", Pf = "afterWrite", Of = [Cf, xf, Sf, Tf, Df, Ef, Af, kf, Pf];
function Mn(t) {
  return t ? (t.nodeName || "").toLowerCase() : null;
}
function un(t) {
  if (t == null)
    return window;
  if (t.toString() !== "[object Window]") {
    var n = t.ownerDocument;
    return n && n.defaultView || window;
  }
  return t;
}
function Mr(t) {
  var n = un(t).Element;
  return t instanceof n || t instanceof Element;
}
function vn(t) {
  var n = un(t).HTMLElement;
  return t instanceof n || t instanceof HTMLElement;
}
function ou(t) {
  if (typeof ShadowRoot > "u")
    return !1;
  var n = un(t).ShadowRoot;
  return t instanceof n || t instanceof ShadowRoot;
}
function Ug(t) {
  var n = t.state;
  Object.keys(n.elements).forEach(function(e) {
    var r = n.styles[e] || {}, i = n.attributes[e] || {}, s = n.elements[e];
    !vn(s) || !Mn(s) || (Object.assign(s.style, r), Object.keys(i).forEach(function(a) {
      var o = i[a];
      o === !1 ? s.removeAttribute(a) : s.setAttribute(a, o === !0 ? "" : o);
    }));
  });
}
function Kg(t) {
  var n = t.state, e = {
    popper: {
      position: n.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  return Object.assign(n.elements.popper.style, e.popper), n.styles = e, n.elements.arrow && Object.assign(n.elements.arrow.style, e.arrow), function() {
    Object.keys(n.elements).forEach(function(r) {
      var i = n.elements[r], s = n.attributes[r] || {}, a = Object.keys(n.styles.hasOwnProperty(r) ? n.styles[r] : e[r]), o = a.reduce(function(l, c) {
        return l[c] = "", l;
      }, {});
      !vn(i) || !Mn(i) || (Object.assign(i.style, o), Object.keys(s).forEach(function(l) {
        i.removeAttribute(l);
      }));
    });
  };
}
const lu = {
  name: "applyStyles",
  enabled: !0,
  phase: "write",
  fn: Ug,
  effect: Kg,
  requires: ["computeStyles"]
};
function In(t) {
  return t.split("-")[0];
}
var Ir = Math.max, ka = Math.min, Di = Math.round;
function Cl() {
  var t = navigator.userAgentData;
  return t != null && t.brands && Array.isArray(t.brands) ? t.brands.map(function(n) {
    return n.brand + "/" + n.version;
  }).join(" ") : navigator.userAgent;
}
function If() {
  return !/^((?!chrome|android).)*safari/i.test(Cl());
}
function Ei(t, n, e) {
  n === void 0 && (n = !1), e === void 0 && (e = !1);
  var r = t.getBoundingClientRect(), i = 1, s = 1;
  n && vn(t) && (i = t.offsetWidth > 0 && Di(r.width) / t.offsetWidth || 1, s = t.offsetHeight > 0 && Di(r.height) / t.offsetHeight || 1);
  var a = Mr(t) ? un(t) : window, o = a.visualViewport, l = !If() && e, c = (r.left + (l && o ? o.offsetLeft : 0)) / i, f = (r.top + (l && o ? o.offsetTop : 0)) / s, g = r.width / i, b = r.height / s;
  return {
    width: g,
    height: b,
    top: f,
    right: c + g,
    bottom: f + b,
    left: c,
    x: c,
    y: f
  };
}
function uu(t) {
  var n = Ei(t), e = t.offsetWidth, r = t.offsetHeight;
  return Math.abs(n.width - e) <= 1 && (e = n.width), Math.abs(n.height - r) <= 1 && (r = n.height), {
    x: t.offsetLeft,
    y: t.offsetTop,
    width: e,
    height: r
  };
}
function Nf(t, n) {
  var e = n.getRootNode && n.getRootNode();
  if (t.contains(n))
    return !0;
  if (e && ou(e)) {
    var r = n;
    do {
      if (r && t.isSameNode(r))
        return !0;
      r = r.parentNode || r.host;
    } while (r);
  }
  return !1;
}
function zn(t) {
  return un(t).getComputedStyle(t);
}
function Xg(t) {
  return ["table", "td", "th"].indexOf(Mn(t)) >= 0;
}
function fr(t) {
  return ((Mr(t) ? t.ownerDocument : (
    // $FlowFixMe[prop-missing]
    t.document
  )) || window.document).documentElement;
}
function eo(t) {
  return Mn(t) === "html" ? t : (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    t.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    t.parentNode || // DOM Element detected
    (ou(t) ? t.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    fr(t)
  );
}
function Mc(t) {
  return !vn(t) || // https://github.com/popperjs/popper-core/issues/837
  zn(t).position === "fixed" ? null : t.offsetParent;
}
function Qg(t) {
  var n = /firefox/i.test(Cl()), e = /Trident/i.test(Cl());
  if (e && vn(t)) {
    var r = zn(t);
    if (r.position === "fixed")
      return null;
  }
  var i = eo(t);
  for (ou(i) && (i = i.host); vn(i) && ["html", "body"].indexOf(Mn(i)) < 0; ) {
    var s = zn(i);
    if (s.transform !== "none" || s.perspective !== "none" || s.contain === "paint" || ["transform", "perspective"].indexOf(s.willChange) !== -1 || n && s.willChange === "filter" || n && s.filter && s.filter !== "none")
      return i;
    i = i.parentNode;
  }
  return null;
}
function xs(t) {
  for (var n = un(t), e = Mc(t); e && Xg(e) && zn(e).position === "static"; )
    e = Mc(e);
  return e && (Mn(e) === "html" || Mn(e) === "body" && zn(e).position === "static") ? n : e || Qg(t) || n;
}
function cu(t) {
  return ["top", "bottom"].indexOf(t) >= 0 ? "x" : "y";
}
function ss(t, n, e) {
  return Ir(t, ka(n, e));
}
function Jg(t, n, e) {
  var r = ss(t, n, e);
  return r > e ? e : r;
}
function Rf() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}
function Lf(t) {
  return Object.assign({}, Rf(), t);
}
function Mf(t, n) {
  return n.reduce(function(e, r) {
    return e[r] = t, e;
  }, {});
}
var Zg = function(n, e) {
  return n = typeof n == "function" ? n(Object.assign({}, e.rects, {
    placement: e.placement
  })) : n, Lf(typeof n != "number" ? n : Mf(n, Mi));
};
function em(t) {
  var n, e = t.state, r = t.name, i = t.options, s = e.elements.arrow, a = e.modifiersData.popperOffsets, o = In(e.placement), l = cu(o), c = [Bt, ln].indexOf(o) >= 0, f = c ? "height" : "width";
  if (!(!s || !a)) {
    var g = Zg(i.padding, e), b = uu(s), w = l === "y" ? Ft : Bt, C = l === "y" ? on : ln, m = e.rects.reference[f] + e.rects.reference[l] - a[l] - e.rects.popper[f], h = a[l] - e.rects.reference[l], u = xs(s), p = u ? l === "y" ? u.clientHeight || 0 : u.clientWidth || 0 : 0, v = m / 2 - h / 2, S = g[w], T = p - b[f] - g[C], A = p / 2 - b[f] / 2 + v, y = ss(S, A, T), I = l;
    e.modifiersData[r] = (n = {}, n[I] = y, n.centerOffset = y - A, n);
  }
}
function tm(t) {
  var n = t.state, e = t.options, r = e.element, i = r === void 0 ? "[data-popper-arrow]" : r;
  i != null && (typeof i == "string" && (i = n.elements.popper.querySelector(i), !i) || Nf(n.elements.popper, i) && (n.elements.arrow = i));
}
const Ff = {
  name: "arrow",
  enabled: !0,
  phase: "main",
  fn: em,
  effect: tm,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};
function Ai(t) {
  return t.split("-")[1];
}
var nm = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function rm(t, n) {
  var e = t.x, r = t.y, i = n.devicePixelRatio || 1;
  return {
    x: Di(e * i) / i || 0,
    y: Di(r * i) / i || 0
  };
}
function Fc(t) {
  var n, e = t.popper, r = t.popperRect, i = t.placement, s = t.variation, a = t.offsets, o = t.position, l = t.gpuAcceleration, c = t.adaptive, f = t.roundOffsets, g = t.isFixed, b = a.x, w = b === void 0 ? 0 : b, C = a.y, m = C === void 0 ? 0 : C, h = typeof f == "function" ? f({
    x: w,
    y: m
  }) : {
    x: w,
    y: m
  };
  w = h.x, m = h.y;
  var u = a.hasOwnProperty("x"), p = a.hasOwnProperty("y"), v = Bt, S = Ft, T = window;
  if (c) {
    var A = xs(e), y = "clientHeight", I = "clientWidth";
    if (A === un(e) && (A = fr(e), zn(A).position !== "static" && o === "absolute" && (y = "scrollHeight", I = "scrollWidth")), A = A, i === Ft || (i === Bt || i === ln) && s === Ti) {
      S = on;
      var $ = g && A === T && T.visualViewport ? T.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        A[y]
      );
      m -= $ - r.height, m *= l ? 1 : -1;
    }
    if (i === Bt || (i === Ft || i === on) && s === Ti) {
      v = ln;
      var V = g && A === T && T.visualViewport ? T.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        A[I]
      );
      w -= V - r.width, w *= l ? 1 : -1;
    }
  }
  var z = Object.assign({
    position: o
  }, c && nm), F = f === !0 ? rm({
    x: w,
    y: m
  }, un(e)) : {
    x: w,
    y: m
  };
  if (w = F.x, m = F.y, l) {
    var P;
    return Object.assign({}, z, (P = {}, P[S] = p ? "0" : "", P[v] = u ? "0" : "", P.transform = (T.devicePixelRatio || 1) <= 1 ? "translate(" + w + "px, " + m + "px)" : "translate3d(" + w + "px, " + m + "px, 0)", P));
  }
  return Object.assign({}, z, (n = {}, n[S] = p ? m + "px" : "", n[v] = u ? w + "px" : "", n.transform = "", n));
}
function im(t) {
  var n = t.state, e = t.options, r = e.gpuAcceleration, i = r === void 0 ? !0 : r, s = e.adaptive, a = s === void 0 ? !0 : s, o = e.roundOffsets, l = o === void 0 ? !0 : o, c = {
    placement: In(n.placement),
    variation: Ai(n.placement),
    popper: n.elements.popper,
    popperRect: n.rects.popper,
    gpuAcceleration: i,
    isFixed: n.options.strategy === "fixed"
  };
  n.modifiersData.popperOffsets != null && (n.styles.popper = Object.assign({}, n.styles.popper, Fc(Object.assign({}, c, {
    offsets: n.modifiersData.popperOffsets,
    position: n.options.strategy,
    adaptive: a,
    roundOffsets: l
  })))), n.modifiersData.arrow != null && (n.styles.arrow = Object.assign({}, n.styles.arrow, Fc(Object.assign({}, c, {
    offsets: n.modifiersData.arrow,
    position: "absolute",
    adaptive: !1,
    roundOffsets: l
  })))), n.attributes.popper = Object.assign({}, n.attributes.popper, {
    "data-popper-placement": n.placement
  });
}
const du = {
  name: "computeStyles",
  enabled: !0,
  phase: "beforeWrite",
  fn: im,
  data: {}
};
var Js = {
  passive: !0
};
function sm(t) {
  var n = t.state, e = t.instance, r = t.options, i = r.scroll, s = i === void 0 ? !0 : i, a = r.resize, o = a === void 0 ? !0 : a, l = un(n.elements.popper), c = [].concat(n.scrollParents.reference, n.scrollParents.popper);
  return s && c.forEach(function(f) {
    f.addEventListener("scroll", e.update, Js);
  }), o && l.addEventListener("resize", e.update, Js), function() {
    s && c.forEach(function(f) {
      f.removeEventListener("scroll", e.update, Js);
    }), o && l.removeEventListener("resize", e.update, Js);
  };
}
const fu = {
  name: "eventListeners",
  enabled: !0,
  phase: "write",
  fn: function() {
  },
  effect: sm,
  data: {}
};
var am = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function ha(t) {
  return t.replace(/left|right|bottom|top/g, function(n) {
    return am[n];
  });
}
var om = {
  start: "end",
  end: "start"
};
function Bc(t) {
  return t.replace(/start|end/g, function(n) {
    return om[n];
  });
}
function hu(t) {
  var n = un(t), e = n.pageXOffset, r = n.pageYOffset;
  return {
    scrollLeft: e,
    scrollTop: r
  };
}
function pu(t) {
  return Ei(fr(t)).left + hu(t).scrollLeft;
}
function lm(t, n) {
  var e = un(t), r = fr(t), i = e.visualViewport, s = r.clientWidth, a = r.clientHeight, o = 0, l = 0;
  if (i) {
    s = i.width, a = i.height;
    var c = If();
    (c || !c && n === "fixed") && (o = i.offsetLeft, l = i.offsetTop);
  }
  return {
    width: s,
    height: a,
    x: o + pu(t),
    y: l
  };
}
function um(t) {
  var n, e = fr(t), r = hu(t), i = (n = t.ownerDocument) == null ? void 0 : n.body, s = Ir(e.scrollWidth, e.clientWidth, i ? i.scrollWidth : 0, i ? i.clientWidth : 0), a = Ir(e.scrollHeight, e.clientHeight, i ? i.scrollHeight : 0, i ? i.clientHeight : 0), o = -r.scrollLeft + pu(t), l = -r.scrollTop;
  return zn(i || e).direction === "rtl" && (o += Ir(e.clientWidth, i ? i.clientWidth : 0) - s), {
    width: s,
    height: a,
    x: o,
    y: l
  };
}
function gu(t) {
  var n = zn(t), e = n.overflow, r = n.overflowX, i = n.overflowY;
  return /auto|scroll|overlay|hidden/.test(e + i + r);
}
function Bf(t) {
  return ["html", "body", "#document"].indexOf(Mn(t)) >= 0 ? t.ownerDocument.body : vn(t) && gu(t) ? t : Bf(eo(t));
}
function as(t, n) {
  var e;
  n === void 0 && (n = []);
  var r = Bf(t), i = r === ((e = t.ownerDocument) == null ? void 0 : e.body), s = un(r), a = i ? [s].concat(s.visualViewport || [], gu(r) ? r : []) : r, o = n.concat(a);
  return i ? o : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    o.concat(as(eo(a)))
  );
}
function xl(t) {
  return Object.assign({}, t, {
    left: t.x,
    top: t.y,
    right: t.x + t.width,
    bottom: t.y + t.height
  });
}
function cm(t, n) {
  var e = Ei(t, !1, n === "fixed");
  return e.top = e.top + t.clientTop, e.left = e.left + t.clientLeft, e.bottom = e.top + t.clientHeight, e.right = e.left + t.clientWidth, e.width = t.clientWidth, e.height = t.clientHeight, e.x = e.left, e.y = e.top, e;
}
function Hc(t, n, e) {
  return n === su ? xl(lm(t, e)) : Mr(n) ? cm(n, e) : xl(um(fr(t)));
}
function dm(t) {
  var n = as(eo(t)), e = ["absolute", "fixed"].indexOf(zn(t).position) >= 0, r = e && vn(t) ? xs(t) : t;
  return Mr(r) ? n.filter(function(i) {
    return Mr(i) && Nf(i, r) && Mn(i) !== "body";
  }) : [];
}
function fm(t, n, e, r) {
  var i = n === "clippingParents" ? dm(t) : [].concat(n), s = [].concat(i, [e]), a = s[0], o = s.reduce(function(l, c) {
    var f = Hc(t, c, r);
    return l.top = Ir(f.top, l.top), l.right = ka(f.right, l.right), l.bottom = ka(f.bottom, l.bottom), l.left = Ir(f.left, l.left), l;
  }, Hc(t, a, r));
  return o.width = o.right - o.left, o.height = o.bottom - o.top, o.x = o.left, o.y = o.top, o;
}
function Hf(t) {
  var n = t.reference, e = t.element, r = t.placement, i = r ? In(r) : null, s = r ? Ai(r) : null, a = n.x + n.width / 2 - e.width / 2, o = n.y + n.height / 2 - e.height / 2, l;
  switch (i) {
    case Ft:
      l = {
        x: a,
        y: n.y - e.height
      };
      break;
    case on:
      l = {
        x: a,
        y: n.y + n.height
      };
      break;
    case ln:
      l = {
        x: n.x + n.width,
        y: o
      };
      break;
    case Bt:
      l = {
        x: n.x - e.width,
        y: o
      };
      break;
    default:
      l = {
        x: n.x,
        y: n.y
      };
  }
  var c = i ? cu(i) : null;
  if (c != null) {
    var f = c === "y" ? "height" : "width";
    switch (s) {
      case Lr:
        l[c] = l[c] - (n[f] / 2 - e[f] / 2);
        break;
      case Ti:
        l[c] = l[c] + (n[f] / 2 - e[f] / 2);
        break;
    }
  }
  return l;
}
function ki(t, n) {
  n === void 0 && (n = {});
  var e = n, r = e.placement, i = r === void 0 ? t.placement : r, s = e.strategy, a = s === void 0 ? t.strategy : s, o = e.boundary, l = o === void 0 ? wf : o, c = e.rootBoundary, f = c === void 0 ? su : c, g = e.elementContext, b = g === void 0 ? di : g, w = e.altBoundary, C = w === void 0 ? !1 : w, m = e.padding, h = m === void 0 ? 0 : m, u = Lf(typeof h != "number" ? h : Mf(h, Mi)), p = b === di ? _f : di, v = t.rects.popper, S = t.elements[C ? p : b], T = fm(Mr(S) ? S : S.contextElement || fr(t.elements.popper), l, f, a), A = Ei(t.elements.reference), y = Hf({
    reference: A,
    element: v,
    strategy: "absolute",
    placement: i
  }), I = xl(Object.assign({}, v, y)), $ = b === di ? I : A, V = {
    top: T.top - $.top + u.top,
    bottom: $.bottom - T.bottom + u.bottom,
    left: T.left - $.left + u.left,
    right: $.right - T.right + u.right
  }, z = t.modifiersData.offset;
  if (b === di && z) {
    var F = z[i];
    Object.keys(V).forEach(function(P) {
      var k = [ln, on].indexOf(P) >= 0 ? 1 : -1, R = [Ft, on].indexOf(P) >= 0 ? "y" : "x";
      V[P] += F[R] * k;
    });
  }
  return V;
}
function hm(t, n) {
  n === void 0 && (n = {});
  var e = n, r = e.placement, i = e.boundary, s = e.rootBoundary, a = e.padding, o = e.flipVariations, l = e.allowedAutoPlacements, c = l === void 0 ? au : l, f = Ai(r), g = f ? o ? _l : _l.filter(function(C) {
    return Ai(C) === f;
  }) : Mi, b = g.filter(function(C) {
    return c.indexOf(C) >= 0;
  });
  b.length === 0 && (b = g);
  var w = b.reduce(function(C, m) {
    return C[m] = ki(t, {
      placement: m,
      boundary: i,
      rootBoundary: s,
      padding: a
    })[In(m)], C;
  }, {});
  return Object.keys(w).sort(function(C, m) {
    return w[C] - w[m];
  });
}
function pm(t) {
  if (In(t) === Za)
    return [];
  var n = ha(t);
  return [Bc(t), n, Bc(n)];
}
function gm(t) {
  var n = t.state, e = t.options, r = t.name;
  if (!n.modifiersData[r]._skip) {
    for (var i = e.mainAxis, s = i === void 0 ? !0 : i, a = e.altAxis, o = a === void 0 ? !0 : a, l = e.fallbackPlacements, c = e.padding, f = e.boundary, g = e.rootBoundary, b = e.altBoundary, w = e.flipVariations, C = w === void 0 ? !0 : w, m = e.allowedAutoPlacements, h = n.options.placement, u = In(h), p = u === h, v = l || (p || !C ? [ha(h)] : pm(h)), S = [h].concat(v).reduce(function(ee, ie) {
      return ee.concat(In(ie) === Za ? hm(n, {
        placement: ie,
        boundary: f,
        rootBoundary: g,
        padding: c,
        flipVariations: C,
        allowedAutoPlacements: m
      }) : ie);
    }, []), T = n.rects.reference, A = n.rects.popper, y = /* @__PURE__ */ new Map(), I = !0, $ = S[0], V = 0; V < S.length; V++) {
      var z = S[V], F = In(z), P = Ai(z) === Lr, k = [Ft, on].indexOf(F) >= 0, R = k ? "width" : "height", L = ki(n, {
        placement: z,
        boundary: f,
        rootBoundary: g,
        altBoundary: b,
        padding: c
      }), E = k ? P ? ln : Bt : P ? on : Ft;
      T[R] > A[R] && (E = ha(E));
      var M = ha(E), W = [];
      if (s && W.push(L[F] <= 0), o && W.push(L[E] <= 0, L[M] <= 0), W.every(function(ee) {
        return ee;
      })) {
        $ = z, I = !1;
        break;
      }
      y.set(z, W);
    }
    if (I)
      for (var Y = C ? 3 : 1, J = function(ie) {
        var oe = S.find(function(ce) {
          var he = y.get(ce);
          if (he)
            return he.slice(0, ie).every(function(we) {
              return we;
            });
        });
        if (oe)
          return $ = oe, "break";
      }, X = Y; X > 0; X--) {
        var Q = J(X);
        if (Q === "break")
          break;
      }
    n.placement !== $ && (n.modifiersData[r]._skip = !0, n.placement = $, n.reset = !0);
  }
}
const $f = {
  name: "flip",
  enabled: !0,
  phase: "main",
  fn: gm,
  requiresIfExists: ["offset"],
  data: {
    _skip: !1
  }
};
function $c(t, n, e) {
  return e === void 0 && (e = {
    x: 0,
    y: 0
  }), {
    top: t.top - n.height - e.y,
    right: t.right - n.width + e.x,
    bottom: t.bottom - n.height + e.y,
    left: t.left - n.width - e.x
  };
}
function Vc(t) {
  return [Ft, ln, on, Bt].some(function(n) {
    return t[n] >= 0;
  });
}
function mm(t) {
  var n = t.state, e = t.name, r = n.rects.reference, i = n.rects.popper, s = n.modifiersData.preventOverflow, a = ki(n, {
    elementContext: "reference"
  }), o = ki(n, {
    altBoundary: !0
  }), l = $c(a, r), c = $c(o, i, s), f = Vc(l), g = Vc(c);
  n.modifiersData[e] = {
    referenceClippingOffsets: l,
    popperEscapeOffsets: c,
    isReferenceHidden: f,
    hasPopperEscaped: g
  }, n.attributes.popper = Object.assign({}, n.attributes.popper, {
    "data-popper-reference-hidden": f,
    "data-popper-escaped": g
  });
}
const Vf = {
  name: "hide",
  enabled: !0,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: mm
};
function vm(t, n, e) {
  var r = In(t), i = [Bt, Ft].indexOf(r) >= 0 ? -1 : 1, s = typeof e == "function" ? e(Object.assign({}, n, {
    placement: t
  })) : e, a = s[0], o = s[1];
  return a = a || 0, o = (o || 0) * i, [Bt, ln].indexOf(r) >= 0 ? {
    x: o,
    y: a
  } : {
    x: a,
    y: o
  };
}
function bm(t) {
  var n = t.state, e = t.options, r = t.name, i = e.offset, s = i === void 0 ? [0, 0] : i, a = au.reduce(function(f, g) {
    return f[g] = vm(g, n.rects, s), f;
  }, {}), o = a[n.placement], l = o.x, c = o.y;
  n.modifiersData.popperOffsets != null && (n.modifiersData.popperOffsets.x += l, n.modifiersData.popperOffsets.y += c), n.modifiersData[r] = a;
}
const Wf = {
  name: "offset",
  enabled: !0,
  phase: "main",
  requires: ["popperOffsets"],
  fn: bm
};
function ym(t) {
  var n = t.state, e = t.name;
  n.modifiersData[e] = Hf({
    reference: n.rects.reference,
    element: n.rects.popper,
    strategy: "absolute",
    placement: n.placement
  });
}
const mu = {
  name: "popperOffsets",
  enabled: !0,
  phase: "read",
  fn: ym,
  data: {}
};
function wm(t) {
  return t === "x" ? "y" : "x";
}
function _m(t) {
  var n = t.state, e = t.options, r = t.name, i = e.mainAxis, s = i === void 0 ? !0 : i, a = e.altAxis, o = a === void 0 ? !1 : a, l = e.boundary, c = e.rootBoundary, f = e.altBoundary, g = e.padding, b = e.tether, w = b === void 0 ? !0 : b, C = e.tetherOffset, m = C === void 0 ? 0 : C, h = ki(n, {
    boundary: l,
    rootBoundary: c,
    padding: g,
    altBoundary: f
  }), u = In(n.placement), p = Ai(n.placement), v = !p, S = cu(u), T = wm(S), A = n.modifiersData.popperOffsets, y = n.rects.reference, I = n.rects.popper, $ = typeof m == "function" ? m(Object.assign({}, n.rects, {
    placement: n.placement
  })) : m, V = typeof $ == "number" ? {
    mainAxis: $,
    altAxis: $
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, $), z = n.modifiersData.offset ? n.modifiersData.offset[n.placement] : null, F = {
    x: 0,
    y: 0
  };
  if (A) {
    if (s) {
      var P, k = S === "y" ? Ft : Bt, R = S === "y" ? on : ln, L = S === "y" ? "height" : "width", E = A[S], M = E + h[k], W = E - h[R], Y = w ? -I[L] / 2 : 0, J = p === Lr ? y[L] : I[L], X = p === Lr ? -I[L] : -y[L], Q = n.elements.arrow, ee = w && Q ? uu(Q) : {
        width: 0,
        height: 0
      }, ie = n.modifiersData["arrow#persistent"] ? n.modifiersData["arrow#persistent"].padding : Rf(), oe = ie[k], ce = ie[R], he = ss(0, y[L], ee[L]), we = v ? y[L] / 2 - Y - he - oe - V.mainAxis : J - he - oe - V.mainAxis, Oe = v ? -y[L] / 2 + Y + he + ce + V.mainAxis : X + he + ce + V.mainAxis, Ve = n.elements.arrow && xs(n.elements.arrow), Ie = Ve ? S === "y" ? Ve.clientTop || 0 : Ve.clientLeft || 0 : 0, Ze = (P = z == null ? void 0 : z[S]) != null ? P : 0, et = E + we - Ze - Ie, He = E + Oe - Ze, Ke = ss(w ? ka(M, et) : M, E, w ? Ir(W, He) : W);
      A[S] = Ke, F[S] = Ke - E;
    }
    if (o) {
      var Ct, yt = S === "x" ? Ft : Bt, wt = S === "x" ? on : ln, rt = A[T], mt = T === "y" ? "height" : "width", St = rt + h[yt], ut = rt - h[wt], Ot = [Ft, Bt].indexOf(u) !== -1, dt = (Ct = z == null ? void 0 : z[T]) != null ? Ct : 0, mr = Ot ? St : rt - y[mt] - I[mt] - dt + V.altAxis, It = Ot ? rt + y[mt] + I[mt] - dt - V.altAxis : ut, Ht = w && Ot ? Jg(mr, rt, It) : ss(w ? mr : St, rt, w ? It : ut);
      A[T] = Ht, F[T] = Ht - rt;
    }
    n.modifiersData[r] = F;
  }
}
const jf = {
  name: "preventOverflow",
  enabled: !0,
  phase: "main",
  fn: _m,
  requiresIfExists: ["offset"]
};
function Cm(t) {
  return {
    scrollLeft: t.scrollLeft,
    scrollTop: t.scrollTop
  };
}
function xm(t) {
  return t === un(t) || !vn(t) ? hu(t) : Cm(t);
}
function Sm(t) {
  var n = t.getBoundingClientRect(), e = Di(n.width) / t.offsetWidth || 1, r = Di(n.height) / t.offsetHeight || 1;
  return e !== 1 || r !== 1;
}
function Tm(t, n, e) {
  e === void 0 && (e = !1);
  var r = vn(n), i = vn(n) && Sm(n), s = fr(n), a = Ei(t, i, e), o = {
    scrollLeft: 0,
    scrollTop: 0
  }, l = {
    x: 0,
    y: 0
  };
  return (r || !r && !e) && ((Mn(n) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
  gu(s)) && (o = xm(n)), vn(n) ? (l = Ei(n, !0), l.x += n.clientLeft, l.y += n.clientTop) : s && (l.x = pu(s))), {
    x: a.left + o.scrollLeft - l.x,
    y: a.top + o.scrollTop - l.y,
    width: a.width,
    height: a.height
  };
}
function Dm(t) {
  var n = /* @__PURE__ */ new Map(), e = /* @__PURE__ */ new Set(), r = [];
  t.forEach(function(s) {
    n.set(s.name, s);
  });
  function i(s) {
    e.add(s.name);
    var a = [].concat(s.requires || [], s.requiresIfExists || []);
    a.forEach(function(o) {
      if (!e.has(o)) {
        var l = n.get(o);
        l && i(l);
      }
    }), r.push(s);
  }
  return t.forEach(function(s) {
    e.has(s.name) || i(s);
  }), r;
}
function Em(t) {
  var n = Dm(t);
  return Of.reduce(function(e, r) {
    return e.concat(n.filter(function(i) {
      return i.phase === r;
    }));
  }, []);
}
function Am(t) {
  var n;
  return function() {
    return n || (n = new Promise(function(e) {
      Promise.resolve().then(function() {
        n = void 0, e(t());
      });
    })), n;
  };
}
function km(t) {
  var n = t.reduce(function(e, r) {
    var i = e[r.name];
    return e[r.name] = i ? Object.assign({}, i, r, {
      options: Object.assign({}, i.options, r.options),
      data: Object.assign({}, i.data, r.data)
    }) : r, e;
  }, {});
  return Object.keys(n).map(function(e) {
    return n[e];
  });
}
var Wc = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function jc() {
  for (var t = arguments.length, n = new Array(t), e = 0; e < t; e++)
    n[e] = arguments[e];
  return !n.some(function(r) {
    return !(r && typeof r.getBoundingClientRect == "function");
  });
}
function to(t) {
  t === void 0 && (t = {});
  var n = t, e = n.defaultModifiers, r = e === void 0 ? [] : e, i = n.defaultOptions, s = i === void 0 ? Wc : i;
  return function(o, l, c) {
    c === void 0 && (c = s);
    var f = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, Wc, s),
      modifiersData: {},
      elements: {
        reference: o,
        popper: l
      },
      attributes: {},
      styles: {}
    }, g = [], b = !1, w = {
      state: f,
      setOptions: function(u) {
        var p = typeof u == "function" ? u(f.options) : u;
        m(), f.options = Object.assign({}, s, f.options, p), f.scrollParents = {
          reference: Mr(o) ? as(o) : o.contextElement ? as(o.contextElement) : [],
          popper: as(l)
        };
        var v = Em(km([].concat(r, f.options.modifiers)));
        return f.orderedModifiers = v.filter(function(S) {
          return S.enabled;
        }), C(), w.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function() {
        if (!b) {
          var u = f.elements, p = u.reference, v = u.popper;
          if (jc(p, v)) {
            f.rects = {
              reference: Tm(p, xs(v), f.options.strategy === "fixed"),
              popper: uu(v)
            }, f.reset = !1, f.placement = f.options.placement, f.orderedModifiers.forEach(function(V) {
              return f.modifiersData[V.name] = Object.assign({}, V.data);
            });
            for (var S = 0; S < f.orderedModifiers.length; S++) {
              if (f.reset === !0) {
                f.reset = !1, S = -1;
                continue;
              }
              var T = f.orderedModifiers[S], A = T.fn, y = T.options, I = y === void 0 ? {} : y, $ = T.name;
              typeof A == "function" && (f = A({
                state: f,
                options: I,
                name: $,
                instance: w
              }) || f);
            }
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: Am(function() {
        return new Promise(function(h) {
          w.forceUpdate(), h(f);
        });
      }),
      destroy: function() {
        m(), b = !0;
      }
    };
    if (!jc(o, l))
      return w;
    w.setOptions(c).then(function(h) {
      !b && c.onFirstUpdate && c.onFirstUpdate(h);
    });
    function C() {
      f.orderedModifiers.forEach(function(h) {
        var u = h.name, p = h.options, v = p === void 0 ? {} : p, S = h.effect;
        if (typeof S == "function") {
          var T = S({
            state: f,
            name: u,
            instance: w,
            options: v
          }), A = function() {
          };
          g.push(T || A);
        }
      });
    }
    function m() {
      g.forEach(function(h) {
        return h();
      }), g = [];
    }
    return w;
  };
}
var Pm = /* @__PURE__ */ to(), Om = [fu, mu, du, lu], Im = /* @__PURE__ */ to({
  defaultModifiers: Om
}), Nm = [fu, mu, du, lu, Wf, $f, jf, Ff, Vf], vu = /* @__PURE__ */ to({
  defaultModifiers: Nm
});
const zf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  afterMain: Ef,
  afterRead: Sf,
  afterWrite: Pf,
  applyStyles: lu,
  arrow: Ff,
  auto: Za,
  basePlacements: Mi,
  beforeMain: Tf,
  beforeRead: Cf,
  beforeWrite: Af,
  bottom: on,
  clippingParents: wf,
  computeStyles: du,
  createPopper: vu,
  createPopperBase: Pm,
  createPopperLite: Im,
  detectOverflow: ki,
  end: Ti,
  eventListeners: fu,
  flip: $f,
  hide: Vf,
  left: Bt,
  main: Df,
  modifierPhases: Of,
  offset: Wf,
  placements: au,
  popper: di,
  popperGenerator: to,
  popperOffsets: mu,
  preventOverflow: jf,
  read: xf,
  reference: _f,
  right: ln,
  start: Lr,
  top: Ft,
  variationPlacements: _l,
  viewport: su,
  write: kf
}, Symbol.toStringTag, { value: "Module" }));
/*!
  * Bootstrap v5.3.2 (https://getbootstrap.com/)
  * Copyright 2011-2023 The Bootstrap Authors (https://github.com/twbs/bootstrap/graphs/contributors)
  * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)
  */
const Zn = /* @__PURE__ */ new Map(), Bo = {
  set(t, n, e) {
    Zn.has(t) || Zn.set(t, /* @__PURE__ */ new Map());
    const r = Zn.get(t);
    if (!r.has(n) && r.size !== 0) {
      console.error(`Bootstrap doesn't allow more than one instance per element. Bound instance: ${Array.from(r.keys())[0]}.`);
      return;
    }
    r.set(n, e);
  },
  get(t, n) {
    return Zn.has(t) && Zn.get(t).get(n) || null;
  },
  remove(t, n) {
    if (!Zn.has(t))
      return;
    const e = Zn.get(t);
    e.delete(n), e.size === 0 && Zn.delete(t);
  }
}, Rm = 1e6, Lm = 1e3, Sl = "transitionend", Yf = (t) => (t && window.CSS && window.CSS.escape && (t = t.replace(/#([^\s"#']+)/g, (n, e) => `#${CSS.escape(e)}`)), t), Mm = (t) => t == null ? `${t}` : Object.prototype.toString.call(t).match(/\s([a-z]+)/i)[1].toLowerCase(), Fm = (t) => {
  do
    t += Math.floor(Math.random() * Rm);
  while (document.getElementById(t));
  return t;
}, Bm = (t) => {
  if (!t)
    return 0;
  let {
    transitionDuration: n,
    transitionDelay: e
  } = window.getComputedStyle(t);
  const r = Number.parseFloat(n), i = Number.parseFloat(e);
  return !r && !i ? 0 : (n = n.split(",")[0], e = e.split(",")[0], (Number.parseFloat(n) + Number.parseFloat(e)) * Lm);
}, qf = (t) => {
  t.dispatchEvent(new Event(Sl));
}, $n = (t) => !t || typeof t != "object" ? !1 : (typeof t.jquery < "u" && (t = t[0]), typeof t.nodeType < "u"), ur = (t) => $n(t) ? t.jquery ? t[0] : t : typeof t == "string" && t.length > 0 ? document.querySelector(Yf(t)) : null, Fi = (t) => {
  if (!$n(t) || t.getClientRects().length === 0)
    return !1;
  const n = getComputedStyle(t).getPropertyValue("visibility") === "visible", e = t.closest("details:not([open])");
  if (!e)
    return n;
  if (e !== t) {
    const r = t.closest("summary");
    if (r && r.parentNode !== e || r === null)
      return !1;
  }
  return n;
}, cr = (t) => !t || t.nodeType !== Node.ELEMENT_NODE || t.classList.contains("disabled") ? !0 : typeof t.disabled < "u" ? t.disabled : t.hasAttribute("disabled") && t.getAttribute("disabled") !== "false", Gf = (t) => {
  if (!document.documentElement.attachShadow)
    return null;
  if (typeof t.getRootNode == "function") {
    const n = t.getRootNode();
    return n instanceof ShadowRoot ? n : null;
  }
  return t instanceof ShadowRoot ? t : t.parentNode ? Gf(t.parentNode) : null;
}, Pa = () => {
}, Ss = (t) => {
  t.offsetHeight;
}, Uf = () => window.jQuery && !document.body.hasAttribute("data-bs-no-jquery") ? window.jQuery : null, Ho = [], Hm = (t) => {
  document.readyState === "loading" ? (Ho.length || document.addEventListener("DOMContentLoaded", () => {
    for (const n of Ho)
      n();
  }), Ho.push(t)) : t();
}, bn = () => document.documentElement.dir === "rtl", Cn = (t) => {
  Hm(() => {
    const n = Uf();
    if (n) {
      const e = t.NAME, r = n.fn[e];
      n.fn[e] = t.jQueryInterface, n.fn[e].Constructor = t, n.fn[e].noConflict = () => (n.fn[e] = r, t.jQueryInterface);
    }
  });
}, Gt = (t, n = [], e = t) => typeof t == "function" ? t(...n) : e, Kf = (t, n, e = !0) => {
  if (!e) {
    Gt(t);
    return;
  }
  const r = 5, i = Bm(n) + r;
  let s = !1;
  const a = ({
    target: o
  }) => {
    o === n && (s = !0, n.removeEventListener(Sl, a), Gt(t));
  };
  n.addEventListener(Sl, a), setTimeout(() => {
    s || qf(n);
  }, i);
}, bu = (t, n, e, r) => {
  const i = t.length;
  let s = t.indexOf(n);
  return s === -1 ? !e && r ? t[i - 1] : t[0] : (s += e ? 1 : -1, r && (s = (s + i) % i), t[Math.max(0, Math.min(s, i - 1))]);
}, $m = /[^.]*(?=\..*)\.|.*/, Vm = /\..*/, Wm = /::\d+$/, $o = {};
let zc = 1;
const Xf = {
  mouseenter: "mouseover",
  mouseleave: "mouseout"
}, jm = /* @__PURE__ */ new Set(["click", "dblclick", "mouseup", "mousedown", "contextmenu", "mousewheel", "DOMMouseScroll", "mouseover", "mouseout", "mousemove", "selectstart", "selectend", "keydown", "keypress", "keyup", "orientationchange", "touchstart", "touchmove", "touchend", "touchcancel", "pointerdown", "pointermove", "pointerup", "pointerleave", "pointercancel", "gesturestart", "gesturechange", "gestureend", "focus", "blur", "change", "reset", "select", "submit", "focusin", "focusout", "load", "unload", "beforeunload", "resize", "move", "DOMContentLoaded", "readystatechange", "error", "abort", "scroll"]);
function Qf(t, n) {
  return n && `${n}::${zc++}` || t.uidEvent || zc++;
}
function Jf(t) {
  const n = Qf(t);
  return t.uidEvent = n, $o[n] = $o[n] || {}, $o[n];
}
function zm(t, n) {
  return function e(r) {
    return yu(r, {
      delegateTarget: t
    }), e.oneOff && de.off(t, r.type, n), n.apply(t, [r]);
  };
}
function Ym(t, n, e) {
  return function r(i) {
    const s = t.querySelectorAll(n);
    for (let {
      target: a
    } = i; a && a !== this; a = a.parentNode)
      for (const o of s)
        if (o === a)
          return yu(i, {
            delegateTarget: a
          }), r.oneOff && de.off(t, i.type, n, e), e.apply(a, [i]);
  };
}
function Zf(t, n, e = null) {
  return Object.values(t).find((r) => r.callable === n && r.delegationSelector === e);
}
function eh(t, n, e) {
  const r = typeof n == "string", i = r ? e : n || e;
  let s = th(t);
  return jm.has(s) || (s = t), [r, i, s];
}
function Yc(t, n, e, r, i) {
  if (typeof n != "string" || !t)
    return;
  let [s, a, o] = eh(n, e, r);
  n in Xf && (a = ((C) => function(m) {
    if (!m.relatedTarget || m.relatedTarget !== m.delegateTarget && !m.delegateTarget.contains(m.relatedTarget))
      return C.call(this, m);
  })(a));
  const l = Jf(t), c = l[o] || (l[o] = {}), f = Zf(c, a, s ? e : null);
  if (f) {
    f.oneOff = f.oneOff && i;
    return;
  }
  const g = Qf(a, n.replace($m, "")), b = s ? Ym(t, e, a) : zm(t, a);
  b.delegationSelector = s ? e : null, b.callable = a, b.oneOff = i, b.uidEvent = g, c[g] = b, t.addEventListener(o, b, s);
}
function Tl(t, n, e, r, i) {
  const s = Zf(n[e], r, i);
  s && (t.removeEventListener(e, s, !!i), delete n[e][s.uidEvent]);
}
function qm(t, n, e, r) {
  const i = n[e] || {};
  for (const [s, a] of Object.entries(i))
    s.includes(r) && Tl(t, n, e, a.callable, a.delegationSelector);
}
function th(t) {
  return t = t.replace(Vm, ""), Xf[t] || t;
}
const de = {
  on(t, n, e, r) {
    Yc(t, n, e, r, !1);
  },
  one(t, n, e, r) {
    Yc(t, n, e, r, !0);
  },
  off(t, n, e, r) {
    if (typeof n != "string" || !t)
      return;
    const [i, s, a] = eh(n, e, r), o = a !== n, l = Jf(t), c = l[a] || {}, f = n.startsWith(".");
    if (typeof s < "u") {
      if (!Object.keys(c).length)
        return;
      Tl(t, l, a, s, i ? e : null);
      return;
    }
    if (f)
      for (const g of Object.keys(l))
        qm(t, l, g, n.slice(1));
    for (const [g, b] of Object.entries(c)) {
      const w = g.replace(Wm, "");
      (!o || n.includes(w)) && Tl(t, l, a, b.callable, b.delegationSelector);
    }
  },
  trigger(t, n, e) {
    if (typeof n != "string" || !t)
      return null;
    const r = Uf(), i = th(n), s = n !== i;
    let a = null, o = !0, l = !0, c = !1;
    s && r && (a = r.Event(n, e), r(t).trigger(a), o = !a.isPropagationStopped(), l = !a.isImmediatePropagationStopped(), c = a.isDefaultPrevented());
    const f = yu(new Event(n, {
      bubbles: o,
      cancelable: !0
    }), e);
    return c && f.preventDefault(), l && t.dispatchEvent(f), f.defaultPrevented && a && a.preventDefault(), f;
  }
};
function yu(t, n = {}) {
  for (const [e, r] of Object.entries(n))
    try {
      t[e] = r;
    } catch {
      Object.defineProperty(t, e, {
        configurable: !0,
        get() {
          return r;
        }
      });
    }
  return t;
}
function qc(t) {
  if (t === "true")
    return !0;
  if (t === "false")
    return !1;
  if (t === Number(t).toString())
    return Number(t);
  if (t === "" || t === "null")
    return null;
  if (typeof t != "string")
    return t;
  try {
    return JSON.parse(decodeURIComponent(t));
  } catch {
    return t;
  }
}
function Vo(t) {
  return t.replace(/[A-Z]/g, (n) => `-${n.toLowerCase()}`);
}
const Vn = {
  setDataAttribute(t, n, e) {
    t.setAttribute(`data-bs-${Vo(n)}`, e);
  },
  removeDataAttribute(t, n) {
    t.removeAttribute(`data-bs-${Vo(n)}`);
  },
  getDataAttributes(t) {
    if (!t)
      return {};
    const n = {}, e = Object.keys(t.dataset).filter((r) => r.startsWith("bs") && !r.startsWith("bsConfig"));
    for (const r of e) {
      let i = r.replace(/^bs/, "");
      i = i.charAt(0).toLowerCase() + i.slice(1, i.length), n[i] = qc(t.dataset[r]);
    }
    return n;
  },
  getDataAttribute(t, n) {
    return qc(t.getAttribute(`data-bs-${Vo(n)}`));
  }
};
class Ts {
  // Getters
  static get Default() {
    return {};
  }
  static get DefaultType() {
    return {};
  }
  static get NAME() {
    throw new Error('You have to implement the static method "NAME", for each component!');
  }
  _getConfig(n) {
    return n = this._mergeConfigObj(n), n = this._configAfterMerge(n), this._typeCheckConfig(n), n;
  }
  _configAfterMerge(n) {
    return n;
  }
  _mergeConfigObj(n, e) {
    const r = $n(e) ? Vn.getDataAttribute(e, "config") : {};
    return {
      ...this.constructor.Default,
      ...typeof r == "object" ? r : {},
      ...$n(e) ? Vn.getDataAttributes(e) : {},
      ...typeof n == "object" ? n : {}
    };
  }
  _typeCheckConfig(n, e = this.constructor.DefaultType) {
    for (const [r, i] of Object.entries(e)) {
      const s = n[r], a = $n(s) ? "element" : Mm(s);
      if (!new RegExp(i).test(a))
        throw new TypeError(`${this.constructor.NAME.toUpperCase()}: Option "${r}" provided type "${a}" but expected type "${i}".`);
    }
  }
}
const Gm = "5.3.2";
class kn extends Ts {
  constructor(n, e) {
    super(), n = ur(n), n && (this._element = n, this._config = this._getConfig(e), Bo.set(this._element, this.constructor.DATA_KEY, this));
  }
  // Public
  dispose() {
    Bo.remove(this._element, this.constructor.DATA_KEY), de.off(this._element, this.constructor.EVENT_KEY);
    for (const n of Object.getOwnPropertyNames(this))
      this[n] = null;
  }
  _queueCallback(n, e, r = !0) {
    Kf(n, e, r);
  }
  _getConfig(n) {
    return n = this._mergeConfigObj(n, this._element), n = this._configAfterMerge(n), this._typeCheckConfig(n), n;
  }
  // Static
  static getInstance(n) {
    return Bo.get(ur(n), this.DATA_KEY);
  }
  static getOrCreateInstance(n, e = {}) {
    return this.getInstance(n) || new this(n, typeof e == "object" ? e : null);
  }
  static get VERSION() {
    return Gm;
  }
  static get DATA_KEY() {
    return `bs.${this.NAME}`;
  }
  static get EVENT_KEY() {
    return `.${this.DATA_KEY}`;
  }
  static eventName(n) {
    return `${n}${this.EVENT_KEY}`;
  }
}
const Wo = (t) => {
  let n = t.getAttribute("data-bs-target");
  if (!n || n === "#") {
    let e = t.getAttribute("href");
    if (!e || !e.includes("#") && !e.startsWith("."))
      return null;
    e.includes("#") && !e.startsWith("#") && (e = `#${e.split("#")[1]}`), n = e && e !== "#" ? Yf(e.trim()) : null;
  }
  return n;
}, Se = {
  find(t, n = document.documentElement) {
    return [].concat(...Element.prototype.querySelectorAll.call(n, t));
  },
  findOne(t, n = document.documentElement) {
    return Element.prototype.querySelector.call(n, t);
  },
  children(t, n) {
    return [].concat(...t.children).filter((e) => e.matches(n));
  },
  parents(t, n) {
    const e = [];
    let r = t.parentNode.closest(n);
    for (; r; )
      e.push(r), r = r.parentNode.closest(n);
    return e;
  },
  prev(t, n) {
    let e = t.previousElementSibling;
    for (; e; ) {
      if (e.matches(n))
        return [e];
      e = e.previousElementSibling;
    }
    return [];
  },
  // TODO: this is now unused; remove later along with prev()
  next(t, n) {
    let e = t.nextElementSibling;
    for (; e; ) {
      if (e.matches(n))
        return [e];
      e = e.nextElementSibling;
    }
    return [];
  },
  focusableChildren(t) {
    const n = ["a", "button", "input", "textarea", "select", "details", "[tabindex]", '[contenteditable="true"]'].map((e) => `${e}:not([tabindex^="-"])`).join(",");
    return this.find(n, t).filter((e) => !cr(e) && Fi(e));
  },
  getSelectorFromElement(t) {
    const n = Wo(t);
    return n && Se.findOne(n) ? n : null;
  },
  getElementFromSelector(t) {
    const n = Wo(t);
    return n ? Se.findOne(n) : null;
  },
  getMultipleElementsFromSelector(t) {
    const n = Wo(t);
    return n ? Se.find(n) : [];
  }
}, no = (t, n = "hide") => {
  const e = `click.dismiss${t.EVENT_KEY}`, r = t.NAME;
  de.on(document, e, `[data-bs-dismiss="${r}"]`, function(i) {
    if (["A", "AREA"].includes(this.tagName) && i.preventDefault(), cr(this))
      return;
    const s = Se.getElementFromSelector(this) || this.closest(`.${r}`);
    t.getOrCreateInstance(s)[n]();
  });
}, Um = "alert", Km = "bs.alert", nh = `.${Km}`, Xm = `close${nh}`, Qm = `closed${nh}`, Jm = "fade", Zm = "show";
class Ds extends kn {
  // Getters
  static get NAME() {
    return Um;
  }
  // Public
  close() {
    if (de.trigger(this._element, Xm).defaultPrevented)
      return;
    this._element.classList.remove(Zm);
    const e = this._element.classList.contains(Jm);
    this._queueCallback(() => this._destroyElement(), this._element, e);
  }
  // Private
  _destroyElement() {
    this._element.remove(), de.trigger(this._element, Qm), this.dispose();
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Ds.getOrCreateInstance(this);
      if (typeof n == "string") {
        if (e[n] === void 0 || n.startsWith("_") || n === "constructor")
          throw new TypeError(`No method named "${n}"`);
        e[n](this);
      }
    });
  }
}
no(Ds, "close");
Cn(Ds);
const ev = "button", tv = "bs.button", nv = `.${tv}`, rv = ".data-api", iv = "active", Gc = '[data-bs-toggle="button"]', sv = `click${nv}${rv}`;
class Es extends kn {
  // Getters
  static get NAME() {
    return ev;
  }
  // Public
  toggle() {
    this._element.setAttribute("aria-pressed", this._element.classList.toggle(iv));
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Es.getOrCreateInstance(this);
      n === "toggle" && e[n]();
    });
  }
}
de.on(document, sv, Gc, (t) => {
  t.preventDefault();
  const n = t.target.closest(Gc);
  Es.getOrCreateInstance(n).toggle();
});
Cn(Es);
const av = "swipe", Bi = ".bs.swipe", ov = `touchstart${Bi}`, lv = `touchmove${Bi}`, uv = `touchend${Bi}`, cv = `pointerdown${Bi}`, dv = `pointerup${Bi}`, fv = "touch", hv = "pen", pv = "pointer-event", gv = 40, mv = {
  endCallback: null,
  leftCallback: null,
  rightCallback: null
}, vv = {
  endCallback: "(function|null)",
  leftCallback: "(function|null)",
  rightCallback: "(function|null)"
};
class Oa extends Ts {
  constructor(n, e) {
    super(), this._element = n, !(!n || !Oa.isSupported()) && (this._config = this._getConfig(e), this._deltaX = 0, this._supportPointerEvents = !!window.PointerEvent, this._initEvents());
  }
  // Getters
  static get Default() {
    return mv;
  }
  static get DefaultType() {
    return vv;
  }
  static get NAME() {
    return av;
  }
  // Public
  dispose() {
    de.off(this._element, Bi);
  }
  // Private
  _start(n) {
    if (!this._supportPointerEvents) {
      this._deltaX = n.touches[0].clientX;
      return;
    }
    this._eventIsPointerPenTouch(n) && (this._deltaX = n.clientX);
  }
  _end(n) {
    this._eventIsPointerPenTouch(n) && (this._deltaX = n.clientX - this._deltaX), this._handleSwipe(), Gt(this._config.endCallback);
  }
  _move(n) {
    this._deltaX = n.touches && n.touches.length > 1 ? 0 : n.touches[0].clientX - this._deltaX;
  }
  _handleSwipe() {
    const n = Math.abs(this._deltaX);
    if (n <= gv)
      return;
    const e = n / this._deltaX;
    this._deltaX = 0, e && Gt(e > 0 ? this._config.rightCallback : this._config.leftCallback);
  }
  _initEvents() {
    this._supportPointerEvents ? (de.on(this._element, cv, (n) => this._start(n)), de.on(this._element, dv, (n) => this._end(n)), this._element.classList.add(pv)) : (de.on(this._element, ov, (n) => this._start(n)), de.on(this._element, lv, (n) => this._move(n)), de.on(this._element, uv, (n) => this._end(n)));
  }
  _eventIsPointerPenTouch(n) {
    return this._supportPointerEvents && (n.pointerType === hv || n.pointerType === fv);
  }
  // Static
  static isSupported() {
    return "ontouchstart" in document.documentElement || navigator.maxTouchPoints > 0;
  }
}
const bv = "carousel", yv = "bs.carousel", hr = `.${yv}`, rh = ".data-api", wv = "ArrowLeft", _v = "ArrowRight", Cv = 500, Zi = "next", ai = "prev", fi = "left", pa = "right", xv = `slide${hr}`, jo = `slid${hr}`, Sv = `keydown${hr}`, Tv = `mouseenter${hr}`, Dv = `mouseleave${hr}`, Ev = `dragstart${hr}`, Av = `load${hr}${rh}`, kv = `click${hr}${rh}`, ih = "carousel", Zs = "active", Pv = "slide", Ov = "carousel-item-end", Iv = "carousel-item-start", Nv = "carousel-item-next", Rv = "carousel-item-prev", sh = ".active", ah = ".carousel-item", Lv = sh + ah, Mv = ".carousel-item img", Fv = ".carousel-indicators", Bv = "[data-bs-slide], [data-bs-slide-to]", Hv = '[data-bs-ride="carousel"]', $v = {
  [wv]: pa,
  [_v]: fi
}, Vv = {
  interval: 5e3,
  keyboard: !0,
  pause: "hover",
  ride: !1,
  touch: !0,
  wrap: !0
}, Wv = {
  interval: "(number|boolean)",
  // TODO:v6 remove boolean support
  keyboard: "boolean",
  pause: "(string|boolean)",
  ride: "(boolean|string)",
  touch: "boolean",
  wrap: "boolean"
};
class Hi extends kn {
  constructor(n, e) {
    super(n, e), this._interval = null, this._activeElement = null, this._isSliding = !1, this.touchTimeout = null, this._swipeHelper = null, this._indicatorsElement = Se.findOne(Fv, this._element), this._addEventListeners(), this._config.ride === ih && this.cycle();
  }
  // Getters
  static get Default() {
    return Vv;
  }
  static get DefaultType() {
    return Wv;
  }
  static get NAME() {
    return bv;
  }
  // Public
  next() {
    this._slide(Zi);
  }
  nextWhenVisible() {
    !document.hidden && Fi(this._element) && this.next();
  }
  prev() {
    this._slide(ai);
  }
  pause() {
    this._isSliding && qf(this._element), this._clearInterval();
  }
  cycle() {
    this._clearInterval(), this._updateInterval(), this._interval = setInterval(() => this.nextWhenVisible(), this._config.interval);
  }
  _maybeEnableCycle() {
    if (this._config.ride) {
      if (this._isSliding) {
        de.one(this._element, jo, () => this.cycle());
        return;
      }
      this.cycle();
    }
  }
  to(n) {
    const e = this._getItems();
    if (n > e.length - 1 || n < 0)
      return;
    if (this._isSliding) {
      de.one(this._element, jo, () => this.to(n));
      return;
    }
    const r = this._getItemIndex(this._getActive());
    if (r === n)
      return;
    const i = n > r ? Zi : ai;
    this._slide(i, e[n]);
  }
  dispose() {
    this._swipeHelper && this._swipeHelper.dispose(), super.dispose();
  }
  // Private
  _configAfterMerge(n) {
    return n.defaultInterval = n.interval, n;
  }
  _addEventListeners() {
    this._config.keyboard && de.on(this._element, Sv, (n) => this._keydown(n)), this._config.pause === "hover" && (de.on(this._element, Tv, () => this.pause()), de.on(this._element, Dv, () => this._maybeEnableCycle())), this._config.touch && Oa.isSupported() && this._addTouchEventListeners();
  }
  _addTouchEventListeners() {
    for (const r of Se.find(Mv, this._element))
      de.on(r, Ev, (i) => i.preventDefault());
    const e = {
      leftCallback: () => this._slide(this._directionToOrder(fi)),
      rightCallback: () => this._slide(this._directionToOrder(pa)),
      endCallback: () => {
        this._config.pause === "hover" && (this.pause(), this.touchTimeout && clearTimeout(this.touchTimeout), this.touchTimeout = setTimeout(() => this._maybeEnableCycle(), Cv + this._config.interval));
      }
    };
    this._swipeHelper = new Oa(this._element, e);
  }
  _keydown(n) {
    if (/input|textarea/i.test(n.target.tagName))
      return;
    const e = $v[n.key];
    e && (n.preventDefault(), this._slide(this._directionToOrder(e)));
  }
  _getItemIndex(n) {
    return this._getItems().indexOf(n);
  }
  _setActiveIndicatorElement(n) {
    if (!this._indicatorsElement)
      return;
    const e = Se.findOne(sh, this._indicatorsElement);
    e.classList.remove(Zs), e.removeAttribute("aria-current");
    const r = Se.findOne(`[data-bs-slide-to="${n}"]`, this._indicatorsElement);
    r && (r.classList.add(Zs), r.setAttribute("aria-current", "true"));
  }
  _updateInterval() {
    const n = this._activeElement || this._getActive();
    if (!n)
      return;
    const e = Number.parseInt(n.getAttribute("data-bs-interval"), 10);
    this._config.interval = e || this._config.defaultInterval;
  }
  _slide(n, e = null) {
    if (this._isSliding)
      return;
    const r = this._getActive(), i = n === Zi, s = e || bu(this._getItems(), r, i, this._config.wrap);
    if (s === r)
      return;
    const a = this._getItemIndex(s), o = (w) => de.trigger(this._element, w, {
      relatedTarget: s,
      direction: this._orderToDirection(n),
      from: this._getItemIndex(r),
      to: a
    });
    if (o(xv).defaultPrevented || !r || !s)
      return;
    const c = !!this._interval;
    this.pause(), this._isSliding = !0, this._setActiveIndicatorElement(a), this._activeElement = s;
    const f = i ? Iv : Ov, g = i ? Nv : Rv;
    s.classList.add(g), Ss(s), r.classList.add(f), s.classList.add(f);
    const b = () => {
      s.classList.remove(f, g), s.classList.add(Zs), r.classList.remove(Zs, g, f), this._isSliding = !1, o(jo);
    };
    this._queueCallback(b, r, this._isAnimated()), c && this.cycle();
  }
  _isAnimated() {
    return this._element.classList.contains(Pv);
  }
  _getActive() {
    return Se.findOne(Lv, this._element);
  }
  _getItems() {
    return Se.find(ah, this._element);
  }
  _clearInterval() {
    this._interval && (clearInterval(this._interval), this._interval = null);
  }
  _directionToOrder(n) {
    return bn() ? n === fi ? ai : Zi : n === fi ? Zi : ai;
  }
  _orderToDirection(n) {
    return bn() ? n === ai ? fi : pa : n === ai ? pa : fi;
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Hi.getOrCreateInstance(this, n);
      if (typeof n == "number") {
        e.to(n);
        return;
      }
      if (typeof n == "string") {
        if (e[n] === void 0 || n.startsWith("_") || n === "constructor")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
}
de.on(document, kv, Bv, function(t) {
  const n = Se.getElementFromSelector(this);
  if (!n || !n.classList.contains(ih))
    return;
  t.preventDefault();
  const e = Hi.getOrCreateInstance(n), r = this.getAttribute("data-bs-slide-to");
  if (r) {
    e.to(r), e._maybeEnableCycle();
    return;
  }
  if (Vn.getDataAttribute(this, "slide") === "next") {
    e.next(), e._maybeEnableCycle();
    return;
  }
  e.prev(), e._maybeEnableCycle();
});
de.on(window, Av, () => {
  const t = Se.find(Hv);
  for (const n of t)
    Hi.getOrCreateInstance(n);
});
Cn(Hi);
const jv = "collapse", zv = "bs.collapse", As = `.${zv}`, Yv = ".data-api", qv = `show${As}`, Gv = `shown${As}`, Uv = `hide${As}`, Kv = `hidden${As}`, Xv = `click${As}${Yv}`, zo = "show", gi = "collapse", ea = "collapsing", Qv = "collapsed", Jv = `:scope .${gi} .${gi}`, Zv = "collapse-horizontal", eb = "width", tb = "height", nb = ".collapse.show, .collapse.collapsing", Dl = '[data-bs-toggle="collapse"]', rb = {
  parent: null,
  toggle: !0
}, ib = {
  parent: "(null|element)",
  toggle: "boolean"
};
class Fr extends kn {
  constructor(n, e) {
    super(n, e), this._isTransitioning = !1, this._triggerArray = [];
    const r = Se.find(Dl);
    for (const i of r) {
      const s = Se.getSelectorFromElement(i), a = Se.find(s).filter((o) => o === this._element);
      s !== null && a.length && this._triggerArray.push(i);
    }
    this._initializeChildren(), this._config.parent || this._addAriaAndCollapsedClass(this._triggerArray, this._isShown()), this._config.toggle && this.toggle();
  }
  // Getters
  static get Default() {
    return rb;
  }
  static get DefaultType() {
    return ib;
  }
  static get NAME() {
    return jv;
  }
  // Public
  toggle() {
    this._isShown() ? this.hide() : this.show();
  }
  show() {
    if (this._isTransitioning || this._isShown())
      return;
    let n = [];
    if (this._config.parent && (n = this._getFirstLevelChildren(nb).filter((o) => o !== this._element).map((o) => Fr.getOrCreateInstance(o, {
      toggle: !1
    }))), n.length && n[0]._isTransitioning || de.trigger(this._element, qv).defaultPrevented)
      return;
    for (const o of n)
      o.hide();
    const r = this._getDimension();
    this._element.classList.remove(gi), this._element.classList.add(ea), this._element.style[r] = 0, this._addAriaAndCollapsedClass(this._triggerArray, !0), this._isTransitioning = !0;
    const i = () => {
      this._isTransitioning = !1, this._element.classList.remove(ea), this._element.classList.add(gi, zo), this._element.style[r] = "", de.trigger(this._element, Gv);
    }, a = `scroll${r[0].toUpperCase() + r.slice(1)}`;
    this._queueCallback(i, this._element, !0), this._element.style[r] = `${this._element[a]}px`;
  }
  hide() {
    if (this._isTransitioning || !this._isShown() || de.trigger(this._element, Uv).defaultPrevented)
      return;
    const e = this._getDimension();
    this._element.style[e] = `${this._element.getBoundingClientRect()[e]}px`, Ss(this._element), this._element.classList.add(ea), this._element.classList.remove(gi, zo);
    for (const i of this._triggerArray) {
      const s = Se.getElementFromSelector(i);
      s && !this._isShown(s) && this._addAriaAndCollapsedClass([i], !1);
    }
    this._isTransitioning = !0;
    const r = () => {
      this._isTransitioning = !1, this._element.classList.remove(ea), this._element.classList.add(gi), de.trigger(this._element, Kv);
    };
    this._element.style[e] = "", this._queueCallback(r, this._element, !0);
  }
  _isShown(n = this._element) {
    return n.classList.contains(zo);
  }
  // Private
  _configAfterMerge(n) {
    return n.toggle = !!n.toggle, n.parent = ur(n.parent), n;
  }
  _getDimension() {
    return this._element.classList.contains(Zv) ? eb : tb;
  }
  _initializeChildren() {
    if (!this._config.parent)
      return;
    const n = this._getFirstLevelChildren(Dl);
    for (const e of n) {
      const r = Se.getElementFromSelector(e);
      r && this._addAriaAndCollapsedClass([e], this._isShown(r));
    }
  }
  _getFirstLevelChildren(n) {
    const e = Se.find(Jv, this._config.parent);
    return Se.find(n, this._config.parent).filter((r) => !e.includes(r));
  }
  _addAriaAndCollapsedClass(n, e) {
    if (n.length)
      for (const r of n)
        r.classList.toggle(Qv, !e), r.setAttribute("aria-expanded", e);
  }
  // Static
  static jQueryInterface(n) {
    const e = {};
    return typeof n == "string" && /show|hide/.test(n) && (e.toggle = !1), this.each(function() {
      const r = Fr.getOrCreateInstance(this, e);
      if (typeof n == "string") {
        if (typeof r[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        r[n]();
      }
    });
  }
}
de.on(document, Xv, Dl, function(t) {
  (t.target.tagName === "A" || t.delegateTarget && t.delegateTarget.tagName === "A") && t.preventDefault();
  for (const n of Se.getMultipleElementsFromSelector(this))
    Fr.getOrCreateInstance(n, {
      toggle: !1
    }).toggle();
});
Cn(Fr);
const Uc = "dropdown", sb = "bs.dropdown", jr = `.${sb}`, wu = ".data-api", ab = "Escape", Kc = "Tab", ob = "ArrowUp", Xc = "ArrowDown", lb = 2, ub = `hide${jr}`, cb = `hidden${jr}`, db = `show${jr}`, fb = `shown${jr}`, oh = `click${jr}${wu}`, lh = `keydown${jr}${wu}`, hb = `keyup${jr}${wu}`, hi = "show", pb = "dropup", gb = "dropend", mb = "dropstart", vb = "dropup-center", bb = "dropdown-center", kr = '[data-bs-toggle="dropdown"]:not(.disabled):not(:disabled)', yb = `${kr}.${hi}`, ga = ".dropdown-menu", wb = ".navbar", _b = ".navbar-nav", Cb = ".dropdown-menu .dropdown-item:not(.disabled):not(:disabled)", xb = bn() ? "top-end" : "top-start", Sb = bn() ? "top-start" : "top-end", Tb = bn() ? "bottom-end" : "bottom-start", Db = bn() ? "bottom-start" : "bottom-end", Eb = bn() ? "left-start" : "right-start", Ab = bn() ? "right-start" : "left-start", kb = "top", Pb = "bottom", Ob = {
  autoClose: !0,
  boundary: "clippingParents",
  display: "dynamic",
  offset: [0, 2],
  popperConfig: null,
  reference: "toggle"
}, Ib = {
  autoClose: "(boolean|string)",
  boundary: "(string|element)",
  display: "string",
  offset: "(array|string|function)",
  popperConfig: "(null|object|function)",
  reference: "(string|element|object)"
};
class An extends kn {
  constructor(n, e) {
    super(n, e), this._popper = null, this._parent = this._element.parentNode, this._menu = Se.next(this._element, ga)[0] || Se.prev(this._element, ga)[0] || Se.findOne(ga, this._parent), this._inNavbar = this._detectNavbar();
  }
  // Getters
  static get Default() {
    return Ob;
  }
  static get DefaultType() {
    return Ib;
  }
  static get NAME() {
    return Uc;
  }
  // Public
  toggle() {
    return this._isShown() ? this.hide() : this.show();
  }
  show() {
    if (cr(this._element) || this._isShown())
      return;
    const n = {
      relatedTarget: this._element
    };
    if (!de.trigger(this._element, db, n).defaultPrevented) {
      if (this._createPopper(), "ontouchstart" in document.documentElement && !this._parent.closest(_b))
        for (const r of [].concat(...document.body.children))
          de.on(r, "mouseover", Pa);
      this._element.focus(), this._element.setAttribute("aria-expanded", !0), this._menu.classList.add(hi), this._element.classList.add(hi), de.trigger(this._element, fb, n);
    }
  }
  hide() {
    if (cr(this._element) || !this._isShown())
      return;
    const n = {
      relatedTarget: this._element
    };
    this._completeHide(n);
  }
  dispose() {
    this._popper && this._popper.destroy(), super.dispose();
  }
  update() {
    this._inNavbar = this._detectNavbar(), this._popper && this._popper.update();
  }
  // Private
  _completeHide(n) {
    if (!de.trigger(this._element, ub, n).defaultPrevented) {
      if ("ontouchstart" in document.documentElement)
        for (const r of [].concat(...document.body.children))
          de.off(r, "mouseover", Pa);
      this._popper && this._popper.destroy(), this._menu.classList.remove(hi), this._element.classList.remove(hi), this._element.setAttribute("aria-expanded", "false"), Vn.removeDataAttribute(this._menu, "popper"), de.trigger(this._element, cb, n);
    }
  }
  _getConfig(n) {
    if (n = super._getConfig(n), typeof n.reference == "object" && !$n(n.reference) && typeof n.reference.getBoundingClientRect != "function")
      throw new TypeError(`${Uc.toUpperCase()}: Option "reference" provided type "object" without a required "getBoundingClientRect" method.`);
    return n;
  }
  _createPopper() {
    if (typeof zf > "u")
      throw new TypeError("Bootstrap's dropdowns require Popper (https://popper.js.org)");
    let n = this._element;
    this._config.reference === "parent" ? n = this._parent : $n(this._config.reference) ? n = ur(this._config.reference) : typeof this._config.reference == "object" && (n = this._config.reference);
    const e = this._getPopperConfig();
    this._popper = vu(n, this._menu, e);
  }
  _isShown() {
    return this._menu.classList.contains(hi);
  }
  _getPlacement() {
    const n = this._parent;
    if (n.classList.contains(gb))
      return Eb;
    if (n.classList.contains(mb))
      return Ab;
    if (n.classList.contains(vb))
      return kb;
    if (n.classList.contains(bb))
      return Pb;
    const e = getComputedStyle(this._menu).getPropertyValue("--bs-position").trim() === "end";
    return n.classList.contains(pb) ? e ? Sb : xb : e ? Db : Tb;
  }
  _detectNavbar() {
    return this._element.closest(wb) !== null;
  }
  _getOffset() {
    const {
      offset: n
    } = this._config;
    return typeof n == "string" ? n.split(",").map((e) => Number.parseInt(e, 10)) : typeof n == "function" ? (e) => n(e, this._element) : n;
  }
  _getPopperConfig() {
    const n = {
      placement: this._getPlacement(),
      modifiers: [{
        name: "preventOverflow",
        options: {
          boundary: this._config.boundary
        }
      }, {
        name: "offset",
        options: {
          offset: this._getOffset()
        }
      }]
    };
    return (this._inNavbar || this._config.display === "static") && (Vn.setDataAttribute(this._menu, "popper", "static"), n.modifiers = [{
      name: "applyStyles",
      enabled: !1
    }]), {
      ...n,
      ...Gt(this._config.popperConfig, [n])
    };
  }
  _selectMenuItem({
    key: n,
    target: e
  }) {
    const r = Se.find(Cb, this._menu).filter((i) => Fi(i));
    r.length && bu(r, e, n === Xc, !r.includes(e)).focus();
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = An.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (typeof e[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
  static clearMenus(n) {
    if (n.button === lb || n.type === "keyup" && n.key !== Kc)
      return;
    const e = Se.find(yb);
    for (const r of e) {
      const i = An.getInstance(r);
      if (!i || i._config.autoClose === !1)
        continue;
      const s = n.composedPath(), a = s.includes(i._menu);
      if (s.includes(i._element) || i._config.autoClose === "inside" && !a || i._config.autoClose === "outside" && a || i._menu.contains(n.target) && (n.type === "keyup" && n.key === Kc || /input|select|option|textarea|form/i.test(n.target.tagName)))
        continue;
      const o = {
        relatedTarget: i._element
      };
      n.type === "click" && (o.clickEvent = n), i._completeHide(o);
    }
  }
  static dataApiKeydownHandler(n) {
    const e = /input|textarea/i.test(n.target.tagName), r = n.key === ab, i = [ob, Xc].includes(n.key);
    if (!i && !r || e && !r)
      return;
    n.preventDefault();
    const s = this.matches(kr) ? this : Se.prev(this, kr)[0] || Se.next(this, kr)[0] || Se.findOne(kr, n.delegateTarget.parentNode), a = An.getOrCreateInstance(s);
    if (i) {
      n.stopPropagation(), a.show(), a._selectMenuItem(n);
      return;
    }
    a._isShown() && (n.stopPropagation(), a.hide(), s.focus());
  }
}
de.on(document, lh, kr, An.dataApiKeydownHandler);
de.on(document, lh, ga, An.dataApiKeydownHandler);
de.on(document, oh, An.clearMenus);
de.on(document, hb, An.clearMenus);
de.on(document, oh, kr, function(t) {
  t.preventDefault(), An.getOrCreateInstance(this).toggle();
});
Cn(An);
const uh = "backdrop", Nb = "fade", Qc = "show", Jc = `mousedown.bs.${uh}`, Rb = {
  className: "modal-backdrop",
  clickCallback: null,
  isAnimated: !1,
  isVisible: !0,
  // if false, we use the backdrop helper without adding any element to the dom
  rootElement: "body"
  // give the choice to place backdrop under different elements
}, Lb = {
  className: "string",
  clickCallback: "(function|null)",
  isAnimated: "boolean",
  isVisible: "boolean",
  rootElement: "(element|string)"
};
class ch extends Ts {
  constructor(n) {
    super(), this._config = this._getConfig(n), this._isAppended = !1, this._element = null;
  }
  // Getters
  static get Default() {
    return Rb;
  }
  static get DefaultType() {
    return Lb;
  }
  static get NAME() {
    return uh;
  }
  // Public
  show(n) {
    if (!this._config.isVisible) {
      Gt(n);
      return;
    }
    this._append();
    const e = this._getElement();
    this._config.isAnimated && Ss(e), e.classList.add(Qc), this._emulateAnimation(() => {
      Gt(n);
    });
  }
  hide(n) {
    if (!this._config.isVisible) {
      Gt(n);
      return;
    }
    this._getElement().classList.remove(Qc), this._emulateAnimation(() => {
      this.dispose(), Gt(n);
    });
  }
  dispose() {
    this._isAppended && (de.off(this._element, Jc), this._element.remove(), this._isAppended = !1);
  }
  // Private
  _getElement() {
    if (!this._element) {
      const n = document.createElement("div");
      n.className = this._config.className, this._config.isAnimated && n.classList.add(Nb), this._element = n;
    }
    return this._element;
  }
  _configAfterMerge(n) {
    return n.rootElement = ur(n.rootElement), n;
  }
  _append() {
    if (this._isAppended)
      return;
    const n = this._getElement();
    this._config.rootElement.append(n), de.on(n, Jc, () => {
      Gt(this._config.clickCallback);
    }), this._isAppended = !0;
  }
  _emulateAnimation(n) {
    Kf(n, this._getElement(), this._config.isAnimated);
  }
}
const Mb = "focustrap", Fb = "bs.focustrap", Ia = `.${Fb}`, Bb = `focusin${Ia}`, Hb = `keydown.tab${Ia}`, $b = "Tab", Vb = "forward", Zc = "backward", Wb = {
  autofocus: !0,
  trapElement: null
  // The element to trap focus inside of
}, jb = {
  autofocus: "boolean",
  trapElement: "element"
};
class dh extends Ts {
  constructor(n) {
    super(), this._config = this._getConfig(n), this._isActive = !1, this._lastTabNavDirection = null;
  }
  // Getters
  static get Default() {
    return Wb;
  }
  static get DefaultType() {
    return jb;
  }
  static get NAME() {
    return Mb;
  }
  // Public
  activate() {
    this._isActive || (this._config.autofocus && this._config.trapElement.focus(), de.off(document, Ia), de.on(document, Bb, (n) => this._handleFocusin(n)), de.on(document, Hb, (n) => this._handleKeydown(n)), this._isActive = !0);
  }
  deactivate() {
    this._isActive && (this._isActive = !1, de.off(document, Ia));
  }
  // Private
  _handleFocusin(n) {
    const {
      trapElement: e
    } = this._config;
    if (n.target === document || n.target === e || e.contains(n.target))
      return;
    const r = Se.focusableChildren(e);
    r.length === 0 ? e.focus() : this._lastTabNavDirection === Zc ? r[r.length - 1].focus() : r[0].focus();
  }
  _handleKeydown(n) {
    n.key === $b && (this._lastTabNavDirection = n.shiftKey ? Zc : Vb);
  }
}
const ed = ".fixed-top, .fixed-bottom, .is-fixed, .sticky-top", td = ".sticky-top", ta = "padding-right", nd = "margin-right";
class El {
  constructor() {
    this._element = document.body;
  }
  // Public
  getWidth() {
    const n = document.documentElement.clientWidth;
    return Math.abs(window.innerWidth - n);
  }
  hide() {
    const n = this.getWidth();
    this._disableOverFlow(), this._setElementAttributes(this._element, ta, (e) => e + n), this._setElementAttributes(ed, ta, (e) => e + n), this._setElementAttributes(td, nd, (e) => e - n);
  }
  reset() {
    this._resetElementAttributes(this._element, "overflow"), this._resetElementAttributes(this._element, ta), this._resetElementAttributes(ed, ta), this._resetElementAttributes(td, nd);
  }
  isOverflowing() {
    return this.getWidth() > 0;
  }
  // Private
  _disableOverFlow() {
    this._saveInitialAttribute(this._element, "overflow"), this._element.style.overflow = "hidden";
  }
  _setElementAttributes(n, e, r) {
    const i = this.getWidth(), s = (a) => {
      if (a !== this._element && window.innerWidth > a.clientWidth + i)
        return;
      this._saveInitialAttribute(a, e);
      const o = window.getComputedStyle(a).getPropertyValue(e);
      a.style.setProperty(e, `${r(Number.parseFloat(o))}px`);
    };
    this._applyManipulationCallback(n, s);
  }
  _saveInitialAttribute(n, e) {
    const r = n.style.getPropertyValue(e);
    r && Vn.setDataAttribute(n, e, r);
  }
  _resetElementAttributes(n, e) {
    const r = (i) => {
      const s = Vn.getDataAttribute(i, e);
      if (s === null) {
        i.style.removeProperty(e);
        return;
      }
      Vn.removeDataAttribute(i, e), i.style.setProperty(e, s);
    };
    this._applyManipulationCallback(n, r);
  }
  _applyManipulationCallback(n, e) {
    if ($n(n)) {
      e(n);
      return;
    }
    for (const r of Se.find(n, this._element))
      e(r);
  }
}
const zb = "modal", Yb = "bs.modal", yn = `.${Yb}`, qb = ".data-api", Gb = "Escape", Ub = `hide${yn}`, Kb = `hidePrevented${yn}`, fh = `hidden${yn}`, hh = `show${yn}`, Xb = `shown${yn}`, Qb = `resize${yn}`, Jb = `click.dismiss${yn}`, Zb = `mousedown.dismiss${yn}`, ey = `keydown.dismiss${yn}`, ty = `click${yn}${qb}`, rd = "modal-open", ny = "fade", id = "show", Yo = "modal-static", ry = ".modal.show", iy = ".modal-dialog", sy = ".modal-body", ay = '[data-bs-toggle="modal"]', oy = {
  backdrop: !0,
  focus: !0,
  keyboard: !0
}, ly = {
  backdrop: "(boolean|string)",
  focus: "boolean",
  keyboard: "boolean"
};
class Br extends kn {
  constructor(n, e) {
    super(n, e), this._dialog = Se.findOne(iy, this._element), this._backdrop = this._initializeBackDrop(), this._focustrap = this._initializeFocusTrap(), this._isShown = !1, this._isTransitioning = !1, this._scrollBar = new El(), this._addEventListeners();
  }
  // Getters
  static get Default() {
    return oy;
  }
  static get DefaultType() {
    return ly;
  }
  static get NAME() {
    return zb;
  }
  // Public
  toggle(n) {
    return this._isShown ? this.hide() : this.show(n);
  }
  show(n) {
    this._isShown || this._isTransitioning || de.trigger(this._element, hh, {
      relatedTarget: n
    }).defaultPrevented || (this._isShown = !0, this._isTransitioning = !0, this._scrollBar.hide(), document.body.classList.add(rd), this._adjustDialog(), this._backdrop.show(() => this._showElement(n)));
  }
  hide() {
    !this._isShown || this._isTransitioning || de.trigger(this._element, Ub).defaultPrevented || (this._isShown = !1, this._isTransitioning = !0, this._focustrap.deactivate(), this._element.classList.remove(id), this._queueCallback(() => this._hideModal(), this._element, this._isAnimated()));
  }
  dispose() {
    de.off(window, yn), de.off(this._dialog, yn), this._backdrop.dispose(), this._focustrap.deactivate(), super.dispose();
  }
  handleUpdate() {
    this._adjustDialog();
  }
  // Private
  _initializeBackDrop() {
    return new ch({
      isVisible: !!this._config.backdrop,
      // 'static' option will be translated to true, and booleans will keep their value,
      isAnimated: this._isAnimated()
    });
  }
  _initializeFocusTrap() {
    return new dh({
      trapElement: this._element
    });
  }
  _showElement(n) {
    document.body.contains(this._element) || document.body.append(this._element), this._element.style.display = "block", this._element.removeAttribute("aria-hidden"), this._element.setAttribute("aria-modal", !0), this._element.setAttribute("role", "dialog"), this._element.scrollTop = 0;
    const e = Se.findOne(sy, this._dialog);
    e && (e.scrollTop = 0), Ss(this._element), this._element.classList.add(id);
    const r = () => {
      this._config.focus && this._focustrap.activate(), this._isTransitioning = !1, de.trigger(this._element, Xb, {
        relatedTarget: n
      });
    };
    this._queueCallback(r, this._dialog, this._isAnimated());
  }
  _addEventListeners() {
    de.on(this._element, ey, (n) => {
      if (n.key === Gb) {
        if (this._config.keyboard) {
          this.hide();
          return;
        }
        this._triggerBackdropTransition();
      }
    }), de.on(window, Qb, () => {
      this._isShown && !this._isTransitioning && this._adjustDialog();
    }), de.on(this._element, Zb, (n) => {
      de.one(this._element, Jb, (e) => {
        if (!(this._element !== n.target || this._element !== e.target)) {
          if (this._config.backdrop === "static") {
            this._triggerBackdropTransition();
            return;
          }
          this._config.backdrop && this.hide();
        }
      });
    });
  }
  _hideModal() {
    this._element.style.display = "none", this._element.setAttribute("aria-hidden", !0), this._element.removeAttribute("aria-modal"), this._element.removeAttribute("role"), this._isTransitioning = !1, this._backdrop.hide(() => {
      document.body.classList.remove(rd), this._resetAdjustments(), this._scrollBar.reset(), de.trigger(this._element, fh);
    });
  }
  _isAnimated() {
    return this._element.classList.contains(ny);
  }
  _triggerBackdropTransition() {
    if (de.trigger(this._element, Kb).defaultPrevented)
      return;
    const e = this._element.scrollHeight > document.documentElement.clientHeight, r = this._element.style.overflowY;
    r === "hidden" || this._element.classList.contains(Yo) || (e || (this._element.style.overflowY = "hidden"), this._element.classList.add(Yo), this._queueCallback(() => {
      this._element.classList.remove(Yo), this._queueCallback(() => {
        this._element.style.overflowY = r;
      }, this._dialog);
    }, this._dialog), this._element.focus());
  }
  /**
   * The following methods are used to handle overflowing modals
   */
  _adjustDialog() {
    const n = this._element.scrollHeight > document.documentElement.clientHeight, e = this._scrollBar.getWidth(), r = e > 0;
    if (r && !n) {
      const i = bn() ? "paddingLeft" : "paddingRight";
      this._element.style[i] = `${e}px`;
    }
    if (!r && n) {
      const i = bn() ? "paddingRight" : "paddingLeft";
      this._element.style[i] = `${e}px`;
    }
  }
  _resetAdjustments() {
    this._element.style.paddingLeft = "", this._element.style.paddingRight = "";
  }
  // Static
  static jQueryInterface(n, e) {
    return this.each(function() {
      const r = Br.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (typeof r[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        r[n](e);
      }
    });
  }
}
de.on(document, ty, ay, function(t) {
  const n = Se.getElementFromSelector(this);
  ["A", "AREA"].includes(this.tagName) && t.preventDefault(), de.one(n, hh, (i) => {
    i.defaultPrevented || de.one(n, fh, () => {
      Fi(this) && this.focus();
    });
  });
  const e = Se.findOne(ry);
  e && Br.getInstance(e).hide(), Br.getOrCreateInstance(n).toggle(this);
});
no(Br);
Cn(Br);
const uy = "offcanvas", cy = "bs.offcanvas", Gn = `.${cy}`, ph = ".data-api", dy = `load${Gn}${ph}`, fy = "Escape", sd = "show", ad = "showing", od = "hiding", hy = "offcanvas-backdrop", gh = ".offcanvas.show", py = `show${Gn}`, gy = `shown${Gn}`, my = `hide${Gn}`, ld = `hidePrevented${Gn}`, mh = `hidden${Gn}`, vy = `resize${Gn}`, by = `click${Gn}${ph}`, yy = `keydown.dismiss${Gn}`, wy = '[data-bs-toggle="offcanvas"]', _y = {
  backdrop: !0,
  keyboard: !0,
  scroll: !1
}, Cy = {
  backdrop: "(boolean|string)",
  keyboard: "boolean",
  scroll: "boolean"
};
class Yn extends kn {
  constructor(n, e) {
    super(n, e), this._isShown = !1, this._backdrop = this._initializeBackDrop(), this._focustrap = this._initializeFocusTrap(), this._addEventListeners();
  }
  // Getters
  static get Default() {
    return _y;
  }
  static get DefaultType() {
    return Cy;
  }
  static get NAME() {
    return uy;
  }
  // Public
  toggle(n) {
    return this._isShown ? this.hide() : this.show(n);
  }
  show(n) {
    if (this._isShown || de.trigger(this._element, py, {
      relatedTarget: n
    }).defaultPrevented)
      return;
    this._isShown = !0, this._backdrop.show(), this._config.scroll || new El().hide(), this._element.setAttribute("aria-modal", !0), this._element.setAttribute("role", "dialog"), this._element.classList.add(ad);
    const r = () => {
      (!this._config.scroll || this._config.backdrop) && this._focustrap.activate(), this._element.classList.add(sd), this._element.classList.remove(ad), de.trigger(this._element, gy, {
        relatedTarget: n
      });
    };
    this._queueCallback(r, this._element, !0);
  }
  hide() {
    if (!this._isShown || de.trigger(this._element, my).defaultPrevented)
      return;
    this._focustrap.deactivate(), this._element.blur(), this._isShown = !1, this._element.classList.add(od), this._backdrop.hide();
    const e = () => {
      this._element.classList.remove(sd, od), this._element.removeAttribute("aria-modal"), this._element.removeAttribute("role"), this._config.scroll || new El().reset(), de.trigger(this._element, mh);
    };
    this._queueCallback(e, this._element, !0);
  }
  dispose() {
    this._backdrop.dispose(), this._focustrap.deactivate(), super.dispose();
  }
  // Private
  _initializeBackDrop() {
    const n = () => {
      if (this._config.backdrop === "static") {
        de.trigger(this._element, ld);
        return;
      }
      this.hide();
    }, e = !!this._config.backdrop;
    return new ch({
      className: hy,
      isVisible: e,
      isAnimated: !0,
      rootElement: this._element.parentNode,
      clickCallback: e ? n : null
    });
  }
  _initializeFocusTrap() {
    return new dh({
      trapElement: this._element
    });
  }
  _addEventListeners() {
    de.on(this._element, yy, (n) => {
      if (n.key === fy) {
        if (this._config.keyboard) {
          this.hide();
          return;
        }
        de.trigger(this._element, ld);
      }
    });
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Yn.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (e[n] === void 0 || n.startsWith("_") || n === "constructor")
          throw new TypeError(`No method named "${n}"`);
        e[n](this);
      }
    });
  }
}
de.on(document, by, wy, function(t) {
  const n = Se.getElementFromSelector(this);
  if (["A", "AREA"].includes(this.tagName) && t.preventDefault(), cr(this))
    return;
  de.one(n, mh, () => {
    Fi(this) && this.focus();
  });
  const e = Se.findOne(gh);
  e && e !== n && Yn.getInstance(e).hide(), Yn.getOrCreateInstance(n).toggle(this);
});
de.on(window, dy, () => {
  for (const t of Se.find(gh))
    Yn.getOrCreateInstance(t).show();
});
de.on(window, vy, () => {
  for (const t of Se.find("[aria-modal][class*=show][class*=offcanvas-]"))
    getComputedStyle(t).position !== "fixed" && Yn.getOrCreateInstance(t).hide();
});
no(Yn);
Cn(Yn);
const xy = /^aria-[\w-]*$/i, vh = {
  // Global attributes allowed on any supplied element below.
  "*": ["class", "dir", "id", "lang", "role", xy],
  a: ["target", "href", "title", "rel"],
  area: [],
  b: [],
  br: [],
  col: [],
  code: [],
  div: [],
  em: [],
  hr: [],
  h1: [],
  h2: [],
  h3: [],
  h4: [],
  h5: [],
  h6: [],
  i: [],
  img: ["src", "srcset", "alt", "title", "width", "height"],
  li: [],
  ol: [],
  p: [],
  pre: [],
  s: [],
  small: [],
  span: [],
  sub: [],
  sup: [],
  strong: [],
  u: [],
  ul: []
}, Sy = /* @__PURE__ */ new Set(["background", "cite", "href", "itemtype", "longdesc", "poster", "src", "xlink:href"]), Ty = /^(?!javascript:)(?:[a-z0-9+.-]+:|[^&:/?#]*(?:[/?#]|$))/i, Dy = (t, n) => {
  const e = t.nodeName.toLowerCase();
  return n.includes(e) ? Sy.has(e) ? !!Ty.test(t.nodeValue) : !0 : n.filter((r) => r instanceof RegExp).some((r) => r.test(e));
};
function Ey(t, n, e) {
  if (!t.length)
    return t;
  if (e && typeof e == "function")
    return e(t);
  const i = new window.DOMParser().parseFromString(t, "text/html"), s = [].concat(...i.body.querySelectorAll("*"));
  for (const a of s) {
    const o = a.nodeName.toLowerCase();
    if (!Object.keys(n).includes(o)) {
      a.remove();
      continue;
    }
    const l = [].concat(...a.attributes), c = [].concat(n["*"] || [], n[o] || []);
    for (const f of l)
      Dy(f, c) || a.removeAttribute(f.nodeName);
  }
  return i.body.innerHTML;
}
const Ay = "TemplateFactory", ky = {
  allowList: vh,
  content: {},
  // { selector : text ,  selector2 : text2 , }
  extraClass: "",
  html: !1,
  sanitize: !0,
  sanitizeFn: null,
  template: "<div></div>"
}, Py = {
  allowList: "object",
  content: "object",
  extraClass: "(string|function)",
  html: "boolean",
  sanitize: "boolean",
  sanitizeFn: "(null|function)",
  template: "string"
}, Oy = {
  entry: "(string|element|function|null)",
  selector: "(string|element)"
};
class Iy extends Ts {
  constructor(n) {
    super(), this._config = this._getConfig(n);
  }
  // Getters
  static get Default() {
    return ky;
  }
  static get DefaultType() {
    return Py;
  }
  static get NAME() {
    return Ay;
  }
  // Public
  getContent() {
    return Object.values(this._config.content).map((n) => this._resolvePossibleFunction(n)).filter(Boolean);
  }
  hasContent() {
    return this.getContent().length > 0;
  }
  changeContent(n) {
    return this._checkContent(n), this._config.content = {
      ...this._config.content,
      ...n
    }, this;
  }
  toHtml() {
    const n = document.createElement("div");
    n.innerHTML = this._maybeSanitize(this._config.template);
    for (const [i, s] of Object.entries(this._config.content))
      this._setContent(n, s, i);
    const e = n.children[0], r = this._resolvePossibleFunction(this._config.extraClass);
    return r && e.classList.add(...r.split(" ")), e;
  }
  // Private
  _typeCheckConfig(n) {
    super._typeCheckConfig(n), this._checkContent(n.content);
  }
  _checkContent(n) {
    for (const [e, r] of Object.entries(n))
      super._typeCheckConfig({
        selector: e,
        entry: r
      }, Oy);
  }
  _setContent(n, e, r) {
    const i = Se.findOne(r, n);
    if (i) {
      if (e = this._resolvePossibleFunction(e), !e) {
        i.remove();
        return;
      }
      if ($n(e)) {
        this._putElementInTemplate(ur(e), i);
        return;
      }
      if (this._config.html) {
        i.innerHTML = this._maybeSanitize(e);
        return;
      }
      i.textContent = e;
    }
  }
  _maybeSanitize(n) {
    return this._config.sanitize ? Ey(n, this._config.allowList, this._config.sanitizeFn) : n;
  }
  _resolvePossibleFunction(n) {
    return Gt(n, [this]);
  }
  _putElementInTemplate(n, e) {
    if (this._config.html) {
      e.innerHTML = "", e.append(n);
      return;
    }
    e.textContent = n.textContent;
  }
}
const Ny = "tooltip", Ry = /* @__PURE__ */ new Set(["sanitize", "allowList", "sanitizeFn"]), qo = "fade", Ly = "modal", na = "show", My = ".tooltip-inner", ud = `.${Ly}`, cd = "hide.bs.modal", es = "hover", Go = "focus", Fy = "click", By = "manual", Hy = "hide", $y = "hidden", Vy = "show", Wy = "shown", jy = "inserted", zy = "click", Yy = "focusin", qy = "focusout", Gy = "mouseenter", Uy = "mouseleave", Ky = {
  AUTO: "auto",
  TOP: "top",
  RIGHT: bn() ? "left" : "right",
  BOTTOM: "bottom",
  LEFT: bn() ? "right" : "left"
}, Xy = {
  allowList: vh,
  animation: !0,
  boundary: "clippingParents",
  container: !1,
  customClass: "",
  delay: 0,
  fallbackPlacements: ["top", "right", "bottom", "left"],
  html: !1,
  offset: [0, 6],
  placement: "top",
  popperConfig: null,
  sanitize: !0,
  sanitizeFn: null,
  selector: !1,
  template: '<div class="tooltip" role="tooltip"><div class="tooltip-arrow"></div><div class="tooltip-inner"></div></div>',
  title: "",
  trigger: "hover focus"
}, Qy = {
  allowList: "object",
  animation: "boolean",
  boundary: "(string|element)",
  container: "(string|element|boolean)",
  customClass: "(string|function)",
  delay: "(number|object)",
  fallbackPlacements: "array",
  html: "boolean",
  offset: "(array|string|function)",
  placement: "(string|function)",
  popperConfig: "(null|object|function)",
  sanitize: "boolean",
  sanitizeFn: "(null|function)",
  selector: "(string|boolean)",
  template: "string",
  title: "(string|element|function)",
  trigger: "string"
};
class pr extends kn {
  constructor(n, e) {
    if (typeof zf > "u")
      throw new TypeError("Bootstrap's tooltips require Popper (https://popper.js.org)");
    super(n, e), this._isEnabled = !0, this._timeout = 0, this._isHovered = null, this._activeTrigger = {}, this._popper = null, this._templateFactory = null, this._newContent = null, this.tip = null, this._setListeners(), this._config.selector || this._fixTitle();
  }
  // Getters
  static get Default() {
    return Xy;
  }
  static get DefaultType() {
    return Qy;
  }
  static get NAME() {
    return Ny;
  }
  // Public
  enable() {
    this._isEnabled = !0;
  }
  disable() {
    this._isEnabled = !1;
  }
  toggleEnabled() {
    this._isEnabled = !this._isEnabled;
  }
  toggle() {
    if (this._isEnabled) {
      if (this._activeTrigger.click = !this._activeTrigger.click, this._isShown()) {
        this._leave();
        return;
      }
      this._enter();
    }
  }
  dispose() {
    clearTimeout(this._timeout), de.off(this._element.closest(ud), cd, this._hideModalHandler), this._element.getAttribute("data-bs-original-title") && this._element.setAttribute("title", this._element.getAttribute("data-bs-original-title")), this._disposePopper(), super.dispose();
  }
  show() {
    if (this._element.style.display === "none")
      throw new Error("Please use show on visible elements");
    if (!(this._isWithContent() && this._isEnabled))
      return;
    const n = de.trigger(this._element, this.constructor.eventName(Vy)), r = (Gf(this._element) || this._element.ownerDocument.documentElement).contains(this._element);
    if (n.defaultPrevented || !r)
      return;
    this._disposePopper();
    const i = this._getTipElement();
    this._element.setAttribute("aria-describedby", i.getAttribute("id"));
    const {
      container: s
    } = this._config;
    if (this._element.ownerDocument.documentElement.contains(this.tip) || (s.append(i), de.trigger(this._element, this.constructor.eventName(jy))), this._popper = this._createPopper(i), i.classList.add(na), "ontouchstart" in document.documentElement)
      for (const o of [].concat(...document.body.children))
        de.on(o, "mouseover", Pa);
    const a = () => {
      de.trigger(this._element, this.constructor.eventName(Wy)), this._isHovered === !1 && this._leave(), this._isHovered = !1;
    };
    this._queueCallback(a, this.tip, this._isAnimated());
  }
  hide() {
    if (!this._isShown() || de.trigger(this._element, this.constructor.eventName(Hy)).defaultPrevented)
      return;
    if (this._getTipElement().classList.remove(na), "ontouchstart" in document.documentElement)
      for (const i of [].concat(...document.body.children))
        de.off(i, "mouseover", Pa);
    this._activeTrigger[Fy] = !1, this._activeTrigger[Go] = !1, this._activeTrigger[es] = !1, this._isHovered = null;
    const r = () => {
      this._isWithActiveTrigger() || (this._isHovered || this._disposePopper(), this._element.removeAttribute("aria-describedby"), de.trigger(this._element, this.constructor.eventName($y)));
    };
    this._queueCallback(r, this.tip, this._isAnimated());
  }
  update() {
    this._popper && this._popper.update();
  }
  // Protected
  _isWithContent() {
    return !!this._getTitle();
  }
  _getTipElement() {
    return this.tip || (this.tip = this._createTipElement(this._newContent || this._getContentForTemplate())), this.tip;
  }
  _createTipElement(n) {
    const e = this._getTemplateFactory(n).toHtml();
    if (!e)
      return null;
    e.classList.remove(qo, na), e.classList.add(`bs-${this.constructor.NAME}-auto`);
    const r = Fm(this.constructor.NAME).toString();
    return e.setAttribute("id", r), this._isAnimated() && e.classList.add(qo), e;
  }
  setContent(n) {
    this._newContent = n, this._isShown() && (this._disposePopper(), this.show());
  }
  _getTemplateFactory(n) {
    return this._templateFactory ? this._templateFactory.changeContent(n) : this._templateFactory = new Iy({
      ...this._config,
      // the `content` var has to be after `this._config`
      // to override config.content in case of popover
      content: n,
      extraClass: this._resolvePossibleFunction(this._config.customClass)
    }), this._templateFactory;
  }
  _getContentForTemplate() {
    return {
      [My]: this._getTitle()
    };
  }
  _getTitle() {
    return this._resolvePossibleFunction(this._config.title) || this._element.getAttribute("data-bs-original-title");
  }
  // Private
  _initializeOnDelegatedTarget(n) {
    return this.constructor.getOrCreateInstance(n.delegateTarget, this._getDelegateConfig());
  }
  _isAnimated() {
    return this._config.animation || this.tip && this.tip.classList.contains(qo);
  }
  _isShown() {
    return this.tip && this.tip.classList.contains(na);
  }
  _createPopper(n) {
    const e = Gt(this._config.placement, [this, n, this._element]), r = Ky[e.toUpperCase()];
    return vu(this._element, n, this._getPopperConfig(r));
  }
  _getOffset() {
    const {
      offset: n
    } = this._config;
    return typeof n == "string" ? n.split(",").map((e) => Number.parseInt(e, 10)) : typeof n == "function" ? (e) => n(e, this._element) : n;
  }
  _resolvePossibleFunction(n) {
    return Gt(n, [this._element]);
  }
  _getPopperConfig(n) {
    const e = {
      placement: n,
      modifiers: [{
        name: "flip",
        options: {
          fallbackPlacements: this._config.fallbackPlacements
        }
      }, {
        name: "offset",
        options: {
          offset: this._getOffset()
        }
      }, {
        name: "preventOverflow",
        options: {
          boundary: this._config.boundary
        }
      }, {
        name: "arrow",
        options: {
          element: `.${this.constructor.NAME}-arrow`
        }
      }, {
        name: "preSetPlacement",
        enabled: !0,
        phase: "beforeMain",
        fn: (r) => {
          this._getTipElement().setAttribute("data-popper-placement", r.state.placement);
        }
      }]
    };
    return {
      ...e,
      ...Gt(this._config.popperConfig, [e])
    };
  }
  _setListeners() {
    const n = this._config.trigger.split(" ");
    for (const e of n)
      if (e === "click")
        de.on(this._element, this.constructor.eventName(zy), this._config.selector, (r) => {
          this._initializeOnDelegatedTarget(r).toggle();
        });
      else if (e !== By) {
        const r = e === es ? this.constructor.eventName(Gy) : this.constructor.eventName(Yy), i = e === es ? this.constructor.eventName(Uy) : this.constructor.eventName(qy);
        de.on(this._element, r, this._config.selector, (s) => {
          const a = this._initializeOnDelegatedTarget(s);
          a._activeTrigger[s.type === "focusin" ? Go : es] = !0, a._enter();
        }), de.on(this._element, i, this._config.selector, (s) => {
          const a = this._initializeOnDelegatedTarget(s);
          a._activeTrigger[s.type === "focusout" ? Go : es] = a._element.contains(s.relatedTarget), a._leave();
        });
      }
    this._hideModalHandler = () => {
      this._element && this.hide();
    }, de.on(this._element.closest(ud), cd, this._hideModalHandler);
  }
  _fixTitle() {
    const n = this._element.getAttribute("title");
    n && (!this._element.getAttribute("aria-label") && !this._element.textContent.trim() && this._element.setAttribute("aria-label", n), this._element.setAttribute("data-bs-original-title", n), this._element.removeAttribute("title"));
  }
  _enter() {
    if (this._isShown() || this._isHovered) {
      this._isHovered = !0;
      return;
    }
    this._isHovered = !0, this._setTimeout(() => {
      this._isHovered && this.show();
    }, this._config.delay.show);
  }
  _leave() {
    this._isWithActiveTrigger() || (this._isHovered = !1, this._setTimeout(() => {
      this._isHovered || this.hide();
    }, this._config.delay.hide));
  }
  _setTimeout(n, e) {
    clearTimeout(this._timeout), this._timeout = setTimeout(n, e);
  }
  _isWithActiveTrigger() {
    return Object.values(this._activeTrigger).includes(!0);
  }
  _getConfig(n) {
    const e = Vn.getDataAttributes(this._element);
    for (const r of Object.keys(e))
      Ry.has(r) && delete e[r];
    return n = {
      ...e,
      ...typeof n == "object" && n ? n : {}
    }, n = this._mergeConfigObj(n), n = this._configAfterMerge(n), this._typeCheckConfig(n), n;
  }
  _configAfterMerge(n) {
    return n.container = n.container === !1 ? document.body : ur(n.container), typeof n.delay == "number" && (n.delay = {
      show: n.delay,
      hide: n.delay
    }), typeof n.title == "number" && (n.title = n.title.toString()), typeof n.content == "number" && (n.content = n.content.toString()), n;
  }
  _getDelegateConfig() {
    const n = {};
    for (const [e, r] of Object.entries(this._config))
      this.constructor.Default[e] !== r && (n[e] = r);
    return n.selector = !1, n.trigger = "manual", n;
  }
  _disposePopper() {
    this._popper && (this._popper.destroy(), this._popper = null), this.tip && (this.tip.remove(), this.tip = null);
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = pr.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (typeof e[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
}
Cn(pr);
const Jy = "popover", Zy = ".popover-header", e0 = ".popover-body", t0 = {
  ...pr.Default,
  content: "",
  offset: [0, 8],
  placement: "right",
  template: '<div class="popover" role="tooltip"><div class="popover-arrow"></div><h3 class="popover-header"></h3><div class="popover-body"></div></div>',
  trigger: "click"
}, n0 = {
  ...pr.DefaultType,
  content: "(null|string|element|function)"
};
class ro extends pr {
  // Getters
  static get Default() {
    return t0;
  }
  static get DefaultType() {
    return n0;
  }
  static get NAME() {
    return Jy;
  }
  // Overrides
  _isWithContent() {
    return this._getTitle() || this._getContent();
  }
  // Private
  _getContentForTemplate() {
    return {
      [Zy]: this._getTitle(),
      [e0]: this._getContent()
    };
  }
  _getContent() {
    return this._resolvePossibleFunction(this._config.content);
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = ro.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (typeof e[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
}
Cn(ro);
const r0 = "scrollspy", i0 = "bs.scrollspy", _u = `.${i0}`, s0 = ".data-api", a0 = `activate${_u}`, dd = `click${_u}`, o0 = `load${_u}${s0}`, l0 = "dropdown-item", oi = "active", u0 = '[data-bs-spy="scroll"]', Uo = "[href]", c0 = ".nav, .list-group", fd = ".nav-link", d0 = ".nav-item", f0 = ".list-group-item", h0 = `${fd}, ${d0} > ${fd}, ${f0}`, p0 = ".dropdown", g0 = ".dropdown-toggle", m0 = {
  offset: null,
  // TODO: v6 @deprecated, keep it for backwards compatibility reasons
  rootMargin: "0px 0px -25%",
  smoothScroll: !1,
  target: null,
  threshold: [0.1, 0.5, 1]
}, v0 = {
  offset: "(number|null)",
  // TODO v6 @deprecated, keep it for backwards compatibility reasons
  rootMargin: "string",
  smoothScroll: "boolean",
  target: "element",
  threshold: "array"
};
class ks extends kn {
  constructor(n, e) {
    super(n, e), this._targetLinks = /* @__PURE__ */ new Map(), this._observableSections = /* @__PURE__ */ new Map(), this._rootElement = getComputedStyle(this._element).overflowY === "visible" ? null : this._element, this._activeTarget = null, this._observer = null, this._previousScrollData = {
      visibleEntryTop: 0,
      parentScrollTop: 0
    }, this.refresh();
  }
  // Getters
  static get Default() {
    return m0;
  }
  static get DefaultType() {
    return v0;
  }
  static get NAME() {
    return r0;
  }
  // Public
  refresh() {
    this._initializeTargetsAndObservables(), this._maybeEnableSmoothScroll(), this._observer ? this._observer.disconnect() : this._observer = this._getNewObserver();
    for (const n of this._observableSections.values())
      this._observer.observe(n);
  }
  dispose() {
    this._observer.disconnect(), super.dispose();
  }
  // Private
  _configAfterMerge(n) {
    return n.target = ur(n.target) || document.body, n.rootMargin = n.offset ? `${n.offset}px 0px -30%` : n.rootMargin, typeof n.threshold == "string" && (n.threshold = n.threshold.split(",").map((e) => Number.parseFloat(e))), n;
  }
  _maybeEnableSmoothScroll() {
    this._config.smoothScroll && (de.off(this._config.target, dd), de.on(this._config.target, dd, Uo, (n) => {
      const e = this._observableSections.get(n.target.hash);
      if (e) {
        n.preventDefault();
        const r = this._rootElement || window, i = e.offsetTop - this._element.offsetTop;
        if (r.scrollTo) {
          r.scrollTo({
            top: i,
            behavior: "smooth"
          });
          return;
        }
        r.scrollTop = i;
      }
    }));
  }
  _getNewObserver() {
    const n = {
      root: this._rootElement,
      threshold: this._config.threshold,
      rootMargin: this._config.rootMargin
    };
    return new IntersectionObserver((e) => this._observerCallback(e), n);
  }
  // The logic of selection
  _observerCallback(n) {
    const e = (a) => this._targetLinks.get(`#${a.target.id}`), r = (a) => {
      this._previousScrollData.visibleEntryTop = a.target.offsetTop, this._process(e(a));
    }, i = (this._rootElement || document.documentElement).scrollTop, s = i >= this._previousScrollData.parentScrollTop;
    this._previousScrollData.parentScrollTop = i;
    for (const a of n) {
      if (!a.isIntersecting) {
        this._activeTarget = null, this._clearActiveClass(e(a));
        continue;
      }
      const o = a.target.offsetTop >= this._previousScrollData.visibleEntryTop;
      if (s && o) {
        if (r(a), !i)
          return;
        continue;
      }
      !s && !o && r(a);
    }
  }
  _initializeTargetsAndObservables() {
    this._targetLinks = /* @__PURE__ */ new Map(), this._observableSections = /* @__PURE__ */ new Map();
    const n = Se.find(Uo, this._config.target);
    for (const e of n) {
      if (!e.hash || cr(e))
        continue;
      const r = Se.findOne(decodeURI(e.hash), this._element);
      Fi(r) && (this._targetLinks.set(decodeURI(e.hash), e), this._observableSections.set(e.hash, r));
    }
  }
  _process(n) {
    this._activeTarget !== n && (this._clearActiveClass(this._config.target), this._activeTarget = n, n.classList.add(oi), this._activateParents(n), de.trigger(this._element, a0, {
      relatedTarget: n
    }));
  }
  _activateParents(n) {
    if (n.classList.contains(l0)) {
      Se.findOne(g0, n.closest(p0)).classList.add(oi);
      return;
    }
    for (const e of Se.parents(n, c0))
      for (const r of Se.prev(e, h0))
        r.classList.add(oi);
  }
  _clearActiveClass(n) {
    n.classList.remove(oi);
    const e = Se.find(`${Uo}.${oi}`, n);
    for (const r of e)
      r.classList.remove(oi);
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = ks.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (e[n] === void 0 || n.startsWith("_") || n === "constructor")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
}
de.on(window, o0, () => {
  for (const t of Se.find(u0))
    ks.getOrCreateInstance(t);
});
Cn(ks);
const b0 = "tab", y0 = "bs.tab", zr = `.${y0}`, w0 = `hide${zr}`, _0 = `hidden${zr}`, C0 = `show${zr}`, x0 = `shown${zr}`, S0 = `click${zr}`, T0 = `keydown${zr}`, D0 = `load${zr}`, E0 = "ArrowLeft", hd = "ArrowRight", A0 = "ArrowUp", pd = "ArrowDown", Ko = "Home", gd = "End", Pr = "active", md = "fade", Xo = "show", k0 = "dropdown", bh = ".dropdown-toggle", P0 = ".dropdown-menu", Qo = `:not(${bh})`, O0 = '.list-group, .nav, [role="tablist"]', I0 = ".nav-item, .list-group-item", N0 = `.nav-link${Qo}, .list-group-item${Qo}, [role="tab"]${Qo}`, yh = '[data-bs-toggle="tab"], [data-bs-toggle="pill"], [data-bs-toggle="list"]', Jo = `${N0}, ${yh}`, R0 = `.${Pr}[data-bs-toggle="tab"], .${Pr}[data-bs-toggle="pill"], .${Pr}[data-bs-toggle="list"]`;
class Hr extends kn {
  constructor(n) {
    super(n), this._parent = this._element.closest(O0), this._parent && (this._setInitialAttributes(this._parent, this._getChildren()), de.on(this._element, T0, (e) => this._keydown(e)));
  }
  // Getters
  static get NAME() {
    return b0;
  }
  // Public
  show() {
    const n = this._element;
    if (this._elemIsActive(n))
      return;
    const e = this._getActiveElem(), r = e ? de.trigger(e, w0, {
      relatedTarget: n
    }) : null;
    de.trigger(n, C0, {
      relatedTarget: e
    }).defaultPrevented || r && r.defaultPrevented || (this._deactivate(e, n), this._activate(n, e));
  }
  // Private
  _activate(n, e) {
    if (!n)
      return;
    n.classList.add(Pr), this._activate(Se.getElementFromSelector(n));
    const r = () => {
      if (n.getAttribute("role") !== "tab") {
        n.classList.add(Xo);
        return;
      }
      n.removeAttribute("tabindex"), n.setAttribute("aria-selected", !0), this._toggleDropDown(n, !0), de.trigger(n, x0, {
        relatedTarget: e
      });
    };
    this._queueCallback(r, n, n.classList.contains(md));
  }
  _deactivate(n, e) {
    if (!n)
      return;
    n.classList.remove(Pr), n.blur(), this._deactivate(Se.getElementFromSelector(n));
    const r = () => {
      if (n.getAttribute("role") !== "tab") {
        n.classList.remove(Xo);
        return;
      }
      n.setAttribute("aria-selected", !1), n.setAttribute("tabindex", "-1"), this._toggleDropDown(n, !1), de.trigger(n, _0, {
        relatedTarget: e
      });
    };
    this._queueCallback(r, n, n.classList.contains(md));
  }
  _keydown(n) {
    if (![E0, hd, A0, pd, Ko, gd].includes(n.key))
      return;
    n.stopPropagation(), n.preventDefault();
    const e = this._getChildren().filter((i) => !cr(i));
    let r;
    if ([Ko, gd].includes(n.key))
      r = e[n.key === Ko ? 0 : e.length - 1];
    else {
      const i = [hd, pd].includes(n.key);
      r = bu(e, n.target, i, !0);
    }
    r && (r.focus({
      preventScroll: !0
    }), Hr.getOrCreateInstance(r).show());
  }
  _getChildren() {
    return Se.find(Jo, this._parent);
  }
  _getActiveElem() {
    return this._getChildren().find((n) => this._elemIsActive(n)) || null;
  }
  _setInitialAttributes(n, e) {
    this._setAttributeIfNotExists(n, "role", "tablist");
    for (const r of e)
      this._setInitialAttributesOnChild(r);
  }
  _setInitialAttributesOnChild(n) {
    n = this._getInnerElement(n);
    const e = this._elemIsActive(n), r = this._getOuterElement(n);
    n.setAttribute("aria-selected", e), r !== n && this._setAttributeIfNotExists(r, "role", "presentation"), e || n.setAttribute("tabindex", "-1"), this._setAttributeIfNotExists(n, "role", "tab"), this._setInitialAttributesOnTargetPanel(n);
  }
  _setInitialAttributesOnTargetPanel(n) {
    const e = Se.getElementFromSelector(n);
    e && (this._setAttributeIfNotExists(e, "role", "tabpanel"), n.id && this._setAttributeIfNotExists(e, "aria-labelledby", `${n.id}`));
  }
  _toggleDropDown(n, e) {
    const r = this._getOuterElement(n);
    if (!r.classList.contains(k0))
      return;
    const i = (s, a) => {
      const o = Se.findOne(s, r);
      o && o.classList.toggle(a, e);
    };
    i(bh, Pr), i(P0, Xo), r.setAttribute("aria-expanded", e);
  }
  _setAttributeIfNotExists(n, e, r) {
    n.hasAttribute(e) || n.setAttribute(e, r);
  }
  _elemIsActive(n) {
    return n.classList.contains(Pr);
  }
  // Try to get the inner element (usually the .nav-link)
  _getInnerElement(n) {
    return n.matches(Jo) ? n : Se.findOne(Jo, n);
  }
  // Try to get the outer element (usually the .nav-item)
  _getOuterElement(n) {
    return n.closest(I0) || n;
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Hr.getOrCreateInstance(this);
      if (typeof n == "string") {
        if (e[n] === void 0 || n.startsWith("_") || n === "constructor")
          throw new TypeError(`No method named "${n}"`);
        e[n]();
      }
    });
  }
}
de.on(document, S0, yh, function(t) {
  ["A", "AREA"].includes(this.tagName) && t.preventDefault(), !cr(this) && Hr.getOrCreateInstance(this).show();
});
de.on(window, D0, () => {
  for (const t of Se.find(R0))
    Hr.getOrCreateInstance(t);
});
Cn(Hr);
const L0 = "toast", M0 = "bs.toast", gr = `.${M0}`, F0 = `mouseover${gr}`, B0 = `mouseout${gr}`, H0 = `focusin${gr}`, $0 = `focusout${gr}`, V0 = `hide${gr}`, W0 = `hidden${gr}`, j0 = `show${gr}`, z0 = `shown${gr}`, Y0 = "fade", vd = "hide", ra = "show", ia = "showing", q0 = {
  animation: "boolean",
  autohide: "boolean",
  delay: "number"
}, G0 = {
  animation: !0,
  autohide: !0,
  delay: 5e3
};
class Ps extends kn {
  constructor(n, e) {
    super(n, e), this._timeout = null, this._hasMouseInteraction = !1, this._hasKeyboardInteraction = !1, this._setListeners();
  }
  // Getters
  static get Default() {
    return G0;
  }
  static get DefaultType() {
    return q0;
  }
  static get NAME() {
    return L0;
  }
  // Public
  show() {
    if (de.trigger(this._element, j0).defaultPrevented)
      return;
    this._clearTimeout(), this._config.animation && this._element.classList.add(Y0);
    const e = () => {
      this._element.classList.remove(ia), de.trigger(this._element, z0), this._maybeScheduleHide();
    };
    this._element.classList.remove(vd), Ss(this._element), this._element.classList.add(ra, ia), this._queueCallback(e, this._element, this._config.animation);
  }
  hide() {
    if (!this.isShown() || de.trigger(this._element, V0).defaultPrevented)
      return;
    const e = () => {
      this._element.classList.add(vd), this._element.classList.remove(ia, ra), de.trigger(this._element, W0);
    };
    this._element.classList.add(ia), this._queueCallback(e, this._element, this._config.animation);
  }
  dispose() {
    this._clearTimeout(), this.isShown() && this._element.classList.remove(ra), super.dispose();
  }
  isShown() {
    return this._element.classList.contains(ra);
  }
  // Private
  _maybeScheduleHide() {
    this._config.autohide && (this._hasMouseInteraction || this._hasKeyboardInteraction || (this._timeout = setTimeout(() => {
      this.hide();
    }, this._config.delay)));
  }
  _onInteraction(n, e) {
    switch (n.type) {
      case "mouseover":
      case "mouseout": {
        this._hasMouseInteraction = e;
        break;
      }
      case "focusin":
      case "focusout": {
        this._hasKeyboardInteraction = e;
        break;
      }
    }
    if (e) {
      this._clearTimeout();
      return;
    }
    const r = n.relatedTarget;
    this._element === r || this._element.contains(r) || this._maybeScheduleHide();
  }
  _setListeners() {
    de.on(this._element, F0, (n) => this._onInteraction(n, !0)), de.on(this._element, B0, (n) => this._onInteraction(n, !1)), de.on(this._element, H0, (n) => this._onInteraction(n, !0)), de.on(this._element, $0, (n) => this._onInteraction(n, !1));
  }
  _clearTimeout() {
    clearTimeout(this._timeout), this._timeout = null;
  }
  // Static
  static jQueryInterface(n) {
    return this.each(function() {
      const e = Ps.getOrCreateInstance(this, n);
      if (typeof n == "string") {
        if (typeof e[n] > "u")
          throw new TypeError(`No method named "${n}"`);
        e[n](this);
      }
    });
  }
}
no(Ps);
Cn(Ps);
var U0 = { exports: {} };
/*! tablesorter (FORK) - updated 2024-08-13 (v2.32.0)*/
(function(t) {
  (function(n) {
    t.exports = n(wa);
  })(function(n) {
    return function(e) {
      var r = e.tablesorter = {
        version: "2.32.0",
        parsers: [],
        widgets: [],
        defaults: {
          // *** appearance
          theme: "default",
          // adds tablesorter-{theme} to the table for styling
          widthFixed: !1,
          // adds colgroup to fix widths of columns
          showProcessing: !1,
          // show an indeterminate timer icon in the header when the table is sorted or filtered.
          headerTemplate: "{content}",
          // header layout template (HTML ok); {content} = innerHTML, {icon} = <i></i> // class from cssIcon
          onRenderTemplate: null,
          // function( index, template ) { return template; }, // template is a string
          onRenderHeader: null,
          // function( index ) {}, // nothing to return
          // *** functionality
          cancelSelection: !0,
          // prevent text selection in the header
          tabIndex: !0,
          // add tabindex to header for keyboard accessibility
          dateFormat: "mmddyyyy",
          // other options: 'ddmmyyy' or 'yyyymmdd'
          sortMultiSortKey: "shiftKey",
          // key used to select additional columns
          sortResetKey: "ctrlKey",
          // key used to remove sorting on a column
          usNumberFormat: !0,
          // false for German '1.234.567,89' or French '1 234 567,89'
          delayInit: !1,
          // if false, the parsed table contents will not update until the first sort
          serverSideSorting: !1,
          // if true, server-side sorting should be performed because client-side sorting will be disabled, but the ui and events will still be used.
          resort: !0,
          // default setting to trigger a resort after an 'update', 'addRows', 'updateCell', etc has completed
          // *** sort options
          headers: null,
          // set sorter, string, empty, locked order, sortInitialOrder, filter, etc.
          ignoreCase: !0,
          // ignore case while sorting
          sortForce: null,
          // column(s) first sorted; always applied
          sortList: [],
          // Initial sort order; applied initially; updated when manually sorted
          sortAppend: null,
          // column(s) sorted last; always applied
          sortStable: !1,
          // when sorting two rows with exactly the same content, the original sort order is maintained
          sortInitialOrder: "asc",
          // sort direction on first click
          sortLocaleCompare: !1,
          // replace equivalent character (accented characters)
          sortReset: !1,
          // third click on the header will reset column to default - unsorted
          sortRestart: !1,
          // restart sort to 'sortInitialOrder' when clicking on previously unsorted columns
          emptyTo: "bottom",
          // sort empty cell to bottom, top, none, zero, emptyMax, emptyMin
          stringTo: "max",
          // sort strings in numerical column as max, min, top, bottom, zero
          duplicateSpan: !0,
          // colspan cells in the tbody will have duplicated content in the cache for each spanned column
          textExtraction: "basic",
          // text extraction method/function - function( node, table, cellIndex ) {}
          textAttribute: "data-text",
          // data-attribute that contains alternate cell text (used in default textExtraction function)
          textSorter: null,
          // choose overall or specific column sorter function( a, b, direction, table, columnIndex ) [alt: ts.sortText]
          numberSorter: null,
          // choose overall numeric sorter function( a, b, direction, maxColumnValue )
          // *** widget options
          initWidgets: !0,
          // apply widgets on tablesorter initialization
          widgetClass: "widget-{name}",
          // table class name template to match to include a widget
          widgets: [],
          // method to add widgets, e.g. widgets: ['zebra']
          widgetOptions: {
            zebra: ["even", "odd"]
            // zebra widget alternating row class names
          },
          // *** callbacks
          initialized: null,
          // function( table ) {},
          // *** extra css class names
          tableClass: "",
          cssAsc: "",
          cssDesc: "",
          cssNone: "",
          cssHeader: "",
          cssHeaderRow: "",
          cssProcessing: "",
          // processing icon applied to header during sort/filter
          cssChildRow: "tablesorter-childRow",
          // class name indiciating that a row is to be attached to its parent
          cssInfoBlock: "tablesorter-infoOnly",
          // don't sort tbody with this class name (only one class name allowed here!)
          cssNoSort: "tablesorter-noSort",
          // class name added to element inside header; clicking on it won't cause a sort
          cssIgnoreRow: "tablesorter-ignoreRow",
          // header row to ignore; cells within this row will not be added to c.$headers
          cssIcon: "tablesorter-icon",
          // if this class does not exist, the {icon} will not be added from the headerTemplate
          cssIconNone: "",
          // class name added to the icon when there is no column sort
          cssIconAsc: "",
          // class name added to the icon when the column has an ascending sort
          cssIconDesc: "",
          // class name added to the icon when the column has a descending sort
          cssIconDisabled: "",
          // class name added to the icon when the column has a disabled sort
          // *** events
          pointerClick: "click",
          pointerDown: "mousedown",
          pointerUp: "mouseup",
          // *** selectors
          selectorHeaders: "> thead th, > thead td",
          selectorSort: "th, td",
          // jQuery selector of content within selectorHeaders that is clickable to trigger a sort
          selectorRemove: ".remove-me",
          // *** advanced
          debug: !1,
          // *** Internal variables
          headerList: [],
          empties: {},
          strings: {},
          parsers: [],
          // *** parser options for validator; values must be falsy!
          globalize: 0,
          imgAttr: 0
          // removed: widgetZebra: { css: ['even', 'odd'] }
        },
        // internal css classes - these will ALWAYS be added to
        // the table and MUST only contain one class name - fixes #381
        css: {
          table: "tablesorter",
          cssHasChild: "tablesorter-hasChildRow",
          childRow: "tablesorter-childRow",
          colgroup: "tablesorter-colgroup",
          header: "tablesorter-header",
          headerRow: "tablesorter-headerRow",
          headerIn: "tablesorter-header-inner",
          icon: "tablesorter-icon",
          processing: "tablesorter-processing",
          sortAsc: "tablesorter-headerAsc",
          sortDesc: "tablesorter-headerDesc",
          sortNone: "tablesorter-headerUnSorted"
        },
        // labels applied to sortable headers for accessibility (aria) support
        language: {
          sortAsc: "Ascending sort applied, ",
          sortDesc: "Descending sort applied, ",
          sortNone: "No sort applied, ",
          sortDisabled: "sorting is disabled",
          nextAsc: "activate to apply an ascending sort",
          nextDesc: "activate to apply a descending sort",
          nextNone: "activate to remove the sort"
        },
        regex: {
          templateContent: /\{content\}/g,
          templateIcon: /\{icon\}/g,
          templateName: /\{name\}/i,
          spaces: /\s+/g,
          nonWord: /\W/g,
          formElements: /(input|select|button|textarea)/i,
          // *** sort functions ***
          // regex used in natural sort
          // chunk/tokenize numbers & letters
          chunk: /(^([+\-]?(?:\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?)?$|^0x[0-9a-f]+$|\d+)/gi,
          // replace chunks @ ends
          chunks: /(^\\0|\\0$)/,
          hex: /^0x[0-9a-f]+$/i,
          // *** formatFloat ***
          comma: /,/g,
          digitNonUS: /[\s|\.]/g,
          digitNegativeTest: /^\s*\([.\d]+\)/,
          digitNegativeReplace: /^\s*\(([.\d]+)\)/,
          // *** isDigit ***
          digitTest: /^[\-+(]?\d+[)]?$/,
          digitReplace: /[,.'"\s]/g
        },
        // digit sort, text location
        string: {
          max: 1,
          min: -1,
          emptymin: 1,
          emptymax: -1,
          zero: 0,
          none: 0,
          null: 0,
          top: !0,
          bottom: !1
        },
        keyCodes: {
          enter: 13
        },
        // placeholder date parser data (globalize)
        dates: {},
        // These methods can be applied on table.config instance
        instanceMethods: {},
        /*
        ▄█████ ██████ ██████ ██  ██ █████▄
        ▀█▄    ██▄▄     ██   ██  ██ ██▄▄██
           ▀█▄ ██▀▀     ██   ██  ██ ██▀▀▀
        █████▀ ██████   ██   ▀████▀ ██
        */
        setup: function(i, s) {
          if (!i || !i.tHead || i.tBodies.length === 0 || i.hasInitialized === !0) {
            r.debug(s, "core") && (i.hasInitialized ? console.warn("Stopping initialization. Tablesorter has already been initialized") : console.error("Stopping initialization! No table, thead or tbody", i));
            return;
          }
          var a = "", o = e(i), l = e.metadata;
          i.hasInitialized = !1, i.isProcessing = !0, i.config = s, e.data(i, "tablesorter", s), r.debug(s, "core") && (console[console.group ? "group" : "log"]("Initializing tablesorter v" + r.version), e.data(i, "startoveralltimer", /* @__PURE__ */ new Date())), s.supportsDataObject = function(c) {
            return c[0] = parseInt(c[0], 10), c[0] > 1 || c[0] === 1 && parseInt(c[1], 10) >= 4;
          }(e.fn.jquery.split(".")), s.emptyTo = s.emptyTo.toLowerCase(), s.stringTo = s.stringTo.toLowerCase(), s.last = { sortList: [], clickedIndex: -1 }, /tablesorter\-/.test(o.attr("class")) || (a = s.theme !== "" ? " tablesorter-" + s.theme : ""), s.namespace ? s.namespace = "." + s.namespace.replace(r.regex.nonWord, "") : s.namespace = ".tablesorter" + Math.random().toString(16).slice(2), s.table = i, s.$table = o.addClass(r.css.table + " " + s.tableClass + a + " " + s.namespace.slice(1)).attr("role", "grid"), s.$headers = o.find(s.selectorHeaders), s.$table.children().children("tr").attr("role", "row"), s.$tbodies = o.children("tbody:not(." + s.cssInfoBlock + ")").attr({
            "aria-live": "polite",
            "aria-relevant": "all"
          }), s.$table.children("caption").length && (a = s.$table.children("caption")[0], a.id || (a.id = s.namespace.slice(1) + "caption"), s.$table.attr("aria-labelledby", a.id)), s.widgetInit = {}, s.textExtraction = s.$table.attr("data-text-extraction") || s.textExtraction || "basic", r.buildHeaders(s), r.fixColumnWidth(i), r.addWidgetFromClass(i), r.applyWidgetOptions(i), r.setupParsers(s), s.totalRows = 0, s.debug && r.validateOptions(s), s.delayInit || r.buildCache(s), r.bindEvents(i, s.$headers, !0), r.bindMethods(s), s.supportsDataObject && typeof o.data().sortlist < "u" ? s.sortList = o.data().sortlist : l && o.metadata() && o.metadata().sortlist && (s.sortList = o.metadata().sortlist), r.applyWidget(i, !0), s.sortList.length > 0 ? (s.last.sortList = s.sortList, r.sortOn(s, s.sortList, {}, !s.initWidgets)) : (r.setHeadersCss(s), s.initWidgets && r.applyWidget(i, !1)), s.showProcessing && o.unbind("sortBegin" + s.namespace + " sortEnd" + s.namespace).bind("sortBegin" + s.namespace + " sortEnd" + s.namespace, function(c) {
            clearTimeout(s.timerProcessing), r.isProcessing(i), c.type === "sortBegin" && (s.timerProcessing = setTimeout(function() {
              r.isProcessing(i, !0);
            }, 500));
          }), i.hasInitialized = !0, i.isProcessing = !1, r.debug(s, "core") && (console.log("Overall initialization time:" + r.benchmark(e.data(i, "startoveralltimer"))), r.debug(s, "core") && console.groupEnd && console.groupEnd()), o.triggerHandler("tablesorter-initialized", i), typeof s.initialized == "function" && s.initialized(i);
        },
        bindMethods: function(i) {
          var s = i.$table, a = i.namespace, o = "sortReset update updateRows updateAll updateHeaders addRows updateCell updateComplete sorton appendCache updateCache applyWidgetId applyWidgets refreshWidgets destroy mouseup mouseleave ".split(" ").join(a + " ");
          s.unbind(o.replace(r.regex.spaces, " ")).bind("sortReset" + a, function(l, c) {
            l.stopPropagation(), r.sortReset(this.config, function(f) {
              f.isApplyingWidgets ? setTimeout(function() {
                r.applyWidget(f, "", c);
              }, 100) : r.applyWidget(f, "", c);
            });
          }).bind("updateAll" + a, function(l, c, f) {
            l.stopPropagation(), r.updateAll(this.config, c, f);
          }).bind("update" + a + " updateRows" + a, function(l, c, f) {
            l.stopPropagation(), r.update(this.config, c, f);
          }).bind("updateHeaders" + a, function(l, c) {
            l.stopPropagation(), r.updateHeaders(this.config, c);
          }).bind("updateCell" + a, function(l, c, f, g) {
            l.stopPropagation(), r.updateCell(this.config, c, f, g);
          }).bind("addRows" + a, function(l, c, f, g) {
            l.stopPropagation(), r.addRows(this.config, c, f, g);
          }).bind("updateComplete" + a, function() {
            this.isUpdating = !1;
          }).bind("sorton" + a, function(l, c, f, g) {
            l.stopPropagation(), r.sortOn(this.config, c, f, g);
          }).bind("appendCache" + a, function(l, c, f) {
            l.stopPropagation(), r.appendCache(this.config, f), e.isFunction(c) && c(this);
          }).bind("updateCache" + a, function(l, c, f) {
            l.stopPropagation(), r.updateCache(this.config, c, f);
          }).bind("applyWidgetId" + a, function(l, c) {
            l.stopPropagation(), r.applyWidgetId(this, c);
          }).bind("applyWidgets" + a, function(l, c) {
            l.stopPropagation(), r.applyWidget(this, !1, c);
          }).bind("refreshWidgets" + a, function(l, c, f) {
            l.stopPropagation(), r.refreshWidgets(this, c, f);
          }).bind("removeWidget" + a, function(l, c, f) {
            l.stopPropagation(), r.removeWidget(this, c, f);
          }).bind("destroy" + a, function(l, c, f) {
            l.stopPropagation(), r.destroy(this, c, f);
          }).bind("resetToLoadState" + a, function(l) {
            l.stopPropagation(), r.removeWidget(this, !0, !1);
            var c = e.extend(!0, {}, i.originalSettings);
            i = e.extend(!0, {}, r.defaults, c), i.originalSettings = c, this.hasInitialized = !1, r.setup(this, i);
          });
        },
        bindEvents: function(i, s, a) {
          i = e(i)[0];
          var o, l = i.config, c = l.namespace, f = null;
          a !== !0 && (s.addClass(c.slice(1) + "_extra_headers"), o = r.getClosest(s, "table"), o.length && o[0].nodeName === "TABLE" && o[0] !== i && e(o[0]).addClass(c.slice(1) + "_extra_table")), o = (l.pointerDown + " " + l.pointerUp + " " + l.pointerClick + " sort keyup ").replace(r.regex.spaces, " ").split(" ").join(c + " "), s.find(l.selectorSort).add(s.filter(l.selectorSort)).unbind(o).bind(o, function(g, b) {
            var w, C, m, h = e(g.target), u = " " + g.type + " ";
            if (!((g.which || g.button) !== 1 && !u.match(" " + l.pointerClick + " | sort | keyup ") || // allow pressing enter
            u === " keyup " && g.which !== r.keyCodes.enter || // allow triggering a click event (e.which is undefined) & ignore physical clicks
            u.match(" " + l.pointerClick + " ") && typeof g.which < "u") && !(u.match(" " + l.pointerUp + " ") && f !== g.target && b !== !0)) {
              if (u.match(" " + l.pointerDown + " ")) {
                f = g.target, m = h.jquery.split("."), m[0] === "1" && m[1] < 4 && g.preventDefault();
                return;
              }
              if (f = null, w = r.getClosest(e(this), "." + r.css.header), r.regex.formElements.test(g.target.nodeName) || // nosort class name, or elements within a nosort container
              h.hasClass(l.cssNoSort) || h.parents("." + l.cssNoSort).length > 0 || // disabled cell directly clicked
              w.hasClass("sorter-false") || // elements within a button
              h.parents("button").length > 0)
                return !l.cancelSelection;
              l.delayInit && r.isEmptyObject(l.cache) && r.buildCache(l), l.last.clickedIndex = w.attr("data-column") || w.index(), C = l.$headerIndexed[l.last.clickedIndex][0], C && !C.sortDisabled && r.initSort(l, C, g);
            }
          }), l.cancelSelection && s.attr("unselectable", "on").bind("selectstart", !1).css({
            "user-select": "none",
            MozUserSelect: "none"
            // not needed for jQuery 1.8+
          });
        },
        buildHeaders: function(i) {
          var s, a, o, l;
          for (i.headerList = [], i.headerContent = [], i.sortVars = [], r.debug(i, "core") && (o = /* @__PURE__ */ new Date()), i.columns = r.computeColumnIndex(i.$table.children("thead, tfoot").children("tr")), a = i.cssIcon ? '<i class="' + (i.cssIcon === r.css.icon ? r.css.icon : i.cssIcon + " " + r.css.icon) + '"></i>' : "", i.$headers = e(e.map(i.$table.find(i.selectorHeaders), function(c, f) {
            var g, b, w, C, m, h = e(c);
            if (!r.getClosest(h, "tr").hasClass(i.cssIgnoreRow))
              return /(th|td)/i.test(c.nodeName) || (m = r.getClosest(h, "th, td"), h.attr("data-column", m.attr("data-column"))), g = r.getColumnData(i.table, i.headers, f, !0), i.headerContent[f] = h.html(), i.headerTemplate !== "" && !h.find("." + r.css.headerIn).length && (C = i.headerTemplate.replace(r.regex.templateContent, h.html()).replace(r.regex.templateIcon, h.find("." + r.css.icon).length ? "" : a), i.onRenderTemplate && (b = i.onRenderTemplate.apply(h, [f, C]), b && typeof b == "string" && (C = b)), h.html('<div class="' + r.css.headerIn + '">' + C + "</div>")), i.onRenderHeader && i.onRenderHeader.apply(h, [f, i, i.$table]), w = parseInt(h.attr("data-column"), 10), c.column = w, m = r.getOrder(r.getData(h, g, "sortInitialOrder") || i.sortInitialOrder), i.sortVars[w] = {
                count: -1,
                // set to -1 because clicking on the header automatically adds one
                order: m ? i.sortReset ? [1, 0, 2] : [1, 0] : (
                  // desc, asc, unsorted
                  i.sortReset ? [0, 1, 2] : [0, 1]
                ),
                // asc, desc, unsorted
                lockedOrder: !1,
                sortedBy: ""
              }, m = r.getData(h, g, "lockedOrder") || !1, typeof m < "u" && m !== !1 && (i.sortVars[w].lockedOrder = !0, i.sortVars[w].order = r.getOrder(m) ? [1, 1] : [0, 0]), i.headerList[f] = c, h.addClass(r.css.header + " " + i.cssHeader), r.getClosest(h, "tr").addClass(r.css.headerRow + " " + i.cssHeaderRow).attr("role", "row"), i.tabIndex && h.attr("tabindex", 0), c;
          })), i.$headerIndexed = [], l = 0; l < i.columns; l++)
            r.isEmptyObject(i.sortVars[l]) && (i.sortVars[l] = {}), s = i.$headers.filter('[data-column="' + l + '"]'), i.$headerIndexed[l] = s.length ? s.not(".sorter-false").length ? s.not(".sorter-false").filter(":last") : s.filter(":last") : e();
          i.$table.find(i.selectorHeaders).attr({
            scope: "col",
            role: "columnheader"
          }), r.updateHeader(i), r.debug(i, "core") && (console.log("Built headers:" + r.benchmark(o)), console.log(i.$headers));
        },
        // Use it to add a set of methods to table.config which will be available for all tables.
        // This should be done before table initialization
        addInstanceMethods: function(i) {
          e.extend(r.instanceMethods, i);
        },
        /*
        █████▄ ▄████▄ █████▄ ▄█████ ██████ █████▄ ▄█████
        ██▄▄██ ██▄▄██ ██▄▄██ ▀█▄    ██▄▄   ██▄▄██ ▀█▄
        ██▀▀▀  ██▀▀██ ██▀██     ▀█▄ ██▀▀   ██▀██     ▀█▄
        ██     ██  ██ ██  ██ █████▀ ██████ ██  ██ █████▀
        */
        setupParsers: function(i, s) {
          var a, o, l, c, f, g, b, w, C, m, h, u, p, v, S = i.table, T = 0, A = r.debug(i, "core"), y = {};
          if (i.$tbodies = i.$table.children("tbody:not(." + i.cssInfoBlock + ")"), p = typeof s > "u" ? i.$tbodies : s, v = p.length, v === 0)
            return A ? console.warn("Warning: *Empty table!* Not building a parser cache") : "";
          for (A && (u = /* @__PURE__ */ new Date(), console[console.group ? "group" : "log"]("Detecting parsers for each column")), o = {
            extractors: [],
            parsers: []
          }; T < v; ) {
            if (a = p[T].rows, a.length)
              for (f = 0, c = i.columns, g = 0; g < c; g++) {
                if (b = i.$headerIndexed[f], b && b.length && (w = r.getColumnData(S, i.headers, f), h = r.getParserById(r.getData(b, w, "extractor")), m = r.getParserById(r.getData(b, w, "sorter")), C = r.getData(b, w, "parser") === "false", i.empties[f] = (r.getData(b, w, "empty") || i.emptyTo || (i.emptyToBottom ? "bottom" : "top")).toLowerCase(), i.strings[f] = (r.getData(b, w, "string") || i.stringTo || "max").toLowerCase(), C && (m = r.getParserById("no-parser")), h || (h = !1), m || (m = r.detectParserForColumn(i, a, -1, f)), A && (y["(" + f + ") " + b.text()] = {
                  parser: m.id,
                  extractor: h ? h.id : "none",
                  string: i.strings[f],
                  empty: i.empties[f]
                }), o.parsers[f] = m, o.extractors[f] = h, l = b[0].colSpan - 1, l > 0))
                  for (f += l, c += l; l + 1 > 0; )
                    o.parsers[f - l] = m, o.extractors[f - l] = h, l--;
                f++;
              }
            T += o.parsers.length ? v : 1;
          }
          A && (r.isEmptyObject(y) ? console.warn("  No parsers detected!") : console[console.table ? "table" : "log"](y), console.log("Completed detecting parsers" + r.benchmark(u)), console.groupEnd && console.groupEnd()), i.parsers = o.parsers, i.extractors = o.extractors;
        },
        addParser: function(i) {
          var s, a = r.parsers.length, o = !0;
          for (s = 0; s < a; s++)
            r.parsers[s].id.toLowerCase() === i.id.toLowerCase() && (o = !1);
          o && (r.parsers[r.parsers.length] = i);
        },
        getParserById: function(i) {
          if (i == "false")
            return !1;
          var s, a = r.parsers.length;
          for (s = 0; s < a; s++)
            if (r.parsers[s].id.toLowerCase() === i.toString().toLowerCase())
              return r.parsers[s];
          return !1;
        },
        detectParserForColumn: function(i, s, a, o) {
          for (var l, c, f, g = r.parsers.length, b = !1, w = "", C = r.debug(i, "core"), m = !0; w === "" && m; )
            a++, f = s[a], f && a < 50 ? f.className.indexOf(r.cssIgnoreRow) < 0 && (b = s[a].cells[o], w = r.getElementText(i, b, o), c = e(b), C && console.log("Checking if value was empty on row " + a + ", column: " + o + ': "' + w + '"')) : m = !1;
          for (; --g >= 0; )
            if (l = r.parsers[g], l && l.id !== "text" && l.is && l.is(w, i.table, b, c))
              return l;
          return r.getParserById("text");
        },
        getElementText: function(i, s, a) {
          if (!s)
            return "";
          var o, l = i.textExtraction || "", c = s.jquery ? s : e(s);
          return typeof l == "string" ? l === "basic" && typeof (o = c.attr(i.textAttribute)) < "u" ? e.trim(o) : e.trim(s.textContent || c.text()) : typeof l == "function" ? e.trim(l(c[0], i.table, a)) : typeof (o = r.getColumnData(i.table, l, a)) == "function" ? e.trim(o(c[0], i.table, a)) : e.trim(c[0].textContent || c.text());
        },
        // centralized function to extract/parse cell contents
        getParsedText: function(i, s, a, o) {
          typeof o > "u" && (o = r.getElementText(i, s, a));
          var l = "" + o, c = i.parsers[a], f = i.extractors[a];
          return c && (f && typeof f.format == "function" && (o = f.format(o, i.table, s, a)), l = c.id === "no-parser" ? "" : (
            // make sure txt is a string (extractor may have converted it)
            c.format("" + o, i.table, s, a)
          ), i.ignoreCase && typeof l == "string" && (l = l.toLowerCase())), l;
        },
        /*
        ▄████▄ ▄████▄ ▄████▄ ██  ██ ██████
        ██  ▀▀ ██▄▄██ ██  ▀▀ ██▄▄██ ██▄▄
        ██  ▄▄ ██▀▀██ ██  ▄▄ ██▀▀██ ██▀▀
        ▀████▀ ██  ██ ▀████▀ ██  ██ ██████
        */
        buildCache: function(i, s, a) {
          var o, l, c, f, g, b, w, C, m, h, u, p, v, S, T, A, y, I, $, V, z, F, P = i.table, k = i.parsers, R = r.debug(i, "core");
          if (i.$tbodies = i.$table.children("tbody:not(." + i.cssInfoBlock + ")"), w = typeof a > "u" ? i.$tbodies : a, i.cache = {}, i.totalRows = 0, !k)
            return R ? console.warn("Warning: *Empty table!* Not building a cache") : "";
          for (R && (p = /* @__PURE__ */ new Date()), i.showProcessing && r.isProcessing(P, !0), b = 0; b < w.length; b++) {
            for (A = [], o = i.cache[b] = {
              normalized: []
              // array of normalized row data; last entry contains 'rowData' above
              // colMax: #   // added at the end
            }, v = w[b] && w[b].rows.length || 0, f = 0; f < v; ++f)
              if (S = {
                // order: original row order #
                // $row : jQuery Object[]
                child: [],
                // child row text (filter widget)
                raw: []
                // original row text
              }, C = e(w[b].rows[f]), m = [], !C.hasClass(i.selectorRemove.slice(1))) {
                if (C.hasClass(i.cssChildRow) && f !== 0) {
                  for (z = o.normalized.length - 1, T = o.normalized[z][i.columns], T.$row = T.$row.add(C), C.prev().hasClass(i.cssChildRow) || C.prev().addClass(r.css.cssHasChild), h = C.children("th, td"), z = T.child.length, T.child[z] = [], I = 0, V = i.columns, g = 0; g < V; g++)
                    u = h[g], u && (T.child[z][g] = r.getParsedText(i, u, g), y = h[g].colSpan - 1, y > 0 && (I += y, V += y)), I++;
                  continue;
                }
                for (S.$row = C, S.order = f, I = 0, V = i.columns, g = 0; g < V; ++g) {
                  if (u = C[0].cells[g], u && I < i.columns && ($ = typeof k[I] < "u", !$ && R && console.warn("No parser found for row: " + f + ", column: " + g + '; cell containing: "' + e(u).text() + '"; does it have a header?'), l = r.getElementText(i, u, I), S.raw[I] = l, c = r.getParsedText(i, u, I, l), m[I] = c, $ && (k[I].type || "").toLowerCase() === "numeric" && (A[I] = Math.max(Math.abs(c) || 0, A[I] || 0)), y = u.colSpan - 1, y > 0)) {
                    for (F = 0; F <= y; )
                      c = i.duplicateSpan || F === 0 ? c : typeof i.textExtraction != "string" && r.getElementText(i, u, I + F) || "", S.raw[I + F] = c, m[I + F] = c, F++;
                    I += y, V += y;
                  }
                  I++;
                }
                m[i.columns] = S, o.normalized[o.normalized.length] = m;
              }
            o.colMax = A, i.totalRows += o.normalized.length;
          }
          if (i.showProcessing && r.isProcessing(P), R) {
            for (z = Math.min(5, i.cache[0].normalized.length), console[console.group ? "group" : "log"]("Building cache for " + i.totalRows + " rows (showing " + z + " rows in log) and " + i.columns + " columns" + r.benchmark(p)), l = {}, g = 0; g < i.columns; g++)
              for (I = 0; I < z; I++)
                l["row: " + I] || (l["row: " + I] = {}), l["row: " + I][i.$headerIndexed[g].text()] = i.cache[0].normalized[I][g];
            console[console.table ? "table" : "log"](l), console.groupEnd && console.groupEnd();
          }
          e.isFunction(s) && s(P);
        },
        getColumnText: function(i, s, a, o) {
          i = e(i)[0];
          var l, c, f, g, b, w, C, m, h, u, p = typeof a == "function", v = s === "all", S = { raw: [], parsed: [], $cell: [] }, T = i.config;
          if (r.isEmptyObject(T))
            r.debug(T, "core") && console.warn("No cache found - aborting getColumnText function!");
          else {
            for (b = T.$tbodies.length, l = 0; l < b; l++)
              for (f = T.cache[l].normalized, w = f.length, c = 0; c < w; c++)
                g = f[c], !(o && !g[T.columns].$row.is(o)) && (u = !0, m = v ? g.slice(0, T.columns) : g[s], g = g[T.columns], C = v ? g.raw : g.raw[s], h = v ? g.$row.children() : g.$row.children().eq(s), p && (u = a({
                  tbodyIndex: l,
                  rowIndex: c,
                  parsed: m,
                  raw: C,
                  $row: g.$row,
                  $cell: h
                })), u !== !1 && (S.parsed[S.parsed.length] = m, S.raw[S.raw.length] = C, S.$cell[S.$cell.length] = h));
            return S;
          }
        },
        /*
        ██  ██ █████▄ █████▄ ▄████▄ ██████ ██████
        ██  ██ ██▄▄██ ██  ██ ██▄▄██   ██   ██▄▄
        ██  ██ ██▀▀▀  ██  ██ ██▀▀██   ██   ██▀▀
        ▀████▀ ██     █████▀ ██  ██   ██   ██████
        */
        setHeadersCss: function(i) {
          var s, a, o = i.sortList, l = o.length, c = r.css.sortNone + " " + i.cssNone, f = [r.css.sortAsc + " " + i.cssAsc, r.css.sortDesc + " " + i.cssDesc], g = [i.cssIconAsc, i.cssIconDesc, i.cssIconNone], b = ["ascending", "descending"], w = function(h, u) {
            h.removeClass(c).addClass(f[u]).attr("aria-sort", b[u]).find("." + r.css.icon).removeClass(g[2]).addClass(g[u]);
          }, C = i.$table.find("tfoot tr").children("td, th").add(e(i.namespace + "_extra_headers")).removeClass(f.join(" ")), m = i.$headers.add(e("thead " + i.namespace + "_extra_headers")).removeClass(f.join(" ")).addClass(c).attr("aria-sort", "none").find("." + r.css.icon).removeClass(g.join(" ")).end();
          for (m.not(".sorter-false").find("." + r.css.icon).addClass(g[2]), i.cssIconDisabled && m.filter(".sorter-false").find("." + r.css.icon).addClass(i.cssIconDisabled), s = 0; s < l; s++)
            if (o[s][1] !== 2) {
              if (m = i.$headers.filter(function(h) {
                for (var u = !0, p = i.$headers.eq(h), v = parseInt(p.attr("data-column"), 10), S = v + r.getClosest(p, "th, td")[0].colSpan; v < S; v++)
                  u = u ? u || r.isValueInArray(v, i.sortList) > -1 : !1;
                return u;
              }), m = m.not(".sorter-false").filter('[data-column="' + o[s][0] + '"]' + (l === 1 ? ":last" : "")), m.length)
                for (a = 0; a < m.length; a++)
                  m[a].sortDisabled || w(m.eq(a), o[s][1]);
              C.length && w(C.filter('[data-column="' + o[s][0] + '"]'), o[s][1]);
            }
          for (l = i.$headers.length, s = 0; s < l; s++)
            r.setColumnAriaLabel(i, i.$headers.eq(s));
        },
        getClosest: function(i, s) {
          return e.fn.closest ? i.closest(s) : i.is(s) ? i : i.parents(s).filter(":first");
        },
        // nextSort (optional), lets you disable next sort text
        setColumnAriaLabel: function(i, s, a) {
          if (s.length) {
            var o = parseInt(s.attr("data-column"), 10), l = i.sortVars[o], c = s.hasClass(r.css.sortAsc) ? "sortAsc" : s.hasClass(r.css.sortDesc) ? "sortDesc" : "sortNone", f = e.trim(s.text()) + ": " + r.language[c];
            s.hasClass("sorter-false") || a === !1 ? f += r.language.sortDisabled : (c = (l.count + 1) % l.order.length, a = l.order[c], f += r.language[a === 0 ? "nextAsc" : a === 1 ? "nextDesc" : "nextNone"]), s.attr("aria-label", f), l.sortedBy ? s.attr("data-sortedBy", l.sortedBy) : s.removeAttr("data-sortedBy");
          }
        },
        updateHeader: function(i) {
          var s, a, o, l, c = i.table, f = i.$headers.length;
          for (s = 0; s < f; s++)
            o = i.$headers.eq(s), l = r.getColumnData(c, i.headers, s, !0), a = r.getData(o, l, "sorter") === "false" || r.getData(o, l, "parser") === "false", r.setColumnSort(i, o, a);
        },
        setColumnSort: function(i, s, a) {
          var o = i.table.id;
          s[0].sortDisabled = a, s[a ? "addClass" : "removeClass"]("sorter-false").attr("aria-disabled", "" + a), i.tabIndex && (a ? s.removeAttr("tabindex") : s.attr("tabindex", "0")), o && (a ? s.removeAttr("aria-controls") : s.attr("aria-controls", o));
        },
        updateHeaderSortCount: function(i, s) {
          var a, o, l, c, f, g, b, w, C = s || i.sortList, m = C.length;
          for (i.sortList = [], c = 0; c < m; c++)
            if (b = C[c], a = parseInt(b[0], 10), a < i.columns) {
              switch (i.sortVars[a].order || (r.getOrder(i.sortInitialOrder) ? w = i.sortReset ? [1, 0, 2] : [1, 0] : w = i.sortReset ? [0, 1, 2] : [0, 1], i.sortVars[a].order = w, i.sortVars[a].count = 0), w = i.sortVars[a].order, o = ("" + b[1]).match(/^(1|d|s|o|n)/), o = o ? o[0] : "", o) {
                case "1":
                case "d":
                  o = 1;
                  break;
                case "s":
                  o = f || 0;
                  break;
                case "o":
                  g = w[(f || 0) % w.length], o = g === 0 ? 1 : g === 1 ? 0 : 2;
                  break;
                case "n":
                  o = w[++i.sortVars[a].count % w.length];
                  break;
                default:
                  o = 0;
                  break;
              }
              f = c === 0 ? o : f, l = [a, parseInt(o, 10) || 0], i.sortList[i.sortList.length] = l, o = e.inArray(l[1], w), i.sortVars[a].count = o >= 0 ? o : l[1] % w.length;
            }
        },
        updateAll: function(i, s, a) {
          var o = i.table;
          o.isUpdating = !0, r.refreshWidgets(o, !0, !0), r.buildHeaders(i), r.bindEvents(o, i.$headers, !0), r.bindMethods(i), r.commonUpdate(i, s, a);
        },
        update: function(i, s, a) {
          var o = i.table;
          o.isUpdating = !0, r.updateHeader(i), r.commonUpdate(i, s, a);
        },
        // simple header update - see #989
        updateHeaders: function(i, s) {
          i.table.isUpdating = !0, r.buildHeaders(i), r.bindEvents(i.table, i.$headers, !0), r.resortComplete(i, s);
        },
        updateCell: function(i, s, a, o) {
          if (e(s).closest("tr").hasClass(i.cssChildRow)) {
            console.warn('Tablesorter Warning! "updateCell" for child row content has been disabled, use "update" instead');
            return;
          }
          if (r.isEmptyObject(i.cache)) {
            r.updateHeader(i), r.commonUpdate(i, a, o);
            return;
          }
          i.table.isUpdating = !0, i.$table.find(i.selectorRemove).remove();
          var l, c, f, g, b, w, C = i.$tbodies, m = e(s), h = C.index(r.getClosest(m, "tbody")), u = i.cache[h], p = r.getClosest(m, "tr");
          if (s = m[0], C.length && h >= 0) {
            if (f = C.eq(h).find("tr").not("." + i.cssChildRow).index(p), b = u.normalized[f], w = p[0].cells.length, w !== i.columns)
              for (g = 0, l = !1, c = 0; c < w; c++)
                !l && p[0].cells[c] !== s ? g += p[0].cells[c].colSpan : l = !0;
            else
              g = m.index();
            l = r.getElementText(i, s, g), b[i.columns].raw[g] = l, l = r.getParsedText(i, s, g, l), b[g] = l, (i.parsers[g].type || "").toLowerCase() === "numeric" && (u.colMax[g] = Math.max(Math.abs(l) || 0, u.colMax[g] || 0)), l = a !== "undefined" ? a : i.resort, l !== !1 ? r.checkResort(i, l, o) : r.resortComplete(i, o);
          } else
            r.debug(i, "core") && console.error("updateCell aborted, tbody missing or not within the indicated table"), i.table.isUpdating = !1;
        },
        addRows: function(i, s, a, o) {
          var l, c, f, g, b, w, C, m, h, u, p, v, S, T = typeof s == "string" && i.$tbodies.length === 1 && /<tr/.test(s || ""), A = i.table;
          if (T)
            s = e(s), i.$tbodies.append(s);
          else if (!s || // row is a jQuery object?
          !(s instanceof e) || // row contained in the table?
          r.getClosest(s, "table")[0] !== i.table)
            return r.debug(i, "core") && console.error("addRows method requires (1) a jQuery selector reference to rows that have already been added to the table, or (2) row HTML string to be added to a table with only one tbody"), !1;
          if (A.isUpdating = !0, r.isEmptyObject(i.cache))
            r.updateHeader(i), r.commonUpdate(i, a, o);
          else {
            for (b = s.filter("tr").attr("role", "row").length, f = i.$tbodies.index(s.parents("tbody").filter(":first")), i.parsers && i.parsers.length || r.setupParsers(i), g = 0; g < b; g++) {
              for (h = 0, C = s[g].cells.length, m = i.cache[f].normalized.length, p = [], u = {
                child: [],
                raw: [],
                $row: s.eq(g),
                order: m
              }, w = 0; w < C; w++)
                v = s[g].cells[w], l = r.getElementText(i, v, h), u.raw[h] = l, c = r.getParsedText(i, v, h, l), p[h] = c, (i.parsers[h].type || "").toLowerCase() === "numeric" && (i.cache[f].colMax[h] = Math.max(Math.abs(c) || 0, i.cache[f].colMax[h] || 0)), S = v.colSpan - 1, S > 0 && (h += S), h++;
              p[i.columns] = u, i.cache[f].normalized[m] = p;
            }
            r.checkResort(i, a, o);
          }
        },
        updateCache: function(i, s, a) {
          i.parsers && i.parsers.length || r.setupParsers(i, a), r.buildCache(i, s, a);
        },
        // init flag (true) used by pager plugin to prevent widget application
        // renamed from appendToTable
        appendCache: function(i, s) {
          var a, o, l, c, f, g, b, w = i.table, C = i.$tbodies, m = [], h = i.cache;
          if (r.isEmptyObject(h))
            return i.appender ? i.appender(w, m) : w.isUpdating ? i.$table.triggerHandler("updateComplete", w) : "";
          for (r.debug(i, "core") && (b = /* @__PURE__ */ new Date()), g = 0; g < C.length; g++)
            if (l = C.eq(g), l.length) {
              for (c = r.processTbody(w, l, !0), a = h[g].normalized, o = a.length, f = 0; f < o; f++)
                m[m.length] = a[f][i.columns].$row, (!i.appender || i.pager && !i.pager.removeRows && !i.pager.ajax) && c.append(a[f][i.columns].$row);
              r.processTbody(w, c, !1);
            }
          i.appender && i.appender(w, m), r.debug(i, "core") && console.log("Rebuilt table" + r.benchmark(b)), !s && !i.appender && r.applyWidget(w), w.isUpdating && i.$table.triggerHandler("updateComplete", w);
        },
        commonUpdate: function(i, s, a) {
          i.$table.find(i.selectorRemove).remove(), r.setupParsers(i), r.buildCache(i), r.checkResort(i, s, a);
        },
        /*
        ▄█████ ▄████▄ █████▄ ██████ ██ █████▄ ▄████▄
        ▀█▄    ██  ██ ██▄▄██   ██   ██ ██  ██ ██ ▄▄▄
           ▀█▄ ██  ██ ██▀██    ██   ██ ██  ██ ██ ▀██
        █████▀ ▀████▀ ██  ██   ██   ██ ██  ██ ▀████▀
        */
        initSort: function(i, s, a) {
          if (i.table.isUpdating)
            return setTimeout(function() {
              r.initSort(i, s, a);
            }, 50);
          var o, l, c, f, g, b, w, C = !a[i.sortMultiSortKey], m = i.table, h = i.$headers.length, u = r.getClosest(e(s), "th, td"), p = parseInt(u.attr("data-column"), 10), v = a.type === "mouseup" ? "user" : a.type, S = i.sortVars[p].order;
          if (u = u[0], i.$table.triggerHandler("sortStart", m), b = (i.sortVars[p].count + 1) % S.length, i.sortVars[p].count = a[i.sortResetKey] ? 2 : b, i.sortRestart)
            for (c = 0; c < h; c++)
              w = i.$headers.eq(c), b = parseInt(w.attr("data-column"), 10), p !== b && (C || w.hasClass(r.css.sortNone)) && (i.sortVars[b].count = -1);
          if (C) {
            if (e.each(i.sortVars, function(T) {
              i.sortVars[T].sortedBy = "";
            }), i.sortList = [], i.last.sortList = [], i.sortForce !== null)
              for (o = i.sortForce, l = 0; l < o.length; l++)
                o[l][0] !== p && (i.sortList[i.sortList.length] = o[l], i.sortVars[o[l][0]].sortedBy = "sortForce");
            if (f = S[i.sortVars[p].count], f < 2 && (i.sortList[i.sortList.length] = [p, f], i.sortVars[p].sortedBy = v, u.colSpan > 1))
              for (l = 1; l < u.colSpan; l++)
                i.sortList[i.sortList.length] = [p + l, f], i.sortVars[p + l].count = e.inArray(f, S), i.sortVars[p + l].sortedBy = v;
          } else if (i.sortList = e.extend([], i.last.sortList), r.isValueInArray(p, i.sortList) >= 0)
            for (i.sortVars[p].sortedBy = v, l = 0; l < i.sortList.length; l++)
              b = i.sortList[l], b[0] === p && (b[1] = S[i.sortVars[p].count], b[1] === 2 && (i.sortList.splice(l, 1), i.sortVars[p].count = -1));
          else if (f = S[i.sortVars[p].count], i.sortVars[p].sortedBy = v, f < 2 && (i.sortList[i.sortList.length] = [p, f], u.colSpan > 1))
            for (l = 1; l < u.colSpan; l++)
              i.sortList[i.sortList.length] = [p + l, f], i.sortVars[p + l].count = e.inArray(f, S), i.sortVars[p + l].sortedBy = v;
          if (i.last.sortList = e.extend([], i.sortList), i.sortList.length && i.sortAppend && (o = e.isArray(i.sortAppend) ? i.sortAppend : i.sortAppend[i.sortList[0][0]], !r.isEmptyObject(o))) {
            for (l = 0; l < o.length; l++)
              if (o[l][0] !== p && r.isValueInArray(o[l][0], i.sortList) < 0) {
                if (f = o[l][1], g = ("" + f).match(/^(a|d|s|o|n)/), g)
                  switch (b = i.sortList[0][1], g[0]) {
                    case "d":
                      f = 1;
                      break;
                    case "s":
                      f = b;
                      break;
                    case "o":
                      f = b === 0 ? 1 : 0;
                      break;
                    case "n":
                      f = (b + 1) % S.length;
                      break;
                    default:
                      f = 0;
                      break;
                  }
                i.sortList[i.sortList.length] = [o[l][0], f], i.sortVars[o[l][0]].sortedBy = "sortAppend";
              }
          }
          i.$table.triggerHandler("sortBegin", m), setTimeout(function() {
            r.setHeadersCss(i), r.multisort(i), r.appendCache(i), i.$table.triggerHandler("sortBeforeEnd", m), i.$table.triggerHandler("sortEnd", m);
          }, 1);
        },
        // sort multiple columns
        multisort: function(i) {
          var s, a, o, l, c, f = i.table, g = [], b = 0, w = i.textSorter || "", C = i.sortList, m = C.length, h = i.$tbodies.length;
          if (!(i.serverSideSorting || r.isEmptyObject(i.cache))) {
            if (r.debug(i, "core") && (a = /* @__PURE__ */ new Date()), typeof w == "object")
              for (o = i.columns; o--; )
                c = r.getColumnData(f, w, o), typeof c == "function" && (g[o] = c);
            for (s = 0; s < h; s++)
              o = i.cache[s].colMax, l = i.cache[s].normalized, l.sort(function(u, p) {
                var v, S, T, A, y, I, $;
                for (v = 0; v < m; v++) {
                  if (T = C[v][0], A = C[v][1], b = A === 0, i.sortStable && u[T] === p[T] && m === 1)
                    return u[i.columns].order - p[i.columns].order;
                  if (S = /n/i.test(r.getSortType(i.parsers, T)), S && i.strings[T] ? (typeof r.string[i.strings[T]] == "boolean" ? S = (b ? 1 : -1) * (r.string[i.strings[T]] ? -1 : 1) : S = i.strings[T] && r.string[i.strings[T]] || 0, y = i.numberSorter ? i.numberSorter(u[T], p[T], b, o[T], f) : r["sortNumeric" + (b ? "Asc" : "Desc")](u[T], p[T], S, o[T], T, i)) : (I = b ? u : p, $ = b ? p : u, typeof w == "function" ? y = w(I[T], $[T], b, T, f) : typeof g[T] == "function" ? y = g[T](I[T], $[T], b, T, f) : y = r["sortNatural" + (b ? "Asc" : "Desc")](u[T] || "", p[T] || "", T, i)), y)
                    return y;
                }
                return u[i.columns].order - p[i.columns].order;
              });
            r.debug(i, "core") && console.log("Applying sort " + C.toString() + r.benchmark(a));
          }
        },
        resortComplete: function(i, s) {
          i.table.isUpdating && i.$table.triggerHandler("updateComplete", i.table), e.isFunction(s) && s(i.table);
        },
        checkResort: function(i, s, a) {
          var o = e.isArray(s) ? s : i.sortList, l = typeof s > "u" ? i.resort : s;
          l !== !1 && !i.serverSideSorting && !i.table.isProcessing ? o.length ? r.sortOn(i, o, function() {
            r.resortComplete(i, a);
          }, !0) : r.sortReset(i, function() {
            r.resortComplete(i, a), r.applyWidget(i.table, !1);
          }) : (r.resortComplete(i, a), r.applyWidget(i.table, !1));
        },
        sortOn: function(i, s, a, o) {
          var l, c = i.table;
          for (i.$table.triggerHandler("sortStart", c), l = 0; l < i.columns; l++)
            i.sortVars[l].sortedBy = r.isValueInArray(l, s) > -1 ? "sorton" : "";
          r.updateHeaderSortCount(i, s), r.setHeadersCss(i), i.delayInit && r.isEmptyObject(i.cache) && r.buildCache(i), i.$table.triggerHandler("sortBegin", c), r.multisort(i), r.appendCache(i, o), i.$table.triggerHandler("sortBeforeEnd", c), i.$table.triggerHandler("sortEnd", c), r.applyWidget(c), e.isFunction(a) && a(c);
        },
        sortReset: function(i, s) {
          i.sortList = [];
          var a;
          for (a = 0; a < i.columns; a++)
            i.sortVars[a].count = -1, i.sortVars[a].sortedBy = "";
          r.setHeadersCss(i), r.multisort(i), r.appendCache(i), e.isFunction(s) && s(i.table);
        },
        getSortType: function(i, s) {
          return i && i[s] && i[s].type || "";
        },
        getOrder: function(i) {
          return /^d/i.test(i) || i === 1;
        },
        // Natural sort - https://github.com/overset/javascript-natural-sort (date sorting removed)
        sortNatural: function(i, s) {
          if (i === s)
            return 0;
          i = (i || "").toString(), s = (s || "").toString();
          var a, o, l, c, f, g, b = r.regex;
          if (b.hex.test(s)) {
            if (a = parseInt(i.match(b.hex), 16), o = parseInt(s.match(b.hex), 16), a < o)
              return -1;
            if (a > o)
              return 1;
          }
          for (a = i.replace(b.chunk, "\\0$1\\0").replace(b.chunks, "").split("\\0"), o = s.replace(b.chunk, "\\0$1\\0").replace(b.chunks, "").split("\\0"), g = Math.max(a.length, o.length), f = 0; f < g; f++) {
            if (l = isNaN(a[f]) ? a[f] || 0 : parseFloat(a[f]) || 0, c = isNaN(o[f]) ? o[f] || 0 : parseFloat(o[f]) || 0, isNaN(l) !== isNaN(c))
              return isNaN(l) ? 1 : -1;
            if (typeof l != typeof c && (l += "", c += ""), l < c)
              return -1;
            if (l > c)
              return 1;
          }
          return 0;
        },
        sortNaturalAsc: function(i, s, a, o) {
          if (i === s)
            return 0;
          var l = r.string[o.empties[a] || o.emptyTo];
          return i === "" && l !== 0 ? typeof l == "boolean" ? l ? -1 : 1 : -l || -1 : s === "" && l !== 0 ? typeof l == "boolean" ? l ? 1 : -1 : l || 1 : r.sortNatural(i, s);
        },
        sortNaturalDesc: function(i, s, a, o) {
          if (i === s)
            return 0;
          var l = r.string[o.empties[a] || o.emptyTo];
          return i === "" && l !== 0 ? typeof l == "boolean" ? l ? -1 : 1 : l || 1 : s === "" && l !== 0 ? typeof l == "boolean" ? l ? 1 : -1 : -l || -1 : r.sortNatural(s, i);
        },
        // basic alphabetical sort
        sortText: function(i, s) {
          return i > s ? 1 : i < s ? -1 : 0;
        },
        // return text string value by adding up ascii value
        // so the text is somewhat sorted when using a digital sort
        // this is NOT an alphanumeric sort
        getTextValue: function(i, s, a) {
          if (a) {
            var o, l = i ? i.length : 0, c = a + s;
            for (o = 0; o < l; o++)
              c += i.charCodeAt(o);
            return s * c;
          }
          return 0;
        },
        sortNumericAsc: function(i, s, a, o, l, c) {
          if (i === s)
            return 0;
          var f = r.string[c.empties[l] || c.emptyTo];
          return i === "" && f !== 0 ? typeof f == "boolean" ? f ? -1 : 1 : -f || -1 : s === "" && f !== 0 ? typeof f == "boolean" ? f ? 1 : -1 : f || 1 : (isNaN(i) && (i = r.getTextValue(i, a, o)), isNaN(s) && (s = r.getTextValue(s, a, o)), i - s);
        },
        sortNumericDesc: function(i, s, a, o, l, c) {
          if (i === s)
            return 0;
          var f = r.string[c.empties[l] || c.emptyTo];
          return i === "" && f !== 0 ? typeof f == "boolean" ? f ? -1 : 1 : f || 1 : s === "" && f !== 0 ? typeof f == "boolean" ? f ? 1 : -1 : -f || -1 : (isNaN(i) && (i = r.getTextValue(i, a, o)), isNaN(s) && (s = r.getTextValue(s, a, o)), s - i);
        },
        sortNumeric: function(i, s) {
          return i - s;
        },
        /*
        ██ ██ ██ ██ █████▄ ▄████▄ ██████ ██████ ▄█████
        ██ ██ ██ ██ ██  ██ ██ ▄▄▄ ██▄▄     ██   ▀█▄
        ██ ██ ██ ██ ██  ██ ██ ▀██ ██▀▀     ██      ▀█▄
        ███████▀ ██ █████▀ ▀████▀ ██████   ██   █████▀
        */
        addWidget: function(i) {
          i.id && !r.isEmptyObject(r.getWidgetById(i.id)) && console.warn('"' + i.id + '" widget was loaded more than once!'), r.widgets[r.widgets.length] = i;
        },
        hasWidget: function(i, s) {
          return i = e(i), i.length && i[0].config && i[0].config.widgetInit[s] || !1;
        },
        getWidgetById: function(i) {
          var s, a, o = r.widgets.length;
          for (s = 0; s < o; s++)
            if (a = r.widgets[s], a && a.id && a.id.toLowerCase() === i.toLowerCase())
              return a;
        },
        applyWidgetOptions: function(i) {
          var s, a, o, l = i.config, c = l.widgets.length;
          if (c)
            for (s = 0; s < c; s++)
              a = r.getWidgetById(l.widgets[s]), a && a.options && (o = e.extend(!0, {}, a.options), l.widgetOptions = e.extend(!0, o, l.widgetOptions), e.extend(!0, r.defaults.widgetOptions, a.options));
        },
        addWidgetFromClass: function(i) {
          var s, a, o = i.config, l = "^" + o.widgetClass.replace(r.regex.templateName, "(\\S+)+") + "$", c = new RegExp(l, "g"), f = (i.className || "").split(r.regex.spaces);
          if (f.length)
            for (s = f.length, a = 0; a < s; a++)
              f[a].match(c) && (o.widgets[o.widgets.length] = f[a].replace(c, "$1"));
        },
        applyWidgetId: function(i, s, a) {
          i = e(i)[0];
          var o, l, c, f = i.config, g = f.widgetOptions, b = r.debug(f, "core"), w = r.getWidgetById(s);
          w && (c = w.id, o = !1, e.inArray(c, f.widgets) < 0 && (f.widgets[f.widgets.length] = c), b && (l = /* @__PURE__ */ new Date()), (a || !f.widgetInit[c]) && (f.widgetInit[c] = !0, i.hasInitialized && r.applyWidgetOptions(i), typeof w.init == "function" && (o = !0, b && console[console.group ? "group" : "log"]("Initializing " + c + " widget"), w.init(i, w, f, g))), !a && typeof w.format == "function" && (o = !0, b && console[console.group ? "group" : "log"]("Updating " + c + " widget"), w.format(i, f, g, !1)), b && o && (console.log("Completed " + (a ? "initializing " : "applying ") + c + " widget" + r.benchmark(l)), console.groupEnd && console.groupEnd()));
        },
        applyWidget: function(i, s, a) {
          i = e(i)[0];
          var o, l, c, f, g, b = i.config, w = r.debug(b, "core"), C = [];
          if (!(s !== !1 && i.hasInitialized && (i.isApplyingWidgets || i.isUpdating))) {
            if (w && (g = /* @__PURE__ */ new Date()), r.addWidgetFromClass(i), clearTimeout(b.timerReady), b.widgets.length) {
              for (i.isApplyingWidgets = !0, b.widgets = e.grep(b.widgets, function(m, h) {
                return e.inArray(m, b.widgets) === h;
              }), c = b.widgets || [], l = c.length, o = 0; o < l; o++)
                f = r.getWidgetById(c[o]), f && f.id ? (f.priority || (f.priority = 10), C[o] = f) : w && console.warn('"' + c[o] + '" was enabled, but the widget code has not been loaded!');
              for (C.sort(function(m, h) {
                return m.priority < h.priority ? -1 : m.priority === h.priority ? 0 : 1;
              }), l = C.length, w && console[console.group ? "group" : "log"]("Start " + (s ? "initializing" : "applying") + " widgets"), o = 0; o < l; o++)
                f = C[o], f && f.id && r.applyWidgetId(i, f.id, s);
              w && console.groupEnd && console.groupEnd();
            }
            b.timerReady = setTimeout(function() {
              i.isApplyingWidgets = !1, e.data(i, "lastWidgetApplication", /* @__PURE__ */ new Date()), b.$table.triggerHandler("tablesorter-ready"), !s && typeof a == "function" && a(i), w && (f = b.widgets.length, console.log("Completed " + (s === !0 ? "initializing " : "applying ") + f + " widget" + (f !== 1 ? "s" : "") + r.benchmark(g)));
            }, 10);
          }
        },
        removeWidget: function(i, s, a) {
          i = e(i)[0];
          var o, l, c, f, g = i.config;
          if (s === !0)
            for (s = [], f = r.widgets.length, c = 0; c < f; c++)
              l = r.widgets[c], l && l.id && (s[s.length] = l.id);
          else
            s = (e.isArray(s) ? s.join(",") : s || "").toLowerCase().split(/[\s,]+/);
          for (f = s.length, o = 0; o < f; o++)
            l = r.getWidgetById(s[o]), c = e.inArray(s[o], g.widgets), c >= 0 && a !== !0 && g.widgets.splice(c, 1), l && l.remove && (r.debug(g, "core") && console.log((a ? "Refreshing" : "Removing") + ' "' + s[o] + '" widget'), l.remove(i, g, g.widgetOptions, a), g.widgetInit[s[o]] = !1);
          g.$table.triggerHandler("widgetRemoveEnd", i);
        },
        refreshWidgets: function(i, s, a) {
          i = e(i)[0];
          var o, l, c = i.config, f = c.widgets, g = r.widgets, b = g.length, w = [], C = function(m) {
            e(m).triggerHandler("refreshComplete");
          };
          for (o = 0; o < b; o++)
            l = g[o], l && l.id && (s || e.inArray(l.id, f) < 0) && (w[w.length] = l.id);
          r.removeWidget(i, w.join(","), !0), a !== !0 ? (r.applyWidget(i, s || !1, C), s && r.applyWidget(i, !1, C)) : C(i);
        },
        /*
        ██  ██ ██████ ██ ██     ██ ██████ ██ ██████ ▄█████
        ██  ██   ██   ██ ██     ██   ██   ██ ██▄▄   ▀█▄
        ██  ██   ██   ██ ██     ██   ██   ██ ██▀▀      ▀█▄
        ▀████▀   ██   ██ ██████ ██   ██   ██ ██████ █████▀
        */
        benchmark: function(i) {
          return " (" + ((/* @__PURE__ */ new Date()).getTime() - i.getTime()) + " ms)";
        },
        // deprecated ts.log
        log: function() {
          console.log(arguments);
        },
        debug: function(i, s) {
          return i && (i.debug === !0 || typeof i.debug == "string" && i.debug.indexOf(s) > -1);
        },
        // $.isEmptyObject from jQuery v1.4
        isEmptyObject: function(i) {
          for (var s in i)
            return !1;
          return !0;
        },
        isValueInArray: function(i, s) {
          var a, o = s && s.length || 0;
          for (a = 0; a < o; a++)
            if (s[a][0] === i)
              return a;
          return -1;
        },
        formatFloat: function(i, s) {
          if (typeof i != "string" || i === "")
            return i;
          var a, o = s && s.config ? s.config.usNumberFormat !== !1 : typeof s < "u" ? s : !0;
          return o ? i = i.replace(r.regex.comma, "") : i = i.replace(r.regex.digitNonUS, "").replace(r.regex.comma, "."), r.regex.digitNegativeTest.test(i) && (i = i.replace(r.regex.digitNegativeReplace, "-$1")), a = parseFloat(i), isNaN(a) ? e.trim(i) : a;
        },
        isDigit: function(i) {
          return isNaN(i) ? r.regex.digitTest.test(i.toString().replace(r.regex.digitReplace, "")) : i !== "";
        },
        // computeTableHeaderCellIndexes from:
        // http://www.javascripttoolbox.com/lib/table/examples.php
        // http://www.javascripttoolbox.com/temp/table_cellindex.html
        computeColumnIndex: function(i, s) {
          var a, o, l, c, f, g, b, w, C, m, h = s && s.columns || 0, u = [], p = new Array(h);
          for (a = 0; a < i.length; a++)
            for (g = i[a].cells, o = 0; o < g.length; o++) {
              for (f = g[o], b = a, w = f.rowSpan || 1, C = f.colSpan || 1, typeof u[b] > "u" && (u[b] = []), l = 0; l < u[b].length + 1; l++)
                if (typeof u[b][l] > "u") {
                  m = l;
                  break;
                }
              for (h && f.cellIndex === m || (f.setAttribute ? f.setAttribute("data-column", m) : e(f).attr("data-column", m)), l = b; l < b + w; l++)
                for (typeof u[l] > "u" && (u[l] = []), p = u[l], c = m; c < m + C; c++)
                  p[c] = "x";
            }
          return r.checkColumnCount(i, u, p.length), p.length;
        },
        checkColumnCount: function(i, s, a) {
          var o, l, c = !0, f = [];
          for (o = 0; o < s.length; o++)
            if (s[o] && (l = s[o].length, s[o].length !== a)) {
              c = !1;
              break;
            }
          c || (i.each(function(g, b) {
            var w = b.parentElement.nodeName;
            f.indexOf(w) < 0 && f.push(w);
          }), console.error(
            "Invalid or incorrect number of columns in the " + f.join(" or ") + "; expected " + a + ", but found " + l + " columns"
          ));
        },
        // automatically add a colgroup with col elements set to a percentage width
        fixColumnWidth: function(i) {
          i = e(i)[0];
          var s, a, o, l, c, f = i.config, g = f.$table.children("colgroup");
          if (g.length && g.hasClass(r.css.colgroup) && g.remove(), f.widthFixed && f.$table.children("colgroup").length === 0) {
            for (g = e('<colgroup class="' + r.css.colgroup + '">'), s = f.$table.width(), o = f.$tbodies.find("tr:first").children(":visible"), l = o.length, c = 0; c < l; c++)
              a = parseInt(o.eq(c).width() / s * 1e3, 10) / 10 + "%", g.append(e("<col>").css("width", a));
            f.$table.prepend(g);
          }
        },
        // get sorter, string, empty, etc options for each column from
        // jQuery data, metadata, header option or header class name ('sorter-false')
        // priority = jQuery data > meta > headers option > header class name
        getData: function(i, s, a) {
          var o, l, c = "", f = e(i);
          return f.length ? (o = e.metadata ? f.metadata() : !1, l = " " + (f.attr("class") || ""), typeof f.data(a) < "u" || typeof f.data(a.toLowerCase()) < "u" ? c += f.data(a) || f.data(a.toLowerCase()) : o && typeof o[a] < "u" ? c += o[a] : s && typeof s[a] < "u" ? c += s[a] : l !== " " && l.match(" " + a + "-") && (c = l.match(new RegExp("\\s" + a + "-([\\w-]+)"))[1] || ""), e.trim(c)) : "";
        },
        getColumnData: function(i, s, a, o, l) {
          if (typeof s != "object" || s === null)
            return s;
          i = e(i)[0];
          var c, f, g = i.config, b = l || g.$headers, w = g.$headerIndexed && g.$headerIndexed[a] || b.find('[data-column="' + a + '"]:last');
          if (typeof s[a] < "u")
            return o ? s[a] : s[b.index(w)];
          for (f in s)
            if (typeof f == "string" && (c = w.filter(f).add(w.find(f)), c.length))
              return s[f];
        },
        // *** Process table ***
        // add processing indicator
        isProcessing: function(i, s, a) {
          i = e(i);
          var o = i[0].config, l = a || i.find("." + r.css.header);
          s ? (typeof a < "u" && o.sortList.length > 0 && (l = l.filter(function() {
            return this.sortDisabled ? !1 : r.isValueInArray(parseFloat(e(this).attr("data-column")), o.sortList) >= 0;
          })), i.add(l).addClass(r.css.processing + " " + o.cssProcessing)) : i.add(l).removeClass(r.css.processing + " " + o.cssProcessing);
        },
        // detach tbody but save the position
        // don't use tbody because there are portions that look for a tbody index (updateCell)
        processTbody: function(i, s, a) {
          if (i = e(i)[0], a)
            return i.isProcessing = !0, s.before('<colgroup class="tablesorter-savemyplace"/>'), e.fn.detach ? s.detach() : s.remove();
          var o = e(i).find("colgroup.tablesorter-savemyplace");
          s.insertAfter(o), o.remove(), i.isProcessing = !1;
        },
        clearTableBody: function(i) {
          e(i)[0].config.$tbodies.children().detach();
        },
        // used when replacing accented characters during sorting
        characterEquivalents: {
          a: "áàâãäąå",
          // áàâãäąå
          A: "ÁÀÂÃÄĄÅ",
          // ÁÀÂÃÄĄÅ
          c: "çćč",
          // çćč
          C: "ÇĆČ",
          // ÇĆČ
          e: "éèêëěę",
          // éèêëěę
          E: "ÉÈÊËĚĘ",
          // ÉÈÊËĚĘ
          i: "íìİîïı",
          // íìİîïı
          I: "ÍÌİÎÏ",
          // ÍÌİÎÏ
          o: "óòôõöō",
          // óòôõöō
          O: "ÓÒÔÕÖŌ",
          // ÓÒÔÕÖŌ
          ss: "ß",
          // ß (s sharp)
          SS: "ẞ",
          // ẞ (Capital sharp s)
          u: "úùûüů",
          // úùûüů
          U: "ÚÙÛÜŮ"
          // ÚÙÛÜŮ
        },
        replaceAccents: function(i) {
          var s, a = "[", o = r.characterEquivalents;
          if (!r.characterRegex) {
            r.characterRegexArray = {};
            for (s in o)
              typeof s == "string" && (a += o[s], r.characterRegexArray[s] = new RegExp("[" + o[s] + "]", "g"));
            r.characterRegex = new RegExp(a + "]");
          }
          if (r.characterRegex.test(i))
            for (s in o)
              typeof s == "string" && (i = i.replace(r.characterRegexArray[s], s));
          return i;
        },
        validateOptions: function(i) {
          var s, a, o, l, c = "headers sortForce sortList sortAppend widgets".split(" "), f = i.originalSettings;
          if (f) {
            r.debug(i, "core") && (l = /* @__PURE__ */ new Date());
            for (s in f)
              if (o = typeof r.defaults[s], o === "undefined")
                console.warn('Tablesorter Warning! "table.config.' + s + '" option not recognized');
              else if (o === "object")
                for (a in f[s])
                  o = r.defaults[s] && typeof r.defaults[s][a], e.inArray(s, c) < 0 && o === "undefined" && console.warn('Tablesorter Warning! "table.config.' + s + "." + a + '" option not recognized');
            r.debug(i, "core") && console.log("validate options time:" + r.benchmark(l));
          }
        },
        // restore headers
        restoreHeaders: function(i) {
          var s, a, o = e(i)[0].config, l = o.$table.find(o.selectorHeaders), c = l.length;
          for (s = 0; s < c; s++)
            a = l.eq(s), a.find("." + r.css.headerIn).length && a.html(o.headerContent[s]);
        },
        destroy: function(i, s, a) {
          if (i = e(i)[0], !!i.hasInitialized) {
            r.removeWidget(i, !0, !1);
            var o, l = e(i), c = i.config, f = l.find("thead:first"), g = f.find("tr." + r.css.headerRow).removeClass(r.css.headerRow + " " + c.cssHeaderRow), b = l.find("tfoot:first > tr").children("th, td");
            s === !1 && e.inArray("uitheme", c.widgets) >= 0 && (l.triggerHandler("applyWidgetId", ["uitheme"]), l.triggerHandler("applyWidgetId", ["zebra"])), f.find("tr").not(g).remove(), o = "sortReset update updateRows updateAll updateHeaders updateCell addRows updateComplete sorton appendCache updateCache applyWidgetId applyWidgets refreshWidgets removeWidget destroy mouseup mouseleave " + "keypress sortBegin sortEnd resetToLoadState ".split(" ").join(c.namespace + " "), l.removeData("tablesorter").unbind(o.replace(r.regex.spaces, " ")), c.$headers.add(b).removeClass([r.css.header, c.cssHeader, c.cssAsc, c.cssDesc, r.css.sortAsc, r.css.sortDesc, r.css.sortNone].join(" ")).removeAttr("data-column").removeAttr("aria-label").attr("aria-disabled", "true"), g.find(c.selectorSort).unbind("mousedown mouseup keypress ".split(" ").join(c.namespace + " ").replace(r.regex.spaces, " ")), r.restoreHeaders(i), l.toggleClass(r.css.table + " " + c.tableClass + " tablesorter-" + c.theme, s === !1), l.removeClass(c.namespace.slice(1)), i.hasInitialized = !1, delete i.config.cache, typeof a == "function" && a(i), r.debug(c, "core") && console.log("tablesorter has been removed");
          }
        }
      };
      e.fn.tablesorter = function(i) {
        return this.each(function() {
          var s = this, a = e.extend(!0, {}, r.defaults, i, r.instanceMethods);
          a.originalSettings = i, !s.hasInitialized && r.buildTable && this.nodeName !== "TABLE" ? r.buildTable(s, a) : r.setup(s, a);
        });
      }, window.console && window.console.log || (r.logs = [], console = {}, console.log = console.warn = console.error = console.table = function() {
        var i = arguments.length > 1 ? arguments : arguments[0];
        r.logs[r.logs.length] = { date: Date.now(), log: i };
      }), r.addParser({
        id: "no-parser",
        is: function() {
          return !1;
        },
        format: function() {
          return "";
        },
        type: "text"
      }), r.addParser({
        id: "text",
        is: function() {
          return !0;
        },
        format: function(i, s) {
          var a = s.config;
          return i && (i = e.trim(a.ignoreCase ? i.toLocaleLowerCase() : i), i = a.sortLocaleCompare ? r.replaceAccents(i) : i), i;
        },
        type: "text"
      }), r.regex.nondigit = /[^\w,. \-()]/g, r.addParser({
        id: "digit",
        is: function(i) {
          return r.isDigit(i);
        },
        format: function(i, s) {
          var a = r.formatFloat((i || "").replace(r.regex.nondigit, ""), s);
          return i && typeof a == "number" ? a : i && e.trim(i && s.config.ignoreCase ? i.toLocaleLowerCase() : i);
        },
        type: "numeric"
      }), r.regex.currencyReplace = /[+\-,. ]/g, r.regex.currencyTest = /^\(?\d+[\u00a3$\u20ac\u00a4\u00a5\u00a2?.]|[\u00a3$\u20ac\u00a4\u00a5\u00a2?.]\d+\)?$/, r.addParser({
        id: "currency",
        is: function(i) {
          return i = (i || "").replace(r.regex.currencyReplace, ""), r.regex.currencyTest.test(i);
        },
        format: function(i, s) {
          var a = r.formatFloat((i || "").replace(r.regex.nondigit, ""), s);
          return i && typeof a == "number" ? a : i && e.trim(i && s.config.ignoreCase ? i.toLocaleLowerCase() : i);
        },
        type: "numeric"
      }), r.regex.urlProtocolTest = /^(https?|ftp|file):\/\//, r.regex.urlProtocolReplace = /(https?|ftp|file):\/\/(www\.)?/, r.addParser({
        id: "url",
        is: function(i) {
          return r.regex.urlProtocolTest.test(i);
        },
        format: function(i) {
          return i && e.trim(i.replace(r.regex.urlProtocolReplace, ""));
        },
        type: "text"
      }), r.regex.dash = /-/g, r.regex.isoDate = /^\d{4}[\/\-]\d{1,2}[\/\-]\d{1,2}/, r.addParser({
        id: "isoDate",
        is: function(i) {
          return r.regex.isoDate.test(i);
        },
        format: function(i) {
          var s = i && new Date(i.replace(r.regex.dash, "/"));
          return s instanceof Date && isFinite(s) ? s.getTime() : i;
        },
        type: "numeric"
      }), r.regex.percent = /%/g, r.regex.percentTest = /(\d\s*?%|%\s*?\d)/, r.addParser({
        id: "percent",
        is: function(i) {
          return r.regex.percentTest.test(i) && i.length < 15;
        },
        format: function(i, s) {
          return i && r.formatFloat(i.replace(r.regex.percent, ""), s);
        },
        type: "numeric"
      }), r.addParser({
        id: "image",
        is: function(i, s, a, o) {
          return o.find("img").length > 0;
        },
        format: function(i, s, a) {
          return e(a).find("img").attr(s.config.imgAttr || "alt") || i;
        },
        parsed: !0,
        // filter widget flag
        type: "text"
      }), r.regex.dateReplace = /(\S)([AP]M)$/i, r.regex.usLongDateTest1 = /^[A-Z]{3,10}\.?\s+\d{1,2},?\s+(\d{4})(\s+\d{1,2}:\d{2}(:\d{2})?(\s+[AP]M)?)?$/i, r.regex.usLongDateTest2 = /^\d{1,2}\s+[A-Z]{3,10}\s+\d{4}/i, r.addParser({
        id: "usLongDate",
        is: function(i) {
          return r.regex.usLongDateTest1.test(i) || r.regex.usLongDateTest2.test(i);
        },
        format: function(i) {
          var s = i && new Date(i.replace(r.regex.dateReplace, "$1 $2"));
          return s instanceof Date && isFinite(s) ? s.getTime() : i;
        },
        type: "numeric"
      }), r.regex.shortDateTest = /(^\d{1,2}[\/\s]\d{1,2}[\/\s]\d{4})|(^\d{4}[\/\s]\d{1,2}[\/\s]\d{1,2})/, r.regex.shortDateReplace = /[\-.,]/g, r.regex.shortDateXXY = /(\d{1,2})[\/\s](\d{1,2})[\/\s](\d{4})/, r.regex.shortDateYMD = /(\d{4})[\/\s](\d{1,2})[\/\s](\d{1,2})/, r.convertFormat = function(i, s) {
        i = (i || "").replace(r.regex.spaces, " ").replace(r.regex.shortDateReplace, "/"), s === "mmddyyyy" ? i = i.replace(r.regex.shortDateXXY, "$3/$1/$2") : s === "ddmmyyyy" ? i = i.replace(r.regex.shortDateXXY, "$3/$2/$1") : s === "yyyymmdd" && (i = i.replace(r.regex.shortDateYMD, "$1/$2/$3"));
        var a = new Date(i);
        return a instanceof Date && isFinite(a) ? a.getTime() : "";
      }, r.addParser({
        id: "shortDate",
        // 'mmddyyyy', 'ddmmyyyy' or 'yyyymmdd'
        is: function(i) {
          return i = (i || "").replace(r.regex.spaces, " ").replace(r.regex.shortDateReplace, "/"), r.regex.shortDateTest.test(i);
        },
        format: function(i, s, a, o) {
          if (i) {
            var l = s.config, c = l.$headerIndexed[o], f = c.length && c.data("dateFormat") || r.getData(c, r.getColumnData(s, l.headers, o), "dateFormat") || l.dateFormat;
            return c.length && c.data("dateFormat", f), r.convertFormat(i, f) || i;
          }
          return i;
        },
        type: "numeric"
      }), r.regex.timeTest = /^(0?[1-9]|1[0-2]):([0-5]\d)(\s[AP]M)$|^((?:[01]\d|[2][0-4]):[0-5]\d)$/i, r.regex.timeMatch = /(0?[1-9]|1[0-2]):([0-5]\d)(\s[AP]M)|((?:[01]\d|[2][0-4]):[0-5]\d)/i, r.addParser({
        id: "time",
        is: function(i) {
          return r.regex.timeTest.test(i);
        },
        format: function(i) {
          var s, a = (i || "").match(r.regex.timeMatch), o = new Date(i), l = i && (a !== null ? a[0] : "00:00 AM"), c = l && /* @__PURE__ */ new Date("2000/01/01 " + l.replace(r.regex.dateReplace, "$1 $2"));
          return c instanceof Date && isFinite(c) ? (s = o instanceof Date && isFinite(o) ? o.getTime() : 0, s ? parseFloat(c.getTime() + "." + o.getTime()) : c.getTime()) : i;
        },
        type: "numeric"
      }), r.addParser({
        id: "metadata",
        is: function() {
          return !1;
        },
        format: function(i, s, a) {
          var o = s.config, l = o.parserMetadataName ? o.parserMetadataName : "sortValue";
          return e(a).metadata()[l];
        },
        type: "numeric"
      }), r.addWidget({
        id: "zebra",
        priority: 90,
        format: function(i, s, a) {
          var o, l, c, f, g, b, w, C = new RegExp(s.cssChildRow, "i"), m = s.$tbodies.add(e(s.namespace + "_extra_table").children("tbody:not(." + s.cssInfoBlock + ")"));
          for (g = 0; g < m.length; g++)
            for (c = 0, o = m.eq(g).children("tr:visible").not(s.selectorRemove), w = o.length, b = 0; b < w; b++)
              l = o.eq(b), C.test(l[0].className) || c++, f = c % 2 === 0, l.removeClass(a.zebra[f ? 1 : 0]).addClass(a.zebra[f ? 0 : 1]);
        },
        remove: function(i, s, a, o) {
          if (!o) {
            var l, c, f = s.$tbodies, g = (a.zebra || ["even", "odd"]).join(" ");
            for (l = 0; l < f.length; l++)
              c = r.processTbody(i, f.eq(l), !0), c.children().removeClass(g), r.processTbody(i, c, !1);
          }
        }
      });
    }(n), function(e, r, i) {
      var s = e.tablesorter || {};
      e.extend(!0, s.defaults, {
        fixedUrl: "",
        widgetOptions: {
          storage_fixedUrl: "",
          storage_group: "",
          storage_page: "",
          storage_storageType: "",
          storage_tableId: "",
          storage_useSessionStorage: ""
        }
      }), s.storage = function(a, o, l, c) {
        a = e(a)[0];
        var f, g, b, w = !1, C = {}, m = a.config, h = m && m.widgetOptions, u = s.debug(m, "storage"), p = (c && c.storageType || h && h.storage_storageType).toString().charAt(0).toLowerCase(), v = p ? "" : c && c.useSessionStorage || h && h.storage_useSessionStorage, S = e(a), T = c && c.id || S.attr(c && c.group || h && h.storage_group || "data-table-group") || h && h.storage_tableId || a.id || e(".tablesorter").index(S), A = c && c.url || S.attr(c && c.page || h && h.storage_page || "data-table-page") || h && h.storage_fixedUrl || m && m.fixedUrl || r.location.pathname;
        if (p !== "c" && (p = p === "s" || v ? "sessionStorage" : "localStorage", p in r))
          try {
            r[p].setItem("_tmptest", "temp"), w = !0, r[p].removeItem("_tmptest");
          } catch {
            console.warn(p + " is not supported in this browser");
          }
        if (u && console.log("Storage >> Using", w ? p : "cookies"), e.parseJSON && (w ? C = e.parseJSON(r[p][o] || "null") || {} : (g = i.cookie.split(/[;\s|=]/), f = e.inArray(o, g) + 1, C = f !== 0 ? e.parseJSON(g[f] || "null") || {} : {})), typeof l < "u" && r.JSON && JSON.hasOwnProperty("stringify"))
          C[A] || (C[A] = {}), C[A][T] = l, w ? r[p][o] = JSON.stringify(C) : (b = /* @__PURE__ */ new Date(), b.setTime(b.getTime() + 31536e6), i.cookie = o + "=" + JSON.stringify(C).replace(/\"/g, '"') + "; expires=" + b.toGMTString() + "; path=/");
        else
          return C && C[A] ? C[A][T] : "";
      };
    }(n, window, document), function(e) {
      var r = e.tablesorter || {};
      r.themes = {
        bootstrap: {
          table: "table table-bordered table-striped",
          caption: "caption",
          // header class names
          header: "bootstrap-header",
          // give the header a gradient background (theme.bootstrap_2.css)
          sortNone: "",
          sortAsc: "",
          sortDesc: "",
          active: "",
          // applied when column is sorted
          hover: "",
          // custom css required - a defined bootstrap style may not override other classes
          // icon class names
          icons: "",
          // add 'bootstrap-icon-white' to make them white; this icon class is added to the <i> in the header
          iconSortNone: "bootstrap-icon-unsorted",
          // class name added to icon when column is not sorted
          iconSortAsc: "glyphicon glyphicon-chevron-up",
          // class name added to icon when column has ascending sort
          iconSortDesc: "glyphicon glyphicon-chevron-down",
          // class name added to icon when column has descending sort
          filterRow: "",
          // filter row class
          footerRow: "",
          footerCells: "",
          even: "",
          // even row zebra striping
          odd: ""
          // odd row zebra striping
        },
        jui: {
          table: "ui-widget ui-widget-content ui-corner-all",
          // table classes
          caption: "ui-widget-content",
          // header class names
          header: "ui-widget-header ui-corner-all ui-state-default",
          // header classes
          sortNone: "",
          sortAsc: "",
          sortDesc: "",
          active: "ui-state-active",
          // applied when column is sorted
          hover: "ui-state-hover",
          // hover class
          // icon class names
          icons: "ui-icon",
          // icon class added to the <i> in the header
          iconSortNone: "ui-icon-carat-2-n-s ui-icon-caret-2-n-s",
          // class name added to icon when column is not sorted
          iconSortAsc: "ui-icon-carat-1-n ui-icon-caret-1-n",
          // class name added to icon when column has ascending sort
          iconSortDesc: "ui-icon-carat-1-s ui-icon-caret-1-s",
          // class name added to icon when column has descending sort
          filterRow: "",
          footerRow: "",
          footerCells: "",
          even: "ui-widget-content",
          // even row zebra striping
          odd: "ui-state-default"
          // odd row zebra striping
        }
      }, e.extend(r.css, {
        wrapper: "tablesorter-wrapper"
        // ui theme & resizable
      }), r.addWidget({
        id: "uitheme",
        priority: 10,
        format: function(i, s, a) {
          var o, l, c, f, g, b, w, C, m, h, u, p, v, S = r.themes, T = s.$table.add(e(s.namespace + "_extra_table")), A = s.$headers.add(e(s.namespace + "_extra_headers")), y = s.theme || "jui", I = S[y] || {}, $ = e.trim([I.sortNone, I.sortDesc, I.sortAsc, I.active].join(" ")), V = e.trim([I.iconSortNone, I.iconSortDesc, I.iconSortAsc].join(" ")), z = r.debug(s, "uitheme");
          for (z && (g = /* @__PURE__ */ new Date()), (!T.hasClass("tablesorter-" + y) || s.theme !== s.appliedTheme || !a.uitheme_applied) && (a.uitheme_applied = !0, h = S[s.appliedTheme] || {}, v = !e.isEmptyObject(h), u = v ? [h.sortNone, h.sortDesc, h.sortAsc, h.active].join(" ") : "", p = v ? [h.iconSortNone, h.iconSortDesc, h.iconSortAsc].join(" ") : "", v && (a.zebra[0] = e.trim(" " + a.zebra[0].replace(" " + h.even, "")), a.zebra[1] = e.trim(" " + a.zebra[1].replace(" " + h.odd, "")), s.$tbodies.children().removeClass([h.even, h.odd].join(" "))), I.even && (a.zebra[0] += " " + I.even), I.odd && (a.zebra[1] += " " + I.odd), T.children("caption").removeClass(h.caption || "").addClass(I.caption), C = T.removeClass((s.appliedTheme ? "tablesorter-" + (s.appliedTheme || "") : "") + " " + (h.table || "")).addClass("tablesorter-" + y + " " + (I.table || "")).children("tfoot"), s.appliedTheme = s.theme, C.length && C.children("tr").removeClass(h.footerRow || "").addClass(I.footerRow).children("th, td").removeClass(h.footerCells || "").addClass(I.footerCells), A.removeClass((v ? [h.header, h.hover, u].join(" ") : "") || "").addClass(I.header).not(".sorter-false").unbind("mouseenter.tsuitheme mouseleave.tsuitheme").bind("mouseenter.tsuitheme mouseleave.tsuitheme", function(F) {
            e(this)[F.type === "mouseenter" ? "addClass" : "removeClass"](I.hover || "");
          }), A.each(function() {
            var F = e(this);
            F.find("." + r.css.wrapper).length || F.wrapInner('<div class="' + r.css.wrapper + '" style="position:relative;height:100%;width:100%"></div>');
          }), s.cssIcon && A.find("." + r.css.icon).removeClass(v ? [h.icons, p].join(" ") : "").addClass(I.icons || ""), r.hasWidget(s.table, "filter") && (l = function() {
            T.children("thead").children("." + r.css.filterRow).removeClass(v && h.filterRow || "").addClass(I.filterRow || "");
          }, a.filter_initialized ? l() : T.one("filterInit", function() {
            l();
          }))), o = 0; o < s.columns; o++)
            b = s.$headers.add(e(s.namespace + "_extra_headers")).not(".sorter-false").filter('[data-column="' + o + '"]'), w = r.css.icon ? b.find("." + r.css.icon) : e(), m = A.not(".sorter-false").filter('[data-column="' + o + '"]:last'), m.length && (b.removeClass($), w.removeClass(V), m[0].sortDisabled ? w.removeClass(I.icons || "") : (c = I.sortNone, f = I.iconSortNone, m.hasClass(r.css.sortAsc) ? (c = [I.sortAsc, I.active].join(" "), f = I.iconSortAsc) : m.hasClass(r.css.sortDesc) && (c = [I.sortDesc, I.active].join(" "), f = I.iconSortDesc), b.addClass(c), w.addClass(f || "")));
          z && console.log("uitheme >> Applied " + y + " theme" + r.benchmark(g));
        },
        remove: function(i, s, a, o) {
          if (a.uitheme_applied) {
            var l = s.$table, c = s.appliedTheme || "jui", f = r.themes[c] || r.themes.jui, g = l.children("thead").children(), b = f.sortNone + " " + f.sortDesc + " " + f.sortAsc, w = f.iconSortNone + " " + f.iconSortDesc + " " + f.iconSortAsc;
            l.removeClass("tablesorter-" + c + " " + f.table), a.uitheme_applied = !1, !o && (l.find(r.css.header).removeClass(f.header), g.unbind("mouseenter.tsuitheme mouseleave.tsuitheme").removeClass(f.hover + " " + b + " " + f.active).filter("." + r.css.filterRow).removeClass(f.filterRow), g.find("." + r.css.icon).removeClass(f.icons + " " + w));
          }
        }
      });
    }(n), function(e) {
      var r = e.tablesorter || {};
      r.addWidget({
        id: "columns",
        priority: 65,
        options: {
          columns: ["primary", "secondary", "tertiary"]
        },
        format: function(i, s, a) {
          var o, l, c, f, g, b, w, C, m = s.$table, h = s.$tbodies, u = s.sortList, p = u.length, v = a && a.columns || ["primary", "secondary", "tertiary"], S = v.length - 1;
          for (w = v.join(" "), l = 0; l < h.length; l++)
            o = r.processTbody(i, h.eq(l), !0), c = o.children("tr"), c.each(function() {
              if (g = e(this), this.style.display !== "none" && (b = g.children().removeClass(w), u && u[0] && (b.eq(u[0][0]).addClass(v[0]), p > 1)))
                for (C = 1; C < p; C++)
                  b.eq(u[C][0]).addClass(v[C] || v[S]);
            }), r.processTbody(i, o, !1);
          if (f = a.columns_thead !== !1 ? ["thead tr"] : [], a.columns_tfoot !== !1 && f.push("tfoot tr"), f.length && (c = m.find(f.join(",")).children().removeClass(w), p))
            for (C = 0; C < p; C++)
              c.filter('[data-column="' + u[C][0] + '"]').addClass(v[C] || v[S]);
        },
        remove: function(i, s, a) {
          var o, l, c = s.$tbodies, f = (a.columns || ["primary", "secondary", "tertiary"]).join(" ");
          for (s.$headers.removeClass(f), s.$table.children("tfoot").children("tr").children("th, td").removeClass(f), o = 0; o < c.length; o++)
            l = r.processTbody(i, c.eq(o), !0), l.children("tr").each(function() {
              e(this).children().removeClass(f);
            }), r.processTbody(i, l, !1);
        }
      });
    }(n), function(e) {
      var r, i, s = e.tablesorter || {}, a = s.css, o = s.keyCodes;
      e.extend(a, {
        filterRow: "tablesorter-filter-row",
        filter: "tablesorter-filter",
        filterDisabled: "disabled",
        filterRowHide: "hideme"
      }), e.extend(o, {
        backSpace: 8,
        escape: 27,
        space: 32,
        left: 37,
        down: 40
      }), s.addWidget({
        id: "filter",
        priority: 50,
        options: {
          filter_cellFilter: "",
          // css class name added to the filter cell ( string or array )
          filter_childRows: !1,
          // if true, filter includes child row content in the search
          filter_childByColumn: !1,
          // ( filter_childRows must be true ) if true = search child rows by column; false = search all child row text grouped
          filter_childWithSibs: !0,
          // if true, include matching child row siblings
          filter_columnAnyMatch: !0,
          // if true, allows using '#:{query}' in AnyMatch searches ( column:query )
          filter_columnFilters: !0,
          // if true, a filter will be added to the top of each table column
          filter_cssFilter: "",
          // css class name added to the filter row & each input in the row ( tablesorter-filter is ALWAYS added )
          filter_defaultAttrib: "data-value",
          // data attribute in the header cell that contains the default filter value
          filter_defaultFilter: {},
          // add a default column filter type '~{query}' to make fuzzy searches default; '{q1} AND {q2}' to make all searches use a logical AND.
          filter_excludeFilter: {},
          // filters to exclude, per column
          filter_external: "",
          // jQuery selector string ( or jQuery object ) of external filters
          filter_filteredRow: "filtered",
          // class added to filtered rows; define in css with "display:none" to hide the filtered-out rows
          filter_filterLabel: 'Filter "{{label}}" column by...',
          // Aria-label added to filter input/select; see #1495
          filter_formatter: null,
          // add custom filter elements to the filter row
          filter_functions: null,
          // add custom filter functions using this option
          filter_hideEmpty: !0,
          // hide filter row when table is empty
          filter_hideFilters: !1,
          // collapse filter row when mouse leaves the area
          filter_ignoreCase: !0,
          // if true, make all searches case-insensitive
          filter_liveSearch: !0,
          // if true, search column content while the user types ( with a delay )
          filter_matchType: { input: "exact", select: "exact" },
          // global query settings ('exact' or 'match'); overridden by "filter-match" or "filter-exact" class
          filter_onlyAvail: "filter-onlyAvail",
          // a header with a select dropdown & this class name will only show available ( visible ) options within the drop down
          filter_placeholder: { search: "", select: "" },
          // default placeholder text ( overridden by any header 'data-placeholder' setting )
          filter_reset: null,
          // jQuery selector string of an element used to reset the filters
          filter_resetOnEsc: !0,
          // Reset filter input when the user presses escape - normalized across browsers
          filter_saveFilters: !1,
          // Use the $.tablesorter.storage utility to save the most recent filters
          filter_searchDelay: 300,
          // typing delay in milliseconds before starting a search
          filter_searchFiltered: !0,
          // allow searching through already filtered rows in special circumstances; will speed up searching in large tables if true
          filter_selectSource: null,
          // include a function to return an array of values to be added to the column filter select
          filter_selectSourceSeparator: "|",
          // filter_selectSource array text left of the separator is added to the option value, right into the option text
          filter_serversideFiltering: !1,
          // if true, must perform server-side filtering b/c client-side filtering is disabled, but the ui and events will still be used.
          filter_startsWith: !1,
          // if true, filter start from the beginning of the cell contents
          filter_useParsedData: !1
          // filter all data using parsed content
        },
        format: function(l, c, f) {
          c.$table.hasClass("hasFilters") || r.init(l, c, f);
        },
        remove: function(l, c, f, g) {
          var b, w, C = c.$table, m = c.$tbodies, h = "addRows updateCell update updateRows updateComplete appendCache filterReset filterAndSortReset filterFomatterUpdate filterEnd search stickyHeadersInit ".split(" ").join(c.namespace + "filter ");
          if (C.removeClass("hasFilters").unbind(h.replace(s.regex.spaces, " ")).find("." + a.filterRow).remove(), f.filter_initialized = !1, !g) {
            for (b = 0; b < m.length; b++)
              w = s.processTbody(l, m.eq(b), !0), w.children().removeClass(f.filter_filteredRow).show(), s.processTbody(l, w, !1);
            f.filter_reset && e(document).undelegate(f.filter_reset, "click" + c.namespace + "filter");
          }
        }
      }), r = s.filter = {
        // regex used in filter 'check' functions - not for general use and not documented
        regex: {
          regex: /^\/((?:\\\/|[^\/])+)\/([migyu]{0,5})?$/,
          // regex to test for regex
          child: /tablesorter-childRow/,
          // child row class name; this gets updated in the script
          filtered: /filtered/,
          // filtered (hidden) row class name; updated in the script
          type: /undefined|number/,
          // check type
          exact: /(^[\"\'=]+)|([\"\'=]+$)/g,
          // exact match (allow '==')
          operators: /[<>=]/g,
          // replace operators
          query: "(q|query)",
          // replace filter queries
          wild01: /\?/g,
          // wild card match 0 or 1
          wild0More: /\*/g,
          // wild care match 0 or more
          quote: /\"/g,
          isNeg1: /(>=?\s*-\d)/,
          isNeg2: /(<=?\s*\d)/
        },
        // function( c, data ) { }
        // c = table.config
        // data.$row = jQuery object of the row currently being processed
        // data.$cells = jQuery object of all cells within the current row
        // data.filters = array of filters for all columns ( some may be undefined )
        // data.filter = filter for the current column
        // data.iFilter = same as data.filter, except lowercase ( if wo.filter_ignoreCase is true )
        // data.exact = table cell text ( or parsed data if column parser enabled; may be a number & not a string )
        // data.iExact = same as data.exact, except lowercase ( if wo.filter_ignoreCase is true; may be a number & not a string )
        // data.cache = table cell text from cache, so it has been parsed ( & in all lower case if c.ignoreCase is true )
        // data.cacheArray = An array of parsed content from each table cell in the row being processed
        // data.index = column index; table = table element ( DOM )
        // data.parsed = array ( by column ) of boolean values ( from filter_useParsedData or 'filter-parsed' class )
        types: {
          or: function(l, c, f) {
            if ((i.orTest.test(c.iFilter) || i.orSplit.test(c.filter)) && // this test for regex has potential to slow down the overall search
            !i.regex.test(c.filter)) {
              var g, b, w, C, m = e.extend({}, c), h = c.filter.split(i.orSplit), u = c.iFilter.split(i.orSplit), p = h.length;
              for (g = 0; g < p; g++) {
                m.nestedFilters = !0, m.filter = "" + (r.parseFilter(l, h[g], c) || ""), m.iFilter = "" + (r.parseFilter(l, u[g], c) || ""), w = "(" + (r.parseFilter(l, m.filter, c) || "") + ")";
                try {
                  if (C = new RegExp(c.isMatch ? w : "^" + w + "$", l.widgetOptions.filter_ignoreCase ? "i" : ""), b = C.test(m.exact) || r.processTypes(l, m, f), b)
                    return b;
                } catch {
                  return null;
                }
              }
              return b || !1;
            }
            return null;
          },
          // Look for an AND or && operator ( logical and )
          and: function(l, c, f) {
            if (i.andTest.test(c.filter)) {
              var g, b, w, C, m, h = e.extend({}, c), u = c.filter.split(i.andSplit), p = c.iFilter.split(i.andSplit), v = u.length;
              for (g = 0; g < v; g++) {
                h.nestedFilters = !0, h.filter = "" + (r.parseFilter(l, u[g], c) || ""), h.iFilter = "" + (r.parseFilter(l, p[g], c) || ""), C = ("(" + (r.parseFilter(l, h.filter, c) || "") + ")").replace(i.wild01, "\\S{1}").replace(i.wild0More, "\\S*");
                try {
                  m = new RegExp(c.isMatch ? C : "^" + C + "$", l.widgetOptions.filter_ignoreCase ? "i" : ""), w = m.test(h.exact) || r.processTypes(l, h, f), g === 0 ? b = w : b = b && w;
                } catch {
                  return null;
                }
              }
              return b || !1;
            }
            return null;
          },
          // Look for regex
          regex: function(l, c) {
            if (i.regex.test(c.filter)) {
              var f, g = c.filter_regexCache[c.index] || i.regex.exec(c.filter), b = g instanceof RegExp;
              try {
                b || (c.filter_regexCache[c.index] = g = new RegExp(g[1], g[2])), f = g.test(c.exact);
              } catch {
                f = !1;
              }
              return f;
            }
            return null;
          },
          // Look for operators >, >=, < or <=
          operators: function(l, c) {
            if (i.operTest.test(c.iFilter) && c.iExact !== "") {
              var f, g, b, w = l.table, C = c.parsed[c.index], m = s.formatFloat(c.iFilter.replace(i.operators, ""), w), h = l.parsers[c.index] || {}, u = m;
              return (C || h.type === "numeric") && (b = e.trim("" + c.iFilter.replace(i.operators, "")), g = r.parseFilter(l, b, c, !0), m = typeof g == "number" && g !== "" && !isNaN(g) ? g : m), (C || h.type === "numeric") && !isNaN(m) && typeof c.cache < "u" ? f = c.cache : (b = isNaN(c.iExact) ? c.iExact.replace(s.regex.nondigit, "") : c.iExact, f = s.formatFloat(b, w)), i.gtTest.test(c.iFilter) ? g = i.gteTest.test(c.iFilter) ? f >= m : f > m : i.ltTest.test(c.iFilter) && (g = i.lteTest.test(c.iFilter) ? f <= m : f < m), !g && u === "" && (g = !0), g;
            }
            return null;
          },
          // Look for a not match
          notMatch: function(l, c) {
            if (i.notTest.test(c.iFilter)) {
              var f, g = c.iFilter.replace("!", ""), b = r.parseFilter(l, g, c) || "";
              return i.exact.test(b) ? (b = b.replace(i.exact, ""), b === "" ? !0 : e.trim(b) !== c.iExact) : (f = c.iExact.search(e.trim(b)), b === "" ? !0 : (
                // return true if not found
                c.anyMatch ? f < 0 : (
                  // return false if found
                  !(l.widgetOptions.filter_startsWith ? f === 0 : f >= 0)
                )
              ));
            }
            return null;
          },
          // Look for quotes or equals to get an exact match; ignore type since iExact could be numeric
          exact: function(l, c) {
            if (i.exact.test(c.iFilter)) {
              var f = c.iFilter.replace(i.exact, ""), g = r.parseFilter(l, f, c) || "";
              return c.anyMatch ? e.inArray(g, c.rowArray) >= 0 : g == c.iExact;
            }
            return null;
          },
          // Look for a range ( using ' to ' or ' - ' ) - see issue #166; thanks matzhu!
          range: function(l, c) {
            if (i.toTest.test(c.iFilter)) {
              var f, g, b, w, C = l.table, m = c.index, h = c.parsed[m], u = c.iFilter.split(i.toSplit);
              return g = u[0].replace(s.regex.nondigit, "") || "", b = s.formatFloat(r.parseFilter(l, g, c), C), g = u[1].replace(s.regex.nondigit, "") || "", w = s.formatFloat(r.parseFilter(l, g, c), C), (h || l.parsers[m].type === "numeric") && (f = l.parsers[m].format("" + u[0], C, l.$headers.eq(m), m), b = f !== "" && !isNaN(f) ? f : b, f = l.parsers[m].format("" + u[1], C, l.$headers.eq(m), m), w = f !== "" && !isNaN(f) ? f : w), (h || l.parsers[m].type === "numeric") && !isNaN(b) && !isNaN(w) ? f = c.cache : (g = isNaN(c.iExact) ? c.iExact.replace(s.regex.nondigit, "") : c.iExact, f = s.formatFloat(g, C)), b > w && (g = b, b = w, w = g), f >= b && f <= w || b === "" || w === "";
            }
            return null;
          },
          // Look for wild card: ? = single, * = multiple, or | = logical OR
          wild: function(l, c) {
            if (i.wildOrTest.test(c.iFilter)) {
              var f = "" + (r.parseFilter(l, c.iFilter, c) || "");
              !i.wildTest.test(f) && c.nestedFilters && (f = c.isMatch ? f : "^(" + f + ")$");
              try {
                return new RegExp(
                  f.replace(i.wild01, "\\S{1}").replace(i.wild0More, "\\S*"),
                  l.widgetOptions.filter_ignoreCase ? "i" : ""
                ).test(c.exact);
              } catch {
                return null;
              }
            }
            return null;
          },
          // fuzzy text search; modified from https://github.com/mattyork/fuzzy ( MIT license )
          fuzzy: function(l, c) {
            if (i.fuzzyTest.test(c.iFilter)) {
              var f, g = 0, b = c.iExact.length, w = c.iFilter.slice(1), C = r.parseFilter(l, w, c) || "";
              for (f = 0; f < b; f++)
                c.iExact[f] === C[g] && (g += 1);
              return g === C.length;
            }
            return null;
          }
        },
        init: function(l) {
          s.language = e.extend(!0, {}, {
            to: "to",
            or: "or",
            and: "and"
          }, s.language);
          var c, f, g, b, w, C, m, h, u = l.config, p = u.widgetOptions, v = function(S, T, A) {
            return T = T.trim(), T === "" ? "" : (S || "") + T + (A || "");
          };
          if (u.$table.addClass("hasFilters"), u.lastSearch = [], p.filter_searchTimer = null, p.filter_initTimer = null, p.filter_formatterCount = 0, p.filter_formatterInit = [], p.filter_anyColumnSelector = '[data-column="all"],[data-column="any"]', p.filter_multipleColumnSelector = '[data-column*="-"],[data-column*=","]', C = "\\{" + i.query + "\\}", e.extend(i, {
            child: new RegExp(u.cssChildRow),
            filtered: new RegExp(p.filter_filteredRow),
            alreadyFiltered: new RegExp("(\\s+(-" + v("|", s.language.or) + v("|", s.language.to) + ")\\s+)", "i"),
            toTest: new RegExp("\\s+(-" + v("|", s.language.to) + ")\\s+", "i"),
            toSplit: new RegExp("(?:\\s+(?:-" + v("|", s.language.to) + ")\\s+)", "gi"),
            andTest: new RegExp("\\s+(" + v("", s.language.and, "|") + "&&)\\s+", "i"),
            andSplit: new RegExp("(?:\\s+(?:" + v("", s.language.and, "|") + "&&)\\s+)", "gi"),
            orTest: new RegExp("(\\|" + v("|\\s+", s.language.or, "\\s+") + ")", "i"),
            orSplit: new RegExp("(?:\\|" + v("|\\s+(?:", s.language.or, ")\\s+") + ")", "gi"),
            iQuery: new RegExp(C, "i"),
            igQuery: new RegExp(C, "ig"),
            operTest: /^[<>]=?/,
            gtTest: />/,
            gteTest: />=/,
            ltTest: /</,
            lteTest: /<=/,
            notTest: /^\!/,
            wildOrTest: /[\?\*\|]/,
            wildTest: /\?\*/,
            fuzzyTest: /^~/,
            exactTest: /[=\"\|!]/
          }), C = u.$headers.filter(".filter-false, .parser-false").length, p.filter_columnFilters !== !1 && C !== u.$headers.length && r.buildRow(l, u, p), g = "addRows updateCell update updateRows updateComplete appendCache filterReset " + "filterAndSortReset filterResetSaved filterEnd search ".split(" ").join(u.namespace + "filter "), u.$table.bind(g, function(S, T) {
            return C = p.filter_hideEmpty && e.isEmptyObject(u.cache) && !(u.delayInit && S.type === "appendCache"), u.$table.find("." + a.filterRow).toggleClass(p.filter_filteredRow, C), /(search|filter)/.test(S.type) || (S.stopPropagation(), r.buildDefault(l, !0)), S.type === "filterReset" || S.type === "filterAndSortReset" ? (u.$table.find("." + a.filter).add(p.filter_$externalFilters).val(""), S.type === "filterAndSortReset" ? s.sortReset(this.config, function() {
              r.searching(l, []);
            }) : r.searching(l, [])) : S.type === "filterResetSaved" ? s.storage(l, "tablesorter-filters", "") : S.type === "filterEnd" ? r.buildDefault(l, !0) : (T = S.type === "search" ? T : S.type === "updateComplete" ? u.$table.data("lastSearch") : "", /(update|add)/.test(S.type) && S.type !== "updateComplete" && (u.lastCombinedFilter = null, u.lastSearch = [], setTimeout(function() {
              u.$table.triggerHandler("filterFomatterUpdate");
            }, 100)), r.searching(l, T, !0)), !1;
          }), p.filter_reset && (p.filter_reset instanceof e ? p.filter_reset.click(function() {
            u.$table.triggerHandler("filterReset");
          }) : e(p.filter_reset).length && e(document).undelegate(p.filter_reset, "click" + u.namespace + "filter").delegate(p.filter_reset, "click" + u.namespace + "filter", function() {
            u.$table.triggerHandler("filterReset");
          })), p.filter_functions) {
            for (w = 0; w < u.columns; w++)
              if (m = s.getColumnData(l, p.filter_functions, w), m) {
                if (b = u.$headerIndexed[w].removeClass("filter-select"), h = !(b.hasClass("filter-false") || b.hasClass("parser-false")), c = "", m === !0 && h)
                  r.buildSelect(l, w);
                else if (typeof m == "object" && h) {
                  for (f in m)
                    typeof f == "string" && (c += c === "" ? '<option value="">' + (b.data("placeholder") || b.attr("data-placeholder") || p.filter_placeholder.select || "") + "</option>" : "", C = f, g = f, f.indexOf(p.filter_selectSourceSeparator) >= 0 && (C = f.split(p.filter_selectSourceSeparator), g = C[1], C = C[0]), c += "<option " + (g === C ? "" : 'data-function-name="' + f + '" ') + 'value="' + C + '">' + g + "</option>");
                  u.$table.find("thead").find("select." + a.filter + '[data-column="' + w + '"]').append(c), g = p.filter_selectSource, m = typeof g == "function" ? !0 : s.getColumnData(l, g, w), m && r.buildSelect(u.table, w, "", !0, b.hasClass(p.filter_onlyAvail));
                }
              }
          }
          r.buildDefault(l, !0), r.bindSearch(l, u.$table.find("." + a.filter), !0), p.filter_external && r.bindSearch(l, p.filter_external), p.filter_hideFilters && r.hideFilters(u), u.showProcessing && (g = "filterStart filterEnd ".split(" ").join(u.namespace + "filter-sp "), u.$table.unbind(g.replace(s.regex.spaces, " ")).bind(g, function(S, T) {
            b = T ? u.$table.find("." + a.header).filter("[data-column]").filter(function() {
              return T[e(this).data("column")] !== "";
            }) : "", s.isProcessing(l, S.type === "filterStart", T ? b : "");
          })), u.filteredRows = u.totalRows, g = "tablesorter-initialized pagerBeforeInitialized ".split(" ").join(u.namespace + "filter "), u.$table.unbind(g.replace(s.regex.spaces, " ")).bind(g, function() {
            r.completeInit(this);
          }), u.pager && u.pager.initialized && !p.filter_initialized ? (u.$table.triggerHandler("filterFomatterUpdate"), setTimeout(function() {
            r.filterInitComplete(u);
          }, 100)) : p.filter_initialized || r.completeInit(l);
        },
        completeInit: function(l) {
          var c = l.config, f = c.widgetOptions, g = r.setDefaults(l, c, f) || [];
          g.length && (c.delayInit && g.join("") === "" || s.setFilters(l, g, !0)), c.$table.triggerHandler("filterFomatterUpdate"), setTimeout(function() {
            f.filter_initialized || r.filterInitComplete(c);
          }, 100);
        },
        // $cell parameter, but not the config, is passed to the filter_formatters,
        // so we have to work with it instead
        formatterUpdated: function(l, c) {
          var f = l && l.closest("table"), g = f.length && f[0].config, b = g && g.widgetOptions;
          b && !b.filter_initialized && (b.filter_formatterInit[c] = 1);
        },
        filterInitComplete: function(l) {
          var c, f, g = l.widgetOptions, b = 0, w = function() {
            g.filter_initialized = !0, l.lastSearch = l.$table.data("lastSearch"), l.$table.triggerHandler("filterInit", l), r.findRows(l.table, l.lastSearch || []), s.debug(l, "filter") && console.log("Filter >> Widget initialized");
          };
          if (e.isEmptyObject(g.filter_formatter))
            w();
          else {
            for (f = g.filter_formatterInit.length, c = 0; c < f; c++)
              g.filter_formatterInit[c] === 1 && b++;
            clearTimeout(g.filter_initTimer), !g.filter_initialized && b === g.filter_formatterCount ? w() : g.filter_initialized || (g.filter_initTimer = setTimeout(function() {
              w();
            }, 500));
          }
        },
        // encode or decode filters for storage; see #1026
        processFilters: function(l, c) {
          var f, g = [], b = c ? encodeURIComponent : decodeURIComponent, w = l.length;
          for (f = 0; f < w; f++)
            l[f] && (g[f] = b(l[f]));
          return g;
        },
        setDefaults: function(l, c, f) {
          var g, b, w, C, m, h = s.getFilters(l) || [];
          if (f.filter_saveFilters && s.storage && (b = s.storage(l, "tablesorter-filters") || [], g = e.isArray(b), g && b.join("") === "" || !g || (h = r.processFilters(b))), h.join("") === "")
            for (m = c.$headers.add(f.filter_$externalFilters).filter("[" + f.filter_defaultAttrib + "]"), w = 0; w <= c.columns; w++)
              C = w === c.columns ? "all" : w, h[w] = m.filter('[data-column="' + C + '"]').attr(f.filter_defaultAttrib) || h[w] || "";
          return c.$table.data("lastSearch", h), h;
        },
        parseFilter: function(l, c, f, g) {
          return g || f.parsed[f.index] ? l.parsers[f.index].format(c, l.table, [], f.index) : c;
        },
        buildRow: function(l, c, f) {
          var g, b, w, C, m, h, u, p, v, S = f.filter_cellFilter, T = c.columns, A = e.isArray(S), y = '<tr role="search" class="' + a.filterRow + " " + c.cssIgnoreRow + '">';
          for (w = 0; w < T; w++)
            c.$headerIndexed[w].length && (v = c.$headerIndexed[w] && c.$headerIndexed[w][0].colSpan || 0, v > 1 ? y += '<td data-column="' + w + "-" + (w + v - 1) + '" colspan="' + v + '"' : y += '<td data-column="' + w + '"', A ? y += S[w] ? ' class="' + S[w] + '"' : "" : y += S !== "" ? ' class="' + S + '"' : "", y += "></td>");
          for (c.$filters = e(y += "</tr>").appendTo(c.$table.children("thead").eq(0)).children("td"), w = 0; w < T; w++)
            h = !1, C = c.$headerIndexed[w], C && C.length && (g = r.getColumnElm(c, c.$filters, w), p = s.getColumnData(l, f.filter_functions, w), m = f.filter_functions && p && typeof p != "function" || C.hasClass("filter-select"), b = s.getColumnData(l, c.headers, w), h = s.getData(C[0], b, "filter") === "false" || s.getData(C[0], b, "parser") === "false", m ? y = e("<select>").appendTo(g) : (p = s.getColumnData(l, f.filter_formatter, w), p ? (f.filter_formatterCount++, y = p(g, w), y && y.length === 0 && (y = g.children("input")), y && (y.parent().length === 0 || y.parent().length && y.parent()[0] !== g[0]) && g.append(y)) : y = e('<input type="search">').appendTo(g), y && (v = C.data("placeholder") || C.attr("data-placeholder") || f.filter_placeholder.search || "", y.attr("placeholder", v))), y && (u = (e.isArray(f.filter_cssFilter) ? typeof f.filter_cssFilter[w] < "u" && f.filter_cssFilter[w] || "" : f.filter_cssFilter) || "", y.addClass(a.filter + " " + u), u = f.filter_filterLabel, v = u.match(/{{([^}]+?)}}/g), v || (v = ["{{label}}"]), e.each(v, function(I, $) {
              var V = new RegExp($, "g"), z = C.attr("data-" + $.replace(/{{|}}/g, "")), F = typeof z > "u" ? C.text() : z;
              u = u.replace(V, e.trim(F));
            }), y.attr({
              "data-column": g.attr("data-column"),
              "aria-label": u
            }), h && (y.attr("placeholder", "").addClass(a.filterDisabled)[0].disabled = !0)));
        },
        bindSearch: function(l, c, f) {
          if (l = e(l)[0], c = e(c), !!c.length) {
            var g, b = l.config, w = b.widgetOptions, C = b.namespace + "filter", m = w.filter_$externalFilters;
            f !== !0 && (g = w.filter_anyColumnSelector + "," + w.filter_multipleColumnSelector, w.filter_$anyMatch = c.filter(g), m && m.length ? w.filter_$externalFilters = w.filter_$externalFilters.add(c) : w.filter_$externalFilters = c, s.setFilters(l, b.$table.data("lastSearch") || [], f === !1)), g = "keypress keyup keydown search change input ".split(" ").join(C + " "), c.attr("data-lastSearchTime", (/* @__PURE__ */ new Date()).getTime()).unbind(g.replace(s.regex.spaces, " ")).bind("keydown" + C, function(h) {
              if (h.which === o.escape && !l.config.widgetOptions.filter_resetOnEsc)
                return !1;
            }).bind("keyup" + C, function(h) {
              w = l.config.widgetOptions;
              var u = parseInt(e(this).attr("data-column"), 10), p = typeof w.filter_liveSearch == "boolean" ? w.filter_liveSearch : s.getColumnData(l, w.filter_liveSearch, u);
              if (typeof p > "u" && (p = w.filter_liveSearch.fallback || !1), e(this).attr("data-lastSearchTime", (/* @__PURE__ */ new Date()).getTime()), h.which === o.escape)
                this.value = w.filter_resetOnEsc ? "" : b.lastSearch[u];
              else {
                if (this.value !== "" && // liveSearch can contain a min value length; ignore arrow and meta keys, but allow backspace
                (typeof p == "number" && this.value.length < p || // let return & backspace continue on, but ignore arrows & non-valid characters
                h.which !== o.enter && h.which !== o.backSpace && (h.which < o.space || h.which >= o.left && h.which <= o.down)))
                  return;
                if (p === !1 && this.value !== "" && h.which !== o.enter)
                  return;
              }
              r.searching(l, !0, !0, u);
            }).bind("search change keypress input blur ".split(" ").join(C + " "), function(h) {
              var u = parseInt(e(this).attr("data-column"), 10), p = h.type, v = typeof w.filter_liveSearch == "boolean" ? w.filter_liveSearch : s.getColumnData(l, w.filter_liveSearch, u);
              l.config.widgetOptions.filter_initialized && // immediate search if user presses enter
              (h.which === o.enter || // immediate search if a "search" or "blur" is triggered on the input
              p === "search" || p === "blur" || // change & input events must be ignored if liveSearch !== true
              (p === "change" || p === "input") && // prevent search if liveSearch is a number
              (v === !0 || v !== !0 && h.target.nodeName !== "INPUT") && // don't allow 'change' or 'input' event to process if the input value
              // is the same - fixes #685
              this.value !== b.lastSearch[u]) && (h.preventDefault(), e(this).attr("data-lastSearchTime", (/* @__PURE__ */ new Date()).getTime()), r.searching(l, p !== "keypress" || h.which === o.enter, !0, u));
            });
          }
        },
        searching: function(l, c, f, g) {
          var b, w = l.config.widgetOptions;
          typeof g > "u" ? b = !1 : (b = typeof w.filter_liveSearch == "boolean" ? w.filter_liveSearch : (
            // get column setting, or set to fallback value, or default to false
            s.getColumnData(l, w.filter_liveSearch, g)
          ), typeof b > "u" && (b = w.filter_liveSearch.fallback || !1)), clearTimeout(w.filter_searchTimer), typeof c > "u" || c === !0 ? w.filter_searchTimer = setTimeout(function() {
            r.checkFilters(l, c, f);
          }, b ? w.filter_searchDelay : 10) : r.checkFilters(l, c, f);
        },
        equalFilters: function(l, c, f) {
          var g, b = [], w = [], C = l.columns + 1;
          for (c = e.isArray(c) ? c : [], f = e.isArray(f) ? f : [], g = 0; g < C; g++)
            b[g] = c[g] || "", w[g] = f[g] || "";
          return b.join(",") === w.join(",");
        },
        checkFilters: function(l, c, f) {
          var g = l.config, b = g.widgetOptions, w = e.isArray(c), C = w ? c : s.getFilters(l, !0), m = C || [];
          if (e.isEmptyObject(g.cache)) {
            g.delayInit && (!g.pager || g.pager && g.pager.initialized) && s.updateCache(g, function() {
              r.checkFilters(l, !1, f);
            });
            return;
          }
          if (w && (s.setFilters(l, C, !1, f !== !0), b.filter_initialized || (g.lastSearch = [], g.lastCombinedFilter = "")), b.filter_hideFilters && g.$table.find("." + a.filterRow).triggerHandler(r.hideFiltersCheck(g) ? "mouseleave" : "mouseenter"), r.equalFilters(g, g.lastSearch, m)) {
            if (c !== !1)
              return;
            g.lastCombinedFilter = "", g.lastSearch = [];
          }
          if (C = C || [], C = Array.prototype.map ? C.map(String) : (
            // for IE8 & older browsers - maybe not the best method
            C.join("�").split("�")
          ), b.filter_initialized && g.$table.triggerHandler("filterStart", [C]), g.showProcessing)
            setTimeout(function() {
              return r.findRows(l, C, m), !1;
            }, 30);
          else
            return r.findRows(l, C, m), !1;
        },
        hideFiltersCheck: function(l) {
          if (typeof l.widgetOptions.filter_hideFilters == "function") {
            var c = l.widgetOptions.filter_hideFilters(l);
            if (typeof c == "boolean")
              return c;
          }
          return s.getFilters(l.$table).join("") === "";
        },
        hideFilters: function(l, c) {
          var f;
          (c || l.$table).find("." + a.filterRow).addClass(a.filterRowHide).bind("mouseenter mouseleave", function(g) {
            var b = g, w = e(this);
            clearTimeout(f), f = setTimeout(function() {
              /enter|over/.test(b.type) ? w.removeClass(a.filterRowHide) : e(document.activeElement).closest("tr")[0] !== w[0] && w.toggleClass(a.filterRowHide, r.hideFiltersCheck(l));
            }, 200);
          }).find("input, select").bind("focus blur", function(g) {
            var b = g, w = e(this).closest("tr");
            clearTimeout(f), f = setTimeout(function() {
              clearTimeout(f), w.toggleClass(a.filterRowHide, r.hideFiltersCheck(l) && b.type !== "focus");
            }, 200);
          });
        },
        defaultFilter: function(l, c) {
          if (l === "")
            return l;
          var f = i.iQuery, g = c.match(i.igQuery).length, b = g > 1 ? e.trim(l).split(/\s/) : [e.trim(l)], w = b.length - 1, C = 0, m = c;
          for (w < 1 && g > 1 && (b[1] = b[0]); f.test(m); )
            m = m.replace(f, b[C++] || ""), f.test(m) && C < w && (b[C] || "") !== "" && (m = c.replace(f, m));
          return m;
        },
        getLatestSearch: function(l) {
          return l ? l.sort(function(c, f) {
            return e(f).attr("data-lastSearchTime") - e(c).attr("data-lastSearchTime");
          }) : l || e();
        },
        findRange: function(l, c, f) {
          var g, b, w, C, m, h, u, p, v, S = [];
          if (/^[0-9]+$/.test(c))
            return [parseInt(c, 10)];
          if (!f && /-/.test(c))
            for (b = c.match(/(\d+)\s*-\s*(\d+)/g), v = b ? b.length : 0, p = 0; p < v; p++) {
              for (w = b[p].split(/\s*-\s*/), C = parseInt(w[0], 10) || 0, m = parseInt(w[1], 10) || l.columns - 1, C > m && (g = C, C = m, m = g), m >= l.columns && (m = l.columns - 1); C <= m; C++)
                S[S.length] = C;
              c = c.replace(b[p], "");
            }
          if (!f && /,/.test(c))
            for (h = c.split(/\s*,\s*/), v = h.length, u = 0; u < v; u++)
              h[u] !== "" && (p = parseInt(h[u], 10), p < l.columns && (S[S.length] = p));
          if (!S.length)
            for (p = 0; p < l.columns; p++)
              S[S.length] = p;
          return S;
        },
        getColumnElm: function(l, c, f) {
          return c.filter(function() {
            var g = r.findRange(l, e(this).attr("data-column"));
            return e.inArray(f, g) > -1;
          });
        },
        multipleColumns: function(l, c) {
          var f = l.widgetOptions, g = f.filter_initialized || !c.filter(f.filter_anyColumnSelector).length, b = e.trim(r.getLatestSearch(c).attr("data-column") || "");
          return r.findRange(l, b, !g);
        },
        processTypes: function(l, c, f) {
          var g, b = null, w = null;
          for (g in r.types)
            e.inArray(g, f.excludeMatch) < 0 && w === null && (w = r.types[g](l, c, f), w !== null && (c.matchedOn = g, b = w));
          return b;
        },
        matchType: function(l, c) {
          var f, g = l.widgetOptions, b = l.$headerIndexed[c];
          return b.hasClass("filter-exact") ? f = !1 : b.hasClass("filter-match") ? f = !0 : (g.filter_columnFilters ? b = l.$filters.find("." + a.filter).add(g.filter_$externalFilters).filter('[data-column="' + c + '"]') : g.filter_$externalFilters && (b = g.filter_$externalFilters.filter('[data-column="' + c + '"]')), f = b.length ? l.widgetOptions.filter_matchType[(b[0].nodeName || "").toLowerCase()] === "match" : (
            // default to exact, if no inputs found
            !1
          )), f;
        },
        processRow: function(l, c, f) {
          var g, b, w, C, m, h = l.widgetOptions, u = !0, p = h.filter_$anyMatch && h.filter_$anyMatch.length, v = h.filter_$anyMatch && h.filter_$anyMatch.length ? (
            // look for multiple columns '1-3,4-6,8'
            r.multipleColumns(l, h.filter_$anyMatch)
          ) : [];
          if (c.$cells = c.$row.children(), c.matchedOn = null, c.anyMatchFlag && v.length > 1 || c.anyMatchFilter && !p) {
            if (c.anyMatch = !0, c.isMatch = !0, c.rowArray = c.$cells.map(function(S) {
              if (e.inArray(S, v) > -1 || c.anyMatchFilter && !p)
                return c.parsed[S] ? m = c.cacheArray[S] : (m = c.rawArray[S], m = e.trim(h.filter_ignoreCase ? m.toLowerCase() : m), l.sortLocaleCompare && (m = s.replaceAccents(m))), m;
            }).get(), c.filter = c.anyMatchFilter, c.iFilter = c.iAnyMatchFilter, c.exact = c.rowArray.join(" "), c.iExact = h.filter_ignoreCase ? c.exact.toLowerCase() : c.exact, c.cache = c.cacheArray.slice(0, -1).join(" "), f.excludeMatch = f.noAnyMatch, b = r.processTypes(l, c, f), b !== null)
              u = b;
            else if (h.filter_startsWith)
              for (u = !1, v = Math.min(l.columns, c.rowArray.length); !u && v > 0; )
                v--, u = u || c.rowArray[v].indexOf(c.iFilter) === 0;
            else
              u = (c.iExact + c.childRowText).indexOf(c.iFilter) >= 0;
            if (c.anyMatch = !1, c.filters.join("") === c.filter)
              return u;
          }
          for (v = 0; v < l.columns; v++)
            c.filter = c.filters[v], c.index = v, f.excludeMatch = f.excludeFilter[v], c.filter && (c.cache = c.cacheArray[v], g = c.parsed[v] ? c.cache : c.rawArray[v] || "", c.exact = l.sortLocaleCompare ? s.replaceAccents(g) : g, c.iExact = !i.type.test(typeof c.exact) && h.filter_ignoreCase ? c.exact.toLowerCase() : c.exact, c.isMatch = r.matchType(l, v), g = u, C = h.filter_columnFilters && l.$filters.add(h.filter_$externalFilters).filter('[data-column="' + v + '"]').find("select option:selected").attr("data-function-name") || "", l.sortLocaleCompare && (c.filter = s.replaceAccents(c.filter)), h.filter_defaultFilter && i.iQuery.test(f.defaultColFilter[v]) && (c.filter = r.defaultFilter(c.filter, f.defaultColFilter[v])), c.iFilter = h.filter_ignoreCase ? (c.filter || "").toLowerCase() : c.filter, w = f.functions[v], b = null, w && (typeof w == "function" ? b = w(c.exact, c.cache, c.filter, v, c.$row, l, c) : typeof w[C || c.filter] == "function" && (m = C || c.filter, b = w[m](c.exact, c.cache, c.filter, v, c.$row, l, c))), b === null ? (b = r.processTypes(l, c, f), m = w === !0 && (c.matchedOn === "and" || c.matchedOn === "or"), b !== null && !m ? g = b : w === !0 ? g = c.isMatch ? (
              // data.iExact may be a number
              ("" + c.iExact).search(c.iFilter) >= 0
            ) : c.filter === c.exact : (m = (c.iExact + c.childRowText).indexOf(r.parseFilter(l, c.iFilter, c)), g = !h.filter_startsWith && m >= 0 || h.filter_startsWith && m === 0)) : g = b, u = g ? u : !1);
          return u;
        },
        findRows: function(l, c, f) {
          if (!(r.equalFilters(l.config, l.config.lastSearch, f) || !l.config.widgetOptions.filter_initialized)) {
            var g, b, w, C, m, h, u, p, v, S, T, A, y, I, $, V, z, F, P, k, R, L, E, M, W = e.extend([], c), Y = l.config, J = Y.widgetOptions, X = s.debug(Y, "filter"), Q = {
              anyMatch: !1,
              filters: c,
              // regex filter type cache
              filter_regexCache: []
            }, ee = {
              // anyMatch really screws up with these types of filters
              noAnyMatch: ["range", "operators"],
              // cache filter variables that use ts.getColumnData in the main loop
              functions: [],
              excludeFilter: [],
              defaultColFilter: [],
              defaultAnyFilter: s.getColumnData(l, J.filter_defaultFilter, Y.columns, !0) || ""
            };
            for (Q.parsed = [], v = 0; v < Y.columns; v++)
              Q.parsed[v] = J.filter_useParsedData || // parser has a "parsed" parameter
              Y.parsers && Y.parsers[v] && Y.parsers[v].parsed || // getData may not return 'parsed' if other 'filter-' class names exist
              // ( e.g. <th class="filter-select filter-parsed"> )
              s.getData && s.getData(
                Y.$headerIndexed[v],
                s.getColumnData(l, Y.headers, v),
                "filter"
              ) === "parsed" || Y.$headerIndexed[v].hasClass("filter-parsed"), ee.functions[v] = s.getColumnData(l, J.filter_functions, v) || Y.$headerIndexed[v].hasClass("filter-select"), ee.defaultColFilter[v] = s.getColumnData(l, J.filter_defaultFilter, v) || "", ee.excludeFilter[v] = (s.getColumnData(l, J.filter_excludeFilter, v, !0) || "").split(/\s+/);
            for (X && (console.log("Filter >> Starting filter widget search", c), $ = /* @__PURE__ */ new Date()), Y.filteredRows = 0, Y.totalRows = 0, f = W || [], u = 0; u < Y.$tbodies.length; u++) {
              if (p = s.processTbody(l, Y.$tbodies.eq(u), !0), v = Y.columns, b = Y.cache[u].normalized, C = e(e.map(b, function(ie) {
                return ie[v].$row.get();
              })), f.join("") === "" || J.filter_serversideFiltering)
                C.removeClass(J.filter_filteredRow).not("." + Y.cssChildRow).css("display", "");
              else {
                if (C = C.not("." + Y.cssChildRow), g = C.length, (J.filter_$anyMatch && J.filter_$anyMatch.length || typeof c[Y.columns] < "u") && (Q.anyMatchFlag = !0, Q.anyMatchFilter = "" + (c[Y.columns] || J.filter_$anyMatch && r.getLatestSearch(J.filter_$anyMatch).val() || ""), J.filter_columnAnyMatch)) {
                  for (k = Q.anyMatchFilter.split(i.andSplit), R = !1, z = 0; z < k.length; z++)
                    L = k[z].split(":"), L.length > 1 && (isNaN(L[0]) ? e.each(Y.headerContent, function(ie, oe) {
                      oe.toLowerCase().indexOf(L[0]) > -1 && (E = ie, c[E] = L[1]);
                    }) : E = parseInt(L[0], 10) - 1, E >= 0 && E < Y.columns && (c[E] = L[1], k.splice(z, 1), z--, R = !0));
                  R && (Q.anyMatchFilter = k.join(" && "));
                }
                if (P = J.filter_searchFiltered, A = Y.lastSearch || Y.$table.data("lastSearch") || [], P)
                  for (z = 0; z < v + 1; z++)
                    V = c[z] || "", P || (z = v), P = P && A.length && // there are no changes from beginning of filter
                    V.indexOf(A[z] || "") === 0 && // if there is NOT a logical 'or', or range ( 'to' or '-' ) in the string
                    !i.alreadyFiltered.test(V) && // if we are not doing exact matches, using '|' ( logical or ) or not '!'
                    !i.exactTest.test(V) && // don't search only filtered if the value is negative
                    // ( '> -10' => '> -100' will ignore hidden rows )
                    !(i.isNeg1.test(V) || i.isNeg2.test(V)) && // if filtering using a select without a 'filter-match' class ( exact match ) - fixes #593
                    !(V !== "" && Y.$filters && Y.$filters.filter('[data-column="' + z + '"]').find("select").length && !r.matchType(Y, z));
                for (F = C.not("." + J.filter_filteredRow).length, P && F === 0 && (P = !1), X && console.log("Filter >> Searching through " + (P && F < g ? F : "all") + " rows"), Q.anyMatchFlag && (Y.sortLocaleCompare && (Q.anyMatchFilter = s.replaceAccents(Q.anyMatchFilter)), J.filter_defaultFilter && i.iQuery.test(ee.defaultAnyFilter) && (Q.anyMatchFilter = r.defaultFilter(Q.anyMatchFilter, ee.defaultAnyFilter), P = !1), Q.iAnyMatchFilter = J.filter_ignoreCase && Y.ignoreCase ? Q.anyMatchFilter.toLowerCase() : Q.anyMatchFilter), h = 0; h < g; h++)
                  if (M = C[h].className, S = h && i.child.test(M), !(S || P && i.filtered.test(M))) {
                    if (Q.$row = C.eq(h), Q.rowIndex = h, Q.cacheArray = b[h], w = Q.cacheArray[Y.columns], Q.rawArray = w.raw, Q.childRowText = "", !J.filter_childByColumn) {
                      for (M = "", T = w.child, z = 0; z < T.length; z++)
                        M += " " + T[z].join(" ") || "";
                      Q.childRowText = J.filter_childRows ? J.filter_ignoreCase ? M.toLowerCase() : M : "";
                    }
                    if (y = !1, I = r.processRow(Y, Q, ee), m = w.$row, V = !!I, T = w.$row.filter(":gt(0)"), J.filter_childRows && T.length) {
                      if (J.filter_childByColumn)
                        for (J.filter_childWithSibs || (T.addClass(J.filter_filteredRow), m = m.eq(0)), z = 0; z < T.length; z++)
                          Q.$row = T.eq(z), Q.cacheArray = w.child[z], Q.rawArray = Q.cacheArray, V = r.processRow(Y, Q, ee), y = y || V, !J.filter_childWithSibs && V && T.eq(z).removeClass(J.filter_filteredRow);
                      y = y || I;
                    } else
                      y = V;
                    m.toggleClass(J.filter_filteredRow, !y)[0].display = y ? "" : "none";
                  }
              }
              Y.filteredRows += C.not("." + J.filter_filteredRow).length, Y.totalRows += C.length, s.processTbody(l, p, !1);
            }
            Y.lastCombinedFilter = W.join(""), Y.lastSearch = W, Y.$table.data("lastSearch", W), J.filter_saveFilters && s.storage && s.storage(l, "tablesorter-filters", r.processFilters(W, !0)), X && console.log("Filter >> Completed search" + s.benchmark($)), J.filter_initialized && (Y.$table.triggerHandler("filterBeforeEnd", Y), Y.$table.triggerHandler("filterEnd", Y)), setTimeout(function() {
              s.applyWidget(Y.table);
            }, 0);
          }
        },
        getOptionSource: function(l, c, f) {
          l = e(l)[0];
          var g = l.config, b = g.widgetOptions, w = !1, C = b.filter_selectSource, m = g.$table.data("lastSearch") || [], h = typeof C == "function" ? !0 : s.getColumnData(l, C, c);
          if (f && m[c] !== "" && (f = !1), h === !0)
            w = C(l, c, f);
          else {
            if (h instanceof e || e.type(h) === "string" && h.indexOf("</option>") >= 0)
              return h;
            if (e.isArray(h))
              w = h;
            else if (e.type(C) === "object" && h && (w = h(l, c, f), w === null))
              return null;
          }
          return w === !1 && (w = r.getOptions(l, c, f)), r.processOptions(l, c, w);
        },
        processOptions: function(l, c, f) {
          if (!e.isArray(f))
            return !1;
          l = e(l)[0];
          var g, b, w, C, m, h, u = l.config, p = typeof c < "u" && c !== null && c >= 0 && c < u.columns, v = p ? u.$headerIndexed[c].hasClass("filter-select-sort-desc") : !1, S = [];
          if (f = e.grep(f, function(T, A) {
            return T.text ? !0 : e.inArray(T, f) === A;
          }), p && u.$headerIndexed[c].hasClass("filter-select-nosort"))
            return f;
          for (C = f.length, w = 0; w < C; w++)
            b = f[w], h = b.text ? b.text : b, m = (p && u.parsers && u.parsers.length && u.parsers[c].format(h, l, [], c) || h).toString(), m = u.widgetOptions.filter_ignoreCase ? m.toLowerCase() : m, b.text ? (b.parsed = m, S[S.length] = b) : S[S.length] = {
              text: b,
              // check parser length - fixes #934
              parsed: m
            };
          for (g = u.textSorter || "", S.sort(function(T, A) {
            var y = v ? A.parsed : T.parsed, I = v ? T.parsed : A.parsed;
            return p && typeof g == "function" ? g(y, I, !0, c, l) : p && typeof g == "object" && g.hasOwnProperty(c) ? g[c](y, I, !0, c, l) : s.sortNatural ? s.sortNatural(y, I) : !0;
          }), f = [], C = S.length, w = 0; w < C; w++)
            f[f.length] = S[w];
          return f;
        },
        getOptions: function(l, c, f) {
          l = e(l)[0];
          var g, b, w, C, m, h, u, p, v = l.config, S = v.widgetOptions, T = [];
          for (b = 0; b < v.$tbodies.length; b++)
            for (m = v.cache[b], w = v.cache[b].normalized.length, g = 0; g < w; g++)
              if (C = m.row ? m.row[g] : m.normalized[g][v.columns].$row[0], !(f && C.className.match(S.filter_filteredRow))) {
                if (S.filter_useParsedData || v.parsers[c].parsed || v.$headerIndexed[c].hasClass("filter-parsed")) {
                  if (T[T.length] = "" + m.normalized[g][c], S.filter_childRows && S.filter_childByColumn)
                    for (p = m.normalized[g][v.columns].$row.length - 1, h = 0; h < p; h++)
                      T[T.length] = "" + m.normalized[g][v.columns].child[h][c];
                } else if (T[T.length] = m.normalized[g][v.columns].raw[c], S.filter_childRows && S.filter_childByColumn)
                  for (p = m.normalized[g][v.columns].$row.length, h = 1; h < p; h++)
                    u = m.normalized[g][v.columns].$row.eq(h).children().eq(c), T[T.length] = "" + s.getElementText(v, u, c);
              }
          return T;
        },
        buildSelect: function(l, c, f, g, b) {
          if (l = e(l)[0], c = parseInt(c, 10), !(!l.config.cache || e.isEmptyObject(l.config.cache))) {
            var w, C, m, h, u, p, v, S = l.config, T = S.widgetOptions, A = S.$headerIndexed[c], y = '<option value="">' + (A.data("placeholder") || A.attr("data-placeholder") || T.filter_placeholder.select || "") + "</option>", I = S.$table.find("thead").find("select." + a.filter + '[data-column="' + c + '"]').val();
            if (!((typeof f > "u" || f === "") && (f = r.getOptionSource(l, c, b), f === null))) {
              if (e.isArray(f)) {
                for (w = 0; w < f.length; w++)
                  if (v = f[w], v.text) {
                    v["data-function-name"] = typeof v.value > "u" ? v.text : v.value, y += "<option";
                    for (C in v)
                      v.hasOwnProperty(C) && C !== "text" && (y += " " + C + '="' + v[C].replace(i.quote, "&quot;") + '"');
                    v.value || (y += ' value="' + v.text.replace(i.quote, "&quot;") + '"'), y += ">" + v.text.replace(i.quote, "&quot;") + "</option>";
                  } else
                    "" + v != "[object Object]" && (m = v = ("" + v).replace(i.quote, "&quot;"), C = m, m.indexOf(T.filter_selectSourceSeparator) >= 0 && (h = m.split(T.filter_selectSourceSeparator), C = h[0], m = h[1]), y += v !== "" ? "<option " + (C === m ? "" : 'data-function-name="' + v + '" ') + 'value="' + C + '">' + m + "</option>" : "");
                f = [];
              }
              u = (S.$filters ? S.$filters : S.$table.children("thead")).find("." + a.filter), T.filter_$externalFilters && (u = u && u.length ? u.add(T.filter_$externalFilters) : T.filter_$externalFilters), p = u.filter('select[data-column="' + c + '"]'), p.length && (p[g ? "html" : "append"](y), e.isArray(f) || p.append(f).val(I), p.val(I));
            }
          }
        },
        buildDefault: function(l, c) {
          var f, g, b, w = l.config, C = w.widgetOptions, m = w.columns;
          for (f = 0; f < m; f++)
            g = w.$headerIndexed[f], b = !(g.hasClass("filter-false") || g.hasClass("parser-false")), (g.hasClass("filter-select") || s.getColumnData(l, C.filter_functions, f) === !0) && b && r.buildSelect(l, f, "", c, g.hasClass(C.filter_onlyAvail));
        }
      }, i = r.regex, s.getFilters = function(l, c, f, g) {
        var b, w, C, m, h = [], u = l ? e(l)[0].config : "", p = u ? u.widgetOptions : "";
        if (c !== !0 && p && !p.filter_columnFilters || // setFilters called, but last search is exactly the same as the current
        // fixes issue #733 & #903 where calling update causes the input values to reset
        e.isArray(f) && r.equalFilters(u, f, u.lastSearch))
          return e(l).data("lastSearch") || [];
        if (u && (u.$filters && (w = u.$filters.find("." + a.filter)), p.filter_$externalFilters && (w = w && w.length ? w.add(p.filter_$externalFilters) : p.filter_$externalFilters), w && w.length))
          for (h = f || [], b = 0; b < u.columns + 1; b++)
            m = b === u.columns ? (
              // 'all' columns can now include a range or set of columms ( data-column='0-2,4,6-7' )
              p.filter_anyColumnSelector + "," + p.filter_multipleColumnSelector
            ) : '[data-column="' + b + '"]', C = w.filter(m), C.length && (C = r.getLatestSearch(C), e.isArray(f) ? (g && C.length > 1 && (C = C.slice(1)), b === u.columns && (m = C.filter(p.filter_anyColumnSelector), C = m.length ? m : C), C.val(f[b]).trigger("change" + u.namespace)) : (h[b] = C.val() || "", b === u.columns ? C.slice(1).filter('[data-column*="' + C.attr("data-column") + '"]').val(h[b]) : C.slice(1).val(h[b])), b === u.columns && C.length && (p.filter_$anyMatch = C));
        return h;
      }, s.setFilters = function(l, c, f, g) {
        var b = l ? e(l)[0].config : "", w = s.getFilters(l, !0, c, g);
        return typeof f > "u" && (f = !0), b && f && (b.lastCombinedFilter = null, b.lastSearch = [], r.searching(b.table, c, g), b.$table.triggerHandler("filterFomatterUpdate")), w.length !== 0;
      };
    }(n), function(e, r) {
      var i = e.tablesorter || {};
      e.extend(i.css, {
        sticky: "tablesorter-stickyHeader",
        // stickyHeader
        stickyVis: "tablesorter-sticky-visible",
        stickyHide: "tablesorter-sticky-hidden",
        stickyWrap: "tablesorter-sticky-wrapper"
      }), i.addHeaderResizeEvent = function(a, o, l) {
        if (a = e(a)[0], !!a.config) {
          var c = {
            timer: 250
          }, f = e.extend({}, c, l), g = a.config, b = g.widgetOptions, w = function(C) {
            var m, h, u, p, v, S, T = g.$headers.length;
            for (b.resize_flag = !0, h = [], m = 0; m < T; m++)
              u = g.$headers.eq(m), p = u.data("savedSizes") || [0, 0], v = u[0].offsetWidth, S = u[0].offsetHeight, (v !== p[0] || S !== p[1]) && (u.data("savedSizes", [v, S]), h.push(u[0]));
            h.length && C !== !1 && g.$table.triggerHandler("resize", [h]), b.resize_flag = !1;
          };
          if (clearInterval(b.resize_timer), o)
            return b.resize_flag = !1, !1;
          w(!1), b.resize_timer = setInterval(function() {
            b.resize_flag || w();
          }, f.timer);
        }
      };
      function s(a, o) {
        var l = isNaN(o.stickyHeaders_offset) ? e(o.stickyHeaders_offset) : [];
        return l.length ? l.height() || 0 : parseInt(o.stickyHeaders_offset, 10) || 0;
      }
      i.addWidget({
        id: "stickyHeaders",
        priority: 54,
        // sticky widget must be initialized after the filter & before pager widget!
        options: {
          stickyHeaders: "",
          // extra class name added to the sticky header row
          stickyHeaders_appendTo: null,
          // jQuery selector or object to phycially attach the sticky headers
          stickyHeaders_attachTo: null,
          // jQuery selector or object to attach scroll listener to (overridden by xScroll & yScroll settings)
          stickyHeaders_xScroll: null,
          // jQuery selector or object to monitor horizontal scroll position (defaults: xScroll > attachTo > window)
          stickyHeaders_yScroll: null,
          // jQuery selector or object to monitor vertical scroll position (defaults: yScroll > attachTo > window)
          stickyHeaders_offset: 0,
          // number or jquery selector targeting the position:fixed element
          stickyHeaders_filteredToTop: !0,
          // scroll table top into view after filtering
          stickyHeaders_cloneId: "-sticky",
          // added to table ID, if it exists
          stickyHeaders_addResizeEvent: !0,
          // trigger 'resize' event on headers
          stickyHeaders_includeCaption: !0,
          // if false and a caption exist, it won't be included in the sticky header
          stickyHeaders_zIndex: 2
          // The zIndex of the stickyHeaders, allows the user to adjust this to their needs
        },
        format: function(a, o, l) {
          if (!(o.$table.hasClass("hasStickyHeaders") || e.inArray("filter", o.widgets) >= 0 && !o.$table.hasClass("hasFilters"))) {
            var c, f, g, b = o.$table, w = e(l.stickyHeaders_attachTo || l.stickyHeaders_appendTo), C = o.namespace + "stickyheaders ", m = e(l.stickyHeaders_yScroll || l.stickyHeaders_attachTo || r), h = e(l.stickyHeaders_xScroll || l.stickyHeaders_attachTo || r), u = b.children("thead:first"), p = u.children("tr").not(".sticky-false").children(), v = b.children("tfoot"), S = s(o, l), T = b.parent().closest("." + i.css.table).hasClass("hasStickyHeaders") ? b.parent().closest("table.tablesorter")[0].config.widgetOptions.$sticky.parent() : [], A = T.length ? T.height() : 0, y = l.$sticky = b.clone().addClass("containsStickyHeaders " + i.css.sticky + " " + l.stickyHeaders + " " + o.namespace.slice(1) + "_extra_table").wrap('<div class="' + i.css.stickyWrap + '">'), I = y.parent().addClass(i.css.stickyHide).css({
              position: w.length ? "absolute" : "fixed",
              padding: parseInt(y.parent().parent().css("padding-left"), 10),
              top: S + A,
              left: 0,
              visibility: "hidden",
              zIndex: l.stickyHeaders_zIndex || 2
            }), $ = y.children("thead:first"), V, z = "", F = function(L, E) {
              var M, W, Y, J, X, Q = L.filter(":visible"), ee = Q.length;
              for (M = 0; M < ee; M++)
                J = E.filter(":visible").eq(M), X = Q.eq(M), X.css("box-sizing") === "border-box" ? W = X.outerWidth() : J.css("border-collapse") === "collapse" ? r.getComputedStyle ? W = parseFloat(r.getComputedStyle(X[0], null).width) : (Y = parseFloat(X.css("border-width")), W = X.outerWidth() - parseFloat(X.css("padding-left")) - parseFloat(X.css("padding-right")) - Y) : W = X.width(), J.css({
                  width: W,
                  "min-width": W,
                  "max-width": W
                });
            }, P = function(L) {
              return L === !1 && T.length ? b.position().left : w.length ? parseInt(w.css("padding-left"), 10) || 0 : b.offset().left - parseInt(b.css("margin-left"), 10) - e(r).scrollLeft();
            }, k = function() {
              I.css({
                left: P(),
                width: b.outerWidth()
              }), F(b, y), F(p, V);
            }, R = function(L) {
              if (b.is(":visible")) {
                A = T.length ? T.offset().top - m.scrollTop() + T.height() : 0;
                var E, M = b.offset(), W = s(o, l), Y = e.isWindow(m[0]), J = Y ? m.scrollTop() : (
                  // use parent sticky position if nested AND inside of a scrollable element - see #1512
                  T.length ? parseInt(T[0].style.top, 10) : m.offset().top
                ), X = w.length ? J : m.scrollTop(), Q = l.stickyHeaders_includeCaption ? 0 : b.children("caption").height() || 0, ee = X + W + A - Q, ie = b.height() - (I.height() + (v.height() || 0)) - Q, oe = ee > M.top && ee < M.top + ie ? "visible" : "hidden", ce = oe === "visible" ? i.css.stickyVis : i.css.stickyHide, he = !I.hasClass(ce), we = { visibility: oe };
                w.length && (he = !0, we.top = Y ? ee - w.offset().top : w.scrollTop()), E = P(Y), E !== parseInt(I.css("left"), 10) && (he = !0, we.left = E), we.top = (we.top || 0) + // If nested AND inside of a scrollable element, only add parent sticky height
                (!Y && T.length ? T.height() : W + A), he && I.removeClass(i.css.stickyVis + " " + i.css.stickyHide).addClass(ce).css(we), (oe !== z || L) && (k(), z = oe);
              }
            };
            if (w.length && !w.css("position") && w.css("position", "relative"), y.attr("id") && (y[0].id += l.stickyHeaders_cloneId), y.find("> thead:gt(0), tr.sticky-false").hide(), y.find("> tbody, > tfoot").remove(), y.find("caption").toggle(l.stickyHeaders_includeCaption), V = $.children().children(), y.css({ height: 0, width: 0, margin: 0 }), V.find("." + i.css.resizer).remove(), b.addClass("hasStickyHeaders").bind("pagerComplete" + C, function() {
              k();
            }), i.bindEvents(a, $.children().children("." + i.css.header)), l.stickyHeaders_appendTo ? e(l.stickyHeaders_appendTo).append(I) : b.after(I), o.onRenderHeader)
              for (g = $.children("tr").children(), f = g.length, c = 0; c < f; c++)
                o.onRenderHeader.apply(g.eq(c), [c, o, y]);
            h.add(m).unbind("scroll resize ".split(" ").join(C).replace(/\s+/g, " ")).bind("scroll resize ".split(" ").join(C), function(L) {
              R(L.type === "resize");
            }), o.$table.unbind("stickyHeadersUpdate" + C).bind("stickyHeadersUpdate" + C, function() {
              R(!0);
            }), l.stickyHeaders_addResizeEvent && i.addHeaderResizeEvent(a), b.hasClass("hasFilters") && l.filter_columnFilters && (b.bind("filterEnd" + C, function() {
              var L = e(document.activeElement).closest("td"), E = L.parent().children().index(L);
              I.hasClass(i.css.stickyVis) && l.stickyHeaders_filteredToTop && (r.scrollTo(0, b.position().top), E >= 0 && o.$filters && o.$filters.eq(E).find("a, select, input").filter(":visible").focus());
            }), i.filter.bindSearch(b, V.find("." + i.css.filter)), l.filter_hideFilters && i.filter.hideFilters(o, y)), l.stickyHeaders_addResizeEvent && b.bind("resize" + o.namespace + "stickyheaders", function() {
              k();
            }), R(!0), b.triggerHandler("stickyHeadersInit");
          }
        },
        remove: function(a, o, l) {
          var c = o.namespace + "stickyheaders ";
          o.$table.removeClass("hasStickyHeaders").unbind("pagerComplete resize filterEnd stickyHeadersUpdate ".split(" ").join(c).replace(/\s+/g, " ")).next("." + i.css.stickyWrap).remove(), l.$sticky && l.$sticky.length && l.$sticky.remove(), e(r).add(l.stickyHeaders_xScroll).add(l.stickyHeaders_yScroll).add(l.stickyHeaders_attachTo).unbind("scroll resize ".split(" ").join(c).replace(/\s+/g, " ")), i.addHeaderResizeEvent(a, !0);
        }
      });
    }(n, window), function(e, r) {
      var i = e.tablesorter || {};
      e.extend(i.css, {
        resizableContainer: "tablesorter-resizable-container",
        resizableHandle: "tablesorter-resizable-handle",
        resizableNoSelect: "tablesorter-disableSelection",
        resizableStorage: "tablesorter-resizable"
      }), e(function() {
        var s = "<style>body." + i.css.resizableNoSelect + " { -ms-user-select: none; -moz-user-select: -moz-none;-khtml-user-select: none; -webkit-user-select: none; user-select: none; }." + i.css.resizableContainer + " { position: relative; height: 1px; }." + i.css.resizableHandle + " { position: absolute; display: inline-block; width: 8px;top: 1px; cursor: ew-resize; z-index: 3; user-select: none; -moz-user-select: none; }</style>";
        e("head").append(s);
      }), i.resizable = {
        init: function(s, a) {
          if (!s.$table.hasClass("hasResizable")) {
            s.$table.addClass("hasResizable");
            var o, l, c, f, g, b = s.$table, w = b.parent(), C = parseInt(b.css("margin-top"), 10), m = a.resizable_vars = {
              useStorage: i.storage && a.resizable !== !1,
              $wrap: w,
              mouseXPosition: 0,
              $target: null,
              $next: null,
              overflow: w.css("overflow") === "auto" || w.css("overflow") === "scroll" || w.css("overflow-x") === "auto" || w.css("overflow-x") === "scroll",
              storedSizes: []
            };
            for (i.resizableReset(s.table, !0), m.tableWidth = b.width(), m.fullWidth = Math.abs(w.width() - m.tableWidth) < 20, m.useStorage && m.overflow && (i.storage(s.table, "tablesorter-table-original-css-width", m.tableWidth), g = i.storage(s.table, "tablesorter-table-resized-width") || "auto", i.resizable.setWidth(b, g, !0)), a.resizable_vars.storedSizes = f = (m.useStorage ? i.storage(s.table, i.css.resizableStorage) : []) || [], i.resizable.setWidths(s, a, f), i.resizable.updateStoredSizes(s, a), a.$resizable_container = e('<div class="' + i.css.resizableContainer + '">').css({ top: C }).insertBefore(b), c = 0; c < s.columns; c++)
              l = s.$headerIndexed[c], g = i.getColumnData(s.table, s.headers, c), o = i.getData(l, g, "resizable") === "false", o || e('<div class="' + i.css.resizableHandle + '">').appendTo(a.$resizable_container).attr({
                "data-column": c,
                unselectable: "on"
              }).data("header", l).bind("selectstart", !1);
            i.resizable.bindings(s, a);
          }
        },
        updateStoredSizes: function(s, a) {
          var o, l, c = s.columns, f = a.resizable_vars;
          for (f.storedSizes = [], o = 0; o < c; o++)
            l = s.$headerIndexed[o], f.storedSizes[o] = l.is(":visible") ? l.width() : 0;
        },
        setWidth: function(s, a, o) {
          s.css({
            width: a,
            "min-width": o ? a : "",
            "max-width": o ? a : ""
          });
        },
        setWidths: function(s, a, o) {
          var l, c, f = a.resizable_vars, g = e(s.namespace + "_extra_headers"), b = s.$table.children("colgroup").children("col");
          if (o = o || f.storedSizes || [], o.length) {
            for (l = 0; l < s.columns; l++)
              i.resizable.setWidth(s.$headerIndexed[l], o[l], f.overflow), g.length && (c = g.eq(l).add(b.eq(l)), i.resizable.setWidth(c, o[l], f.overflow));
            c = e(s.namespace + "_extra_table"), c.length && !i.hasWidget(s.table, "scroller") && i.resizable.setWidth(c, s.$table.outerWidth(), f.overflow);
          }
        },
        setHandlePosition: function(s, a) {
          var o, l = s.$table.height(), c = a.$resizable_container.children(), f = Math.floor(c.width() / 2);
          i.hasWidget(s.table, "scroller") && (l = 0, s.$table.closest("." + i.css.scrollerWrap).children().each(function() {
            var g = e(this);
            l += g.filter('[style*="height"]').length ? g.height() : g.children("table").height();
          })), !a.resizable_includeFooter && s.$table.children("tfoot").length && (l -= s.$table.children("tfoot").height()), o = parseFloat(e.fn.jquery) >= 3.3 ? 0 : s.$table.position().left, c.each(function() {
            var g = e(this), b = parseInt(g.attr("data-column"), 10), w = s.columns - 1, C = g.data("header");
            C && (!C.is(":visible") || !a.resizable_addLastColumn && i.resizable.checkVisibleColumns(s, b) ? g.hide() : (b < w || b === w && a.resizable_addLastColumn) && g.css({
              display: "inline-block",
              height: l,
              left: C.position().left - o + C.outerWidth() - f
            }));
          });
        },
        // Fixes #1485
        checkVisibleColumns: function(s, a) {
          var o, l = 0;
          for (o = a + 1; o < s.columns; o++)
            l += s.$headerIndexed[o].is(":visible") ? 1 : 0;
          return l === 0;
        },
        // prevent text selection while dragging resize bar
        toggleTextSelection: function(s, a, o) {
          var l = s.namespace + "tsresize";
          a.resizable_vars.disabled = o, e("body").toggleClass(i.css.resizableNoSelect, o), o ? e("body").attr("unselectable", "on").bind("selectstart" + l, !1) : e("body").removeAttr("unselectable").unbind("selectstart" + l);
        },
        bindings: function(s, a) {
          var o = s.namespace + "tsresize";
          a.$resizable_container.children().bind("mousedown", function(l) {
            var c, f = a.resizable_vars, g = e(s.namespace + "_extra_headers"), b = e(l.target).data("header");
            c = parseInt(b.attr("data-column"), 10), f.$target = b = b.add(g.filter('[data-column="' + c + '"]')), f.target = c, f.$next = l.shiftKey || a.resizable_targetLast ? b.parent().children().not(".resizable-false").filter(":last") : b.nextAll(":not(.resizable-false)").eq(0), c = parseInt(f.$next.attr("data-column"), 10), f.$next = f.$next.add(g.filter('[data-column="' + c + '"]')), f.next = c, f.mouseXPosition = l.pageX, i.resizable.updateStoredSizes(s, a), i.resizable.toggleTextSelection(s, a, !0);
          }), e(document).bind("mousemove" + o, function(l) {
            var c = a.resizable_vars;
            !c.disabled || c.mouseXPosition === 0 || !c.$target || (a.resizable_throttle ? (clearTimeout(c.timer), c.timer = setTimeout(function() {
              i.resizable.mouseMove(s, a, l);
            }, isNaN(a.resizable_throttle) ? 5 : a.resizable_throttle)) : i.resizable.mouseMove(s, a, l));
          }).bind("mouseup" + o, function() {
            a.resizable_vars.disabled && (i.resizable.toggleTextSelection(s, a, !1), i.resizable.stopResize(s, a), i.resizable.setHandlePosition(s, a));
          }), e(r).bind("resize" + o + " resizeEnd" + o, function() {
            i.resizable.setHandlePosition(s, a);
          }), s.$table.bind("columnUpdate pagerComplete resizableUpdate ".split(" ").join(o + " "), function() {
            i.resizable.setHandlePosition(s, a);
          }).bind("resizableReset" + o, function() {
            i.resizableReset(s.table);
          }).find("thead:first").add(e(s.namespace + "_extra_table").find("thead:first")).bind("contextmenu" + o, function() {
            var l = a.resizable_vars.storedSizes.length === 0;
            return i.resizableReset(s.table), i.resizable.setHandlePosition(s, a), a.resizable_vars.storedSizes = [], l;
          });
        },
        mouseMove: function(s, a, o) {
          if (!(a.resizable_vars.mouseXPosition === 0 || !a.resizable_vars.$target)) {
            var l, c = 0, f = a.resizable_vars, g = f.$next, b = f.storedSizes[f.target], w = o.pageX - f.mouseXPosition;
            if (f.overflow) {
              if (b + w > 0) {
                for (f.storedSizes[f.target] += w, i.resizable.setWidth(f.$target, f.storedSizes[f.target], !0), l = 0; l < s.columns; l++)
                  c += f.storedSizes[l];
                i.resizable.setWidth(s.$table.add(e(s.namespace + "_extra_table")), c);
              }
              g.length || (f.$wrap[0].scrollLeft = s.$table.width());
            } else
              f.fullWidth ? (f.storedSizes[f.target] += w, f.storedSizes[f.next] -= w, i.resizable.setWidths(s, a)) : (f.storedSizes[f.target] += w, i.resizable.setWidths(s, a));
            f.mouseXPosition = o.pageX, s.$table.triggerHandler("stickyHeadersUpdate");
          }
        },
        stopResize: function(s, a) {
          var o = a.resizable_vars;
          i.resizable.updateStoredSizes(s, a), o.useStorage && (i.storage(s.table, i.css.resizableStorage, o.storedSizes), i.storage(s.table, "tablesorter-table-resized-width", s.$table.width())), o.mouseXPosition = 0, o.$target = o.$next = null, s.$table.triggerHandler("stickyHeadersUpdate"), s.$table.triggerHandler("resizableComplete");
        }
      }, i.addWidget({
        id: "resizable",
        priority: 40,
        options: {
          resizable: !0,
          // save column widths to storage
          resizable_addLastColumn: !1,
          resizable_includeFooter: !0,
          resizable_widths: [],
          resizable_throttle: !1,
          // set to true (5ms) or any number 0-10 range
          resizable_targetLast: !1
        },
        init: function(s, a, o, l) {
          i.resizable.init(o, l);
        },
        format: function(s, a, o) {
          i.resizable.setHandlePosition(a, o);
        },
        remove: function(s, a, o, l) {
          if (o.$resizable_container) {
            var c = a.namespace + "tsresize";
            a.$table.add(e(a.namespace + "_extra_table")).removeClass("hasResizable").children("thead").unbind("contextmenu" + c), o.$resizable_container.remove(), i.resizable.toggleTextSelection(a, o, !1), i.resizableReset(s, l), e(document).unbind("mousemove" + c + " mouseup" + c);
          }
        }
      }), i.resizableReset = function(s, a) {
        e(s).each(function() {
          var o, l, c = this.config, f = c && c.widgetOptions, g = f.resizable_vars;
          if (s && c && c.$headerIndexed.length) {
            for (g.overflow && g.tableWidth && (i.resizable.setWidth(c.$table, g.tableWidth, !0), g.useStorage && i.storage(s, "tablesorter-table-resized-width", g.tableWidth)), o = 0; o < c.columns; o++)
              l = c.$headerIndexed[o], f.resizable_widths && f.resizable_widths[o] ? i.resizable.setWidth(l, f.resizable_widths[o], g.overflow) : l.hasClass("resizable-false") || i.resizable.setWidth(l, "", g.overflow);
            c.$table.triggerHandler("stickyHeadersUpdate"), i.storage && !a && i.storage(this, i.css.resizableStorage, []);
          }
        });
      };
    }(n, window), function(e) {
      var r = e.tablesorter || {};
      function i(a) {
        var o = r.storage(a.table, "tablesorter-savesort");
        return o && o.hasOwnProperty("sortList") && e.isArray(o.sortList) ? o.sortList : [];
      }
      function s(a, o) {
        return (o || i(a)).join(",") !== a.sortList.join(",");
      }
      r.addWidget({
        id: "saveSort",
        priority: 20,
        options: {
          saveSort: !0
        },
        init: function(a, o, l, c) {
          o.format(a, l, c, !0);
        },
        format: function(a, o, l, c) {
          var f, g = o.$table, b = l.saveSort !== !1, w = { sortList: o.sortList }, C = r.debug(o, "saveSort");
          C && (f = /* @__PURE__ */ new Date()), g.hasClass("hasSaveSort") ? b && a.hasInitialized && r.storage && s(o) && (r.storage(a, "tablesorter-savesort", w), C && console.log("saveSort >> Saving last sort: " + o.sortList + r.benchmark(f))) : (g.addClass("hasSaveSort"), w = "", r.storage && (w = i(o), C && console.log('saveSort >> Last sort loaded: "' + w + '"' + r.benchmark(f)), g.bind("saveSortReset", function(m) {
            m.stopPropagation(), r.storage(a, "tablesorter-savesort", "");
          })), c && w && w.length > 0 ? o.sortList = w : a.hasInitialized && w && w.length > 0 && s(o, w) && r.sortOn(o, w));
        },
        remove: function(a, o) {
          o.$table.removeClass("hasSaveSort"), r.storage && r.storage(a, "tablesorter-savesort", "");
        }
      });
    }(n), n.tablesorter;
  });
})(U0);
var wh = { exports: {} };
/*!
 * typeahead.js 0.11.1
 * https://github.com/twitter/typeahead.js
 * Copyright 2013-2015 Twitter, Inc. and other contributors; Licensed MIT
 */
(function(t, n) {
  (function(e, r) {
    t.exports = r(wa);
  })(sn, function(e) {
    var r = function() {
      return {
        isMsie: function() {
          return /(msie|trident)/i.test(navigator.userAgent) ? navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2] : !1;
        },
        isBlankString: function(C) {
          return !C || /^\s*$/.test(C);
        },
        escapeRegExChars: function(C) {
          return C.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
        },
        isString: function(C) {
          return typeof C == "string";
        },
        isNumber: function(C) {
          return typeof C == "number";
        },
        isArray: e.isArray,
        isFunction: e.isFunction,
        isObject: e.isPlainObject,
        isUndefined: function(C) {
          return typeof C > "u";
        },
        isElement: function(C) {
          return !!(C && C.nodeType === 1);
        },
        isJQuery: function(C) {
          return C instanceof e;
        },
        toStr: function(m) {
          return r.isUndefined(m) || m === null ? "" : m + "";
        },
        bind: e.proxy,
        each: function(C, m) {
          e.each(C, h);
          function h(u, p) {
            return m(p, u);
          }
        },
        map: e.map,
        filter: e.grep,
        every: function(C, m) {
          var h = !0;
          return C ? (e.each(C, function(u, p) {
            if (!(h = m.call(null, p, u, C)))
              return !1;
          }), !!h) : h;
        },
        some: function(C, m) {
          var h = !1;
          return C ? (e.each(C, function(u, p) {
            if (h = m.call(null, p, u, C))
              return !1;
          }), !!h) : h;
        },
        mixin: e.extend,
        identity: function(C) {
          return C;
        },
        clone: function(C) {
          return e.extend(!0, {}, C);
        },
        getIdGenerator: function() {
          var C = 0;
          return function() {
            return C++;
          };
        },
        templatify: function(m) {
          return e.isFunction(m) ? m : h;
          function h() {
            return String(m);
          }
        },
        defer: function(C) {
          setTimeout(C, 0);
        },
        debounce: function(C, m, h) {
          var u, p;
          return function() {
            var v = this, S = arguments, T, A;
            return T = function() {
              u = null, h || (p = C.apply(v, S));
            }, A = h && !u, clearTimeout(u), u = setTimeout(T, m), A && (p = C.apply(v, S)), p;
          };
        },
        throttle: function(C, m) {
          var h, u, p, v, S, T;
          return S = 0, T = function() {
            S = /* @__PURE__ */ new Date(), p = null, v = C.apply(h, u);
          }, function() {
            var A = /* @__PURE__ */ new Date(), y = m - (A - S);
            return h = this, u = arguments, y <= 0 ? (clearTimeout(p), p = null, S = A, v = C.apply(h, u)) : p || (p = setTimeout(T, y)), v;
          };
        },
        stringify: function(C) {
          return r.isString(C) ? C : JSON.stringify(C);
        },
        noop: function() {
        }
      };
    }(), i = "0.11.1", s = function() {
      return {
        nonword: m,
        whitespace: C,
        obj: {
          nonword: h(m),
          whitespace: h(C)
        }
      };
      function C(u) {
        return u = r.toStr(u), u ? u.split(/\s+/) : [];
      }
      function m(u) {
        return u = r.toStr(u), u ? u.split(/\W+/) : [];
      }
      function h(u) {
        return function(v) {
          return v = r.isArray(v) ? v : [].slice.call(arguments, 0), function(T) {
            var A = [];
            return r.each(v, function(y) {
              A = A.concat(u(r.toStr(T[y])));
            }), A;
          };
        };
      }
    }(), a = function() {
      function C(u) {
        this.maxSize = r.isNumber(u) ? u : 100, this.reset(), this.maxSize <= 0 && (this.set = this.get = e.noop);
      }
      r.mixin(C.prototype, {
        set: function(p, v) {
          var S = this.list.tail, T;
          this.size >= this.maxSize && (this.list.remove(S), delete this.hash[S.key], this.size--), (T = this.hash[p]) ? (T.val = v, this.list.moveToFront(T)) : (T = new h(p, v), this.list.add(T), this.hash[p] = T, this.size++);
        },
        get: function(p) {
          var v = this.hash[p];
          if (v)
            return this.list.moveToFront(v), v.val;
        },
        reset: function() {
          this.size = 0, this.hash = {}, this.list = new m();
        }
      });
      function m() {
        this.head = this.tail = null;
      }
      r.mixin(m.prototype, {
        add: function(p) {
          this.head && (p.next = this.head, this.head.prev = p), this.head = p, this.tail = this.tail || p;
        },
        remove: function(p) {
          p.prev ? p.prev.next = p.next : this.head = p.next, p.next ? p.next.prev = p.prev : this.tail = p.prev;
        },
        moveToFront: function(u) {
          this.remove(u), this.add(u);
        }
      });
      function h(u, p) {
        this.key = u, this.val = p, this.prev = this.next = null;
      }
      return C;
    }(), o = function() {
      var C;
      try {
        C = window.localStorage, C.setItem("~~~", "!"), C.removeItem("~~~");
      } catch {
        C = null;
      }
      function m(S, T) {
        this.prefix = ["__", S, "__"].join(""), this.ttlKey = "__ttl__", this.keyMatcher = new RegExp("^" + r.escapeRegExChars(this.prefix)), this.ls = T || C, !this.ls && this._noop();
      }
      return r.mixin(m.prototype, {
        _prefix: function(S) {
          return this.prefix + S;
        },
        _ttlKey: function(S) {
          return this._prefix(S) + this.ttlKey;
        },
        _noop: function() {
          this.get = this.set = this.remove = this.clear = this.isExpired = r.noop;
        },
        _safeSet: function(S, T) {
          try {
            this.ls.setItem(S, T);
          } catch (A) {
            A.name === "QuotaExceededError" && (this.clear(), this._noop());
          }
        },
        get: function(S) {
          return this.isExpired(S) && this.remove(S), p(this.ls.getItem(this._prefix(S)));
        },
        set: function(S, T, A) {
          return r.isNumber(A) ? this._safeSet(this._ttlKey(S), u(h() + A)) : this.ls.removeItem(this._ttlKey(S)), this._safeSet(this._prefix(S), u(T));
        },
        remove: function(S) {
          return this.ls.removeItem(this._ttlKey(S)), this.ls.removeItem(this._prefix(S)), this;
        },
        clear: function() {
          var S, T = v(this.keyMatcher);
          for (S = T.length; S--; )
            this.remove(T[S]);
          return this;
        },
        isExpired: function(S) {
          var T = p(this.ls.getItem(this._ttlKey(S)));
          return !!(r.isNumber(T) && h() > T);
        }
      }), m;
      function h() {
        return (/* @__PURE__ */ new Date()).getTime();
      }
      function u(S) {
        return JSON.stringify(r.isUndefined(S) ? null : S);
      }
      function p(S) {
        return e.parseJSON(S);
      }
      function v(S) {
        var T, A, y = [], I = C.length;
        for (T = 0; T < I; T++)
          (A = C.key(T)).match(S) && y.push(A.replace(S, ""));
        return y;
      }
    }(), l = function() {
      var C = 0, m = {}, h = 6, u = new a(10);
      function p(v) {
        v = v || {}, this.cancelled = !1, this.lastReq = null, this._send = v.transport, this._get = v.limiter ? v.limiter(this._get) : this._get, this._cache = v.cache === !1 ? new a(0) : u;
      }
      return p.setMaxPendingRequests = function(S) {
        h = S;
      }, p.resetCache = function() {
        u.reset();
      }, r.mixin(p.prototype, {
        _fingerprint: function(S) {
          return S = S || {}, S.url + S.type + e.param(S.data || {});
        },
        _get: function(v, S) {
          var T = this, A, y;
          if (A = this._fingerprint(v), this.cancelled || A !== this.lastReq)
            return;
          (y = m[A]) ? y.done(I).fail($) : C < h ? (C++, m[A] = this._send(v).done(I).fail($).always(V)) : this.onDeckRequestArgs = [].slice.call(arguments, 0);
          function I(z) {
            S(null, z), T._cache.set(A, z);
          }
          function $() {
            S(!0);
          }
          function V() {
            C--, delete m[A], T.onDeckRequestArgs && (T._get.apply(T, T.onDeckRequestArgs), T.onDeckRequestArgs = null);
          }
        },
        get: function(v, S) {
          var T, A;
          S = S || e.noop, v = r.isString(v) ? {
            url: v
          } : v || {}, A = this._fingerprint(v), this.cancelled = !1, this.lastReq = A, (T = this._cache.get(A)) ? S(null, T) : this._get(v, S);
        },
        cancel: function() {
          this.cancelled = !0;
        }
      }), p;
    }(), c = window.SearchIndex = function() {
      var C = "c", m = "i";
      function h(T) {
        T = T || {}, (!T.datumTokenizer || !T.queryTokenizer) && e.error("datumTokenizer and queryTokenizer are both required"), this.identify = T.identify || r.stringify, this.datumTokenizer = T.datumTokenizer, this.queryTokenizer = T.queryTokenizer, this.reset();
      }
      return r.mixin(h.prototype, {
        bootstrap: function(A) {
          this.datums = A.datums, this.trie = A.trie;
        },
        add: function(T) {
          var A = this;
          T = r.isArray(T) ? T : [T], r.each(T, function(y) {
            var I, $;
            A.datums[I = A.identify(y)] = y, $ = u(A.datumTokenizer(y)), r.each($, function(V) {
              var z, F, P;
              for (z = A.trie, F = V.split(""); P = F.shift(); )
                z = z[C][P] || (z[C][P] = p()), z[m].push(I);
            });
          });
        },
        get: function(A) {
          var y = this;
          return r.map(A, function(I) {
            return y.datums[I];
          });
        },
        search: function(A) {
          var y = this, I, $;
          return I = u(this.queryTokenizer(A)), r.each(I, function(V) {
            var z, F, P, k;
            if ($ && $.length === 0)
              return !1;
            for (z = y.trie, F = V.split(""); z && (P = F.shift()); )
              z = z[C][P];
            if (z && F.length === 0)
              k = z[m].slice(0), $ = $ ? S($, k) : k;
            else
              return $ = [], !1;
          }), $ ? r.map(v($), function(V) {
            return y.datums[V];
          }) : [];
        },
        all: function() {
          var A = [];
          for (var y in this.datums)
            A.push(this.datums[y]);
          return A;
        },
        reset: function() {
          this.datums = {}, this.trie = p();
        },
        serialize: function() {
          return {
            datums: this.datums,
            trie: this.trie
          };
        }
      }), h;
      function u(T) {
        return T = r.filter(T, function(A) {
          return !!A;
        }), T = r.map(T, function(A) {
          return A.toLowerCase();
        }), T;
      }
      function p() {
        var T = {};
        return T[m] = [], T[C] = {}, T;
      }
      function v(T) {
        for (var A = {}, y = [], I = 0, $ = T.length; I < $; I++)
          A[T[I]] || (A[T[I]] = !0, y.push(T[I]));
        return y;
      }
      function S(T, A) {
        var y = 0, I = 0, $ = [];
        T = T.sort(), A = A.sort();
        for (var V = T.length, z = A.length; y < V && I < z; )
          T[y] < A[I] ? y++ : (T[y] > A[I] || ($.push(T[y]), y++), I++);
        return $;
      }
    }(), f = function() {
      var C;
      C = {
        data: "data",
        protocol: "protocol",
        thumbprint: "thumbprint"
      };
      function m(h) {
        this.url = h.url, this.ttl = h.ttl, this.cache = h.cache, this.prepare = h.prepare, this.transform = h.transform, this.transport = h.transport, this.thumbprint = h.thumbprint, this.storage = new o(h.cacheKey);
      }
      return r.mixin(m.prototype, {
        _settings: function() {
          return {
            url: this.url,
            type: "GET",
            dataType: "json"
          };
        },
        store: function(u) {
          this.cache && (this.storage.set(C.data, u, this.ttl), this.storage.set(C.protocol, location.protocol, this.ttl), this.storage.set(C.thumbprint, this.thumbprint, this.ttl));
        },
        fromCache: function() {
          var u = {}, p;
          return this.cache ? (u.data = this.storage.get(C.data), u.protocol = this.storage.get(C.protocol), u.thumbprint = this.storage.get(C.thumbprint), p = u.thumbprint !== this.thumbprint || u.protocol !== location.protocol, u.data && !p ? u.data : null) : null;
        },
        fromNetwork: function(h) {
          var u = this, p;
          if (!h)
            return;
          p = this.prepare(this._settings()), this.transport(p).fail(v).done(S);
          function v() {
            h(!0);
          }
          function S(T) {
            h(null, u.transform(T));
          }
        },
        clear: function() {
          return this.storage.clear(), this;
        }
      }), m;
    }(), g = function() {
      function C(m) {
        this.url = m.url, this.prepare = m.prepare, this.transform = m.transform, this.transport = new l({
          cache: m.cache,
          limiter: m.limiter,
          transport: m.transport
        });
      }
      return r.mixin(C.prototype, {
        _settings: function() {
          return {
            url: this.url,
            type: "GET",
            dataType: "json"
          };
        },
        get: function(h, u) {
          var p = this, v;
          if (!u)
            return;
          return h = h || "", v = this.prepare(h, this._settings()), this.transport.get(v, S);
          function S(T, A) {
            u(T ? [] : p.transform(A));
          }
        },
        cancelLastRequest: function() {
          this.transport.cancel();
        }
      }), C;
    }(), b = /* @__PURE__ */ function() {
      return function(S) {
        var T, A;
        return T = {
          initialize: !0,
          identify: r.stringify,
          datumTokenizer: null,
          queryTokenizer: null,
          sufficient: 5,
          sorter: null,
          local: [],
          prefetch: null,
          remote: null
        }, S = r.mixin(T, S || {}), !S.datumTokenizer && e.error("datumTokenizer is required"), !S.queryTokenizer && e.error("queryTokenizer is required"), A = S.sorter, S.sorter = A ? function(y) {
          return y.sort(A);
        } : r.identity, S.local = r.isFunction(S.local) ? S.local() : S.local, S.prefetch = C(S.prefetch), S.remote = m(S.remote), S;
      };
      function C(v) {
        var S;
        return v ? (S = {
          url: null,
          ttl: 24 * 60 * 60 * 1e3,
          cache: !0,
          cacheKey: null,
          thumbprint: "",
          prepare: r.identity,
          transform: r.identity,
          transport: null
        }, v = r.isString(v) ? {
          url: v
        } : v, v = r.mixin(S, v), !v.url && e.error("prefetch requires url to be set"), v.transform = v.filter || v.transform, v.cacheKey = v.cacheKey || v.url, v.thumbprint = i + v.thumbprint, v.transport = v.transport ? p(v.transport) : e.ajax, v) : null;
      }
      function m(v) {
        var S;
        if (v)
          return S = {
            url: null,
            cache: !0,
            prepare: null,
            replace: null,
            wildcard: null,
            limiter: null,
            rateLimitBy: "debounce",
            rateLimitWait: 300,
            transform: r.identity,
            transport: null
          }, v = r.isString(v) ? {
            url: v
          } : v, v = r.mixin(S, v), !v.url && e.error("remote requires url to be set"), v.transform = v.filter || v.transform, v.prepare = h(v), v.limiter = u(v), v.transport = v.transport ? p(v.transport) : e.ajax, delete v.replace, delete v.wildcard, delete v.rateLimitBy, delete v.rateLimitWait, v;
      }
      function h(v) {
        var S, T, A;
        if (S = v.prepare, T = v.replace, A = v.wildcard, S)
          return S;
        return T ? S = y : v.wildcard ? S = I : S = $, S;
        function y(V, z) {
          return z.url = T(z.url, V), z;
        }
        function I(V, z) {
          return z.url = z.url.replace(A, encodeURIComponent(V)), z;
        }
        function $(V, z) {
          return z;
        }
      }
      function u(v) {
        var S, T, A;
        return S = v.limiter, T = v.rateLimitBy, A = v.rateLimitWait, S || (S = /^throttle$/i.test(T) ? I(A) : y(A)), S;
        function y($) {
          return function(z) {
            return r.debounce(z, $);
          };
        }
        function I($) {
          return function(z) {
            return r.throttle(z, $);
          };
        }
      }
      function p(v) {
        return function(T) {
          var A = e.Deferred();
          return v(T, y, I), A;
          function y($) {
            r.defer(function() {
              A.resolve($);
            });
          }
          function I($) {
            r.defer(function() {
              A.reject($);
            });
          }
        };
      }
    }(), w = function() {
      var C;
      C = window && window.Bloodhound;
      function m(h) {
        h = b(h), this.sorter = h.sorter, this.identify = h.identify, this.sufficient = h.sufficient, this.local = h.local, this.remote = h.remote ? new g(h.remote) : null, this.prefetch = h.prefetch ? new f(h.prefetch) : null, this.index = new c({
          identify: this.identify,
          datumTokenizer: h.datumTokenizer,
          queryTokenizer: h.queryTokenizer
        }), h.initialize !== !1 && this.initialize();
      }
      return m.noConflict = function() {
        return window && (window.Bloodhound = C), m;
      }, m.tokenizers = s, r.mixin(m.prototype, {
        __ttAdapter: function() {
          var u = this;
          return this.remote ? p : v;
          function p(S, T, A) {
            return u.search(S, T, A);
          }
          function v(S, T) {
            return u.search(S, T);
          }
        },
        _loadPrefetch: function() {
          var u = this, p, v;
          return p = e.Deferred(), this.prefetch ? (v = this.prefetch.fromCache()) ? (this.index.bootstrap(v), p.resolve()) : this.prefetch.fromNetwork(S) : p.resolve(), p.promise();
          function S(T, A) {
            if (T)
              return p.reject();
            u.add(A), u.prefetch.store(u.index.serialize()), p.resolve();
          }
        },
        _initialize: function() {
          var u = this;
          return this.clear(), (this.initPromise = this._loadPrefetch()).done(p), this.initPromise;
          function p() {
            u.add(u.local);
          }
        },
        initialize: function(u) {
          return !this.initPromise || u ? this._initialize() : this.initPromise;
        },
        add: function(u) {
          return this.index.add(u), this;
        },
        get: function(u) {
          return u = r.isArray(u) ? u : [].slice.call(arguments), this.index.get(u);
        },
        search: function(u, p, v) {
          var S = this, T;
          return T = this.sorter(this.index.search(u)), p(this.remote ? T.slice() : T), this.remote && T.length < this.sufficient ? this.remote.get(u, A) : this.remote && this.remote.cancelLastRequest(), this;
          function A(y) {
            var I = [];
            r.each(y, function($) {
              !r.some(T, function(V) {
                return S.identify($) === S.identify(V);
              }) && I.push($);
            }), v && v(I);
          }
        },
        all: function() {
          return this.index.all();
        },
        clear: function() {
          return this.index.reset(), this;
        },
        clearPrefetchCache: function() {
          return this.prefetch && this.prefetch.clear(), this;
        },
        clearRemoteCache: function() {
          return l.resetCache(), this;
        },
        ttAdapter: function() {
          return this.__ttAdapter();
        }
      }), m;
    }();
    return w;
  }), function(e, r) {
    t.exports = r(wa);
  }(sn, function(e) {
    var r = function() {
      return {
        isMsie: function() {
          return /(msie|trident)/i.test(navigator.userAgent) ? navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2] : !1;
        },
        isBlankString: function(w) {
          return !w || /^\s*$/.test(w);
        },
        escapeRegExChars: function(w) {
          return w.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
        },
        isString: function(w) {
          return typeof w == "string";
        },
        isNumber: function(w) {
          return typeof w == "number";
        },
        isArray: e.isArray,
        isFunction: e.isFunction,
        isObject: e.isPlainObject,
        isUndefined: function(w) {
          return typeof w > "u";
        },
        isElement: function(w) {
          return !!(w && w.nodeType === 1);
        },
        isJQuery: function(w) {
          return w instanceof e;
        },
        toStr: function(C) {
          return r.isUndefined(C) || C === null ? "" : C + "";
        },
        bind: e.proxy,
        each: function(w, C) {
          e.each(w, m);
          function m(h, u) {
            return C(u, h);
          }
        },
        map: e.map,
        filter: e.grep,
        every: function(w, C) {
          var m = !0;
          return w ? (e.each(w, function(h, u) {
            if (!(m = C.call(null, u, h, w)))
              return !1;
          }), !!m) : m;
        },
        some: function(w, C) {
          var m = !1;
          return w ? (e.each(w, function(h, u) {
            if (m = C.call(null, u, h, w))
              return !1;
          }), !!m) : m;
        },
        mixin: e.extend,
        identity: function(w) {
          return w;
        },
        clone: function(w) {
          return e.extend(!0, {}, w);
        },
        getIdGenerator: function() {
          var w = 0;
          return function() {
            return w++;
          };
        },
        templatify: function(C) {
          return e.isFunction(C) ? C : m;
          function m() {
            return String(C);
          }
        },
        defer: function(w) {
          setTimeout(w, 0);
        },
        debounce: function(w, C, m) {
          var h, u;
          return function() {
            var p = this, v = arguments, S, T;
            return S = function() {
              h = null, m || (u = w.apply(p, v));
            }, T = m && !h, clearTimeout(h), h = setTimeout(S, C), T && (u = w.apply(p, v)), u;
          };
        },
        throttle: function(w, C) {
          var m, h, u, p, v, S;
          return v = 0, S = function() {
            v = /* @__PURE__ */ new Date(), u = null, p = w.apply(m, h);
          }, function() {
            var T = /* @__PURE__ */ new Date(), A = C - (T - v);
            return m = this, h = arguments, A <= 0 ? (clearTimeout(u), u = null, v = T, p = w.apply(m, h)) : u || (u = setTimeout(S, A)), p;
          };
        },
        stringify: function(w) {
          return r.isString(w) ? w : JSON.stringify(w);
        },
        noop: function() {
        }
      };
    }(), i = /* @__PURE__ */ function() {
      var w = {
        wrapper: "twitter-typeahead",
        input: "tt-input",
        hint: "tt-hint",
        menu: "tt-menu",
        dataset: "tt-dataset",
        suggestion: "tt-suggestion",
        selectable: "tt-selectable",
        empty: "tt-empty",
        open: "tt-open",
        cursor: "tt-cursor",
        highlight: "tt-highlight"
      };
      return C;
      function C(p) {
        var v, S;
        return S = r.mixin({}, w, p), v = {
          css: u(),
          classes: S,
          html: m(S),
          selectors: h(S)
        }, {
          css: v.css,
          html: v.html,
          classes: v.classes,
          selectors: v.selectors,
          mixin: function(T) {
            r.mixin(T, v);
          }
        };
      }
      function m(p) {
        return {
          wrapper: '<span class="' + p.wrapper + '"></span>',
          menu: '<div class="' + p.menu + '"></div>'
        };
      }
      function h(p) {
        var v = {};
        return r.each(p, function(S, T) {
          v[T] = "." + S;
        }), v;
      }
      function u() {
        var p = {
          wrapper: {
            position: "relative",
            display: "inline-block"
          },
          hint: {
            position: "absolute",
            top: "0",
            left: "0",
            borderColor: "transparent",
            boxShadow: "none",
            opacity: "1"
          },
          input: {
            position: "relative",
            verticalAlign: "top",
            backgroundColor: "transparent"
          },
          inputWithNoHint: {
            position: "relative",
            verticalAlign: "top"
          },
          menu: {
            position: "absolute",
            top: "100%",
            left: "0",
            zIndex: "100",
            display: "none"
          },
          ltr: {
            left: "0",
            right: "auto"
          },
          rtl: {
            left: "auto",
            right: " 0"
          }
        };
        return r.isMsie() && r.mixin(p.input, {
          backgroundImage: "url(data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)"
        }), p;
      }
    }(), s = function() {
      var w, C;
      w = "typeahead:", C = {
        render: "rendered",
        cursorchange: "cursorchanged",
        select: "selected",
        autocomplete: "autocompleted"
      };
      function m(h) {
        (!h || !h.el) && e.error("EventBus initialized without el"), this.$el = e(h.el);
      }
      return r.mixin(m.prototype, {
        _trigger: function(h, u) {
          var p;
          return p = e.Event(w + h), (u = u || []).unshift(p), this.$el.trigger.apply(this.$el, u), p;
        },
        before: function(h) {
          var u, p;
          return u = [].slice.call(arguments, 1), p = this._trigger("before" + h, u), p.isDefaultPrevented();
        },
        trigger: function(h) {
          var u;
          this._trigger(h, [].slice.call(arguments, 1)), (u = C[h]) && this._trigger(u, [].slice.call(arguments, 1));
        }
      }), m;
    }(), a = function() {
      var w = /\s+/, C = T();
      return {
        onSync: u,
        onAsync: h,
        off: p,
        trigger: v
      };
      function m(y, I, $, V) {
        var z;
        if (!$)
          return this;
        for (I = I.split(w), $ = V ? A($, V) : $, this._callbacks = this._callbacks || {}; z = I.shift(); )
          this._callbacks[z] = this._callbacks[z] || {
            sync: [],
            async: []
          }, this._callbacks[z][y].push($);
        return this;
      }
      function h(y, I, $) {
        return m.call(this, "async", y, I, $);
      }
      function u(y, I, $) {
        return m.call(this, "sync", y, I, $);
      }
      function p(y) {
        var I;
        if (!this._callbacks)
          return this;
        for (y = y.split(w); I = y.shift(); )
          delete this._callbacks[I];
        return this;
      }
      function v(y) {
        var I, $, V, z, F;
        if (!this._callbacks)
          return this;
        for (y = y.split(w), V = [].slice.call(arguments, 1); (I = y.shift()) && ($ = this._callbacks[I]); )
          z = S($.sync, this, [I].concat(V)), F = S($.async, this, [I].concat(V)), z() && C(F);
        return this;
      }
      function S(y, I, $) {
        return V;
        function V() {
          for (var z, F = 0, P = y.length; !z && F < P; F += 1)
            z = y[F].apply(I, $) === !1;
          return !z;
        }
      }
      function T() {
        var y;
        return window.setImmediate ? y = function($) {
          setImmediate(function() {
            $();
          });
        } : y = function($) {
          setTimeout(function() {
            $();
          }, 0);
        }, y;
      }
      function A(y, I) {
        return y.bind ? y.bind(I) : function() {
          y.apply(I, [].slice.call(arguments, 0));
        };
      }
    }(), o = /* @__PURE__ */ function(w) {
      var C = {
        node: null,
        pattern: null,
        tagName: "strong",
        className: null,
        wordsOnly: !1,
        caseSensitive: !1
      };
      return function(u) {
        var p;
        if (u = r.mixin({}, C, u), !u.node || !u.pattern)
          return;
        u.pattern = r.isArray(u.pattern) ? u.pattern : [u.pattern], p = m(u.pattern, u.caseSensitive, u.wordsOnly), S(u.node, v);
        function v(T) {
          var A, y, I;
          return (A = p.exec(T.data)) && (I = w.createElement(u.tagName), u.className && (I.className = u.className), y = T.splitText(A.index), y.splitText(A[0].length), I.appendChild(y.cloneNode(!0)), T.parentNode.replaceChild(I, y)), !!A;
        }
        function S(T, A) {
          for (var y, I = 3, $ = 0; $ < T.childNodes.length; $++)
            y = T.childNodes[$], y.nodeType === I ? $ += A(y) ? 1 : 0 : S(y, A);
        }
      };
      function m(h, u, p) {
        for (var v = [], S, T = 0, A = h.length; T < A; T++)
          v.push(r.escapeRegExChars(h[T]));
        return S = p ? "\\b(" + v.join("|") + ")\\b" : "(" + v.join("|") + ")", u ? new RegExp(S) : new RegExp(S, "i");
      }
    }(window.document), l = function() {
      var w;
      w = {
        9: "tab",
        27: "esc",
        37: "left",
        39: "right",
        13: "enter",
        38: "up",
        40: "down"
      };
      function C(p, v) {
        p = p || {}, p.input || e.error("input is missing"), v.mixin(this), this.$hint = e(p.hint), this.$input = e(p.input), this.query = this.$input.val(), this.queryWhenFocused = this.hasFocus() ? this.query : null, this.$overflowHelper = m(this.$input), this._checkLanguageDirection(), this.$hint.length === 0 && (this.setHint = this.getHint = this.clearHint = this.clearHintIfInvalid = r.noop);
      }
      return C.normalizeQuery = function(p) {
        return r.toStr(p).replace(/^\s*/g, "").replace(/\s{2,}/g, " ");
      }, r.mixin(C.prototype, a, {
        _onBlur: function() {
          this.resetInputValue(), this.trigger("blurred");
        },
        _onFocus: function() {
          this.queryWhenFocused = this.query, this.trigger("focused");
        },
        _onKeydown: function(v) {
          var S = w[v.which || v.keyCode];
          this._managePreventDefault(S, v), S && this._shouldTrigger(S, v) && this.trigger(S + "Keyed", v);
        },
        _onInput: function() {
          this._setQuery(this.getInputValue()), this.clearHintIfInvalid(), this._checkLanguageDirection();
        },
        _managePreventDefault: function(v, S) {
          var T;
          switch (v) {
            case "up":
            case "down":
              T = !u(S);
              break;
            default:
              T = !1;
          }
          T && S.preventDefault();
        },
        _shouldTrigger: function(v, S) {
          var T;
          switch (v) {
            case "tab":
              T = !u(S);
              break;
            default:
              T = !0;
          }
          return T;
        },
        _checkLanguageDirection: function() {
          var v = (this.$input.css("direction") || "ltr").toLowerCase();
          this.dir !== v && (this.dir = v, this.$hint.attr("dir", v), this.trigger("langDirChanged", v));
        },
        _setQuery: function(v, S) {
          var T, A;
          T = h(v, this.query), A = T ? this.query.length !== v.length : !1, this.query = v, !S && !T ? this.trigger("queryChanged", this.query) : !S && A && this.trigger("whitespaceChanged", this.query);
        },
        bind: function() {
          var p = this, v, S, T, A;
          return v = r.bind(this._onBlur, this), S = r.bind(this._onFocus, this), T = r.bind(this._onKeydown, this), A = r.bind(this._onInput, this), this.$input.on("blur.tt", v).on("focus.tt", S).on("keydown.tt", T), !r.isMsie() || r.isMsie() > 9 ? this.$input.on("input.tt", A) : this.$input.on("keydown.tt keypress.tt cut.tt paste.tt", function(y) {
            w[y.which || y.keyCode] || r.defer(r.bind(p._onInput, p, y));
          }), this;
        },
        focus: function() {
          this.$input.focus();
        },
        blur: function() {
          this.$input.blur();
        },
        getLangDir: function() {
          return this.dir;
        },
        getQuery: function() {
          return this.query || "";
        },
        setQuery: function(v, S) {
          this.setInputValue(v), this._setQuery(v, S);
        },
        hasQueryChangedSinceLastFocus: function() {
          return this.query !== this.queryWhenFocused;
        },
        getInputValue: function() {
          return this.$input.val();
        },
        setInputValue: function(v) {
          this.$input.val(v), this.clearHintIfInvalid(), this._checkLanguageDirection();
        },
        resetInputValue: function() {
          this.setInputValue(this.query);
        },
        getHint: function() {
          return this.$hint.val();
        },
        setHint: function(v) {
          this.$hint.val(v);
        },
        clearHint: function() {
          this.setHint("");
        },
        clearHintIfInvalid: function() {
          var v, S, T, A;
          v = this.getInputValue(), S = this.getHint(), T = v !== S && S.indexOf(v) === 0, A = v !== "" && T && !this.hasOverflow(), !A && this.clearHint();
        },
        hasFocus: function() {
          return this.$input.is(":focus");
        },
        hasOverflow: function() {
          var v = this.$input.width() - 2;
          return this.$overflowHelper.text(this.getInputValue()), this.$overflowHelper.width() >= v;
        },
        isCursorAtEnd: function() {
          var p, v, S;
          return p = this.$input.val().length, v = this.$input[0].selectionStart, r.isNumber(v) ? v === p : document.selection ? (S = document.selection.createRange(), S.moveStart("character", -p), p === S.text.length) : !0;
        },
        destroy: function() {
          this.$hint.off(".tt"), this.$input.off(".tt"), this.$overflowHelper.remove(), this.$hint = this.$input = this.$overflowHelper = e("<div>");
        }
      }), C;
      function m(p) {
        return e('<pre aria-hidden="true"></pre>').css({
          position: "absolute",
          visibility: "hidden",
          whiteSpace: "pre",
          fontFamily: p.css("font-family"),
          fontSize: p.css("font-size"),
          fontStyle: p.css("font-style"),
          fontVariant: p.css("font-variant"),
          fontWeight: p.css("font-weight"),
          wordSpacing: p.css("word-spacing"),
          letterSpacing: p.css("letter-spacing"),
          textIndent: p.css("text-indent"),
          textRendering: p.css("text-rendering"),
          textTransform: p.css("text-transform")
        }).insertAfter(p);
      }
      function h(p, v) {
        return C.normalizeQuery(p) === C.normalizeQuery(v);
      }
      function u(p) {
        return p.altKey || p.ctrlKey || p.metaKey || p.shiftKey;
      }
    }(), c = function() {
      var w, C;
      w = {
        val: "tt-selectable-display",
        obj: "tt-selectable-object"
      }, C = r.getIdGenerator();
      function m(v, S) {
        v = v || {}, v.templates = v.templates || {}, v.templates.notFound = v.templates.notFound || v.templates.empty, v.source || e.error("missing source"), v.node || e.error("missing node"), v.name && !p(v.name) && e.error("invalid dataset name: " + v.name), S.mixin(this), this.highlight = !!v.highlight, this.name = v.name || C(), this.limit = v.limit || 5, this.displayFn = h(v.display || v.displayKey), this.templates = u(v.templates, this.displayFn), this.source = v.source.__ttAdapter ? v.source.__ttAdapter() : v.source, this.async = r.isUndefined(v.async) ? this.source.length > 2 : !!v.async, this._resetLastSuggestion(), this.$el = e(v.node).addClass(this.classes.dataset).addClass(this.classes.dataset + "-" + this.name);
      }
      return m.extractData = function(S) {
        var T = e(S);
        return T.data(w.obj) ? {
          val: T.data(w.val) || "",
          obj: T.data(w.obj) || null
        } : null;
      }, r.mixin(m.prototype, a, {
        _overwrite: function(S, T) {
          T = T || [], T.length ? this._renderSuggestions(S, T) : this.async && this.templates.pending ? this._renderPending(S) : !this.async && this.templates.notFound ? this._renderNotFound(S) : this._empty(), this.trigger("rendered", this.name, T, !1);
        },
        _append: function(S, T) {
          T = T || [], T.length && this.$lastSuggestion.length ? this._appendSuggestions(S, T) : T.length ? this._renderSuggestions(S, T) : !this.$lastSuggestion.length && this.templates.notFound && this._renderNotFound(S), this.trigger("rendered", this.name, T, !0);
        },
        _renderSuggestions: function(S, T) {
          var A;
          A = this._getSuggestionsFragment(S, T), this.$lastSuggestion = A.children().last(), this.$el.html(A).prepend(this._getHeader(S, T)).append(this._getFooter(S, T));
        },
        _appendSuggestions: function(S, T) {
          var A, y;
          A = this._getSuggestionsFragment(S, T), y = A.children().last(), this.$lastSuggestion.after(A), this.$lastSuggestion = y;
        },
        _renderPending: function(S) {
          var T = this.templates.pending;
          this._resetLastSuggestion(), T && this.$el.html(T({
            query: S,
            dataset: this.name
          }));
        },
        _renderNotFound: function(S) {
          var T = this.templates.notFound;
          this._resetLastSuggestion(), T && this.$el.html(T({
            query: S,
            dataset: this.name
          }));
        },
        _empty: function() {
          this.$el.empty(), this._resetLastSuggestion();
        },
        _getSuggestionsFragment: function(S, T) {
          var A = this, y;
          return y = document.createDocumentFragment(), r.each(T, function($) {
            var V, z;
            z = A._injectQuery(S, $), V = e(A.templates.suggestion(z)).data(w.obj, $).data(w.val, A.displayFn($)).addClass(A.classes.suggestion + " " + A.classes.selectable), y.appendChild(V[0]);
          }), this.highlight && o({
            className: this.classes.highlight,
            node: y,
            pattern: S
          }), e(y);
        },
        _getFooter: function(S, T) {
          return this.templates.footer ? this.templates.footer({
            query: S,
            suggestions: T,
            dataset: this.name
          }) : null;
        },
        _getHeader: function(S, T) {
          return this.templates.header ? this.templates.header({
            query: S,
            suggestions: T,
            dataset: this.name
          }) : null;
        },
        _resetLastSuggestion: function() {
          this.$lastSuggestion = e();
        },
        _injectQuery: function(S, T) {
          return r.isObject(T) ? r.mixin({
            _query: S
          }, T) : T;
        },
        update: function(S) {
          var T = this, A = !1, y = !1, I = 0;
          this.cancel(), this.cancel = function() {
            A = !0, T.cancel = e.noop, T.async && T.trigger("asyncCanceled", S);
          }, this.source(S, $, V), !y && $([]);
          function $(z) {
            y || (y = !0, z = (z || []).slice(0, T.limit), I = z.length, T._overwrite(S, z), I < T.limit && T.async && T.trigger("asyncRequested", S));
          }
          function V(z) {
            z = z || [], !A && I < T.limit && (T.cancel = e.noop, I += z.length, T._append(S, z.slice(0, T.limit - I)), T.async && T.trigger("asyncReceived", S));
          }
        },
        cancel: e.noop,
        clear: function() {
          this._empty(), this.cancel(), this.trigger("cleared");
        },
        isEmpty: function() {
          return this.$el.is(":empty");
        },
        destroy: function() {
          this.$el = e("<div>");
        }
      }), m;
      function h(v) {
        return v = v || r.stringify, r.isFunction(v) ? v : S;
        function S(T) {
          return T[v];
        }
      }
      function u(v, S) {
        return {
          notFound: v.notFound && r.templatify(v.notFound),
          pending: v.pending && r.templatify(v.pending),
          header: v.header && r.templatify(v.header),
          footer: v.footer && r.templatify(v.footer),
          suggestion: v.suggestion || T
        };
        function T(A) {
          return e("<div>").text(S(A));
        }
      }
      function p(v) {
        return /^[_a-zA-Z0-9-]+$/.test(v);
      }
    }(), f = function() {
      function w(C, m) {
        var h = this;
        C = C || {}, C.node || e.error("node is required"), m.mixin(this), this.$node = e(C.node), this.query = null, this.datasets = r.map(C.datasets, u);
        function u(p) {
          var v = h.$node.find(p.node).first();
          return p.node = v.length ? v : e("<div>").appendTo(h.$node), new c(p, m);
        }
      }
      return r.mixin(w.prototype, a, {
        _onSelectableClick: function(m) {
          this.trigger("selectableClicked", e(m.currentTarget));
        },
        _onRendered: function(m, h, u, p) {
          this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty()), this.trigger("datasetRendered", h, u, p);
        },
        _onCleared: function() {
          this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty()), this.trigger("datasetCleared");
        },
        _propagate: function() {
          this.trigger.apply(this, arguments);
        },
        _allDatasetsEmpty: function() {
          return r.every(this.datasets, m);
          function m(h) {
            return h.isEmpty();
          }
        },
        _getSelectables: function() {
          return this.$node.find(this.selectors.selectable);
        },
        _removeCursor: function() {
          var m = this.getActiveSelectable();
          m && m.removeClass(this.classes.cursor);
        },
        _ensureVisible: function(m) {
          var h, u, p, v;
          h = m.position().top, u = h + m.outerHeight(!0), p = this.$node.scrollTop(), v = this.$node.height() + parseInt(this.$node.css("paddingTop"), 10) + parseInt(this.$node.css("paddingBottom"), 10), h < 0 ? this.$node.scrollTop(p + h) : v < u && this.$node.scrollTop(p + (u - v));
        },
        bind: function() {
          var C = this, m;
          return m = r.bind(this._onSelectableClick, this), this.$node.on("click.tt", this.selectors.selectable, m), r.each(this.datasets, function(h) {
            h.onSync("asyncRequested", C._propagate, C).onSync("asyncCanceled", C._propagate, C).onSync("asyncReceived", C._propagate, C).onSync("rendered", C._onRendered, C).onSync("cleared", C._onCleared, C);
          }), this;
        },
        isOpen: function() {
          return this.$node.hasClass(this.classes.open);
        },
        open: function() {
          this.$node.addClass(this.classes.open);
        },
        close: function() {
          this.$node.removeClass(this.classes.open), this._removeCursor();
        },
        setLanguageDirection: function(m) {
          this.$node.attr("dir", m);
        },
        selectableRelativeToCursor: function(m) {
          var h, u, p, v;
          return u = this.getActiveSelectable(), h = this._getSelectables(), p = u ? h.index(u) : -1, v = p + m, v = (v + 1) % (h.length + 1) - 1, v = v < -1 ? h.length - 1 : v, v === -1 ? null : h.eq(v);
        },
        setCursor: function(m) {
          this._removeCursor(), (m = m && m.first()) && (m.addClass(this.classes.cursor), this._ensureVisible(m));
        },
        getSelectableData: function(m) {
          return m && m.length ? c.extractData(m) : null;
        },
        getActiveSelectable: function() {
          var m = this._getSelectables().filter(this.selectors.cursor).first();
          return m.length ? m : null;
        },
        getTopSelectable: function() {
          var m = this._getSelectables().first();
          return m.length ? m : null;
        },
        update: function(m) {
          var h = m !== this.query;
          return h && (this.query = m, r.each(this.datasets, u)), h;
          function u(p) {
            p.update(m);
          }
        },
        empty: function() {
          r.each(this.datasets, m), this.query = null, this.$node.addClass(this.classes.empty);
          function m(h) {
            h.clear();
          }
        },
        destroy: function() {
          this.$node.off(".tt"), this.$node = e("<div>"), r.each(this.datasets, m);
          function m(h) {
            h.destroy();
          }
        }
      }), w;
    }(), g = function() {
      var w = f.prototype;
      function C() {
        f.apply(this, [].slice.call(arguments, 0));
      }
      return r.mixin(C.prototype, f.prototype, {
        open: function() {
          return !this._allDatasetsEmpty() && this._show(), w.open.apply(this, [].slice.call(arguments, 0));
        },
        close: function() {
          return this._hide(), w.close.apply(this, [].slice.call(arguments, 0));
        },
        _onRendered: function() {
          return this._allDatasetsEmpty() ? this._hide() : this.isOpen() && this._show(), w._onRendered.apply(this, [].slice.call(arguments, 0));
        },
        _onCleared: function() {
          return this._allDatasetsEmpty() ? this._hide() : this.isOpen() && this._show(), w._onCleared.apply(this, [].slice.call(arguments, 0));
        },
        setLanguageDirection: function(h) {
          return this.$node.css(h === "ltr" ? this.css.ltr : this.css.rtl), w.setLanguageDirection.apply(this, [].slice.call(arguments, 0));
        },
        _hide: function() {
          this.$node.hide();
        },
        _show: function() {
          this.$node.css("display", "block");
        }
      }), C;
    }(), b = function() {
      function w(m, h) {
        var u, p, v, S, T, A, y, I, $, V, z;
        m = m || {}, m.input || e.error("missing input"), m.menu || e.error("missing menu"), m.eventBus || e.error("missing event bus"), h.mixin(this), this.eventBus = m.eventBus, this.minLength = r.isNumber(m.minLength) ? m.minLength : 1, this.input = m.input, this.menu = m.menu, this.enabled = !0, this.active = !1, this.input.hasFocus() && this.activate(), this.dir = this.input.getLangDir(), this._hacks(), this.menu.bind().onSync("selectableClicked", this._onSelectableClicked, this).onSync("asyncRequested", this._onAsyncRequested, this).onSync("asyncCanceled", this._onAsyncCanceled, this).onSync("asyncReceived", this._onAsyncReceived, this).onSync("datasetRendered", this._onDatasetRendered, this).onSync("datasetCleared", this._onDatasetCleared, this), u = C(this, "activate", "open", "_onFocused"), p = C(this, "deactivate", "_onBlurred"), v = C(this, "isActive", "isOpen", "_onEnterKeyed"), S = C(this, "isActive", "isOpen", "_onTabKeyed"), T = C(this, "isActive", "_onEscKeyed"), A = C(this, "isActive", "open", "_onUpKeyed"), y = C(this, "isActive", "open", "_onDownKeyed"), I = C(this, "isActive", "isOpen", "_onLeftKeyed"), $ = C(this, "isActive", "isOpen", "_onRightKeyed"), V = C(this, "_openIfActive", "_onQueryChanged"), z = C(this, "_openIfActive", "_onWhitespaceChanged"), this.input.bind().onSync("focused", u, this).onSync("blurred", p, this).onSync("enterKeyed", v, this).onSync("tabKeyed", S, this).onSync("escKeyed", T, this).onSync("upKeyed", A, this).onSync("downKeyed", y, this).onSync("leftKeyed", I, this).onSync("rightKeyed", $, this).onSync("queryChanged", V, this).onSync("whitespaceChanged", z, this).onSync("langDirChanged", this._onLangDirChanged, this);
      }
      return r.mixin(w.prototype, {
        _hacks: function() {
          var h, u;
          h = this.input.$input || e("<div>"), u = this.menu.$node || e("<div>"), h.on("blur.tt", function(p) {
            var v, S, T;
            v = document.activeElement, S = u.is(v), T = u.has(v).length > 0, r.isMsie() && (S || T) && (p.preventDefault(), p.stopImmediatePropagation(), r.defer(function() {
              h.focus();
            }));
          }), u.on("mousedown.tt", function(p) {
            p.preventDefault();
          });
        },
        _onSelectableClicked: function(h, u) {
          this.select(u);
        },
        _onDatasetCleared: function() {
          this._updateHint();
        },
        _onDatasetRendered: function(h, u, p, v) {
          this._updateHint(), this.eventBus.trigger("render", p, v, u);
        },
        _onAsyncRequested: function(h, u, p) {
          this.eventBus.trigger("asyncrequest", p, u);
        },
        _onAsyncCanceled: function(h, u, p) {
          this.eventBus.trigger("asynccancel", p, u);
        },
        _onAsyncReceived: function(h, u, p) {
          this.eventBus.trigger("asyncreceive", p, u);
        },
        _onFocused: function() {
          this._minLengthMet() && this.menu.update(this.input.getQuery());
        },
        _onBlurred: function() {
          this.input.hasQueryChangedSinceLastFocus() && this.eventBus.trigger("change", this.input.getQuery());
        },
        _onEnterKeyed: function(h, u) {
          var p;
          (p = this.menu.getActiveSelectable()) && this.select(p) && u.preventDefault();
        },
        _onTabKeyed: function(h, u) {
          var p;
          (p = this.menu.getActiveSelectable()) ? this.select(p) && u.preventDefault() : (p = this.menu.getTopSelectable()) && this.autocomplete(p) && u.preventDefault();
        },
        _onEscKeyed: function() {
          this.close();
        },
        _onUpKeyed: function() {
          this.moveCursor(-1);
        },
        _onDownKeyed: function() {
          this.moveCursor(1);
        },
        _onLeftKeyed: function() {
          this.dir === "rtl" && this.input.isCursorAtEnd() && this.autocomplete(this.menu.getTopSelectable());
        },
        _onRightKeyed: function() {
          this.dir === "ltr" && this.input.isCursorAtEnd() && this.autocomplete(this.menu.getTopSelectable());
        },
        _onQueryChanged: function(h, u) {
          this._minLengthMet(u) ? this.menu.update(u) : this.menu.empty();
        },
        _onWhitespaceChanged: function() {
          this._updateHint();
        },
        _onLangDirChanged: function(h, u) {
          this.dir !== u && (this.dir = u, this.menu.setLanguageDirection(u));
        },
        _openIfActive: function() {
          this.isActive() && this.open();
        },
        _minLengthMet: function(h) {
          return h = r.isString(h) ? h : this.input.getQuery() || "", h.length >= this.minLength;
        },
        _updateHint: function() {
          var h, u, p, v, S, T, A;
          h = this.menu.getTopSelectable(), u = this.menu.getSelectableData(h), p = this.input.getInputValue(), u && !r.isBlankString(p) && !this.input.hasOverflow() ? (v = l.normalizeQuery(p), S = r.escapeRegExChars(v), T = new RegExp("^(?:" + S + ")(.+$)", "i"), A = T.exec(u.val), A && this.input.setHint(p + A[1])) : this.input.clearHint();
        },
        isEnabled: function() {
          return this.enabled;
        },
        enable: function() {
          this.enabled = !0;
        },
        disable: function() {
          this.enabled = !1;
        },
        isActive: function() {
          return this.active;
        },
        activate: function() {
          return this.isActive() ? !0 : !this.isEnabled() || this.eventBus.before("active") ? !1 : (this.active = !0, this.eventBus.trigger("active"), !0);
        },
        deactivate: function() {
          return this.isActive() ? this.eventBus.before("idle") ? !1 : (this.active = !1, this.close(), this.eventBus.trigger("idle"), !0) : !0;
        },
        isOpen: function() {
          return this.menu.isOpen();
        },
        open: function() {
          return !this.isOpen() && !this.eventBus.before("open") && (this.menu.open(), this._updateHint(), this.eventBus.trigger("open")), this.isOpen();
        },
        close: function() {
          return this.isOpen() && !this.eventBus.before("close") && (this.menu.close(), this.input.clearHint(), this.input.resetInputValue(), this.eventBus.trigger("close")), !this.isOpen();
        },
        setVal: function(h) {
          this.input.setQuery(r.toStr(h));
        },
        getVal: function() {
          return this.input.getQuery();
        },
        select: function(h) {
          var u = this.menu.getSelectableData(h);
          return u && !this.eventBus.before("select", u.obj) ? (this.input.setQuery(u.val, !0), this.eventBus.trigger("select", u.obj), this.close(), !0) : !1;
        },
        autocomplete: function(h) {
          var u, p, v;
          return u = this.input.getQuery(), p = this.menu.getSelectableData(h), v = p && u !== p.val, v && !this.eventBus.before("autocomplete", p.obj) ? (this.input.setQuery(p.val), this.eventBus.trigger("autocomplete", p.obj), !0) : !1;
        },
        moveCursor: function(h) {
          var u, p, v, S, T;
          return u = this.input.getQuery(), p = this.menu.selectableRelativeToCursor(h), v = this.menu.getSelectableData(p), S = v ? v.obj : null, T = this._minLengthMet() && this.menu.update(u), !T && !this.eventBus.before("cursorchange", S) ? (this.menu.setCursor(p), v ? this.input.setInputValue(v.val) : (this.input.resetInputValue(), this._updateHint()), this.eventBus.trigger("cursorchange", S), !0) : !1;
        },
        destroy: function() {
          this.input.destroy(), this.menu.destroy();
        }
      }), w;
      function C(m) {
        var h = [].slice.call(arguments, 1);
        return function() {
          var u = [].slice.call(arguments);
          r.each(h, function(p) {
            return m[p].apply(m, u);
          });
        };
      }
    }();
    (function() {
      var w, C, m;
      w = e.fn.typeahead, C = {
        www: "tt-www",
        attrs: "tt-attrs",
        typeahead: "tt-typeahead"
      }, m = {
        initialize: function(y, I) {
          var $;
          return I = r.isArray(I) ? I : [].slice.call(arguments, 1), y = y || {}, $ = i(y.classNames), this.each(V);
          function V() {
            var z, F, P, k, R, L, E, M, W, Y, J;
            r.each(I, function(X) {
              X.highlight = !!y.highlight;
            }), z = e(this), F = e($.html.wrapper), P = T(y.hint), k = T(y.menu), R = y.hint !== !1 && !P, L = y.menu !== !1 && !k, R && (P = u(z, $)), L && (k = e($.html.menu).css($.css.menu)), P && P.val(""), z = p(z, $), (R || L) && (F.css($.css.wrapper), z.css(R ? $.css.input : $.css.inputWithNoHint), z.wrap(F).parent().prepend(R ? P : null).append(L ? k : null)), J = L ? g : f, E = new s({
              el: z
            }), M = new l({
              hint: P,
              input: z
            }, $), W = new J({
              node: k,
              datasets: I
            }, $), Y = new b({
              input: M,
              menu: W,
              eventBus: E,
              minLength: y.minLength
            }, $), z.data(C.www, $), z.data(C.typeahead, Y);
          }
        },
        isEnabled: function() {
          var y;
          return h(this.first(), function(I) {
            y = I.isEnabled();
          }), y;
        },
        enable: function() {
          return h(this, function(y) {
            y.enable();
          }), this;
        },
        disable: function() {
          return h(this, function(y) {
            y.disable();
          }), this;
        },
        isActive: function() {
          var y;
          return h(this.first(), function(I) {
            y = I.isActive();
          }), y;
        },
        activate: function() {
          return h(this, function(y) {
            y.activate();
          }), this;
        },
        deactivate: function() {
          return h(this, function(y) {
            y.deactivate();
          }), this;
        },
        isOpen: function() {
          var y;
          return h(this.first(), function(I) {
            y = I.isOpen();
          }), y;
        },
        open: function() {
          return h(this, function(y) {
            y.open();
          }), this;
        },
        close: function() {
          return h(this, function(y) {
            y.close();
          }), this;
        },
        select: function(y) {
          var I = !1, $ = e(y);
          return h(this.first(), function(V) {
            I = V.select($);
          }), I;
        },
        autocomplete: function(y) {
          var I = !1, $ = e(y);
          return h(this.first(), function(V) {
            I = V.autocomplete($);
          }), I;
        },
        moveCursor: function(y) {
          var I = !1;
          return h(this.first(), function($) {
            I = $.moveCursor(y);
          }), I;
        },
        val: function(y) {
          var I;
          return arguments.length ? (h(this, function($) {
            $.setVal(y);
          }), this) : (h(this.first(), function($) {
            I = $.getVal();
          }), I);
        },
        destroy: function() {
          return h(this, function(y, I) {
            S(I), y.destroy();
          }), this;
        }
      }, e.fn.typeahead = function(A) {
        return m[A] ? m[A].apply(this, [].slice.call(arguments, 1)) : m.initialize.apply(this, arguments);
      }, e.fn.typeahead.noConflict = function() {
        return e.fn.typeahead = w, this;
      };
      function h(A, y) {
        A.each(function() {
          var I = e(this), $;
          ($ = I.data(C.typeahead)) && y($, I);
        });
      }
      function u(A, y) {
        return A.clone().addClass(y.classes.hint).removeData().css(y.css.hint).css(v(A)).prop("readonly", !0).removeAttr("id name placeholder required").attr({
          autocomplete: "off",
          spellcheck: "false",
          tabindex: -1
        });
      }
      function p(A, y) {
        A.data(C.attrs, {
          dir: A.attr("dir"),
          autocomplete: A.attr("autocomplete"),
          spellcheck: A.attr("spellcheck"),
          style: A.attr("style")
        }), A.addClass(y.classes.input).attr({
          autocomplete: "off",
          spellcheck: !1
        });
        try {
          !A.attr("dir") && A.attr("dir", "auto");
        } catch {
        }
        return A;
      }
      function v(A) {
        return {
          backgroundAttachment: A.css("background-attachment"),
          backgroundClip: A.css("background-clip"),
          backgroundColor: A.css("background-color"),
          backgroundImage: A.css("background-image"),
          backgroundOrigin: A.css("background-origin"),
          backgroundPosition: A.css("background-position"),
          backgroundRepeat: A.css("background-repeat"),
          backgroundSize: A.css("background-size")
        };
      }
      function S(A) {
        var y, I;
        y = A.data(C.www), I = A.parent().filter(y.selectors.wrapper), r.each(A.data(C.attrs), function($, V) {
          r.isUndefined($) ? A.removeAttr(V) : A.attr(V, $);
        }), A.removeData(C.typeahead).removeData(C.www).removeData(C.attr).removeClass(y.classes.input), I.length && (A.detach().insertAfter(I), I.remove());
      }
      function T(A) {
        var y, I;
        return y = r.isJQuery(A) || r.isElement(A), I = y ? e(A).first() : [], I.length ? I : null;
      }
    })();
  });
})(wh);
var K0 = wh.exports;
const Zw = /* @__PURE__ */ wn(K0), e_ = {
  /**
   * Trigger an animation class on an element.
   * Removes the class after animationend so it can be re-triggered.
   *
   * @param el        Target element
   * @param cls       Animation class name (e.g. 'animate-shake')
   * @param onEnd     Optional callback fired when animation completes
   */
  trigger(t, n, e) {
    t.classList.add("animate", n), t.addEventListener("animationend", () => {
      t.classList.remove("animate", n), e && e();
    }, { once: !0 });
  },
  /**
   * Observe elements matching selector and apply their data-animate class
   * when they enter the viewport. Each element animates once then stops
   * being observed.
   *
   * @param selector   CSS selector (default: '[data-animate]')
   * @param options    IntersectionObserver options
   */
  observe(t = "[data-animate]", n = { rootMargin: "0px 0px -40px 0px", threshold: 0.1 }) {
    const e = document.querySelectorAll(t);
    if (e.length === 0)
      return;
    const r = new IntersectionObserver((i, s) => {
      i.forEach((a) => {
        if (!a.isIntersecting)
          return;
        const o = a.target, l = o.dataset.animate;
        l && (o.classList.add("animate", l), delete o.dataset.animate), s.unobserve(o);
      });
    }, n);
    e.forEach((i) => r.observe(i));
  },
  /**
   * Fire a callback once when a sentinel element enters the viewport.
   * The observer disconnects after firing — attach a new sentinel for
   * subsequent pages in an infinite scroll.
   *
   * @param sentinel   Sentinel element to watch
   * @param onEnter    Callback fired when the sentinel is visible
   * @param options    IntersectionObserver options
   */
  onEnter(t, n, e = { rootMargin: "0px 0px 100px 0px", threshold: 0 }) {
    new IntersectionObserver((i, s) => {
      var a;
      (a = i[0]) != null && a.isIntersecting && (s.disconnect(), n());
    }, e).observe(t);
  }
}, X0 = {
  delay: 500,
  instantlyCloseOthers: !0
};
function _h() {
  return "ontouchstart" in document;
}
function Ch(t) {
  return t.closest(".dropdown, .nav-item");
}
function Q0(t) {
  const n = t.closest("nav, .navbar-nav, ul");
  return n ? n.querySelectorAll('[data-hover="dropdown"], [data-bsee-hover="dropdown"]') : document.querySelectorAll("__none__");
}
function J0(t) {
  Q0(t).forEach((n) => {
    if (n === t)
      return;
    const e = Ch(n);
    if (e) {
      e.classList.remove("show");
      const r = e.querySelector(".dropdown-menu");
      r && r.classList.remove("show"), n.setAttribute("aria-expanded", "false");
    }
  });
}
function xh(t, n = {}) {
  if (_h())
    return;
  const e = { ...X0, ...n }, r = t.dataset.delay, i = t.dataset.closeOthers;
  r !== void 0 && (e.delay = parseInt(r, 10)), i !== void 0 && (e.instantlyCloseOthers = i !== "false");
  const s = Ch(t);
  if (!s)
    return;
  let a = null;
  const o = s.querySelector(".dropdown-menu");
  function l() {
    a !== null && (clearTimeout(a), a = null), e.instantlyCloseOthers && J0(t), s.classList.add("show"), o && o.classList.add("show"), t.setAttribute("aria-expanded", "true");
  }
  function c() {
    a = setTimeout(() => {
      s.classList.remove("show"), o && o.classList.remove("show"), t.setAttribute("aria-expanded", "false"), a = null;
    }, e.delay);
  }
  s.addEventListener("mouseenter", l), s.addEventListener("mouseleave", c), t.dataset.bseeHover = "dropdown";
}
function Z0(t = {}) {
  _h() || document.querySelectorAll('[data-hover="dropdown"]').forEach((n) => {
    n.dataset.bseeHover !== "dropdown" && xh(n, t);
  });
}
const ew = { init: Z0, attach: xh };
function t_(t, n) {
  const e = /* @__PURE__ */ new WeakSet();
  function r(a) {
    e.has(a) || (e.add(a), n(a));
  }
  function i(a) {
    if (a.nodeType !== Node.ELEMENT_NODE)
      return;
    const o = a;
    o.matches(t) && r(o), o.querySelectorAll(t).forEach(r);
  }
  document.querySelectorAll(t).forEach(r);
  const s = new MutationObserver((a) => {
    a.forEach((o) => {
      o.addedNodes.forEach(i);
    });
  });
  return s.observe(document.body, { childList: !0, subtree: !0 }), () => s.disconnect();
}
function tw() {
  const t = document.getElementById("bsee-toast-container");
  if (t)
    return t;
  const n = document.createElement("div");
  return n.id = "bsee-toast-container", n.style.position = "fixed", n.style.zIndex = "1090", n.style.right = "15px", n.style.display = "flex", n.style.flexDirection = "column", n.style.gap = "8px", document.body.appendChild(n), n;
}
function nw(t, n = {}) {
  var c, f;
  const e = n.type ?? "success", r = n.delay ?? 5e3, i = ((c = n.offset) == null ? void 0 : c.from) ?? "top", s = ((f = n.offset) == null ? void 0 : f.amount) ?? 60, a = tw();
  a.style[i] = `${s}px`, i === "top" ? (a.style.bottom = "", a.style.flexDirection = "column") : (a.style.top = "", a.style.flexDirection = "column-reverse");
  const o = document.createElement("div");
  o.className = `toast align-items-center text-bg-${e} border-0`, n.id && (o.id = n.id), o.setAttribute("role", "alert"), o.setAttribute("aria-live", "assertive"), o.setAttribute("aria-atomic", "true"), o.innerHTML = `
        <div class="d-flex">
            <div class="toast-body">${t}</div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto"
                data-bs-dismiss="toast" aria-label="Close"></button>
        </div>`, a.appendChild(o);
  const { Toast: l } = window.bootstrap ?? {};
  if (l) {
    const g = new l(o, {
      autohide: r > 0,
      delay: r > 0 ? r : 5e3
    });
    o.addEventListener("hidden.bs.toast", () => o.remove()), g.show();
  } else
    o.classList.add("show"), r > 0 && setTimeout(() => o.remove(), r);
}
const n_ = nw;
var Sh = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    var e = 1e3, r = 6e4, i = 36e5, s = "millisecond", a = "second", o = "minute", l = "hour", c = "day", f = "week", g = "month", b = "quarter", w = "year", C = "date", m = "Invalid Date", h = /^(\d{4})[-/]?(\d{1,2})?[-/]?(\d{0,2})[Tt\s]*(\d{1,2})?:?(\d{1,2})?:?(\d{1,2})?[.:]?(\d+)?$/, u = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, p = { name: "en", weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_"), ordinal: function(k) {
      var R = ["th", "st", "nd", "rd"], L = k % 100;
      return "[" + k + (R[(L - 20) % 10] || R[L] || R[0]) + "]";
    } }, v = function(k, R, L) {
      var E = String(k);
      return !E || E.length >= R ? k : "" + Array(R + 1 - E.length).join(L) + k;
    }, S = { s: v, z: function(k) {
      var R = -k.utcOffset(), L = Math.abs(R), E = Math.floor(L / 60), M = L % 60;
      return (R <= 0 ? "+" : "-") + v(E, 2, "0") + ":" + v(M, 2, "0");
    }, m: function k(R, L) {
      if (R.date() < L.date())
        return -k(L, R);
      var E = 12 * (L.year() - R.year()) + (L.month() - R.month()), M = R.clone().add(E, g), W = L - M < 0, Y = R.clone().add(E + (W ? -1 : 1), g);
      return +(-(E + (L - M) / (W ? M - Y : Y - M)) || 0);
    }, a: function(k) {
      return k < 0 ? Math.ceil(k) || 0 : Math.floor(k);
    }, p: function(k) {
      return { M: g, y: w, w: f, d: c, D: C, h: l, m: o, s: a, ms: s, Q: b }[k] || String(k || "").toLowerCase().replace(/s$/, "");
    }, u: function(k) {
      return k === void 0;
    } }, T = "en", A = {};
    A[T] = p;
    var y = "$isDayjsObject", I = function(k) {
      return k instanceof F || !(!k || !k[y]);
    }, $ = function k(R, L, E) {
      var M;
      if (!R)
        return T;
      if (typeof R == "string") {
        var W = R.toLowerCase();
        A[W] && (M = W), L && (A[W] = L, M = W);
        var Y = R.split("-");
        if (!M && Y.length > 1)
          return k(Y[0]);
      } else {
        var J = R.name;
        A[J] = R, M = J;
      }
      return !E && M && (T = M), M || !E && T;
    }, V = function(k, R) {
      if (I(k))
        return k.clone();
      var L = typeof R == "object" ? R : {};
      return L.date = k, L.args = arguments, new F(L);
    }, z = S;
    z.l = $, z.i = I, z.w = function(k, R) {
      return V(k, { locale: R.$L, utc: R.$u, x: R.$x, $offset: R.$offset });
    };
    var F = function() {
      function k(L) {
        this.$L = $(L.locale, null, !0), this.parse(L), this.$x = this.$x || L.x || {}, this[y] = !0;
      }
      var R = k.prototype;
      return R.parse = function(L) {
        this.$d = function(E) {
          var M = E.date, W = E.utc;
          if (M === null)
            return /* @__PURE__ */ new Date(NaN);
          if (z.u(M))
            return /* @__PURE__ */ new Date();
          if (M instanceof Date)
            return new Date(M);
          if (typeof M == "string" && !/Z$/i.test(M)) {
            var Y = M.match(h);
            if (Y) {
              var J = Y[2] - 1 || 0, X = (Y[7] || "0").substring(0, 3);
              return W ? new Date(Date.UTC(Y[1], J, Y[3] || 1, Y[4] || 0, Y[5] || 0, Y[6] || 0, X)) : new Date(Y[1], J, Y[3] || 1, Y[4] || 0, Y[5] || 0, Y[6] || 0, X);
            }
          }
          return new Date(M);
        }(L), this.init();
      }, R.init = function() {
        var L = this.$d;
        this.$y = L.getFullYear(), this.$M = L.getMonth(), this.$D = L.getDate(), this.$W = L.getDay(), this.$H = L.getHours(), this.$m = L.getMinutes(), this.$s = L.getSeconds(), this.$ms = L.getMilliseconds();
      }, R.$utils = function() {
        return z;
      }, R.isValid = function() {
        return this.$d.toString() !== m;
      }, R.isSame = function(L, E) {
        var M = V(L);
        return this.startOf(E) <= M && M <= this.endOf(E);
      }, R.isAfter = function(L, E) {
        return V(L) < this.startOf(E);
      }, R.isBefore = function(L, E) {
        return this.endOf(E) < V(L);
      }, R.$g = function(L, E, M) {
        return z.u(L) ? this[E] : this.set(M, L);
      }, R.unix = function() {
        return Math.floor(this.valueOf() / 1e3);
      }, R.valueOf = function() {
        return this.$d.getTime();
      }, R.startOf = function(L, E) {
        var M = this, W = !!z.u(E) || E, Y = z.p(L), J = function(we, Oe) {
          var Ve = z.w(M.$u ? Date.UTC(M.$y, Oe, we) : new Date(M.$y, Oe, we), M);
          return W ? Ve : Ve.endOf(c);
        }, X = function(we, Oe) {
          return z.w(M.toDate()[we].apply(M.toDate("s"), (W ? [0, 0, 0, 0] : [23, 59, 59, 999]).slice(Oe)), M);
        }, Q = this.$W, ee = this.$M, ie = this.$D, oe = "set" + (this.$u ? "UTC" : "");
        switch (Y) {
          case w:
            return W ? J(1, 0) : J(31, 11);
          case g:
            return W ? J(1, ee) : J(0, ee + 1);
          case f:
            var ce = this.$locale().weekStart || 0, he = (Q < ce ? Q + 7 : Q) - ce;
            return J(W ? ie - he : ie + (6 - he), ee);
          case c:
          case C:
            return X(oe + "Hours", 0);
          case l:
            return X(oe + "Minutes", 1);
          case o:
            return X(oe + "Seconds", 2);
          case a:
            return X(oe + "Milliseconds", 3);
          default:
            return this.clone();
        }
      }, R.endOf = function(L) {
        return this.startOf(L, !1);
      }, R.$set = function(L, E) {
        var M, W = z.p(L), Y = "set" + (this.$u ? "UTC" : ""), J = (M = {}, M[c] = Y + "Date", M[C] = Y + "Date", M[g] = Y + "Month", M[w] = Y + "FullYear", M[l] = Y + "Hours", M[o] = Y + "Minutes", M[a] = Y + "Seconds", M[s] = Y + "Milliseconds", M)[W], X = W === c ? this.$D + (E - this.$W) : E;
        if (W === g || W === w) {
          var Q = this.clone().set(C, 1);
          Q.$d[J](X), Q.init(), this.$d = Q.set(C, Math.min(this.$D, Q.daysInMonth())).$d;
        } else
          J && this.$d[J](X);
        return this.init(), this;
      }, R.set = function(L, E) {
        return this.clone().$set(L, E);
      }, R.get = function(L) {
        return this[z.p(L)]();
      }, R.add = function(L, E) {
        var M, W = this;
        L = Number(L);
        var Y = z.p(E), J = function(ee) {
          var ie = V(W);
          return z.w(ie.date(ie.date() + Math.round(ee * L)), W);
        };
        if (Y === g)
          return this.set(g, this.$M + L);
        if (Y === w)
          return this.set(w, this.$y + L);
        if (Y === c)
          return J(1);
        if (Y === f)
          return J(7);
        var X = (M = {}, M[o] = r, M[l] = i, M[a] = e, M)[Y] || 1, Q = this.$d.getTime() + L * X;
        return z.w(Q, this);
      }, R.subtract = function(L, E) {
        return this.add(-1 * L, E);
      }, R.format = function(L) {
        var E = this, M = this.$locale();
        if (!this.isValid())
          return M.invalidDate || m;
        var W = L || "YYYY-MM-DDTHH:mm:ssZ", Y = z.z(this), J = this.$H, X = this.$m, Q = this.$M, ee = M.weekdays, ie = M.months, oe = M.meridiem, ce = function(Oe, Ve, Ie, Ze) {
          return Oe && (Oe[Ve] || Oe(E, W)) || Ie[Ve].slice(0, Ze);
        }, he = function(Oe) {
          return z.s(J % 12 || 12, Oe, "0");
        }, we = oe || function(Oe, Ve, Ie) {
          var Ze = Oe < 12 ? "AM" : "PM";
          return Ie ? Ze.toLowerCase() : Ze;
        };
        return W.replace(u, function(Oe, Ve) {
          return Ve || function(Ie) {
            switch (Ie) {
              case "YY":
                return String(E.$y).slice(-2);
              case "YYYY":
                return z.s(E.$y, 4, "0");
              case "M":
                return Q + 1;
              case "MM":
                return z.s(Q + 1, 2, "0");
              case "MMM":
                return ce(M.monthsShort, Q, ie, 3);
              case "MMMM":
                return ce(ie, Q);
              case "D":
                return E.$D;
              case "DD":
                return z.s(E.$D, 2, "0");
              case "d":
                return String(E.$W);
              case "dd":
                return ce(M.weekdaysMin, E.$W, ee, 2);
              case "ddd":
                return ce(M.weekdaysShort, E.$W, ee, 3);
              case "dddd":
                return ee[E.$W];
              case "H":
                return String(J);
              case "HH":
                return z.s(J, 2, "0");
              case "h":
                return he(1);
              case "hh":
                return he(2);
              case "a":
                return we(J, X, !0);
              case "A":
                return we(J, X, !1);
              case "m":
                return String(X);
              case "mm":
                return z.s(X, 2, "0");
              case "s":
                return String(E.$s);
              case "ss":
                return z.s(E.$s, 2, "0");
              case "SSS":
                return z.s(E.$ms, 3, "0");
              case "Z":
                return Y;
            }
            return null;
          }(Oe) || Y.replace(":", "");
        });
      }, R.utcOffset = function() {
        return 15 * -Math.round(this.$d.getTimezoneOffset() / 15);
      }, R.diff = function(L, E, M) {
        var W, Y = this, J = z.p(E), X = V(L), Q = (X.utcOffset() - this.utcOffset()) * r, ee = this - X, ie = function() {
          return z.m(Y, X);
        };
        switch (J) {
          case w:
            W = ie() / 12;
            break;
          case g:
            W = ie();
            break;
          case b:
            W = ie() / 3;
            break;
          case f:
            W = (ee - Q) / 6048e5;
            break;
          case c:
            W = (ee - Q) / 864e5;
            break;
          case l:
            W = ee / i;
            break;
          case o:
            W = ee / r;
            break;
          case a:
            W = ee / e;
            break;
          default:
            W = ee;
        }
        return M ? W : z.a(W);
      }, R.daysInMonth = function() {
        return this.endOf(g).$D;
      }, R.$locale = function() {
        return A[this.$L];
      }, R.locale = function(L, E) {
        if (!L)
          return this.$L;
        var M = this.clone(), W = $(L, E, !0);
        return W && (M.$L = W), M;
      }, R.clone = function() {
        return z.w(this.$d, this);
      }, R.toDate = function() {
        return new Date(this.valueOf());
      }, R.toJSON = function() {
        return this.isValid() ? this.toISOString() : null;
      }, R.toISOString = function() {
        return this.$d.toISOString();
      }, R.toString = function() {
        return this.$d.toUTCString();
      }, k;
    }(), P = F.prototype;
    return V.prototype = P, [["$ms", s], ["$s", a], ["$m", o], ["$H", l], ["$W", c], ["$M", g], ["$y", w], ["$D", C]].forEach(function(k) {
      P[k[1]] = function(R) {
        return this.$g(R, k[0], k[1]);
      };
    }), V.extend = function(k, R) {
      return k.$i || (k(R, F, V), k.$i = !0), V;
    }, V.locale = $, V.isDayjs = I, V.unix = function(k) {
      return V(1e3 * k);
    }, V.en = A[T], V.Ls = A, V.p = {}, V;
  });
})(Sh);
var rw = Sh.exports;
const Yr = /* @__PURE__ */ wn(rw);
var Th = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    return function(e, r, i) {
      e = e || {};
      var s = r.prototype, a = { future: "in %s", past: "%s ago", s: "a few seconds", m: "a minute", mm: "%d minutes", h: "an hour", hh: "%d hours", d: "a day", dd: "%d days", M: "a month", MM: "%d months", y: "a year", yy: "%d years" };
      function o(c, f, g, b) {
        return s.fromToBase(c, f, g, b);
      }
      i.en.relativeTime = a, s.fromToBase = function(c, f, g, b, w) {
        for (var C, m, h, u = g.$locale().relativeTime || a, p = e.thresholds || [{ l: "s", r: 44, d: "second" }, { l: "m", r: 89 }, { l: "mm", r: 44, d: "minute" }, { l: "h", r: 89 }, { l: "hh", r: 21, d: "hour" }, { l: "d", r: 35 }, { l: "dd", r: 25, d: "day" }, { l: "M", r: 45 }, { l: "MM", r: 10, d: "month" }, { l: "y", r: 17 }, { l: "yy", d: "year" }], v = p.length, S = 0; S < v; S += 1) {
          var T = p[S];
          T.d && (C = b ? i(c).diff(g, T.d, !0) : g.diff(c, T.d, !0));
          var A = (e.rounding || Math.round)(Math.abs(C));
          if (h = C > 0, A <= T.r || !T.r) {
            A <= 1 && S > 0 && (T = p[S - 1]);
            var y = u[T.l];
            w && (A = w("" + A)), m = typeof y == "string" ? y.replace("%d", A) : y(A, f, T.l, h);
            break;
          }
        }
        if (f)
          return m;
        var I = h ? u.future : u.past;
        return typeof I == "function" ? I(m) : I.replace("%s", m);
      }, s.to = function(c, f) {
        return o(c, f, this, !0);
      }, s.from = function(c, f) {
        return o(c, f, this);
      };
      var l = function(c) {
        return c.$u ? i.utc() : i();
      };
      s.toNow = function(c) {
        return this.to(l(this), c);
      }, s.fromNow = function(c) {
        return this.from(l(this), c);
      };
    };
  });
})(Th);
var iw = Th.exports;
const sw = /* @__PURE__ */ wn(iw);
var Dh = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    var e, r, i = 1e3, s = 6e4, a = 36e5, o = 864e5, l = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, c = 31536e6, f = 2628e6, g = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/, b = { years: c, months: f, days: o, hours: a, minutes: s, seconds: i, milliseconds: 1, weeks: 6048e5 }, w = function(A) {
      return A instanceof S;
    }, C = function(A, y, I) {
      return new S(A, I, y.$l);
    }, m = function(A) {
      return r.p(A) + "s";
    }, h = function(A) {
      return A < 0;
    }, u = function(A) {
      return h(A) ? Math.ceil(A) : Math.floor(A);
    }, p = function(A) {
      return Math.abs(A);
    }, v = function(A, y) {
      return A ? h(A) ? { negative: !0, format: "" + p(A) + y } : { negative: !1, format: "" + A + y } : { negative: !1, format: "" };
    }, S = function() {
      function A(I, $, V) {
        var z = this;
        if (this.$d = {}, this.$l = V, I === void 0 && (this.$ms = 0, this.parseFromMilliseconds()), $)
          return C(I * b[m($)], this);
        if (typeof I == "number")
          return this.$ms = I, this.parseFromMilliseconds(), this;
        if (typeof I == "object")
          return Object.keys(I).forEach(function(k) {
            z.$d[m(k)] = I[k];
          }), this.calMilliseconds(), this;
        if (typeof I == "string") {
          var F = I.match(g);
          if (F) {
            var P = F.slice(2).map(function(k) {
              return k != null ? Number(k) : 0;
            });
            return this.$d.years = P[0], this.$d.months = P[1], this.$d.weeks = P[2], this.$d.days = P[3], this.$d.hours = P[4], this.$d.minutes = P[5], this.$d.seconds = P[6], this.calMilliseconds(), this;
          }
        }
        return this;
      }
      var y = A.prototype;
      return y.calMilliseconds = function() {
        var I = this;
        this.$ms = Object.keys(this.$d).reduce(function($, V) {
          return $ + (I.$d[V] || 0) * b[V];
        }, 0);
      }, y.parseFromMilliseconds = function() {
        var I = this.$ms;
        this.$d.years = u(I / c), I %= c, this.$d.months = u(I / f), I %= f, this.$d.days = u(I / o), I %= o, this.$d.hours = u(I / a), I %= a, this.$d.minutes = u(I / s), I %= s, this.$d.seconds = u(I / i), I %= i, this.$d.milliseconds = I;
      }, y.toISOString = function() {
        var I = v(this.$d.years, "Y"), $ = v(this.$d.months, "M"), V = +this.$d.days || 0;
        this.$d.weeks && (V += 7 * this.$d.weeks);
        var z = v(V, "D"), F = v(this.$d.hours, "H"), P = v(this.$d.minutes, "M"), k = this.$d.seconds || 0;
        this.$d.milliseconds && (k += this.$d.milliseconds / 1e3, k = Math.round(1e3 * k) / 1e3);
        var R = v(k, "S"), L = I.negative || $.negative || z.negative || F.negative || P.negative || R.negative, E = F.format || P.format || R.format ? "T" : "", M = (L ? "-" : "") + "P" + I.format + $.format + z.format + E + F.format + P.format + R.format;
        return M === "P" || M === "-P" ? "P0D" : M;
      }, y.toJSON = function() {
        return this.toISOString();
      }, y.format = function(I) {
        var $ = I || "YYYY-MM-DDTHH:mm:ss", V = { Y: this.$d.years, YY: r.s(this.$d.years, 2, "0"), YYYY: r.s(this.$d.years, 4, "0"), M: this.$d.months, MM: r.s(this.$d.months, 2, "0"), D: this.$d.days, DD: r.s(this.$d.days, 2, "0"), H: this.$d.hours, HH: r.s(this.$d.hours, 2, "0"), m: this.$d.minutes, mm: r.s(this.$d.minutes, 2, "0"), s: this.$d.seconds, ss: r.s(this.$d.seconds, 2, "0"), SSS: r.s(this.$d.milliseconds, 3, "0") };
        return $.replace(l, function(z, F) {
          return F || String(V[z]);
        });
      }, y.as = function(I) {
        return this.$ms / b[m(I)];
      }, y.get = function(I) {
        var $ = this.$ms, V = m(I);
        return V === "milliseconds" ? $ %= 1e3 : $ = V === "weeks" ? u($ / b[V]) : this.$d[V], $ || 0;
      }, y.add = function(I, $, V) {
        var z;
        return z = $ ? I * b[m($)] : w(I) ? I.$ms : C(I, this).$ms, C(this.$ms + z * (V ? -1 : 1), this);
      }, y.subtract = function(I, $) {
        return this.add(I, $, !0);
      }, y.locale = function(I) {
        var $ = this.clone();
        return $.$l = I, $;
      }, y.clone = function() {
        return C(this.$ms, this);
      }, y.humanize = function(I) {
        return e().add(this.$ms, "ms").locale(this.$l).fromNow(!I);
      }, y.valueOf = function() {
        return this.asMilliseconds();
      }, y.milliseconds = function() {
        return this.get("milliseconds");
      }, y.asMilliseconds = function() {
        return this.as("milliseconds");
      }, y.seconds = function() {
        return this.get("seconds");
      }, y.asSeconds = function() {
        return this.as("seconds");
      }, y.minutes = function() {
        return this.get("minutes");
      }, y.asMinutes = function() {
        return this.as("minutes");
      }, y.hours = function() {
        return this.get("hours");
      }, y.asHours = function() {
        return this.as("hours");
      }, y.days = function() {
        return this.get("days");
      }, y.asDays = function() {
        return this.as("days");
      }, y.weeks = function() {
        return this.get("weeks");
      }, y.asWeeks = function() {
        return this.as("weeks");
      }, y.months = function() {
        return this.get("months");
      }, y.asMonths = function() {
        return this.as("months");
      }, y.years = function() {
        return this.get("years");
      }, y.asYears = function() {
        return this.as("years");
      }, A;
    }(), T = function(A, y, I) {
      return A.add(y.years() * I, "y").add(y.months() * I, "M").add(y.days() * I, "d").add(y.hours() * I, "h").add(y.minutes() * I, "m").add(y.seconds() * I, "s").add(y.milliseconds() * I, "ms");
    };
    return function(A, y, I) {
      e = I, r = I().$utils(), I.duration = function(z, F) {
        var P = I.locale();
        return C(z, { $l: P }, F);
      }, I.isDuration = w;
      var $ = y.prototype.add, V = y.prototype.subtract;
      y.prototype.add = function(z, F) {
        return w(z) ? T(this, z, 1) : $.bind(this)(z, F);
      }, y.prototype.subtract = function(z, F) {
        return w(z) ? T(this, z, -1) : V.bind(this)(z, F);
      };
    };
  });
})(Dh);
var aw = Dh.exports;
const ow = /* @__PURE__ */ wn(aw);
var Eh = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    var e = { LTS: "h:mm:ss A", LT: "h:mm A", L: "MM/DD/YYYY", LL: "MMMM D, YYYY", LLL: "MMMM D, YYYY h:mm A", LLLL: "dddd, MMMM D, YYYY h:mm A" };
    return function(r, i, s) {
      var a = i.prototype, o = a.format;
      s.en.formats = e, a.format = function(l) {
        l === void 0 && (l = "YYYY-MM-DDTHH:mm:ssZ");
        var c = this.$locale().formats, f = function(g, b) {
          return g.replace(/(\[[^\]]+])|(LTS?|l{1,4}|L{1,4})/g, function(w, C, m) {
            var h = m && m.toUpperCase();
            return C || b[m] || e[m] || b[h].replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, function(u, p, v) {
              return p || v.slice(1);
            });
          });
        }(l, c === void 0 ? {} : c);
        return o.call(this, f);
      };
    };
  });
})(Eh);
var lw = Eh.exports;
const uw = /* @__PURE__ */ wn(lw);
var Ah = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    var e = { LTS: "h:mm:ss A", LT: "h:mm A", L: "MM/DD/YYYY", LL: "MMMM D, YYYY", LLL: "MMMM D, YYYY h:mm A", LLLL: "dddd, MMMM D, YYYY h:mm A" }, r = /(\[[^[]*\])|([-_:/.,()\s]+)|(A|a|Q|YYYY|YY?|ww?|MM?M?M?|Do|DD?|hh?|HH?|mm?|ss?|S{1,3}|z|ZZ?)/g, i = /\d/, s = /\d\d/, a = /\d\d?/, o = /\d*[^-_:/,()\s\d]+/, l = {}, c = function(h) {
      return (h = +h) + (h > 68 ? 1900 : 2e3);
    }, f = function(h) {
      return function(u) {
        this[h] = +u;
      };
    }, g = [/[+-]\d\d:?(\d\d)?|Z/, function(h) {
      (this.zone || (this.zone = {})).offset = function(u) {
        if (!u || u === "Z")
          return 0;
        var p = u.match(/([+-]|\d\d)/g), v = 60 * p[1] + (+p[2] || 0);
        return v === 0 ? 0 : p[0] === "+" ? -v : v;
      }(h);
    }], b = function(h) {
      var u = l[h];
      return u && (u.indexOf ? u : u.s.concat(u.f));
    }, w = function(h, u) {
      var p, v = l.meridiem;
      if (v) {
        for (var S = 1; S <= 24; S += 1)
          if (h.indexOf(v(S, 0, u)) > -1) {
            p = S > 12;
            break;
          }
      } else
        p = h === (u ? "pm" : "PM");
      return p;
    }, C = { A: [o, function(h) {
      this.afternoon = w(h, !1);
    }], a: [o, function(h) {
      this.afternoon = w(h, !0);
    }], Q: [i, function(h) {
      this.month = 3 * (h - 1) + 1;
    }], S: [i, function(h) {
      this.milliseconds = 100 * +h;
    }], SS: [s, function(h) {
      this.milliseconds = 10 * +h;
    }], SSS: [/\d{3}/, function(h) {
      this.milliseconds = +h;
    }], s: [a, f("seconds")], ss: [a, f("seconds")], m: [a, f("minutes")], mm: [a, f("minutes")], H: [a, f("hours")], h: [a, f("hours")], HH: [a, f("hours")], hh: [a, f("hours")], D: [a, f("day")], DD: [s, f("day")], Do: [o, function(h) {
      var u = l.ordinal, p = h.match(/\d+/);
      if (this.day = p[0], u)
        for (var v = 1; v <= 31; v += 1)
          u(v).replace(/\[|\]/g, "") === h && (this.day = v);
    }], w: [a, f("week")], ww: [s, f("week")], M: [a, f("month")], MM: [s, f("month")], MMM: [o, function(h) {
      var u = b("months"), p = (b("monthsShort") || u.map(function(v) {
        return v.slice(0, 3);
      })).indexOf(h) + 1;
      if (p < 1)
        throw new Error();
      this.month = p % 12 || p;
    }], MMMM: [o, function(h) {
      var u = b("months").indexOf(h) + 1;
      if (u < 1)
        throw new Error();
      this.month = u % 12 || u;
    }], Y: [/[+-]?\d+/, f("year")], YY: [s, function(h) {
      this.year = c(h);
    }], YYYY: [/\d{4}/, f("year")], Z: g, ZZ: g };
    function m(h) {
      var u, p;
      u = h, p = l && l.formats;
      for (var v = (h = u.replace(/(\[[^\]]+])|(LTS?|l{1,4}|L{1,4})/g, function(V, z, F) {
        var P = F && F.toUpperCase();
        return z || p[F] || e[F] || p[P].replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, function(k, R, L) {
          return R || L.slice(1);
        });
      })).match(r), S = v.length, T = 0; T < S; T += 1) {
        var A = v[T], y = C[A], I = y && y[0], $ = y && y[1];
        v[T] = $ ? { regex: I, parser: $ } : A.replace(/^\[|\]$/g, "");
      }
      return function(V) {
        for (var z = {}, F = 0, P = 0; F < S; F += 1) {
          var k = v[F];
          if (typeof k == "string")
            P += k.length;
          else {
            var R = k.regex, L = k.parser, E = V.slice(P), M = R.exec(E)[0];
            L.call(z, M), V = V.replace(M, "");
          }
        }
        return function(W) {
          var Y = W.afternoon;
          if (Y !== void 0) {
            var J = W.hours;
            Y ? J < 12 && (W.hours += 12) : J === 12 && (W.hours = 0), delete W.afternoon;
          }
        }(z), z;
      };
    }
    return function(h, u, p) {
      p.p.customParseFormat = !0, h && h.parseTwoDigitYear && (c = h.parseTwoDigitYear);
      var v = u.prototype, S = v.parse;
      v.parse = function(T) {
        var A = T.date, y = T.utc, I = T.args;
        this.$u = y;
        var $ = I[1];
        if (typeof $ == "string") {
          var V = I[2] === !0, z = I[3] === !0, F = V || z, P = I[2];
          z && (P = I[2]), l = this.$locale(), !V && P && (l = p.Ls[P]), this.$d = function(E, M, W, Y) {
            try {
              if (["x", "X"].indexOf(M) > -1)
                return new Date((M === "X" ? 1e3 : 1) * E);
              var J = m(M)(E), X = J.year, Q = J.month, ee = J.day, ie = J.hours, oe = J.minutes, ce = J.seconds, he = J.milliseconds, we = J.zone, Oe = J.week, Ve = /* @__PURE__ */ new Date(), Ie = ee || (X || Q ? 1 : Ve.getDate()), Ze = X || Ve.getFullYear(), et = 0;
              X && !Q || (et = Q > 0 ? Q - 1 : Ve.getMonth());
              var He, Ke = ie || 0, Ct = oe || 0, yt = ce || 0, wt = he || 0;
              return we ? new Date(Date.UTC(Ze, et, Ie, Ke, Ct, yt, wt + 60 * we.offset * 1e3)) : W ? new Date(Date.UTC(Ze, et, Ie, Ke, Ct, yt, wt)) : (He = new Date(Ze, et, Ie, Ke, Ct, yt, wt), Oe && (He = Y(He).week(Oe).toDate()), He);
            } catch {
              return /* @__PURE__ */ new Date("");
            }
          }(A, $, y, p), this.init(), P && P !== !0 && (this.$L = this.locale(P).$L), F && A != this.format($) && (this.$d = /* @__PURE__ */ new Date("")), l = {};
        } else if ($ instanceof Array)
          for (var k = $.length, R = 1; R <= k; R += 1) {
            I[1] = $[R - 1];
            var L = p.apply(this, I);
            if (L.isValid()) {
              this.$d = L.$d, this.$L = L.$L, this.init();
              break;
            }
            R === k && (this.$d = /* @__PURE__ */ new Date(""));
          }
        else
          S.call(this, T);
      };
    };
  });
})(Ah);
var cw = Ah.exports;
const dw = /* @__PURE__ */ wn(cw);
var kh = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    return function(e, r) {
      r.prototype.isSameOrBefore = function(i, s) {
        return this.isSame(i, s) || this.isBefore(i, s);
      };
    };
  });
})(kh);
var fw = kh.exports;
const hw = /* @__PURE__ */ wn(fw);
var Ph = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    return function(e, r) {
      r.prototype.isSameOrAfter = function(i, s) {
        return this.isSame(i, s) || this.isAfter(i, s);
      };
    };
  });
})(Ph);
var pw = Ph.exports;
const gw = /* @__PURE__ */ wn(pw);
var Oh = { exports: {} };
(function(t, n) {
  (function(e, r) {
    t.exports = r();
  })(sn, function() {
    var e = "minute", r = /[+-]\d\d(?::?\d\d)?/g, i = /([+-]|\d\d)/g;
    return function(s, a, o) {
      var l = a.prototype;
      o.utc = function(m) {
        var h = { date: m, utc: !0, args: arguments };
        return new a(h);
      }, l.utc = function(m) {
        var h = o(this.toDate(), { locale: this.$L, utc: !0 });
        return m ? h.add(this.utcOffset(), e) : h;
      }, l.local = function() {
        return o(this.toDate(), { locale: this.$L, utc: !1 });
      };
      var c = l.parse;
      l.parse = function(m) {
        m.utc && (this.$u = !0), this.$utils().u(m.$offset) || (this.$offset = m.$offset), c.call(this, m);
      };
      var f = l.init;
      l.init = function() {
        if (this.$u) {
          var m = this.$d;
          this.$y = m.getUTCFullYear(), this.$M = m.getUTCMonth(), this.$D = m.getUTCDate(), this.$W = m.getUTCDay(), this.$H = m.getUTCHours(), this.$m = m.getUTCMinutes(), this.$s = m.getUTCSeconds(), this.$ms = m.getUTCMilliseconds();
        } else
          f.call(this);
      };
      var g = l.utcOffset;
      l.utcOffset = function(m, h) {
        var u = this.$utils().u;
        if (u(m))
          return this.$u ? 0 : u(this.$offset) ? g.call(this) : this.$offset;
        if (typeof m == "string" && (m = function(T) {
          T === void 0 && (T = "");
          var A = T.match(r);
          if (!A)
            return null;
          var y = ("" + A[0]).match(i) || ["-", 0, 0], I = y[0], $ = 60 * +y[1] + +y[2];
          return $ === 0 ? 0 : I === "+" ? $ : -$;
        }(m), m === null))
          return this;
        var p = Math.abs(m) <= 16 ? 60 * m : m;
        if (p === 0)
          return this.utc(h);
        var v = this.clone();
        if (h)
          return v.$offset = p, v.$u = !1, v;
        var S = this.$u ? this.toDate().getTimezoneOffset() : -1 * this.utcOffset();
        return (v = this.local().add(p + S, e)).$offset = p, v.$x.$localOffset = S, v;
      };
      var b = l.format;
      l.format = function(m) {
        var h = m || (this.$u ? "YYYY-MM-DDTHH:mm:ss[Z]" : "");
        return b.call(this, h);
      }, l.valueOf = function() {
        var m = this.$utils().u(this.$offset) ? 0 : this.$offset + (this.$x.$localOffset || this.$d.getTimezoneOffset());
        return this.$d.valueOf() - 6e4 * m;
      }, l.isUTC = function() {
        return !!this.$u;
      }, l.toISOString = function() {
        return this.toDate().toISOString();
      }, l.toString = function() {
        return this.toDate().toUTCString();
      };
      var w = l.toDate;
      l.toDate = function(m) {
        return m === "s" && this.$offset ? o(this.format("YYYY-MM-DD HH:mm:ss:SSS")).toDate() : w.call(this);
      };
      var C = l.diff;
      l.diff = function(m, h, u) {
        if (m && this.$u === m.$u)
          return C.call(this, m, h, u);
        var p = this.local(), v = o(m).local();
        return C.call(p, v, h, u);
      };
    };
  });
})(Oh);
var mw = Oh.exports;
const vw = /* @__PURE__ */ wn(mw);
Yr.extend(sw);
Yr.extend(ow);
Yr.extend(uw);
Yr.extend(dw);
Yr.extend(hw);
Yr.extend(gw);
Yr.extend(vw);
var Ih = { exports: {} };
/*!
 * clipboard.js v2.0.11
 * https://clipboardjs.com/
 *
 * Licensed MIT © Zeno Rocha
 */
(function(t, n) {
  (function(r, i) {
    t.exports = i();
  })(sn, function() {
    return (
      /******/
      function() {
        var e = {
          /***/
          686: (
            /***/
            function(s, a, o) {
              o.d(a, {
                default: function() {
                  return (
                    /* binding */
                    J
                  );
                }
              });
              var l = o(279), c = /* @__PURE__ */ o.n(l), f = o(370), g = /* @__PURE__ */ o.n(f), b = o(817), w = /* @__PURE__ */ o.n(b);
              function C(X) {
                try {
                  return document.execCommand(X);
                } catch {
                  return !1;
                }
              }
              var m = function(Q) {
                var ee = w()(Q);
                return C("cut"), ee;
              }, h = m;
              function u(X) {
                var Q = document.documentElement.getAttribute("dir") === "rtl", ee = document.createElement("textarea");
                ee.style.fontSize = "12pt", ee.style.border = "0", ee.style.padding = "0", ee.style.margin = "0", ee.style.position = "absolute", ee.style[Q ? "right" : "left"] = "-9999px";
                var ie = window.pageYOffset || document.documentElement.scrollTop;
                return ee.style.top = "".concat(ie, "px"), ee.setAttribute("readonly", ""), ee.value = X, ee;
              }
              var p = function(Q, ee) {
                var ie = u(Q);
                ee.container.appendChild(ie);
                var oe = w()(ie);
                return C("copy"), ie.remove(), oe;
              }, v = function(Q) {
                var ee = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
                  container: document.body
                }, ie = "";
                return typeof Q == "string" ? ie = p(Q, ee) : Q instanceof HTMLInputElement && !["text", "search", "url", "tel", "password"].includes(Q == null ? void 0 : Q.type) ? ie = p(Q.value, ee) : (ie = w()(Q), C("copy")), ie;
              }, S = v;
              function T(X) {
                "@babel/helpers - typeof";
                return typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? T = function(ee) {
                  return typeof ee;
                } : T = function(ee) {
                  return ee && typeof Symbol == "function" && ee.constructor === Symbol && ee !== Symbol.prototype ? "symbol" : typeof ee;
                }, T(X);
              }
              var A = function() {
                var Q = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, ee = Q.action, ie = ee === void 0 ? "copy" : ee, oe = Q.container, ce = Q.target, he = Q.text;
                if (ie !== "copy" && ie !== "cut")
                  throw new Error('Invalid "action" value, use either "copy" or "cut"');
                if (ce !== void 0)
                  if (ce && T(ce) === "object" && ce.nodeType === 1) {
                    if (ie === "copy" && ce.hasAttribute("disabled"))
                      throw new Error('Invalid "target" attribute. Please use "readonly" instead of "disabled" attribute');
                    if (ie === "cut" && (ce.hasAttribute("readonly") || ce.hasAttribute("disabled")))
                      throw new Error(`Invalid "target" attribute. You can't cut text from elements with "readonly" or "disabled" attributes`);
                  } else
                    throw new Error('Invalid "target" value, use a valid Element');
                if (he)
                  return S(he, {
                    container: oe
                  });
                if (ce)
                  return ie === "cut" ? h(ce) : S(ce, {
                    container: oe
                  });
              }, y = A;
              function I(X) {
                "@babel/helpers - typeof";
                return typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? I = function(ee) {
                  return typeof ee;
                } : I = function(ee) {
                  return ee && typeof Symbol == "function" && ee.constructor === Symbol && ee !== Symbol.prototype ? "symbol" : typeof ee;
                }, I(X);
              }
              function $(X, Q) {
                if (!(X instanceof Q))
                  throw new TypeError("Cannot call a class as a function");
              }
              function V(X, Q) {
                for (var ee = 0; ee < Q.length; ee++) {
                  var ie = Q[ee];
                  ie.enumerable = ie.enumerable || !1, ie.configurable = !0, "value" in ie && (ie.writable = !0), Object.defineProperty(X, ie.key, ie);
                }
              }
              function z(X, Q, ee) {
                return Q && V(X.prototype, Q), ee && V(X, ee), X;
              }
              function F(X, Q) {
                if (typeof Q != "function" && Q !== null)
                  throw new TypeError("Super expression must either be null or a function");
                X.prototype = Object.create(Q && Q.prototype, { constructor: { value: X, writable: !0, configurable: !0 } }), Q && P(X, Q);
              }
              function P(X, Q) {
                return P = Object.setPrototypeOf || function(ie, oe) {
                  return ie.__proto__ = oe, ie;
                }, P(X, Q);
              }
              function k(X) {
                var Q = E();
                return function() {
                  var ie = M(X), oe;
                  if (Q) {
                    var ce = M(this).constructor;
                    oe = Reflect.construct(ie, arguments, ce);
                  } else
                    oe = ie.apply(this, arguments);
                  return R(this, oe);
                };
              }
              function R(X, Q) {
                return Q && (I(Q) === "object" || typeof Q == "function") ? Q : L(X);
              }
              function L(X) {
                if (X === void 0)
                  throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
                return X;
              }
              function E() {
                if (typeof Reflect > "u" || !Reflect.construct || Reflect.construct.sham)
                  return !1;
                if (typeof Proxy == "function")
                  return !0;
                try {
                  return Date.prototype.toString.call(Reflect.construct(Date, [], function() {
                  })), !0;
                } catch {
                  return !1;
                }
              }
              function M(X) {
                return M = Object.setPrototypeOf ? Object.getPrototypeOf : function(ee) {
                  return ee.__proto__ || Object.getPrototypeOf(ee);
                }, M(X);
              }
              function W(X, Q) {
                var ee = "data-clipboard-".concat(X);
                if (Q.hasAttribute(ee))
                  return Q.getAttribute(ee);
              }
              var Y = /* @__PURE__ */ function(X) {
                F(ee, X);
                var Q = k(ee);
                function ee(ie, oe) {
                  var ce;
                  return $(this, ee), ce = Q.call(this), ce.resolveOptions(oe), ce.listenClick(ie), ce;
                }
                return z(ee, [{
                  key: "resolveOptions",
                  value: function() {
                    var oe = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                    this.action = typeof oe.action == "function" ? oe.action : this.defaultAction, this.target = typeof oe.target == "function" ? oe.target : this.defaultTarget, this.text = typeof oe.text == "function" ? oe.text : this.defaultText, this.container = I(oe.container) === "object" ? oe.container : document.body;
                  }
                  /**
                   * Adds a click event listener to the passed trigger.
                   * @param {String|HTMLElement|HTMLCollection|NodeList} trigger
                   */
                }, {
                  key: "listenClick",
                  value: function(oe) {
                    var ce = this;
                    this.listener = g()(oe, "click", function(he) {
                      return ce.onClick(he);
                    });
                  }
                  /**
                   * Defines a new `ClipboardAction` on each click event.
                   * @param {Event} e
                   */
                }, {
                  key: "onClick",
                  value: function(oe) {
                    var ce = oe.delegateTarget || oe.currentTarget, he = this.action(ce) || "copy", we = y({
                      action: he,
                      container: this.container,
                      target: this.target(ce),
                      text: this.text(ce)
                    });
                    this.emit(we ? "success" : "error", {
                      action: he,
                      text: we,
                      trigger: ce,
                      clearSelection: function() {
                        ce && ce.focus(), window.getSelection().removeAllRanges();
                      }
                    });
                  }
                  /**
                   * Default `action` lookup function.
                   * @param {Element} trigger
                   */
                }, {
                  key: "defaultAction",
                  value: function(oe) {
                    return W("action", oe);
                  }
                  /**
                   * Default `target` lookup function.
                   * @param {Element} trigger
                   */
                }, {
                  key: "defaultTarget",
                  value: function(oe) {
                    var ce = W("target", oe);
                    if (ce)
                      return document.querySelector(ce);
                  }
                  /**
                   * Allow fire programmatically a copy action
                   * @param {String|HTMLElement} target
                   * @param {Object} options
                   * @returns Text copied.
                   */
                }, {
                  key: "defaultText",
                  /**
                   * Default `text` lookup function.
                   * @param {Element} trigger
                   */
                  value: function(oe) {
                    return W("text", oe);
                  }
                  /**
                   * Destroy lifecycle.
                   */
                }, {
                  key: "destroy",
                  value: function() {
                    this.listener.destroy();
                  }
                }], [{
                  key: "copy",
                  value: function(oe) {
                    var ce = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
                      container: document.body
                    };
                    return S(oe, ce);
                  }
                  /**
                   * Allow fire programmatically a cut action
                   * @param {String|HTMLElement} target
                   * @returns Text cutted.
                   */
                }, {
                  key: "cut",
                  value: function(oe) {
                    return h(oe);
                  }
                  /**
                   * Returns the support of the given action, or all actions if no action is
                   * given.
                   * @param {String} [action]
                   */
                }, {
                  key: "isSupported",
                  value: function() {
                    var oe = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : ["copy", "cut"], ce = typeof oe == "string" ? [oe] : oe, he = !!document.queryCommandSupported;
                    return ce.forEach(function(we) {
                      he = he && !!document.queryCommandSupported(we);
                    }), he;
                  }
                }]), ee;
              }(c()), J = Y;
            }
          ),
          /***/
          828: (
            /***/
            function(s) {
              var a = 9;
              if (typeof Element < "u" && !Element.prototype.matches) {
                var o = Element.prototype;
                o.matches = o.matchesSelector || o.mozMatchesSelector || o.msMatchesSelector || o.oMatchesSelector || o.webkitMatchesSelector;
              }
              function l(c, f) {
                for (; c && c.nodeType !== a; ) {
                  if (typeof c.matches == "function" && c.matches(f))
                    return c;
                  c = c.parentNode;
                }
              }
              s.exports = l;
            }
          ),
          /***/
          438: (
            /***/
            function(s, a, o) {
              var l = o(828);
              function c(b, w, C, m, h) {
                var u = g.apply(this, arguments);
                return b.addEventListener(C, u, h), {
                  destroy: function() {
                    b.removeEventListener(C, u, h);
                  }
                };
              }
              function f(b, w, C, m, h) {
                return typeof b.addEventListener == "function" ? c.apply(null, arguments) : typeof C == "function" ? c.bind(null, document).apply(null, arguments) : (typeof b == "string" && (b = document.querySelectorAll(b)), Array.prototype.map.call(b, function(u) {
                  return c(u, w, C, m, h);
                }));
              }
              function g(b, w, C, m) {
                return function(h) {
                  h.delegateTarget = l(h.target, w), h.delegateTarget && m.call(b, h);
                };
              }
              s.exports = f;
            }
          ),
          /***/
          879: (
            /***/
            function(s, a) {
              a.node = function(o) {
                return o !== void 0 && o instanceof HTMLElement && o.nodeType === 1;
              }, a.nodeList = function(o) {
                var l = Object.prototype.toString.call(o);
                return o !== void 0 && (l === "[object NodeList]" || l === "[object HTMLCollection]") && "length" in o && (o.length === 0 || a.node(o[0]));
              }, a.string = function(o) {
                return typeof o == "string" || o instanceof String;
              }, a.fn = function(o) {
                var l = Object.prototype.toString.call(o);
                return l === "[object Function]";
              };
            }
          ),
          /***/
          370: (
            /***/
            function(s, a, o) {
              var l = o(879), c = o(438);
              function f(C, m, h) {
                if (!C && !m && !h)
                  throw new Error("Missing required arguments");
                if (!l.string(m))
                  throw new TypeError("Second argument must be a String");
                if (!l.fn(h))
                  throw new TypeError("Third argument must be a Function");
                if (l.node(C))
                  return g(C, m, h);
                if (l.nodeList(C))
                  return b(C, m, h);
                if (l.string(C))
                  return w(C, m, h);
                throw new TypeError("First argument must be a String, HTMLElement, HTMLCollection, or NodeList");
              }
              function g(C, m, h) {
                return C.addEventListener(m, h), {
                  destroy: function() {
                    C.removeEventListener(m, h);
                  }
                };
              }
              function b(C, m, h) {
                return Array.prototype.forEach.call(C, function(u) {
                  u.addEventListener(m, h);
                }), {
                  destroy: function() {
                    Array.prototype.forEach.call(C, function(u) {
                      u.removeEventListener(m, h);
                    });
                  }
                };
              }
              function w(C, m, h) {
                return c(document.body, C, m, h);
              }
              s.exports = f;
            }
          ),
          /***/
          817: (
            /***/
            function(s) {
              function a(o) {
                var l;
                if (o.nodeName === "SELECT")
                  o.focus(), l = o.value;
                else if (o.nodeName === "INPUT" || o.nodeName === "TEXTAREA") {
                  var c = o.hasAttribute("readonly");
                  c || o.setAttribute("readonly", ""), o.select(), o.setSelectionRange(0, o.value.length), c || o.removeAttribute("readonly"), l = o.value;
                } else {
                  o.hasAttribute("contenteditable") && o.focus();
                  var f = window.getSelection(), g = document.createRange();
                  g.selectNodeContents(o), f.removeAllRanges(), f.addRange(g), l = f.toString();
                }
                return l;
              }
              s.exports = a;
            }
          ),
          /***/
          279: (
            /***/
            function(s) {
              function a() {
              }
              a.prototype = {
                on: function(o, l, c) {
                  var f = this.e || (this.e = {});
                  return (f[o] || (f[o] = [])).push({
                    fn: l,
                    ctx: c
                  }), this;
                },
                once: function(o, l, c) {
                  var f = this;
                  function g() {
                    f.off(o, g), l.apply(c, arguments);
                  }
                  return g._ = l, this.on(o, g, c);
                },
                emit: function(o) {
                  var l = [].slice.call(arguments, 1), c = ((this.e || (this.e = {}))[o] || []).slice(), f = 0, g = c.length;
                  for (f; f < g; f++)
                    c[f].fn.apply(c[f].ctx, l);
                  return this;
                },
                off: function(o, l) {
                  var c = this.e || (this.e = {}), f = c[o], g = [];
                  if (f && l)
                    for (var b = 0, w = f.length; b < w; b++)
                      f[b].fn !== l && f[b].fn._ !== l && g.push(f[b]);
                  return g.length ? c[o] = g : delete c[o], this;
                }
              }, s.exports = a, s.exports.TinyEmitter = a;
            }
          )
          /******/
        }, r = {};
        function i(s) {
          if (r[s])
            return r[s].exports;
          var a = r[s] = {
            /******/
            // no module.id needed
            /******/
            // no module.loaded needed
            /******/
            exports: {}
            /******/
          };
          return e[s](a, a.exports, i), a.exports;
        }
        return function() {
          i.n = function(s) {
            var a = s && s.__esModule ? (
              /******/
              function() {
                return s.default;
              }
            ) : (
              /******/
              function() {
                return s;
              }
            );
            return i.d(a, { a }), a;
          };
        }(), function() {
          i.d = function(s, a) {
            for (var o in a)
              i.o(a, o) && !i.o(s, o) && Object.defineProperty(s, o, { enumerable: !0, get: a[o] });
          };
        }(), function() {
          i.o = function(s, a) {
            return Object.prototype.hasOwnProperty.call(s, a);
          };
        }(), i(686);
      }().default
    );
  });
})(Ih);
var bw = Ih.exports;
const r_ = /* @__PURE__ */ wn(bw);
/**!
 * Sortable 1.15.7
 * @author	RubaXa   <trash@rubaxa.org>
 * @author	owenm    <owen23355@gmail.com>
 * @license MIT
 */
function yw(t, n, e) {
  return (n = xw(n)) in t ? Object.defineProperty(t, n, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : t[n] = e, t;
}
function qn() {
  return qn = Object.assign ? Object.assign.bind() : function(t) {
    for (var n = 1; n < arguments.length; n++) {
      var e = arguments[n];
      for (var r in e)
        ({}).hasOwnProperty.call(e, r) && (t[r] = e[r]);
    }
    return t;
  }, qn.apply(null, arguments);
}
function bd(t, n) {
  var e = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    n && (r = r.filter(function(i) {
      return Object.getOwnPropertyDescriptor(t, i).enumerable;
    })), e.push.apply(e, r);
  }
  return e;
}
function Fn(t) {
  for (var n = 1; n < arguments.length; n++) {
    var e = arguments[n] != null ? arguments[n] : {};
    n % 2 ? bd(Object(e), !0).forEach(function(r) {
      yw(t, r, e[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(e)) : bd(Object(e)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(e, r));
    });
  }
  return t;
}
function ww(t, n) {
  if (t == null)
    return {};
  var e, r, i = _w(t, n);
  if (Object.getOwnPropertySymbols) {
    var s = Object.getOwnPropertySymbols(t);
    for (r = 0; r < s.length; r++)
      e = s[r], n.indexOf(e) === -1 && {}.propertyIsEnumerable.call(t, e) && (i[e] = t[e]);
  }
  return i;
}
function _w(t, n) {
  if (t == null)
    return {};
  var e = {};
  for (var r in t)
    if ({}.hasOwnProperty.call(t, r)) {
      if (n.indexOf(r) !== -1)
        continue;
      e[r] = t[r];
    }
  return e;
}
function Cw(t, n) {
  if (typeof t != "object" || !t)
    return t;
  var e = t[Symbol.toPrimitive];
  if (e !== void 0) {
    var r = e.call(t, n || "default");
    if (typeof r != "object")
      return r;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (n === "string" ? String : Number)(t);
}
function xw(t) {
  var n = Cw(t, "string");
  return typeof n == "symbol" ? n : n + "";
}
function Al(t) {
  "@babel/helpers - typeof";
  return Al = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(n) {
    return typeof n;
  } : function(n) {
    return n && typeof Symbol == "function" && n.constructor === Symbol && n !== Symbol.prototype ? "symbol" : typeof n;
  }, Al(t);
}
var Sw = "1.15.7";
function Wn(t) {
  if (typeof window < "u" && window.navigator)
    return !!/* @__PURE__ */ navigator.userAgent.match(t);
}
var Un = Wn(/(?:Trident.*rv[ :]?11\.|msie|iemobile|Windows Phone)/i), Os = Wn(/Edge/i), yd = Wn(/firefox/i), os = Wn(/safari/i) && !Wn(/chrome/i) && !Wn(/android/i), Cu = Wn(/iP(ad|od|hone)/i), Nh = Wn(/chrome/i) && Wn(/android/i), Rh = {
  capture: !1,
  passive: !1
};
function ze(t, n, e) {
  t.addEventListener(n, e, !Un && Rh);
}
function je(t, n, e) {
  t.removeEventListener(n, e, !Un && Rh);
}
function Na(t, n) {
  if (n) {
    if (n[0] === ">" && (n = n.substring(1)), t)
      try {
        if (t.matches)
          return t.matches(n);
        if (t.msMatchesSelector)
          return t.msMatchesSelector(n);
        if (t.webkitMatchesSelector)
          return t.webkitMatchesSelector(n);
      } catch {
        return !1;
      }
    return !1;
  }
}
function Lh(t) {
  return t.host && t !== document && t.host.nodeType && t.host !== t ? t.host : t.parentNode;
}
function Dn(t, n, e, r) {
  if (t) {
    e = e || document;
    do {
      if (n != null && (n[0] === ">" ? t.parentNode === e && Na(t, n) : Na(t, n)) || r && t === e)
        return t;
      if (t === e)
        break;
    } while (t = Lh(t));
  }
  return null;
}
var wd = /\s+/g;
function Qt(t, n, e) {
  if (t && n)
    if (t.classList)
      t.classList[e ? "add" : "remove"](n);
    else {
      var r = (" " + t.className + " ").replace(wd, " ").replace(" " + n + " ", " ");
      t.className = (r + (e ? " " + n : "")).replace(wd, " ");
    }
}
function ke(t, n, e) {
  var r = t && t.style;
  if (r) {
    if (e === void 0)
      return document.defaultView && document.defaultView.getComputedStyle ? e = document.defaultView.getComputedStyle(t, "") : t.currentStyle && (e = t.currentStyle), n === void 0 ? e : e[n];
    !(n in r) && n.indexOf("webkit") === -1 && (n = "-webkit-" + n), r[n] = e + (typeof e == "string" ? "" : "px");
  }
}
function yi(t, n) {
  var e = "";
  if (typeof t == "string")
    e = t;
  else
    do {
      var r = ke(t, "transform");
      r && r !== "none" && (e = r + " " + e);
    } while (!n && (t = t.parentNode));
  var i = window.DOMMatrix || window.WebKitCSSMatrix || window.CSSMatrix || window.MSCSSMatrix;
  return i && new i(e);
}
function Mh(t, n, e) {
  if (t) {
    var r = t.getElementsByTagName(n), i = 0, s = r.length;
    if (e)
      for (; i < s; i++)
        e(r[i], i);
    return r;
  }
  return [];
}
function Nn() {
  var t = document.scrollingElement;
  return t || document.documentElement;
}
function gt(t, n, e, r, i) {
  if (!(!t.getBoundingClientRect && t !== window)) {
    var s, a, o, l, c, f, g;
    if (t !== window && t.parentNode && t !== Nn() ? (s = t.getBoundingClientRect(), a = s.top, o = s.left, l = s.bottom, c = s.right, f = s.height, g = s.width) : (a = 0, o = 0, l = window.innerHeight, c = window.innerWidth, f = window.innerHeight, g = window.innerWidth), (n || e) && t !== window && (i = i || t.parentNode, !Un))
      do
        if (i && i.getBoundingClientRect && (ke(i, "transform") !== "none" || e && ke(i, "position") !== "static")) {
          var b = i.getBoundingClientRect();
          a -= b.top + parseInt(ke(i, "border-top-width")), o -= b.left + parseInt(ke(i, "border-left-width")), l = a + s.height, c = o + s.width;
          break;
        }
      while (i = i.parentNode);
    if (r && t !== window) {
      var w = yi(i || t), C = w && w.a, m = w && w.d;
      w && (a /= m, o /= C, g /= C, f /= m, l = a + f, c = o + g);
    }
    return {
      top: a,
      left: o,
      bottom: l,
      right: c,
      width: g,
      height: f
    };
  }
}
function _d(t, n, e) {
  for (var r = ar(t, !0), i = gt(t)[n]; r; ) {
    var s = gt(r)[e], a = void 0;
    if (e === "top" || e === "left" ? a = i >= s : a = i <= s, !a)
      return r;
    if (r === Nn())
      break;
    r = ar(r, !1);
  }
  return !1;
}
function Pi(t, n, e, r) {
  for (var i = 0, s = 0, a = t.children; s < a.length; ) {
    if (a[s].style.display !== "none" && a[s] !== Pe.ghost && (r || a[s] !== Pe.dragged) && Dn(a[s], e.draggable, t, !1)) {
      if (i === n)
        return a[s];
      i++;
    }
    s++;
  }
  return null;
}
function xu(t, n) {
  for (var e = t.lastElementChild; e && (e === Pe.ghost || ke(e, "display") === "none" || n && !Na(e, n)); )
    e = e.previousElementSibling;
  return e || null;
}
function gn(t, n) {
  var e = 0;
  if (!t || !t.parentNode)
    return -1;
  for (; t = t.previousElementSibling; )
    t.nodeName.toUpperCase() !== "TEMPLATE" && t !== Pe.clone && (!n || Na(t, n)) && e++;
  return e;
}
function Cd(t) {
  var n = 0, e = 0, r = Nn();
  if (t)
    do {
      var i = yi(t), s = i.a, a = i.d;
      n += t.scrollLeft * s, e += t.scrollTop * a;
    } while (t !== r && (t = t.parentNode));
  return [n, e];
}
function Tw(t, n) {
  for (var e in t)
    if (t.hasOwnProperty(e)) {
      for (var r in n)
        if (n.hasOwnProperty(r) && n[r] === t[e][r])
          return Number(e);
    }
  return -1;
}
function ar(t, n) {
  if (!t || !t.getBoundingClientRect)
    return Nn();
  var e = t, r = !1;
  do
    if (e.clientWidth < e.scrollWidth || e.clientHeight < e.scrollHeight) {
      var i = ke(e);
      if (e.clientWidth < e.scrollWidth && (i.overflowX == "auto" || i.overflowX == "scroll") || e.clientHeight < e.scrollHeight && (i.overflowY == "auto" || i.overflowY == "scroll")) {
        if (!e.getBoundingClientRect || e === document.body)
          return Nn();
        if (r || n)
          return e;
        r = !0;
      }
    }
  while (e = e.parentNode);
  return Nn();
}
function Dw(t, n) {
  if (t && n)
    for (var e in n)
      n.hasOwnProperty(e) && (t[e] = n[e]);
  return t;
}
function Zo(t, n) {
  return Math.round(t.top) === Math.round(n.top) && Math.round(t.left) === Math.round(n.left) && Math.round(t.height) === Math.round(n.height) && Math.round(t.width) === Math.round(n.width);
}
var ls;
function Fh(t, n) {
  return function() {
    if (!ls) {
      var e = arguments, r = this;
      e.length === 1 ? t.call(r, e[0]) : t.apply(r, e), ls = setTimeout(function() {
        ls = void 0;
      }, n);
    }
  };
}
function Ew() {
  clearTimeout(ls), ls = void 0;
}
function Bh(t, n, e) {
  t.scrollLeft += n, t.scrollTop += e;
}
function Hh(t) {
  var n = window.Polymer, e = window.jQuery || window.Zepto;
  return n && n.dom ? n.dom(t).cloneNode(!0) : e ? e(t).clone(!0)[0] : t.cloneNode(!0);
}
function $h(t, n, e) {
  var r = {};
  return Array.from(t.children).forEach(function(i) {
    var s, a, o, l;
    if (!(!Dn(i, n.draggable, t, !1) || i.animated || i === e)) {
      var c = gt(i);
      r.left = Math.min((s = r.left) !== null && s !== void 0 ? s : 1 / 0, c.left), r.top = Math.min((a = r.top) !== null && a !== void 0 ? a : 1 / 0, c.top), r.right = Math.max((o = r.right) !== null && o !== void 0 ? o : -1 / 0, c.right), r.bottom = Math.max((l = r.bottom) !== null && l !== void 0 ? l : -1 / 0, c.bottom);
    }
  }), r.width = r.right - r.left, r.height = r.bottom - r.top, r.x = r.left, r.y = r.top, r;
}
var Ut = "Sortable" + (/* @__PURE__ */ new Date()).getTime();
function Aw() {
  var t = [], n;
  return {
    captureAnimationState: function() {
      if (t = [], !!this.options.animation) {
        var r = [].slice.call(this.el.children);
        r.forEach(function(i) {
          if (!(ke(i, "display") === "none" || i === Pe.ghost)) {
            t.push({
              target: i,
              rect: gt(i)
            });
            var s = Fn({}, t[t.length - 1].rect);
            if (i.thisAnimationDuration) {
              var a = yi(i, !0);
              a && (s.top -= a.f, s.left -= a.e);
            }
            i.fromRect = s;
          }
        });
      }
    },
    addAnimationState: function(r) {
      t.push(r);
    },
    removeAnimationState: function(r) {
      t.splice(Tw(t, {
        target: r
      }), 1);
    },
    animateAll: function(r) {
      var i = this;
      if (!this.options.animation) {
        clearTimeout(n), typeof r == "function" && r();
        return;
      }
      var s = !1, a = 0;
      t.forEach(function(o) {
        var l = 0, c = o.target, f = c.fromRect, g = gt(c), b = c.prevFromRect, w = c.prevToRect, C = o.rect, m = yi(c, !0);
        m && (g.top -= m.f, g.left -= m.e), c.toRect = g, c.thisAnimationDuration && Zo(b, g) && !Zo(f, g) && // Make sure animatingRect is on line between toRect & fromRect
        (C.top - g.top) / (C.left - g.left) === (f.top - g.top) / (f.left - g.left) && (l = Pw(C, b, w, i.options)), Zo(g, f) || (c.prevFromRect = f, c.prevToRect = g, l || (l = i.options.animation), i.animate(c, C, g, l)), l && (s = !0, a = Math.max(a, l), clearTimeout(c.animationResetTimer), c.animationResetTimer = setTimeout(function() {
          c.animationTime = 0, c.prevFromRect = null, c.fromRect = null, c.prevToRect = null, c.thisAnimationDuration = null;
        }, l), c.thisAnimationDuration = l);
      }), clearTimeout(n), s ? n = setTimeout(function() {
        typeof r == "function" && r();
      }, a) : typeof r == "function" && r(), t = [];
    },
    animate: function(r, i, s, a) {
      if (a) {
        ke(r, "transition", ""), ke(r, "transform", "");
        var o = yi(this.el), l = o && o.a, c = o && o.d, f = (i.left - s.left) / (l || 1), g = (i.top - s.top) / (c || 1);
        r.animatingX = !!f, r.animatingY = !!g, ke(r, "transform", "translate3d(" + f + "px," + g + "px,0)"), this.forRepaintDummy = kw(r), ke(r, "transition", "transform " + a + "ms" + (this.options.easing ? " " + this.options.easing : "")), ke(r, "transform", "translate3d(0,0,0)"), typeof r.animated == "number" && clearTimeout(r.animated), r.animated = setTimeout(function() {
          ke(r, "transition", ""), ke(r, "transform", ""), r.animated = !1, r.animatingX = !1, r.animatingY = !1;
        }, a);
      }
    }
  };
}
function kw(t) {
  return t.offsetWidth;
}
function Pw(t, n, e, r) {
  return Math.sqrt(Math.pow(n.top - t.top, 2) + Math.pow(n.left - t.left, 2)) / Math.sqrt(Math.pow(n.top - e.top, 2) + Math.pow(n.left - e.left, 2)) * r.animation;
}
var li = [], el = {
  initializeByDefault: !0
}, Is = {
  mount: function(n) {
    for (var e in el)
      el.hasOwnProperty(e) && !(e in n) && (n[e] = el[e]);
    li.forEach(function(r) {
      if (r.pluginName === n.pluginName)
        throw "Sortable: Cannot mount plugin ".concat(n.pluginName, " more than once");
    }), li.push(n);
  },
  pluginEvent: function(n, e, r) {
    var i = this;
    this.eventCanceled = !1, r.cancel = function() {
      i.eventCanceled = !0;
    };
    var s = n + "Global";
    li.forEach(function(a) {
      e[a.pluginName] && (e[a.pluginName][s] && e[a.pluginName][s](Fn({
        sortable: e
      }, r)), e.options[a.pluginName] && e[a.pluginName][n] && e[a.pluginName][n](Fn({
        sortable: e
      }, r)));
    });
  },
  initializePlugins: function(n, e, r, i) {
    li.forEach(function(o) {
      var l = o.pluginName;
      if (!(!n.options[l] && !o.initializeByDefault)) {
        var c = new o(n, e, n.options);
        c.sortable = n, c.options = n.options, n[l] = c, qn(r, c.defaults);
      }
    });
    for (var s in n.options)
      if (n.options.hasOwnProperty(s)) {
        var a = this.modifyOption(n, s, n.options[s]);
        typeof a < "u" && (n.options[s] = a);
      }
  },
  getEventProperties: function(n, e) {
    var r = {};
    return li.forEach(function(i) {
      typeof i.eventProperties == "function" && qn(r, i.eventProperties.call(e[i.pluginName], n));
    }), r;
  },
  modifyOption: function(n, e, r) {
    var i;
    return li.forEach(function(s) {
      n[s.pluginName] && s.optionListeners && typeof s.optionListeners[e] == "function" && (i = s.optionListeners[e].call(n[s.pluginName], r));
    }), i;
  }
};
function Ow(t) {
  var n = t.sortable, e = t.rootEl, r = t.name, i = t.targetEl, s = t.cloneEl, a = t.toEl, o = t.fromEl, l = t.oldIndex, c = t.newIndex, f = t.oldDraggableIndex, g = t.newDraggableIndex, b = t.originalEvent, w = t.putSortable, C = t.extraEventProperties;
  if (n = n || e && e[Ut], !!n) {
    var m, h = n.options, u = "on" + r.charAt(0).toUpperCase() + r.substr(1);
    window.CustomEvent && !Un && !Os ? m = new CustomEvent(r, {
      bubbles: !0,
      cancelable: !0
    }) : (m = document.createEvent("Event"), m.initEvent(r, !0, !0)), m.to = a || e, m.from = o || e, m.item = i || e, m.clone = s, m.oldIndex = l, m.newIndex = c, m.oldDraggableIndex = f, m.newDraggableIndex = g, m.originalEvent = b, m.pullMode = w ? w.lastPutMode : void 0;
    var p = Fn(Fn({}, C), Is.getEventProperties(r, n));
    for (var v in p)
      m[v] = p[v];
    e && e.dispatchEvent(m), h[u] && h[u].call(n, m);
  }
}
var Iw = ["evt"], zt = function(n, e) {
  var r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, i = r.evt, s = ww(r, Iw);
  Is.pluginEvent.bind(Pe)(n, e, Fn({
    dragEl: ge,
    parentEl: ct,
    ghostEl: Me,
    rootEl: st,
    nextEl: Sr,
    lastDownEl: ma,
    cloneEl: at,
    cloneHidden: nr,
    dragStarted: ns,
    putSortable: xt,
    activeSortable: Pe.active,
    originalEvent: i,
    oldIndex: mi,
    oldDraggableIndex: us,
    newIndex: Zt,
    newDraggableIndex: er,
    hideGhostForTarget: zh,
    unhideGhostForTarget: Yh,
    cloneNowHidden: function() {
      nr = !0;
    },
    cloneNowShown: function() {
      nr = !1;
    },
    dispatchSortableEvent: function(o) {
      Mt({
        sortable: e,
        name: o,
        originalEvent: i
      });
    }
  }, s));
};
function Mt(t) {
  Ow(Fn({
    putSortable: xt,
    cloneEl: at,
    targetEl: ge,
    rootEl: st,
    oldIndex: mi,
    oldDraggableIndex: us,
    newIndex: Zt,
    newDraggableIndex: er
  }, t));
}
var ge, ct, Me, st, Sr, ma, at, nr, mi, Zt, us, er, sa, xt, pi = !1, Ra = !1, La = [], Cr, Tn, tl, nl, xd, Sd, ns, ui, cs, ds = !1, aa = !1, va, Et, rl = [], kl = !1, Ma = [], io = typeof document < "u", oa = Cu, Td = Os || Un ? "cssFloat" : "float", Nw = io && !Nh && !Cu && "draggable" in document.createElement("div"), Vh = function() {
  if (io) {
    if (Un)
      return !1;
    var t = document.createElement("x");
    return t.style.cssText = "pointer-events:auto", t.style.pointerEvents === "auto";
  }
}(), Wh = function(n, e) {
  var r = ke(n), i = parseInt(r.width) - parseInt(r.paddingLeft) - parseInt(r.paddingRight) - parseInt(r.borderLeftWidth) - parseInt(r.borderRightWidth), s = Pi(n, 0, e), a = Pi(n, 1, e), o = s && ke(s), l = a && ke(a), c = o && parseInt(o.marginLeft) + parseInt(o.marginRight) + gt(s).width, f = l && parseInt(l.marginLeft) + parseInt(l.marginRight) + gt(a).width;
  if (r.display === "flex")
    return r.flexDirection === "column" || r.flexDirection === "column-reverse" ? "vertical" : "horizontal";
  if (r.display === "grid")
    return r.gridTemplateColumns.split(" ").length <= 1 ? "vertical" : "horizontal";
  if (s && o.float && o.float !== "none") {
    var g = o.float === "left" ? "left" : "right";
    return a && (l.clear === "both" || l.clear === g) ? "vertical" : "horizontal";
  }
  return s && (o.display === "block" || o.display === "flex" || o.display === "table" || o.display === "grid" || c >= i && r[Td] === "none" || a && r[Td] === "none" && c + f > i) ? "vertical" : "horizontal";
}, Rw = function(n, e, r) {
  var i = r ? n.left : n.top, s = r ? n.right : n.bottom, a = r ? n.width : n.height, o = r ? e.left : e.top, l = r ? e.right : e.bottom, c = r ? e.width : e.height;
  return i === o || s === l || i + a / 2 === o + c / 2;
}, Lw = function(n, e) {
  var r;
  return La.some(function(i) {
    var s = i[Ut].options.emptyInsertThreshold;
    if (!(!s || xu(i))) {
      var a = gt(i), o = n >= a.left - s && n <= a.right + s, l = e >= a.top - s && e <= a.bottom + s;
      if (o && l)
        return r = i;
    }
  }), r;
}, jh = function(n) {
  function e(s, a) {
    return function(o, l, c, f) {
      var g = o.options.group.name && l.options.group.name && o.options.group.name === l.options.group.name;
      if (s == null && (a || g))
        return !0;
      if (s == null || s === !1)
        return !1;
      if (a && s === "clone")
        return s;
      if (typeof s == "function")
        return e(s(o, l, c, f), a)(o, l, c, f);
      var b = (a ? o : l).options.group.name;
      return s === !0 || typeof s == "string" && s === b || s.join && s.indexOf(b) > -1;
    };
  }
  var r = {}, i = n.group;
  (!i || Al(i) != "object") && (i = {
    name: i
  }), r.name = i.name, r.checkPull = e(i.pull, !0), r.checkPut = e(i.put), r.revertClone = i.revertClone, n.group = r;
}, zh = function() {
  !Vh && Me && ke(Me, "display", "none");
}, Yh = function() {
  !Vh && Me && ke(Me, "display", "");
};
io && !Nh && document.addEventListener("click", function(t) {
  if (Ra)
    return t.preventDefault(), t.stopPropagation && t.stopPropagation(), t.stopImmediatePropagation && t.stopImmediatePropagation(), Ra = !1, !1;
}, !0);
var xr = function(n) {
  if (ge) {
    n = n.touches ? n.touches[0] : n;
    var e = Lw(n.clientX, n.clientY);
    if (e) {
      var r = {};
      for (var i in n)
        n.hasOwnProperty(i) && (r[i] = n[i]);
      r.target = r.rootEl = e, r.preventDefault = void 0, r.stopPropagation = void 0, e[Ut]._onDragOver(r);
    }
  }
}, Mw = function(n) {
  ge && ge.parentNode[Ut]._isOutsideThisEl(n.target);
};
function Pe(t, n) {
  if (!(t && t.nodeType && t.nodeType === 1))
    throw "Sortable: `el` must be an HTMLElement, not ".concat({}.toString.call(t));
  this.el = t, this.options = n = qn({}, n), t[Ut] = this;
  var e = {
    group: null,
    sort: !0,
    disabled: !1,
    store: null,
    handle: null,
    draggable: /^[uo]l$/i.test(t.nodeName) ? ">li" : ">*",
    swapThreshold: 1,
    // percentage; 0 <= x <= 1
    invertSwap: !1,
    // invert always
    invertedSwapThreshold: null,
    // will be set to same as swapThreshold if default
    removeCloneOnHide: !0,
    direction: function() {
      return Wh(t, this.options);
    },
    ghostClass: "sortable-ghost",
    chosenClass: "sortable-chosen",
    dragClass: "sortable-drag",
    ignore: "a, img",
    filter: null,
    preventOnFilter: !0,
    animation: 0,
    easing: null,
    setData: function(a, o) {
      a.setData("Text", o.textContent);
    },
    dropBubble: !1,
    dragoverBubble: !1,
    dataIdAttr: "data-id",
    delay: 0,
    delayOnTouchOnly: !1,
    touchStartThreshold: (Number.parseInt ? Number : window).parseInt(window.devicePixelRatio, 10) || 1,
    forceFallback: !1,
    fallbackClass: "sortable-fallback",
    fallbackOnBody: !1,
    fallbackTolerance: 0,
    fallbackOffset: {
      x: 0,
      y: 0
    },
    // Disabled on Safari: #1571; Enabled on Safari IOS: #2244
    supportPointer: Pe.supportPointer !== !1 && "PointerEvent" in window && (!os || Cu),
    emptyInsertThreshold: 5
  };
  Is.initializePlugins(this, t, e);
  for (var r in e)
    !(r in n) && (n[r] = e[r]);
  jh(n);
  for (var i in this)
    i.charAt(0) === "_" && typeof this[i] == "function" && (this[i] = this[i].bind(this));
  this.nativeDraggable = n.forceFallback ? !1 : Nw, this.nativeDraggable && (this.options.touchStartThreshold = 1), n.supportPointer ? ze(t, "pointerdown", this._onTapStart) : (ze(t, "mousedown", this._onTapStart), ze(t, "touchstart", this._onTapStart)), this.nativeDraggable && (ze(t, "dragover", this), ze(t, "dragenter", this)), La.push(this.el), n.store && n.store.get && this.sort(n.store.get(this) || []), qn(this, Aw());
}
Pe.prototype = /** @lends Sortable.prototype */
{
  constructor: Pe,
  _isOutsideThisEl: function(n) {
    !this.el.contains(n) && n !== this.el && (ui = null);
  },
  _getDirection: function(n, e) {
    return typeof this.options.direction == "function" ? this.options.direction.call(this, n, e, ge) : this.options.direction;
  },
  _onTapStart: function(n) {
    if (n.cancelable) {
      var e = this, r = this.el, i = this.options, s = i.preventOnFilter, a = n.type, o = n.touches && n.touches[0] || n.pointerType && n.pointerType === "touch" && n, l = (o || n).target, c = n.target.shadowRoot && (n.path && n.path[0] || n.composedPath && n.composedPath()[0]) || l, f = i.filter;
      if (zw(r), !ge && !(/mousedown|pointerdown/.test(a) && n.button !== 0 || i.disabled) && !c.isContentEditable && !(!this.nativeDraggable && os && l && l.tagName.toUpperCase() === "SELECT") && (l = Dn(l, i.draggable, r, !1), !(l && l.animated) && ma !== l)) {
        if (mi = gn(l), us = gn(l, i.draggable), typeof f == "function") {
          if (f.call(this, n, l, this)) {
            Mt({
              sortable: e,
              rootEl: c,
              name: "filter",
              targetEl: l,
              toEl: r,
              fromEl: r
            }), zt("filter", e, {
              evt: n
            }), s && n.preventDefault();
            return;
          }
        } else if (f && (f = f.split(",").some(function(g) {
          if (g = Dn(c, g.trim(), r, !1), g)
            return Mt({
              sortable: e,
              rootEl: g,
              name: "filter",
              targetEl: l,
              fromEl: r,
              toEl: r
            }), zt("filter", e, {
              evt: n
            }), !0;
        }), f)) {
          s && n.preventDefault();
          return;
        }
        i.handle && !Dn(c, i.handle, r, !1) || this._prepareDragStart(n, o, l);
      }
    }
  },
  _prepareDragStart: function(n, e, r) {
    var i = this, s = i.el, a = i.options, o = s.ownerDocument, l;
    if (r && !ge && r.parentNode === s) {
      var c = gt(r);
      if (st = s, ge = r, ct = ge.parentNode, Sr = ge.nextSibling, ma = r, sa = a.group, Pe.dragged = ge, Cr = {
        target: ge,
        clientX: (e || n).clientX,
        clientY: (e || n).clientY
      }, xd = Cr.clientX - c.left, Sd = Cr.clientY - c.top, this._lastX = (e || n).clientX, this._lastY = (e || n).clientY, ge.style["will-change"] = "all", l = function() {
        if (zt("delayEnded", i, {
          evt: n
        }), Pe.eventCanceled) {
          i._onDrop();
          return;
        }
        i._disableDelayedDragEvents(), !yd && i.nativeDraggable && (ge.draggable = !0), i._triggerDragStart(n, e), Mt({
          sortable: i,
          name: "choose",
          originalEvent: n
        }), Qt(ge, a.chosenClass, !0);
      }, a.ignore.split(",").forEach(function(f) {
        Mh(ge, f.trim(), il);
      }), ze(o, "dragover", xr), ze(o, "mousemove", xr), ze(o, "touchmove", xr), a.supportPointer ? (ze(o, "pointerup", i._onDrop), !this.nativeDraggable && ze(o, "pointercancel", i._onDrop)) : (ze(o, "mouseup", i._onDrop), ze(o, "touchend", i._onDrop), ze(o, "touchcancel", i._onDrop)), yd && this.nativeDraggable && (this.options.touchStartThreshold = 4, ge.draggable = !0), zt("delayStart", this, {
        evt: n
      }), a.delay && (!a.delayOnTouchOnly || e) && (!this.nativeDraggable || !(Os || Un))) {
        if (Pe.eventCanceled) {
          this._onDrop();
          return;
        }
        a.supportPointer ? (ze(o, "pointerup", i._disableDelayedDrag), ze(o, "pointercancel", i._disableDelayedDrag)) : (ze(o, "mouseup", i._disableDelayedDrag), ze(o, "touchend", i._disableDelayedDrag), ze(o, "touchcancel", i._disableDelayedDrag)), ze(o, "mousemove", i._delayedDragTouchMoveHandler), ze(o, "touchmove", i._delayedDragTouchMoveHandler), a.supportPointer && ze(o, "pointermove", i._delayedDragTouchMoveHandler), i._dragStartTimer = setTimeout(l, a.delay);
      } else
        l();
    }
  },
  _delayedDragTouchMoveHandler: function(n) {
    var e = n.touches ? n.touches[0] : n;
    Math.max(Math.abs(e.clientX - this._lastX), Math.abs(e.clientY - this._lastY)) >= Math.floor(this.options.touchStartThreshold / (this.nativeDraggable && window.devicePixelRatio || 1)) && this._disableDelayedDrag();
  },
  _disableDelayedDrag: function() {
    ge && il(ge), clearTimeout(this._dragStartTimer), this._disableDelayedDragEvents();
  },
  _disableDelayedDragEvents: function() {
    var n = this.el.ownerDocument;
    je(n, "mouseup", this._disableDelayedDrag), je(n, "touchend", this._disableDelayedDrag), je(n, "touchcancel", this._disableDelayedDrag), je(n, "pointerup", this._disableDelayedDrag), je(n, "pointercancel", this._disableDelayedDrag), je(n, "mousemove", this._delayedDragTouchMoveHandler), je(n, "touchmove", this._delayedDragTouchMoveHandler), je(n, "pointermove", this._delayedDragTouchMoveHandler);
  },
  _triggerDragStart: function(n, e) {
    e = e || n.pointerType == "touch" && n, !this.nativeDraggable || e ? this.options.supportPointer ? ze(document, "pointermove", this._onTouchMove) : e ? ze(document, "touchmove", this._onTouchMove) : ze(document, "mousemove", this._onTouchMove) : (ze(ge, "dragend", this), ze(st, "dragstart", this._onDragStart));
    try {
      document.selection ? ba(function() {
        document.selection.empty();
      }) : window.getSelection().removeAllRanges();
    } catch {
    }
  },
  _dragStarted: function(n, e) {
    if (pi = !1, st && ge) {
      zt("dragStarted", this, {
        evt: e
      }), this.nativeDraggable && ze(document, "dragover", Mw);
      var r = this.options;
      !n && Qt(ge, r.dragClass, !1), Qt(ge, r.ghostClass, !0), Pe.active = this, n && this._appendGhost(), Mt({
        sortable: this,
        name: "start",
        originalEvent: e
      });
    } else
      this._nulling();
  },
  _emulateDragOver: function() {
    if (Tn) {
      this._lastX = Tn.clientX, this._lastY = Tn.clientY, zh();
      for (var n = document.elementFromPoint(Tn.clientX, Tn.clientY), e = n; n && n.shadowRoot && (n = n.shadowRoot.elementFromPoint(Tn.clientX, Tn.clientY), n !== e); )
        e = n;
      if (ge.parentNode[Ut]._isOutsideThisEl(n), e)
        do {
          if (e[Ut]) {
            var r = void 0;
            if (r = e[Ut]._onDragOver({
              clientX: Tn.clientX,
              clientY: Tn.clientY,
              target: n,
              rootEl: e
            }), r && !this.options.dragoverBubble)
              break;
          }
          n = e;
        } while (e = Lh(e));
      Yh();
    }
  },
  _onTouchMove: function(n) {
    if (Cr) {
      var e = this.options, r = e.fallbackTolerance, i = e.fallbackOffset, s = n.touches ? n.touches[0] : n, a = Me && yi(Me, !0), o = Me && a && a.a, l = Me && a && a.d, c = oa && Et && Cd(Et), f = (s.clientX - Cr.clientX + i.x) / (o || 1) + (c ? c[0] - rl[0] : 0) / (o || 1), g = (s.clientY - Cr.clientY + i.y) / (l || 1) + (c ? c[1] - rl[1] : 0) / (l || 1);
      if (!Pe.active && !pi) {
        if (r && Math.max(Math.abs(s.clientX - this._lastX), Math.abs(s.clientY - this._lastY)) < r)
          return;
        this._onDragStart(n, !0);
      }
      if (Me) {
        a ? (a.e += f - (tl || 0), a.f += g - (nl || 0)) : a = {
          a: 1,
          b: 0,
          c: 0,
          d: 1,
          e: f,
          f: g
        };
        var b = "matrix(".concat(a.a, ",").concat(a.b, ",").concat(a.c, ",").concat(a.d, ",").concat(a.e, ",").concat(a.f, ")");
        ke(Me, "webkitTransform", b), ke(Me, "mozTransform", b), ke(Me, "msTransform", b), ke(Me, "transform", b), tl = f, nl = g, Tn = s;
      }
      n.cancelable && n.preventDefault();
    }
  },
  _appendGhost: function() {
    if (!Me) {
      var n = this.options.fallbackOnBody ? document.body : st, e = gt(ge, !0, oa, !0, n), r = this.options;
      if (oa) {
        for (Et = n; ke(Et, "position") === "static" && ke(Et, "transform") === "none" && Et !== document; )
          Et = Et.parentNode;
        Et !== document.body && Et !== document.documentElement ? (Et === document && (Et = Nn()), e.top += Et.scrollTop, e.left += Et.scrollLeft) : Et = Nn(), rl = Cd(Et);
      }
      Me = ge.cloneNode(!0), Qt(Me, r.ghostClass, !1), Qt(Me, r.fallbackClass, !0), Qt(Me, r.dragClass, !0), ke(Me, "transition", ""), ke(Me, "transform", ""), ke(Me, "box-sizing", "border-box"), ke(Me, "margin", 0), ke(Me, "top", e.top), ke(Me, "left", e.left), ke(Me, "width", e.width), ke(Me, "height", e.height), ke(Me, "opacity", "0.8"), ke(Me, "position", oa ? "absolute" : "fixed"), ke(Me, "zIndex", "100000"), ke(Me, "pointerEvents", "none"), Pe.ghost = Me, n.appendChild(Me), ke(Me, "transform-origin", xd / parseInt(Me.style.width) * 100 + "% " + Sd / parseInt(Me.style.height) * 100 + "%");
    }
  },
  _onDragStart: function(n, e) {
    var r = this, i = n.dataTransfer, s = r.options;
    if (zt("dragStart", this, {
      evt: n
    }), Pe.eventCanceled) {
      this._onDrop();
      return;
    }
    zt("setupClone", this), Pe.eventCanceled || (at = Hh(ge), at.removeAttribute("id"), at.draggable = !1, at.style["will-change"] = "", this._hideClone(), Qt(at, this.options.chosenClass, !1), Pe.clone = at), r.cloneId = ba(function() {
      zt("clone", r), !Pe.eventCanceled && (r.options.removeCloneOnHide || st.insertBefore(at, ge), r._hideClone(), Mt({
        sortable: r,
        name: "clone"
      }));
    }), !e && Qt(ge, s.dragClass, !0), e ? (Ra = !0, r._loopId = setInterval(r._emulateDragOver, 50)) : (je(document, "mouseup", r._onDrop), je(document, "touchend", r._onDrop), je(document, "touchcancel", r._onDrop), i && (i.effectAllowed = "move", s.setData && s.setData.call(r, i, ge)), ze(document, "drop", r), ke(ge, "transform", "translateZ(0)")), pi = !0, r._dragStartId = ba(r._dragStarted.bind(r, e, n)), ze(document, "selectstart", r), ns = !0, window.getSelection().removeAllRanges(), os && ke(document.body, "user-select", "none");
  },
  // Returns true - if no further action is needed (either inserted or another condition)
  _onDragOver: function(n) {
    var e = this.el, r = n.target, i, s, a, o = this.options, l = o.group, c = Pe.active, f = sa === l, g = o.sort, b = xt || c, w, C = this, m = !1;
    if (kl)
      return;
    function h(M, W) {
      zt(M, C, Fn({
        evt: n,
        isOwner: f,
        axis: w ? "vertical" : "horizontal",
        revert: a,
        dragRect: i,
        targetRect: s,
        canSort: g,
        fromSortable: b,
        target: r,
        completed: p,
        onMove: function(J, X) {
          return la(st, e, ge, i, J, gt(J), n, X);
        },
        changed: v
      }, W));
    }
    function u() {
      h("dragOverAnimationCapture"), C.captureAnimationState(), C !== b && b.captureAnimationState();
    }
    function p(M) {
      return h("dragOverCompleted", {
        insertion: M
      }), M && (f ? c._hideClone() : c._showClone(C), C !== b && (Qt(ge, xt ? xt.options.ghostClass : c.options.ghostClass, !1), Qt(ge, o.ghostClass, !0)), xt !== C && C !== Pe.active ? xt = C : C === Pe.active && xt && (xt = null), b === C && (C._ignoreWhileAnimating = r), C.animateAll(function() {
        h("dragOverAnimationComplete"), C._ignoreWhileAnimating = null;
      }), C !== b && (b.animateAll(), b._ignoreWhileAnimating = null)), (r === ge && !ge.animated || r === e && !r.animated) && (ui = null), !o.dragoverBubble && !n.rootEl && r !== document && (ge.parentNode[Ut]._isOutsideThisEl(n.target), !M && xr(n)), !o.dragoverBubble && n.stopPropagation && n.stopPropagation(), m = !0;
    }
    function v() {
      Zt = gn(ge), er = gn(ge, o.draggable), Mt({
        sortable: C,
        name: "change",
        toEl: e,
        newIndex: Zt,
        newDraggableIndex: er,
        originalEvent: n
      });
    }
    if (n.preventDefault !== void 0 && n.cancelable && n.preventDefault(), r = Dn(r, o.draggable, e, !0), h("dragOver"), Pe.eventCanceled)
      return m;
    if (ge.contains(n.target) || r.animated && r.animatingX && r.animatingY || C._ignoreWhileAnimating === r)
      return p(!1);
    if (Ra = !1, c && !o.disabled && (f ? g || (a = ct !== st) : xt === this || (this.lastPutMode = sa.checkPull(this, c, ge, n)) && l.checkPut(this, c, ge, n))) {
      if (w = this._getDirection(n, r) === "vertical", i = gt(ge), h("dragOverValid"), Pe.eventCanceled)
        return m;
      if (a)
        return ct = st, u(), this._hideClone(), h("revert"), Pe.eventCanceled || (Sr ? st.insertBefore(ge, Sr) : st.appendChild(ge)), p(!0);
      var S = xu(e, o.draggable);
      if (!S || $w(n, w, this) && !S.animated) {
        if (S === ge)
          return p(!1);
        if (S && e === n.target && (r = S), r && (s = gt(r)), la(st, e, ge, i, r, s, n, !!r) !== !1)
          return u(), S && S.nextSibling ? e.insertBefore(ge, S.nextSibling) : e.appendChild(ge), ct = e, v(), p(!0);
      } else if (S && Hw(n, w, this)) {
        var T = Pi(e, 0, o, !0);
        if (T === ge)
          return p(!1);
        if (r = T, s = gt(r), la(st, e, ge, i, r, s, n, !1) !== !1)
          return u(), e.insertBefore(ge, T), ct = e, v(), p(!0);
      } else if (r.parentNode === e) {
        s = gt(r);
        var A = 0, y, I = ge.parentNode !== e, $ = !Rw(ge.animated && ge.toRect || i, r.animated && r.toRect || s, w), V = w ? "top" : "left", z = _d(r, "top", "top") || _d(ge, "top", "top"), F = z ? z.scrollTop : void 0;
        ui !== r && (y = s[V], ds = !1, aa = !$ && o.invertSwap || I), A = Vw(n, r, s, w, $ ? 1 : o.swapThreshold, o.invertedSwapThreshold == null ? o.swapThreshold : o.invertedSwapThreshold, aa, ui === r);
        var P;
        if (A !== 0) {
          var k = gn(ge);
          do
            k -= A, P = ct.children[k];
          while (P && (ke(P, "display") === "none" || P === Me));
        }
        if (A === 0 || P === r)
          return p(!1);
        ui = r, cs = A;
        var R = r.nextElementSibling, L = !1;
        L = A === 1;
        var E = la(st, e, ge, i, r, s, n, L);
        if (E !== !1)
          return (E === 1 || E === -1) && (L = E === 1), kl = !0, setTimeout(Bw, 30), u(), L && !R ? e.appendChild(ge) : r.parentNode.insertBefore(ge, L ? R : r), z && Bh(z, 0, F - z.scrollTop), ct = ge.parentNode, y !== void 0 && !aa && (va = Math.abs(y - gt(r)[V])), v(), p(!0);
      }
      if (e.contains(ge))
        return p(!1);
    }
    return !1;
  },
  _ignoreWhileAnimating: null,
  _offMoveEvents: function() {
    je(document, "mousemove", this._onTouchMove), je(document, "touchmove", this._onTouchMove), je(document, "pointermove", this._onTouchMove), je(document, "dragover", xr), je(document, "mousemove", xr), je(document, "touchmove", xr);
  },
  _offUpEvents: function() {
    var n = this.el.ownerDocument;
    je(n, "mouseup", this._onDrop), je(n, "touchend", this._onDrop), je(n, "pointerup", this._onDrop), je(n, "pointercancel", this._onDrop), je(n, "touchcancel", this._onDrop), je(document, "selectstart", this);
  },
  _onDrop: function(n) {
    var e = this.el, r = this.options;
    if (Zt = gn(ge), er = gn(ge, r.draggable), zt("drop", this, {
      evt: n
    }), ct = ge && ge.parentNode, Zt = gn(ge), er = gn(ge, r.draggable), Pe.eventCanceled) {
      this._nulling();
      return;
    }
    pi = !1, aa = !1, ds = !1, clearInterval(this._loopId), clearTimeout(this._dragStartTimer), Pl(this.cloneId), Pl(this._dragStartId), this.nativeDraggable && (je(document, "drop", this), je(e, "dragstart", this._onDragStart)), this._offMoveEvents(), this._offUpEvents(), os && ke(document.body, "user-select", ""), ke(ge, "transform", ""), n && (ns && (n.cancelable && n.preventDefault(), !r.dropBubble && n.stopPropagation()), Me && Me.parentNode && Me.parentNode.removeChild(Me), (st === ct || xt && xt.lastPutMode !== "clone") && at && at.parentNode && at.parentNode.removeChild(at), ge && (this.nativeDraggable && je(ge, "dragend", this), il(ge), ge.style["will-change"] = "", ns && !pi && Qt(ge, xt ? xt.options.ghostClass : this.options.ghostClass, !1), Qt(ge, this.options.chosenClass, !1), Mt({
      sortable: this,
      name: "unchoose",
      toEl: ct,
      newIndex: null,
      newDraggableIndex: null,
      originalEvent: n
    }), st !== ct ? (Zt >= 0 && (Mt({
      rootEl: ct,
      name: "add",
      toEl: ct,
      fromEl: st,
      originalEvent: n
    }), Mt({
      sortable: this,
      name: "remove",
      toEl: ct,
      originalEvent: n
    }), Mt({
      rootEl: ct,
      name: "sort",
      toEl: ct,
      fromEl: st,
      originalEvent: n
    }), Mt({
      sortable: this,
      name: "sort",
      toEl: ct,
      originalEvent: n
    })), xt && xt.save()) : Zt !== mi && Zt >= 0 && (Mt({
      sortable: this,
      name: "update",
      toEl: ct,
      originalEvent: n
    }), Mt({
      sortable: this,
      name: "sort",
      toEl: ct,
      originalEvent: n
    })), Pe.active && ((Zt == null || Zt === -1) && (Zt = mi, er = us), Mt({
      sortable: this,
      name: "end",
      toEl: ct,
      originalEvent: n
    }), this.save()))), this._nulling();
  },
  _nulling: function() {
    zt("nulling", this), st = ge = ct = Me = Sr = at = ma = nr = Cr = Tn = ns = Zt = er = mi = us = ui = cs = xt = sa = Pe.dragged = Pe.ghost = Pe.clone = Pe.active = null;
    var n = this.el;
    Ma.forEach(function(e) {
      n.contains(e) && (e.checked = !0);
    }), Ma.length = tl = nl = 0;
  },
  handleEvent: function(n) {
    switch (n.type) {
      case "drop":
      case "dragend":
        this._onDrop(n);
        break;
      case "dragenter":
      case "dragover":
        ge && (this._onDragOver(n), Fw(n));
        break;
      case "selectstart":
        n.preventDefault();
        break;
    }
  },
  /**
   * Serializes the item into an array of string.
   * @returns {String[]}
   */
  toArray: function() {
    for (var n = [], e, r = this.el.children, i = 0, s = r.length, a = this.options; i < s; i++)
      e = r[i], Dn(e, a.draggable, this.el, !1) && n.push(e.getAttribute(a.dataIdAttr) || jw(e));
    return n;
  },
  /**
   * Sorts the elements according to the array.
   * @param  {String[]}  order  order of the items
   */
  sort: function(n, e) {
    var r = {}, i = this.el;
    this.toArray().forEach(function(s, a) {
      var o = i.children[a];
      Dn(o, this.options.draggable, i, !1) && (r[s] = o);
    }, this), e && this.captureAnimationState(), n.forEach(function(s) {
      r[s] && (i.removeChild(r[s]), i.appendChild(r[s]));
    }), e && this.animateAll();
  },
  /**
   * Save the current sorting
   */
  save: function() {
    var n = this.options.store;
    n && n.set && n.set(this);
  },
  /**
   * For each element in the set, get the first element that matches the selector by testing the element itself and traversing up through its ancestors in the DOM tree.
   * @param   {HTMLElement}  el
   * @param   {String}       [selector]  default: `options.draggable`
   * @returns {HTMLElement|null}
   */
  closest: function(n, e) {
    return Dn(n, e || this.options.draggable, this.el, !1);
  },
  /**
   * Set/get option
   * @param   {string} name
   * @param   {*}      [value]
   * @returns {*}
   */
  option: function(n, e) {
    var r = this.options;
    if (e === void 0)
      return r[n];
    var i = Is.modifyOption(this, n, e);
    typeof i < "u" ? r[n] = i : r[n] = e, n === "group" && jh(r);
  },
  /**
   * Destroy
   */
  destroy: function() {
    zt("destroy", this);
    var n = this.el;
    n[Ut] = null, je(n, "mousedown", this._onTapStart), je(n, "touchstart", this._onTapStart), je(n, "pointerdown", this._onTapStart), this.nativeDraggable && (je(n, "dragover", this), je(n, "dragenter", this)), Array.prototype.forEach.call(n.querySelectorAll("[draggable]"), function(e) {
      e.removeAttribute("draggable");
    }), this._onDrop(), this._disableDelayedDragEvents(), La.splice(La.indexOf(this.el), 1), this.el = n = null;
  },
  _hideClone: function() {
    if (!nr) {
      if (zt("hideClone", this), Pe.eventCanceled)
        return;
      ke(at, "display", "none"), this.options.removeCloneOnHide && at.parentNode && at.parentNode.removeChild(at), nr = !0;
    }
  },
  _showClone: function(n) {
    if (n.lastPutMode !== "clone") {
      this._hideClone();
      return;
    }
    if (nr) {
      if (zt("showClone", this), Pe.eventCanceled)
        return;
      ge.parentNode == st && !this.options.group.revertClone ? st.insertBefore(at, ge) : Sr ? st.insertBefore(at, Sr) : st.appendChild(at), this.options.group.revertClone && this.animate(ge, at), ke(at, "display", ""), nr = !1;
    }
  }
};
function Fw(t) {
  t.dataTransfer && (t.dataTransfer.dropEffect = "move"), t.cancelable && t.preventDefault();
}
function la(t, n, e, r, i, s, a, o) {
  var l, c = t[Ut], f = c.options.onMove, g;
  return window.CustomEvent && !Un && !Os ? l = new CustomEvent("move", {
    bubbles: !0,
    cancelable: !0
  }) : (l = document.createEvent("Event"), l.initEvent("move", !0, !0)), l.to = n, l.from = t, l.dragged = e, l.draggedRect = r, l.related = i || n, l.relatedRect = s || gt(n), l.willInsertAfter = o, l.originalEvent = a, t.dispatchEvent(l), f && (g = f.call(c, l, a)), g;
}
function il(t) {
  t.draggable = !1;
}
function Bw() {
  kl = !1;
}
function Hw(t, n, e) {
  var r = gt(Pi(e.el, 0, e.options, !0)), i = $h(e.el, e.options, Me), s = 10;
  return n ? t.clientX < i.left - s || t.clientY < r.top && t.clientX < r.right : t.clientY < i.top - s || t.clientY < r.bottom && t.clientX < r.left;
}
function $w(t, n, e) {
  var r = gt(xu(e.el, e.options.draggable)), i = $h(e.el, e.options, Me), s = 10;
  return n ? t.clientX > i.right + s || t.clientY > r.bottom && t.clientX > r.left : t.clientY > i.bottom + s || t.clientX > r.right && t.clientY > r.top;
}
function Vw(t, n, e, r, i, s, a, o) {
  var l = r ? t.clientY : t.clientX, c = r ? e.height : e.width, f = r ? e.top : e.left, g = r ? e.bottom : e.right, b = !1;
  if (!a) {
    if (o && va < c * i) {
      if (!ds && (cs === 1 ? l > f + c * s / 2 : l < g - c * s / 2) && (ds = !0), ds)
        b = !0;
      else if (cs === 1 ? l < f + va : l > g - va)
        return -cs;
    } else if (l > f + c * (1 - i) / 2 && l < g - c * (1 - i) / 2)
      return Ww(n);
  }
  return b = b || a, b && (l < f + c * s / 2 || l > g - c * s / 2) ? l > f + c / 2 ? 1 : -1 : 0;
}
function Ww(t) {
  return gn(ge) < gn(t) ? 1 : -1;
}
function jw(t) {
  for (var n = t.tagName + t.className + t.src + t.href + t.textContent, e = n.length, r = 0; e--; )
    r += n.charCodeAt(e);
  return r.toString(36);
}
function zw(t) {
  Ma.length = 0;
  for (var n = t.getElementsByTagName("input"), e = n.length; e--; ) {
    var r = n[e];
    r.checked && Ma.push(r);
  }
}
function ba(t) {
  return setTimeout(t, 0);
}
function Pl(t) {
  return clearTimeout(t);
}
io && ze(document, "touchmove", function(t) {
  (Pe.active || pi) && t.cancelable && t.preventDefault();
});
Pe.utils = {
  on: ze,
  off: je,
  css: ke,
  find: Mh,
  is: function(n, e) {
    return !!Dn(n, e, n, !1);
  },
  extend: Dw,
  throttle: Fh,
  closest: Dn,
  toggleClass: Qt,
  clone: Hh,
  index: gn,
  nextTick: ba,
  cancelNextTick: Pl,
  detectDirection: Wh,
  getChild: Pi,
  expando: Ut
};
Pe.get = function(t) {
  return t[Ut];
};
Pe.mount = function() {
  for (var t = arguments.length, n = new Array(t), e = 0; e < t; e++)
    n[e] = arguments[e];
  n[0].constructor === Array && (n = n[0]), n.forEach(function(r) {
    if (!r.prototype || !r.prototype.constructor)
      throw "Sortable: Mounted plugin must be a constructor function, not ".concat({}.toString.call(r));
    r.utils && (Pe.utils = Fn(Fn({}, Pe.utils), r.utils)), Is.mount(r);
  });
};
Pe.create = function(t, n) {
  return new Pe(t, n);
};
Pe.version = Sw;
var pt = [], rs, Ol, Il = !1, sl, al, Fa, is;
function Yw() {
  function t() {
    this.defaults = {
      scroll: !0,
      forceAutoScrollFallback: !1,
      scrollSensitivity: 30,
      scrollSpeed: 10,
      bubbleScroll: !0
    };
    for (var n in this)
      n.charAt(0) === "_" && typeof this[n] == "function" && (this[n] = this[n].bind(this));
  }
  return t.prototype = {
    dragStarted: function(e) {
      var r = e.originalEvent;
      this.sortable.nativeDraggable ? ze(document, "dragover", this._handleAutoScroll) : this.options.supportPointer ? ze(document, "pointermove", this._handleFallbackAutoScroll) : r.touches ? ze(document, "touchmove", this._handleFallbackAutoScroll) : ze(document, "mousemove", this._handleFallbackAutoScroll);
    },
    dragOverCompleted: function(e) {
      var r = e.originalEvent;
      !this.options.dragOverBubble && !r.rootEl && this._handleAutoScroll(r);
    },
    drop: function() {
      this.sortable.nativeDraggable ? je(document, "dragover", this._handleAutoScroll) : (je(document, "pointermove", this._handleFallbackAutoScroll), je(document, "touchmove", this._handleFallbackAutoScroll), je(document, "mousemove", this._handleFallbackAutoScroll)), Dd(), ya(), Ew();
    },
    nulling: function() {
      Fa = Ol = rs = Il = is = sl = al = null, pt.length = 0;
    },
    _handleFallbackAutoScroll: function(e) {
      this._handleAutoScroll(e, !0);
    },
    _handleAutoScroll: function(e, r) {
      var i = this, s = (e.touches ? e.touches[0] : e).clientX, a = (e.touches ? e.touches[0] : e).clientY, o = document.elementFromPoint(s, a);
      if (Fa = e, r || this.options.forceAutoScrollFallback || Os || Un || os) {
        ol(e, this.options, o, r);
        var l = ar(o, !0);
        Il && (!is || s !== sl || a !== al) && (is && Dd(), is = setInterval(function() {
          var c = ar(document.elementFromPoint(s, a), !0);
          c !== l && (l = c, ya()), ol(e, i.options, c, r);
        }, 10), sl = s, al = a);
      } else {
        if (!this.options.bubbleScroll || ar(o, !0) === Nn()) {
          ya();
          return;
        }
        ol(e, this.options, ar(o, !1), !1);
      }
    }
  }, qn(t, {
    pluginName: "scroll",
    initializeByDefault: !0
  });
}
function ya() {
  pt.forEach(function(t) {
    clearInterval(t.pid);
  }), pt = [];
}
function Dd() {
  clearInterval(is);
}
var ol = Fh(function(t, n, e, r) {
  if (n.scroll) {
    var i = (t.touches ? t.touches[0] : t).clientX, s = (t.touches ? t.touches[0] : t).clientY, a = n.scrollSensitivity, o = n.scrollSpeed, l = Nn(), c = !1, f;
    Ol !== e && (Ol = e, ya(), rs = n.scroll, f = n.scrollFn, rs === !0 && (rs = ar(e, !0)));
    var g = 0, b = rs;
    do {
      var w = b, C = gt(w), m = C.top, h = C.bottom, u = C.left, p = C.right, v = C.width, S = C.height, T = void 0, A = void 0, y = w.scrollWidth, I = w.scrollHeight, $ = ke(w), V = w.scrollLeft, z = w.scrollTop;
      w === l ? (T = v < y && ($.overflowX === "auto" || $.overflowX === "scroll" || $.overflowX === "visible"), A = S < I && ($.overflowY === "auto" || $.overflowY === "scroll" || $.overflowY === "visible")) : (T = v < y && ($.overflowX === "auto" || $.overflowX === "scroll"), A = S < I && ($.overflowY === "auto" || $.overflowY === "scroll"));
      var F = T && (Math.abs(p - i) <= a && V + v < y) - (Math.abs(u - i) <= a && !!V), P = A && (Math.abs(h - s) <= a && z + S < I) - (Math.abs(m - s) <= a && !!z);
      if (!pt[g])
        for (var k = 0; k <= g; k++)
          pt[k] || (pt[k] = {});
      (pt[g].vx != F || pt[g].vy != P || pt[g].el !== w) && (pt[g].el = w, pt[g].vx = F, pt[g].vy = P, clearInterval(pt[g].pid), (F != 0 || P != 0) && (c = !0, pt[g].pid = setInterval((function() {
        r && this.layer === 0 && Pe.active._onTouchMove(Fa);
        var R = pt[this.layer].vy ? pt[this.layer].vy * o : 0, L = pt[this.layer].vx ? pt[this.layer].vx * o : 0;
        typeof f == "function" && f.call(Pe.dragged.parentNode[Ut], L, R, t, Fa, pt[this.layer].el) !== "continue" || Bh(pt[this.layer].el, L, R);
      }).bind({
        layer: g
      }), 24))), g++;
    } while (n.bubbleScroll && b !== l && (b = ar(b, !1)));
    Il = c;
  }
}, 30), qh = function(n) {
  var e = n.originalEvent, r = n.putSortable, i = n.dragEl, s = n.activeSortable, a = n.dispatchSortableEvent, o = n.hideGhostForTarget, l = n.unhideGhostForTarget;
  if (e) {
    var c = r || s;
    o();
    var f = e.changedTouches && e.changedTouches.length ? e.changedTouches[0] : e, g = document.elementFromPoint(f.clientX, f.clientY);
    l(), c && !c.el.contains(g) && (a("spill"), this.onSpill({
      dragEl: i,
      putSortable: r
    }));
  }
};
function Su() {
}
Su.prototype = {
  startIndex: null,
  dragStart: function(n) {
    var e = n.oldDraggableIndex;
    this.startIndex = e;
  },
  onSpill: function(n) {
    var e = n.dragEl, r = n.putSortable;
    this.sortable.captureAnimationState(), r && r.captureAnimationState();
    var i = Pi(this.sortable.el, this.startIndex, this.options);
    i ? this.sortable.el.insertBefore(e, i) : this.sortable.el.appendChild(e), this.sortable.animateAll(), r && r.animateAll();
  },
  drop: qh
};
qn(Su, {
  pluginName: "revertOnSpill"
});
function Tu() {
}
Tu.prototype = {
  onSpill: function(n) {
    var e = n.dragEl, r = n.putSortable, i = r || this.sortable;
    i.captureAnimationState(), e.parentNode && e.parentNode.removeChild(e), i.animateAll();
  },
  drop: qh
};
qn(Tu, {
  pluginName: "removeOnSpill"
});
Pe.mount(new Yw());
Pe.mount(Tu, Su);
var ll = [
  "onChange",
  "onClose",
  "onDayCreate",
  "onDestroy",
  "onKeyDown",
  "onMonthChange",
  "onOpen",
  "onParseConfig",
  "onReady",
  "onValueUpdate",
  "onYearChange",
  "onPreCalendarPosition"
], wi = {
  _disable: [],
  allowInput: !1,
  allowInvalidPreload: !1,
  altFormat: "F j, Y",
  altInput: !1,
  altInputClass: "form-control input",
  animate: typeof window == "object" && window.navigator.userAgent.indexOf("MSIE") === -1,
  ariaDateFormat: "F j, Y",
  autoFillDefaultTime: !0,
  clickOpens: !0,
  closeOnSelect: !0,
  conjunction: ", ",
  dateFormat: "Y-m-d",
  defaultHour: 12,
  defaultMinute: 0,
  defaultSeconds: 0,
  disable: [],
  disableMobile: !1,
  enableSeconds: !1,
  enableTime: !1,
  errorHandler: function(t) {
    return typeof console < "u" && console.warn(t);
  },
  getWeek: function(t) {
    var n = new Date(t.getTime());
    n.setHours(0, 0, 0, 0), n.setDate(n.getDate() + 3 - (n.getDay() + 6) % 7);
    var e = new Date(n.getFullYear(), 0, 4);
    return 1 + Math.round(((n.getTime() - e.getTime()) / 864e5 - 3 + (e.getDay() + 6) % 7) / 7);
  },
  hourIncrement: 1,
  ignoredFocusElements: [],
  inline: !1,
  locale: "default",
  minuteIncrement: 5,
  mode: "single",
  monthSelectorType: "dropdown",
  nextArrow: "<svg version='1.1' xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink' viewBox='0 0 17 17'><g></g><path d='M13.207 8.472l-7.854 7.854-0.707-0.707 7.146-7.146-7.146-7.148 0.707-0.707 7.854 7.854z' /></svg>",
  noCalendar: !1,
  now: /* @__PURE__ */ new Date(),
  onChange: [],
  onClose: [],
  onDayCreate: [],
  onDestroy: [],
  onKeyDown: [],
  onMonthChange: [],
  onOpen: [],
  onParseConfig: [],
  onReady: [],
  onValueUpdate: [],
  onYearChange: [],
  onPreCalendarPosition: [],
  plugins: [],
  position: "auto",
  positionElement: void 0,
  prevArrow: "<svg version='1.1' xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink' viewBox='0 0 17 17'><g></g><path d='M5.207 8.471l7.146 7.147-0.707 0.707-7.853-7.854 7.854-7.853 0.707 0.707-7.147 7.146z' /></svg>",
  shorthandCurrentMonth: !1,
  showMonths: 1,
  static: !1,
  time_24hr: !1,
  weekNumbers: !1,
  wrap: !1
}, vs = {
  weekdays: {
    shorthand: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
    longhand: [
      "Sunday",
      "Monday",
      "Tuesday",
      "Wednesday",
      "Thursday",
      "Friday",
      "Saturday"
    ]
  },
  months: {
    shorthand: [
      "Jan",
      "Feb",
      "Mar",
      "Apr",
      "May",
      "Jun",
      "Jul",
      "Aug",
      "Sep",
      "Oct",
      "Nov",
      "Dec"
    ],
    longhand: [
      "January",
      "February",
      "March",
      "April",
      "May",
      "June",
      "July",
      "August",
      "September",
      "October",
      "November",
      "December"
    ]
  },
  daysInMonth: [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31],
  firstDayOfWeek: 0,
  ordinal: function(t) {
    var n = t % 100;
    if (n > 3 && n < 21)
      return "th";
    switch (n % 10) {
      case 1:
        return "st";
      case 2:
        return "nd";
      case 3:
        return "rd";
      default:
        return "th";
    }
  },
  rangeSeparator: " to ",
  weekAbbreviation: "Wk",
  scrollTitle: "Scroll to increment",
  toggleTitle: "Click to toggle",
  amPM: ["AM", "PM"],
  yearAriaLabel: "Year",
  monthAriaLabel: "Month",
  hourAriaLabel: "Hour",
  minuteAriaLabel: "Minute",
  time_24hr: !1
}, qt = function(t, n) {
  return n === void 0 && (n = 2), ("000" + t).slice(n * -1);
}, pn = function(t) {
  return t === !0 ? 1 : 0;
};
function Ed(t, n) {
  var e;
  return function() {
    var r = this, i = arguments;
    clearTimeout(e), e = setTimeout(function() {
      return t.apply(r, i);
    }, n);
  };
}
var ul = function(t) {
  return t instanceof Array ? t : [t];
};
function Rt(t, n, e) {
  if (e === !0)
    return t.classList.add(n);
  t.classList.remove(n);
}
function Ue(t, n, e) {
  var r = window.document.createElement(t);
  return n = n || "", e = e || "", r.className = n, e !== void 0 && (r.textContent = e), r;
}
function ua(t) {
  for (; t.firstChild; )
    t.removeChild(t.firstChild);
}
function Gh(t, n) {
  if (n(t))
    return t;
  if (t.parentNode)
    return Gh(t.parentNode, n);
}
function ca(t, n) {
  var e = Ue("div", "numInputWrapper"), r = Ue("input", "numInput " + t), i = Ue("span", "arrowUp"), s = Ue("span", "arrowDown");
  if (navigator.userAgent.indexOf("MSIE 9.0") === -1 ? r.type = "number" : (r.type = "text", r.pattern = "\\d*"), n !== void 0)
    for (var a in n)
      r.setAttribute(a, n[a]);
  return e.appendChild(r), e.appendChild(i), e.appendChild(s), e;
}
function Xt(t) {
  try {
    if (typeof t.composedPath == "function") {
      var n = t.composedPath();
      return n[0];
    }
    return t.target;
  } catch {
    return t.target;
  }
}
var cl = function() {
}, Ba = function(t, n, e) {
  return e.months[n ? "shorthand" : "longhand"][t];
}, qw = {
  D: cl,
  F: function(t, n, e) {
    t.setMonth(e.months.longhand.indexOf(n));
  },
  G: function(t, n) {
    t.setHours((t.getHours() >= 12 ? 12 : 0) + parseFloat(n));
  },
  H: function(t, n) {
    t.setHours(parseFloat(n));
  },
  J: function(t, n) {
    t.setDate(parseFloat(n));
  },
  K: function(t, n, e) {
    t.setHours(t.getHours() % 12 + 12 * pn(new RegExp(e.amPM[1], "i").test(n)));
  },
  M: function(t, n, e) {
    t.setMonth(e.months.shorthand.indexOf(n));
  },
  S: function(t, n) {
    t.setSeconds(parseFloat(n));
  },
  U: function(t, n) {
    return new Date(parseFloat(n) * 1e3);
  },
  W: function(t, n, e) {
    var r = parseInt(n), i = new Date(t.getFullYear(), 0, 2 + (r - 1) * 7, 0, 0, 0, 0);
    return i.setDate(i.getDate() - i.getDay() + e.firstDayOfWeek), i;
  },
  Y: function(t, n) {
    t.setFullYear(parseFloat(n));
  },
  Z: function(t, n) {
    return new Date(n);
  },
  d: function(t, n) {
    t.setDate(parseFloat(n));
  },
  h: function(t, n) {
    t.setHours((t.getHours() >= 12 ? 12 : 0) + parseFloat(n));
  },
  i: function(t, n) {
    t.setMinutes(parseFloat(n));
  },
  j: function(t, n) {
    t.setDate(parseFloat(n));
  },
  l: cl,
  m: function(t, n) {
    t.setMonth(parseFloat(n) - 1);
  },
  n: function(t, n) {
    t.setMonth(parseFloat(n) - 1);
  },
  s: function(t, n) {
    t.setSeconds(parseFloat(n));
  },
  u: function(t, n) {
    return new Date(parseFloat(n));
  },
  w: cl,
  y: function(t, n) {
    t.setFullYear(2e3 + parseFloat(n));
  }
}, Tr = {
  D: "",
  F: "",
  G: "(\\d\\d|\\d)",
  H: "(\\d\\d|\\d)",
  J: "(\\d\\d|\\d)\\w+",
  K: "",
  M: "",
  S: "(\\d\\d|\\d)",
  U: "(.+)",
  W: "(\\d\\d|\\d)",
  Y: "(\\d{4})",
  Z: "(.+)",
  d: "(\\d\\d|\\d)",
  h: "(\\d\\d|\\d)",
  i: "(\\d\\d|\\d)",
  j: "(\\d\\d|\\d)",
  l: "",
  m: "(\\d\\d|\\d)",
  n: "(\\d\\d|\\d)",
  s: "(\\d\\d|\\d)",
  u: "(.+)",
  w: "(\\d\\d|\\d)",
  y: "(\\d{2})"
}, fs = {
  Z: function(t) {
    return t.toISOString();
  },
  D: function(t, n, e) {
    return n.weekdays.shorthand[fs.w(t, n, e)];
  },
  F: function(t, n, e) {
    return Ba(fs.n(t, n, e) - 1, !1, n);
  },
  G: function(t, n, e) {
    return qt(fs.h(t, n, e));
  },
  H: function(t) {
    return qt(t.getHours());
  },
  J: function(t, n) {
    return n.ordinal !== void 0 ? t.getDate() + n.ordinal(t.getDate()) : t.getDate();
  },
  K: function(t, n) {
    return n.amPM[pn(t.getHours() > 11)];
  },
  M: function(t, n) {
    return Ba(t.getMonth(), !0, n);
  },
  S: function(t) {
    return qt(t.getSeconds());
  },
  U: function(t) {
    return t.getTime() / 1e3;
  },
  W: function(t, n, e) {
    return e.getWeek(t);
  },
  Y: function(t) {
    return qt(t.getFullYear(), 4);
  },
  d: function(t) {
    return qt(t.getDate());
  },
  h: function(t) {
    return t.getHours() % 12 ? t.getHours() % 12 : 12;
  },
  i: function(t) {
    return qt(t.getMinutes());
  },
  j: function(t) {
    return t.getDate();
  },
  l: function(t, n) {
    return n.weekdays.longhand[t.getDay()];
  },
  m: function(t) {
    return qt(t.getMonth() + 1);
  },
  n: function(t) {
    return t.getMonth() + 1;
  },
  s: function(t) {
    return t.getSeconds();
  },
  u: function(t) {
    return t.getTime();
  },
  w: function(t) {
    return t.getDay();
  },
  y: function(t) {
    return String(t.getFullYear()).substring(2);
  }
}, Uh = function(t) {
  var n = t.config, e = n === void 0 ? wi : n, r = t.l10n, i = r === void 0 ? vs : r, s = t.isMobile, a = s === void 0 ? !1 : s;
  return function(o, l, c) {
    var f = c || i;
    return e.formatDate !== void 0 && !a ? e.formatDate(o, l, f) : l.split("").map(function(g, b, w) {
      return fs[g] && w[b - 1] !== "\\" ? fs[g](o, f, e) : g !== "\\" ? g : "";
    }).join("");
  };
}, Nl = function(t) {
  var n = t.config, e = n === void 0 ? wi : n, r = t.l10n, i = r === void 0 ? vs : r;
  return function(s, a, o, l) {
    if (!(s !== 0 && !s)) {
      var c = l || i, f, g = s;
      if (s instanceof Date)
        f = new Date(s.getTime());
      else if (typeof s != "string" && s.toFixed !== void 0)
        f = new Date(s);
      else if (typeof s == "string") {
        var b = a || (e || wi).dateFormat, w = String(s).trim();
        if (w === "today")
          f = /* @__PURE__ */ new Date(), o = !0;
        else if (e && e.parseDate)
          f = e.parseDate(s, b);
        else if (/Z$/.test(w) || /GMT$/.test(w))
          f = new Date(s);
        else {
          for (var C = void 0, m = [], h = 0, u = 0, p = ""; h < b.length; h++) {
            var v = b[h], S = v === "\\", T = b[h - 1] === "\\" || S;
            if (Tr[v] && !T) {
              p += Tr[v];
              var A = new RegExp(p).exec(s);
              A && (C = !0) && m[v !== "Y" ? "push" : "unshift"]({
                fn: qw[v],
                val: A[++u]
              });
            } else
              S || (p += ".");
          }
          f = !e || !e.noCalendar ? new Date((/* @__PURE__ */ new Date()).getFullYear(), 0, 1, 0, 0, 0, 0) : new Date((/* @__PURE__ */ new Date()).setHours(0, 0, 0, 0)), m.forEach(function(y) {
            var I = y.fn, $ = y.val;
            return f = I(f, $, c) || f;
          }), f = C ? f : void 0;
        }
      }
      if (!(f instanceof Date && !isNaN(f.getTime()))) {
        e.errorHandler(new Error("Invalid date provided: " + g));
        return;
      }
      return o === !0 && f.setHours(0, 0, 0, 0), f;
    }
  };
};
function Jt(t, n, e) {
  return e === void 0 && (e = !0), e !== !1 ? new Date(t.getTime()).setHours(0, 0, 0, 0) - new Date(n.getTime()).setHours(0, 0, 0, 0) : t.getTime() - n.getTime();
}
var Gw = function(t, n, e) {
  return t > Math.min(n, e) && t < Math.max(n, e);
}, dl = function(t, n, e) {
  return t * 3600 + n * 60 + e;
}, Uw = function(t) {
  var n = Math.floor(t / 3600), e = (t - n * 3600) / 60;
  return [n, e, t - n * 3600 - e * 60];
}, Kw = {
  DAY: 864e5
};
function fl(t) {
  var n = t.defaultHour, e = t.defaultMinute, r = t.defaultSeconds;
  if (t.minDate !== void 0) {
    var i = t.minDate.getHours(), s = t.minDate.getMinutes(), a = t.minDate.getSeconds();
    n < i && (n = i), n === i && e < s && (e = s), n === i && e === s && r < a && (r = t.minDate.getSeconds());
  }
  if (t.maxDate !== void 0) {
    var o = t.maxDate.getHours(), l = t.maxDate.getMinutes();
    n = Math.min(n, o), n === o && (e = Math.min(l, e)), n === o && e === l && (r = t.maxDate.getSeconds());
  }
  return { hours: n, minutes: e, seconds: r };
}
typeof Object.assign != "function" && (Object.assign = function(t) {
  for (var n = [], e = 1; e < arguments.length; e++)
    n[e - 1] = arguments[e];
  if (!t)
    throw TypeError("Cannot convert undefined or null to object");
  for (var r = function(o) {
    o && Object.keys(o).forEach(function(l) {
      return t[l] = o[l];
    });
  }, i = 0, s = n; i < s.length; i++) {
    var a = s[i];
    r(a);
  }
  return t;
});
var At = function() {
  return At = Object.assign || function(t) {
    for (var n, e = 1, r = arguments.length; e < r; e++) {
      n = arguments[e];
      for (var i in n)
        Object.prototype.hasOwnProperty.call(n, i) && (t[i] = n[i]);
    }
    return t;
  }, At.apply(this, arguments);
}, Ad = function() {
  for (var t = 0, n = 0, e = arguments.length; n < e; n++)
    t += arguments[n].length;
  for (var r = Array(t), i = 0, n = 0; n < e; n++)
    for (var s = arguments[n], a = 0, o = s.length; a < o; a++, i++)
      r[i] = s[a];
  return r;
}, Xw = 300;
function Qw(t, n) {
  var e = {
    config: At(At({}, wi), vt.defaultConfig),
    l10n: vs
  };
  e.parseDate = Nl({ config: e.config, l10n: e.l10n }), e._handlers = [], e.pluginElements = [], e.loadedPlugins = [], e._bind = m, e._setHoursFromDate = b, e._positionCalendar = wt, e.changeMonth = J, e.changeYear = ce, e.clear = X, e.close = Q, e.onMouseOver = Ie, e._createElement = Ue, e.createDay = A, e.destroy = ee, e.isEnabled = he, e.jumpToDate = p, e.updateValue = ft, e.open = et, e.redraw = St, e.set = mr, e.setDate = Ht, e.toggle = ao;
  function r() {
    e.utils = {
      getDaysInMonth: function(j, G) {
        return j === void 0 && (j = e.currentMonth), G === void 0 && (G = e.currentYear), j === 1 && (G % 4 === 0 && G % 100 !== 0 || G % 400 === 0) ? 29 : e.l10n.daysInMonth[j];
      }
    };
  }
  function i() {
    e.element = e.input = t, e.isOpen = !1, Ke(), yt(), Ls(), Rs(), r(), e.isMobile || T(), u(), (e.selectedDates.length || e.config.noCalendar) && (e.config.enableTime && b(e.config.noCalendar ? e.latestSelectedDateObj : void 0), ft(!1)), o();
    var j = /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
    !e.isMobile && j && wt(), tt("onReady");
  }
  function s() {
    var j;
    return ((j = e.calendarContainer) === null || j === void 0 ? void 0 : j.getRootNode()).activeElement || document.activeElement;
  }
  function a(j) {
    return j.bind(e);
  }
  function o() {
    var j = e.config;
    j.weekNumbers === !1 && j.showMonths === 1 || j.noCalendar !== !0 && window.requestAnimationFrame(function() {
      if (e.calendarContainer !== void 0 && (e.calendarContainer.style.visibility = "hidden", e.calendarContainer.style.display = "block"), e.daysContainer !== void 0) {
        var G = (e.days.offsetWidth + 1) * j.showMonths;
        e.daysContainer.style.width = G + "px", e.calendarContainer.style.width = G + (e.weekWrapper !== void 0 ? e.weekWrapper.offsetWidth : 0) + "px", e.calendarContainer.style.removeProperty("visibility"), e.calendarContainer.style.removeProperty("display");
      }
    });
  }
  function l(j) {
    if (e.selectedDates.length === 0) {
      var G = e.config.minDate === void 0 || Jt(/* @__PURE__ */ new Date(), e.config.minDate) >= 0 ? /* @__PURE__ */ new Date() : new Date(e.config.minDate.getTime()), te = fl(e.config);
      G.setHours(te.hours, te.minutes, te.seconds, G.getMilliseconds()), e.selectedDates = [G], e.latestSelectedDateObj = G;
    }
    j !== void 0 && j.type !== "blur" && lo(j);
    var ae = e._input.value;
    g(), ft(), e._input.value !== ae && e._debouncedChange();
  }
  function c(j, G) {
    return j % 12 + 12 * pn(G === e.l10n.amPM[1]);
  }
  function f(j) {
    switch (j % 24) {
      case 0:
      case 12:
        return 12;
      default:
        return j % 12;
    }
  }
  function g() {
    if (!(e.hourElement === void 0 || e.minuteElement === void 0)) {
      var j = (parseInt(e.hourElement.value.slice(-2), 10) || 0) % 24, G = (parseInt(e.minuteElement.value, 10) || 0) % 60, te = e.secondElement !== void 0 ? (parseInt(e.secondElement.value, 10) || 0) % 60 : 0;
      e.amPM !== void 0 && (j = c(j, e.amPM.textContent));
      var ae = e.config.minTime !== void 0 || e.config.minDate && e.minDateHasTime && e.latestSelectedDateObj && Jt(e.latestSelectedDateObj, e.config.minDate, !0) === 0, pe = e.config.maxTime !== void 0 || e.config.maxDate && e.maxDateHasTime && e.latestSelectedDateObj && Jt(e.latestSelectedDateObj, e.config.maxDate, !0) === 0;
      if (e.config.maxTime !== void 0 && e.config.minTime !== void 0 && e.config.minTime > e.config.maxTime) {
        var me = dl(e.config.minTime.getHours(), e.config.minTime.getMinutes(), e.config.minTime.getSeconds()), Re = dl(e.config.maxTime.getHours(), e.config.maxTime.getMinutes(), e.config.maxTime.getSeconds()), ye = dl(j, G, te);
        if (ye > Re && ye < me) {
          var Fe = Uw(me);
          j = Fe[0], G = Fe[1], te = Fe[2];
        }
      } else {
        if (pe) {
          var be = e.config.maxTime !== void 0 ? e.config.maxTime : e.config.maxDate;
          j = Math.min(j, be.getHours()), j === be.getHours() && (G = Math.min(G, be.getMinutes())), G === be.getMinutes() && (te = Math.min(te, be.getSeconds()));
        }
        if (ae) {
          var De = e.config.minTime !== void 0 ? e.config.minTime : e.config.minDate;
          j = Math.max(j, De.getHours()), j === De.getHours() && G < De.getMinutes() && (G = De.getMinutes()), G === De.getMinutes() && (te = Math.max(te, De.getSeconds()));
        }
      }
      w(j, G, te);
    }
  }
  function b(j) {
    var G = j || e.latestSelectedDateObj;
    G && G instanceof Date && w(G.getHours(), G.getMinutes(), G.getSeconds());
  }
  function w(j, G, te) {
    e.latestSelectedDateObj !== void 0 && e.latestSelectedDateObj.setHours(j % 24, G, te || 0, 0), !(!e.hourElement || !e.minuteElement || e.isMobile) && (e.hourElement.value = qt(e.config.time_24hr ? j : (12 + j) % 12 + 12 * pn(j % 12 === 0)), e.minuteElement.value = qt(G), e.amPM !== void 0 && (e.amPM.textContent = e.l10n.amPM[pn(j >= 12)]), e.secondElement !== void 0 && (e.secondElement.value = qt(te)));
  }
  function C(j) {
    var G = Xt(j), te = parseInt(G.value) + (j.delta || 0);
    (te / 1e3 > 1 || j.key === "Enter" && !/[^\d]/.test(te.toString())) && ce(te);
  }
  function m(j, G, te, ae) {
    if (G instanceof Array)
      return G.forEach(function(pe) {
        return m(j, pe, te, ae);
      });
    if (j instanceof Array)
      return j.forEach(function(pe) {
        return m(pe, G, te, ae);
      });
    j.addEventListener(G, te, ae), e._handlers.push({
      remove: function() {
        return j.removeEventListener(G, te, ae);
      }
    });
  }
  function h() {
    tt("onChange");
  }
  function u() {
    if (e.config.wrap && ["open", "close", "toggle", "clear"].forEach(function(te) {
      Array.prototype.forEach.call(e.element.querySelectorAll("[data-" + te + "]"), function(ae) {
        return m(ae, "click", e[te]);
      });
    }), e.isMobile) {
      so();
      return;
    }
    var j = Ed(Ze, 50);
    if (e._debouncedChange = Ed(h, Xw), e.daysContainer && !/iPhone|iPad|iPod/i.test(navigator.userAgent) && m(e.daysContainer, "mouseover", function(te) {
      e.config.mode === "range" && Ie(Xt(te));
    }), m(e._input, "keydown", Ve), e.calendarContainer !== void 0 && m(e.calendarContainer, "keydown", Ve), !e.config.inline && !e.config.static && m(window, "resize", j), window.ontouchstart !== void 0 ? m(window.document, "touchstart", oe) : m(window.document, "mousedown", oe), m(window.document, "focus", oe, { capture: !0 }), e.config.clickOpens === !0 && (m(e._input, "focus", e.open), m(e._input, "click", e.open)), e.daysContainer !== void 0 && (m(e.monthNav, "click", cn), m(e.monthNav, ["keyup", "increment"], C), m(e.daysContainer, "click", Ot)), e.timeContainer !== void 0 && e.minuteElement !== void 0 && e.hourElement !== void 0) {
      var G = function(te) {
        return Xt(te).select();
      };
      m(e.timeContainer, ["increment"], l), m(e.timeContainer, "blur", l, { capture: !0 }), m(e.timeContainer, "click", v), m([e.hourElement, e.minuteElement], ["focus", "click"], G), e.secondElement !== void 0 && m(e.secondElement, "focus", function() {
        return e.secondElement && e.secondElement.select();
      }), e.amPM !== void 0 && m(e.amPM, "click", function(te) {
        l(te);
      });
    }
    e.config.allowInput && m(e._input, "blur", Oe);
  }
  function p(j, G) {
    var te = j !== void 0 ? e.parseDate(j) : e.latestSelectedDateObj || (e.config.minDate && e.config.minDate > e.now ? e.config.minDate : e.config.maxDate && e.config.maxDate < e.now ? e.config.maxDate : e.now), ae = e.currentYear, pe = e.currentMonth;
    try {
      te !== void 0 && (e.currentYear = te.getFullYear(), e.currentMonth = te.getMonth());
    } catch (me) {
      me.message = "Invalid date supplied: " + te, e.config.errorHandler(me);
    }
    G && e.currentYear !== ae && (tt("onYearChange"), P()), G && (e.currentYear !== ae || e.currentMonth !== pe) && tt("onMonthChange"), e.redraw();
  }
  function v(j) {
    var G = Xt(j);
    ~G.className.indexOf("arrow") && S(j, G.classList.contains("arrowUp") ? 1 : -1);
  }
  function S(j, G, te) {
    var ae = j && Xt(j), pe = te || ae && ae.parentNode && ae.parentNode.firstChild, me = $i("increment");
    me.delta = G, pe && pe.dispatchEvent(me);
  }
  function T() {
    var j = window.document.createDocumentFragment();
    if (e.calendarContainer = Ue("div", "flatpickr-calendar"), e.calendarContainer.tabIndex = -1, !e.config.noCalendar) {
      if (j.appendChild(L()), e.innerContainer = Ue("div", "flatpickr-innerContainer"), e.config.weekNumbers) {
        var G = Y(), te = G.weekWrapper, ae = G.weekNumbers;
        e.innerContainer.appendChild(te), e.weekNumbers = ae, e.weekWrapper = te;
      }
      e.rContainer = Ue("div", "flatpickr-rContainer"), e.rContainer.appendChild(M()), e.daysContainer || (e.daysContainer = Ue("div", "flatpickr-days"), e.daysContainer.tabIndex = -1), F(), e.rContainer.appendChild(e.daysContainer), e.innerContainer.appendChild(e.rContainer), j.appendChild(e.innerContainer);
    }
    e.config.enableTime && j.appendChild(E()), Rt(e.calendarContainer, "rangeMode", e.config.mode === "range"), Rt(e.calendarContainer, "animate", e.config.animate === !0), Rt(e.calendarContainer, "multiMonth", e.config.showMonths > 1), e.calendarContainer.appendChild(j);
    var pe = e.config.appendTo !== void 0 && e.config.appendTo.nodeType !== void 0;
    if ((e.config.inline || e.config.static) && (e.calendarContainer.classList.add(e.config.inline ? "inline" : "static"), e.config.inline && (!pe && e.element.parentNode ? e.element.parentNode.insertBefore(e.calendarContainer, e._input.nextSibling) : e.config.appendTo !== void 0 && e.config.appendTo.appendChild(e.calendarContainer)), e.config.static)) {
      var me = Ue("div", "flatpickr-wrapper");
      e.element.parentNode && e.element.parentNode.insertBefore(me, e.element), me.appendChild(e.element), e.altInput && me.appendChild(e.altInput), me.appendChild(e.calendarContainer);
    }
    !e.config.static && !e.config.inline && (e.config.appendTo !== void 0 ? e.config.appendTo : window.document.body).appendChild(e.calendarContainer);
  }
  function A(j, G, te, ae) {
    var pe = he(G, !0), me = Ue("span", j, G.getDate().toString());
    return me.dateObj = G, me.$i = ae, me.setAttribute("aria-label", e.formatDate(G, e.config.ariaDateFormat)), j.indexOf("hidden") === -1 && Jt(G, e.now) === 0 && (e.todayDateElem = me, me.classList.add("today"), me.setAttribute("aria-current", "date")), pe ? (me.tabIndex = -1, Vi(G) && (me.classList.add("selected"), e.selectedDateElem = me, e.config.mode === "range" && (Rt(me, "startRange", e.selectedDates[0] && Jt(G, e.selectedDates[0], !0) === 0), Rt(me, "endRange", e.selectedDates[1] && Jt(G, e.selectedDates[1], !0) === 0), j === "nextMonthDay" && me.classList.add("inRange")))) : me.classList.add("flatpickr-disabled"), e.config.mode === "range" && oo(G) && !Vi(G) && me.classList.add("inRange"), e.weekNumbers && e.config.showMonths === 1 && j !== "prevMonthDay" && ae % 7 === 6 && e.weekNumbers.insertAdjacentHTML("beforeend", "<span class='flatpickr-day'>" + e.config.getWeek(G) + "</span>"), tt("onDayCreate", me), me;
  }
  function y(j) {
    j.focus(), e.config.mode === "range" && Ie(j);
  }
  function I(j) {
    for (var G = j > 0 ? 0 : e.config.showMonths - 1, te = j > 0 ? e.config.showMonths : -1, ae = G; ae != te; ae += j)
      for (var pe = e.daysContainer.children[ae], me = j > 0 ? 0 : pe.children.length - 1, Re = j > 0 ? pe.children.length : -1, ye = me; ye != Re; ye += j) {
        var Fe = pe.children[ye];
        if (Fe.className.indexOf("hidden") === -1 && he(Fe.dateObj))
          return Fe;
      }
  }
  function $(j, G) {
    for (var te = j.className.indexOf("Month") === -1 ? j.dateObj.getMonth() : e.currentMonth, ae = G > 0 ? e.config.showMonths : -1, pe = G > 0 ? 1 : -1, me = te - e.currentMonth; me != ae; me += pe)
      for (var Re = e.daysContainer.children[me], ye = te - e.currentMonth === me ? j.$i + G : G < 0 ? Re.children.length - 1 : 0, Fe = Re.children.length, be = ye; be >= 0 && be < Fe && be != (G > 0 ? Fe : -1); be += pe) {
        var De = Re.children[be];
        if (De.className.indexOf("hidden") === -1 && he(De.dateObj) && Math.abs(j.$i - be) >= Math.abs(G))
          return y(De);
      }
    e.changeMonth(pe), V(I(pe), 0);
  }
  function V(j, G) {
    var te = s(), ae = we(te || document.body), pe = j !== void 0 ? j : ae ? te : e.selectedDateElem !== void 0 && we(e.selectedDateElem) ? e.selectedDateElem : e.todayDateElem !== void 0 && we(e.todayDateElem) ? e.todayDateElem : I(G > 0 ? 1 : -1);
    pe === void 0 ? e._input.focus() : ae ? $(pe, G) : y(pe);
  }
  function z(j, G) {
    for (var te = (new Date(j, G, 1).getDay() - e.l10n.firstDayOfWeek + 7) % 7, ae = e.utils.getDaysInMonth((G - 1 + 12) % 12, j), pe = e.utils.getDaysInMonth(G, j), me = window.document.createDocumentFragment(), Re = e.config.showMonths > 1, ye = Re ? "prevMonthDay hidden" : "prevMonthDay", Fe = Re ? "nextMonthDay hidden" : "nextMonthDay", be = ae + 1 - te, De = 0; be <= ae; be++, De++)
      me.appendChild(A("flatpickr-day " + ye, new Date(j, G - 1, be), be, De));
    for (be = 1; be <= pe; be++, De++)
      me.appendChild(A("flatpickr-day", new Date(j, G, be), be, De));
    for (var Xe = pe + 1; Xe <= 42 - te && (e.config.showMonths === 1 || De % 7 !== 0); Xe++, De++)
      me.appendChild(A("flatpickr-day " + Fe, new Date(j, G + 1, Xe % pe), Xe, De));
    var $t = Ue("div", "dayContainer");
    return $t.appendChild(me), $t;
  }
  function F() {
    if (e.daysContainer !== void 0) {
      ua(e.daysContainer), e.weekNumbers && ua(e.weekNumbers);
      for (var j = document.createDocumentFragment(), G = 0; G < e.config.showMonths; G++) {
        var te = new Date(e.currentYear, e.currentMonth, 1);
        te.setMonth(e.currentMonth + G), j.appendChild(z(te.getFullYear(), te.getMonth()));
      }
      e.daysContainer.appendChild(j), e.days = e.daysContainer.firstChild, e.config.mode === "range" && e.selectedDates.length === 1 && Ie();
    }
  }
  function P() {
    if (!(e.config.showMonths > 1 || e.config.monthSelectorType !== "dropdown")) {
      var j = function(ae) {
        return e.config.minDate !== void 0 && e.currentYear === e.config.minDate.getFullYear() && ae < e.config.minDate.getMonth() ? !1 : !(e.config.maxDate !== void 0 && e.currentYear === e.config.maxDate.getFullYear() && ae > e.config.maxDate.getMonth());
      };
      e.monthsDropdownContainer.tabIndex = -1, e.monthsDropdownContainer.innerHTML = "";
      for (var G = 0; G < 12; G++)
        if (j(G)) {
          var te = Ue("option", "flatpickr-monthDropdown-month");
          te.value = new Date(e.currentYear, G).getMonth().toString(), te.textContent = Ba(G, e.config.shorthandCurrentMonth, e.l10n), te.tabIndex = -1, e.currentMonth === G && (te.selected = !0), e.monthsDropdownContainer.appendChild(te);
        }
    }
  }
  function k() {
    var j = Ue("div", "flatpickr-month"), G = window.document.createDocumentFragment(), te;
    e.config.showMonths > 1 || e.config.monthSelectorType === "static" ? te = Ue("span", "cur-month") : (e.monthsDropdownContainer = Ue("select", "flatpickr-monthDropdown-months"), e.monthsDropdownContainer.setAttribute("aria-label", e.l10n.monthAriaLabel), m(e.monthsDropdownContainer, "change", function(Re) {
      var ye = Xt(Re), Fe = parseInt(ye.value, 10);
      e.changeMonth(Fe - e.currentMonth), tt("onMonthChange");
    }), P(), te = e.monthsDropdownContainer);
    var ae = ca("cur-year", { tabindex: "-1" }), pe = ae.getElementsByTagName("input")[0];
    pe.setAttribute("aria-label", e.l10n.yearAriaLabel), e.config.minDate && pe.setAttribute("min", e.config.minDate.getFullYear().toString()), e.config.maxDate && (pe.setAttribute("max", e.config.maxDate.getFullYear().toString()), pe.disabled = !!e.config.minDate && e.config.minDate.getFullYear() === e.config.maxDate.getFullYear());
    var me = Ue("div", "flatpickr-current-month");
    return me.appendChild(te), me.appendChild(ae), G.appendChild(me), j.appendChild(G), {
      container: j,
      yearElement: pe,
      monthElement: te
    };
  }
  function R() {
    ua(e.monthNav), e.monthNav.appendChild(e.prevMonthNav), e.config.showMonths && (e.yearElements = [], e.monthElements = []);
    for (var j = e.config.showMonths; j--; ) {
      var G = k();
      e.yearElements.push(G.yearElement), e.monthElements.push(G.monthElement), e.monthNav.appendChild(G.container);
    }
    e.monthNav.appendChild(e.nextMonthNav);
  }
  function L() {
    return e.monthNav = Ue("div", "flatpickr-months"), e.yearElements = [], e.monthElements = [], e.prevMonthNav = Ue("span", "flatpickr-prev-month"), e.prevMonthNav.innerHTML = e.config.prevArrow, e.nextMonthNav = Ue("span", "flatpickr-next-month"), e.nextMonthNav.innerHTML = e.config.nextArrow, R(), Object.defineProperty(e, "_hidePrevMonthArrow", {
      get: function() {
        return e.__hidePrevMonthArrow;
      },
      set: function(j) {
        e.__hidePrevMonthArrow !== j && (Rt(e.prevMonthNav, "flatpickr-disabled", j), e.__hidePrevMonthArrow = j);
      }
    }), Object.defineProperty(e, "_hideNextMonthArrow", {
      get: function() {
        return e.__hideNextMonthArrow;
      },
      set: function(j) {
        e.__hideNextMonthArrow !== j && (Rt(e.nextMonthNav, "flatpickr-disabled", j), e.__hideNextMonthArrow = j);
      }
    }), e.currentYearElement = e.yearElements[0], qr(), e.monthNav;
  }
  function E() {
    e.calendarContainer.classList.add("hasTime"), e.config.noCalendar && e.calendarContainer.classList.add("noCalendar");
    var j = fl(e.config);
    e.timeContainer = Ue("div", "flatpickr-time"), e.timeContainer.tabIndex = -1;
    var G = Ue("span", "flatpickr-time-separator", ":"), te = ca("flatpickr-hour", {
      "aria-label": e.l10n.hourAriaLabel
    });
    e.hourElement = te.getElementsByTagName("input")[0];
    var ae = ca("flatpickr-minute", {
      "aria-label": e.l10n.minuteAriaLabel
    });
    if (e.minuteElement = ae.getElementsByTagName("input")[0], e.hourElement.tabIndex = e.minuteElement.tabIndex = -1, e.hourElement.value = qt(e.latestSelectedDateObj ? e.latestSelectedDateObj.getHours() : e.config.time_24hr ? j.hours : f(j.hours)), e.minuteElement.value = qt(e.latestSelectedDateObj ? e.latestSelectedDateObj.getMinutes() : j.minutes), e.hourElement.setAttribute("step", e.config.hourIncrement.toString()), e.minuteElement.setAttribute("step", e.config.minuteIncrement.toString()), e.hourElement.setAttribute("min", e.config.time_24hr ? "0" : "1"), e.hourElement.setAttribute("max", e.config.time_24hr ? "23" : "12"), e.hourElement.setAttribute("maxlength", "2"), e.minuteElement.setAttribute("min", "0"), e.minuteElement.setAttribute("max", "59"), e.minuteElement.setAttribute("maxlength", "2"), e.timeContainer.appendChild(te), e.timeContainer.appendChild(G), e.timeContainer.appendChild(ae), e.config.time_24hr && e.timeContainer.classList.add("time24hr"), e.config.enableSeconds) {
      e.timeContainer.classList.add("hasSeconds");
      var pe = ca("flatpickr-second");
      e.secondElement = pe.getElementsByTagName("input")[0], e.secondElement.value = qt(e.latestSelectedDateObj ? e.latestSelectedDateObj.getSeconds() : j.seconds), e.secondElement.setAttribute("step", e.minuteElement.getAttribute("step")), e.secondElement.setAttribute("min", "0"), e.secondElement.setAttribute("max", "59"), e.secondElement.setAttribute("maxlength", "2"), e.timeContainer.appendChild(Ue("span", "flatpickr-time-separator", ":")), e.timeContainer.appendChild(pe);
    }
    return e.config.time_24hr || (e.amPM = Ue("span", "flatpickr-am-pm", e.l10n.amPM[pn((e.latestSelectedDateObj ? e.hourElement.value : e.config.defaultHour) > 11)]), e.amPM.title = e.l10n.toggleTitle, e.amPM.tabIndex = -1, e.timeContainer.appendChild(e.amPM)), e.timeContainer;
  }
  function M() {
    e.weekdayContainer ? ua(e.weekdayContainer) : e.weekdayContainer = Ue("div", "flatpickr-weekdays");
    for (var j = e.config.showMonths; j--; ) {
      var G = Ue("div", "flatpickr-weekdaycontainer");
      e.weekdayContainer.appendChild(G);
    }
    return W(), e.weekdayContainer;
  }
  function W() {
    if (e.weekdayContainer) {
      var j = e.l10n.firstDayOfWeek, G = Ad(e.l10n.weekdays.shorthand);
      j > 0 && j < G.length && (G = Ad(G.splice(j, G.length), G.splice(0, j)));
      for (var te = e.config.showMonths; te--; )
        e.weekdayContainer.children[te].innerHTML = `
      <span class='flatpickr-weekday'>
        ` + G.join("</span><span class='flatpickr-weekday'>") + `
      </span>
      `;
    }
  }
  function Y() {
    e.calendarContainer.classList.add("hasWeeks");
    var j = Ue("div", "flatpickr-weekwrapper");
    j.appendChild(Ue("span", "flatpickr-weekday", e.l10n.weekAbbreviation));
    var G = Ue("div", "flatpickr-weeks");
    return j.appendChild(G), {
      weekWrapper: j,
      weekNumbers: G
    };
  }
  function J(j, G) {
    G === void 0 && (G = !0);
    var te = G ? j : j - e.currentMonth;
    te < 0 && e._hidePrevMonthArrow === !0 || te > 0 && e._hideNextMonthArrow === !0 || (e.currentMonth += te, (e.currentMonth < 0 || e.currentMonth > 11) && (e.currentYear += e.currentMonth > 11 ? 1 : -1, e.currentMonth = (e.currentMonth + 12) % 12, tt("onYearChange"), P()), F(), tt("onMonthChange"), qr());
  }
  function X(j, G) {
    if (j === void 0 && (j = !0), G === void 0 && (G = !0), e.input.value = "", e.altInput !== void 0 && (e.altInput.value = ""), e.mobileInput !== void 0 && (e.mobileInput.value = ""), e.selectedDates = [], e.latestSelectedDateObj = void 0, G === !0 && (e.currentYear = e._initialDate.getFullYear(), e.currentMonth = e._initialDate.getMonth()), e.config.enableTime === !0) {
      var te = fl(e.config), ae = te.hours, pe = te.minutes, me = te.seconds;
      w(ae, pe, me);
    }
    e.redraw(), j && tt("onChange");
  }
  function Q() {
    e.isOpen = !1, e.isMobile || (e.calendarContainer !== void 0 && e.calendarContainer.classList.remove("open"), e._input !== void 0 && e._input.classList.remove("active")), tt("onClose");
  }
  function ee() {
    e.config !== void 0 && tt("onDestroy");
    for (var j = e._handlers.length; j--; )
      e._handlers[j].remove();
    if (e._handlers = [], e.mobileInput)
      e.mobileInput.parentNode && e.mobileInput.parentNode.removeChild(e.mobileInput), e.mobileInput = void 0;
    else if (e.calendarContainer && e.calendarContainer.parentNode)
      if (e.config.static && e.calendarContainer.parentNode) {
        var G = e.calendarContainer.parentNode;
        if (G.lastChild && G.removeChild(G.lastChild), G.parentNode) {
          for (; G.firstChild; )
            G.parentNode.insertBefore(G.firstChild, G);
          G.parentNode.removeChild(G);
        }
      } else
        e.calendarContainer.parentNode.removeChild(e.calendarContainer);
    e.altInput && (e.input.type = "text", e.altInput.parentNode && e.altInput.parentNode.removeChild(e.altInput), delete e.altInput), e.input && (e.input.type = e.input._type, e.input.classList.remove("flatpickr-input"), e.input.removeAttribute("readonly")), [
      "_showTimeInput",
      "latestSelectedDateObj",
      "_hideNextMonthArrow",
      "_hidePrevMonthArrow",
      "__hideNextMonthArrow",
      "__hidePrevMonthArrow",
      "isMobile",
      "isOpen",
      "selectedDateElem",
      "minDateHasTime",
      "maxDateHasTime",
      "days",
      "daysContainer",
      "_input",
      "_positionElement",
      "innerContainer",
      "rContainer",
      "monthNav",
      "todayDateElem",
      "calendarContainer",
      "weekdayContainer",
      "prevMonthNav",
      "nextMonthNav",
      "monthsDropdownContainer",
      "currentMonthElement",
      "currentYearElement",
      "navigationCurrentMonth",
      "selectedDateElem",
      "config"
    ].forEach(function(te) {
      try {
        delete e[te];
      } catch {
      }
    });
  }
  function ie(j) {
    return e.calendarContainer.contains(j);
  }
  function oe(j) {
    if (e.isOpen && !e.config.inline) {
      var G = Xt(j), te = ie(G), ae = G === e.input || G === e.altInput || e.element.contains(G) || j.path && j.path.indexOf && (~j.path.indexOf(e.input) || ~j.path.indexOf(e.altInput)), pe = !ae && !te && !ie(j.relatedTarget), me = !e.config.ignoredFocusElements.some(function(Re) {
        return Re.contains(G);
      });
      pe && me && (e.config.allowInput && e.setDate(e._input.value, !1, e.config.altInput ? e.config.altFormat : e.config.dateFormat), e.timeContainer !== void 0 && e.minuteElement !== void 0 && e.hourElement !== void 0 && e.input.value !== "" && e.input.value !== void 0 && l(), e.close(), e.config && e.config.mode === "range" && e.selectedDates.length === 1 && e.clear(!1));
    }
  }
  function ce(j) {
    if (!(!j || e.config.minDate && j < e.config.minDate.getFullYear() || e.config.maxDate && j > e.config.maxDate.getFullYear())) {
      var G = j, te = e.currentYear !== G;
      e.currentYear = G || e.currentYear, e.config.maxDate && e.currentYear === e.config.maxDate.getFullYear() ? e.currentMonth = Math.min(e.config.maxDate.getMonth(), e.currentMonth) : e.config.minDate && e.currentYear === e.config.minDate.getFullYear() && (e.currentMonth = Math.max(e.config.minDate.getMonth(), e.currentMonth)), te && (e.redraw(), tt("onYearChange"), P());
    }
  }
  function he(j, G) {
    var te;
    G === void 0 && (G = !0);
    var ae = e.parseDate(j, void 0, G);
    if (e.config.minDate && ae && Jt(ae, e.config.minDate, G !== void 0 ? G : !e.minDateHasTime) < 0 || e.config.maxDate && ae && Jt(ae, e.config.maxDate, G !== void 0 ? G : !e.maxDateHasTime) > 0)
      return !1;
    if (!e.config.enable && e.config.disable.length === 0)
      return !0;
    if (ae === void 0)
      return !1;
    for (var pe = !!e.config.enable, me = (te = e.config.enable) !== null && te !== void 0 ? te : e.config.disable, Re = 0, ye = void 0; Re < me.length; Re++) {
      if (ye = me[Re], typeof ye == "function" && ye(ae))
        return pe;
      if (ye instanceof Date && ae !== void 0 && ye.getTime() === ae.getTime())
        return pe;
      if (typeof ye == "string") {
        var Fe = e.parseDate(ye, void 0, !0);
        return Fe && Fe.getTime() === ae.getTime() ? pe : !pe;
      } else if (typeof ye == "object" && ae !== void 0 && ye.from && ye.to && ae.getTime() >= ye.from.getTime() && ae.getTime() <= ye.to.getTime())
        return pe;
    }
    return !pe;
  }
  function we(j) {
    return e.daysContainer !== void 0 ? j.className.indexOf("hidden") === -1 && j.className.indexOf("flatpickr-disabled") === -1 && e.daysContainer.contains(j) : !1;
  }
  function Oe(j) {
    var G = j.target === e._input, te = e._input.value.trimEnd() !== Wi();
    G && te && !(j.relatedTarget && ie(j.relatedTarget)) && e.setDate(e._input.value, !0, j.target === e.altInput ? e.config.altFormat : e.config.dateFormat);
  }
  function Ve(j) {
    var G = Xt(j), te = e.config.wrap ? t.contains(G) : G === e._input, ae = e.config.allowInput, pe = e.isOpen && (!ae || !te), me = e.config.inline && te && !ae;
    if (j.keyCode === 13 && te) {
      if (ae)
        return e.setDate(e._input.value, !0, G === e.altInput ? e.config.altFormat : e.config.dateFormat), e.close(), G.blur();
      e.open();
    } else if (ie(G) || pe || me) {
      var Re = !!e.timeContainer && e.timeContainer.contains(G);
      switch (j.keyCode) {
        case 13:
          Re ? (j.preventDefault(), l(), ut()) : Ot(j);
          break;
        case 27:
          j.preventDefault(), ut();
          break;
        case 8:
        case 46:
          te && !e.config.allowInput && (j.preventDefault(), e.clear());
          break;
        case 37:
        case 39:
          if (!Re && !te) {
            j.preventDefault();
            var ye = s();
            if (e.daysContainer !== void 0 && (ae === !1 || ye && we(ye))) {
              var Fe = j.keyCode === 39 ? 1 : -1;
              j.ctrlKey ? (j.stopPropagation(), J(Fe), V(I(1), 0)) : V(void 0, Fe);
            }
          } else
            e.hourElement && e.hourElement.focus();
          break;
        case 38:
        case 40:
          j.preventDefault();
          var be = j.keyCode === 40 ? 1 : -1;
          e.daysContainer && G.$i !== void 0 || G === e.input || G === e.altInput ? j.ctrlKey ? (j.stopPropagation(), ce(e.currentYear - be), V(I(1), 0)) : Re || V(void 0, be * 7) : G === e.currentYearElement ? ce(e.currentYear - be) : e.config.enableTime && (!Re && e.hourElement && e.hourElement.focus(), l(j), e._debouncedChange());
          break;
        case 9:
          if (Re) {
            var De = [
              e.hourElement,
              e.minuteElement,
              e.secondElement,
              e.amPM
            ].concat(e.pluginElements).filter(function(Tt) {
              return Tt;
            }), Xe = De.indexOf(G);
            if (Xe !== -1) {
              var $t = De[Xe + (j.shiftKey ? -1 : 1)];
              j.preventDefault(), ($t || e._input).focus();
            }
          } else
            !e.config.noCalendar && e.daysContainer && e.daysContainer.contains(G) && j.shiftKey && (j.preventDefault(), e._input.focus());
          break;
      }
    }
    if (e.amPM !== void 0 && G === e.amPM)
      switch (j.key) {
        case e.l10n.amPM[0].charAt(0):
        case e.l10n.amPM[0].charAt(0).toLowerCase():
          e.amPM.textContent = e.l10n.amPM[0], g(), ft();
          break;
        case e.l10n.amPM[1].charAt(0):
        case e.l10n.amPM[1].charAt(0).toLowerCase():
          e.amPM.textContent = e.l10n.amPM[1], g(), ft();
          break;
      }
    (te || ie(G)) && tt("onKeyDown", j);
  }
  function Ie(j, G) {
    if (G === void 0 && (G = "flatpickr-day"), !(e.selectedDates.length !== 1 || j && (!j.classList.contains(G) || j.classList.contains("flatpickr-disabled")))) {
      for (var te = j ? j.dateObj.getTime() : e.days.firstElementChild.dateObj.getTime(), ae = e.parseDate(e.selectedDates[0], void 0, !0).getTime(), pe = Math.min(te, e.selectedDates[0].getTime()), me = Math.max(te, e.selectedDates[0].getTime()), Re = !1, ye = 0, Fe = 0, be = pe; be < me; be += Kw.DAY)
        he(new Date(be), !0) || (Re = Re || be > pe && be < me, be < ae && (!ye || be > ye) ? ye = be : be > ae && (!Fe || be < Fe) && (Fe = be));
      var De = Array.from(e.rContainer.querySelectorAll("*:nth-child(-n+" + e.config.showMonths + ") > ." + G));
      De.forEach(function(Xe) {
        var $t = Xe.dateObj, Tt = $t.getTime(), dn = ye > 0 && Tt < ye || Fe > 0 && Tt > Fe;
        if (dn) {
          Xe.classList.add("notAllowed"), ["inRange", "startRange", "endRange"].forEach(function(Bn) {
            Xe.classList.remove(Bn);
          });
          return;
        } else if (Re && !dn)
          return;
        ["startRange", "inRange", "endRange", "notAllowed"].forEach(function(Bn) {
          Xe.classList.remove(Bn);
        }), j !== void 0 && (j.classList.add(te <= e.selectedDates[0].getTime() ? "startRange" : "endRange"), ae < te && Tt === ae ? Xe.classList.add("startRange") : ae > te && Tt === ae && Xe.classList.add("endRange"), Tt >= ye && (Fe === 0 || Tt <= Fe) && Gw(Tt, ae, te) && Xe.classList.add("inRange"));
      });
    }
  }
  function Ze() {
    e.isOpen && !e.config.static && !e.config.inline && wt();
  }
  function et(j, G) {
    if (G === void 0 && (G = e._positionElement), e.isMobile === !0) {
      if (j) {
        j.preventDefault();
        var te = Xt(j);
        te && te.blur();
      }
      e.mobileInput !== void 0 && (e.mobileInput.focus(), e.mobileInput.click()), tt("onOpen");
      return;
    } else if (e._input.disabled || e.config.inline)
      return;
    var ae = e.isOpen;
    e.isOpen = !0, ae || (e.calendarContainer.classList.add("open"), e._input.classList.add("active"), tt("onOpen"), wt(G)), e.config.enableTime === !0 && e.config.noCalendar === !0 && e.config.allowInput === !1 && (j === void 0 || !e.timeContainer.contains(j.relatedTarget)) && setTimeout(function() {
      return e.hourElement.select();
    }, 50);
  }
  function He(j) {
    return function(G) {
      var te = e.config["_" + j + "Date"] = e.parseDate(G, e.config.dateFormat), ae = e.config["_" + (j === "min" ? "max" : "min") + "Date"];
      te !== void 0 && (e[j === "min" ? "minDateHasTime" : "maxDateHasTime"] = te.getHours() > 0 || te.getMinutes() > 0 || te.getSeconds() > 0), e.selectedDates && (e.selectedDates = e.selectedDates.filter(function(pe) {
        return he(pe);
      }), !e.selectedDates.length && j === "min" && b(te), ft()), e.daysContainer && (St(), te !== void 0 ? e.currentYearElement[j] = te.getFullYear().toString() : e.currentYearElement.removeAttribute(j), e.currentYearElement.disabled = !!ae && te !== void 0 && ae.getFullYear() === te.getFullYear());
    };
  }
  function Ke() {
    var j = [
      "wrap",
      "weekNumbers",
      "allowInput",
      "allowInvalidPreload",
      "clickOpens",
      "time_24hr",
      "enableTime",
      "noCalendar",
      "altInput",
      "shorthandCurrentMonth",
      "inline",
      "static",
      "enableSeconds",
      "disableMobile"
    ], G = At(At({}, JSON.parse(JSON.stringify(t.dataset || {}))), n), te = {};
    e.config.parseDate = G.parseDate, e.config.formatDate = G.formatDate, Object.defineProperty(e.config, "enable", {
      get: function() {
        return e.config._enable;
      },
      set: function(De) {
        e.config._enable = Ns(De);
      }
    }), Object.defineProperty(e.config, "disable", {
      get: function() {
        return e.config._disable;
      },
      set: function(De) {
        e.config._disable = Ns(De);
      }
    });
    var ae = G.mode === "time";
    if (!G.dateFormat && (G.enableTime || ae)) {
      var pe = vt.defaultConfig.dateFormat || wi.dateFormat;
      te.dateFormat = G.noCalendar || ae ? "H:i" + (G.enableSeconds ? ":S" : "") : pe + " H:i" + (G.enableSeconds ? ":S" : "");
    }
    if (G.altInput && (G.enableTime || ae) && !G.altFormat) {
      var me = vt.defaultConfig.altFormat || wi.altFormat;
      te.altFormat = G.noCalendar || ae ? "h:i" + (G.enableSeconds ? ":S K" : " K") : me + (" h:i" + (G.enableSeconds ? ":S" : "") + " K");
    }
    Object.defineProperty(e.config, "minDate", {
      get: function() {
        return e.config._minDate;
      },
      set: He("min")
    }), Object.defineProperty(e.config, "maxDate", {
      get: function() {
        return e.config._maxDate;
      },
      set: He("max")
    });
    var Re = function(De) {
      return function(Xe) {
        e.config[De === "min" ? "_minTime" : "_maxTime"] = e.parseDate(Xe, "H:i:S");
      };
    };
    Object.defineProperty(e.config, "minTime", {
      get: function() {
        return e.config._minTime;
      },
      set: Re("min")
    }), Object.defineProperty(e.config, "maxTime", {
      get: function() {
        return e.config._maxTime;
      },
      set: Re("max")
    }), G.mode === "time" && (e.config.noCalendar = !0, e.config.enableTime = !0), Object.assign(e.config, te, G);
    for (var ye = 0; ye < j.length; ye++)
      e.config[j[ye]] = e.config[j[ye]] === !0 || e.config[j[ye]] === "true";
    ll.filter(function(De) {
      return e.config[De] !== void 0;
    }).forEach(function(De) {
      e.config[De] = ul(e.config[De] || []).map(a);
    }), e.isMobile = !e.config.disableMobile && !e.config.inline && e.config.mode === "single" && !e.config.disable.length && !e.config.enable && !e.config.weekNumbers && /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
    for (var ye = 0; ye < e.config.plugins.length; ye++) {
      var Fe = e.config.plugins[ye](e) || {};
      for (var be in Fe)
        ll.indexOf(be) > -1 ? e.config[be] = ul(Fe[be]).map(a).concat(e.config[be]) : typeof G[be] > "u" && (e.config[be] = Fe[be]);
    }
    G.altInputClass || (e.config.altInputClass = Ct().className + " " + e.config.altInputClass), tt("onParseConfig");
  }
  function Ct() {
    return e.config.wrap ? t.querySelector("[data-input]") : t;
  }
  function yt() {
    typeof e.config.locale != "object" && typeof vt.l10ns[e.config.locale] > "u" && e.config.errorHandler(new Error("flatpickr: invalid locale " + e.config.locale)), e.l10n = At(At({}, vt.l10ns.default), typeof e.config.locale == "object" ? e.config.locale : e.config.locale !== "default" ? vt.l10ns[e.config.locale] : void 0), Tr.D = "(" + e.l10n.weekdays.shorthand.join("|") + ")", Tr.l = "(" + e.l10n.weekdays.longhand.join("|") + ")", Tr.M = "(" + e.l10n.months.shorthand.join("|") + ")", Tr.F = "(" + e.l10n.months.longhand.join("|") + ")", Tr.K = "(" + e.l10n.amPM[0] + "|" + e.l10n.amPM[1] + "|" + e.l10n.amPM[0].toLowerCase() + "|" + e.l10n.amPM[1].toLowerCase() + ")";
    var j = At(At({}, n), JSON.parse(JSON.stringify(t.dataset || {})));
    j.time_24hr === void 0 && vt.defaultConfig.time_24hr === void 0 && (e.config.time_24hr = e.l10n.time_24hr), e.formatDate = Uh(e), e.parseDate = Nl({ config: e.config, l10n: e.l10n });
  }
  function wt(j) {
    if (typeof e.config.position == "function")
      return void e.config.position(e, j);
    if (e.calendarContainer !== void 0) {
      tt("onPreCalendarPosition");
      var G = j || e._positionElement, te = Array.prototype.reduce.call(e.calendarContainer.children, function(ho, Bs) {
        return ho + Bs.offsetHeight;
      }, 0), ae = e.calendarContainer.offsetWidth, pe = e.config.position.split(" "), me = pe[0], Re = pe.length > 1 ? pe[1] : null, ye = G.getBoundingClientRect(), Fe = window.innerHeight - ye.bottom, be = me === "above" || me !== "below" && Fe < te && ye.top > te, De = window.pageYOffset + ye.top + (be ? -te - 2 : G.offsetHeight + 2);
      if (Rt(e.calendarContainer, "arrowTop", !be), Rt(e.calendarContainer, "arrowBottom", be), !e.config.inline) {
        var Xe = window.pageXOffset + ye.left, $t = !1, Tt = !1;
        Re === "center" ? (Xe -= (ae - ye.width) / 2, $t = !0) : Re === "right" && (Xe -= ae - ye.width, Tt = !0), Rt(e.calendarContainer, "arrowLeft", !$t && !Tt), Rt(e.calendarContainer, "arrowCenter", $t), Rt(e.calendarContainer, "arrowRight", Tt);
        var dn = window.document.body.offsetWidth - (window.pageXOffset + ye.right), Bn = Xe + ae > window.document.body.offsetWidth, Ms = dn + ae > window.document.body.offsetWidth;
        if (Rt(e.calendarContainer, "rightMost", Bn), !e.config.static)
          if (e.calendarContainer.style.top = De + "px", !Bn)
            e.calendarContainer.style.left = Xe + "px", e.calendarContainer.style.right = "auto";
          else if (!Ms)
            e.calendarContainer.style.left = "auto", e.calendarContainer.style.right = dn + "px";
          else {
            var Gr = rt();
            if (Gr === void 0)
              return;
            var Fs = window.document.body.offsetWidth, ji = Math.max(0, Fs / 2 - ae / 2), Ur = ".flatpickr-calendar.centerMost:before", uo = ".flatpickr-calendar.centerMost:after", co = Gr.cssRules.length, fo = "{left:" + ye.left + "px;right:auto;}";
            Rt(e.calendarContainer, "rightMost", !1), Rt(e.calendarContainer, "centerMost", !0), Gr.insertRule(Ur + "," + uo + fo, co), e.calendarContainer.style.left = ji + "px", e.calendarContainer.style.right = "auto";
          }
      }
    }
  }
  function rt() {
    for (var j = null, G = 0; G < document.styleSheets.length; G++) {
      var te = document.styleSheets[G];
      if (te.cssRules) {
        try {
          te.cssRules;
        } catch {
          continue;
        }
        j = te;
        break;
      }
    }
    return j ?? mt();
  }
  function mt() {
    var j = document.createElement("style");
    return document.head.appendChild(j), j.sheet;
  }
  function St() {
    e.config.noCalendar || e.isMobile || (P(), qr(), F());
  }
  function ut() {
    e._input.focus(), window.navigator.userAgent.indexOf("MSIE") !== -1 || navigator.msMaxTouchPoints !== void 0 ? setTimeout(e.close, 0) : e.close();
  }
  function Ot(j) {
    j.preventDefault(), j.stopPropagation();
    var G = function(De) {
      return De.classList && De.classList.contains("flatpickr-day") && !De.classList.contains("flatpickr-disabled") && !De.classList.contains("notAllowed");
    }, te = Gh(Xt(j), G);
    if (te !== void 0) {
      var ae = te, pe = e.latestSelectedDateObj = new Date(ae.dateObj.getTime()), me = (pe.getMonth() < e.currentMonth || pe.getMonth() > e.currentMonth + e.config.showMonths - 1) && e.config.mode !== "range";
      if (e.selectedDateElem = ae, e.config.mode === "single")
        e.selectedDates = [pe];
      else if (e.config.mode === "multiple") {
        var Re = Vi(pe);
        Re ? e.selectedDates.splice(parseInt(Re), 1) : e.selectedDates.push(pe);
      } else
        e.config.mode === "range" && (e.selectedDates.length === 2 && e.clear(!1, !1), e.latestSelectedDateObj = pe, e.selectedDates.push(pe), Jt(pe, e.selectedDates[0], !0) !== 0 && e.selectedDates.sort(function(De, Xe) {
          return De.getTime() - Xe.getTime();
        }));
      if (g(), me) {
        var ye = e.currentYear !== pe.getFullYear();
        e.currentYear = pe.getFullYear(), e.currentMonth = pe.getMonth(), ye && (tt("onYearChange"), P()), tt("onMonthChange");
      }
      if (qr(), F(), ft(), !me && e.config.mode !== "range" && e.config.showMonths === 1 ? y(ae) : e.selectedDateElem !== void 0 && e.hourElement === void 0 && e.selectedDateElem && e.selectedDateElem.focus(), e.hourElement !== void 0 && e.hourElement !== void 0 && e.hourElement.focus(), e.config.closeOnSelect) {
        var Fe = e.config.mode === "single" && !e.config.enableTime, be = e.config.mode === "range" && e.selectedDates.length === 2 && !e.config.enableTime;
        (Fe || be) && ut();
      }
      h();
    }
  }
  var dt = {
    locale: [yt, W],
    showMonths: [R, o, M],
    minDate: [p],
    maxDate: [p],
    positionElement: [vr],
    clickOpens: [
      function() {
        e.config.clickOpens === !0 ? (m(e._input, "focus", e.open), m(e._input, "click", e.open)) : (e._input.removeEventListener("focus", e.open), e._input.removeEventListener("click", e.open));
      }
    ]
  };
  function mr(j, G) {
    if (j !== null && typeof j == "object") {
      Object.assign(e.config, j);
      for (var te in j)
        dt[te] !== void 0 && dt[te].forEach(function(ae) {
          return ae();
        });
    } else
      e.config[j] = G, dt[j] !== void 0 ? dt[j].forEach(function(ae) {
        return ae();
      }) : ll.indexOf(j) > -1 && (e.config[j] = ul(G));
    e.redraw(), ft(!0);
  }
  function It(j, G) {
    var te = [];
    if (j instanceof Array)
      te = j.map(function(ae) {
        return e.parseDate(ae, G);
      });
    else if (j instanceof Date || typeof j == "number")
      te = [e.parseDate(j, G)];
    else if (typeof j == "string")
      switch (e.config.mode) {
        case "single":
        case "time":
          te = [e.parseDate(j, G)];
          break;
        case "multiple":
          te = j.split(e.config.conjunction).map(function(ae) {
            return e.parseDate(ae, G);
          });
          break;
        case "range":
          te = j.split(e.l10n.rangeSeparator).map(function(ae) {
            return e.parseDate(ae, G);
          });
          break;
      }
    else
      e.config.errorHandler(new Error("Invalid date supplied: " + JSON.stringify(j)));
    e.selectedDates = e.config.allowInvalidPreload ? te : te.filter(function(ae) {
      return ae instanceof Date && he(ae, !1);
    }), e.config.mode === "range" && e.selectedDates.sort(function(ae, pe) {
      return ae.getTime() - pe.getTime();
    });
  }
  function Ht(j, G, te) {
    if (G === void 0 && (G = !1), te === void 0 && (te = e.config.dateFormat), j !== 0 && !j || j instanceof Array && j.length === 0)
      return e.clear(G);
    It(j, te), e.latestSelectedDateObj = e.selectedDates[e.selectedDates.length - 1], e.redraw(), p(void 0, G), b(), e.selectedDates.length === 0 && e.clear(!1), ft(G), G && tt("onChange");
  }
  function Ns(j) {
    return j.slice().map(function(G) {
      return typeof G == "string" || typeof G == "number" || G instanceof Date ? e.parseDate(G, void 0, !0) : G && typeof G == "object" && G.from && G.to ? {
        from: e.parseDate(G.from, void 0),
        to: e.parseDate(G.to, void 0)
      } : G;
    }).filter(function(G) {
      return G;
    });
  }
  function Rs() {
    e.selectedDates = [], e.now = e.parseDate(e.config.now) || /* @__PURE__ */ new Date();
    var j = e.config.defaultDate || ((e.input.nodeName === "INPUT" || e.input.nodeName === "TEXTAREA") && e.input.placeholder && e.input.value === e.input.placeholder ? null : e.input.value);
    j && It(j, e.config.dateFormat), e._initialDate = e.selectedDates.length > 0 ? e.selectedDates[0] : e.config.minDate && e.config.minDate.getTime() > e.now.getTime() ? e.config.minDate : e.config.maxDate && e.config.maxDate.getTime() < e.now.getTime() ? e.config.maxDate : e.now, e.currentYear = e._initialDate.getFullYear(), e.currentMonth = e._initialDate.getMonth(), e.selectedDates.length > 0 && (e.latestSelectedDateObj = e.selectedDates[0]), e.config.minTime !== void 0 && (e.config.minTime = e.parseDate(e.config.minTime, "H:i")), e.config.maxTime !== void 0 && (e.config.maxTime = e.parseDate(e.config.maxTime, "H:i")), e.minDateHasTime = !!e.config.minDate && (e.config.minDate.getHours() > 0 || e.config.minDate.getMinutes() > 0 || e.config.minDate.getSeconds() > 0), e.maxDateHasTime = !!e.config.maxDate && (e.config.maxDate.getHours() > 0 || e.config.maxDate.getMinutes() > 0 || e.config.maxDate.getSeconds() > 0);
  }
  function Ls() {
    if (e.input = Ct(), !e.input) {
      e.config.errorHandler(new Error("Invalid input element specified"));
      return;
    }
    e.input._type = e.input.type, e.input.type = "text", e.input.classList.add("flatpickr-input"), e._input = e.input, e.config.altInput && (e.altInput = Ue(e.input.nodeName, e.config.altInputClass), e._input = e.altInput, e.altInput.placeholder = e.input.placeholder, e.altInput.disabled = e.input.disabled, e.altInput.required = e.input.required, e.altInput.tabIndex = e.input.tabIndex, e.altInput.type = "text", e.input.setAttribute("type", "hidden"), !e.config.static && e.input.parentNode && e.input.parentNode.insertBefore(e.altInput, e.input.nextSibling)), e.config.allowInput || e._input.setAttribute("readonly", "readonly"), vr();
  }
  function vr() {
    e._positionElement = e.config.positionElement || e._input;
  }
  function so() {
    var j = e.config.enableTime ? e.config.noCalendar ? "time" : "datetime-local" : "date";
    e.mobileInput = Ue("input", e.input.className + " flatpickr-mobile"), e.mobileInput.tabIndex = 1, e.mobileInput.type = j, e.mobileInput.disabled = e.input.disabled, e.mobileInput.required = e.input.required, e.mobileInput.placeholder = e.input.placeholder, e.mobileFormatStr = j === "datetime-local" ? "Y-m-d\\TH:i:S" : j === "date" ? "Y-m-d" : "H:i:S", e.selectedDates.length > 0 && (e.mobileInput.defaultValue = e.mobileInput.value = e.formatDate(e.selectedDates[0], e.mobileFormatStr)), e.config.minDate && (e.mobileInput.min = e.formatDate(e.config.minDate, "Y-m-d")), e.config.maxDate && (e.mobileInput.max = e.formatDate(e.config.maxDate, "Y-m-d")), e.input.getAttribute("step") && (e.mobileInput.step = String(e.input.getAttribute("step"))), e.input.type = "hidden", e.altInput !== void 0 && (e.altInput.type = "hidden");
    try {
      e.input.parentNode && e.input.parentNode.insertBefore(e.mobileInput, e.input.nextSibling);
    } catch {
    }
    m(e.mobileInput, "change", function(G) {
      e.setDate(Xt(G).value, !1, e.mobileFormatStr), tt("onChange"), tt("onClose");
    });
  }
  function ao(j) {
    if (e.isOpen === !0)
      return e.close();
    e.open(j);
  }
  function tt(j, G) {
    if (e.config !== void 0) {
      var te = e.config[j];
      if (te !== void 0 && te.length > 0)
        for (var ae = 0; te[ae] && ae < te.length; ae++)
          te[ae](e.selectedDates, e.input.value, e, G);
      j === "onChange" && (e.input.dispatchEvent($i("change")), e.input.dispatchEvent($i("input")));
    }
  }
  function $i(j) {
    var G = document.createEvent("Event");
    return G.initEvent(j, !0, !0), G;
  }
  function Vi(j) {
    for (var G = 0; G < e.selectedDates.length; G++) {
      var te = e.selectedDates[G];
      if (te instanceof Date && Jt(te, j) === 0)
        return "" + G;
    }
    return !1;
  }
  function oo(j) {
    return e.config.mode !== "range" || e.selectedDates.length < 2 ? !1 : Jt(j, e.selectedDates[0]) >= 0 && Jt(j, e.selectedDates[1]) <= 0;
  }
  function qr() {
    e.config.noCalendar || e.isMobile || !e.monthNav || (e.yearElements.forEach(function(j, G) {
      var te = new Date(e.currentYear, e.currentMonth, 1);
      te.setMonth(e.currentMonth + G), e.config.showMonths > 1 || e.config.monthSelectorType === "static" ? e.monthElements[G].textContent = Ba(te.getMonth(), e.config.shorthandCurrentMonth, e.l10n) + " " : e.monthsDropdownContainer.value = te.getMonth().toString(), j.value = te.getFullYear().toString();
    }), e._hidePrevMonthArrow = e.config.minDate !== void 0 && (e.currentYear === e.config.minDate.getFullYear() ? e.currentMonth <= e.config.minDate.getMonth() : e.currentYear < e.config.minDate.getFullYear()), e._hideNextMonthArrow = e.config.maxDate !== void 0 && (e.currentYear === e.config.maxDate.getFullYear() ? e.currentMonth + 1 > e.config.maxDate.getMonth() : e.currentYear > e.config.maxDate.getFullYear()));
  }
  function Wi(j) {
    var G = j || (e.config.altInput ? e.config.altFormat : e.config.dateFormat);
    return e.selectedDates.map(function(te) {
      return e.formatDate(te, G);
    }).filter(function(te, ae, pe) {
      return e.config.mode !== "range" || e.config.enableTime || pe.indexOf(te) === ae;
    }).join(e.config.mode !== "range" ? e.config.conjunction : e.l10n.rangeSeparator);
  }
  function ft(j) {
    j === void 0 && (j = !0), e.mobileInput !== void 0 && e.mobileFormatStr && (e.mobileInput.value = e.latestSelectedDateObj !== void 0 ? e.formatDate(e.latestSelectedDateObj, e.mobileFormatStr) : ""), e.input.value = Wi(e.config.dateFormat), e.altInput !== void 0 && (e.altInput.value = Wi(e.config.altFormat)), j !== !1 && tt("onValueUpdate");
  }
  function cn(j) {
    var G = Xt(j), te = e.prevMonthNav.contains(G), ae = e.nextMonthNav.contains(G);
    te || ae ? J(te ? -1 : 1) : e.yearElements.indexOf(G) >= 0 ? G.select() : G.classList.contains("arrowUp") ? e.changeYear(e.currentYear + 1) : G.classList.contains("arrowDown") && e.changeYear(e.currentYear - 1);
  }
  function lo(j) {
    j.preventDefault();
    var G = j.type === "keydown", te = Xt(j), ae = te;
    e.amPM !== void 0 && te === e.amPM && (e.amPM.textContent = e.l10n.amPM[pn(e.amPM.textContent === e.l10n.amPM[0])]);
    var pe = parseFloat(ae.getAttribute("min")), me = parseFloat(ae.getAttribute("max")), Re = parseFloat(ae.getAttribute("step")), ye = parseInt(ae.value, 10), Fe = j.delta || (G ? j.which === 38 ? 1 : -1 : 0), be = ye + Re * Fe;
    if (typeof ae.value < "u" && ae.value.length === 2) {
      var De = ae === e.hourElement, Xe = ae === e.minuteElement;
      be < pe ? (be = me + be + pn(!De) + (pn(De) && pn(!e.amPM)), Xe && S(void 0, -1, e.hourElement)) : be > me && (be = ae === e.hourElement ? be - me - pn(!e.amPM) : pe, Xe && S(void 0, 1, e.hourElement)), e.amPM && De && (Re === 1 ? be + ye === 23 : Math.abs(be - ye) > Re) && (e.amPM.textContent = e.l10n.amPM[pn(e.amPM.textContent === e.l10n.amPM[0])]), ae.value = qt(be);
    }
  }
  return i(), e;
}
function _i(t, n) {
  for (var e = Array.prototype.slice.call(t).filter(function(a) {
    return a instanceof HTMLElement;
  }), r = [], i = 0; i < e.length; i++) {
    var s = e[i];
    try {
      if (s.getAttribute("data-fp-omit") !== null)
        continue;
      s._flatpickr !== void 0 && (s._flatpickr.destroy(), s._flatpickr = void 0), s._flatpickr = Qw(s, n || {}), r.push(s._flatpickr);
    } catch (a) {
      console.error(a);
    }
  }
  return r.length === 1 ? r[0] : r;
}
typeof HTMLElement < "u" && typeof HTMLCollection < "u" && typeof NodeList < "u" && (HTMLCollection.prototype.flatpickr = NodeList.prototype.flatpickr = function(t) {
  return _i(this, t);
}, HTMLElement.prototype.flatpickr = function(t) {
  return _i([this], t);
});
var vt = function(t, n) {
  return typeof t == "string" ? _i(window.document.querySelectorAll(t), n) : t instanceof Node ? _i([t], n) : _i(t, n);
};
vt.defaultConfig = {};
vt.l10ns = {
  en: At({}, vs),
  default: At({}, vs)
};
vt.localize = function(t) {
  vt.l10ns.default = At(At({}, vt.l10ns.default), t);
};
vt.setDefaults = function(t) {
  vt.defaultConfig = At(At({}, vt.defaultConfig), t);
};
vt.parseDate = Nl({});
vt.formatDate = Uh({});
vt.compareDates = Jt;
typeof jQuery < "u" && typeof jQuery.fn < "u" && (jQuery.fn.flatpickr = function(t) {
  return _i(this, t);
});
Date.prototype.fp_incr = function(t) {
  return new Date(this.getFullYear(), this.getMonth(), this.getDate() + (typeof t == "string" ? parseInt(t, 10) : t));
};
typeof window < "u" && (window.flatpickr = vt);
var Kh = { exports: {} };
/**
 * jscolor - JavaScript Color Picker
 *
 * @link    http://jscolor.com
 * @license For open source use: GPLv3
 *          For commercial use: JSColor Commercial License
 * @author  Jan Odvarko - East Desire
 *
 * See usage examples at http://jscolor.com/examples/
 */
(function(t) {
  (function(n, e) {
    {
      t.exports = n.document ? e(n) : function(r) {
        if (!r.document)
          throw new Error("jscolor needs a window with document");
        return e(r);
      };
      return;
    }
  })(typeof window < "u" ? window : sn, function(n) {
    var e = function() {
      var r = {
        initialized: !1,
        instances: [],
        // created instances of jscolor
        readyQueue: [],
        // functions waiting to be called after init
        register: function() {
          typeof n < "u" && n.document && (n.document.readyState !== "loading" ? r.pub.init() : n.document.addEventListener("DOMContentLoaded", r.pub.init, !1));
        },
        installBySelector: function(i, s) {
          if (s = s ? r.node(s) : n.document, !s)
            throw new Error("Missing root node");
          for (var a = s.querySelectorAll(i), o = new RegExp("(^|\\s)(" + r.pub.lookupClass + ")(\\s*(\\{[^}]*\\})|\\s|$)", "i"), l = 0; l < a.length; l += 1)
            if (!(a[l].jscolor && a[l].jscolor instanceof r.pub) && !(a[l].type !== void 0 && a[l].type.toLowerCase() == "color" && r.isColorAttrSupported)) {
              var c, f;
              if ((c = r.getDataAttr(a[l], "jscolor")) !== null || a[l].className && (f = a[l].className.match(o))) {
                var g = a[l], b = "";
                c !== null ? b = c : f && (console.warn('Installation using class name is DEPRECATED. Use data-jscolor="" attribute instead.' + r.docsRef), f[4] && (b = f[4]));
                var w = null;
                if (b.trim())
                  try {
                    w = r.parseOptionsStr(b);
                  } catch (C) {
                    console.warn(C + `
` + b);
                  }
                try {
                  new r.pub(g, w);
                } catch (C) {
                  console.warn(C);
                }
              }
            }
        },
        parseOptionsStr: function(i) {
          var s = null;
          try {
            s = JSON.parse(i);
          } catch (a) {
            if (r.pub.looseJSON)
              try {
                s = new Function("var opts = (" + i + '); return typeof opts === "object" ? opts : {};')();
              } catch (o) {
                throw new Error("Could not evaluate jscolor options: " + o);
              }
            else
              throw new Error("Could not parse jscolor options as JSON: " + a);
          }
          return s;
        },
        getInstances: function() {
          for (var i = [], s = 0; s < r.instances.length; s += 1)
            r.instances[s] && r.instances[s].targetElement && i.push(r.instances[s]);
          return i;
        },
        createEl: function(i) {
          var s = n.document.createElement(i);
          return r.setData(s, "gui", !0), s;
        },
        node: function(i) {
          if (!i)
            return null;
          if (typeof i == "string") {
            var s = i, a = null;
            try {
              a = n.document.querySelector(s);
            } catch (o) {
              return console.warn(o), null;
            }
            return a || console.warn("No element matches the selector: %s", s), a;
          }
          return r.isNode(i) ? i : (console.warn("Invalid node of type %s: %s", typeof i, i), null);
        },
        // See https://stackoverflow.com/questions/384286/
        isNode: function(i) {
          return typeof Node == "object" ? i instanceof Node : i && typeof i == "object" && typeof i.nodeType == "number" && typeof i.nodeName == "string";
        },
        nodeName: function(i) {
          return i && i.nodeName ? i.nodeName.toLowerCase() : !1;
        },
        removeChildren: function(i) {
          for (; i.firstChild; )
            i.removeChild(i.firstChild);
        },
        isTextInput: function(i) {
          return i && r.nodeName(i) === "input" && i.type.toLowerCase() === "text";
        },
        isButton: function(i) {
          if (!i)
            return !1;
          var s = r.nodeName(i);
          return s === "button" || s === "input" && ["button", "submit", "reset"].indexOf(i.type.toLowerCase()) > -1;
        },
        isButtonEmpty: function(i) {
          switch (r.nodeName(i)) {
            case "input":
              return !i.value || i.value.trim() === "";
            case "button":
              return i.textContent.trim() === "";
          }
          return null;
        },
        // See https://github.com/WICG/EventListenerOptions/blob/gh-pages/explainer.md
        isPassiveEventSupported: function() {
          var i = !1;
          try {
            var s = Object.defineProperty({}, "passive", {
              get: function() {
                i = !0;
              }
            });
            n.addEventListener("testPassive", null, s), n.removeEventListener("testPassive", null, s);
          } catch {
          }
          return i;
        }(),
        isColorAttrSupported: function() {
          var i = n.document.createElement("input");
          return !!(i.setAttribute && (i.setAttribute("type", "color"), i.type.toLowerCase() == "color"));
        }(),
        dataProp: "_data_jscolor",
        // usage:
        //   setData(obj, prop, value)
        //   setData(obj, {prop:value, ...})
        //
        setData: function() {
          var i = arguments[0];
          if (arguments.length === 3) {
            var s = i.hasOwnProperty(r.dataProp) ? i[r.dataProp] : i[r.dataProp] = {}, a = arguments[1], o = arguments[2];
            return s[a] = o, !0;
          } else if (arguments.length === 2 && typeof arguments[1] == "object") {
            var s = i.hasOwnProperty(r.dataProp) ? i[r.dataProp] : i[r.dataProp] = {}, l = arguments[1];
            for (var a in l)
              l.hasOwnProperty(a) && (s[a] = l[a]);
            return !0;
          }
          throw new Error("Invalid arguments");
        },
        // usage:
        //   removeData(obj, prop, [prop...])
        //
        removeData: function() {
          var i = arguments[0];
          if (!i.hasOwnProperty(r.dataProp))
            return !0;
          for (var s = 1; s < arguments.length; s += 1) {
            var a = arguments[s];
            delete i[r.dataProp][a];
          }
          return !0;
        },
        getData: function(i, s, a) {
          if (!i.hasOwnProperty(r.dataProp))
            if (a !== void 0)
              i[r.dataProp] = {};
            else
              return;
          var o = i[r.dataProp];
          return !o.hasOwnProperty(s) && a !== void 0 && (o[s] = a), o[s];
        },
        getDataAttr: function(i, s) {
          var a = "data-" + s, o = i.getAttribute(a);
          return o;
        },
        setDataAttr: function(i, s, a) {
          var o = "data-" + s;
          i.setAttribute(o, a);
        },
        _attachedGroupEvents: {},
        attachGroupEvent: function(i, s, a, o) {
          r._attachedGroupEvents.hasOwnProperty(i) || (r._attachedGroupEvents[i] = []), r._attachedGroupEvents[i].push([s, a, o]), s.addEventListener(a, o, !1);
        },
        detachGroupEvents: function(i) {
          if (r._attachedGroupEvents.hasOwnProperty(i)) {
            for (var s = 0; s < r._attachedGroupEvents[i].length; s += 1) {
              var a = r._attachedGroupEvents[i][s];
              a[0].removeEventListener(a[1], a[2], !1);
            }
            delete r._attachedGroupEvents[i];
          }
        },
        preventDefault: function(i) {
          i.preventDefault && i.preventDefault(), i.returnValue = !1;
        },
        triggerEvent: function(i, s, a, o) {
          if (i) {
            var l = null;
            return typeof Event == "function" ? l = new Event(s, {
              bubbles: a,
              cancelable: o
            }) : (l = n.document.createEvent("Event"), l.initEvent(s, a, o)), l ? (r.setData(l, "internal", !0), i.dispatchEvent(l), !0) : !1;
          }
        },
        triggerInputEvent: function(i, s, a, o) {
          i && r.isTextInput(i) && r.triggerEvent(i, s, a, o);
        },
        eventKey: function(i) {
          var s = {
            9: "Tab",
            13: "Enter",
            27: "Escape"
          };
          return typeof i.code == "string" ? i.code : i.keyCode !== void 0 && s.hasOwnProperty(i.keyCode) ? s[i.keyCode] : null;
        },
        strList: function(i) {
          return i ? i.replace(/^\s+|\s+$/g, "").split(/\s+/) : [];
        },
        // The className parameter (str) can only contain a single class name
        hasClass: function(i, s) {
          return s ? i.classList !== void 0 ? i.classList.contains(s) : (" " + i.className.replace(/\s+/g, " ") + " ").indexOf(" " + s + " ") != -1 : !1;
        },
        // The className parameter (str) can contain multiple class names separated by whitespace
        addClass: function(i, s) {
          var a = r.strList(s);
          if (i.classList !== void 0) {
            for (var o = 0; o < a.length; o += 1)
              i.classList.add(a[o]);
            return;
          }
          for (var o = 0; o < a.length; o += 1)
            r.hasClass(i, a[o]) || (i.className += (i.className ? " " : "") + a[o]);
        },
        // The className parameter (str) can contain multiple class names separated by whitespace
        removeClass: function(i, s) {
          var a = r.strList(s);
          if (i.classList !== void 0) {
            for (var o = 0; o < a.length; o += 1)
              i.classList.remove(a[o]);
            return;
          }
          for (var o = 0; o < a.length; o += 1) {
            var l = new RegExp(
              "^\\s*" + a[o] + "\\s*|\\s*" + a[o] + "\\s*$|\\s+" + a[o] + "(\\s+)",
              "g"
            );
            i.className = i.className.replace(l, "$1");
          }
        },
        getCompStyle: function(i) {
          var s = n.getComputedStyle ? n.getComputedStyle(i) : i.currentStyle;
          return s || {};
        },
        // Note:
        //   Setting a property to NULL reverts it to the state before it was first set
        //   with the 'reversible' flag enabled
        //
        setStyle: function(i, s, a, o) {
          var l = a ? "important" : "", c = null;
          for (var f in s)
            if (s.hasOwnProperty(f)) {
              var g = null;
              s[f] === null ? (c || (c = r.getData(i, "origStyle")), c && c.hasOwnProperty(f) && (g = c[f])) : (o && (c || (c = r.getData(i, "origStyle", {})), c.hasOwnProperty(f) || (c[f] = i.style[f])), g = s[f]), g !== null && i.style.setProperty(f, g, l);
            }
        },
        appendCss: function(i) {
          var s = document.querySelector("head"), a = document.createElement("style");
          a.innerText = i, s.appendChild(a);
        },
        appendDefaultCss: function(i) {
          r.appendCss(
            [
              ".jscolor-wrap, .jscolor-wrap div, .jscolor-wrap canvas { position:static; display:block; visibility:visible; overflow:visible; margin:0; padding:0; border:none; border-radius:0; outline:none; z-index:auto; float:none; width:auto; height:auto; left:auto; right:auto; top:auto; bottom:auto; min-width:0; min-height:0; max-width:none; max-height:none; background:none; clip:auto; opacity:1; transform:none; box-shadow:none; box-sizing:content-box; }",
              ".jscolor-wrap { clear:both; }",
              ".jscolor-wrap .jscolor-picker { position:relative; }",
              ".jscolor-wrap .jscolor-shadow { position:absolute; left:0; top:0; width:100%; height:100%; }",
              ".jscolor-wrap .jscolor-border { position:relative; }",
              ".jscolor-wrap .jscolor-palette { position:absolute; }",
              ".jscolor-wrap .jscolor-palette-sw { position:absolute; display:block; cursor:pointer; }",
              ".jscolor-wrap .jscolor-btn { position:absolute; overflow:hidden; white-space:nowrap; font:13px sans-serif; text-align:center; cursor:pointer; }"
            ].join(`
`)
          );
        },
        hexColor: function(i, s, a) {
          return "#" + (("0" + Math.round(i).toString(16)).slice(-2) + ("0" + Math.round(s).toString(16)).slice(-2) + ("0" + Math.round(a).toString(16)).slice(-2)).toUpperCase();
        },
        hexaColor: function(i, s, a, o) {
          return "#" + (("0" + Math.round(i).toString(16)).slice(-2) + ("0" + Math.round(s).toString(16)).slice(-2) + ("0" + Math.round(a).toString(16)).slice(-2) + ("0" + Math.round(o * 255).toString(16)).slice(-2)).toUpperCase();
        },
        rgbColor: function(i, s, a) {
          return "rgb(" + Math.round(i) + "," + Math.round(s) + "," + Math.round(a) + ")";
        },
        rgbaColor: function(i, s, a, o) {
          return "rgba(" + Math.round(i) + "," + Math.round(s) + "," + Math.round(a) + "," + Math.round((o ?? 1) * 100) / 100 + ")";
        },
        linearGradient: function() {
          function i() {
            for (var a = "linear-gradient", o = ["", "-webkit-", "-moz-", "-o-", "-ms-"], l = n.document.createElement("div"), c = 0; c < o.length; c += 1) {
              var f = o[c] + a, g = f + "(to right, rgba(0,0,0,0), rgba(0,0,0,0))";
              if (l.style.background = g, l.style.background)
                return f;
            }
            return a;
          }
          var s = i();
          return function() {
            return s + "(" + Array.prototype.join.call(arguments, ", ") + ")";
          };
        }(),
        setBorderRadius: function(i, s) {
          r.setStyle(i, { "border-radius": s || "0" });
        },
        setBoxShadow: function(i, s) {
          r.setStyle(i, { "box-shadow": s || "none" });
        },
        getElementPos: function(i, s) {
          var a = 0, o = 0, l = i.getBoundingClientRect();
          if (a = l.left, o = l.top, !s) {
            var c = r.getViewPos();
            a += c[0], o += c[1];
          }
          return [a, o];
        },
        getElementSize: function(i) {
          return [i.offsetWidth, i.offsetHeight];
        },
        // get pointer's X/Y coordinates relative to viewport
        getAbsPointerPos: function(i) {
          var s = 0, a = 0;
          return typeof i.changedTouches < "u" && i.changedTouches.length ? (s = i.changedTouches[0].clientX, a = i.changedTouches[0].clientY) : typeof i.clientX == "number" && (s = i.clientX, a = i.clientY), { x: s, y: a };
        },
        // get pointer's X/Y coordinates relative to target element
        getRelPointerPos: function(i) {
          var s = i.target || i.srcElement, a = s.getBoundingClientRect(), o = 0, l = 0, c = 0, f = 0;
          return typeof i.changedTouches < "u" && i.changedTouches.length ? (c = i.changedTouches[0].clientX, f = i.changedTouches[0].clientY) : typeof i.clientX == "number" && (c = i.clientX, f = i.clientY), o = c - a.left, l = f - a.top, { x: o, y: l };
        },
        getViewPos: function() {
          var i = n.document.documentElement;
          return [
            (n.pageXOffset || i.scrollLeft) - (i.clientLeft || 0),
            (n.pageYOffset || i.scrollTop) - (i.clientTop || 0)
          ];
        },
        getViewSize: function() {
          var i = n.document.documentElement;
          return [
            n.innerWidth || i.clientWidth,
            n.innerHeight || i.clientHeight
          ];
        },
        // r: 0-255
        // g: 0-255
        // b: 0-255
        //
        // returns: [ 0-360, 0-100, 0-100 ]
        //
        RGB_HSV: function(i, s, a) {
          i /= 255, s /= 255, a /= 255;
          var o = Math.min(Math.min(i, s), a), l = Math.max(Math.max(i, s), a), c = l - o;
          if (c === 0)
            return [null, 0, 100 * l];
          var f = i === o ? 3 + (a - s) / c : s === o ? 5 + (i - a) / c : 1 + (s - i) / c;
          return [
            60 * (f === 6 ? 0 : f),
            100 * (c / l),
            100 * l
          ];
        },
        // h: 0-360
        // s: 0-100
        // v: 0-100
        //
        // returns: [ 0-255, 0-255, 0-255 ]
        //
        HSV_RGB: function(i, s, a) {
          var o = 255 * (a / 100);
          if (i === null)
            return [o, o, o];
          i /= 60, s /= 100;
          var l = Math.floor(i), c = l % 2 ? i - l : 1 - (i - l), f = o * (1 - s), g = o * (1 - s * c);
          switch (l) {
            case 6:
            case 0:
              return [o, g, f];
            case 1:
              return [g, o, f];
            case 2:
              return [f, o, g];
            case 3:
              return [f, g, o];
            case 4:
              return [g, f, o];
            case 5:
              return [o, f, g];
          }
        },
        parseColorString: function(i) {
          var s = {
            rgba: null,
            format: null
            // 'hex' | 'hexa' | 'rgb' | 'rgba'
          }, a;
          if (a = i.match(/^\W*([0-9A-F]{3,8})\W*$/i)) {
            if (a[1].length === 8)
              s.format = "hexa", s.rgba = [
                parseInt(a[1].slice(0, 2), 16),
                parseInt(a[1].slice(2, 4), 16),
                parseInt(a[1].slice(4, 6), 16),
                parseInt(a[1].slice(6, 8), 16) / 255
              ];
            else if (a[1].length === 6)
              s.format = "hex", s.rgba = [
                parseInt(a[1].slice(0, 2), 16),
                parseInt(a[1].slice(2, 4), 16),
                parseInt(a[1].slice(4, 6), 16),
                null
              ];
            else if (a[1].length === 3)
              s.format = "hex", s.rgba = [
                parseInt(a[1].charAt(0) + a[1].charAt(0), 16),
                parseInt(a[1].charAt(1) + a[1].charAt(1), 16),
                parseInt(a[1].charAt(2) + a[1].charAt(2), 16),
                null
              ];
            else
              return !1;
            return s;
          }
          if (a = i.match(/^\W*rgba?\(([^)]*)\)\W*$/i)) {
            var o = a[1].split(","), l = /^\s*(\d+|\d*\.\d+|\d+\.\d*)\s*$/, c, f, g, b;
            if (o.length >= 3 && (c = o[0].match(l)) && (f = o[1].match(l)) && (g = o[2].match(l)))
              return s.format = "rgb", s.rgba = [
                parseFloat(c[1]) || 0,
                parseFloat(f[1]) || 0,
                parseFloat(g[1]) || 0,
                null
              ], o.length >= 4 && (b = o[3].match(l)) && (s.format = "rgba", s.rgba[3] = parseFloat(b[1]) || 0), s;
          }
          return !1;
        },
        parsePaletteValue: function(i) {
          var s = [];
          typeof i == "string" ? i.replace(/#[0-9A-F]{3}\b|#[0-9A-F]{6}([0-9A-F]{2})?\b|rgba?\(([^)]*)\)/ig, function(c) {
            s.push(c);
          }) : Array.isArray(i) && (s = i);
          for (var a = [], o = 0; o < s.length; o++) {
            var l = r.parseColorString(s[o]);
            l && a.push(l);
          }
          return a;
        },
        containsTranparentColor: function(i) {
          for (var s = 0; s < i.length; s++) {
            var a = i[s].rgba[3];
            if (a !== null && a < 1)
              return !0;
          }
          return !1;
        },
        isAlphaFormat: function(i) {
          switch (i.toLowerCase()) {
            case "hexa":
            case "rgba":
              return !0;
          }
          return !1;
        },
        // Canvas scaling for retina displays
        //
        // adapted from https://www.html5rocks.com/en/tutorials/canvas/hidpi/
        //
        scaleCanvasForHighDPR: function(i) {
          var s = n.devicePixelRatio || 1;
          i.width *= s, i.height *= s;
          var a = i.getContext("2d");
          a.scale(s, s);
        },
        genColorPreviewCanvas: function(i, s, a, o) {
          var l = Math.round(r.pub.previewSeparator.length), c = r.pub.chessboardSize, f = r.pub.chessboardColor1, g = r.pub.chessboardColor2, b = a || c * 2, w = c * 2, C = r.createEl("canvas"), m = C.getContext("2d");
          C.width = b, C.height = w, o && r.scaleCanvasForHighDPR(C), m.fillStyle = f, m.fillRect(0, 0, b, w), m.fillStyle = g;
          for (var h = 0; h < b; h += c * 2)
            m.fillRect(h, 0, c, c), m.fillRect(h + c, c, c, c);
          i && (m.fillStyle = i, m.fillRect(0, 0, b, w));
          var u = null;
          switch (s) {
            case "left":
              u = 0, m.clearRect(0, 0, l / 2, w);
              break;
            case "right":
              u = b - l, m.clearRect(b - l / 2, 0, l / 2, w);
              break;
          }
          if (u !== null) {
            m.lineWidth = 1;
            for (var p = 0; p < r.pub.previewSeparator.length; p += 1)
              m.beginPath(), m.strokeStyle = r.pub.previewSeparator[p], m.moveTo(0.5 + u + p, 0), m.lineTo(0.5 + u + p, w), m.stroke();
          }
          return {
            canvas: C,
            width: b,
            height: w
          };
        },
        // if position or width is not set => fill the entire element (0%-100%)
        genColorPreviewGradient: function(i, s, a) {
          var o = [];
          return s && a ? o = [
            "to " + { left: "right", right: "left" }[s],
            i + " 0%",
            i + " " + a + "px",
            "rgba(0,0,0,0) " + (a + 1) + "px",
            "rgba(0,0,0,0) 100%"
          ] : o = [
            "to right",
            i + " 0%",
            i + " 100%"
          ], r.linearGradient.apply(this, o);
        },
        redrawPosition: function() {
          if (!(!r.picker || !r.picker.owner)) {
            var i = r.picker.owner;
            if (i.container !== n.document.body)
              r._drawPosition(i, 0, 0, "relative", !1);
            else {
              var s, a;
              i.fixed ? (s = r.getElementPos(i.targetElement, !0), a = [0, 0]) : (s = r.getElementPos(i.targetElement), a = r.getViewPos());
              var o = r.getElementSize(i.targetElement), l = r.getViewSize(), c = r.getPickerDims(i), f = [c.borderW, c.borderH], g, b, w;
              switch (i.position.toLowerCase()) {
                case "left":
                  g = 1, b = 0, w = -1;
                  break;
                case "right":
                  g = 1, b = 0, w = 1;
                  break;
                case "top":
                  g = 0, b = 1, w = -1;
                  break;
                default:
                  g = 0, b = 1, w = 1;
                  break;
              }
              var C = (o[b] + f[b]) / 2;
              if (i.smartPosition)
                var m = [
                  -a[g] + s[g] + f[g] > l[g] && -a[g] + s[g] + o[g] / 2 > l[g] / 2 && s[g] + o[g] - f[g] >= 0 ? s[g] + o[g] - f[g] : s[g],
                  -a[b] + s[b] + o[b] + f[b] - C + C * w > l[b] ? -a[b] + s[b] + o[b] / 2 > l[b] / 2 && s[b] + o[b] - C - C * w >= 0 ? s[b] + o[b] - C - C * w : s[b] + o[b] - C + C * w : s[b] + o[b] - C + C * w >= 0 ? s[b] + o[b] - C + C * w : s[b] + o[b] - C - C * w
                ];
              else
                var m = [
                  s[g],
                  s[b] + o[b] - C + C * w
                ];
              var h = m[g], u = m[b], p = i.fixed ? "fixed" : "absolute", v = (m[0] + f[0] > s[0] || m[0] < s[0] + o[0]) && m[1] + f[1] < s[1] + o[1];
              r._drawPosition(i, h, u, p, v);
            }
          }
        },
        _drawPosition: function(i, s, a, o, l) {
          var c = l ? 0 : i.shadowBlur;
          r.picker.wrap.style.position = o, // To avoid unnecessary repositioning during scroll
          (Math.round(parseFloat(r.picker.wrap.style.left)) !== Math.round(s) || Math.round(parseFloat(r.picker.wrap.style.top)) !== Math.round(a)) && (r.picker.wrap.style.left = s + "px", r.picker.wrap.style.top = a + "px"), r.setBoxShadow(
            r.picker.boxS,
            i.shadow ? new r.BoxShadow(0, c, i.shadowBlur, 0, i.shadowColor) : null
          );
        },
        getPickerDims: function(i) {
          var s = 2 * i.controlBorderWidth + i.width, a = 2 * i.controlBorderWidth + i.height, o = 2 * i.controlBorderWidth + 2 * r.getControlPadding(i) + i.sliderSize;
          r.getSliderChannel(i) && (s += o), i.hasAlphaChannel() && (s += o);
          var l = r.getPaletteDims(i, s);
          l.height && (a += l.height + i.padding), i.closeButton && (a += 2 * i.controlBorderWidth + i.padding + i.buttonHeight);
          var c = s + 2 * i.padding, f = a + 2 * i.padding;
          return {
            contentW: s,
            contentH: a,
            paddedW: c,
            paddedH: f,
            borderW: c + 2 * i.borderWidth,
            borderH: f + 2 * i.borderWidth,
            palette: l
          };
        },
        getPaletteDims: function(i, s) {
          var a = 0, o = 0, l = 0, c = 0, f = 0, g = i._palette ? i._palette.length : 0;
          return g && (a = i.paletteCols, o = a > 0 ? Math.ceil(g / a) : 0, l = Math.max(1, Math.floor((s - (a - 1) * i.paletteSpacing) / a)), c = i.paletteHeight ? Math.min(i.paletteHeight, l) : l), o && (f = o * c + (o - 1) * i.paletteSpacing), {
            cols: a,
            rows: o,
            cellW: l,
            cellH: c,
            width: s,
            height: f
          };
        },
        getControlPadding: function(i) {
          return Math.max(
            i.padding / 2,
            2 * i.pointerBorderWidth + i.pointerThickness - i.controlBorderWidth
          );
        },
        getPadYChannel: function(i) {
          switch (i.mode.charAt(1).toLowerCase()) {
            case "v":
              return "v";
          }
          return "s";
        },
        getSliderChannel: function(i) {
          if (i.mode.length > 2)
            switch (i.mode.charAt(2).toLowerCase()) {
              case "s":
                return "s";
              case "v":
                return "v";
            }
          return null;
        },
        // calls function specified in picker's property
        triggerCallback: function(i, s) {
          if (i[s]) {
            var a = null;
            if (typeof i[s] == "string")
              try {
                a = new Function(i[s]);
              } catch (o) {
                console.error(o);
              }
            else
              a = i[s];
            a && a.call(i);
          }
        },
        // Triggers a color change related event(s) on all picker instances.
        // It is possible to specify multiple events separated with a space.
        triggerGlobal: function(i) {
          for (var s = r.getInstances(), a = 0; a < s.length; a += 1)
            s[a].trigger(i);
        },
        _pointerMoveEvent: {
          mouse: "mousemove",
          touch: "touchmove"
        },
        _pointerEndEvent: {
          mouse: "mouseup",
          touch: "touchend"
        },
        _pointerOrigin: null,
        onDocumentKeyUp: function(i) {
          ["Tab", "Escape"].indexOf(r.eventKey(i)) !== -1 && r.picker && r.picker.owner && r.picker.owner.tryHide();
        },
        onWindowResize: function(i) {
          r.redrawPosition();
        },
        onWindowScroll: function(i) {
          r.redrawPosition();
        },
        onParentScroll: function(i) {
          r.picker && r.picker.owner && r.picker.owner.tryHide();
        },
        onDocumentMouseDown: function(i) {
          var s = i.target || i.srcElement;
          if (s.jscolor && s.jscolor instanceof r.pub)
            s.jscolor.showOnClick && !s.disabled && s.jscolor.show();
          else if (r.getData(s, "gui")) {
            var a = r.getData(s, "control");
            a && r.onControlPointerStart(i, s, r.getData(s, "control"), "mouse");
          } else
            r.picker && r.picker.owner && r.picker.owner.tryHide();
        },
        onPickerTouchStart: function(i) {
          var s = i.target || i.srcElement;
          r.getData(s, "control") && r.onControlPointerStart(i, s, r.getData(s, "control"), "touch");
        },
        onControlPointerStart: function(i, s, a, o) {
          var l = r.getData(s, "instance");
          r.preventDefault(i);
          var c = function(C, m) {
            r.attachGroupEvent(
              "drag",
              C,
              r._pointerMoveEvent[o],
              r.onDocumentPointerMove(i, s, a, o, m)
            ), r.attachGroupEvent(
              "drag",
              C,
              r._pointerEndEvent[o],
              r.onDocumentPointerEnd(i, s, a, o)
            );
          };
          if (c(n.document, [0, 0]), n.parent && n.frameElement) {
            var f = n.frameElement.getBoundingClientRect(), g = [-f.left, -f.top];
            c(n.parent.window.document, g);
          }
          var b = r.getAbsPointerPos(i), w = r.getRelPointerPos(i);
          switch (r._pointerOrigin = {
            x: b.x - w.x,
            y: b.y - w.y
          }, a) {
            case "pad":
              r.getSliderChannel(l) === "v" && l.channels.v === 0 && l.fromHSVA(null, null, 100, null), r.setPad(l, i, 0, 0);
              break;
            case "sld":
              r.setSld(l, i, 0);
              break;
            case "asld":
              r.setASld(l, i, 0);
              break;
          }
          l.trigger("input");
        },
        onDocumentPointerMove: function(i, s, a, o, l) {
          return function(c) {
            var f = r.getData(s, "instance");
            switch (a) {
              case "pad":
                r.setPad(f, c, l[0], l[1]);
                break;
              case "sld":
                r.setSld(f, c, l[1]);
                break;
              case "asld":
                r.setASld(f, c, l[1]);
                break;
            }
            f.trigger("input");
          };
        },
        onDocumentPointerEnd: function(i, s, a, o) {
          return function(l) {
            var c = r.getData(s, "instance");
            r.detachGroupEvents("drag"), c.trigger("input"), c.trigger("change");
          };
        },
        onPaletteSampleClick: function(i) {
          var s = i.currentTarget, a = r.getData(s, "instance"), o = r.getData(s, "color");
          a.format.toLowerCase() === "any" && (a._setFormat(o.format), r.isAlphaFormat(a.getFormat()) || (o.rgba[3] = 1)), o.rgba[3] === null && (a.paletteSetsAlpha === !0 || a.paletteSetsAlpha === "auto" && a._paletteHasTransparency) && (o.rgba[3] = 1), a.fromRGBA.apply(a, o.rgba), a.trigger("input"), a.trigger("change"), a.hideOnPaletteClick && a.hide();
        },
        setPad: function(i, s, a, o) {
          var l = r.getAbsPointerPos(s), c = a + l.x - r._pointerOrigin.x - i.padding - i.controlBorderWidth, f = o + l.y - r._pointerOrigin.y - i.padding - i.controlBorderWidth, g = c * (360 / (i.width - 1)), b = 100 - f * (100 / (i.height - 1));
          switch (r.getPadYChannel(i)) {
            case "s":
              i.fromHSVA(g, b, null, null);
              break;
            case "v":
              i.fromHSVA(g, null, b, null);
              break;
          }
        },
        setSld: function(i, s, a) {
          var o = r.getAbsPointerPos(s), l = a + o.y - r._pointerOrigin.y - i.padding - i.controlBorderWidth, c = 100 - l * (100 / (i.height - 1));
          switch (r.getSliderChannel(i)) {
            case "s":
              i.fromHSVA(null, c, null, null);
              break;
            case "v":
              i.fromHSVA(null, null, c, null);
              break;
          }
        },
        setASld: function(i, s, a) {
          var o = r.getAbsPointerPos(s), l = a + o.y - r._pointerOrigin.y - i.padding - i.controlBorderWidth, c = 1 - l * (1 / (i.height - 1));
          if (c < 1) {
            var f = i.getFormat();
            i.format.toLowerCase() === "any" && !r.isAlphaFormat(f) && i._setFormat(f === "hex" ? "hexa" : "rgba");
          }
          i.fromHSVA(null, null, null, c);
        },
        createPadCanvas: function() {
          var i = {
            elm: null,
            draw: null
          }, s = r.createEl("canvas"), a = s.getContext("2d"), o = function(l, c, f) {
            s.width = l, s.height = c, a.clearRect(0, 0, s.width, s.height);
            var g = a.createLinearGradient(0, 0, s.width, 0);
            g.addColorStop(0 / 6, "#F00"), g.addColorStop(1 / 6, "#FF0"), g.addColorStop(2 / 6, "#0F0"), g.addColorStop(3 / 6, "#0FF"), g.addColorStop(4 / 6, "#00F"), g.addColorStop(5 / 6, "#F0F"), g.addColorStop(6 / 6, "#F00"), a.fillStyle = g, a.fillRect(0, 0, s.width, s.height);
            var b = a.createLinearGradient(0, 0, 0, s.height);
            switch (f.toLowerCase()) {
              case "s":
                b.addColorStop(0, "rgba(255,255,255,0)"), b.addColorStop(1, "rgba(255,255,255,1)");
                break;
              case "v":
                b.addColorStop(0, "rgba(0,0,0,0)"), b.addColorStop(1, "rgba(0,0,0,1)");
                break;
            }
            a.fillStyle = b, a.fillRect(0, 0, s.width, s.height);
          };
          return i.elm = s, i.draw = o, i;
        },
        createSliderGradient: function() {
          var i = {
            elm: null,
            draw: null
          }, s = r.createEl("canvas"), a = s.getContext("2d"), o = function(l, c, f, g) {
            s.width = l, s.height = c, a.clearRect(0, 0, s.width, s.height);
            var b = a.createLinearGradient(0, 0, 0, s.height);
            b.addColorStop(0, f), b.addColorStop(1, g), a.fillStyle = b, a.fillRect(0, 0, s.width, s.height);
          };
          return i.elm = s, i.draw = o, i;
        },
        createASliderGradient: function() {
          var i = {
            elm: null,
            draw: null
          }, s = r.createEl("canvas"), a = s.getContext("2d"), o = function(l, c, f) {
            s.width = l, s.height = c, a.clearRect(0, 0, s.width, s.height);
            var g = s.width / 2, b = r.pub.chessboardColor1, w = r.pub.chessboardColor2;
            if (a.fillStyle = b, a.fillRect(0, 0, s.width, s.height), g > 0)
              for (var C = 0; C < s.height; C += g * 2)
                a.fillStyle = w, a.fillRect(0, C, g, g), a.fillRect(g, C + g, g, g);
            var m = a.createLinearGradient(0, 0, 0, s.height);
            m.addColorStop(0, f), m.addColorStop(1, "rgba(0,0,0,0)"), a.fillStyle = m, a.fillRect(0, 0, s.width, s.height);
          };
          return i.elm = s, i.draw = o, i;
        },
        BoxShadow: function() {
          var i = function(s, a, o, l, c, f) {
            this.hShadow = s, this.vShadow = a, this.blur = o, this.spread = l, this.color = c, this.inset = !!f;
          };
          return i.prototype.toString = function() {
            var s = [
              Math.round(this.hShadow) + "px",
              Math.round(this.vShadow) + "px",
              Math.round(this.blur) + "px",
              Math.round(this.spread) + "px",
              this.color
            ];
            return this.inset && s.push("inset"), s.join(" ");
          }, i;
        }(),
        flags: {
          leaveValue: 1,
          leaveAlpha: 2,
          leavePreview: 4
        },
        enumOpts: {
          format: ["auto", "any", "hex", "hexa", "rgb", "rgba"],
          previewPosition: ["left", "right"],
          mode: ["hsv", "hvs", "hs", "hv"],
          position: ["left", "right", "top", "bottom"],
          alphaChannel: ["auto", !0, !1],
          paletteSetsAlpha: ["auto", !0, !1]
        },
        deprecatedOpts: {
          // <old_option>: <new_option>  (<new_option> can be null)
          styleElement: "previewElement",
          onFineChange: "onInput",
          overwriteImportant: "forceStyle",
          closable: "closeButton",
          insetWidth: "controlBorderWidth",
          insetColor: "controlBorderColor",
          refine: null
        },
        docsRef: " See https://jscolor.com/docs/",
        //
        // Usage:
        // var myPicker = new JSColor(<targetElement> [, <options>])
        //
        // (constructor is accessible via both 'jscolor' and 'JSColor' name)
        //
        pub: function(i, s) {
          var a = this;
          s || (s = {}), this.channels = {
            r: 255,
            // red [0-255]
            g: 255,
            // green [0-255]
            b: 255,
            // blue [0-255]
            h: 0,
            // hue [0-360]
            s: 0,
            // saturation [0-100]
            v: 100,
            // value (brightness) [0-100]
            a: 1
            // alpha (opacity) [0.0 - 1.0]
          }, this.format = "auto", this.value = void 0, this.alpha = void 0, this.random = !1, this.onChange = void 0, this.onInput = void 0, this.valueElement = void 0, this.alphaElement = void 0, this.previewElement = void 0, this.previewPosition = "left", this.previewSize = 32, this.previewPadding = 8, this.required = !0, this.hash = !0, this.uppercase = !0, this.forceStyle = !0, this.width = 181, this.height = 101, this.mode = "HSV", this.alphaChannel = "auto", this.position = "bottom", this.smartPosition = !0, this.showOnClick = !0, this.hideOnLeave = !0, this.palette = [], this.paletteCols = 10, this.paletteSetsAlpha = "auto", this.paletteHeight = 16, this.paletteSpacing = 4, this.hideOnPaletteClick = !1, this.sliderSize = 16, this.crossSize = 8, this.closeButton = !1, this.closeText = "Close", this.buttonColor = "rgba(0,0,0,1)", this.buttonHeight = 18, this.padding = 12, this.backgroundColor = "rgba(255,255,255,1)", this.borderWidth = 1, this.borderColor = "rgba(187,187,187,1)", this.borderRadius = 8, this.controlBorderWidth = 1, this.controlBorderColor = "rgba(187,187,187,1)", this.shadow = !0, this.shadowBlur = 15, this.shadowColor = "rgba(0,0,0,0.2)", this.pointerColor = "rgba(76,76,76,1)", this.pointerBorderWidth = 1, this.pointerBorderColor = "rgba(255,255,255,1)", this.pointerThickness = 2, this.zIndex = 5e3, this.container = void 0, this.minS = 0, this.maxS = 100, this.minV = 0, this.maxV = 100, this.minA = 0, this.maxA = 1, this.option = function() {
            if (!arguments.length)
              throw new Error("No option specified");
            if (arguments.length === 1 && typeof arguments[0] == "string") {
              try {
                return l(arguments[0]);
              } catch (Y) {
                console.warn(Y);
              }
              return !1;
            } else if (arguments.length >= 2 && typeof arguments[0] == "string") {
              try {
                if (!o(arguments[0], arguments[1]))
                  return !1;
              } catch (Y) {
                return console.warn(Y), !1;
              }
              return this.redraw(), this.exposeColor(), !0;
            } else if (arguments.length === 1 && typeof arguments[0] == "object") {
              var E = arguments[0], M = !0;
              for (var W in E)
                if (E.hasOwnProperty(W))
                  try {
                    o(W, E[W]) || (M = !1);
                  } catch (Y) {
                    console.warn(Y), M = !1;
                  }
              return this.redraw(), this.exposeColor(), M;
            }
            throw new Error("Invalid arguments");
          }, this.channel = function(E, M) {
            if (typeof E != "string")
              throw new Error("Invalid value for channel name: " + E);
            if (M === void 0)
              return this.channels.hasOwnProperty(E.toLowerCase()) ? this.channels[E.toLowerCase()] : (console.warn("Getting unknown channel: " + E), !1);
            var W = !1;
            switch (E.toLowerCase()) {
              case "r":
                W = this.fromRGBA(M, null, null, null);
                break;
              case "g":
                W = this.fromRGBA(null, M, null, null);
                break;
              case "b":
                W = this.fromRGBA(null, null, M, null);
                break;
              case "h":
                W = this.fromHSVA(M, null, null, null);
                break;
              case "s":
                W = this.fromHSVA(null, M, null, null);
                break;
              case "v":
                W = this.fromHSVA(null, null, M, null);
                break;
              case "a":
                W = this.fromHSVA(null, null, null, M);
                break;
              default:
                return console.warn("Setting unknown channel: " + E), !1;
            }
            return W ? (this.redraw(), !0) : !1;
          }, this.trigger = function(E) {
            for (var M = r.strList(E), W = 0; W < M.length; W += 1) {
              var Y = M[W].toLowerCase(), J = null;
              switch (Y) {
                case "input":
                  J = "onInput";
                  break;
                case "change":
                  J = "onChange";
                  break;
              }
              J && r.triggerCallback(this, J), r.triggerInputEvent(this.valueElement, Y, !0, !0);
            }
          }, this.fromHSVA = function(E, M, W, Y, J) {
            if (E === void 0 && (E = null), M === void 0 && (M = null), W === void 0 && (W = null), Y === void 0 && (Y = null), E !== null) {
              if (isNaN(E))
                return !1;
              this.channels.h = Math.max(0, Math.min(360, E));
            }
            if (M !== null) {
              if (isNaN(M))
                return !1;
              this.channels.s = Math.max(0, Math.min(100, this.maxS, M), this.minS);
            }
            if (W !== null) {
              if (isNaN(W))
                return !1;
              this.channels.v = Math.max(0, Math.min(100, this.maxV, W), this.minV);
            }
            if (Y !== null) {
              if (isNaN(Y))
                return !1;
              this.channels.a = this.hasAlphaChannel() ? Math.max(0, Math.min(1, this.maxA, Y), this.minA) : 1;
            }
            var X = r.HSV_RGB(
              this.channels.h,
              this.channels.s,
              this.channels.v
            );
            return this.channels.r = X[0], this.channels.g = X[1], this.channels.b = X[2], this.exposeColor(J), !0;
          }, this.fromRGBA = function(E, M, W, Y, J) {
            if (E === void 0 && (E = null), M === void 0 && (M = null), W === void 0 && (W = null), Y === void 0 && (Y = null), E !== null) {
              if (isNaN(E))
                return !1;
              E = Math.max(0, Math.min(255, E));
            }
            if (M !== null) {
              if (isNaN(M))
                return !1;
              M = Math.max(0, Math.min(255, M));
            }
            if (W !== null) {
              if (isNaN(W))
                return !1;
              W = Math.max(0, Math.min(255, W));
            }
            if (Y !== null) {
              if (isNaN(Y))
                return !1;
              this.channels.a = this.hasAlphaChannel() ? Math.max(0, Math.min(1, this.maxA, Y), this.minA) : 1;
            }
            var X = r.RGB_HSV(
              E === null ? this.channels.r : E,
              M === null ? this.channels.g : M,
              W === null ? this.channels.b : W
            );
            X[0] !== null && (this.channels.h = Math.max(0, Math.min(360, X[0]))), X[2] !== 0 && (this.channels.s = Math.max(0, this.minS, Math.min(100, this.maxS, X[1]))), this.channels.v = Math.max(0, this.minV, Math.min(100, this.maxV, X[2]));
            var Q = r.HSV_RGB(this.channels.h, this.channels.s, this.channels.v);
            return this.channels.r = Q[0], this.channels.g = Q[1], this.channels.b = Q[2], this.exposeColor(J), !0;
          }, this.fromHSV = function(E, M, W, Y) {
            return console.warn("fromHSV() method is DEPRECATED. Using fromHSVA() instead." + r.docsRef), this.fromHSVA(E, M, W, null, Y);
          }, this.fromRGB = function(E, M, W, Y) {
            return console.warn("fromRGB() method is DEPRECATED. Using fromRGBA() instead." + r.docsRef), this.fromRGBA(E, M, W, null, Y);
          }, this.fromString = function(E, M) {
            if (!this.required && E.trim() === "")
              return this.setPreviewElementBg(null), this.setValueElementValue(""), !0;
            var W = r.parseColorString(E);
            return W ? (this.format.toLowerCase() === "any" && (this._setFormat(W.format), r.isAlphaFormat(this.getFormat()) || (W.rgba[3] = 1)), this.fromRGBA(
              W.rgba[0],
              W.rgba[1],
              W.rgba[2],
              W.rgba[3],
              M
            ), !0) : !1;
          }, this.randomize = function(E, M, W, Y, J, X, Q, ee) {
            E === void 0 && (E = 0), M === void 0 && (M = 100), W === void 0 && (W = 0), Y === void 0 && (Y = 100), J === void 0 && (J = 0), X === void 0 && (X = 359), Q === void 0 && (Q = 1), ee === void 0 && (ee = 1), this.fromHSVA(
              J + Math.floor(Math.random() * (X - J + 1)),
              W + Math.floor(Math.random() * (Y - W + 1)),
              E + Math.floor(Math.random() * (M - E + 1)),
              (100 * Q + Math.floor(Math.random() * (100 * (ee - Q) + 1))) / 100
            );
          }, this.toString = function(E) {
            switch (E === void 0 && (E = this.getFormat()), E.toLowerCase()) {
              case "hex":
                return this.toHEXString();
              case "hexa":
                return this.toHEXAString();
              case "rgb":
                return this.toRGBString();
              case "rgba":
                return this.toRGBAString();
            }
            return !1;
          }, this.toHEXString = function() {
            return r.hexColor(
              this.channels.r,
              this.channels.g,
              this.channels.b
            );
          }, this.toHEXAString = function() {
            return r.hexaColor(
              this.channels.r,
              this.channels.g,
              this.channels.b,
              this.channels.a
            );
          }, this.toRGBString = function() {
            return r.rgbColor(
              this.channels.r,
              this.channels.g,
              this.channels.b
            );
          }, this.toRGBAString = function() {
            return r.rgbaColor(
              this.channels.r,
              this.channels.g,
              this.channels.b,
              this.channels.a
            );
          }, this.toGrayscale = function() {
            return 0.213 * this.channels.r + 0.715 * this.channels.g + 0.072 * this.channels.b;
          }, this.toCanvas = function() {
            return r.genColorPreviewCanvas(this.toRGBAString()).canvas;
          }, this.toDataURL = function() {
            return this.toCanvas().toDataURL();
          }, this.toBackground = function() {
            return r.pub.background(this.toRGBAString());
          }, this.isLight = function() {
            return this.toGrayscale() > 127.5;
          }, this.hide = function() {
            C() && c();
          }, this.show = function() {
            f();
          }, this.redraw = function() {
            C() && f();
          }, this.getFormat = function() {
            return this._currentFormat;
          }, this._setFormat = function(E) {
            this._currentFormat = E.toLowerCase();
          }, this.hasAlphaChannel = function() {
            return this.alphaChannel === "auto" ? this.format.toLowerCase() === "any" || // format can change on the fly (e.g. from hex to rgba), so let's consider the alpha channel enabled
            r.isAlphaFormat(this.getFormat()) || // the current format supports alpha channel
            this.alpha !== void 0 || // initial alpha value is set, so we're working with alpha channel
            this.alphaElement !== void 0 : this.alphaChannel;
          }, this.processValueInput = function(E) {
            this.fromString(E) || this.exposeColor();
          }, this.processAlphaInput = function(E) {
            this.fromHSVA(null, null, null, parseFloat(E)) || this.exposeColor();
          }, this.exposeColor = function(E) {
            var M = this.toString(), W = this.getFormat();
            if (r.setDataAttr(this.targetElement, "current-color", M), !(E & r.flags.leaveValue) && this.valueElement && ((W === "hex" || W === "hexa") && (this.uppercase || (M = M.toLowerCase()), this.hash || (M = M.replace(/^#/, ""))), this.setValueElementValue(M)), !(E & r.flags.leaveAlpha) && this.alphaElement) {
              var Y = Math.round(this.channels.a * 100) / 100;
              this.setAlphaElementValue(Y);
            }
            !(E & r.flags.leavePreview) && this.previewElement && ((r.isTextInput(this.previewElement) || // text input
            r.isButton(this.previewElement) && !r.isButtonEmpty(this.previewElement)) && this.previewPosition, this.setPreviewElementBg(this.toRGBAString())), C() && (g(), b(), w());
          }, this.setPreviewElementBg = function(E) {
            if (this.previewElement) {
              var M = null, W = null;
              (r.isTextInput(this.previewElement) || // text input
              r.isButton(this.previewElement) && !r.isButtonEmpty(this.previewElement)) && (M = this.previewPosition, W = this.previewSize);
              var Y = [];
              if (!E)
                Y.push({
                  image: "none",
                  position: "left top",
                  size: "auto",
                  repeat: "no-repeat",
                  origin: "padding-box"
                });
              else {
                Y.push({
                  image: r.genColorPreviewGradient(
                    E,
                    M,
                    W ? W - r.pub.previewSeparator.length : null
                  ),
                  position: "left top",
                  size: "auto",
                  repeat: M ? "repeat-y" : "repeat",
                  origin: "padding-box"
                });
                var J = r.genColorPreviewCanvas(
                  "rgba(0,0,0,0)",
                  M ? { left: "right", right: "left" }[M] : null,
                  W,
                  !0
                );
                Y.push({
                  image: "url('" + J.canvas.toDataURL() + "')",
                  position: (M || "left") + " top",
                  size: J.width + "px " + J.height + "px",
                  repeat: M ? "repeat-y" : "repeat",
                  origin: "padding-box"
                });
              }
              for (var X = {
                image: [],
                position: [],
                size: [],
                repeat: [],
                origin: []
              }, Q = 0; Q < Y.length; Q += 1)
                X.image.push(Y[Q].image), X.position.push(Y[Q].position), X.size.push(Y[Q].size), X.repeat.push(Y[Q].repeat), X.origin.push(Y[Q].origin);
              var ie = {
                "background-image": X.image.join(", "),
                "background-position": X.position.join(", "),
                "background-size": X.size.join(", "),
                "background-repeat": X.repeat.join(", "),
                "background-origin": X.origin.join(", ")
              };
              r.setStyle(this.previewElement, ie, this.forceStyle);
              var ee = {
                left: null,
                right: null
              };
              M && (ee[M] = this.previewSize + this.previewPadding + "px");
              var ie = {
                "padding-left": ee.left,
                "padding-right": ee.right
              };
              r.setStyle(this.previewElement, ie, this.forceStyle, !0);
            }
          }, this.setValueElementValue = function(E) {
            this.valueElement && (r.nodeName(this.valueElement) === "input" ? this.valueElement.value = E : this.valueElement.innerHTML = E);
          }, this.setAlphaElementValue = function(E) {
            this.alphaElement && (r.nodeName(this.alphaElement) === "input" ? this.alphaElement.value = E : this.alphaElement.innerHTML = E);
          }, this._processParentElementsInDOM = function() {
            if (!this._parentElementsProcessed) {
              this._parentElementsProcessed = !0;
              var E = this.targetElement;
              do {
                var M = r.getCompStyle(E);
                M.position && M.position.toLowerCase() === "fixed" && (this.fixed = !0), E !== this.targetElement && (r.getData(E, "hasScrollListener") || (E.addEventListener("scroll", r.onParentScroll, !1), r.setData(E, "hasScrollListener", !0)));
              } while ((E = E.parentNode) && r.nodeName(E) !== "body");
            }
          }, this.tryHide = function() {
            this.hideOnLeave && this.hide();
          }, this.set__palette = function(E) {
            this.palette = E, this._palette = r.parsePaletteValue(E), this._paletteHasTransparency = r.containsTranparentColor(this._palette);
          };
          function o(E, M) {
            if (typeof E != "string")
              throw new Error("Invalid value for option name: " + E);
            if (r.enumOpts.hasOwnProperty(E) && (typeof M == "string" && (M = M.toLowerCase()), r.enumOpts[E].indexOf(M) === -1))
              throw new Error("Option '" + E + "' has invalid value: " + M);
            if (r.deprecatedOpts.hasOwnProperty(E)) {
              var W = E, Y = r.deprecatedOpts[E];
              if (Y)
                console.warn("Option '%s' is DEPRECATED, using '%s' instead." + r.docsRef, W, Y), E = Y;
              else
                throw new Error("Option '" + E + "' is DEPRECATED");
            }
            var J = "set__" + E;
            if (typeof a[J] == "function")
              return a[J](M), !0;
            if (E in a)
              return a[E] = M, !0;
            throw new Error("Unrecognized configuration option: " + E);
          }
          function l(E) {
            if (typeof E != "string")
              throw new Error("Invalid value for option name: " + E);
            if (r.deprecatedOpts.hasOwnProperty(E)) {
              var M = E, W = r.deprecatedOpts[E];
              if (W)
                console.warn("Option '%s' is DEPRECATED, using '%s' instead." + r.docsRef, M, W), E = W;
              else
                throw new Error("Option '" + E + "' is DEPRECATED");
            }
            var Y = "get__" + E;
            if (typeof a[Y] == "function")
              return a[Y](value);
            if (E in a)
              return a[E];
            throw new Error("Unrecognized configuration option: " + E);
          }
          function c() {
            r.removeClass(a.targetElement, r.pub.activeClassName), r.picker.wrap.parentNode.removeChild(r.picker.wrap), delete r.picker.owner;
          }
          function f() {
            a._processParentElementsInDOM(), r.picker || (r.picker = {
              owner: null,
              // owner picker instance
              wrap: r.createEl("div"),
              box: r.createEl("div"),
              boxS: r.createEl("div"),
              // shadow area
              boxB: r.createEl("div"),
              // border
              pad: r.createEl("div"),
              padB: r.createEl("div"),
              // border
              padM: r.createEl("div"),
              // mouse/touch area
              padCanvas: r.createPadCanvas(),
              cross: r.createEl("div"),
              crossBY: r.createEl("div"),
              // border Y
              crossBX: r.createEl("div"),
              // border X
              crossLY: r.createEl("div"),
              // line Y
              crossLX: r.createEl("div"),
              // line X
              sld: r.createEl("div"),
              // slider
              sldB: r.createEl("div"),
              // border
              sldM: r.createEl("div"),
              // mouse/touch area
              sldGrad: r.createSliderGradient(),
              sldPtrS: r.createEl("div"),
              // slider pointer spacer
              sldPtrIB: r.createEl("div"),
              // slider pointer inner border
              sldPtrMB: r.createEl("div"),
              // slider pointer middle border
              sldPtrOB: r.createEl("div"),
              // slider pointer outer border
              asld: r.createEl("div"),
              // alpha slider
              asldB: r.createEl("div"),
              // border
              asldM: r.createEl("div"),
              // mouse/touch area
              asldGrad: r.createASliderGradient(),
              asldPtrS: r.createEl("div"),
              // slider pointer spacer
              asldPtrIB: r.createEl("div"),
              // slider pointer inner border
              asldPtrMB: r.createEl("div"),
              // slider pointer middle border
              asldPtrOB: r.createEl("div"),
              // slider pointer outer border
              pal: r.createEl("div"),
              // palette
              btn: r.createEl("div"),
              btnT: r.createEl("div")
              // text
            }, r.picker.pad.appendChild(r.picker.padCanvas.elm), r.picker.padB.appendChild(r.picker.pad), r.picker.cross.appendChild(r.picker.crossBY), r.picker.cross.appendChild(r.picker.crossBX), r.picker.cross.appendChild(r.picker.crossLY), r.picker.cross.appendChild(r.picker.crossLX), r.picker.padB.appendChild(r.picker.cross), r.picker.box.appendChild(r.picker.padB), r.picker.box.appendChild(r.picker.padM), r.picker.sld.appendChild(r.picker.sldGrad.elm), r.picker.sldB.appendChild(r.picker.sld), r.picker.sldB.appendChild(r.picker.sldPtrOB), r.picker.sldPtrOB.appendChild(r.picker.sldPtrMB), r.picker.sldPtrMB.appendChild(r.picker.sldPtrIB), r.picker.sldPtrIB.appendChild(r.picker.sldPtrS), r.picker.box.appendChild(r.picker.sldB), r.picker.box.appendChild(r.picker.sldM), r.picker.asld.appendChild(r.picker.asldGrad.elm), r.picker.asldB.appendChild(r.picker.asld), r.picker.asldB.appendChild(r.picker.asldPtrOB), r.picker.asldPtrOB.appendChild(r.picker.asldPtrMB), r.picker.asldPtrMB.appendChild(r.picker.asldPtrIB), r.picker.asldPtrIB.appendChild(r.picker.asldPtrS), r.picker.box.appendChild(r.picker.asldB), r.picker.box.appendChild(r.picker.asldM), r.picker.box.appendChild(r.picker.pal), r.picker.btn.appendChild(r.picker.btnT), r.picker.box.appendChild(r.picker.btn), r.picker.boxB.appendChild(r.picker.box), r.picker.wrap.appendChild(r.picker.boxS), r.picker.wrap.appendChild(r.picker.boxB), r.picker.wrap.addEventListener(
              "touchstart",
              r.onPickerTouchStart,
              r.isPassiveEventSupported ? { passive: !1 } : !1
            ));
            var E = r.picker, M = !!r.getSliderChannel(a), W = a.hasAlphaChannel(), Y = r.getPickerDims(a), J = 2 * a.pointerBorderWidth + a.pointerThickness + 2 * a.crossSize, X = r.getControlPadding(a), Q = Math.min(
              a.borderRadius,
              Math.round(a.padding * Math.PI)
            ), ee = "crosshair";
            E.wrap.className = "jscolor-wrap", E.wrap.style.width = Y.borderW + "px", E.wrap.style.height = Y.borderH + "px", E.wrap.style.zIndex = a.zIndex, E.box.className = "jscolor-picker", E.box.style.width = Y.paddedW + "px", E.box.style.height = Y.paddedH + "px", E.boxS.className = "jscolor-shadow", r.setBorderRadius(E.boxS, Q + "px"), E.boxB.className = "jscolor-border", E.boxB.style.border = a.borderWidth + "px solid", E.boxB.style.borderColor = a.borderColor, E.boxB.style.background = a.backgroundColor, r.setBorderRadius(E.boxB, Q + "px"), E.padM.style.background = "rgba(255,0,0,.2)", E.sldM.style.background = "rgba(0,255,0,.2)", E.asldM.style.background = "rgba(0,0,255,.2)", E.padM.style.opacity = E.sldM.style.opacity = E.asldM.style.opacity = "0", E.pad.style.position = "relative", E.pad.style.width = a.width + "px", E.pad.style.height = a.height + "px", E.padCanvas.draw(a.width, a.height, r.getPadYChannel(a)), E.padB.style.position = "absolute", E.padB.style.left = a.padding + "px", E.padB.style.top = a.padding + "px", E.padB.style.border = a.controlBorderWidth + "px solid", E.padB.style.borderColor = a.controlBorderColor, E.padM.style.position = "absolute", E.padM.style.left = "0px", E.padM.style.top = "0px", E.padM.style.width = a.padding + 2 * a.controlBorderWidth + a.width + X + "px", E.padM.style.height = 2 * a.controlBorderWidth + 2 * a.padding + a.height + "px", E.padM.style.cursor = ee, r.setData(E.padM, {
              instance: a,
              control: "pad"
            }), E.cross.style.position = "absolute", E.cross.style.left = E.cross.style.top = "0", E.cross.style.width = E.cross.style.height = J + "px", E.crossBY.style.position = E.crossBX.style.position = "absolute", E.crossBY.style.background = E.crossBX.style.background = a.pointerBorderColor, E.crossBY.style.width = E.crossBX.style.height = 2 * a.pointerBorderWidth + a.pointerThickness + "px", E.crossBY.style.height = E.crossBX.style.width = J + "px", E.crossBY.style.left = E.crossBX.style.top = Math.floor(J / 2) - Math.floor(a.pointerThickness / 2) - a.pointerBorderWidth + "px", E.crossBY.style.top = E.crossBX.style.left = "0", E.crossLY.style.position = E.crossLX.style.position = "absolute", E.crossLY.style.background = E.crossLX.style.background = a.pointerColor, E.crossLY.style.height = E.crossLX.style.width = J - 2 * a.pointerBorderWidth + "px", E.crossLY.style.width = E.crossLX.style.height = a.pointerThickness + "px", E.crossLY.style.left = E.crossLX.style.top = Math.floor(J / 2) - Math.floor(a.pointerThickness / 2) + "px", E.crossLY.style.top = E.crossLX.style.left = a.pointerBorderWidth + "px", E.sld.style.overflow = "hidden", E.sld.style.width = a.sliderSize + "px", E.sld.style.height = a.height + "px", E.sldGrad.draw(a.sliderSize, a.height, "#000", "#000"), E.sldB.style.display = M ? "block" : "none", E.sldB.style.position = "absolute", E.sldB.style.left = a.padding + a.width + 2 * a.controlBorderWidth + 2 * X + "px", E.sldB.style.top = a.padding + "px", E.sldB.style.border = a.controlBorderWidth + "px solid", E.sldB.style.borderColor = a.controlBorderColor, E.sldM.style.display = M ? "block" : "none", E.sldM.style.position = "absolute", E.sldM.style.left = a.padding + a.width + 2 * a.controlBorderWidth + X + "px", E.sldM.style.top = "0px", E.sldM.style.width = a.sliderSize + 2 * X + 2 * a.controlBorderWidth + (W ? 0 : Math.max(0, a.padding - X)) + "px", E.sldM.style.height = 2 * a.controlBorderWidth + 2 * a.padding + a.height + "px", E.sldM.style.cursor = "default", r.setData(E.sldM, {
              instance: a,
              control: "sld"
            }), E.sldPtrIB.style.border = E.sldPtrOB.style.border = a.pointerBorderWidth + "px solid " + a.pointerBorderColor, E.sldPtrOB.style.position = "absolute", E.sldPtrOB.style.left = -(2 * a.pointerBorderWidth + a.pointerThickness) + "px", E.sldPtrOB.style.top = "0", E.sldPtrMB.style.border = a.pointerThickness + "px solid " + a.pointerColor, E.sldPtrS.style.width = a.sliderSize + "px", E.sldPtrS.style.height = r.pub.sliderInnerSpace + "px", E.asld.style.overflow = "hidden", E.asld.style.width = a.sliderSize + "px", E.asld.style.height = a.height + "px", E.asldGrad.draw(a.sliderSize, a.height, "#000"), E.asldB.style.display = W ? "block" : "none", E.asldB.style.position = "absolute", E.asldB.style.left = a.padding + a.width + 2 * a.controlBorderWidth + X + (M ? a.sliderSize + 3 * X + 2 * a.controlBorderWidth : 0) + "px", E.asldB.style.top = a.padding + "px", E.asldB.style.border = a.controlBorderWidth + "px solid", E.asldB.style.borderColor = a.controlBorderColor, E.asldM.style.display = W ? "block" : "none", E.asldM.style.position = "absolute", E.asldM.style.left = a.padding + a.width + 2 * a.controlBorderWidth + X + (M ? a.sliderSize + 2 * X + 2 * a.controlBorderWidth : 0) + "px", E.asldM.style.top = "0px", E.asldM.style.width = a.sliderSize + 2 * X + 2 * a.controlBorderWidth + Math.max(0, a.padding - X) + "px", E.asldM.style.height = 2 * a.controlBorderWidth + 2 * a.padding + a.height + "px", E.asldM.style.cursor = "default", r.setData(E.asldM, {
              instance: a,
              control: "asld"
            }), E.asldPtrIB.style.border = E.asldPtrOB.style.border = a.pointerBorderWidth + "px solid " + a.pointerBorderColor, E.asldPtrOB.style.position = "absolute", E.asldPtrOB.style.left = -(2 * a.pointerBorderWidth + a.pointerThickness) + "px", E.asldPtrOB.style.top = "0", E.asldPtrMB.style.border = a.pointerThickness + "px solid " + a.pointerColor, E.asldPtrS.style.width = a.sliderSize + "px", E.asldPtrS.style.height = r.pub.sliderInnerSpace + "px", E.pal.className = "jscolor-palette", E.pal.style.display = Y.palette.rows ? "block" : "none", E.pal.style.left = a.padding + "px", E.pal.style.top = 2 * a.controlBorderWidth + 2 * a.padding + a.height + "px", E.pal.innerHTML = "";
            for (var ie = r.genColorPreviewCanvas("rgba(0,0,0,0)"), oe = 0, ce = 0; ce < Y.palette.rows; ce++)
              for (var he = 0; he < Y.palette.cols && oe < a._palette.length; he++, oe++) {
                var we = a._palette[oe], Oe = r.rgbaColor.apply(null, we.rgba), Ve = r.createEl("div");
                Ve.style.width = Y.palette.cellW - 2 * a.controlBorderWidth + "px", Ve.style.height = Y.palette.cellH - 2 * a.controlBorderWidth + "px", Ve.style.backgroundColor = Oe;
                var Ie = r.createEl("div");
                Ie.className = "jscolor-palette-sw", Ie.style.left = (Y.palette.cols <= 1 ? 0 : Math.round(10 * (he * ((Y.contentW - Y.palette.cellW) / (Y.palette.cols - 1)))) / 10) + "px", Ie.style.top = ce * (Y.palette.cellH + a.paletteSpacing) + "px", Ie.style.border = a.controlBorderWidth + "px solid", Ie.style.borderColor = a.controlBorderColor, we.rgba[3] !== null && we.rgba[3] < 1 && (Ie.style.backgroundImage = "url('" + ie.canvas.toDataURL() + "')", Ie.style.backgroundRepeat = "repeat", Ie.style.backgroundPosition = "center center"), r.setData(Ie, {
                  instance: a,
                  control: "palette-sw",
                  color: we
                }), Ie.addEventListener("click", r.onPaletteSampleClick, !1), Ie.appendChild(Ve), E.pal.appendChild(Ie);
              }
            function Ze() {
              var He = a.controlBorderColor.split(/\s+/), Ke = He.length < 2 ? He[0] : He[1] + " " + He[0] + " " + He[0] + " " + He[1];
              E.btn.style.borderColor = Ke;
            }
            var et = 15;
            E.btn.className = "jscolor-btn jscolor-btn-close", E.btn.style.display = a.closeButton ? "block" : "none", E.btn.style.left = a.padding + "px", E.btn.style.bottom = a.padding + "px", E.btn.style.padding = "0 " + et + "px", E.btn.style.maxWidth = Y.contentW - 2 * a.controlBorderWidth - 2 * et + "px", E.btn.style.height = a.buttonHeight + "px", E.btn.style.border = a.controlBorderWidth + "px solid", Ze(), E.btn.style.color = a.buttonColor, E.btn.onmousedown = function() {
              a.hide();
            }, E.btnT.style.display = "inline", E.btnT.style.lineHeight = a.buttonHeight + "px", E.btnT.innerText = a.closeText, g(), b(), w(), r.picker.owner && r.picker.owner !== a && r.removeClass(r.picker.owner.targetElement, r.pub.activeClassName), r.picker.owner = a, r.redrawPosition(), E.wrap.parentNode !== a.container && a.container.appendChild(E.wrap), r.addClass(a.targetElement, r.pub.activeClassName);
          }
          function g() {
            var E = r.getPadYChannel(a), M = Math.round(a.channels.h / 360 * (a.width - 1)), W = Math.round((1 - a.channels[E] / 100) * (a.height - 1)), Y = 2 * a.pointerBorderWidth + a.pointerThickness + 2 * a.crossSize, J = -Math.floor(Y / 2);
            switch (r.picker.cross.style.left = M + J + "px", r.picker.cross.style.top = W + J + "px", r.getSliderChannel(a)) {
              case "s":
                var X = r.HSV_RGB(a.channels.h, 100, a.channels.v), Q = r.HSV_RGB(a.channels.h, 0, a.channels.v), ie = "rgb(" + Math.round(X[0]) + "," + Math.round(X[1]) + "," + Math.round(X[2]) + ")", oe = "rgb(" + Math.round(Q[0]) + "," + Math.round(Q[1]) + "," + Math.round(Q[2]) + ")";
                r.picker.sldGrad.draw(a.sliderSize, a.height, ie, oe);
                break;
              case "v":
                var ee = r.HSV_RGB(a.channels.h, a.channels.s, 100), ie = "rgb(" + Math.round(ee[0]) + "," + Math.round(ee[1]) + "," + Math.round(ee[2]) + ")", oe = "#000";
                r.picker.sldGrad.draw(a.sliderSize, a.height, ie, oe);
                break;
            }
            r.picker.asldGrad.draw(a.sliderSize, a.height, a.toHEXString());
          }
          function b() {
            var E = r.getSliderChannel(a);
            if (E) {
              var M = Math.round((1 - a.channels[E] / 100) * (a.height - 1));
              r.picker.sldPtrOB.style.top = M - (2 * a.pointerBorderWidth + a.pointerThickness) - Math.floor(r.pub.sliderInnerSpace / 2) + "px";
            }
            r.picker.asldGrad.draw(a.sliderSize, a.height, a.toHEXString());
          }
          function w() {
            var E = Math.round((1 - a.channels.a) * (a.height - 1));
            r.picker.asldPtrOB.style.top = E - (2 * a.pointerBorderWidth + a.pointerThickness) - Math.floor(r.pub.sliderInnerSpace / 2) + "px";
          }
          function C() {
            return r.picker && r.picker.owner === a;
          }
          function m(E) {
            r.eventKey(E) === "Enter" && (a.valueElement && a.processValueInput(a.valueElement.value), a.tryHide());
          }
          function h(E) {
            r.eventKey(E) === "Enter" && (a.alphaElement && a.processAlphaInput(a.alphaElement.value), a.tryHide());
          }
          function u(E) {
            if (!r.getData(E, "internal")) {
              var M = a.valueElement.value;
              a.processValueInput(a.valueElement.value), r.triggerCallback(a, "onChange"), a.valueElement.value !== M && r.triggerInputEvent(a.valueElement, "change", !0, !0);
            }
          }
          function p(E) {
            if (!r.getData(E, "internal")) {
              var M = a.alphaElement.value;
              a.processAlphaInput(a.alphaElement.value), r.triggerCallback(a, "onChange"), r.triggerInputEvent(a.valueElement, "change", !0, !0), a.alphaElement.value !== M && r.triggerInputEvent(a.alphaElement, "change", !0, !0);
            }
          }
          function v(E) {
            r.getData(E, "internal") || (a.valueElement && a.fromString(a.valueElement.value, r.flags.leaveValue), r.triggerCallback(a, "onInput"));
          }
          function S(E) {
            r.getData(E, "internal") || (a.alphaElement && a.fromHSVA(null, null, null, parseFloat(a.alphaElement.value), r.flags.leaveAlpha), r.triggerCallback(a, "onInput"), r.triggerInputEvent(a.valueElement, "input", !0, !0));
          }
          if (r.pub.options) {
            for (var T in r.pub.options)
              if (r.pub.options.hasOwnProperty(T))
                try {
                  o(T, r.pub.options[T]);
                } catch (E) {
                  console.warn(E);
                }
          }
          var A = [];
          s.preset && (typeof s.preset == "string" ? A = s.preset.split(/\s+/) : Array.isArray(s.preset) ? A = s.preset.slice() : console.warn("Unrecognized preset value")), A.indexOf("default") === -1 && A.push("default");
          for (var y = A.length - 1; y >= 0; y -= 1) {
            var I = A[y];
            if (I) {
              if (!r.pub.presets.hasOwnProperty(I)) {
                console.warn("Unknown preset: %s", I);
                continue;
              }
              for (var T in r.pub.presets[I])
                if (r.pub.presets[I].hasOwnProperty(T))
                  try {
                    o(T, r.pub.presets[I][T]);
                  } catch (M) {
                    console.warn(M);
                  }
            }
          }
          var $ = [
            // these options won't be set as instance properties
            "preset"
          ];
          for (var T in s)
            if (s.hasOwnProperty(T) && $.indexOf(T) === -1)
              try {
                o(T, s[T]);
              } catch (M) {
                console.warn(M);
              }
          if (this.container === void 0 ? this.container = n.document.body : this.container = r.node(this.container), !this.container)
            throw new Error("Cannot instantiate color picker without a container element");
          if (this.targetElement = r.node(i), !this.targetElement) {
            if (typeof i == "string" && /^[a-zA-Z][\w:.-]*$/.test(i)) {
              var V = i;
              throw new Error("If '" + V + "' is supposed to be an ID, please use '#" + V + "' or any valid CSS selector.");
            }
            throw new Error("Cannot instantiate color picker without a target element");
          }
          if (this.targetElement.jscolor && this.targetElement.jscolor instanceof r.pub)
            throw new Error("Color picker already installed on this element");
          if (this.targetElement.jscolor = this, r.addClass(this.targetElement, r.pub.className), r.instances.push(this), r.isButton(this.targetElement) && (this.targetElement.type.toLowerCase() !== "button" && (this.targetElement.type = "button"), r.isButtonEmpty(this.targetElement))) {
            r.removeChildren(this.targetElement), this.targetElement.appendChild(n.document.createTextNode(" "));
            var z = r.getCompStyle(this.targetElement), F = parseFloat(z["min-width"]) || 0;
            F < this.previewSize && r.setStyle(this.targetElement, {
              "min-width": this.previewSize + "px"
            }, this.forceStyle);
          }
          if (this.valueElement === void 0 ? r.isTextInput(this.targetElement) && (this.valueElement = this.targetElement) : this.valueElement === null || (this.valueElement = r.node(this.valueElement)), this.alphaElement && (this.alphaElement = r.node(this.alphaElement)), this.previewElement === void 0 ? this.previewElement = this.targetElement : this.previewElement === null || (this.previewElement = r.node(this.previewElement)), this.valueElement && r.isTextInput(this.valueElement)) {
            var P = {
              onInput: this.valueElement.oninput
            };
            this.valueElement.oninput = null, this.valueElement.addEventListener("keydown", m, !1), this.valueElement.addEventListener("change", u, !1), this.valueElement.addEventListener("input", v, !1), P.onInput && this.valueElement.addEventListener("input", P.onInput, !1), this.valueElement.setAttribute("autocomplete", "off"), this.valueElement.setAttribute("autocorrect", "off"), this.valueElement.setAttribute("autocapitalize", "off"), this.valueElement.setAttribute("spellcheck", !1);
          }
          this.alphaElement && r.isTextInput(this.alphaElement) && (this.alphaElement.addEventListener("keydown", h, !1), this.alphaElement.addEventListener("change", p, !1), this.alphaElement.addEventListener("input", S, !1), this.alphaElement.setAttribute("autocomplete", "off"), this.alphaElement.setAttribute("autocorrect", "off"), this.alphaElement.setAttribute("autocapitalize", "off"), this.alphaElement.setAttribute("spellcheck", !1));
          var k = "FFFFFF";
          this.value !== void 0 ? k = this.value : this.valueElement && this.valueElement.value !== void 0 && (k = this.valueElement.value);
          var R = void 0;
          if (this.alpha !== void 0 ? R = "" + this.alpha : this.alphaElement && this.alphaElement.value !== void 0 && (R = this.alphaElement.value), this._currentFormat = null, ["auto", "any"].indexOf(this.format.toLowerCase()) > -1) {
            var L = r.parseColorString(k);
            this._currentFormat = L ? L.format : "hex";
          } else
            this._currentFormat = this.format.toLowerCase();
          this.processValueInput(k), R !== void 0 && this.processAlphaInput(R), this.random && this.randomize.apply(this, Array.isArray(this.random) ? this.random : []);
        }
      };
      return r.pub.className = "jscolor", r.pub.activeClassName = "jscolor-active", r.pub.looseJSON = !0, r.pub.presets = {}, r.pub.presets.default = {}, r.pub.presets.light = {
        // default color scheme
        backgroundColor: "rgba(255,255,255,1)",
        controlBorderColor: "rgba(187,187,187,1)",
        buttonColor: "rgba(0,0,0,1)"
      }, r.pub.presets.dark = {
        backgroundColor: "rgba(51,51,51,1)",
        controlBorderColor: "rgba(153,153,153,1)",
        buttonColor: "rgba(240,240,240,1)"
      }, r.pub.presets.small = { width: 101, height: 101, padding: 10, sliderSize: 14, paletteCols: 8 }, r.pub.presets.medium = { width: 181, height: 101, padding: 12, sliderSize: 16, paletteCols: 10 }, r.pub.presets.large = { width: 271, height: 151, padding: 12, sliderSize: 24, paletteCols: 15 }, r.pub.presets.thin = { borderWidth: 1, controlBorderWidth: 1, pointerBorderWidth: 1 }, r.pub.presets.thick = { borderWidth: 2, controlBorderWidth: 2, pointerBorderWidth: 2 }, r.pub.sliderInnerSpace = 3, r.pub.chessboardSize = 8, r.pub.chessboardColor1 = "#666666", r.pub.chessboardColor2 = "#999999", r.pub.previewSeparator = ["rgba(255,255,255,.65)", "rgba(128,128,128,.65)"], r.pub.init = function() {
        if (!r.initialized)
          for (n.document.addEventListener("mousedown", r.onDocumentMouseDown, !1), n.document.addEventListener("keyup", r.onDocumentKeyUp, !1), n.addEventListener("resize", r.onWindowResize, !1), n.addEventListener("scroll", r.onWindowScroll, !1), r.appendDefaultCss(), r.pub.install(), r.initialized = !0; r.readyQueue.length; ) {
            var i = r.readyQueue.shift();
            i();
          }
      }, r.pub.install = function(i) {
        var s = !0;
        try {
          r.installBySelector("[data-jscolor]", i);
        } catch (a) {
          s = !1, console.warn(a);
        }
        if (r.pub.lookupClass)
          try {
            r.installBySelector(
              "input." + r.pub.lookupClass + ", button." + r.pub.lookupClass,
              i
            );
          } catch {
          }
        return s;
      }, r.pub.ready = function(i) {
        return typeof i != "function" ? (console.warn("Passed value is not a function"), !1) : (r.initialized ? i() : r.readyQueue.push(i), !0);
      }, r.pub.trigger = function(i) {
        var s = function() {
          r.triggerGlobal(i);
        };
        r.initialized ? s() : r.pub.ready(s);
      }, r.pub.hide = function() {
        r.picker && r.picker.owner && r.picker.owner.hide();
      }, r.pub.chessboard = function(i) {
        i || (i = "rgba(0,0,0,0)");
        var s = r.genColorPreviewCanvas(i);
        return s.canvas.toDataURL();
      }, r.pub.background = function(i) {
        var s = [];
        s.push(r.genColorPreviewGradient(i));
        var a = r.genColorPreviewCanvas();
        return s.push([
          "url('" + a.canvas.toDataURL() + "')",
          "left top",
          "repeat"
        ].join(" ")), s.join(", ");
      }, r.pub.options = {}, r.pub.lookupClass = "jscolor", r.pub.installByClassName = function() {
        return console.error('jscolor.installByClassName() is DEPRECATED. Use data-jscolor="" attribute instead of a class name.' + r.docsRef), !1;
      }, r.register(), r.pub;
    }();
    return typeof n.jscolor > "u" && (n.jscolor = n.JSColor = e), e;
  });
})(Kh);
var Jw = Kh.exports;
const hl = /* @__PURE__ */ wn(Jw);
window.jQuery = lt;
window.$ = lt;
window.bootstrap = {
  Alert: Ds,
  Button: Es,
  Carousel: Hi,
  Collapse: Fr,
  Dropdown: An,
  Modal: Br,
  Offcanvas: Yn,
  Popover: ro,
  ScrollSpy: ks,
  Tab: Hr,
  Toast: Ps,
  Tooltip: pr
};
document.addEventListener("DOMContentLoaded", () => {
  var t;
  ew.init(), document.querySelectorAll('[data-bs-toggle="collapse"]').forEach((n) => {
    Fr.getOrCreateInstance(n, { toggle: !1 });
  }), document.querySelectorAll('[data-bs-toggle="tooltip"]').forEach((n) => {
    pr.getOrCreateInstance(n);
  }), (t = hl.install) == null || t.call(hl);
});
export {
  Ds as Alert,
  Zw as Bloodhound,
  Es as Button,
  Hi as Carousel,
  r_ as ClipboardJS,
  Fr as Collapse,
  H as DataTable,
  An as Dropdown,
  Br as Modal,
  Yn as Offcanvas,
  ro as Popover,
  ks as ScrollSpy,
  Pe as Sortable,
  Hr as Tab,
  Ps as Toast,
  pr as Tooltip,
  e_ as animate,
  Yr as dayjs,
  ew as dropdownHover,
  vt as flatpickr,
  hl as jscolor,
  t_ as onInsert,
  n_ as toast
};
//# sourceMappingURL=bs-ee.mjs.map
