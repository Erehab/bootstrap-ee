import 'parsleyjs';

// Parsley attaches itself to jQuery as $.fn.parsley and $.fn.validate.
// Importing this module makes Parsley available globally when jQuery is present.
// Usage: $('form').parsley()
export {};
